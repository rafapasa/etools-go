// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testManifestoEletronicoUfs(t *testing.T) {
	t.Parallel()

	query := ManifestoEletronicoUfs()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testManifestoEletronicoUfsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testManifestoEletronicoUfsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := ManifestoEletronicoUfs().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testManifestoEletronicoUfsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ManifestoEletronicoUfSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testManifestoEletronicoUfsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := ManifestoEletronicoUfExists(ctx, tx, o.MdfeID, o.EstID, o.MdfeufItem)
	if err != nil {
		t.Errorf("Unable to check if ManifestoEletronicoUf exists: %s", err)
	}
	if !e {
		t.Errorf("Expected ManifestoEletronicoUfExists to return true, but got false.")
	}
}

func testManifestoEletronicoUfsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	manifestoEletronicoUfFound, err := FindManifestoEletronicoUf(ctx, tx, o.MdfeID, o.EstID, o.MdfeufItem)
	if err != nil {
		t.Error(err)
	}

	if manifestoEletronicoUfFound == nil {
		t.Error("want a record, got nil")
	}
}

func testManifestoEletronicoUfsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = ManifestoEletronicoUfs().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testManifestoEletronicoUfsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := ManifestoEletronicoUfs().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testManifestoEletronicoUfsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	manifestoEletronicoUfOne := &ManifestoEletronicoUf{}
	manifestoEletronicoUfTwo := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, manifestoEletronicoUfOne, manifestoEletronicoUfDBTypes, false, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}
	if err = randomize.Struct(seed, manifestoEletronicoUfTwo, manifestoEletronicoUfDBTypes, false, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = manifestoEletronicoUfOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = manifestoEletronicoUfTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ManifestoEletronicoUfs().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testManifestoEletronicoUfsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	manifestoEletronicoUfOne := &ManifestoEletronicoUf{}
	manifestoEletronicoUfTwo := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, manifestoEletronicoUfOne, manifestoEletronicoUfDBTypes, false, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}
	if err = randomize.Struct(seed, manifestoEletronicoUfTwo, manifestoEletronicoUfDBTypes, false, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = manifestoEletronicoUfOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = manifestoEletronicoUfTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func manifestoEletronicoUfBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func manifestoEletronicoUfAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func manifestoEletronicoUfAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func manifestoEletronicoUfBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func manifestoEletronicoUfAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func manifestoEletronicoUfBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func manifestoEletronicoUfAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func manifestoEletronicoUfBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func manifestoEletronicoUfAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronicoUf) error {
	*o = ManifestoEletronicoUf{}
	return nil
}

func testManifestoEletronicoUfsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &ManifestoEletronicoUf{}
	o := &ManifestoEletronicoUf{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, false); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf object: %s", err)
	}

	AddManifestoEletronicoUfHook(boil.BeforeInsertHook, manifestoEletronicoUfBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfBeforeInsertHooks = []ManifestoEletronicoUfHook{}

	AddManifestoEletronicoUfHook(boil.AfterInsertHook, manifestoEletronicoUfAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfAfterInsertHooks = []ManifestoEletronicoUfHook{}

	AddManifestoEletronicoUfHook(boil.AfterSelectHook, manifestoEletronicoUfAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfAfterSelectHooks = []ManifestoEletronicoUfHook{}

	AddManifestoEletronicoUfHook(boil.BeforeUpdateHook, manifestoEletronicoUfBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfBeforeUpdateHooks = []ManifestoEletronicoUfHook{}

	AddManifestoEletronicoUfHook(boil.AfterUpdateHook, manifestoEletronicoUfAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfAfterUpdateHooks = []ManifestoEletronicoUfHook{}

	AddManifestoEletronicoUfHook(boil.BeforeDeleteHook, manifestoEletronicoUfBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfBeforeDeleteHooks = []ManifestoEletronicoUfHook{}

	AddManifestoEletronicoUfHook(boil.AfterDeleteHook, manifestoEletronicoUfAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfAfterDeleteHooks = []ManifestoEletronicoUfHook{}

	AddManifestoEletronicoUfHook(boil.BeforeUpsertHook, manifestoEletronicoUfBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfBeforeUpsertHooks = []ManifestoEletronicoUfHook{}

	AddManifestoEletronicoUfHook(boil.AfterUpsertHook, manifestoEletronicoUfAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	manifestoEletronicoUfAfterUpsertHooks = []ManifestoEletronicoUfHook{}
}

func testManifestoEletronicoUfsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testManifestoEletronicoUfsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(manifestoEletronicoUfColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testManifestoEletronicoUfToOneEstadoUsingEst(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local ManifestoEletronicoUf
	var foreign Estado

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, manifestoEletronicoUfDBTypes, false, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, estadoDBTypes, false, estadoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Estado struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.EstID = foreign.EstID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Est().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.EstID != foreign.EstID {
		t.Errorf("want: %v, got %v", foreign.EstID, check.EstID)
	}

	ranAfterSelectHook := false
	AddEstadoHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Estado) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := ManifestoEletronicoUfSlice{&local}
	if err = local.L.LoadEst(ctx, tx, false, (*[]*ManifestoEletronicoUf)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Est == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Est = nil
	if err = local.L.LoadEst(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Est == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testManifestoEletronicoUfToOneManifestoEletronicoUsingMdfe(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local ManifestoEletronicoUf
	var foreign ManifestoEletronico

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, manifestoEletronicoUfDBTypes, false, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, manifestoEletronicoDBTypes, false, manifestoEletronicoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronico struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.MdfeID = foreign.MdfeID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Mdfe().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.MdfeID != foreign.MdfeID {
		t.Errorf("want: %v, got %v", foreign.MdfeID, check.MdfeID)
	}

	ranAfterSelectHook := false
	AddManifestoEletronicoHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *ManifestoEletronico) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := ManifestoEletronicoUfSlice{&local}
	if err = local.L.LoadMdfe(ctx, tx, false, (*[]*ManifestoEletronicoUf)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Mdfe == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Mdfe = nil
	if err = local.L.LoadMdfe(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Mdfe == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testManifestoEletronicoUfToOneSetOpEstadoUsingEst(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ManifestoEletronicoUf
	var b, c Estado

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, manifestoEletronicoUfDBTypes, false, strmangle.SetComplement(manifestoEletronicoUfPrimaryKeyColumns, manifestoEletronicoUfColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, estadoDBTypes, false, strmangle.SetComplement(estadoPrimaryKeyColumns, estadoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, estadoDBTypes, false, strmangle.SetComplement(estadoPrimaryKeyColumns, estadoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Estado{&b, &c} {
		err = a.SetEst(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Est != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.EstManifestoEletronicoUfs[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.EstID != x.EstID {
			t.Error("foreign key was wrong value", a.EstID)
		}

		if exists, err := ManifestoEletronicoUfExists(ctx, tx, a.MdfeID, a.EstID, a.MdfeufItem); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}
func testManifestoEletronicoUfToOneSetOpManifestoEletronicoUsingMdfe(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ManifestoEletronicoUf
	var b, c ManifestoEletronico

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, manifestoEletronicoUfDBTypes, false, strmangle.SetComplement(manifestoEletronicoUfPrimaryKeyColumns, manifestoEletronicoUfColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, manifestoEletronicoDBTypes, false, strmangle.SetComplement(manifestoEletronicoPrimaryKeyColumns, manifestoEletronicoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, manifestoEletronicoDBTypes, false, strmangle.SetComplement(manifestoEletronicoPrimaryKeyColumns, manifestoEletronicoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*ManifestoEletronico{&b, &c} {
		err = a.SetMdfe(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Mdfe != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.MdfeManifestoEletronicoUfs[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.MdfeID != x.MdfeID {
			t.Error("foreign key was wrong value", a.MdfeID)
		}

		if exists, err := ManifestoEletronicoUfExists(ctx, tx, a.MdfeID, a.EstID, a.MdfeufItem); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}

func testManifestoEletronicoUfsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testManifestoEletronicoUfsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ManifestoEletronicoUfSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testManifestoEletronicoUfsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ManifestoEletronicoUfs().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	manifestoEletronicoUfDBTypes = map[string]string{`MdfeID`: `int`, `EstID`: `int`, `MdfeufItem`: `smallint`}
	_                            = bytes.MinRead
)

func testManifestoEletronicoUfsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(manifestoEletronicoUfPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(manifestoEletronicoUfAllColumns) == len(manifestoEletronicoUfPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testManifestoEletronicoUfsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(manifestoEletronicoUfAllColumns) == len(manifestoEletronicoUfPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, manifestoEletronicoUfDBTypes, true, manifestoEletronicoUfPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(manifestoEletronicoUfAllColumns, manifestoEletronicoUfPrimaryKeyColumns) {
		fields = manifestoEletronicoUfAllColumns
	} else {
		fields = strmangle.SetComplement(
			manifestoEletronicoUfAllColumns,
			manifestoEletronicoUfPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := ManifestoEletronicoUfSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testManifestoEletronicoUfsUpsert(t *testing.T) {
	t.Parallel()

	if len(manifestoEletronicoUfAllColumns) == len(manifestoEletronicoUfPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLManifestoEletronicoUfUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := ManifestoEletronicoUf{}
	if err = randomize.Struct(seed, &o, manifestoEletronicoUfDBTypes, false); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ManifestoEletronicoUf: %s", err)
	}

	count, err := ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, manifestoEletronicoUfDBTypes, false, manifestoEletronicoUfPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ManifestoEletronicoUf struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ManifestoEletronicoUf: %s", err)
	}

	count, err = ManifestoEletronicoUfs().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
