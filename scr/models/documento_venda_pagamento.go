// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// DocumentoVendaPagamento is an object representing the database table.
type DocumentoVendaPagamento struct {
	DDVID             int           `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	DVPItem           int           `boil:"dvp_item" json:"dvp_item" toml:"dvp_item" yaml:"dvp_item"`
	PorID             int           `boil:"por_id" json:"por_id" toml:"por_id" yaml:"por_id"`
	TdocID            int           `boil:"tdoc_id" json:"tdoc_id" toml:"tdoc_id" yaml:"tdoc_id"`
	RocID             null.Int      `boil:"roc_id" json:"roc_id,omitempty" toml:"roc_id" yaml:"roc_id,omitempty"`
	DVPDatavencimento time.Time     `boil:"dvp_datavencimento" json:"dvp_datavencimento" toml:"dvp_datavencimento" yaml:"dvp_datavencimento"`
	DVPValor          types.Decimal `boil:"dvp_valor" json:"dvp_valor" toml:"dvp_valor" yaml:"dvp_valor"`
	FrmpgtoID         null.Int      `boil:"frmpgto_id" json:"frmpgto_id,omitempty" toml:"frmpgto_id" yaml:"frmpgto_id,omitempty"`
	DVPDocumento      null.String   `boil:"dvp_documento" json:"dvp_documento,omitempty" toml:"dvp_documento" yaml:"dvp_documento,omitempty"`
	DVPCodautorizacao null.String   `boil:"dvp_codautorizacao" json:"dvp_codautorizacao,omitempty" toml:"dvp_codautorizacao" yaml:"dvp_codautorizacao,omitempty"`
	DVPCoo            null.Int      `boil:"dvp_coo" json:"dvp_coo,omitempty" toml:"dvp_coo" yaml:"dvp_coo,omitempty"`

	R *documentoVendaPagamentoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L documentoVendaPagamentoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DocumentoVendaPagamentoColumns = struct {
	DDVID             string
	DVPItem           string
	PorID             string
	TdocID            string
	RocID             string
	DVPDatavencimento string
	DVPValor          string
	FrmpgtoID         string
	DVPDocumento      string
	DVPCodautorizacao string
	DVPCoo            string
}{
	DDVID:             "ddv_id",
	DVPItem:           "dvp_item",
	PorID:             "por_id",
	TdocID:            "tdoc_id",
	RocID:             "roc_id",
	DVPDatavencimento: "dvp_datavencimento",
	DVPValor:          "dvp_valor",
	FrmpgtoID:         "frmpgto_id",
	DVPDocumento:      "dvp_documento",
	DVPCodautorizacao: "dvp_codautorizacao",
	DVPCoo:            "dvp_coo",
}

var DocumentoVendaPagamentoTableColumns = struct {
	DDVID             string
	DVPItem           string
	PorID             string
	TdocID            string
	RocID             string
	DVPDatavencimento string
	DVPValor          string
	FrmpgtoID         string
	DVPDocumento      string
	DVPCodautorizacao string
	DVPCoo            string
}{
	DDVID:             "documento_venda_pagamento.ddv_id",
	DVPItem:           "documento_venda_pagamento.dvp_item",
	PorID:             "documento_venda_pagamento.por_id",
	TdocID:            "documento_venda_pagamento.tdoc_id",
	RocID:             "documento_venda_pagamento.roc_id",
	DVPDatavencimento: "documento_venda_pagamento.dvp_datavencimento",
	DVPValor:          "documento_venda_pagamento.dvp_valor",
	FrmpgtoID:         "documento_venda_pagamento.frmpgto_id",
	DVPDocumento:      "documento_venda_pagamento.dvp_documento",
	DVPCodautorizacao: "documento_venda_pagamento.dvp_codautorizacao",
	DVPCoo:            "documento_venda_pagamento.dvp_coo",
}

// Generated where

var DocumentoVendaPagamentoWhere = struct {
	DDVID             whereHelperint
	DVPItem           whereHelperint
	PorID             whereHelperint
	TdocID            whereHelperint
	RocID             whereHelpernull_Int
	DVPDatavencimento whereHelpertime_Time
	DVPValor          whereHelpertypes_Decimal
	FrmpgtoID         whereHelpernull_Int
	DVPDocumento      whereHelpernull_String
	DVPCodautorizacao whereHelpernull_String
	DVPCoo            whereHelpernull_Int
}{
	DDVID:             whereHelperint{field: "`documento_venda_pagamento`.`ddv_id`"},
	DVPItem:           whereHelperint{field: "`documento_venda_pagamento`.`dvp_item`"},
	PorID:             whereHelperint{field: "`documento_venda_pagamento`.`por_id`"},
	TdocID:            whereHelperint{field: "`documento_venda_pagamento`.`tdoc_id`"},
	RocID:             whereHelpernull_Int{field: "`documento_venda_pagamento`.`roc_id`"},
	DVPDatavencimento: whereHelpertime_Time{field: "`documento_venda_pagamento`.`dvp_datavencimento`"},
	DVPValor:          whereHelpertypes_Decimal{field: "`documento_venda_pagamento`.`dvp_valor`"},
	FrmpgtoID:         whereHelpernull_Int{field: "`documento_venda_pagamento`.`frmpgto_id`"},
	DVPDocumento:      whereHelpernull_String{field: "`documento_venda_pagamento`.`dvp_documento`"},
	DVPCodautorizacao: whereHelpernull_String{field: "`documento_venda_pagamento`.`dvp_codautorizacao`"},
	DVPCoo:            whereHelpernull_Int{field: "`documento_venda_pagamento`.`dvp_coo`"},
}

// DocumentoVendaPagamentoRels is where relationship names are stored.
var DocumentoVendaPagamentoRels = struct {
	DDV     string
	Por     string
	Roc     string
	Tdoc    string
	Frmpgto string
}{
	DDV:     "DDV",
	Por:     "Por",
	Roc:     "Roc",
	Tdoc:    "Tdoc",
	Frmpgto: "Frmpgto",
}

// documentoVendaPagamentoR is where relationships are stored.
type documentoVendaPagamentoR struct {
	DDV     *DocumentoVenda `boil:"DDV" json:"DDV" toml:"DDV" yaml:"DDV"`
	Por     *Portador       `boil:"Por" json:"Por" toml:"Por" yaml:"Por"`
	Roc     *RotinaContabil `boil:"Roc" json:"Roc" toml:"Roc" yaml:"Roc"`
	Tdoc    *TipoDocumento  `boil:"Tdoc" json:"Tdoc" toml:"Tdoc" yaml:"Tdoc"`
	Frmpgto *FormaPagamento `boil:"Frmpgto" json:"Frmpgto" toml:"Frmpgto" yaml:"Frmpgto"`
}

// NewStruct creates a new relationship struct
func (*documentoVendaPagamentoR) NewStruct() *documentoVendaPagamentoR {
	return &documentoVendaPagamentoR{}
}

func (r *documentoVendaPagamentoR) GetDDV() *DocumentoVenda {
	if r == nil {
		return nil
	}
	return r.DDV
}

func (r *documentoVendaPagamentoR) GetPor() *Portador {
	if r == nil {
		return nil
	}
	return r.Por
}

func (r *documentoVendaPagamentoR) GetRoc() *RotinaContabil {
	if r == nil {
		return nil
	}
	return r.Roc
}

func (r *documentoVendaPagamentoR) GetTdoc() *TipoDocumento {
	if r == nil {
		return nil
	}
	return r.Tdoc
}

func (r *documentoVendaPagamentoR) GetFrmpgto() *FormaPagamento {
	if r == nil {
		return nil
	}
	return r.Frmpgto
}

// documentoVendaPagamentoL is where Load methods for each relationship are stored.
type documentoVendaPagamentoL struct{}

var (
	documentoVendaPagamentoAllColumns            = []string{"ddv_id", "dvp_item", "por_id", "tdoc_id", "roc_id", "dvp_datavencimento", "dvp_valor", "frmpgto_id", "dvp_documento", "dvp_codautorizacao", "dvp_coo"}
	documentoVendaPagamentoColumnsWithoutDefault = []string{"ddv_id", "dvp_item", "por_id", "tdoc_id", "roc_id", "dvp_datavencimento", "dvp_valor", "frmpgto_id", "dvp_documento", "dvp_codautorizacao", "dvp_coo"}
	documentoVendaPagamentoColumnsWithDefault    = []string{}
	documentoVendaPagamentoPrimaryKeyColumns     = []string{"ddv_id", "dvp_item"}
	documentoVendaPagamentoGeneratedColumns      = []string{}
)

type (
	// DocumentoVendaPagamentoSlice is an alias for a slice of pointers to DocumentoVendaPagamento.
	// This should almost always be used instead of []DocumentoVendaPagamento.
	DocumentoVendaPagamentoSlice []*DocumentoVendaPagamento
	// DocumentoVendaPagamentoHook is the signature for custom DocumentoVendaPagamento hook methods
	DocumentoVendaPagamentoHook func(context.Context, boil.ContextExecutor, *DocumentoVendaPagamento) error

	documentoVendaPagamentoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	documentoVendaPagamentoType                 = reflect.TypeOf(&DocumentoVendaPagamento{})
	documentoVendaPagamentoMapping              = queries.MakeStructMapping(documentoVendaPagamentoType)
	documentoVendaPagamentoPrimaryKeyMapping, _ = queries.BindMapping(documentoVendaPagamentoType, documentoVendaPagamentoMapping, documentoVendaPagamentoPrimaryKeyColumns)
	documentoVendaPagamentoInsertCacheMut       sync.RWMutex
	documentoVendaPagamentoInsertCache          = make(map[string]insertCache)
	documentoVendaPagamentoUpdateCacheMut       sync.RWMutex
	documentoVendaPagamentoUpdateCache          = make(map[string]updateCache)
	documentoVendaPagamentoUpsertCacheMut       sync.RWMutex
	documentoVendaPagamentoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var documentoVendaPagamentoAfterSelectMu sync.Mutex
var documentoVendaPagamentoAfterSelectHooks []DocumentoVendaPagamentoHook

var documentoVendaPagamentoBeforeInsertMu sync.Mutex
var documentoVendaPagamentoBeforeInsertHooks []DocumentoVendaPagamentoHook
var documentoVendaPagamentoAfterInsertMu sync.Mutex
var documentoVendaPagamentoAfterInsertHooks []DocumentoVendaPagamentoHook

var documentoVendaPagamentoBeforeUpdateMu sync.Mutex
var documentoVendaPagamentoBeforeUpdateHooks []DocumentoVendaPagamentoHook
var documentoVendaPagamentoAfterUpdateMu sync.Mutex
var documentoVendaPagamentoAfterUpdateHooks []DocumentoVendaPagamentoHook

var documentoVendaPagamentoBeforeDeleteMu sync.Mutex
var documentoVendaPagamentoBeforeDeleteHooks []DocumentoVendaPagamentoHook
var documentoVendaPagamentoAfterDeleteMu sync.Mutex
var documentoVendaPagamentoAfterDeleteHooks []DocumentoVendaPagamentoHook

var documentoVendaPagamentoBeforeUpsertMu sync.Mutex
var documentoVendaPagamentoBeforeUpsertHooks []DocumentoVendaPagamentoHook
var documentoVendaPagamentoAfterUpsertMu sync.Mutex
var documentoVendaPagamentoAfterUpsertHooks []DocumentoVendaPagamentoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DocumentoVendaPagamento) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DocumentoVendaPagamento) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DocumentoVendaPagamento) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DocumentoVendaPagamento) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DocumentoVendaPagamento) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DocumentoVendaPagamento) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DocumentoVendaPagamento) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DocumentoVendaPagamento) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DocumentoVendaPagamento) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDocumentoVendaPagamentoHook registers your hook function for all future operations.
func AddDocumentoVendaPagamentoHook(hookPoint boil.HookPoint, documentoVendaPagamentoHook DocumentoVendaPagamentoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		documentoVendaPagamentoAfterSelectMu.Lock()
		documentoVendaPagamentoAfterSelectHooks = append(documentoVendaPagamentoAfterSelectHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		documentoVendaPagamentoBeforeInsertMu.Lock()
		documentoVendaPagamentoBeforeInsertHooks = append(documentoVendaPagamentoBeforeInsertHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		documentoVendaPagamentoAfterInsertMu.Lock()
		documentoVendaPagamentoAfterInsertHooks = append(documentoVendaPagamentoAfterInsertHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		documentoVendaPagamentoBeforeUpdateMu.Lock()
		documentoVendaPagamentoBeforeUpdateHooks = append(documentoVendaPagamentoBeforeUpdateHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		documentoVendaPagamentoAfterUpdateMu.Lock()
		documentoVendaPagamentoAfterUpdateHooks = append(documentoVendaPagamentoAfterUpdateHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		documentoVendaPagamentoBeforeDeleteMu.Lock()
		documentoVendaPagamentoBeforeDeleteHooks = append(documentoVendaPagamentoBeforeDeleteHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		documentoVendaPagamentoAfterDeleteMu.Lock()
		documentoVendaPagamentoAfterDeleteHooks = append(documentoVendaPagamentoAfterDeleteHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		documentoVendaPagamentoBeforeUpsertMu.Lock()
		documentoVendaPagamentoBeforeUpsertHooks = append(documentoVendaPagamentoBeforeUpsertHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		documentoVendaPagamentoAfterUpsertMu.Lock()
		documentoVendaPagamentoAfterUpsertHooks = append(documentoVendaPagamentoAfterUpsertHooks, documentoVendaPagamentoHook)
		documentoVendaPagamentoAfterUpsertMu.Unlock()
	}
}

// One returns a single documentoVendaPagamento record from the query.
func (q documentoVendaPagamentoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DocumentoVendaPagamento, error) {
	o := &DocumentoVendaPagamento{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for documento_venda_pagamento")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DocumentoVendaPagamento records from the query.
func (q documentoVendaPagamentoQuery) All(ctx context.Context, exec boil.ContextExecutor) (DocumentoVendaPagamentoSlice, error) {
	var o []*DocumentoVendaPagamento

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DocumentoVendaPagamento slice")
	}

	if len(documentoVendaPagamentoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DocumentoVendaPagamento records in the query.
func (q documentoVendaPagamentoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count documento_venda_pagamento rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q documentoVendaPagamentoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if documento_venda_pagamento exists")
	}

	return count > 0, nil
}

// DDV pointed to by the foreign key.
func (o *DocumentoVendaPagamento) DDV(mods ...qm.QueryMod) documentoVendaQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ddv_id` = ?", o.DDVID),
	}

	queryMods = append(queryMods, mods...)

	return DocumentoVendas(queryMods...)
}

// Por pointed to by the foreign key.
func (o *DocumentoVendaPagamento) Por(mods ...qm.QueryMod) portadorQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`por_id` = ?", o.PorID),
	}

	queryMods = append(queryMods, mods...)

	return Portadors(queryMods...)
}

// Roc pointed to by the foreign key.
func (o *DocumentoVendaPagamento) Roc(mods ...qm.QueryMod) rotinaContabilQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`roc_id` = ?", o.RocID),
	}

	queryMods = append(queryMods, mods...)

	return RotinaContabils(queryMods...)
}

// Tdoc pointed to by the foreign key.
func (o *DocumentoVendaPagamento) Tdoc(mods ...qm.QueryMod) tipoDocumentoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`tdoc_id` = ?", o.TdocID),
	}

	queryMods = append(queryMods, mods...)

	return TipoDocumentos(queryMods...)
}

// Frmpgto pointed to by the foreign key.
func (o *DocumentoVendaPagamento) Frmpgto(mods ...qm.QueryMod) formaPagamentoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`frmpgto_id` = ?", o.FrmpgtoID),
	}

	queryMods = append(queryMods, mods...)

	return FormaPagamentos(queryMods...)
}

// LoadDDV allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaPagamentoL) LoadDDV(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaPagamento interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaPagamento
	var object *DocumentoVendaPagamento

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaPagamento.(*DocumentoVendaPagamento)
		if !ok {
			object = new(DocumentoVendaPagamento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaPagamento))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaPagamento.(*[]*DocumentoVendaPagamento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaPagamento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaPagamentoR{}
		}
		args[object.DDVID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaPagamentoR{}
			}

			args[obj.DDVID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documento_venda`),
		qm.WhereIn(`documento_venda.ddv_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DocumentoVenda")
	}

	var resultSlice []*DocumentoVenda
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DocumentoVenda")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for documento_venda")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documento_venda")
	}

	if len(documentoVendaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.DDV = foreign
		if foreign.R == nil {
			foreign.R = &documentoVendaR{}
		}
		foreign.R.DDVDocumentoVendaPagamentos = append(foreign.R.DDVDocumentoVendaPagamentos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.DDVID == foreign.DDVID {
				local.R.DDV = foreign
				if foreign.R == nil {
					foreign.R = &documentoVendaR{}
				}
				foreign.R.DDVDocumentoVendaPagamentos = append(foreign.R.DDVDocumentoVendaPagamentos, local)
				break
			}
		}
	}

	return nil
}

// LoadPor allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaPagamentoL) LoadPor(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaPagamento interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaPagamento
	var object *DocumentoVendaPagamento

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaPagamento.(*DocumentoVendaPagamento)
		if !ok {
			object = new(DocumentoVendaPagamento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaPagamento))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaPagamento.(*[]*DocumentoVendaPagamento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaPagamento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaPagamentoR{}
		}
		args[object.PorID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaPagamentoR{}
			}

			args[obj.PorID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portador`),
		qm.WhereIn(`portador.por_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Portador")
	}

	var resultSlice []*Portador
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Portador")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for portador")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portador")
	}

	if len(portadorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Por = foreign
		if foreign.R == nil {
			foreign.R = &portadorR{}
		}
		foreign.R.PorDocumentoVendaPagamentos = append(foreign.R.PorDocumentoVendaPagamentos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PorID == foreign.PorID {
				local.R.Por = foreign
				if foreign.R == nil {
					foreign.R = &portadorR{}
				}
				foreign.R.PorDocumentoVendaPagamentos = append(foreign.R.PorDocumentoVendaPagamentos, local)
				break
			}
		}
	}

	return nil
}

// LoadRoc allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaPagamentoL) LoadRoc(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaPagamento interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaPagamento
	var object *DocumentoVendaPagamento

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaPagamento.(*DocumentoVendaPagamento)
		if !ok {
			object = new(DocumentoVendaPagamento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaPagamento))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaPagamento.(*[]*DocumentoVendaPagamento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaPagamento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaPagamentoR{}
		}
		if !queries.IsNil(object.RocID) {
			args[object.RocID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaPagamentoR{}
			}

			if !queries.IsNil(obj.RocID) {
				args[obj.RocID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil`),
		qm.WhereIn(`rotina_contabil.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RotinaContabil")
	}

	var resultSlice []*RotinaContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RotinaContabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for rotina_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil")
	}

	if len(rotinaContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Roc = foreign
		if foreign.R == nil {
			foreign.R = &rotinaContabilR{}
		}
		foreign.R.RocDocumentoVendaPagamentos = append(foreign.R.RocDocumentoVendaPagamentos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.Roc = foreign
				if foreign.R == nil {
					foreign.R = &rotinaContabilR{}
				}
				foreign.R.RocDocumentoVendaPagamentos = append(foreign.R.RocDocumentoVendaPagamentos, local)
				break
			}
		}
	}

	return nil
}

// LoadTdoc allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaPagamentoL) LoadTdoc(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaPagamento interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaPagamento
	var object *DocumentoVendaPagamento

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaPagamento.(*DocumentoVendaPagamento)
		if !ok {
			object = new(DocumentoVendaPagamento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaPagamento))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaPagamento.(*[]*DocumentoVendaPagamento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaPagamento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaPagamentoR{}
		}
		args[object.TdocID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaPagamentoR{}
			}

			args[obj.TdocID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`tipo_documento`),
		qm.WhereIn(`tipo_documento.tdoc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TipoDocumento")
	}

	var resultSlice []*TipoDocumento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TipoDocumento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tipo_documento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tipo_documento")
	}

	if len(tipoDocumentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tdoc = foreign
		if foreign.R == nil {
			foreign.R = &tipoDocumentoR{}
		}
		foreign.R.TdocDocumentoVendaPagamentos = append(foreign.R.TdocDocumentoVendaPagamentos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TdocID == foreign.TdocID {
				local.R.Tdoc = foreign
				if foreign.R == nil {
					foreign.R = &tipoDocumentoR{}
				}
				foreign.R.TdocDocumentoVendaPagamentos = append(foreign.R.TdocDocumentoVendaPagamentos, local)
				break
			}
		}
	}

	return nil
}

// LoadFrmpgto allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaPagamentoL) LoadFrmpgto(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaPagamento interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaPagamento
	var object *DocumentoVendaPagamento

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaPagamento.(*DocumentoVendaPagamento)
		if !ok {
			object = new(DocumentoVendaPagamento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaPagamento))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaPagamento.(*[]*DocumentoVendaPagamento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaPagamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaPagamento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaPagamentoR{}
		}
		if !queries.IsNil(object.FrmpgtoID) {
			args[object.FrmpgtoID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaPagamentoR{}
			}

			if !queries.IsNil(obj.FrmpgtoID) {
				args[obj.FrmpgtoID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`forma_pagamento`),
		qm.WhereIn(`forma_pagamento.frmpgto_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load FormaPagamento")
	}

	var resultSlice []*FormaPagamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice FormaPagamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for forma_pagamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for forma_pagamento")
	}

	if len(formaPagamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Frmpgto = foreign
		if foreign.R == nil {
			foreign.R = &formaPagamentoR{}
		}
		foreign.R.FrmpgtoDocumentoVendaPagamentos = append(foreign.R.FrmpgtoDocumentoVendaPagamentos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.FrmpgtoID, foreign.FrmpgtoID) {
				local.R.Frmpgto = foreign
				if foreign.R == nil {
					foreign.R = &formaPagamentoR{}
				}
				foreign.R.FrmpgtoDocumentoVendaPagamentos = append(foreign.R.FrmpgtoDocumentoVendaPagamentos, local)
				break
			}
		}
	}

	return nil
}

// SetDDV of the documentoVendaPagamento to the related item.
// Sets o.R.DDV to related.
// Adds o to related.R.DDVDocumentoVendaPagamentos.
func (o *DocumentoVendaPagamento) SetDDV(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DocumentoVenda) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_pagamento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ddv_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoPrimaryKeyColumns),
	)
	values := []interface{}{related.DDVID, o.DDVID, o.DVPItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.DDVID = related.DDVID
	if o.R == nil {
		o.R = &documentoVendaPagamentoR{
			DDV: related,
		}
	} else {
		o.R.DDV = related
	}

	if related.R == nil {
		related.R = &documentoVendaR{
			DDVDocumentoVendaPagamentos: DocumentoVendaPagamentoSlice{o},
		}
	} else {
		related.R.DDVDocumentoVendaPagamentos = append(related.R.DDVDocumentoVendaPagamentos, o)
	}

	return nil
}

// SetPor of the documentoVendaPagamento to the related item.
// Sets o.R.Por to related.
// Adds o to related.R.PorDocumentoVendaPagamentos.
func (o *DocumentoVendaPagamento) SetPor(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Portador) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_pagamento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"por_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoPrimaryKeyColumns),
	)
	values := []interface{}{related.PorID, o.DDVID, o.DVPItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PorID = related.PorID
	if o.R == nil {
		o.R = &documentoVendaPagamentoR{
			Por: related,
		}
	} else {
		o.R.Por = related
	}

	if related.R == nil {
		related.R = &portadorR{
			PorDocumentoVendaPagamentos: DocumentoVendaPagamentoSlice{o},
		}
	} else {
		related.R.PorDocumentoVendaPagamentos = append(related.R.PorDocumentoVendaPagamentos, o)
	}

	return nil
}

// SetRoc of the documentoVendaPagamento to the related item.
// Sets o.R.Roc to related.
// Adds o to related.R.RocDocumentoVendaPagamentos.
func (o *DocumentoVendaPagamento) SetRoc(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RotinaContabil) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_pagamento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoPrimaryKeyColumns),
	)
	values := []interface{}{related.RocID, o.DDVID, o.DVPItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RocID, related.RocID)
	if o.R == nil {
		o.R = &documentoVendaPagamentoR{
			Roc: related,
		}
	} else {
		o.R.Roc = related
	}

	if related.R == nil {
		related.R = &rotinaContabilR{
			RocDocumentoVendaPagamentos: DocumentoVendaPagamentoSlice{o},
		}
	} else {
		related.R.RocDocumentoVendaPagamentos = append(related.R.RocDocumentoVendaPagamentos, o)
	}

	return nil
}

// RemoveRoc relationship.
// Sets o.R.Roc to nil.
// Removes o from all passed in related items' relationships struct.
func (o *DocumentoVendaPagamento) RemoveRoc(ctx context.Context, exec boil.ContextExecutor, related *RotinaContabil) error {
	var err error

	queries.SetScanner(&o.RocID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Roc = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RocDocumentoVendaPagamentos {
		if queries.Equal(o.RocID, ri.RocID) {
			continue
		}

		ln := len(related.R.RocDocumentoVendaPagamentos)
		if ln > 1 && i < ln-1 {
			related.R.RocDocumentoVendaPagamentos[i] = related.R.RocDocumentoVendaPagamentos[ln-1]
		}
		related.R.RocDocumentoVendaPagamentos = related.R.RocDocumentoVendaPagamentos[:ln-1]
		break
	}
	return nil
}

// SetTdoc of the documentoVendaPagamento to the related item.
// Sets o.R.Tdoc to related.
// Adds o to related.R.TdocDocumentoVendaPagamentos.
func (o *DocumentoVendaPagamento) SetTdoc(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TipoDocumento) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_pagamento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"tdoc_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoPrimaryKeyColumns),
	)
	values := []interface{}{related.TdocID, o.DDVID, o.DVPItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TdocID = related.TdocID
	if o.R == nil {
		o.R = &documentoVendaPagamentoR{
			Tdoc: related,
		}
	} else {
		o.R.Tdoc = related
	}

	if related.R == nil {
		related.R = &tipoDocumentoR{
			TdocDocumentoVendaPagamentos: DocumentoVendaPagamentoSlice{o},
		}
	} else {
		related.R.TdocDocumentoVendaPagamentos = append(related.R.TdocDocumentoVendaPagamentos, o)
	}

	return nil
}

// SetFrmpgto of the documentoVendaPagamento to the related item.
// Sets o.R.Frmpgto to related.
// Adds o to related.R.FrmpgtoDocumentoVendaPagamentos.
func (o *DocumentoVendaPagamento) SetFrmpgto(ctx context.Context, exec boil.ContextExecutor, insert bool, related *FormaPagamento) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_pagamento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"frmpgto_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoPrimaryKeyColumns),
	)
	values := []interface{}{related.FrmpgtoID, o.DDVID, o.DVPItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.FrmpgtoID, related.FrmpgtoID)
	if o.R == nil {
		o.R = &documentoVendaPagamentoR{
			Frmpgto: related,
		}
	} else {
		o.R.Frmpgto = related
	}

	if related.R == nil {
		related.R = &formaPagamentoR{
			FrmpgtoDocumentoVendaPagamentos: DocumentoVendaPagamentoSlice{o},
		}
	} else {
		related.R.FrmpgtoDocumentoVendaPagamentos = append(related.R.FrmpgtoDocumentoVendaPagamentos, o)
	}

	return nil
}

// RemoveFrmpgto relationship.
// Sets o.R.Frmpgto to nil.
// Removes o from all passed in related items' relationships struct.
func (o *DocumentoVendaPagamento) RemoveFrmpgto(ctx context.Context, exec boil.ContextExecutor, related *FormaPagamento) error {
	var err error

	queries.SetScanner(&o.FrmpgtoID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("frmpgto_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Frmpgto = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.FrmpgtoDocumentoVendaPagamentos {
		if queries.Equal(o.FrmpgtoID, ri.FrmpgtoID) {
			continue
		}

		ln := len(related.R.FrmpgtoDocumentoVendaPagamentos)
		if ln > 1 && i < ln-1 {
			related.R.FrmpgtoDocumentoVendaPagamentos[i] = related.R.FrmpgtoDocumentoVendaPagamentos[ln-1]
		}
		related.R.FrmpgtoDocumentoVendaPagamentos = related.R.FrmpgtoDocumentoVendaPagamentos[:ln-1]
		break
	}
	return nil
}

// DocumentoVendaPagamentos retrieves all the records using an executor.
func DocumentoVendaPagamentos(mods ...qm.QueryMod) documentoVendaPagamentoQuery {
	mods = append(mods, qm.From("`documento_venda_pagamento`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`documento_venda_pagamento`.*"})
	}

	return documentoVendaPagamentoQuery{q}
}

// FindDocumentoVendaPagamento retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDocumentoVendaPagamento(ctx context.Context, exec boil.ContextExecutor, dDVID int, dVPItem int, selectCols ...string) (*DocumentoVendaPagamento, error) {
	documentoVendaPagamentoObj := &DocumentoVendaPagamento{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `documento_venda_pagamento` where `ddv_id`=? AND `dvp_item`=?", sel,
	)

	q := queries.Raw(query, dDVID, dVPItem)

	err := q.Bind(ctx, exec, documentoVendaPagamentoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from documento_venda_pagamento")
	}

	if err = documentoVendaPagamentoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return documentoVendaPagamentoObj, err
	}

	return documentoVendaPagamentoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DocumentoVendaPagamento) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_pagamento provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaPagamentoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	documentoVendaPagamentoInsertCacheMut.RLock()
	cache, cached := documentoVendaPagamentoInsertCache[key]
	documentoVendaPagamentoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			documentoVendaPagamentoAllColumns,
			documentoVendaPagamentoColumnsWithDefault,
			documentoVendaPagamentoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaPagamentoType, documentoVendaPagamentoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(documentoVendaPagamentoType, documentoVendaPagamentoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `documento_venda_pagamento` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `documento_venda_pagamento` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `documento_venda_pagamento` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into documento_venda_pagamento")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DDVID,
		o.DVPItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_pagamento")
	}

CacheNoHooks:
	if !cached {
		documentoVendaPagamentoInsertCacheMut.Lock()
		documentoVendaPagamentoInsertCache[key] = cache
		documentoVendaPagamentoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DocumentoVendaPagamento.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DocumentoVendaPagamento) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	documentoVendaPagamentoUpdateCacheMut.RLock()
	cache, cached := documentoVendaPagamentoUpdateCache[key]
	documentoVendaPagamentoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			documentoVendaPagamentoAllColumns,
			documentoVendaPagamentoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update documento_venda_pagamento, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `documento_venda_pagamento` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(documentoVendaPagamentoType, documentoVendaPagamentoMapping, append(wl, documentoVendaPagamentoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update documento_venda_pagamento row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for documento_venda_pagamento")
	}

	if !cached {
		documentoVendaPagamentoUpdateCacheMut.Lock()
		documentoVendaPagamentoUpdateCache[key] = cache
		documentoVendaPagamentoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q documentoVendaPagamentoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for documento_venda_pagamento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for documento_venda_pagamento")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DocumentoVendaPagamentoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaPagamentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `documento_venda_pagamento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaPagamentoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in documentoVendaPagamento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all documentoVendaPagamento")
	}
	return rowsAff, nil
}

var mySQLDocumentoVendaPagamentoUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DocumentoVendaPagamento) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_pagamento provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaPagamentoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDocumentoVendaPagamentoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	documentoVendaPagamentoUpsertCacheMut.RLock()
	cache, cached := documentoVendaPagamentoUpsertCache[key]
	documentoVendaPagamentoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			documentoVendaPagamentoAllColumns,
			documentoVendaPagamentoColumnsWithDefault,
			documentoVendaPagamentoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			documentoVendaPagamentoAllColumns,
			documentoVendaPagamentoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert documento_venda_pagamento, could not build update column list")
		}

		ret := strmangle.SetComplement(documentoVendaPagamentoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`documento_venda_pagamento`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `documento_venda_pagamento` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaPagamentoType, documentoVendaPagamentoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(documentoVendaPagamentoType, documentoVendaPagamentoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for documento_venda_pagamento")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(documentoVendaPagamentoType, documentoVendaPagamentoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for documento_venda_pagamento")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_pagamento")
	}

CacheNoHooks:
	if !cached {
		documentoVendaPagamentoUpsertCacheMut.Lock()
		documentoVendaPagamentoUpsertCache[key] = cache
		documentoVendaPagamentoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DocumentoVendaPagamento record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DocumentoVendaPagamento) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DocumentoVendaPagamento provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), documentoVendaPagamentoPrimaryKeyMapping)
	sql := "DELETE FROM `documento_venda_pagamento` WHERE `ddv_id`=? AND `dvp_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from documento_venda_pagamento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for documento_venda_pagamento")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q documentoVendaPagamentoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no documentoVendaPagamentoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documento_venda_pagamento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_pagamento")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DocumentoVendaPagamentoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(documentoVendaPagamentoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaPagamentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `documento_venda_pagamento` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaPagamentoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documentoVendaPagamento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_pagamento")
	}

	if len(documentoVendaPagamentoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DocumentoVendaPagamento) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDocumentoVendaPagamento(ctx, exec, o.DDVID, o.DVPItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DocumentoVendaPagamentoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DocumentoVendaPagamentoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaPagamentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `documento_venda_pagamento`.* FROM `documento_venda_pagamento` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaPagamentoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DocumentoVendaPagamentoSlice")
	}

	*o = slice

	return nil
}

// DocumentoVendaPagamentoExists checks if the DocumentoVendaPagamento row exists.
func DocumentoVendaPagamentoExists(ctx context.Context, exec boil.ContextExecutor, dDVID int, dVPItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `documento_venda_pagamento` where `ddv_id`=? AND `dvp_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, dDVID, dVPItem)
	}
	row := exec.QueryRowContext(ctx, sql, dDVID, dVPItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if documento_venda_pagamento exists")
	}

	return exists, nil
}

// Exists checks if the DocumentoVendaPagamento row exists.
func (o *DocumentoVendaPagamento) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DocumentoVendaPagamentoExists(ctx, exec, o.DDVID, o.DVPItem)
}
