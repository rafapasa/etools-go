// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// EspecieVeiculo is an object representing the database table.
type EspecieVeiculo struct {
	EpvID        int    `boil:"epv_id" json:"epv_id" toml:"epv_id" yaml:"epv_id"`
	EpvDescricao string `boil:"epv_descricao" json:"epv_descricao" toml:"epv_descricao" yaml:"epv_descricao"`

	R *especieVeiculoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L especieVeiculoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EspecieVeiculoColumns = struct {
	EpvID        string
	EpvDescricao string
}{
	EpvID:        "epv_id",
	EpvDescricao: "epv_descricao",
}

var EspecieVeiculoTableColumns = struct {
	EpvID        string
	EpvDescricao string
}{
	EpvID:        "especie_veiculo.epv_id",
	EpvDescricao: "especie_veiculo.epv_descricao",
}

// Generated where

var EspecieVeiculoWhere = struct {
	EpvID        whereHelperint
	EpvDescricao whereHelperstring
}{
	EpvID:        whereHelperint{field: "`especie_veiculo`.`epv_id`"},
	EpvDescricao: whereHelperstring{field: "`especie_veiculo`.`epv_descricao`"},
}

// EspecieVeiculoRels is where relationship names are stored.
var EspecieVeiculoRels = struct {
	EpvNotaFiscalItemVeiculos string
}{
	EpvNotaFiscalItemVeiculos: "EpvNotaFiscalItemVeiculos",
}

// especieVeiculoR is where relationships are stored.
type especieVeiculoR struct {
	EpvNotaFiscalItemVeiculos NotaFiscalItemVeiculoSlice `boil:"EpvNotaFiscalItemVeiculos" json:"EpvNotaFiscalItemVeiculos" toml:"EpvNotaFiscalItemVeiculos" yaml:"EpvNotaFiscalItemVeiculos"`
}

// NewStruct creates a new relationship struct
func (*especieVeiculoR) NewStruct() *especieVeiculoR {
	return &especieVeiculoR{}
}

func (r *especieVeiculoR) GetEpvNotaFiscalItemVeiculos() NotaFiscalItemVeiculoSlice {
	if r == nil {
		return nil
	}
	return r.EpvNotaFiscalItemVeiculos
}

// especieVeiculoL is where Load methods for each relationship are stored.
type especieVeiculoL struct{}

var (
	especieVeiculoAllColumns            = []string{"epv_id", "epv_descricao"}
	especieVeiculoColumnsWithoutDefault = []string{"epv_id", "epv_descricao"}
	especieVeiculoColumnsWithDefault    = []string{}
	especieVeiculoPrimaryKeyColumns     = []string{"epv_id"}
	especieVeiculoGeneratedColumns      = []string{}
)

type (
	// EspecieVeiculoSlice is an alias for a slice of pointers to EspecieVeiculo.
	// This should almost always be used instead of []EspecieVeiculo.
	EspecieVeiculoSlice []*EspecieVeiculo
	// EspecieVeiculoHook is the signature for custom EspecieVeiculo hook methods
	EspecieVeiculoHook func(context.Context, boil.ContextExecutor, *EspecieVeiculo) error

	especieVeiculoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	especieVeiculoType                 = reflect.TypeOf(&EspecieVeiculo{})
	especieVeiculoMapping              = queries.MakeStructMapping(especieVeiculoType)
	especieVeiculoPrimaryKeyMapping, _ = queries.BindMapping(especieVeiculoType, especieVeiculoMapping, especieVeiculoPrimaryKeyColumns)
	especieVeiculoInsertCacheMut       sync.RWMutex
	especieVeiculoInsertCache          = make(map[string]insertCache)
	especieVeiculoUpdateCacheMut       sync.RWMutex
	especieVeiculoUpdateCache          = make(map[string]updateCache)
	especieVeiculoUpsertCacheMut       sync.RWMutex
	especieVeiculoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var especieVeiculoAfterSelectMu sync.Mutex
var especieVeiculoAfterSelectHooks []EspecieVeiculoHook

var especieVeiculoBeforeInsertMu sync.Mutex
var especieVeiculoBeforeInsertHooks []EspecieVeiculoHook
var especieVeiculoAfterInsertMu sync.Mutex
var especieVeiculoAfterInsertHooks []EspecieVeiculoHook

var especieVeiculoBeforeUpdateMu sync.Mutex
var especieVeiculoBeforeUpdateHooks []EspecieVeiculoHook
var especieVeiculoAfterUpdateMu sync.Mutex
var especieVeiculoAfterUpdateHooks []EspecieVeiculoHook

var especieVeiculoBeforeDeleteMu sync.Mutex
var especieVeiculoBeforeDeleteHooks []EspecieVeiculoHook
var especieVeiculoAfterDeleteMu sync.Mutex
var especieVeiculoAfterDeleteHooks []EspecieVeiculoHook

var especieVeiculoBeforeUpsertMu sync.Mutex
var especieVeiculoBeforeUpsertHooks []EspecieVeiculoHook
var especieVeiculoAfterUpsertMu sync.Mutex
var especieVeiculoAfterUpsertHooks []EspecieVeiculoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *EspecieVeiculo) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *EspecieVeiculo) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *EspecieVeiculo) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *EspecieVeiculo) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *EspecieVeiculo) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *EspecieVeiculo) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *EspecieVeiculo) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *EspecieVeiculo) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *EspecieVeiculo) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range especieVeiculoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEspecieVeiculoHook registers your hook function for all future operations.
func AddEspecieVeiculoHook(hookPoint boil.HookPoint, especieVeiculoHook EspecieVeiculoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		especieVeiculoAfterSelectMu.Lock()
		especieVeiculoAfterSelectHooks = append(especieVeiculoAfterSelectHooks, especieVeiculoHook)
		especieVeiculoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		especieVeiculoBeforeInsertMu.Lock()
		especieVeiculoBeforeInsertHooks = append(especieVeiculoBeforeInsertHooks, especieVeiculoHook)
		especieVeiculoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		especieVeiculoAfterInsertMu.Lock()
		especieVeiculoAfterInsertHooks = append(especieVeiculoAfterInsertHooks, especieVeiculoHook)
		especieVeiculoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		especieVeiculoBeforeUpdateMu.Lock()
		especieVeiculoBeforeUpdateHooks = append(especieVeiculoBeforeUpdateHooks, especieVeiculoHook)
		especieVeiculoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		especieVeiculoAfterUpdateMu.Lock()
		especieVeiculoAfterUpdateHooks = append(especieVeiculoAfterUpdateHooks, especieVeiculoHook)
		especieVeiculoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		especieVeiculoBeforeDeleteMu.Lock()
		especieVeiculoBeforeDeleteHooks = append(especieVeiculoBeforeDeleteHooks, especieVeiculoHook)
		especieVeiculoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		especieVeiculoAfterDeleteMu.Lock()
		especieVeiculoAfterDeleteHooks = append(especieVeiculoAfterDeleteHooks, especieVeiculoHook)
		especieVeiculoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		especieVeiculoBeforeUpsertMu.Lock()
		especieVeiculoBeforeUpsertHooks = append(especieVeiculoBeforeUpsertHooks, especieVeiculoHook)
		especieVeiculoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		especieVeiculoAfterUpsertMu.Lock()
		especieVeiculoAfterUpsertHooks = append(especieVeiculoAfterUpsertHooks, especieVeiculoHook)
		especieVeiculoAfterUpsertMu.Unlock()
	}
}

// One returns a single especieVeiculo record from the query.
func (q especieVeiculoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EspecieVeiculo, error) {
	o := &EspecieVeiculo{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for especie_veiculo")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all EspecieVeiculo records from the query.
func (q especieVeiculoQuery) All(ctx context.Context, exec boil.ContextExecutor) (EspecieVeiculoSlice, error) {
	var o []*EspecieVeiculo

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to EspecieVeiculo slice")
	}

	if len(especieVeiculoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all EspecieVeiculo records in the query.
func (q especieVeiculoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count especie_veiculo rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q especieVeiculoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if especie_veiculo exists")
	}

	return count > 0, nil
}

// EpvNotaFiscalItemVeiculos retrieves all the nota_fiscal_item_veiculo's NotaFiscalItemVeiculos with an executor via epv_id column.
func (o *EspecieVeiculo) EpvNotaFiscalItemVeiculos(mods ...qm.QueryMod) notaFiscalItemVeiculoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal_item_veiculo`.`epv_id`=?", o.EpvID),
	)

	return NotaFiscalItemVeiculos(queryMods...)
}

// LoadEpvNotaFiscalItemVeiculos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (especieVeiculoL) LoadEpvNotaFiscalItemVeiculos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEspecieVeiculo interface{}, mods queries.Applicator) error {
	var slice []*EspecieVeiculo
	var object *EspecieVeiculo

	if singular {
		var ok bool
		object, ok = maybeEspecieVeiculo.(*EspecieVeiculo)
		if !ok {
			object = new(EspecieVeiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEspecieVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEspecieVeiculo))
			}
		}
	} else {
		s, ok := maybeEspecieVeiculo.(*[]*EspecieVeiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEspecieVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEspecieVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &especieVeiculoR{}
		}
		args[object.EpvID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &especieVeiculoR{}
			}
			args[obj.EpvID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal_item_veiculo`),
		qm.WhereIn(`nota_fiscal_item_veiculo.epv_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal_item_veiculo")
	}

	var resultSlice []*NotaFiscalItemVeiculo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal_item_veiculo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal_item_veiculo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal_item_veiculo")
	}

	if len(notaFiscalItemVeiculoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.EpvNotaFiscalItemVeiculos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalItemVeiculoR{}
			}
			foreign.R.Epv = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.EpvID, foreign.EpvID) {
				local.R.EpvNotaFiscalItemVeiculos = append(local.R.EpvNotaFiscalItemVeiculos, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalItemVeiculoR{}
				}
				foreign.R.Epv = local
				break
			}
		}
	}

	return nil
}

// AddEpvNotaFiscalItemVeiculos adds the given related objects to the existing relationships
// of the especie_veiculo, optionally inserting them as new records.
// Appends related to o.R.EpvNotaFiscalItemVeiculos.
// Sets related.R.Epv appropriately.
func (o *EspecieVeiculo) AddEpvNotaFiscalItemVeiculos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalItemVeiculo) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.EpvID, o.EpvID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal_item_veiculo` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"epv_id"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalItemVeiculoPrimaryKeyColumns),
			)
			values := []interface{}{o.EpvID, rel.NTFID, rel.NfiItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.EpvID, o.EpvID)
		}
	}

	if o.R == nil {
		o.R = &especieVeiculoR{
			EpvNotaFiscalItemVeiculos: related,
		}
	} else {
		o.R.EpvNotaFiscalItemVeiculos = append(o.R.EpvNotaFiscalItemVeiculos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalItemVeiculoR{
				Epv: o,
			}
		} else {
			rel.R.Epv = o
		}
	}
	return nil
}

// SetEpvNotaFiscalItemVeiculos removes all previously related items of the
// especie_veiculo replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Epv's EpvNotaFiscalItemVeiculos accordingly.
// Replaces o.R.EpvNotaFiscalItemVeiculos with related.
// Sets related.R.Epv's EpvNotaFiscalItemVeiculos accordingly.
func (o *EspecieVeiculo) SetEpvNotaFiscalItemVeiculos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalItemVeiculo) error {
	query := "update `nota_fiscal_item_veiculo` set `epv_id` = null where `epv_id` = ?"
	values := []interface{}{o.EpvID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.EpvNotaFiscalItemVeiculos {
			queries.SetScanner(&rel.EpvID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Epv = nil
		}
		o.R.EpvNotaFiscalItemVeiculos = nil
	}

	return o.AddEpvNotaFiscalItemVeiculos(ctx, exec, insert, related...)
}

// RemoveEpvNotaFiscalItemVeiculos relationships from objects passed in.
// Removes related items from R.EpvNotaFiscalItemVeiculos (uses pointer comparison, removal does not keep order)
// Sets related.R.Epv.
func (o *EspecieVeiculo) RemoveEpvNotaFiscalItemVeiculos(ctx context.Context, exec boil.ContextExecutor, related ...*NotaFiscalItemVeiculo) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.EpvID, nil)
		if rel.R != nil {
			rel.R.Epv = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("epv_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.EpvNotaFiscalItemVeiculos {
			if rel != ri {
				continue
			}

			ln := len(o.R.EpvNotaFiscalItemVeiculos)
			if ln > 1 && i < ln-1 {
				o.R.EpvNotaFiscalItemVeiculos[i] = o.R.EpvNotaFiscalItemVeiculos[ln-1]
			}
			o.R.EpvNotaFiscalItemVeiculos = o.R.EpvNotaFiscalItemVeiculos[:ln-1]
			break
		}
	}

	return nil
}

// EspecieVeiculos retrieves all the records using an executor.
func EspecieVeiculos(mods ...qm.QueryMod) especieVeiculoQuery {
	mods = append(mods, qm.From("`especie_veiculo`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`especie_veiculo`.*"})
	}

	return especieVeiculoQuery{q}
}

// FindEspecieVeiculo retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEspecieVeiculo(ctx context.Context, exec boil.ContextExecutor, epvID int, selectCols ...string) (*EspecieVeiculo, error) {
	especieVeiculoObj := &EspecieVeiculo{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `especie_veiculo` where `epv_id`=?", sel,
	)

	q := queries.Raw(query, epvID)

	err := q.Bind(ctx, exec, especieVeiculoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from especie_veiculo")
	}

	if err = especieVeiculoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return especieVeiculoObj, err
	}

	return especieVeiculoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EspecieVeiculo) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no especie_veiculo provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(especieVeiculoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	especieVeiculoInsertCacheMut.RLock()
	cache, cached := especieVeiculoInsertCache[key]
	especieVeiculoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			especieVeiculoAllColumns,
			especieVeiculoColumnsWithDefault,
			especieVeiculoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(especieVeiculoType, especieVeiculoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(especieVeiculoType, especieVeiculoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `especie_veiculo` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `especie_veiculo` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `especie_veiculo` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, especieVeiculoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into especie_veiculo")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.EpvID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for especie_veiculo")
	}

CacheNoHooks:
	if !cached {
		especieVeiculoInsertCacheMut.Lock()
		especieVeiculoInsertCache[key] = cache
		especieVeiculoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the EspecieVeiculo.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EspecieVeiculo) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	especieVeiculoUpdateCacheMut.RLock()
	cache, cached := especieVeiculoUpdateCache[key]
	especieVeiculoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			especieVeiculoAllColumns,
			especieVeiculoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update especie_veiculo, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `especie_veiculo` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, especieVeiculoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(especieVeiculoType, especieVeiculoMapping, append(wl, especieVeiculoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update especie_veiculo row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for especie_veiculo")
	}

	if !cached {
		especieVeiculoUpdateCacheMut.Lock()
		especieVeiculoUpdateCache[key] = cache
		especieVeiculoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q especieVeiculoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for especie_veiculo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for especie_veiculo")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EspecieVeiculoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), especieVeiculoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `especie_veiculo` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, especieVeiculoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in especieVeiculo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all especieVeiculo")
	}
	return rowsAff, nil
}

var mySQLEspecieVeiculoUniqueColumns = []string{
	"epv_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *EspecieVeiculo) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no especie_veiculo provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(especieVeiculoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLEspecieVeiculoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	especieVeiculoUpsertCacheMut.RLock()
	cache, cached := especieVeiculoUpsertCache[key]
	especieVeiculoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			especieVeiculoAllColumns,
			especieVeiculoColumnsWithDefault,
			especieVeiculoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			especieVeiculoAllColumns,
			especieVeiculoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert especie_veiculo, could not build update column list")
		}

		ret := strmangle.SetComplement(especieVeiculoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`especie_veiculo`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `especie_veiculo` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(especieVeiculoType, especieVeiculoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(especieVeiculoType, especieVeiculoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for especie_veiculo")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(especieVeiculoType, especieVeiculoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for especie_veiculo")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for especie_veiculo")
	}

CacheNoHooks:
	if !cached {
		especieVeiculoUpsertCacheMut.Lock()
		especieVeiculoUpsertCache[key] = cache
		especieVeiculoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single EspecieVeiculo record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EspecieVeiculo) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no EspecieVeiculo provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), especieVeiculoPrimaryKeyMapping)
	sql := "DELETE FROM `especie_veiculo` WHERE `epv_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from especie_veiculo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for especie_veiculo")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q especieVeiculoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no especieVeiculoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from especie_veiculo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for especie_veiculo")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EspecieVeiculoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(especieVeiculoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), especieVeiculoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `especie_veiculo` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, especieVeiculoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from especieVeiculo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for especie_veiculo")
	}

	if len(especieVeiculoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EspecieVeiculo) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEspecieVeiculo(ctx, exec, o.EpvID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EspecieVeiculoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EspecieVeiculoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), especieVeiculoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `especie_veiculo`.* FROM `especie_veiculo` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, especieVeiculoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in EspecieVeiculoSlice")
	}

	*o = slice

	return nil
}

// EspecieVeiculoExists checks if the EspecieVeiculo row exists.
func EspecieVeiculoExists(ctx context.Context, exec boil.ContextExecutor, epvID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `especie_veiculo` where `epv_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, epvID)
	}
	row := exec.QueryRowContext(ctx, sql, epvID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if especie_veiculo exists")
	}

	return exists, nil
}

// Exists checks if the EspecieVeiculo row exists.
func (o *EspecieVeiculo) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return EspecieVeiculoExists(ctx, exec, o.EpvID)
}
