// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// NotaFicalInutilizacao is an object representing the database table.
type NotaFicalInutilizacao struct {
	NfinutID          int       `boil:"nfinut_id" json:"nfinut_id" toml:"nfinut_id" yaml:"nfinut_id"`
	EmfID             int       `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	NfinutData        time.Time `boil:"nfinut_data" json:"nfinut_data" toml:"nfinut_data" yaml:"nfinut_data"`
	NfunitDatainicial time.Time `boil:"nfunit_datainicial" json:"nfunit_datainicial" toml:"nfunit_datainicial" yaml:"nfunit_datainicial"`
	NfunitDatafinal   time.Time `boil:"nfunit_datafinal" json:"nfunit_datafinal" toml:"nfunit_datafinal" yaml:"nfunit_datafinal"`

	R *notaFicalInutilizacaoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L notaFicalInutilizacaoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var NotaFicalInutilizacaoColumns = struct {
	NfinutID          string
	EmfID             string
	NfinutData        string
	NfunitDatainicial string
	NfunitDatafinal   string
}{
	NfinutID:          "nfinut_id",
	EmfID:             "emf_id",
	NfinutData:        "nfinut_data",
	NfunitDatainicial: "nfunit_datainicial",
	NfunitDatafinal:   "nfunit_datafinal",
}

var NotaFicalInutilizacaoTableColumns = struct {
	NfinutID          string
	EmfID             string
	NfinutData        string
	NfunitDatainicial string
	NfunitDatafinal   string
}{
	NfinutID:          "nota_fical_inutilizacao.nfinut_id",
	EmfID:             "nota_fical_inutilizacao.emf_id",
	NfinutData:        "nota_fical_inutilizacao.nfinut_data",
	NfunitDatainicial: "nota_fical_inutilizacao.nfunit_datainicial",
	NfunitDatafinal:   "nota_fical_inutilizacao.nfunit_datafinal",
}

// Generated where

var NotaFicalInutilizacaoWhere = struct {
	NfinutID          whereHelperint
	EmfID             whereHelperint
	NfinutData        whereHelpertime_Time
	NfunitDatainicial whereHelpertime_Time
	NfunitDatafinal   whereHelpertime_Time
}{
	NfinutID:          whereHelperint{field: "`nota_fical_inutilizacao`.`nfinut_id`"},
	EmfID:             whereHelperint{field: "`nota_fical_inutilizacao`.`emf_id`"},
	NfinutData:        whereHelpertime_Time{field: "`nota_fical_inutilizacao`.`nfinut_data`"},
	NfunitDatainicial: whereHelpertime_Time{field: "`nota_fical_inutilizacao`.`nfunit_datainicial`"},
	NfunitDatafinal:   whereHelpertime_Time{field: "`nota_fical_inutilizacao`.`nfunit_datafinal`"},
}

// NotaFicalInutilizacaoRels is where relationship names are stored.
var NotaFicalInutilizacaoRels = struct {
	Emf                              string
	NfinutNotaFicalInutilizacaoNotas string
}{
	Emf:                              "Emf",
	NfinutNotaFicalInutilizacaoNotas: "NfinutNotaFicalInutilizacaoNotas",
}

// notaFicalInutilizacaoR is where relationships are stored.
type notaFicalInutilizacaoR struct {
	Emf                              *EmpresaFilial                  `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	NfinutNotaFicalInutilizacaoNotas NotaFicalInutilizacaoNotasSlice `boil:"NfinutNotaFicalInutilizacaoNotas" json:"NfinutNotaFicalInutilizacaoNotas" toml:"NfinutNotaFicalInutilizacaoNotas" yaml:"NfinutNotaFicalInutilizacaoNotas"`
}

// NewStruct creates a new relationship struct
func (*notaFicalInutilizacaoR) NewStruct() *notaFicalInutilizacaoR {
	return &notaFicalInutilizacaoR{}
}

func (r *notaFicalInutilizacaoR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *notaFicalInutilizacaoR) GetNfinutNotaFicalInutilizacaoNotas() NotaFicalInutilizacaoNotasSlice {
	if r == nil {
		return nil
	}
	return r.NfinutNotaFicalInutilizacaoNotas
}

// notaFicalInutilizacaoL is where Load methods for each relationship are stored.
type notaFicalInutilizacaoL struct{}

var (
	notaFicalInutilizacaoAllColumns            = []string{"nfinut_id", "emf_id", "nfinut_data", "nfunit_datainicial", "nfunit_datafinal"}
	notaFicalInutilizacaoColumnsWithoutDefault = []string{"nfinut_id", "emf_id", "nfinut_data", "nfunit_datainicial", "nfunit_datafinal"}
	notaFicalInutilizacaoColumnsWithDefault    = []string{}
	notaFicalInutilizacaoPrimaryKeyColumns     = []string{"nfinut_id"}
	notaFicalInutilizacaoGeneratedColumns      = []string{}
)

type (
	// NotaFicalInutilizacaoSlice is an alias for a slice of pointers to NotaFicalInutilizacao.
	// This should almost always be used instead of []NotaFicalInutilizacao.
	NotaFicalInutilizacaoSlice []*NotaFicalInutilizacao
	// NotaFicalInutilizacaoHook is the signature for custom NotaFicalInutilizacao hook methods
	NotaFicalInutilizacaoHook func(context.Context, boil.ContextExecutor, *NotaFicalInutilizacao) error

	notaFicalInutilizacaoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	notaFicalInutilizacaoType                 = reflect.TypeOf(&NotaFicalInutilizacao{})
	notaFicalInutilizacaoMapping              = queries.MakeStructMapping(notaFicalInutilizacaoType)
	notaFicalInutilizacaoPrimaryKeyMapping, _ = queries.BindMapping(notaFicalInutilizacaoType, notaFicalInutilizacaoMapping, notaFicalInutilizacaoPrimaryKeyColumns)
	notaFicalInutilizacaoInsertCacheMut       sync.RWMutex
	notaFicalInutilizacaoInsertCache          = make(map[string]insertCache)
	notaFicalInutilizacaoUpdateCacheMut       sync.RWMutex
	notaFicalInutilizacaoUpdateCache          = make(map[string]updateCache)
	notaFicalInutilizacaoUpsertCacheMut       sync.RWMutex
	notaFicalInutilizacaoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var notaFicalInutilizacaoAfterSelectMu sync.Mutex
var notaFicalInutilizacaoAfterSelectHooks []NotaFicalInutilizacaoHook

var notaFicalInutilizacaoBeforeInsertMu sync.Mutex
var notaFicalInutilizacaoBeforeInsertHooks []NotaFicalInutilizacaoHook
var notaFicalInutilizacaoAfterInsertMu sync.Mutex
var notaFicalInutilizacaoAfterInsertHooks []NotaFicalInutilizacaoHook

var notaFicalInutilizacaoBeforeUpdateMu sync.Mutex
var notaFicalInutilizacaoBeforeUpdateHooks []NotaFicalInutilizacaoHook
var notaFicalInutilizacaoAfterUpdateMu sync.Mutex
var notaFicalInutilizacaoAfterUpdateHooks []NotaFicalInutilizacaoHook

var notaFicalInutilizacaoBeforeDeleteMu sync.Mutex
var notaFicalInutilizacaoBeforeDeleteHooks []NotaFicalInutilizacaoHook
var notaFicalInutilizacaoAfterDeleteMu sync.Mutex
var notaFicalInutilizacaoAfterDeleteHooks []NotaFicalInutilizacaoHook

var notaFicalInutilizacaoBeforeUpsertMu sync.Mutex
var notaFicalInutilizacaoBeforeUpsertHooks []NotaFicalInutilizacaoHook
var notaFicalInutilizacaoAfterUpsertMu sync.Mutex
var notaFicalInutilizacaoAfterUpsertHooks []NotaFicalInutilizacaoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *NotaFicalInutilizacao) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *NotaFicalInutilizacao) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *NotaFicalInutilizacao) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *NotaFicalInutilizacao) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *NotaFicalInutilizacao) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *NotaFicalInutilizacao) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *NotaFicalInutilizacao) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *NotaFicalInutilizacao) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *NotaFicalInutilizacao) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddNotaFicalInutilizacaoHook registers your hook function for all future operations.
func AddNotaFicalInutilizacaoHook(hookPoint boil.HookPoint, notaFicalInutilizacaoHook NotaFicalInutilizacaoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		notaFicalInutilizacaoAfterSelectMu.Lock()
		notaFicalInutilizacaoAfterSelectHooks = append(notaFicalInutilizacaoAfterSelectHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		notaFicalInutilizacaoBeforeInsertMu.Lock()
		notaFicalInutilizacaoBeforeInsertHooks = append(notaFicalInutilizacaoBeforeInsertHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		notaFicalInutilizacaoAfterInsertMu.Lock()
		notaFicalInutilizacaoAfterInsertHooks = append(notaFicalInutilizacaoAfterInsertHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		notaFicalInutilizacaoBeforeUpdateMu.Lock()
		notaFicalInutilizacaoBeforeUpdateHooks = append(notaFicalInutilizacaoBeforeUpdateHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		notaFicalInutilizacaoAfterUpdateMu.Lock()
		notaFicalInutilizacaoAfterUpdateHooks = append(notaFicalInutilizacaoAfterUpdateHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		notaFicalInutilizacaoBeforeDeleteMu.Lock()
		notaFicalInutilizacaoBeforeDeleteHooks = append(notaFicalInutilizacaoBeforeDeleteHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		notaFicalInutilizacaoAfterDeleteMu.Lock()
		notaFicalInutilizacaoAfterDeleteHooks = append(notaFicalInutilizacaoAfterDeleteHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		notaFicalInutilizacaoBeforeUpsertMu.Lock()
		notaFicalInutilizacaoBeforeUpsertHooks = append(notaFicalInutilizacaoBeforeUpsertHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		notaFicalInutilizacaoAfterUpsertMu.Lock()
		notaFicalInutilizacaoAfterUpsertHooks = append(notaFicalInutilizacaoAfterUpsertHooks, notaFicalInutilizacaoHook)
		notaFicalInutilizacaoAfterUpsertMu.Unlock()
	}
}

// One returns a single notaFicalInutilizacao record from the query.
func (q notaFicalInutilizacaoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*NotaFicalInutilizacao, error) {
	o := &NotaFicalInutilizacao{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for nota_fical_inutilizacao")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all NotaFicalInutilizacao records from the query.
func (q notaFicalInutilizacaoQuery) All(ctx context.Context, exec boil.ContextExecutor) (NotaFicalInutilizacaoSlice, error) {
	var o []*NotaFicalInutilizacao

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to NotaFicalInutilizacao slice")
	}

	if len(notaFicalInutilizacaoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all NotaFicalInutilizacao records in the query.
func (q notaFicalInutilizacaoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count nota_fical_inutilizacao rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q notaFicalInutilizacaoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if nota_fical_inutilizacao exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *NotaFicalInutilizacao) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// NfinutNotaFicalInutilizacaoNotas retrieves all the nota_fical_inutilizacao_notas's NotaFicalInutilizacaoNotas with an executor via nfinut_id column.
func (o *NotaFicalInutilizacao) NfinutNotaFicalInutilizacaoNotas(mods ...qm.QueryMod) notaFicalInutilizacaoNotasQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fical_inutilizacao_notas`.`nfinut_id`=?", o.NfinutID),
	)

	return NotaFicalInutilizacaoNotas(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (notaFicalInutilizacaoL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNotaFicalInutilizacao interface{}, mods queries.Applicator) error {
	var slice []*NotaFicalInutilizacao
	var object *NotaFicalInutilizacao

	if singular {
		var ok bool
		object, ok = maybeNotaFicalInutilizacao.(*NotaFicalInutilizacao)
		if !ok {
			object = new(NotaFicalInutilizacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNotaFicalInutilizacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNotaFicalInutilizacao))
			}
		}
	} else {
		s, ok := maybeNotaFicalInutilizacao.(*[]*NotaFicalInutilizacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNotaFicalInutilizacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNotaFicalInutilizacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &notaFicalInutilizacaoR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &notaFicalInutilizacaoR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfNotaFicalInutilizacaos = append(foreign.R.EmfNotaFicalInutilizacaos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfNotaFicalInutilizacaos = append(foreign.R.EmfNotaFicalInutilizacaos, local)
				break
			}
		}
	}

	return nil
}

// LoadNfinutNotaFicalInutilizacaoNotas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (notaFicalInutilizacaoL) LoadNfinutNotaFicalInutilizacaoNotas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNotaFicalInutilizacao interface{}, mods queries.Applicator) error {
	var slice []*NotaFicalInutilizacao
	var object *NotaFicalInutilizacao

	if singular {
		var ok bool
		object, ok = maybeNotaFicalInutilizacao.(*NotaFicalInutilizacao)
		if !ok {
			object = new(NotaFicalInutilizacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNotaFicalInutilizacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNotaFicalInutilizacao))
			}
		}
	} else {
		s, ok := maybeNotaFicalInutilizacao.(*[]*NotaFicalInutilizacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNotaFicalInutilizacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNotaFicalInutilizacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &notaFicalInutilizacaoR{}
		}
		args[object.NfinutID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &notaFicalInutilizacaoR{}
			}
			args[obj.NfinutID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fical_inutilizacao_notas`),
		qm.WhereIn(`nota_fical_inutilizacao_notas.nfinut_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fical_inutilizacao_notas")
	}

	var resultSlice []*NotaFicalInutilizacaoNotas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fical_inutilizacao_notas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fical_inutilizacao_notas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fical_inutilizacao_notas")
	}

	if len(notaFicalInutilizacaoNotasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.NfinutNotaFicalInutilizacaoNotas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFicalInutilizacaoNotasR{}
			}
			foreign.R.Nfinut = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.NfinutID == foreign.NfinutID {
				local.R.NfinutNotaFicalInutilizacaoNotas = append(local.R.NfinutNotaFicalInutilizacaoNotas, foreign)
				if foreign.R == nil {
					foreign.R = &notaFicalInutilizacaoNotasR{}
				}
				foreign.R.Nfinut = local
				break
			}
		}
	}

	return nil
}

// SetEmf of the notaFicalInutilizacao to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfNotaFicalInutilizacaos.
func (o *NotaFicalInutilizacao) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `nota_fical_inutilizacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, notaFicalInutilizacaoPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.NfinutID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &notaFicalInutilizacaoR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfNotaFicalInutilizacaos: NotaFicalInutilizacaoSlice{o},
		}
	} else {
		related.R.EmfNotaFicalInutilizacaos = append(related.R.EmfNotaFicalInutilizacaos, o)
	}

	return nil
}

// AddNfinutNotaFicalInutilizacaoNotas adds the given related objects to the existing relationships
// of the nota_fical_inutilizacao, optionally inserting them as new records.
// Appends related to o.R.NfinutNotaFicalInutilizacaoNotas.
// Sets related.R.Nfinut appropriately.
func (o *NotaFicalInutilizacao) AddNfinutNotaFicalInutilizacaoNotas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFicalInutilizacaoNotas) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.NfinutID = o.NfinutID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fical_inutilizacao_notas` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"nfinut_id"}),
				strmangle.WhereClause("`", "`", 0, notaFicalInutilizacaoNotasPrimaryKeyColumns),
			)
			values := []interface{}{o.NfinutID, rel.NfinutID, rel.NfinutitemItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.NfinutID = o.NfinutID
		}
	}

	if o.R == nil {
		o.R = &notaFicalInutilizacaoR{
			NfinutNotaFicalInutilizacaoNotas: related,
		}
	} else {
		o.R.NfinutNotaFicalInutilizacaoNotas = append(o.R.NfinutNotaFicalInutilizacaoNotas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFicalInutilizacaoNotasR{
				Nfinut: o,
			}
		} else {
			rel.R.Nfinut = o
		}
	}
	return nil
}

// NotaFicalInutilizacaos retrieves all the records using an executor.
func NotaFicalInutilizacaos(mods ...qm.QueryMod) notaFicalInutilizacaoQuery {
	mods = append(mods, qm.From("`nota_fical_inutilizacao`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`nota_fical_inutilizacao`.*"})
	}

	return notaFicalInutilizacaoQuery{q}
}

// FindNotaFicalInutilizacao retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindNotaFicalInutilizacao(ctx context.Context, exec boil.ContextExecutor, nfinutID int, selectCols ...string) (*NotaFicalInutilizacao, error) {
	notaFicalInutilizacaoObj := &NotaFicalInutilizacao{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `nota_fical_inutilizacao` where `nfinut_id`=?", sel,
	)

	q := queries.Raw(query, nfinutID)

	err := q.Bind(ctx, exec, notaFicalInutilizacaoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from nota_fical_inutilizacao")
	}

	if err = notaFicalInutilizacaoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return notaFicalInutilizacaoObj, err
	}

	return notaFicalInutilizacaoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *NotaFicalInutilizacao) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no nota_fical_inutilizacao provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(notaFicalInutilizacaoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	notaFicalInutilizacaoInsertCacheMut.RLock()
	cache, cached := notaFicalInutilizacaoInsertCache[key]
	notaFicalInutilizacaoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			notaFicalInutilizacaoAllColumns,
			notaFicalInutilizacaoColumnsWithDefault,
			notaFicalInutilizacaoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(notaFicalInutilizacaoType, notaFicalInutilizacaoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(notaFicalInutilizacaoType, notaFicalInutilizacaoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `nota_fical_inutilizacao` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `nota_fical_inutilizacao` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `nota_fical_inutilizacao` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, notaFicalInutilizacaoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into nota_fical_inutilizacao")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.NfinutID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for nota_fical_inutilizacao")
	}

CacheNoHooks:
	if !cached {
		notaFicalInutilizacaoInsertCacheMut.Lock()
		notaFicalInutilizacaoInsertCache[key] = cache
		notaFicalInutilizacaoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the NotaFicalInutilizacao.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *NotaFicalInutilizacao) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	notaFicalInutilizacaoUpdateCacheMut.RLock()
	cache, cached := notaFicalInutilizacaoUpdateCache[key]
	notaFicalInutilizacaoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			notaFicalInutilizacaoAllColumns,
			notaFicalInutilizacaoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update nota_fical_inutilizacao, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `nota_fical_inutilizacao` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, notaFicalInutilizacaoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(notaFicalInutilizacaoType, notaFicalInutilizacaoMapping, append(wl, notaFicalInutilizacaoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update nota_fical_inutilizacao row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for nota_fical_inutilizacao")
	}

	if !cached {
		notaFicalInutilizacaoUpdateCacheMut.Lock()
		notaFicalInutilizacaoUpdateCache[key] = cache
		notaFicalInutilizacaoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q notaFicalInutilizacaoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for nota_fical_inutilizacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for nota_fical_inutilizacao")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o NotaFicalInutilizacaoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFicalInutilizacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `nota_fical_inutilizacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFicalInutilizacaoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in notaFicalInutilizacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all notaFicalInutilizacao")
	}
	return rowsAff, nil
}

var mySQLNotaFicalInutilizacaoUniqueColumns = []string{
	"nfinut_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *NotaFicalInutilizacao) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no nota_fical_inutilizacao provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(notaFicalInutilizacaoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLNotaFicalInutilizacaoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	notaFicalInutilizacaoUpsertCacheMut.RLock()
	cache, cached := notaFicalInutilizacaoUpsertCache[key]
	notaFicalInutilizacaoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			notaFicalInutilizacaoAllColumns,
			notaFicalInutilizacaoColumnsWithDefault,
			notaFicalInutilizacaoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			notaFicalInutilizacaoAllColumns,
			notaFicalInutilizacaoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert nota_fical_inutilizacao, could not build update column list")
		}

		ret := strmangle.SetComplement(notaFicalInutilizacaoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`nota_fical_inutilizacao`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `nota_fical_inutilizacao` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(notaFicalInutilizacaoType, notaFicalInutilizacaoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(notaFicalInutilizacaoType, notaFicalInutilizacaoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for nota_fical_inutilizacao")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(notaFicalInutilizacaoType, notaFicalInutilizacaoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for nota_fical_inutilizacao")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for nota_fical_inutilizacao")
	}

CacheNoHooks:
	if !cached {
		notaFicalInutilizacaoUpsertCacheMut.Lock()
		notaFicalInutilizacaoUpsertCache[key] = cache
		notaFicalInutilizacaoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single NotaFicalInutilizacao record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *NotaFicalInutilizacao) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no NotaFicalInutilizacao provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), notaFicalInutilizacaoPrimaryKeyMapping)
	sql := "DELETE FROM `nota_fical_inutilizacao` WHERE `nfinut_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from nota_fical_inutilizacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for nota_fical_inutilizacao")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q notaFicalInutilizacaoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no notaFicalInutilizacaoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from nota_fical_inutilizacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for nota_fical_inutilizacao")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o NotaFicalInutilizacaoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(notaFicalInutilizacaoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFicalInutilizacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `nota_fical_inutilizacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFicalInutilizacaoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from notaFicalInutilizacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for nota_fical_inutilizacao")
	}

	if len(notaFicalInutilizacaoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *NotaFicalInutilizacao) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindNotaFicalInutilizacao(ctx, exec, o.NfinutID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *NotaFicalInutilizacaoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := NotaFicalInutilizacaoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFicalInutilizacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `nota_fical_inutilizacao`.* FROM `nota_fical_inutilizacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFicalInutilizacaoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in NotaFicalInutilizacaoSlice")
	}

	*o = slice

	return nil
}

// NotaFicalInutilizacaoExists checks if the NotaFicalInutilizacao row exists.
func NotaFicalInutilizacaoExists(ctx context.Context, exec boil.ContextExecutor, nfinutID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `nota_fical_inutilizacao` where `nfinut_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, nfinutID)
	}
	row := exec.QueryRowContext(ctx, sql, nfinutID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if nota_fical_inutilizacao exists")
	}

	return exists, nil
}

// Exists checks if the NotaFicalInutilizacao row exists.
func (o *NotaFicalInutilizacao) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return NotaFicalInutilizacaoExists(ctx, exec, o.NfinutID)
}
