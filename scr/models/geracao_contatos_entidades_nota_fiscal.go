// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// GeracaoContatosEntidadesNotaFiscal is an object representing the database table.
type GeracaoContatosEntidadesNotaFiscal struct {
	GceID   int      `boil:"gce_id" json:"gce_id" toml:"gce_id" yaml:"gce_id"`
	GcenfID int      `boil:"gcenf_id" json:"gcenf_id" toml:"gcenf_id" yaml:"gcenf_id"`
	EntID   int      `boil:"ent_id" json:"ent_id" toml:"ent_id" yaml:"ent_id"`
	NTFID   null.Int `boil:"ntf_id" json:"ntf_id,omitempty" toml:"ntf_id" yaml:"ntf_id,omitempty"`
	CentID  int      `boil:"cent_id" json:"cent_id" toml:"cent_id" yaml:"cent_id"`
	// 1 - nfe
	//               2 - nfse
	//               3 - pedido
	GcenfTipo         int               `boil:"gcenf_tipo" json:"gcenf_tipo" toml:"gcenf_tipo" yaml:"gcenf_tipo"`
	GcenfValorbruto   types.Decimal     `boil:"gcenf_valorbruto" json:"gcenf_valorbruto" toml:"gcenf_valorbruto" yaml:"gcenf_valorbruto"`
	GcenfValorextra   types.NullDecimal `boil:"gcenf_valorextra" json:"gcenf_valorextra,omitempty" toml:"gcenf_valorextra" yaml:"gcenf_valorextra,omitempty"`
	GcenfValorliquido types.Decimal     `boil:"gcenf_valorliquido" json:"gcenf_valorliquido" toml:"gcenf_valorliquido" yaml:"gcenf_valorliquido"`

	R *geracaoContatosEntidadesNotaFiscalR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L geracaoContatosEntidadesNotaFiscalL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var GeracaoContatosEntidadesNotaFiscalColumns = struct {
	GceID             string
	GcenfID           string
	EntID             string
	NTFID             string
	CentID            string
	GcenfTipo         string
	GcenfValorbruto   string
	GcenfValorextra   string
	GcenfValorliquido string
}{
	GceID:             "gce_id",
	GcenfID:           "gcenf_id",
	EntID:             "ent_id",
	NTFID:             "ntf_id",
	CentID:            "cent_id",
	GcenfTipo:         "gcenf_tipo",
	GcenfValorbruto:   "gcenf_valorbruto",
	GcenfValorextra:   "gcenf_valorextra",
	GcenfValorliquido: "gcenf_valorliquido",
}

var GeracaoContatosEntidadesNotaFiscalTableColumns = struct {
	GceID             string
	GcenfID           string
	EntID             string
	NTFID             string
	CentID            string
	GcenfTipo         string
	GcenfValorbruto   string
	GcenfValorextra   string
	GcenfValorliquido string
}{
	GceID:             "geracao_contatos_entidades_nota_fiscal.gce_id",
	GcenfID:           "geracao_contatos_entidades_nota_fiscal.gcenf_id",
	EntID:             "geracao_contatos_entidades_nota_fiscal.ent_id",
	NTFID:             "geracao_contatos_entidades_nota_fiscal.ntf_id",
	CentID:            "geracao_contatos_entidades_nota_fiscal.cent_id",
	GcenfTipo:         "geracao_contatos_entidades_nota_fiscal.gcenf_tipo",
	GcenfValorbruto:   "geracao_contatos_entidades_nota_fiscal.gcenf_valorbruto",
	GcenfValorextra:   "geracao_contatos_entidades_nota_fiscal.gcenf_valorextra",
	GcenfValorliquido: "geracao_contatos_entidades_nota_fiscal.gcenf_valorliquido",
}

// Generated where

var GeracaoContatosEntidadesNotaFiscalWhere = struct {
	GceID             whereHelperint
	GcenfID           whereHelperint
	EntID             whereHelperint
	NTFID             whereHelpernull_Int
	CentID            whereHelperint
	GcenfTipo         whereHelperint
	GcenfValorbruto   whereHelpertypes_Decimal
	GcenfValorextra   whereHelpertypes_NullDecimal
	GcenfValorliquido whereHelpertypes_Decimal
}{
	GceID:             whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal`.`gce_id`"},
	GcenfID:           whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal`.`gcenf_id`"},
	EntID:             whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal`.`ent_id`"},
	NTFID:             whereHelpernull_Int{field: "`geracao_contatos_entidades_nota_fiscal`.`ntf_id`"},
	CentID:            whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal`.`cent_id`"},
	GcenfTipo:         whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal`.`gcenf_tipo`"},
	GcenfValorbruto:   whereHelpertypes_Decimal{field: "`geracao_contatos_entidades_nota_fiscal`.`gcenf_valorbruto`"},
	GcenfValorextra:   whereHelpertypes_NullDecimal{field: "`geracao_contatos_entidades_nota_fiscal`.`gcenf_valorextra`"},
	GcenfValorliquido: whereHelpertypes_Decimal{field: "`geracao_contatos_entidades_nota_fiscal`.`gcenf_valorliquido`"},
}

// GeracaoContatosEntidadesNotaFiscalRels is where relationship names are stored.
var GeracaoContatosEntidadesNotaFiscalRels = struct {
	Cent string
	Ent  string
	Gce  string
	NTF  string
}{
	Cent: "Cent",
	Ent:  "Ent",
	Gce:  "Gce",
	NTF:  "NTF",
}

// geracaoContatosEntidadesNotaFiscalR is where relationships are stored.
type geracaoContatosEntidadesNotaFiscalR struct {
	Cent *ContratoEntidade        `boil:"Cent" json:"Cent" toml:"Cent" yaml:"Cent"`
	Ent  *Entidade                `boil:"Ent" json:"Ent" toml:"Ent" yaml:"Ent"`
	Gce  *GeracaoContatosEntidade `boil:"Gce" json:"Gce" toml:"Gce" yaml:"Gce"`
	NTF  *NotaFiscal              `boil:"NTF" json:"NTF" toml:"NTF" yaml:"NTF"`
}

// NewStruct creates a new relationship struct
func (*geracaoContatosEntidadesNotaFiscalR) NewStruct() *geracaoContatosEntidadesNotaFiscalR {
	return &geracaoContatosEntidadesNotaFiscalR{}
}

func (r *geracaoContatosEntidadesNotaFiscalR) GetCent() *ContratoEntidade {
	if r == nil {
		return nil
	}
	return r.Cent
}

func (r *geracaoContatosEntidadesNotaFiscalR) GetEnt() *Entidade {
	if r == nil {
		return nil
	}
	return r.Ent
}

func (r *geracaoContatosEntidadesNotaFiscalR) GetGce() *GeracaoContatosEntidade {
	if r == nil {
		return nil
	}
	return r.Gce
}

func (r *geracaoContatosEntidadesNotaFiscalR) GetNTF() *NotaFiscal {
	if r == nil {
		return nil
	}
	return r.NTF
}

// geracaoContatosEntidadesNotaFiscalL is where Load methods for each relationship are stored.
type geracaoContatosEntidadesNotaFiscalL struct{}

var (
	geracaoContatosEntidadesNotaFiscalAllColumns            = []string{"gce_id", "gcenf_id", "ent_id", "ntf_id", "cent_id", "gcenf_tipo", "gcenf_valorbruto", "gcenf_valorextra", "gcenf_valorliquido"}
	geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault = []string{"gce_id", "gcenf_id", "ent_id", "ntf_id", "cent_id", "gcenf_tipo"}
	geracaoContatosEntidadesNotaFiscalColumnsWithDefault    = []string{"gcenf_valorbruto", "gcenf_valorextra", "gcenf_valorliquido"}
	geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns     = []string{"gce_id", "gcenf_id"}
	geracaoContatosEntidadesNotaFiscalGeneratedColumns      = []string{}
)

type (
	// GeracaoContatosEntidadesNotaFiscalSlice is an alias for a slice of pointers to GeracaoContatosEntidadesNotaFiscal.
	// This should almost always be used instead of []GeracaoContatosEntidadesNotaFiscal.
	GeracaoContatosEntidadesNotaFiscalSlice []*GeracaoContatosEntidadesNotaFiscal
	// GeracaoContatosEntidadesNotaFiscalHook is the signature for custom GeracaoContatosEntidadesNotaFiscal hook methods
	GeracaoContatosEntidadesNotaFiscalHook func(context.Context, boil.ContextExecutor, *GeracaoContatosEntidadesNotaFiscal) error

	geracaoContatosEntidadesNotaFiscalQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	geracaoContatosEntidadesNotaFiscalType                 = reflect.TypeOf(&GeracaoContatosEntidadesNotaFiscal{})
	geracaoContatosEntidadesNotaFiscalMapping              = queries.MakeStructMapping(geracaoContatosEntidadesNotaFiscalType)
	geracaoContatosEntidadesNotaFiscalPrimaryKeyMapping, _ = queries.BindMapping(geracaoContatosEntidadesNotaFiscalType, geracaoContatosEntidadesNotaFiscalMapping, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns)
	geracaoContatosEntidadesNotaFiscalInsertCacheMut       sync.RWMutex
	geracaoContatosEntidadesNotaFiscalInsertCache          = make(map[string]insertCache)
	geracaoContatosEntidadesNotaFiscalUpdateCacheMut       sync.RWMutex
	geracaoContatosEntidadesNotaFiscalUpdateCache          = make(map[string]updateCache)
	geracaoContatosEntidadesNotaFiscalUpsertCacheMut       sync.RWMutex
	geracaoContatosEntidadesNotaFiscalUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var geracaoContatosEntidadesNotaFiscalAfterSelectMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalAfterSelectHooks []GeracaoContatosEntidadesNotaFiscalHook

var geracaoContatosEntidadesNotaFiscalBeforeInsertMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalBeforeInsertHooks []GeracaoContatosEntidadesNotaFiscalHook
var geracaoContatosEntidadesNotaFiscalAfterInsertMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalAfterInsertHooks []GeracaoContatosEntidadesNotaFiscalHook

var geracaoContatosEntidadesNotaFiscalBeforeUpdateMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalBeforeUpdateHooks []GeracaoContatosEntidadesNotaFiscalHook
var geracaoContatosEntidadesNotaFiscalAfterUpdateMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalAfterUpdateHooks []GeracaoContatosEntidadesNotaFiscalHook

var geracaoContatosEntidadesNotaFiscalBeforeDeleteMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalBeforeDeleteHooks []GeracaoContatosEntidadesNotaFiscalHook
var geracaoContatosEntidadesNotaFiscalAfterDeleteMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalAfterDeleteHooks []GeracaoContatosEntidadesNotaFiscalHook

var geracaoContatosEntidadesNotaFiscalBeforeUpsertMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalBeforeUpsertHooks []GeracaoContatosEntidadesNotaFiscalHook
var geracaoContatosEntidadesNotaFiscalAfterUpsertMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalAfterUpsertHooks []GeracaoContatosEntidadesNotaFiscalHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *GeracaoContatosEntidadesNotaFiscal) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddGeracaoContatosEntidadesNotaFiscalHook registers your hook function for all future operations.
func AddGeracaoContatosEntidadesNotaFiscalHook(hookPoint boil.HookPoint, geracaoContatosEntidadesNotaFiscalHook GeracaoContatosEntidadesNotaFiscalHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		geracaoContatosEntidadesNotaFiscalAfterSelectMu.Lock()
		geracaoContatosEntidadesNotaFiscalAfterSelectHooks = append(geracaoContatosEntidadesNotaFiscalAfterSelectHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		geracaoContatosEntidadesNotaFiscalBeforeInsertMu.Lock()
		geracaoContatosEntidadesNotaFiscalBeforeInsertHooks = append(geracaoContatosEntidadesNotaFiscalBeforeInsertHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		geracaoContatosEntidadesNotaFiscalAfterInsertMu.Lock()
		geracaoContatosEntidadesNotaFiscalAfterInsertHooks = append(geracaoContatosEntidadesNotaFiscalAfterInsertHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		geracaoContatosEntidadesNotaFiscalBeforeUpdateMu.Lock()
		geracaoContatosEntidadesNotaFiscalBeforeUpdateHooks = append(geracaoContatosEntidadesNotaFiscalBeforeUpdateHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		geracaoContatosEntidadesNotaFiscalAfterUpdateMu.Lock()
		geracaoContatosEntidadesNotaFiscalAfterUpdateHooks = append(geracaoContatosEntidadesNotaFiscalAfterUpdateHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		geracaoContatosEntidadesNotaFiscalBeforeDeleteMu.Lock()
		geracaoContatosEntidadesNotaFiscalBeforeDeleteHooks = append(geracaoContatosEntidadesNotaFiscalBeforeDeleteHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		geracaoContatosEntidadesNotaFiscalAfterDeleteMu.Lock()
		geracaoContatosEntidadesNotaFiscalAfterDeleteHooks = append(geracaoContatosEntidadesNotaFiscalAfterDeleteHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		geracaoContatosEntidadesNotaFiscalBeforeUpsertMu.Lock()
		geracaoContatosEntidadesNotaFiscalBeforeUpsertHooks = append(geracaoContatosEntidadesNotaFiscalBeforeUpsertHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		geracaoContatosEntidadesNotaFiscalAfterUpsertMu.Lock()
		geracaoContatosEntidadesNotaFiscalAfterUpsertHooks = append(geracaoContatosEntidadesNotaFiscalAfterUpsertHooks, geracaoContatosEntidadesNotaFiscalHook)
		geracaoContatosEntidadesNotaFiscalAfterUpsertMu.Unlock()
	}
}

// One returns a single geracaoContatosEntidadesNotaFiscal record from the query.
func (q geracaoContatosEntidadesNotaFiscalQuery) One(ctx context.Context, exec boil.ContextExecutor) (*GeracaoContatosEntidadesNotaFiscal, error) {
	o := &GeracaoContatosEntidadesNotaFiscal{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for geracao_contatos_entidades_nota_fiscal")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all GeracaoContatosEntidadesNotaFiscal records from the query.
func (q geracaoContatosEntidadesNotaFiscalQuery) All(ctx context.Context, exec boil.ContextExecutor) (GeracaoContatosEntidadesNotaFiscalSlice, error) {
	var o []*GeracaoContatosEntidadesNotaFiscal

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to GeracaoContatosEntidadesNotaFiscal slice")
	}

	if len(geracaoContatosEntidadesNotaFiscalAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all GeracaoContatosEntidadesNotaFiscal records in the query.
func (q geracaoContatosEntidadesNotaFiscalQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count geracao_contatos_entidades_nota_fiscal rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q geracaoContatosEntidadesNotaFiscalQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if geracao_contatos_entidades_nota_fiscal exists")
	}

	return count > 0, nil
}

// Cent pointed to by the foreign key.
func (o *GeracaoContatosEntidadesNotaFiscal) Cent(mods ...qm.QueryMod) contratoEntidadeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cent_id` = ?", o.CentID),
	}

	queryMods = append(queryMods, mods...)

	return ContratoEntidades(queryMods...)
}

// Ent pointed to by the foreign key.
func (o *GeracaoContatosEntidadesNotaFiscal) Ent(mods ...qm.QueryMod) entidadeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ent_id` = ?", o.EntID),
	}

	queryMods = append(queryMods, mods...)

	return Entidades(queryMods...)
}

// Gce pointed to by the foreign key.
func (o *GeracaoContatosEntidadesNotaFiscal) Gce(mods ...qm.QueryMod) geracaoContatosEntidadeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`gce_id` = ?", o.GceID),
	}

	queryMods = append(queryMods, mods...)

	return GeracaoContatosEntidades(queryMods...)
}

// NTF pointed to by the foreign key.
func (o *GeracaoContatosEntidadesNotaFiscal) NTF(mods ...qm.QueryMod) notaFiscalQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ntf_id` = ?", o.NTFID),
	}

	queryMods = append(queryMods, mods...)

	return NotaFiscals(queryMods...)
}

// LoadCent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (geracaoContatosEntidadesNotaFiscalL) LoadCent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeracaoContatosEntidadesNotaFiscal interface{}, mods queries.Applicator) error {
	var slice []*GeracaoContatosEntidadesNotaFiscal
	var object *GeracaoContatosEntidadesNotaFiscal

	if singular {
		var ok bool
		object, ok = maybeGeracaoContatosEntidadesNotaFiscal.(*GeracaoContatosEntidadesNotaFiscal)
		if !ok {
			object = new(GeracaoContatosEntidadesNotaFiscal)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeracaoContatosEntidadesNotaFiscal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeracaoContatosEntidadesNotaFiscal))
			}
		}
	} else {
		s, ok := maybeGeracaoContatosEntidadesNotaFiscal.(*[]*GeracaoContatosEntidadesNotaFiscal)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeracaoContatosEntidadesNotaFiscal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeracaoContatosEntidadesNotaFiscal))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &geracaoContatosEntidadesNotaFiscalR{}
		}
		args[object.CentID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geracaoContatosEntidadesNotaFiscalR{}
			}

			args[obj.CentID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`contrato_entidade`),
		qm.WhereIn(`contrato_entidade.cent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ContratoEntidade")
	}

	var resultSlice []*ContratoEntidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ContratoEntidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for contrato_entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for contrato_entidade")
	}

	if len(contratoEntidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cent = foreign
		if foreign.R == nil {
			foreign.R = &contratoEntidadeR{}
		}
		foreign.R.CentGeracaoContatosEntidadesNotaFiscals = append(foreign.R.CentGeracaoContatosEntidadesNotaFiscals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CentID == foreign.CentID {
				local.R.Cent = foreign
				if foreign.R == nil {
					foreign.R = &contratoEntidadeR{}
				}
				foreign.R.CentGeracaoContatosEntidadesNotaFiscals = append(foreign.R.CentGeracaoContatosEntidadesNotaFiscals, local)
				break
			}
		}
	}

	return nil
}

// LoadEnt allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (geracaoContatosEntidadesNotaFiscalL) LoadEnt(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeracaoContatosEntidadesNotaFiscal interface{}, mods queries.Applicator) error {
	var slice []*GeracaoContatosEntidadesNotaFiscal
	var object *GeracaoContatosEntidadesNotaFiscal

	if singular {
		var ok bool
		object, ok = maybeGeracaoContatosEntidadesNotaFiscal.(*GeracaoContatosEntidadesNotaFiscal)
		if !ok {
			object = new(GeracaoContatosEntidadesNotaFiscal)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeracaoContatosEntidadesNotaFiscal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeracaoContatosEntidadesNotaFiscal))
			}
		}
	} else {
		s, ok := maybeGeracaoContatosEntidadesNotaFiscal.(*[]*GeracaoContatosEntidadesNotaFiscal)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeracaoContatosEntidadesNotaFiscal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeracaoContatosEntidadesNotaFiscal))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &geracaoContatosEntidadesNotaFiscalR{}
		}
		args[object.EntID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geracaoContatosEntidadesNotaFiscalR{}
			}

			args[obj.EntID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade`),
		qm.WhereIn(`entidade.ent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Entidade")
	}

	var resultSlice []*Entidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade")
	}

	if len(entidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ent = foreign
		if foreign.R == nil {
			foreign.R = &entidadeR{}
		}
		foreign.R.EntGeracaoContatosEntidadesNotaFiscals = append(foreign.R.EntGeracaoContatosEntidadesNotaFiscals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EntID == foreign.EntID {
				local.R.Ent = foreign
				if foreign.R == nil {
					foreign.R = &entidadeR{}
				}
				foreign.R.EntGeracaoContatosEntidadesNotaFiscals = append(foreign.R.EntGeracaoContatosEntidadesNotaFiscals, local)
				break
			}
		}
	}

	return nil
}

// LoadGce allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (geracaoContatosEntidadesNotaFiscalL) LoadGce(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeracaoContatosEntidadesNotaFiscal interface{}, mods queries.Applicator) error {
	var slice []*GeracaoContatosEntidadesNotaFiscal
	var object *GeracaoContatosEntidadesNotaFiscal

	if singular {
		var ok bool
		object, ok = maybeGeracaoContatosEntidadesNotaFiscal.(*GeracaoContatosEntidadesNotaFiscal)
		if !ok {
			object = new(GeracaoContatosEntidadesNotaFiscal)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeracaoContatosEntidadesNotaFiscal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeracaoContatosEntidadesNotaFiscal))
			}
		}
	} else {
		s, ok := maybeGeracaoContatosEntidadesNotaFiscal.(*[]*GeracaoContatosEntidadesNotaFiscal)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeracaoContatosEntidadesNotaFiscal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeracaoContatosEntidadesNotaFiscal))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &geracaoContatosEntidadesNotaFiscalR{}
		}
		args[object.GceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geracaoContatosEntidadesNotaFiscalR{}
			}

			args[obj.GceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`geracao_contatos_entidades`),
		qm.WhereIn(`geracao_contatos_entidades.gce_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load GeracaoContatosEntidade")
	}

	var resultSlice []*GeracaoContatosEntidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice GeracaoContatosEntidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for geracao_contatos_entidades")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for geracao_contatos_entidades")
	}

	if len(geracaoContatosEntidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Gce = foreign
		if foreign.R == nil {
			foreign.R = &geracaoContatosEntidadeR{}
		}
		foreign.R.GceGeracaoContatosEntidadesNotaFiscals = append(foreign.R.GceGeracaoContatosEntidadesNotaFiscals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.GceID == foreign.GceID {
				local.R.Gce = foreign
				if foreign.R == nil {
					foreign.R = &geracaoContatosEntidadeR{}
				}
				foreign.R.GceGeracaoContatosEntidadesNotaFiscals = append(foreign.R.GceGeracaoContatosEntidadesNotaFiscals, local)
				break
			}
		}
	}

	return nil
}

// LoadNTF allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (geracaoContatosEntidadesNotaFiscalL) LoadNTF(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeracaoContatosEntidadesNotaFiscal interface{}, mods queries.Applicator) error {
	var slice []*GeracaoContatosEntidadesNotaFiscal
	var object *GeracaoContatosEntidadesNotaFiscal

	if singular {
		var ok bool
		object, ok = maybeGeracaoContatosEntidadesNotaFiscal.(*GeracaoContatosEntidadesNotaFiscal)
		if !ok {
			object = new(GeracaoContatosEntidadesNotaFiscal)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeracaoContatosEntidadesNotaFiscal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeracaoContatosEntidadesNotaFiscal))
			}
		}
	} else {
		s, ok := maybeGeracaoContatosEntidadesNotaFiscal.(*[]*GeracaoContatosEntidadesNotaFiscal)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeracaoContatosEntidadesNotaFiscal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeracaoContatosEntidadesNotaFiscal))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &geracaoContatosEntidadesNotaFiscalR{}
		}
		if !queries.IsNil(object.NTFID) {
			args[object.NTFID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geracaoContatosEntidadesNotaFiscalR{}
			}

			if !queries.IsNil(obj.NTFID) {
				args[obj.NTFID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal`),
		qm.WhereIn(`nota_fiscal.ntf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load NotaFiscal")
	}

	var resultSlice []*NotaFiscal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice NotaFiscal")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for nota_fiscal")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal")
	}

	if len(notaFiscalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.NTF = foreign
		if foreign.R == nil {
			foreign.R = &notaFiscalR{}
		}
		foreign.R.NTFGeracaoContatosEntidadesNotaFiscals = append(foreign.R.NTFGeracaoContatosEntidadesNotaFiscals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.NTFID, foreign.NTFID) {
				local.R.NTF = foreign
				if foreign.R == nil {
					foreign.R = &notaFiscalR{}
				}
				foreign.R.NTFGeracaoContatosEntidadesNotaFiscals = append(foreign.R.NTFGeracaoContatosEntidadesNotaFiscals, local)
				break
			}
		}
	}

	return nil
}

// SetCent of the geracaoContatosEntidadesNotaFiscal to the related item.
// Sets o.R.Cent to related.
// Adds o to related.R.CentGeracaoContatosEntidadesNotaFiscals.
func (o *GeracaoContatosEntidadesNotaFiscal) SetCent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ContratoEntidade) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `geracao_contatos_entidades_nota_fiscal` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cent_id"}),
		strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns),
	)
	values := []interface{}{related.CentID, o.GceID, o.GcenfID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CentID = related.CentID
	if o.R == nil {
		o.R = &geracaoContatosEntidadesNotaFiscalR{
			Cent: related,
		}
	} else {
		o.R.Cent = related
	}

	if related.R == nil {
		related.R = &contratoEntidadeR{
			CentGeracaoContatosEntidadesNotaFiscals: GeracaoContatosEntidadesNotaFiscalSlice{o},
		}
	} else {
		related.R.CentGeracaoContatosEntidadesNotaFiscals = append(related.R.CentGeracaoContatosEntidadesNotaFiscals, o)
	}

	return nil
}

// SetEnt of the geracaoContatosEntidadesNotaFiscal to the related item.
// Sets o.R.Ent to related.
// Adds o to related.R.EntGeracaoContatosEntidadesNotaFiscals.
func (o *GeracaoContatosEntidadesNotaFiscal) SetEnt(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Entidade) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `geracao_contatos_entidades_nota_fiscal` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ent_id"}),
		strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns),
	)
	values := []interface{}{related.EntID, o.GceID, o.GcenfID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EntID = related.EntID
	if o.R == nil {
		o.R = &geracaoContatosEntidadesNotaFiscalR{
			Ent: related,
		}
	} else {
		o.R.Ent = related
	}

	if related.R == nil {
		related.R = &entidadeR{
			EntGeracaoContatosEntidadesNotaFiscals: GeracaoContatosEntidadesNotaFiscalSlice{o},
		}
	} else {
		related.R.EntGeracaoContatosEntidadesNotaFiscals = append(related.R.EntGeracaoContatosEntidadesNotaFiscals, o)
	}

	return nil
}

// SetGce of the geracaoContatosEntidadesNotaFiscal to the related item.
// Sets o.R.Gce to related.
// Adds o to related.R.GceGeracaoContatosEntidadesNotaFiscals.
func (o *GeracaoContatosEntidadesNotaFiscal) SetGce(ctx context.Context, exec boil.ContextExecutor, insert bool, related *GeracaoContatosEntidade) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `geracao_contatos_entidades_nota_fiscal` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"gce_id"}),
		strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns),
	)
	values := []interface{}{related.GceID, o.GceID, o.GcenfID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.GceID = related.GceID
	if o.R == nil {
		o.R = &geracaoContatosEntidadesNotaFiscalR{
			Gce: related,
		}
	} else {
		o.R.Gce = related
	}

	if related.R == nil {
		related.R = &geracaoContatosEntidadeR{
			GceGeracaoContatosEntidadesNotaFiscals: GeracaoContatosEntidadesNotaFiscalSlice{o},
		}
	} else {
		related.R.GceGeracaoContatosEntidadesNotaFiscals = append(related.R.GceGeracaoContatosEntidadesNotaFiscals, o)
	}

	return nil
}

// SetNTF of the geracaoContatosEntidadesNotaFiscal to the related item.
// Sets o.R.NTF to related.
// Adds o to related.R.NTFGeracaoContatosEntidadesNotaFiscals.
func (o *GeracaoContatosEntidadesNotaFiscal) SetNTF(ctx context.Context, exec boil.ContextExecutor, insert bool, related *NotaFiscal) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `geracao_contatos_entidades_nota_fiscal` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ntf_id"}),
		strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns),
	)
	values := []interface{}{related.NTFID, o.GceID, o.GcenfID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.NTFID, related.NTFID)
	if o.R == nil {
		o.R = &geracaoContatosEntidadesNotaFiscalR{
			NTF: related,
		}
	} else {
		o.R.NTF = related
	}

	if related.R == nil {
		related.R = &notaFiscalR{
			NTFGeracaoContatosEntidadesNotaFiscals: GeracaoContatosEntidadesNotaFiscalSlice{o},
		}
	} else {
		related.R.NTFGeracaoContatosEntidadesNotaFiscals = append(related.R.NTFGeracaoContatosEntidadesNotaFiscals, o)
	}

	return nil
}

// RemoveNTF relationship.
// Sets o.R.NTF to nil.
// Removes o from all passed in related items' relationships struct.
func (o *GeracaoContatosEntidadesNotaFiscal) RemoveNTF(ctx context.Context, exec boil.ContextExecutor, related *NotaFiscal) error {
	var err error

	queries.SetScanner(&o.NTFID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("ntf_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.NTF = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.NTFGeracaoContatosEntidadesNotaFiscals {
		if queries.Equal(o.NTFID, ri.NTFID) {
			continue
		}

		ln := len(related.R.NTFGeracaoContatosEntidadesNotaFiscals)
		if ln > 1 && i < ln-1 {
			related.R.NTFGeracaoContatosEntidadesNotaFiscals[i] = related.R.NTFGeracaoContatosEntidadesNotaFiscals[ln-1]
		}
		related.R.NTFGeracaoContatosEntidadesNotaFiscals = related.R.NTFGeracaoContatosEntidadesNotaFiscals[:ln-1]
		break
	}
	return nil
}

// GeracaoContatosEntidadesNotaFiscals retrieves all the records using an executor.
func GeracaoContatosEntidadesNotaFiscals(mods ...qm.QueryMod) geracaoContatosEntidadesNotaFiscalQuery {
	mods = append(mods, qm.From("`geracao_contatos_entidades_nota_fiscal`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`geracao_contatos_entidades_nota_fiscal`.*"})
	}

	return geracaoContatosEntidadesNotaFiscalQuery{q}
}

// FindGeracaoContatosEntidadesNotaFiscal retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindGeracaoContatosEntidadesNotaFiscal(ctx context.Context, exec boil.ContextExecutor, gceID int, gcenfID int, selectCols ...string) (*GeracaoContatosEntidadesNotaFiscal, error) {
	geracaoContatosEntidadesNotaFiscalObj := &GeracaoContatosEntidadesNotaFiscal{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `geracao_contatos_entidades_nota_fiscal` where `gce_id`=? AND `gcenf_id`=?", sel,
	)

	q := queries.Raw(query, gceID, gcenfID)

	err := q.Bind(ctx, exec, geracaoContatosEntidadesNotaFiscalObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from geracao_contatos_entidades_nota_fiscal")
	}

	if err = geracaoContatosEntidadesNotaFiscalObj.doAfterSelectHooks(ctx, exec); err != nil {
		return geracaoContatosEntidadesNotaFiscalObj, err
	}

	return geracaoContatosEntidadesNotaFiscalObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *GeracaoContatosEntidadesNotaFiscal) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no geracao_contatos_entidades_nota_fiscal provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(geracaoContatosEntidadesNotaFiscalColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	geracaoContatosEntidadesNotaFiscalInsertCacheMut.RLock()
	cache, cached := geracaoContatosEntidadesNotaFiscalInsertCache[key]
	geracaoContatosEntidadesNotaFiscalInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			geracaoContatosEntidadesNotaFiscalAllColumns,
			geracaoContatosEntidadesNotaFiscalColumnsWithDefault,
			geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalType, geracaoContatosEntidadesNotaFiscalMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalType, geracaoContatosEntidadesNotaFiscalMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `geracao_contatos_entidades_nota_fiscal` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `geracao_contatos_entidades_nota_fiscal` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `geracao_contatos_entidades_nota_fiscal` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into geracao_contatos_entidades_nota_fiscal")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.GceID,
		o.GcenfID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for geracao_contatos_entidades_nota_fiscal")
	}

CacheNoHooks:
	if !cached {
		geracaoContatosEntidadesNotaFiscalInsertCacheMut.Lock()
		geracaoContatosEntidadesNotaFiscalInsertCache[key] = cache
		geracaoContatosEntidadesNotaFiscalInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the GeracaoContatosEntidadesNotaFiscal.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *GeracaoContatosEntidadesNotaFiscal) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	geracaoContatosEntidadesNotaFiscalUpdateCacheMut.RLock()
	cache, cached := geracaoContatosEntidadesNotaFiscalUpdateCache[key]
	geracaoContatosEntidadesNotaFiscalUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			geracaoContatosEntidadesNotaFiscalAllColumns,
			geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update geracao_contatos_entidades_nota_fiscal, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `geracao_contatos_entidades_nota_fiscal` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalType, geracaoContatosEntidadesNotaFiscalMapping, append(wl, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update geracao_contatos_entidades_nota_fiscal row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for geracao_contatos_entidades_nota_fiscal")
	}

	if !cached {
		geracaoContatosEntidadesNotaFiscalUpdateCacheMut.Lock()
		geracaoContatosEntidadesNotaFiscalUpdateCache[key] = cache
		geracaoContatosEntidadesNotaFiscalUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q geracaoContatosEntidadesNotaFiscalQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for geracao_contatos_entidades_nota_fiscal")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for geracao_contatos_entidades_nota_fiscal")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o GeracaoContatosEntidadesNotaFiscalSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadesNotaFiscalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `geracao_contatos_entidades_nota_fiscal` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in geracaoContatosEntidadesNotaFiscal slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all geracaoContatosEntidadesNotaFiscal")
	}
	return rowsAff, nil
}

var mySQLGeracaoContatosEntidadesNotaFiscalUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *GeracaoContatosEntidadesNotaFiscal) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no geracao_contatos_entidades_nota_fiscal provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(geracaoContatosEntidadesNotaFiscalColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLGeracaoContatosEntidadesNotaFiscalUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	geracaoContatosEntidadesNotaFiscalUpsertCacheMut.RLock()
	cache, cached := geracaoContatosEntidadesNotaFiscalUpsertCache[key]
	geracaoContatosEntidadesNotaFiscalUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			geracaoContatosEntidadesNotaFiscalAllColumns,
			geracaoContatosEntidadesNotaFiscalColumnsWithDefault,
			geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			geracaoContatosEntidadesNotaFiscalAllColumns,
			geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert geracao_contatos_entidades_nota_fiscal, could not build update column list")
		}

		ret := strmangle.SetComplement(geracaoContatosEntidadesNotaFiscalAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`geracao_contatos_entidades_nota_fiscal`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `geracao_contatos_entidades_nota_fiscal` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalType, geracaoContatosEntidadesNotaFiscalMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalType, geracaoContatosEntidadesNotaFiscalMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for geracao_contatos_entidades_nota_fiscal")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalType, geracaoContatosEntidadesNotaFiscalMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for geracao_contatos_entidades_nota_fiscal")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for geracao_contatos_entidades_nota_fiscal")
	}

CacheNoHooks:
	if !cached {
		geracaoContatosEntidadesNotaFiscalUpsertCacheMut.Lock()
		geracaoContatosEntidadesNotaFiscalUpsertCache[key] = cache
		geracaoContatosEntidadesNotaFiscalUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single GeracaoContatosEntidadesNotaFiscal record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *GeracaoContatosEntidadesNotaFiscal) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no GeracaoContatosEntidadesNotaFiscal provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), geracaoContatosEntidadesNotaFiscalPrimaryKeyMapping)
	sql := "DELETE FROM `geracao_contatos_entidades_nota_fiscal` WHERE `gce_id`=? AND `gcenf_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from geracao_contatos_entidades_nota_fiscal")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for geracao_contatos_entidades_nota_fiscal")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q geracaoContatosEntidadesNotaFiscalQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no geracaoContatosEntidadesNotaFiscalQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from geracao_contatos_entidades_nota_fiscal")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for geracao_contatos_entidades_nota_fiscal")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o GeracaoContatosEntidadesNotaFiscalSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(geracaoContatosEntidadesNotaFiscalBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadesNotaFiscalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `geracao_contatos_entidades_nota_fiscal` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from geracaoContatosEntidadesNotaFiscal slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for geracao_contatos_entidades_nota_fiscal")
	}

	if len(geracaoContatosEntidadesNotaFiscalAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *GeracaoContatosEntidadesNotaFiscal) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindGeracaoContatosEntidadesNotaFiscal(ctx, exec, o.GceID, o.GcenfID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GeracaoContatosEntidadesNotaFiscalSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := GeracaoContatosEntidadesNotaFiscalSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadesNotaFiscalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `geracao_contatos_entidades_nota_fiscal`.* FROM `geracao_contatos_entidades_nota_fiscal` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in GeracaoContatosEntidadesNotaFiscalSlice")
	}

	*o = slice

	return nil
}

// GeracaoContatosEntidadesNotaFiscalExists checks if the GeracaoContatosEntidadesNotaFiscal row exists.
func GeracaoContatosEntidadesNotaFiscalExists(ctx context.Context, exec boil.ContextExecutor, gceID int, gcenfID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `geracao_contatos_entidades_nota_fiscal` where `gce_id`=? AND `gcenf_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, gceID, gcenfID)
	}
	row := exec.QueryRowContext(ctx, sql, gceID, gcenfID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if geracao_contatos_entidades_nota_fiscal exists")
	}

	return exists, nil
}

// Exists checks if the GeracaoContatosEntidadesNotaFiscal row exists.
func (o *GeracaoContatosEntidadesNotaFiscal) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return GeracaoContatosEntidadesNotaFiscalExists(ctx, exec, o.GceID, o.GcenfID)
}
