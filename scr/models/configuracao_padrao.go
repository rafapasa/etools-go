// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ConfiguracaoPadrao is an object representing the database table.
type ConfiguracaoPadrao struct {
	CFGPDRID        int         `boil:"cfgpdr_id" json:"cfgpdr_id" toml:"cfgpdr_id" yaml:"cfgpdr_id"`
	ModID           int16       `boil:"mod_id" json:"mod_id" toml:"mod_id" yaml:"mod_id"`
	CFGPDRNome      string      `boil:"cfgpdr_nome" json:"cfgpdr_nome" toml:"cfgpdr_nome" yaml:"cfgpdr_nome"`
	CFGPDRValor     string      `boil:"cfgpdr_valor" json:"cfgpdr_valor" toml:"cfgpdr_valor" yaml:"cfgpdr_valor"`
	CFGPDRDatatype  int         `boil:"cfgpdr_datatype" json:"cfgpdr_datatype" toml:"cfgpdr_datatype" yaml:"cfgpdr_datatype"`
	CFGPDRDescricao null.String `boil:"cfgpdr_descricao" json:"cfgpdr_descricao,omitempty" toml:"cfgpdr_descricao" yaml:"cfgpdr_descricao,omitempty"`

	R *configuracaoPadraoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L configuracaoPadraoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ConfiguracaoPadraoColumns = struct {
	CFGPDRID        string
	ModID           string
	CFGPDRNome      string
	CFGPDRValor     string
	CFGPDRDatatype  string
	CFGPDRDescricao string
}{
	CFGPDRID:        "cfgpdr_id",
	ModID:           "mod_id",
	CFGPDRNome:      "cfgpdr_nome",
	CFGPDRValor:     "cfgpdr_valor",
	CFGPDRDatatype:  "cfgpdr_datatype",
	CFGPDRDescricao: "cfgpdr_descricao",
}

var ConfiguracaoPadraoTableColumns = struct {
	CFGPDRID        string
	ModID           string
	CFGPDRNome      string
	CFGPDRValor     string
	CFGPDRDatatype  string
	CFGPDRDescricao string
}{
	CFGPDRID:        "configuracao_padrao.cfgpdr_id",
	ModID:           "configuracao_padrao.mod_id",
	CFGPDRNome:      "configuracao_padrao.cfgpdr_nome",
	CFGPDRValor:     "configuracao_padrao.cfgpdr_valor",
	CFGPDRDatatype:  "configuracao_padrao.cfgpdr_datatype",
	CFGPDRDescricao: "configuracao_padrao.cfgpdr_descricao",
}

// Generated where

var ConfiguracaoPadraoWhere = struct {
	CFGPDRID        whereHelperint
	ModID           whereHelperint16
	CFGPDRNome      whereHelperstring
	CFGPDRValor     whereHelperstring
	CFGPDRDatatype  whereHelperint
	CFGPDRDescricao whereHelpernull_String
}{
	CFGPDRID:        whereHelperint{field: "`configuracao_padrao`.`cfgpdr_id`"},
	ModID:           whereHelperint16{field: "`configuracao_padrao`.`mod_id`"},
	CFGPDRNome:      whereHelperstring{field: "`configuracao_padrao`.`cfgpdr_nome`"},
	CFGPDRValor:     whereHelperstring{field: "`configuracao_padrao`.`cfgpdr_valor`"},
	CFGPDRDatatype:  whereHelperint{field: "`configuracao_padrao`.`cfgpdr_datatype`"},
	CFGPDRDescricao: whereHelpernull_String{field: "`configuracao_padrao`.`cfgpdr_descricao`"},
}

// ConfiguracaoPadraoRels is where relationship names are stored.
var ConfiguracaoPadraoRels = struct {
	Mod string
}{
	Mod: "Mod",
}

// configuracaoPadraoR is where relationships are stored.
type configuracaoPadraoR struct {
	Mod *Modulo `boil:"Mod" json:"Mod" toml:"Mod" yaml:"Mod"`
}

// NewStruct creates a new relationship struct
func (*configuracaoPadraoR) NewStruct() *configuracaoPadraoR {
	return &configuracaoPadraoR{}
}

func (r *configuracaoPadraoR) GetMod() *Modulo {
	if r == nil {
		return nil
	}
	return r.Mod
}

// configuracaoPadraoL is where Load methods for each relationship are stored.
type configuracaoPadraoL struct{}

var (
	configuracaoPadraoAllColumns            = []string{"cfgpdr_id", "mod_id", "cfgpdr_nome", "cfgpdr_valor", "cfgpdr_datatype", "cfgpdr_descricao"}
	configuracaoPadraoColumnsWithoutDefault = []string{"cfgpdr_id", "mod_id", "cfgpdr_nome", "cfgpdr_valor", "cfgpdr_datatype", "cfgpdr_descricao"}
	configuracaoPadraoColumnsWithDefault    = []string{}
	configuracaoPadraoPrimaryKeyColumns     = []string{"cfgpdr_id"}
	configuracaoPadraoGeneratedColumns      = []string{}
)

type (
	// ConfiguracaoPadraoSlice is an alias for a slice of pointers to ConfiguracaoPadrao.
	// This should almost always be used instead of []ConfiguracaoPadrao.
	ConfiguracaoPadraoSlice []*ConfiguracaoPadrao
	// ConfiguracaoPadraoHook is the signature for custom ConfiguracaoPadrao hook methods
	ConfiguracaoPadraoHook func(context.Context, boil.ContextExecutor, *ConfiguracaoPadrao) error

	configuracaoPadraoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	configuracaoPadraoType                 = reflect.TypeOf(&ConfiguracaoPadrao{})
	configuracaoPadraoMapping              = queries.MakeStructMapping(configuracaoPadraoType)
	configuracaoPadraoPrimaryKeyMapping, _ = queries.BindMapping(configuracaoPadraoType, configuracaoPadraoMapping, configuracaoPadraoPrimaryKeyColumns)
	configuracaoPadraoInsertCacheMut       sync.RWMutex
	configuracaoPadraoInsertCache          = make(map[string]insertCache)
	configuracaoPadraoUpdateCacheMut       sync.RWMutex
	configuracaoPadraoUpdateCache          = make(map[string]updateCache)
	configuracaoPadraoUpsertCacheMut       sync.RWMutex
	configuracaoPadraoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var configuracaoPadraoAfterSelectMu sync.Mutex
var configuracaoPadraoAfterSelectHooks []ConfiguracaoPadraoHook

var configuracaoPadraoBeforeInsertMu sync.Mutex
var configuracaoPadraoBeforeInsertHooks []ConfiguracaoPadraoHook
var configuracaoPadraoAfterInsertMu sync.Mutex
var configuracaoPadraoAfterInsertHooks []ConfiguracaoPadraoHook

var configuracaoPadraoBeforeUpdateMu sync.Mutex
var configuracaoPadraoBeforeUpdateHooks []ConfiguracaoPadraoHook
var configuracaoPadraoAfterUpdateMu sync.Mutex
var configuracaoPadraoAfterUpdateHooks []ConfiguracaoPadraoHook

var configuracaoPadraoBeforeDeleteMu sync.Mutex
var configuracaoPadraoBeforeDeleteHooks []ConfiguracaoPadraoHook
var configuracaoPadraoAfterDeleteMu sync.Mutex
var configuracaoPadraoAfterDeleteHooks []ConfiguracaoPadraoHook

var configuracaoPadraoBeforeUpsertMu sync.Mutex
var configuracaoPadraoBeforeUpsertHooks []ConfiguracaoPadraoHook
var configuracaoPadraoAfterUpsertMu sync.Mutex
var configuracaoPadraoAfterUpsertHooks []ConfiguracaoPadraoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ConfiguracaoPadrao) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ConfiguracaoPadrao) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ConfiguracaoPadrao) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ConfiguracaoPadrao) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ConfiguracaoPadrao) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ConfiguracaoPadrao) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ConfiguracaoPadrao) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ConfiguracaoPadrao) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ConfiguracaoPadrao) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configuracaoPadraoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddConfiguracaoPadraoHook registers your hook function for all future operations.
func AddConfiguracaoPadraoHook(hookPoint boil.HookPoint, configuracaoPadraoHook ConfiguracaoPadraoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		configuracaoPadraoAfterSelectMu.Lock()
		configuracaoPadraoAfterSelectHooks = append(configuracaoPadraoAfterSelectHooks, configuracaoPadraoHook)
		configuracaoPadraoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		configuracaoPadraoBeforeInsertMu.Lock()
		configuracaoPadraoBeforeInsertHooks = append(configuracaoPadraoBeforeInsertHooks, configuracaoPadraoHook)
		configuracaoPadraoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		configuracaoPadraoAfterInsertMu.Lock()
		configuracaoPadraoAfterInsertHooks = append(configuracaoPadraoAfterInsertHooks, configuracaoPadraoHook)
		configuracaoPadraoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		configuracaoPadraoBeforeUpdateMu.Lock()
		configuracaoPadraoBeforeUpdateHooks = append(configuracaoPadraoBeforeUpdateHooks, configuracaoPadraoHook)
		configuracaoPadraoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		configuracaoPadraoAfterUpdateMu.Lock()
		configuracaoPadraoAfterUpdateHooks = append(configuracaoPadraoAfterUpdateHooks, configuracaoPadraoHook)
		configuracaoPadraoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		configuracaoPadraoBeforeDeleteMu.Lock()
		configuracaoPadraoBeforeDeleteHooks = append(configuracaoPadraoBeforeDeleteHooks, configuracaoPadraoHook)
		configuracaoPadraoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		configuracaoPadraoAfterDeleteMu.Lock()
		configuracaoPadraoAfterDeleteHooks = append(configuracaoPadraoAfterDeleteHooks, configuracaoPadraoHook)
		configuracaoPadraoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		configuracaoPadraoBeforeUpsertMu.Lock()
		configuracaoPadraoBeforeUpsertHooks = append(configuracaoPadraoBeforeUpsertHooks, configuracaoPadraoHook)
		configuracaoPadraoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		configuracaoPadraoAfterUpsertMu.Lock()
		configuracaoPadraoAfterUpsertHooks = append(configuracaoPadraoAfterUpsertHooks, configuracaoPadraoHook)
		configuracaoPadraoAfterUpsertMu.Unlock()
	}
}

// One returns a single configuracaoPadrao record from the query.
func (q configuracaoPadraoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ConfiguracaoPadrao, error) {
	o := &ConfiguracaoPadrao{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for configuracao_padrao")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ConfiguracaoPadrao records from the query.
func (q configuracaoPadraoQuery) All(ctx context.Context, exec boil.ContextExecutor) (ConfiguracaoPadraoSlice, error) {
	var o []*ConfiguracaoPadrao

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ConfiguracaoPadrao slice")
	}

	if len(configuracaoPadraoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ConfiguracaoPadrao records in the query.
func (q configuracaoPadraoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count configuracao_padrao rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q configuracaoPadraoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if configuracao_padrao exists")
	}

	return count > 0, nil
}

// Mod pointed to by the foreign key.
func (o *ConfiguracaoPadrao) Mod(mods ...qm.QueryMod) moduloQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`mod_id` = ?", o.ModID),
	}

	queryMods = append(queryMods, mods...)

	return Modulos(queryMods...)
}

// LoadMod allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (configuracaoPadraoL) LoadMod(ctx context.Context, e boil.ContextExecutor, singular bool, maybeConfiguracaoPadrao interface{}, mods queries.Applicator) error {
	var slice []*ConfiguracaoPadrao
	var object *ConfiguracaoPadrao

	if singular {
		var ok bool
		object, ok = maybeConfiguracaoPadrao.(*ConfiguracaoPadrao)
		if !ok {
			object = new(ConfiguracaoPadrao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeConfiguracaoPadrao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeConfiguracaoPadrao))
			}
		}
	} else {
		s, ok := maybeConfiguracaoPadrao.(*[]*ConfiguracaoPadrao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeConfiguracaoPadrao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeConfiguracaoPadrao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &configuracaoPadraoR{}
		}
		args[object.ModID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &configuracaoPadraoR{}
			}

			args[obj.ModID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`modulos`),
		qm.WhereIn(`modulos.mod_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Modulo")
	}

	var resultSlice []*Modulo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Modulo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for modulos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for modulos")
	}

	if len(moduloAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Mod = foreign
		if foreign.R == nil {
			foreign.R = &moduloR{}
		}
		foreign.R.ModConfiguracaoPadraos = append(foreign.R.ModConfiguracaoPadraos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ModID == foreign.ModID {
				local.R.Mod = foreign
				if foreign.R == nil {
					foreign.R = &moduloR{}
				}
				foreign.R.ModConfiguracaoPadraos = append(foreign.R.ModConfiguracaoPadraos, local)
				break
			}
		}
	}

	return nil
}

// SetMod of the configuracaoPadrao to the related item.
// Sets o.R.Mod to related.
// Adds o to related.R.ModConfiguracaoPadraos.
func (o *ConfiguracaoPadrao) SetMod(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Modulo) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `configuracao_padrao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"mod_id"}),
		strmangle.WhereClause("`", "`", 0, configuracaoPadraoPrimaryKeyColumns),
	)
	values := []interface{}{related.ModID, o.CFGPDRID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ModID = related.ModID
	if o.R == nil {
		o.R = &configuracaoPadraoR{
			Mod: related,
		}
	} else {
		o.R.Mod = related
	}

	if related.R == nil {
		related.R = &moduloR{
			ModConfiguracaoPadraos: ConfiguracaoPadraoSlice{o},
		}
	} else {
		related.R.ModConfiguracaoPadraos = append(related.R.ModConfiguracaoPadraos, o)
	}

	return nil
}

// ConfiguracaoPadraos retrieves all the records using an executor.
func ConfiguracaoPadraos(mods ...qm.QueryMod) configuracaoPadraoQuery {
	mods = append(mods, qm.From("`configuracao_padrao`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`configuracao_padrao`.*"})
	}

	return configuracaoPadraoQuery{q}
}

// FindConfiguracaoPadrao retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindConfiguracaoPadrao(ctx context.Context, exec boil.ContextExecutor, cFGPDRID int, selectCols ...string) (*ConfiguracaoPadrao, error) {
	configuracaoPadraoObj := &ConfiguracaoPadrao{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `configuracao_padrao` where `cfgpdr_id`=?", sel,
	)

	q := queries.Raw(query, cFGPDRID)

	err := q.Bind(ctx, exec, configuracaoPadraoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from configuracao_padrao")
	}

	if err = configuracaoPadraoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return configuracaoPadraoObj, err
	}

	return configuracaoPadraoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ConfiguracaoPadrao) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no configuracao_padrao provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(configuracaoPadraoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	configuracaoPadraoInsertCacheMut.RLock()
	cache, cached := configuracaoPadraoInsertCache[key]
	configuracaoPadraoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			configuracaoPadraoAllColumns,
			configuracaoPadraoColumnsWithDefault,
			configuracaoPadraoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(configuracaoPadraoType, configuracaoPadraoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(configuracaoPadraoType, configuracaoPadraoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `configuracao_padrao` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `configuracao_padrao` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `configuracao_padrao` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, configuracaoPadraoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into configuracao_padrao")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CFGPDRID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for configuracao_padrao")
	}

CacheNoHooks:
	if !cached {
		configuracaoPadraoInsertCacheMut.Lock()
		configuracaoPadraoInsertCache[key] = cache
		configuracaoPadraoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ConfiguracaoPadrao.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ConfiguracaoPadrao) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	configuracaoPadraoUpdateCacheMut.RLock()
	cache, cached := configuracaoPadraoUpdateCache[key]
	configuracaoPadraoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			configuracaoPadraoAllColumns,
			configuracaoPadraoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update configuracao_padrao, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `configuracao_padrao` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, configuracaoPadraoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(configuracaoPadraoType, configuracaoPadraoMapping, append(wl, configuracaoPadraoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update configuracao_padrao row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for configuracao_padrao")
	}

	if !cached {
		configuracaoPadraoUpdateCacheMut.Lock()
		configuracaoPadraoUpdateCache[key] = cache
		configuracaoPadraoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q configuracaoPadraoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for configuracao_padrao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for configuracao_padrao")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ConfiguracaoPadraoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), configuracaoPadraoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `configuracao_padrao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, configuracaoPadraoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in configuracaoPadrao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all configuracaoPadrao")
	}
	return rowsAff, nil
}

var mySQLConfiguracaoPadraoUniqueColumns = []string{
	"cfgpdr_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ConfiguracaoPadrao) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no configuracao_padrao provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(configuracaoPadraoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLConfiguracaoPadraoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	configuracaoPadraoUpsertCacheMut.RLock()
	cache, cached := configuracaoPadraoUpsertCache[key]
	configuracaoPadraoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			configuracaoPadraoAllColumns,
			configuracaoPadraoColumnsWithDefault,
			configuracaoPadraoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			configuracaoPadraoAllColumns,
			configuracaoPadraoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert configuracao_padrao, could not build update column list")
		}

		ret := strmangle.SetComplement(configuracaoPadraoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`configuracao_padrao`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `configuracao_padrao` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(configuracaoPadraoType, configuracaoPadraoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(configuracaoPadraoType, configuracaoPadraoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for configuracao_padrao")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(configuracaoPadraoType, configuracaoPadraoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for configuracao_padrao")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for configuracao_padrao")
	}

CacheNoHooks:
	if !cached {
		configuracaoPadraoUpsertCacheMut.Lock()
		configuracaoPadraoUpsertCache[key] = cache
		configuracaoPadraoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ConfiguracaoPadrao record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ConfiguracaoPadrao) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ConfiguracaoPadrao provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), configuracaoPadraoPrimaryKeyMapping)
	sql := "DELETE FROM `configuracao_padrao` WHERE `cfgpdr_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from configuracao_padrao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for configuracao_padrao")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q configuracaoPadraoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no configuracaoPadraoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from configuracao_padrao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for configuracao_padrao")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ConfiguracaoPadraoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(configuracaoPadraoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), configuracaoPadraoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `configuracao_padrao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, configuracaoPadraoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from configuracaoPadrao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for configuracao_padrao")
	}

	if len(configuracaoPadraoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ConfiguracaoPadrao) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindConfiguracaoPadrao(ctx, exec, o.CFGPDRID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ConfiguracaoPadraoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ConfiguracaoPadraoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), configuracaoPadraoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `configuracao_padrao`.* FROM `configuracao_padrao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, configuracaoPadraoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ConfiguracaoPadraoSlice")
	}

	*o = slice

	return nil
}

// ConfiguracaoPadraoExists checks if the ConfiguracaoPadrao row exists.
func ConfiguracaoPadraoExists(ctx context.Context, exec boil.ContextExecutor, cFGPDRID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `configuracao_padrao` where `cfgpdr_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cFGPDRID)
	}
	row := exec.QueryRowContext(ctx, sql, cFGPDRID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if configuracao_padrao exists")
	}

	return exists, nil
}

// Exists checks if the ConfiguracaoPadrao row exists.
func (o *ConfiguracaoPadrao) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ConfiguracaoPadraoExists(ctx, exec, o.CFGPDRID)
}
