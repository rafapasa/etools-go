// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testMensagemAlerta(t *testing.T) {
	t.Parallel()

	query := MensagemAlerta()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testMensagemAlertaDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMensagemAlertaQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := MensagemAlerta().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMensagemAlertaSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := MensagemAlertumSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMensagemAlertaExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := MensagemAlertumExists(ctx, tx, o.MsgaltID)
	if err != nil {
		t.Errorf("Unable to check if MensagemAlertum exists: %s", err)
	}
	if !e {
		t.Errorf("Expected MensagemAlertumExists to return true, but got false.")
	}
}

func testMensagemAlertaFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	mensagemAlertumFound, err := FindMensagemAlertum(ctx, tx, o.MsgaltID)
	if err != nil {
		t.Error(err)
	}

	if mensagemAlertumFound == nil {
		t.Error("want a record, got nil")
	}
}

func testMensagemAlertaBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = MensagemAlerta().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testMensagemAlertaOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := MensagemAlerta().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testMensagemAlertaAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	mensagemAlertumOne := &MensagemAlertum{}
	mensagemAlertumTwo := &MensagemAlertum{}
	if err = randomize.Struct(seed, mensagemAlertumOne, mensagemAlertumDBTypes, false, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}
	if err = randomize.Struct(seed, mensagemAlertumTwo, mensagemAlertumDBTypes, false, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = mensagemAlertumOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = mensagemAlertumTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := MensagemAlerta().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testMensagemAlertaCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	mensagemAlertumOne := &MensagemAlertum{}
	mensagemAlertumTwo := &MensagemAlertum{}
	if err = randomize.Struct(seed, mensagemAlertumOne, mensagemAlertumDBTypes, false, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}
	if err = randomize.Struct(seed, mensagemAlertumTwo, mensagemAlertumDBTypes, false, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = mensagemAlertumOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = mensagemAlertumTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func mensagemAlertumBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func mensagemAlertumAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func mensagemAlertumAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func mensagemAlertumBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func mensagemAlertumAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func mensagemAlertumBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func mensagemAlertumAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func mensagemAlertumBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func mensagemAlertumAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *MensagemAlertum) error {
	*o = MensagemAlertum{}
	return nil
}

func testMensagemAlertaHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &MensagemAlertum{}
	o := &MensagemAlertum{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, false); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum object: %s", err)
	}

	AddMensagemAlertumHook(boil.BeforeInsertHook, mensagemAlertumBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumBeforeInsertHooks = []MensagemAlertumHook{}

	AddMensagemAlertumHook(boil.AfterInsertHook, mensagemAlertumAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumAfterInsertHooks = []MensagemAlertumHook{}

	AddMensagemAlertumHook(boil.AfterSelectHook, mensagemAlertumAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumAfterSelectHooks = []MensagemAlertumHook{}

	AddMensagemAlertumHook(boil.BeforeUpdateHook, mensagemAlertumBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumBeforeUpdateHooks = []MensagemAlertumHook{}

	AddMensagemAlertumHook(boil.AfterUpdateHook, mensagemAlertumAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumAfterUpdateHooks = []MensagemAlertumHook{}

	AddMensagemAlertumHook(boil.BeforeDeleteHook, mensagemAlertumBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumBeforeDeleteHooks = []MensagemAlertumHook{}

	AddMensagemAlertumHook(boil.AfterDeleteHook, mensagemAlertumAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumAfterDeleteHooks = []MensagemAlertumHook{}

	AddMensagemAlertumHook(boil.BeforeUpsertHook, mensagemAlertumBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumBeforeUpsertHooks = []MensagemAlertumHook{}

	AddMensagemAlertumHook(boil.AfterUpsertHook, mensagemAlertumAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	mensagemAlertumAfterUpsertHooks = []MensagemAlertumHook{}
}

func testMensagemAlertaInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testMensagemAlertaInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(mensagemAlertumColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testMensagemAlertumToOneAlertasUsingAlt(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local MensagemAlertum
	var foreign Alertas

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, mensagemAlertumDBTypes, false, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, alertasDBTypes, false, alertasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Alertas struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.AltID = foreign.AltID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Alt().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.AltID != foreign.AltID {
		t.Errorf("want: %v, got %v", foreign.AltID, check.AltID)
	}

	ranAfterSelectHook := false
	AddAlertasHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Alertas) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := MensagemAlertumSlice{&local}
	if err = local.L.LoadAlt(ctx, tx, false, (*[]*MensagemAlertum)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Alt == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Alt = nil
	if err = local.L.LoadAlt(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Alt == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testMensagemAlertumToOneEntidadeUsingEnt(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local MensagemAlertum
	var foreign Entidade

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, mensagemAlertumDBTypes, false, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, entidadeDBTypes, false, entidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Entidade struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.EntID = foreign.EntID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Ent().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.EntID != foreign.EntID {
		t.Errorf("want: %v, got %v", foreign.EntID, check.EntID)
	}

	ranAfterSelectHook := false
	AddEntidadeHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Entidade) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := MensagemAlertumSlice{&local}
	if err = local.L.LoadEnt(ctx, tx, false, (*[]*MensagemAlertum)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Ent == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Ent = nil
	if err = local.L.LoadEnt(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Ent == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testMensagemAlertumToOneSetOpAlertasUsingAlt(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a MensagemAlertum
	var b, c Alertas

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, mensagemAlertumDBTypes, false, strmangle.SetComplement(mensagemAlertumPrimaryKeyColumns, mensagemAlertumColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, alertasDBTypes, false, strmangle.SetComplement(alertasPrimaryKeyColumns, alertasColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, alertasDBTypes, false, strmangle.SetComplement(alertasPrimaryKeyColumns, alertasColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Alertas{&b, &c} {
		err = a.SetAlt(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Alt != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.AltMensagemAlerta[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.AltID != x.AltID {
			t.Error("foreign key was wrong value", a.AltID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.AltID))
		reflect.Indirect(reflect.ValueOf(&a.AltID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.AltID != x.AltID {
			t.Error("foreign key was wrong value", a.AltID, x.AltID)
		}
	}
}
func testMensagemAlertumToOneSetOpEntidadeUsingEnt(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a MensagemAlertum
	var b, c Entidade

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, mensagemAlertumDBTypes, false, strmangle.SetComplement(mensagemAlertumPrimaryKeyColumns, mensagemAlertumColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, entidadeDBTypes, false, strmangle.SetComplement(entidadePrimaryKeyColumns, entidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, entidadeDBTypes, false, strmangle.SetComplement(entidadePrimaryKeyColumns, entidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Entidade{&b, &c} {
		err = a.SetEnt(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Ent != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.EntMensagemAlerta[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.EntID != x.EntID {
			t.Error("foreign key was wrong value", a.EntID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.EntID))
		reflect.Indirect(reflect.ValueOf(&a.EntID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.EntID != x.EntID {
			t.Error("foreign key was wrong value", a.EntID, x.EntID)
		}
	}
}

func testMensagemAlertaReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testMensagemAlertaReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := MensagemAlertumSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testMensagemAlertaSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := MensagemAlerta().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	mensagemAlertumDBTypes = map[string]string{`MsgaltID`: `int`, `AltID`: `int`, `EntID`: `int`, `MsgaltMensagem`: `varchar`, `MsgaltDatahora`: `datetime`, `MsgaltSituacao`: `int`, `MsgaltSMS`: `int`, `MsgaltEmail`: `int`, `MsgaltWhatsapp`: `int`, `MsgaltInformacao`: `varchar`}
	_                      = bytes.MinRead
)

func testMensagemAlertaUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(mensagemAlertumPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(mensagemAlertumAllColumns) == len(mensagemAlertumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testMensagemAlertaSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(mensagemAlertumAllColumns) == len(mensagemAlertumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &MensagemAlertum{}
	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, mensagemAlertumDBTypes, true, mensagemAlertumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(mensagemAlertumAllColumns, mensagemAlertumPrimaryKeyColumns) {
		fields = mensagemAlertumAllColumns
	} else {
		fields = strmangle.SetComplement(
			mensagemAlertumAllColumns,
			mensagemAlertumPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := MensagemAlertumSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testMensagemAlertaUpsert(t *testing.T) {
	t.Parallel()

	if len(mensagemAlertumAllColumns) == len(mensagemAlertumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLMensagemAlertumUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := MensagemAlertum{}
	if err = randomize.Struct(seed, &o, mensagemAlertumDBTypes, false); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert MensagemAlertum: %s", err)
	}

	count, err := MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, mensagemAlertumDBTypes, false, mensagemAlertumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MensagemAlertum struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert MensagemAlertum: %s", err)
	}

	count, err = MensagemAlerta().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
