// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// OrcamentoVendaSituacao is an object representing the database table.
type OrcamentoVendaSituacao struct {
	OrcvsID        int    `boil:"orcvs_id" json:"orcvs_id" toml:"orcvs_id" yaml:"orcvs_id"`
	OrcvsDescricao string `boil:"orcvs_descricao" json:"orcvs_descricao" toml:"orcvs_descricao" yaml:"orcvs_descricao"`

	R *orcamentoVendaSituacaoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L orcamentoVendaSituacaoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var OrcamentoVendaSituacaoColumns = struct {
	OrcvsID        string
	OrcvsDescricao string
}{
	OrcvsID:        "orcvs_id",
	OrcvsDescricao: "orcvs_descricao",
}

var OrcamentoVendaSituacaoTableColumns = struct {
	OrcvsID        string
	OrcvsDescricao string
}{
	OrcvsID:        "orcamento_venda_situacao.orcvs_id",
	OrcvsDescricao: "orcamento_venda_situacao.orcvs_descricao",
}

// Generated where

var OrcamentoVendaSituacaoWhere = struct {
	OrcvsID        whereHelperint
	OrcvsDescricao whereHelperstring
}{
	OrcvsID:        whereHelperint{field: "`orcamento_venda_situacao`.`orcvs_id`"},
	OrcvsDescricao: whereHelperstring{field: "`orcamento_venda_situacao`.`orcvs_descricao`"},
}

// OrcamentoVendaSituacaoRels is where relationship names are stored.
var OrcamentoVendaSituacaoRels = struct {
	OrcvOrcamentoVendas string
}{
	OrcvOrcamentoVendas: "OrcvOrcamentoVendas",
}

// orcamentoVendaSituacaoR is where relationships are stored.
type orcamentoVendaSituacaoR struct {
	OrcvOrcamentoVendas OrcamentoVendaSlice `boil:"OrcvOrcamentoVendas" json:"OrcvOrcamentoVendas" toml:"OrcvOrcamentoVendas" yaml:"OrcvOrcamentoVendas"`
}

// NewStruct creates a new relationship struct
func (*orcamentoVendaSituacaoR) NewStruct() *orcamentoVendaSituacaoR {
	return &orcamentoVendaSituacaoR{}
}

func (r *orcamentoVendaSituacaoR) GetOrcvOrcamentoVendas() OrcamentoVendaSlice {
	if r == nil {
		return nil
	}
	return r.OrcvOrcamentoVendas
}

// orcamentoVendaSituacaoL is where Load methods for each relationship are stored.
type orcamentoVendaSituacaoL struct{}

var (
	orcamentoVendaSituacaoAllColumns            = []string{"orcvs_id", "orcvs_descricao"}
	orcamentoVendaSituacaoColumnsWithoutDefault = []string{"orcvs_id", "orcvs_descricao"}
	orcamentoVendaSituacaoColumnsWithDefault    = []string{}
	orcamentoVendaSituacaoPrimaryKeyColumns     = []string{"orcvs_id"}
	orcamentoVendaSituacaoGeneratedColumns      = []string{}
)

type (
	// OrcamentoVendaSituacaoSlice is an alias for a slice of pointers to OrcamentoVendaSituacao.
	// This should almost always be used instead of []OrcamentoVendaSituacao.
	OrcamentoVendaSituacaoSlice []*OrcamentoVendaSituacao
	// OrcamentoVendaSituacaoHook is the signature for custom OrcamentoVendaSituacao hook methods
	OrcamentoVendaSituacaoHook func(context.Context, boil.ContextExecutor, *OrcamentoVendaSituacao) error

	orcamentoVendaSituacaoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	orcamentoVendaSituacaoType                 = reflect.TypeOf(&OrcamentoVendaSituacao{})
	orcamentoVendaSituacaoMapping              = queries.MakeStructMapping(orcamentoVendaSituacaoType)
	orcamentoVendaSituacaoPrimaryKeyMapping, _ = queries.BindMapping(orcamentoVendaSituacaoType, orcamentoVendaSituacaoMapping, orcamentoVendaSituacaoPrimaryKeyColumns)
	orcamentoVendaSituacaoInsertCacheMut       sync.RWMutex
	orcamentoVendaSituacaoInsertCache          = make(map[string]insertCache)
	orcamentoVendaSituacaoUpdateCacheMut       sync.RWMutex
	orcamentoVendaSituacaoUpdateCache          = make(map[string]updateCache)
	orcamentoVendaSituacaoUpsertCacheMut       sync.RWMutex
	orcamentoVendaSituacaoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var orcamentoVendaSituacaoAfterSelectMu sync.Mutex
var orcamentoVendaSituacaoAfterSelectHooks []OrcamentoVendaSituacaoHook

var orcamentoVendaSituacaoBeforeInsertMu sync.Mutex
var orcamentoVendaSituacaoBeforeInsertHooks []OrcamentoVendaSituacaoHook
var orcamentoVendaSituacaoAfterInsertMu sync.Mutex
var orcamentoVendaSituacaoAfterInsertHooks []OrcamentoVendaSituacaoHook

var orcamentoVendaSituacaoBeforeUpdateMu sync.Mutex
var orcamentoVendaSituacaoBeforeUpdateHooks []OrcamentoVendaSituacaoHook
var orcamentoVendaSituacaoAfterUpdateMu sync.Mutex
var orcamentoVendaSituacaoAfterUpdateHooks []OrcamentoVendaSituacaoHook

var orcamentoVendaSituacaoBeforeDeleteMu sync.Mutex
var orcamentoVendaSituacaoBeforeDeleteHooks []OrcamentoVendaSituacaoHook
var orcamentoVendaSituacaoAfterDeleteMu sync.Mutex
var orcamentoVendaSituacaoAfterDeleteHooks []OrcamentoVendaSituacaoHook

var orcamentoVendaSituacaoBeforeUpsertMu sync.Mutex
var orcamentoVendaSituacaoBeforeUpsertHooks []OrcamentoVendaSituacaoHook
var orcamentoVendaSituacaoAfterUpsertMu sync.Mutex
var orcamentoVendaSituacaoAfterUpsertHooks []OrcamentoVendaSituacaoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *OrcamentoVendaSituacao) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *OrcamentoVendaSituacao) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *OrcamentoVendaSituacao) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *OrcamentoVendaSituacao) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *OrcamentoVendaSituacao) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *OrcamentoVendaSituacao) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *OrcamentoVendaSituacao) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *OrcamentoVendaSituacao) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *OrcamentoVendaSituacao) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoVendaSituacaoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddOrcamentoVendaSituacaoHook registers your hook function for all future operations.
func AddOrcamentoVendaSituacaoHook(hookPoint boil.HookPoint, orcamentoVendaSituacaoHook OrcamentoVendaSituacaoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		orcamentoVendaSituacaoAfterSelectMu.Lock()
		orcamentoVendaSituacaoAfterSelectHooks = append(orcamentoVendaSituacaoAfterSelectHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		orcamentoVendaSituacaoBeforeInsertMu.Lock()
		orcamentoVendaSituacaoBeforeInsertHooks = append(orcamentoVendaSituacaoBeforeInsertHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		orcamentoVendaSituacaoAfterInsertMu.Lock()
		orcamentoVendaSituacaoAfterInsertHooks = append(orcamentoVendaSituacaoAfterInsertHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		orcamentoVendaSituacaoBeforeUpdateMu.Lock()
		orcamentoVendaSituacaoBeforeUpdateHooks = append(orcamentoVendaSituacaoBeforeUpdateHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		orcamentoVendaSituacaoAfterUpdateMu.Lock()
		orcamentoVendaSituacaoAfterUpdateHooks = append(orcamentoVendaSituacaoAfterUpdateHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		orcamentoVendaSituacaoBeforeDeleteMu.Lock()
		orcamentoVendaSituacaoBeforeDeleteHooks = append(orcamentoVendaSituacaoBeforeDeleteHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		orcamentoVendaSituacaoAfterDeleteMu.Lock()
		orcamentoVendaSituacaoAfterDeleteHooks = append(orcamentoVendaSituacaoAfterDeleteHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		orcamentoVendaSituacaoBeforeUpsertMu.Lock()
		orcamentoVendaSituacaoBeforeUpsertHooks = append(orcamentoVendaSituacaoBeforeUpsertHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		orcamentoVendaSituacaoAfterUpsertMu.Lock()
		orcamentoVendaSituacaoAfterUpsertHooks = append(orcamentoVendaSituacaoAfterUpsertHooks, orcamentoVendaSituacaoHook)
		orcamentoVendaSituacaoAfterUpsertMu.Unlock()
	}
}

// One returns a single orcamentoVendaSituacao record from the query.
func (q orcamentoVendaSituacaoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*OrcamentoVendaSituacao, error) {
	o := &OrcamentoVendaSituacao{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for orcamento_venda_situacao")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all OrcamentoVendaSituacao records from the query.
func (q orcamentoVendaSituacaoQuery) All(ctx context.Context, exec boil.ContextExecutor) (OrcamentoVendaSituacaoSlice, error) {
	var o []*OrcamentoVendaSituacao

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to OrcamentoVendaSituacao slice")
	}

	if len(orcamentoVendaSituacaoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all OrcamentoVendaSituacao records in the query.
func (q orcamentoVendaSituacaoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count orcamento_venda_situacao rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q orcamentoVendaSituacaoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if orcamento_venda_situacao exists")
	}

	return count > 0, nil
}

// OrcvOrcamentoVendas retrieves all the orcamento_venda's OrcamentoVendas with an executor via orcvs_id column.
func (o *OrcamentoVendaSituacao) OrcvOrcamentoVendas(mods ...qm.QueryMod) orcamentoVendaQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`orcamento_venda`.`orcvs_id`=?", o.OrcvsID),
	)

	return OrcamentoVendas(queryMods...)
}

// LoadOrcvOrcamentoVendas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (orcamentoVendaSituacaoL) LoadOrcvOrcamentoVendas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOrcamentoVendaSituacao interface{}, mods queries.Applicator) error {
	var slice []*OrcamentoVendaSituacao
	var object *OrcamentoVendaSituacao

	if singular {
		var ok bool
		object, ok = maybeOrcamentoVendaSituacao.(*OrcamentoVendaSituacao)
		if !ok {
			object = new(OrcamentoVendaSituacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeOrcamentoVendaSituacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeOrcamentoVendaSituacao))
			}
		}
	} else {
		s, ok := maybeOrcamentoVendaSituacao.(*[]*OrcamentoVendaSituacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeOrcamentoVendaSituacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeOrcamentoVendaSituacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &orcamentoVendaSituacaoR{}
		}
		args[object.OrcvsID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &orcamentoVendaSituacaoR{}
			}
			args[obj.OrcvsID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`orcamento_venda`),
		qm.WhereIn(`orcamento_venda.orcvs_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load orcamento_venda")
	}

	var resultSlice []*OrcamentoVenda
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice orcamento_venda")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on orcamento_venda")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for orcamento_venda")
	}

	if len(orcamentoVendaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.OrcvOrcamentoVendas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &orcamentoVendaR{}
			}
			foreign.R.Orcv = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.OrcvsID == foreign.OrcvsID {
				local.R.OrcvOrcamentoVendas = append(local.R.OrcvOrcamentoVendas, foreign)
				if foreign.R == nil {
					foreign.R = &orcamentoVendaR{}
				}
				foreign.R.Orcv = local
				break
			}
		}
	}

	return nil
}

// AddOrcvOrcamentoVendas adds the given related objects to the existing relationships
// of the orcamento_venda_situacao, optionally inserting them as new records.
// Appends related to o.R.OrcvOrcamentoVendas.
// Sets related.R.Orcv appropriately.
func (o *OrcamentoVendaSituacao) AddOrcvOrcamentoVendas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*OrcamentoVenda) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.OrcvsID = o.OrcvsID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `orcamento_venda` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"orcvs_id"}),
				strmangle.WhereClause("`", "`", 0, orcamentoVendaPrimaryKeyColumns),
			)
			values := []interface{}{o.OrcvsID, rel.OrcvID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.OrcvsID = o.OrcvsID
		}
	}

	if o.R == nil {
		o.R = &orcamentoVendaSituacaoR{
			OrcvOrcamentoVendas: related,
		}
	} else {
		o.R.OrcvOrcamentoVendas = append(o.R.OrcvOrcamentoVendas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &orcamentoVendaR{
				Orcv: o,
			}
		} else {
			rel.R.Orcv = o
		}
	}
	return nil
}

// OrcamentoVendaSituacaos retrieves all the records using an executor.
func OrcamentoVendaSituacaos(mods ...qm.QueryMod) orcamentoVendaSituacaoQuery {
	mods = append(mods, qm.From("`orcamento_venda_situacao`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`orcamento_venda_situacao`.*"})
	}

	return orcamentoVendaSituacaoQuery{q}
}

// FindOrcamentoVendaSituacao retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindOrcamentoVendaSituacao(ctx context.Context, exec boil.ContextExecutor, orcvsID int, selectCols ...string) (*OrcamentoVendaSituacao, error) {
	orcamentoVendaSituacaoObj := &OrcamentoVendaSituacao{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `orcamento_venda_situacao` where `orcvs_id`=?", sel,
	)

	q := queries.Raw(query, orcvsID)

	err := q.Bind(ctx, exec, orcamentoVendaSituacaoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from orcamento_venda_situacao")
	}

	if err = orcamentoVendaSituacaoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return orcamentoVendaSituacaoObj, err
	}

	return orcamentoVendaSituacaoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *OrcamentoVendaSituacao) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no orcamento_venda_situacao provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(orcamentoVendaSituacaoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	orcamentoVendaSituacaoInsertCacheMut.RLock()
	cache, cached := orcamentoVendaSituacaoInsertCache[key]
	orcamentoVendaSituacaoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			orcamentoVendaSituacaoAllColumns,
			orcamentoVendaSituacaoColumnsWithDefault,
			orcamentoVendaSituacaoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(orcamentoVendaSituacaoType, orcamentoVendaSituacaoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(orcamentoVendaSituacaoType, orcamentoVendaSituacaoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `orcamento_venda_situacao` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `orcamento_venda_situacao` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `orcamento_venda_situacao` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, orcamentoVendaSituacaoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into orcamento_venda_situacao")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.OrcvsID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for orcamento_venda_situacao")
	}

CacheNoHooks:
	if !cached {
		orcamentoVendaSituacaoInsertCacheMut.Lock()
		orcamentoVendaSituacaoInsertCache[key] = cache
		orcamentoVendaSituacaoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the OrcamentoVendaSituacao.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *OrcamentoVendaSituacao) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	orcamentoVendaSituacaoUpdateCacheMut.RLock()
	cache, cached := orcamentoVendaSituacaoUpdateCache[key]
	orcamentoVendaSituacaoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			orcamentoVendaSituacaoAllColumns,
			orcamentoVendaSituacaoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update orcamento_venda_situacao, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `orcamento_venda_situacao` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, orcamentoVendaSituacaoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(orcamentoVendaSituacaoType, orcamentoVendaSituacaoMapping, append(wl, orcamentoVendaSituacaoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update orcamento_venda_situacao row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for orcamento_venda_situacao")
	}

	if !cached {
		orcamentoVendaSituacaoUpdateCacheMut.Lock()
		orcamentoVendaSituacaoUpdateCache[key] = cache
		orcamentoVendaSituacaoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q orcamentoVendaSituacaoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for orcamento_venda_situacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for orcamento_venda_situacao")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o OrcamentoVendaSituacaoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orcamentoVendaSituacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `orcamento_venda_situacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, orcamentoVendaSituacaoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in orcamentoVendaSituacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all orcamentoVendaSituacao")
	}
	return rowsAff, nil
}

var mySQLOrcamentoVendaSituacaoUniqueColumns = []string{
	"orcvs_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *OrcamentoVendaSituacao) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no orcamento_venda_situacao provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(orcamentoVendaSituacaoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLOrcamentoVendaSituacaoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	orcamentoVendaSituacaoUpsertCacheMut.RLock()
	cache, cached := orcamentoVendaSituacaoUpsertCache[key]
	orcamentoVendaSituacaoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			orcamentoVendaSituacaoAllColumns,
			orcamentoVendaSituacaoColumnsWithDefault,
			orcamentoVendaSituacaoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			orcamentoVendaSituacaoAllColumns,
			orcamentoVendaSituacaoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert orcamento_venda_situacao, could not build update column list")
		}

		ret := strmangle.SetComplement(orcamentoVendaSituacaoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`orcamento_venda_situacao`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `orcamento_venda_situacao` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(orcamentoVendaSituacaoType, orcamentoVendaSituacaoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(orcamentoVendaSituacaoType, orcamentoVendaSituacaoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for orcamento_venda_situacao")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(orcamentoVendaSituacaoType, orcamentoVendaSituacaoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for orcamento_venda_situacao")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for orcamento_venda_situacao")
	}

CacheNoHooks:
	if !cached {
		orcamentoVendaSituacaoUpsertCacheMut.Lock()
		orcamentoVendaSituacaoUpsertCache[key] = cache
		orcamentoVendaSituacaoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single OrcamentoVendaSituacao record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *OrcamentoVendaSituacao) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no OrcamentoVendaSituacao provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), orcamentoVendaSituacaoPrimaryKeyMapping)
	sql := "DELETE FROM `orcamento_venda_situacao` WHERE `orcvs_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from orcamento_venda_situacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for orcamento_venda_situacao")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q orcamentoVendaSituacaoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no orcamentoVendaSituacaoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from orcamento_venda_situacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for orcamento_venda_situacao")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o OrcamentoVendaSituacaoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(orcamentoVendaSituacaoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orcamentoVendaSituacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `orcamento_venda_situacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, orcamentoVendaSituacaoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from orcamentoVendaSituacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for orcamento_venda_situacao")
	}

	if len(orcamentoVendaSituacaoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *OrcamentoVendaSituacao) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindOrcamentoVendaSituacao(ctx, exec, o.OrcvsID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *OrcamentoVendaSituacaoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := OrcamentoVendaSituacaoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orcamentoVendaSituacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `orcamento_venda_situacao`.* FROM `orcamento_venda_situacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, orcamentoVendaSituacaoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in OrcamentoVendaSituacaoSlice")
	}

	*o = slice

	return nil
}

// OrcamentoVendaSituacaoExists checks if the OrcamentoVendaSituacao row exists.
func OrcamentoVendaSituacaoExists(ctx context.Context, exec boil.ContextExecutor, orcvsID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `orcamento_venda_situacao` where `orcvs_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, orcvsID)
	}
	row := exec.QueryRowContext(ctx, sql, orcvsID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if orcamento_venda_situacao exists")
	}

	return exists, nil
}

// Exists checks if the OrcamentoVendaSituacao row exists.
func (o *OrcamentoVendaSituacao) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return OrcamentoVendaSituacaoExists(ctx, exec, o.OrcvsID)
}
