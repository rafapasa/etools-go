// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testChequesRecebidos(t *testing.T) {
	t.Parallel()

	query := ChequesRecebidos()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testChequesRecebidosDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testChequesRecebidosQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := ChequesRecebidos().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testChequesRecebidosSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ChequesRecebidoSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testChequesRecebidosExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := ChequesRecebidoExists(ctx, tx, o.ChqrecID)
	if err != nil {
		t.Errorf("Unable to check if ChequesRecebido exists: %s", err)
	}
	if !e {
		t.Errorf("Expected ChequesRecebidoExists to return true, but got false.")
	}
}

func testChequesRecebidosFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	chequesRecebidoFound, err := FindChequesRecebido(ctx, tx, o.ChqrecID)
	if err != nil {
		t.Error(err)
	}

	if chequesRecebidoFound == nil {
		t.Error("want a record, got nil")
	}
}

func testChequesRecebidosBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = ChequesRecebidos().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testChequesRecebidosOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := ChequesRecebidos().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testChequesRecebidosAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	chequesRecebidoOne := &ChequesRecebido{}
	chequesRecebidoTwo := &ChequesRecebido{}
	if err = randomize.Struct(seed, chequesRecebidoOne, chequesRecebidoDBTypes, false, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}
	if err = randomize.Struct(seed, chequesRecebidoTwo, chequesRecebidoDBTypes, false, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = chequesRecebidoOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = chequesRecebidoTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ChequesRecebidos().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testChequesRecebidosCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	chequesRecebidoOne := &ChequesRecebido{}
	chequesRecebidoTwo := &ChequesRecebido{}
	if err = randomize.Struct(seed, chequesRecebidoOne, chequesRecebidoDBTypes, false, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}
	if err = randomize.Struct(seed, chequesRecebidoTwo, chequesRecebidoDBTypes, false, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = chequesRecebidoOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = chequesRecebidoTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func chequesRecebidoBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func chequesRecebidoAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func chequesRecebidoAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func chequesRecebidoBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func chequesRecebidoAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func chequesRecebidoBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func chequesRecebidoAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func chequesRecebidoBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func chequesRecebidoAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
	*o = ChequesRecebido{}
	return nil
}

func testChequesRecebidosHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &ChequesRecebido{}
	o := &ChequesRecebido{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, false); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido object: %s", err)
	}

	AddChequesRecebidoHook(boil.BeforeInsertHook, chequesRecebidoBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoBeforeInsertHooks = []ChequesRecebidoHook{}

	AddChequesRecebidoHook(boil.AfterInsertHook, chequesRecebidoAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoAfterInsertHooks = []ChequesRecebidoHook{}

	AddChequesRecebidoHook(boil.AfterSelectHook, chequesRecebidoAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoAfterSelectHooks = []ChequesRecebidoHook{}

	AddChequesRecebidoHook(boil.BeforeUpdateHook, chequesRecebidoBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoBeforeUpdateHooks = []ChequesRecebidoHook{}

	AddChequesRecebidoHook(boil.AfterUpdateHook, chequesRecebidoAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoAfterUpdateHooks = []ChequesRecebidoHook{}

	AddChequesRecebidoHook(boil.BeforeDeleteHook, chequesRecebidoBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoBeforeDeleteHooks = []ChequesRecebidoHook{}

	AddChequesRecebidoHook(boil.AfterDeleteHook, chequesRecebidoAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoAfterDeleteHooks = []ChequesRecebidoHook{}

	AddChequesRecebidoHook(boil.BeforeUpsertHook, chequesRecebidoBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoBeforeUpsertHooks = []ChequesRecebidoHook{}

	AddChequesRecebidoHook(boil.AfterUpsertHook, chequesRecebidoAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	chequesRecebidoAfterUpsertHooks = []ChequesRecebidoHook{}
}

func testChequesRecebidosInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testChequesRecebidosInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(chequesRecebidoColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testChequesRecebidoToManyChqrecChequeOcorenciaCheques(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c ChequeOcorenciaCheque

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, chequeOcorenciaChequeDBTypes, false, chequeOcorenciaChequeColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, chequeOcorenciaChequeDBTypes, false, chequeOcorenciaChequeColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.ChqrecID = a.ChqrecID
	c.ChqrecID = a.ChqrecID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.ChqrecChequeOcorenciaCheques().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.ChqrecID == b.ChqrecID {
			bFound = true
		}
		if v.ChqrecID == c.ChqrecID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := ChequesRecebidoSlice{&a}
	if err = a.L.LoadChqrecChequeOcorenciaCheques(ctx, tx, false, (*[]*ChequesRecebido)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ChqrecChequeOcorenciaCheques); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.ChqrecChequeOcorenciaCheques = nil
	if err = a.L.LoadChqrecChequeOcorenciaCheques(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ChqrecChequeOcorenciaCheques); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testChequesRecebidoToManyChqrecChequeTituloBaixas(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c ChequeTituloBaixa

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, chequeTituloBaixaDBTypes, false, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, chequeTituloBaixaDBTypes, false, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.ChqrecID = a.ChqrecID
	c.ChqrecID = a.ChqrecID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.ChqrecChequeTituloBaixas().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.ChqrecID == b.ChqrecID {
			bFound = true
		}
		if v.ChqrecID == c.ChqrecID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := ChequesRecebidoSlice{&a}
	if err = a.L.LoadChqrecChequeTituloBaixas(ctx, tx, false, (*[]*ChequesRecebido)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ChqrecChequeTituloBaixas); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.ChqrecChequeTituloBaixas = nil
	if err = a.L.LoadChqrecChequeTituloBaixas(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ChqrecChequeTituloBaixas); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testChequesRecebidoToManyChqrecLancamentoFinanceiros(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c LancamentoFinanceiro

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, lancamentoFinanceiroDBTypes, false, lancamentoFinanceiroColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, lancamentoFinanceiroDBTypes, false, lancamentoFinanceiroColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.ChqrecID, a.ChqrecID)
	queries.Assign(&c.ChqrecID, a.ChqrecID)
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.ChqrecLancamentoFinanceiros().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.ChqrecID, b.ChqrecID) {
			bFound = true
		}
		if queries.Equal(v.ChqrecID, c.ChqrecID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := ChequesRecebidoSlice{&a}
	if err = a.L.LoadChqrecLancamentoFinanceiros(ctx, tx, false, (*[]*ChequesRecebido)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ChqrecLancamentoFinanceiros); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.ChqrecLancamentoFinanceiros = nil
	if err = a.L.LoadChqrecLancamentoFinanceiros(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ChqrecLancamentoFinanceiros); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testChequesRecebidoToManyAddOpChqrecChequeOcorenciaCheques(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c, d, e ChequeOcorenciaCheque

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*ChequeOcorenciaCheque{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, chequeOcorenciaChequeDBTypes, false, strmangle.SetComplement(chequeOcorenciaChequePrimaryKeyColumns, chequeOcorenciaChequeColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*ChequeOcorenciaCheque{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddChqrecChequeOcorenciaCheques(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ChqrecID != first.ChqrecID {
			t.Error("foreign key was wrong value", a.ChqrecID, first.ChqrecID)
		}
		if a.ChqrecID != second.ChqrecID {
			t.Error("foreign key was wrong value", a.ChqrecID, second.ChqrecID)
		}

		if first.R.Chqrec != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Chqrec != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.ChqrecChequeOcorenciaCheques[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.ChqrecChequeOcorenciaCheques[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.ChqrecChequeOcorenciaCheques().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testChequesRecebidoToManyAddOpChqrecChequeTituloBaixas(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c, d, e ChequeTituloBaixa

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*ChequeTituloBaixa{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, chequeTituloBaixaDBTypes, false, strmangle.SetComplement(chequeTituloBaixaPrimaryKeyColumns, chequeTituloBaixaColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*ChequeTituloBaixa{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddChqrecChequeTituloBaixas(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ChqrecID != first.ChqrecID {
			t.Error("foreign key was wrong value", a.ChqrecID, first.ChqrecID)
		}
		if a.ChqrecID != second.ChqrecID {
			t.Error("foreign key was wrong value", a.ChqrecID, second.ChqrecID)
		}

		if first.R.Chqrec != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Chqrec != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.ChqrecChequeTituloBaixas[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.ChqrecChequeTituloBaixas[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.ChqrecChequeTituloBaixas().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testChequesRecebidoToManyAddOpChqrecLancamentoFinanceiros(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c, d, e LancamentoFinanceiro

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*LancamentoFinanceiro{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, lancamentoFinanceiroDBTypes, false, strmangle.SetComplement(lancamentoFinanceiroPrimaryKeyColumns, lancamentoFinanceiroColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*LancamentoFinanceiro{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddChqrecLancamentoFinanceiros(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ChqrecID, first.ChqrecID) {
			t.Error("foreign key was wrong value", a.ChqrecID, first.ChqrecID)
		}
		if !queries.Equal(a.ChqrecID, second.ChqrecID) {
			t.Error("foreign key was wrong value", a.ChqrecID, second.ChqrecID)
		}

		if first.R.Chqrec != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Chqrec != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.ChqrecLancamentoFinanceiros[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.ChqrecLancamentoFinanceiros[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.ChqrecLancamentoFinanceiros().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testChequesRecebidoToManySetOpChqrecLancamentoFinanceiros(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c, d, e LancamentoFinanceiro

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*LancamentoFinanceiro{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, lancamentoFinanceiroDBTypes, false, strmangle.SetComplement(lancamentoFinanceiroPrimaryKeyColumns, lancamentoFinanceiroColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetChqrecLancamentoFinanceiros(ctx, tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.ChqrecLancamentoFinanceiros().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetChqrecLancamentoFinanceiros(ctx, tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.ChqrecLancamentoFinanceiros().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.ChqrecID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.ChqrecID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ChqrecID, d.ChqrecID) {
		t.Error("foreign key was wrong value", a.ChqrecID, d.ChqrecID)
	}
	if !queries.Equal(a.ChqrecID, e.ChqrecID) {
		t.Error("foreign key was wrong value", a.ChqrecID, e.ChqrecID)
	}

	if b.R.Chqrec != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Chqrec != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Chqrec != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Chqrec != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.ChqrecLancamentoFinanceiros[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.ChqrecLancamentoFinanceiros[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testChequesRecebidoToManyRemoveOpChqrecLancamentoFinanceiros(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c, d, e LancamentoFinanceiro

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*LancamentoFinanceiro{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, lancamentoFinanceiroDBTypes, false, strmangle.SetComplement(lancamentoFinanceiroPrimaryKeyColumns, lancamentoFinanceiroColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddChqrecLancamentoFinanceiros(ctx, tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.ChqrecLancamentoFinanceiros().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveChqrecLancamentoFinanceiros(ctx, tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.ChqrecLancamentoFinanceiros().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.ChqrecID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.ChqrecID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Chqrec != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Chqrec != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Chqrec != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Chqrec != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.ChqrecLancamentoFinanceiros) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.ChqrecLancamentoFinanceiros[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.ChqrecLancamentoFinanceiros[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testChequesRecebidoToOneBancoUsingBan(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local ChequesRecebido
	var foreign Banco

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, chequesRecebidoDBTypes, false, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, bancoDBTypes, false, bancoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banco struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.BanID = foreign.BanID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Ban().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.BanID != foreign.BanID {
		t.Errorf("want: %v, got %v", foreign.BanID, check.BanID)
	}

	ranAfterSelectHook := false
	AddBancoHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Banco) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := ChequesRecebidoSlice{&local}
	if err = local.L.LoadBan(ctx, tx, false, (*[]*ChequesRecebido)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Ban == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Ban = nil
	if err = local.L.LoadBan(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Ban == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testChequesRecebidoToOneEmpresaFilialUsingEmf(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local ChequesRecebido
	var foreign EmpresaFilial

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, chequesRecebidoDBTypes, false, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, empresaFilialDBTypes, false, empresaFilialColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize EmpresaFilial struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.EmfID = foreign.EmfID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Emf().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.EmfID != foreign.EmfID {
		t.Errorf("want: %v, got %v", foreign.EmfID, check.EmfID)
	}

	ranAfterSelectHook := false
	AddEmpresaFilialHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *EmpresaFilial) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := ChequesRecebidoSlice{&local}
	if err = local.L.LoadEmf(ctx, tx, false, (*[]*ChequesRecebido)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Emf == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Emf = nil
	if err = local.L.LoadEmf(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Emf == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testChequesRecebidoToOneEntidadeUsingEnt(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local ChequesRecebido
	var foreign Entidade

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, chequesRecebidoDBTypes, false, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, entidadeDBTypes, false, entidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Entidade struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.EntID = foreign.EntID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Ent().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.EntID != foreign.EntID {
		t.Errorf("want: %v, got %v", foreign.EntID, check.EntID)
	}

	ranAfterSelectHook := false
	AddEntidadeHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Entidade) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := ChequesRecebidoSlice{&local}
	if err = local.L.LoadEnt(ctx, tx, false, (*[]*ChequesRecebido)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Ent == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Ent = nil
	if err = local.L.LoadEnt(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Ent == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testChequesRecebidoToOneRotinaContabilUsingRoc(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local ChequesRecebido
	var foreign RotinaContabil

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, rotinaContabilDBTypes, false, rotinaContabilColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RotinaContabil struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.RocID, foreign.RocID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Roc().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.RocID, foreign.RocID) {
		t.Errorf("want: %v, got %v", foreign.RocID, check.RocID)
	}

	ranAfterSelectHook := false
	AddRotinaContabilHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *RotinaContabil) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := ChequesRecebidoSlice{&local}
	if err = local.L.LoadRoc(ctx, tx, false, (*[]*ChequesRecebido)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Roc == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Roc = nil
	if err = local.L.LoadRoc(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Roc == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testChequesRecebidoToOneSetOpBancoUsingBan(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c Banco

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, bancoDBTypes, false, strmangle.SetComplement(bancoPrimaryKeyColumns, bancoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, bancoDBTypes, false, strmangle.SetComplement(bancoPrimaryKeyColumns, bancoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Banco{&b, &c} {
		err = a.SetBan(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Ban != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.BanChequesRecebidos[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.BanID != x.BanID {
			t.Error("foreign key was wrong value", a.BanID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.BanID))
		reflect.Indirect(reflect.ValueOf(&a.BanID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.BanID != x.BanID {
			t.Error("foreign key was wrong value", a.BanID, x.BanID)
		}
	}
}
func testChequesRecebidoToOneSetOpEmpresaFilialUsingEmf(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c EmpresaFilial

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, empresaFilialDBTypes, false, strmangle.SetComplement(empresaFilialPrimaryKeyColumns, empresaFilialColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, empresaFilialDBTypes, false, strmangle.SetComplement(empresaFilialPrimaryKeyColumns, empresaFilialColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*EmpresaFilial{&b, &c} {
		err = a.SetEmf(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Emf != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.EmfChequesRecebidos[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.EmfID != x.EmfID {
			t.Error("foreign key was wrong value", a.EmfID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.EmfID))
		reflect.Indirect(reflect.ValueOf(&a.EmfID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.EmfID != x.EmfID {
			t.Error("foreign key was wrong value", a.EmfID, x.EmfID)
		}
	}
}
func testChequesRecebidoToOneSetOpEntidadeUsingEnt(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c Entidade

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, entidadeDBTypes, false, strmangle.SetComplement(entidadePrimaryKeyColumns, entidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, entidadeDBTypes, false, strmangle.SetComplement(entidadePrimaryKeyColumns, entidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Entidade{&b, &c} {
		err = a.SetEnt(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Ent != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.EntChequesRecebidos[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.EntID != x.EntID {
			t.Error("foreign key was wrong value", a.EntID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.EntID))
		reflect.Indirect(reflect.ValueOf(&a.EntID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.EntID != x.EntID {
			t.Error("foreign key was wrong value", a.EntID, x.EntID)
		}
	}
}
func testChequesRecebidoToOneSetOpRotinaContabilUsingRoc(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b, c RotinaContabil

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, rotinaContabilDBTypes, false, strmangle.SetComplement(rotinaContabilPrimaryKeyColumns, rotinaContabilColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, rotinaContabilDBTypes, false, strmangle.SetComplement(rotinaContabilPrimaryKeyColumns, rotinaContabilColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*RotinaContabil{&b, &c} {
		err = a.SetRoc(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Roc != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.RocChequesRecebidos[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.RocID, x.RocID) {
			t.Error("foreign key was wrong value", a.RocID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.RocID))
		reflect.Indirect(reflect.ValueOf(&a.RocID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.RocID, x.RocID) {
			t.Error("foreign key was wrong value", a.RocID, x.RocID)
		}
	}
}

func testChequesRecebidoToOneRemoveOpRotinaContabilUsingRoc(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequesRecebido
	var b RotinaContabil

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, rotinaContabilDBTypes, false, strmangle.SetComplement(rotinaContabilPrimaryKeyColumns, rotinaContabilColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetRoc(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveRoc(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Roc().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Roc != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.RocID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.RocChequesRecebidos) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testChequesRecebidosReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testChequesRecebidosReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ChequesRecebidoSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testChequesRecebidosSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ChequesRecebidos().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	chequesRecebidoDBTypes = map[string]string{`ChqrecID`: `int`, `BanID`: `int`, `EmfID`: `int`, `EntID`: `int`, `RocID`: `int`, `ChqrecConta`: `varchar`, `ChqrecAgencia`: `varchar`, `ChqrecNumero`: `varchar`, `ChqrecValorcheque`: `decimal`, `ChqrecDataemissao`: `date`, `ChqrecDatapre`: `date`, `ChqrecDatacompensacao`: `date`, `ChqrecSituacao`: `smallint`, `ChqrecObservacao`: `text`, `ChqrecValorsaldo`: `decimal`}
	_                      = bytes.MinRead
)

func testChequesRecebidosUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(chequesRecebidoPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(chequesRecebidoAllColumns) == len(chequesRecebidoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testChequesRecebidosSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(chequesRecebidoAllColumns) == len(chequesRecebidoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ChequesRecebido{}
	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, chequesRecebidoDBTypes, true, chequesRecebidoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(chequesRecebidoAllColumns, chequesRecebidoPrimaryKeyColumns) {
		fields = chequesRecebidoAllColumns
	} else {
		fields = strmangle.SetComplement(
			chequesRecebidoAllColumns,
			chequesRecebidoPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := ChequesRecebidoSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testChequesRecebidosUpsert(t *testing.T) {
	t.Parallel()

	if len(chequesRecebidoAllColumns) == len(chequesRecebidoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLChequesRecebidoUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := ChequesRecebido{}
	if err = randomize.Struct(seed, &o, chequesRecebidoDBTypes, false); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ChequesRecebido: %s", err)
	}

	count, err := ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, chequesRecebidoDBTypes, false, chequesRecebidoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ChequesRecebido: %s", err)
	}

	count, err = ChequesRecebidos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
