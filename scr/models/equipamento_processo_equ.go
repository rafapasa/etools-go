// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// EquipamentoProcessoEqu is an object representing the database table.
type EquipamentoProcessoEqu struct {
	EpeID  int `boil:"epe_id" json:"epe_id" toml:"epe_id" yaml:"epe_id"`
	EquID  int `boil:"equ_id" json:"equ_id" toml:"equ_id" yaml:"equ_id"`
	PrceID int `boil:"prce_id" json:"prce_id" toml:"prce_id" yaml:"prce_id"`

	R *equipamentoProcessoEquR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L equipamentoProcessoEquL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EquipamentoProcessoEquColumns = struct {
	EpeID  string
	EquID  string
	PrceID string
}{
	EpeID:  "epe_id",
	EquID:  "equ_id",
	PrceID: "prce_id",
}

var EquipamentoProcessoEquTableColumns = struct {
	EpeID  string
	EquID  string
	PrceID string
}{
	EpeID:  "equipamento_processo_equ.epe_id",
	EquID:  "equipamento_processo_equ.equ_id",
	PrceID: "equipamento_processo_equ.prce_id",
}

// Generated where

var EquipamentoProcessoEquWhere = struct {
	EpeID  whereHelperint
	EquID  whereHelperint
	PrceID whereHelperint
}{
	EpeID:  whereHelperint{field: "`equipamento_processo_equ`.`epe_id`"},
	EquID:  whereHelperint{field: "`equipamento_processo_equ`.`equ_id`"},
	PrceID: whereHelperint{field: "`equipamento_processo_equ`.`prce_id`"},
}

// EquipamentoProcessoEquRels is where relationship names are stored.
var EquipamentoProcessoEquRels = struct {
	Equ                 string
	Prce                string
	EpeProdutoProcessos string
}{
	Equ:                 "Equ",
	Prce:                "Prce",
	EpeProdutoProcessos: "EpeProdutoProcessos",
}

// equipamentoProcessoEquR is where relationships are stored.
type equipamentoProcessoEquR struct {
	Equ                 *Equipamento         `boil:"Equ" json:"Equ" toml:"Equ" yaml:"Equ"`
	Prce                *ProcessoEquipamento `boil:"Prce" json:"Prce" toml:"Prce" yaml:"Prce"`
	EpeProdutoProcessos ProdutoProcessoSlice `boil:"EpeProdutoProcessos" json:"EpeProdutoProcessos" toml:"EpeProdutoProcessos" yaml:"EpeProdutoProcessos"`
}

// NewStruct creates a new relationship struct
func (*equipamentoProcessoEquR) NewStruct() *equipamentoProcessoEquR {
	return &equipamentoProcessoEquR{}
}

func (r *equipamentoProcessoEquR) GetEqu() *Equipamento {
	if r == nil {
		return nil
	}
	return r.Equ
}

func (r *equipamentoProcessoEquR) GetPrce() *ProcessoEquipamento {
	if r == nil {
		return nil
	}
	return r.Prce
}

func (r *equipamentoProcessoEquR) GetEpeProdutoProcessos() ProdutoProcessoSlice {
	if r == nil {
		return nil
	}
	return r.EpeProdutoProcessos
}

// equipamentoProcessoEquL is where Load methods for each relationship are stored.
type equipamentoProcessoEquL struct{}

var (
	equipamentoProcessoEquAllColumns            = []string{"epe_id", "equ_id", "prce_id"}
	equipamentoProcessoEquColumnsWithoutDefault = []string{"epe_id", "equ_id", "prce_id"}
	equipamentoProcessoEquColumnsWithDefault    = []string{}
	equipamentoProcessoEquPrimaryKeyColumns     = []string{"epe_id"}
	equipamentoProcessoEquGeneratedColumns      = []string{}
)

type (
	// EquipamentoProcessoEquSlice is an alias for a slice of pointers to EquipamentoProcessoEqu.
	// This should almost always be used instead of []EquipamentoProcessoEqu.
	EquipamentoProcessoEquSlice []*EquipamentoProcessoEqu
	// EquipamentoProcessoEquHook is the signature for custom EquipamentoProcessoEqu hook methods
	EquipamentoProcessoEquHook func(context.Context, boil.ContextExecutor, *EquipamentoProcessoEqu) error

	equipamentoProcessoEquQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	equipamentoProcessoEquType                 = reflect.TypeOf(&EquipamentoProcessoEqu{})
	equipamentoProcessoEquMapping              = queries.MakeStructMapping(equipamentoProcessoEquType)
	equipamentoProcessoEquPrimaryKeyMapping, _ = queries.BindMapping(equipamentoProcessoEquType, equipamentoProcessoEquMapping, equipamentoProcessoEquPrimaryKeyColumns)
	equipamentoProcessoEquInsertCacheMut       sync.RWMutex
	equipamentoProcessoEquInsertCache          = make(map[string]insertCache)
	equipamentoProcessoEquUpdateCacheMut       sync.RWMutex
	equipamentoProcessoEquUpdateCache          = make(map[string]updateCache)
	equipamentoProcessoEquUpsertCacheMut       sync.RWMutex
	equipamentoProcessoEquUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var equipamentoProcessoEquAfterSelectMu sync.Mutex
var equipamentoProcessoEquAfterSelectHooks []EquipamentoProcessoEquHook

var equipamentoProcessoEquBeforeInsertMu sync.Mutex
var equipamentoProcessoEquBeforeInsertHooks []EquipamentoProcessoEquHook
var equipamentoProcessoEquAfterInsertMu sync.Mutex
var equipamentoProcessoEquAfterInsertHooks []EquipamentoProcessoEquHook

var equipamentoProcessoEquBeforeUpdateMu sync.Mutex
var equipamentoProcessoEquBeforeUpdateHooks []EquipamentoProcessoEquHook
var equipamentoProcessoEquAfterUpdateMu sync.Mutex
var equipamentoProcessoEquAfterUpdateHooks []EquipamentoProcessoEquHook

var equipamentoProcessoEquBeforeDeleteMu sync.Mutex
var equipamentoProcessoEquBeforeDeleteHooks []EquipamentoProcessoEquHook
var equipamentoProcessoEquAfterDeleteMu sync.Mutex
var equipamentoProcessoEquAfterDeleteHooks []EquipamentoProcessoEquHook

var equipamentoProcessoEquBeforeUpsertMu sync.Mutex
var equipamentoProcessoEquBeforeUpsertHooks []EquipamentoProcessoEquHook
var equipamentoProcessoEquAfterUpsertMu sync.Mutex
var equipamentoProcessoEquAfterUpsertHooks []EquipamentoProcessoEquHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *EquipamentoProcessoEqu) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *EquipamentoProcessoEqu) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *EquipamentoProcessoEqu) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *EquipamentoProcessoEqu) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *EquipamentoProcessoEqu) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *EquipamentoProcessoEqu) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *EquipamentoProcessoEqu) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *EquipamentoProcessoEqu) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *EquipamentoProcessoEqu) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipamentoProcessoEquAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEquipamentoProcessoEquHook registers your hook function for all future operations.
func AddEquipamentoProcessoEquHook(hookPoint boil.HookPoint, equipamentoProcessoEquHook EquipamentoProcessoEquHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		equipamentoProcessoEquAfterSelectMu.Lock()
		equipamentoProcessoEquAfterSelectHooks = append(equipamentoProcessoEquAfterSelectHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		equipamentoProcessoEquBeforeInsertMu.Lock()
		equipamentoProcessoEquBeforeInsertHooks = append(equipamentoProcessoEquBeforeInsertHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		equipamentoProcessoEquAfterInsertMu.Lock()
		equipamentoProcessoEquAfterInsertHooks = append(equipamentoProcessoEquAfterInsertHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		equipamentoProcessoEquBeforeUpdateMu.Lock()
		equipamentoProcessoEquBeforeUpdateHooks = append(equipamentoProcessoEquBeforeUpdateHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		equipamentoProcessoEquAfterUpdateMu.Lock()
		equipamentoProcessoEquAfterUpdateHooks = append(equipamentoProcessoEquAfterUpdateHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		equipamentoProcessoEquBeforeDeleteMu.Lock()
		equipamentoProcessoEquBeforeDeleteHooks = append(equipamentoProcessoEquBeforeDeleteHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		equipamentoProcessoEquAfterDeleteMu.Lock()
		equipamentoProcessoEquAfterDeleteHooks = append(equipamentoProcessoEquAfterDeleteHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		equipamentoProcessoEquBeforeUpsertMu.Lock()
		equipamentoProcessoEquBeforeUpsertHooks = append(equipamentoProcessoEquBeforeUpsertHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		equipamentoProcessoEquAfterUpsertMu.Lock()
		equipamentoProcessoEquAfterUpsertHooks = append(equipamentoProcessoEquAfterUpsertHooks, equipamentoProcessoEquHook)
		equipamentoProcessoEquAfterUpsertMu.Unlock()
	}
}

// One returns a single equipamentoProcessoEqu record from the query.
func (q equipamentoProcessoEquQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EquipamentoProcessoEqu, error) {
	o := &EquipamentoProcessoEqu{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for equipamento_processo_equ")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all EquipamentoProcessoEqu records from the query.
func (q equipamentoProcessoEquQuery) All(ctx context.Context, exec boil.ContextExecutor) (EquipamentoProcessoEquSlice, error) {
	var o []*EquipamentoProcessoEqu

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to EquipamentoProcessoEqu slice")
	}

	if len(equipamentoProcessoEquAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all EquipamentoProcessoEqu records in the query.
func (q equipamentoProcessoEquQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count equipamento_processo_equ rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q equipamentoProcessoEquQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if equipamento_processo_equ exists")
	}

	return count > 0, nil
}

// Equ pointed to by the foreign key.
func (o *EquipamentoProcessoEqu) Equ(mods ...qm.QueryMod) equipamentoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`equ_id` = ?", o.EquID),
	}

	queryMods = append(queryMods, mods...)

	return Equipamentos(queryMods...)
}

// Prce pointed to by the foreign key.
func (o *EquipamentoProcessoEqu) Prce(mods ...qm.QueryMod) processoEquipamentoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`prce_id` = ?", o.PrceID),
	}

	queryMods = append(queryMods, mods...)

	return ProcessoEquipamentos(queryMods...)
}

// EpeProdutoProcessos retrieves all the produto_processo's ProdutoProcessos with an executor via epe_id column.
func (o *EquipamentoProcessoEqu) EpeProdutoProcessos(mods ...qm.QueryMod) produtoProcessoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto_processo`.`epe_id`=?", o.EpeID),
	)

	return ProdutoProcessos(queryMods...)
}

// LoadEqu allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (equipamentoProcessoEquL) LoadEqu(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEquipamentoProcessoEqu interface{}, mods queries.Applicator) error {
	var slice []*EquipamentoProcessoEqu
	var object *EquipamentoProcessoEqu

	if singular {
		var ok bool
		object, ok = maybeEquipamentoProcessoEqu.(*EquipamentoProcessoEqu)
		if !ok {
			object = new(EquipamentoProcessoEqu)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEquipamentoProcessoEqu)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEquipamentoProcessoEqu))
			}
		}
	} else {
		s, ok := maybeEquipamentoProcessoEqu.(*[]*EquipamentoProcessoEqu)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEquipamentoProcessoEqu)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEquipamentoProcessoEqu))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &equipamentoProcessoEquR{}
		}
		args[object.EquID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &equipamentoProcessoEquR{}
			}

			args[obj.EquID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`equipamento`),
		qm.WhereIn(`equipamento.equ_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Equipamento")
	}

	var resultSlice []*Equipamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Equipamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for equipamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for equipamento")
	}

	if len(equipamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Equ = foreign
		if foreign.R == nil {
			foreign.R = &equipamentoR{}
		}
		foreign.R.EquEquipamentoProcessoEqus = append(foreign.R.EquEquipamentoProcessoEqus, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EquID == foreign.EquID {
				local.R.Equ = foreign
				if foreign.R == nil {
					foreign.R = &equipamentoR{}
				}
				foreign.R.EquEquipamentoProcessoEqus = append(foreign.R.EquEquipamentoProcessoEqus, local)
				break
			}
		}
	}

	return nil
}

// LoadPrce allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (equipamentoProcessoEquL) LoadPrce(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEquipamentoProcessoEqu interface{}, mods queries.Applicator) error {
	var slice []*EquipamentoProcessoEqu
	var object *EquipamentoProcessoEqu

	if singular {
		var ok bool
		object, ok = maybeEquipamentoProcessoEqu.(*EquipamentoProcessoEqu)
		if !ok {
			object = new(EquipamentoProcessoEqu)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEquipamentoProcessoEqu)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEquipamentoProcessoEqu))
			}
		}
	} else {
		s, ok := maybeEquipamentoProcessoEqu.(*[]*EquipamentoProcessoEqu)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEquipamentoProcessoEqu)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEquipamentoProcessoEqu))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &equipamentoProcessoEquR{}
		}
		args[object.PrceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &equipamentoProcessoEquR{}
			}

			args[obj.PrceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`processo_equipamento`),
		qm.WhereIn(`processo_equipamento.prce_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProcessoEquipamento")
	}

	var resultSlice []*ProcessoEquipamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProcessoEquipamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for processo_equipamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for processo_equipamento")
	}

	if len(processoEquipamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Prce = foreign
		if foreign.R == nil {
			foreign.R = &processoEquipamentoR{}
		}
		foreign.R.PrceEquipamentoProcessoEqus = append(foreign.R.PrceEquipamentoProcessoEqus, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PrceID == foreign.PrceID {
				local.R.Prce = foreign
				if foreign.R == nil {
					foreign.R = &processoEquipamentoR{}
				}
				foreign.R.PrceEquipamentoProcessoEqus = append(foreign.R.PrceEquipamentoProcessoEqus, local)
				break
			}
		}
	}

	return nil
}

// LoadEpeProdutoProcessos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (equipamentoProcessoEquL) LoadEpeProdutoProcessos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEquipamentoProcessoEqu interface{}, mods queries.Applicator) error {
	var slice []*EquipamentoProcessoEqu
	var object *EquipamentoProcessoEqu

	if singular {
		var ok bool
		object, ok = maybeEquipamentoProcessoEqu.(*EquipamentoProcessoEqu)
		if !ok {
			object = new(EquipamentoProcessoEqu)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEquipamentoProcessoEqu)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEquipamentoProcessoEqu))
			}
		}
	} else {
		s, ok := maybeEquipamentoProcessoEqu.(*[]*EquipamentoProcessoEqu)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEquipamentoProcessoEqu)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEquipamentoProcessoEqu))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &equipamentoProcessoEquR{}
		}
		args[object.EpeID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &equipamentoProcessoEquR{}
			}
			args[obj.EpeID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_processo`),
		qm.WhereIn(`produto_processo.epe_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto_processo")
	}

	var resultSlice []*ProdutoProcesso
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto_processo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto_processo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_processo")
	}

	if len(produtoProcessoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.EpeProdutoProcessos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoProcessoR{}
			}
			foreign.R.Epe = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.EpeID == foreign.EpeID {
				local.R.EpeProdutoProcessos = append(local.R.EpeProdutoProcessos, foreign)
				if foreign.R == nil {
					foreign.R = &produtoProcessoR{}
				}
				foreign.R.Epe = local
				break
			}
		}
	}

	return nil
}

// SetEqu of the equipamentoProcessoEqu to the related item.
// Sets o.R.Equ to related.
// Adds o to related.R.EquEquipamentoProcessoEqus.
func (o *EquipamentoProcessoEqu) SetEqu(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Equipamento) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `equipamento_processo_equ` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"equ_id"}),
		strmangle.WhereClause("`", "`", 0, equipamentoProcessoEquPrimaryKeyColumns),
	)
	values := []interface{}{related.EquID, o.EpeID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EquID = related.EquID
	if o.R == nil {
		o.R = &equipamentoProcessoEquR{
			Equ: related,
		}
	} else {
		o.R.Equ = related
	}

	if related.R == nil {
		related.R = &equipamentoR{
			EquEquipamentoProcessoEqus: EquipamentoProcessoEquSlice{o},
		}
	} else {
		related.R.EquEquipamentoProcessoEqus = append(related.R.EquEquipamentoProcessoEqus, o)
	}

	return nil
}

// SetPrce of the equipamentoProcessoEqu to the related item.
// Sets o.R.Prce to related.
// Adds o to related.R.PrceEquipamentoProcessoEqus.
func (o *EquipamentoProcessoEqu) SetPrce(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ProcessoEquipamento) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `equipamento_processo_equ` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"prce_id"}),
		strmangle.WhereClause("`", "`", 0, equipamentoProcessoEquPrimaryKeyColumns),
	)
	values := []interface{}{related.PrceID, o.EpeID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PrceID = related.PrceID
	if o.R == nil {
		o.R = &equipamentoProcessoEquR{
			Prce: related,
		}
	} else {
		o.R.Prce = related
	}

	if related.R == nil {
		related.R = &processoEquipamentoR{
			PrceEquipamentoProcessoEqus: EquipamentoProcessoEquSlice{o},
		}
	} else {
		related.R.PrceEquipamentoProcessoEqus = append(related.R.PrceEquipamentoProcessoEqus, o)
	}

	return nil
}

// AddEpeProdutoProcessos adds the given related objects to the existing relationships
// of the equipamento_processo_equ, optionally inserting them as new records.
// Appends related to o.R.EpeProdutoProcessos.
// Sets related.R.Epe appropriately.
func (o *EquipamentoProcessoEqu) AddEpeProdutoProcessos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoProcesso) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.EpeID = o.EpeID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto_processo` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"epe_id"}),
				strmangle.WhereClause("`", "`", 0, produtoProcessoPrimaryKeyColumns),
			)
			values := []interface{}{o.EpeID, rel.ProID, rel.ProepeSequencia}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.EpeID = o.EpeID
		}
	}

	if o.R == nil {
		o.R = &equipamentoProcessoEquR{
			EpeProdutoProcessos: related,
		}
	} else {
		o.R.EpeProdutoProcessos = append(o.R.EpeProdutoProcessos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoProcessoR{
				Epe: o,
			}
		} else {
			rel.R.Epe = o
		}
	}
	return nil
}

// EquipamentoProcessoEqus retrieves all the records using an executor.
func EquipamentoProcessoEqus(mods ...qm.QueryMod) equipamentoProcessoEquQuery {
	mods = append(mods, qm.From("`equipamento_processo_equ`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`equipamento_processo_equ`.*"})
	}

	return equipamentoProcessoEquQuery{q}
}

// FindEquipamentoProcessoEqu retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEquipamentoProcessoEqu(ctx context.Context, exec boil.ContextExecutor, epeID int, selectCols ...string) (*EquipamentoProcessoEqu, error) {
	equipamentoProcessoEquObj := &EquipamentoProcessoEqu{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `equipamento_processo_equ` where `epe_id`=?", sel,
	)

	q := queries.Raw(query, epeID)

	err := q.Bind(ctx, exec, equipamentoProcessoEquObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from equipamento_processo_equ")
	}

	if err = equipamentoProcessoEquObj.doAfterSelectHooks(ctx, exec); err != nil {
		return equipamentoProcessoEquObj, err
	}

	return equipamentoProcessoEquObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EquipamentoProcessoEqu) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no equipamento_processo_equ provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(equipamentoProcessoEquColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	equipamentoProcessoEquInsertCacheMut.RLock()
	cache, cached := equipamentoProcessoEquInsertCache[key]
	equipamentoProcessoEquInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			equipamentoProcessoEquAllColumns,
			equipamentoProcessoEquColumnsWithDefault,
			equipamentoProcessoEquColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(equipamentoProcessoEquType, equipamentoProcessoEquMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(equipamentoProcessoEquType, equipamentoProcessoEquMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `equipamento_processo_equ` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `equipamento_processo_equ` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `equipamento_processo_equ` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, equipamentoProcessoEquPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into equipamento_processo_equ")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.EpeID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for equipamento_processo_equ")
	}

CacheNoHooks:
	if !cached {
		equipamentoProcessoEquInsertCacheMut.Lock()
		equipamentoProcessoEquInsertCache[key] = cache
		equipamentoProcessoEquInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the EquipamentoProcessoEqu.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EquipamentoProcessoEqu) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	equipamentoProcessoEquUpdateCacheMut.RLock()
	cache, cached := equipamentoProcessoEquUpdateCache[key]
	equipamentoProcessoEquUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			equipamentoProcessoEquAllColumns,
			equipamentoProcessoEquPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update equipamento_processo_equ, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `equipamento_processo_equ` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, equipamentoProcessoEquPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(equipamentoProcessoEquType, equipamentoProcessoEquMapping, append(wl, equipamentoProcessoEquPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update equipamento_processo_equ row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for equipamento_processo_equ")
	}

	if !cached {
		equipamentoProcessoEquUpdateCacheMut.Lock()
		equipamentoProcessoEquUpdateCache[key] = cache
		equipamentoProcessoEquUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q equipamentoProcessoEquQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for equipamento_processo_equ")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for equipamento_processo_equ")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EquipamentoProcessoEquSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipamentoProcessoEquPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `equipamento_processo_equ` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, equipamentoProcessoEquPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in equipamentoProcessoEqu slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all equipamentoProcessoEqu")
	}
	return rowsAff, nil
}

var mySQLEquipamentoProcessoEquUniqueColumns = []string{
	"epe_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *EquipamentoProcessoEqu) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no equipamento_processo_equ provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(equipamentoProcessoEquColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLEquipamentoProcessoEquUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	equipamentoProcessoEquUpsertCacheMut.RLock()
	cache, cached := equipamentoProcessoEquUpsertCache[key]
	equipamentoProcessoEquUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			equipamentoProcessoEquAllColumns,
			equipamentoProcessoEquColumnsWithDefault,
			equipamentoProcessoEquColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			equipamentoProcessoEquAllColumns,
			equipamentoProcessoEquPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert equipamento_processo_equ, could not build update column list")
		}

		ret := strmangle.SetComplement(equipamentoProcessoEquAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`equipamento_processo_equ`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `equipamento_processo_equ` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(equipamentoProcessoEquType, equipamentoProcessoEquMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(equipamentoProcessoEquType, equipamentoProcessoEquMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for equipamento_processo_equ")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(equipamentoProcessoEquType, equipamentoProcessoEquMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for equipamento_processo_equ")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for equipamento_processo_equ")
	}

CacheNoHooks:
	if !cached {
		equipamentoProcessoEquUpsertCacheMut.Lock()
		equipamentoProcessoEquUpsertCache[key] = cache
		equipamentoProcessoEquUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single EquipamentoProcessoEqu record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EquipamentoProcessoEqu) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no EquipamentoProcessoEqu provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), equipamentoProcessoEquPrimaryKeyMapping)
	sql := "DELETE FROM `equipamento_processo_equ` WHERE `epe_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from equipamento_processo_equ")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for equipamento_processo_equ")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q equipamentoProcessoEquQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no equipamentoProcessoEquQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from equipamento_processo_equ")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for equipamento_processo_equ")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EquipamentoProcessoEquSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(equipamentoProcessoEquBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipamentoProcessoEquPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `equipamento_processo_equ` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, equipamentoProcessoEquPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from equipamentoProcessoEqu slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for equipamento_processo_equ")
	}

	if len(equipamentoProcessoEquAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EquipamentoProcessoEqu) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEquipamentoProcessoEqu(ctx, exec, o.EpeID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EquipamentoProcessoEquSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EquipamentoProcessoEquSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipamentoProcessoEquPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `equipamento_processo_equ`.* FROM `equipamento_processo_equ` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, equipamentoProcessoEquPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in EquipamentoProcessoEquSlice")
	}

	*o = slice

	return nil
}

// EquipamentoProcessoEquExists checks if the EquipamentoProcessoEqu row exists.
func EquipamentoProcessoEquExists(ctx context.Context, exec boil.ContextExecutor, epeID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `equipamento_processo_equ` where `epe_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, epeID)
	}
	row := exec.QueryRowContext(ctx, sql, epeID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if equipamento_processo_equ exists")
	}

	return exists, nil
}

// Exists checks if the EquipamentoProcessoEqu row exists.
func (o *EquipamentoProcessoEqu) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return EquipamentoProcessoEquExists(ctx, exec, o.EpeID)
}
