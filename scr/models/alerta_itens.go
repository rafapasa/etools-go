// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// AlertaIten is an object representing the database table.
type AlertaIten struct {
	AltID        int         `boil:"alt_id" json:"alt_id" toml:"alt_id" yaml:"alt_id"`
	AltiItem     int         `boil:"alti_item" json:"alti_item" toml:"alti_item" yaml:"alti_item"`
	TBNID        int         `boil:"tbn_id" json:"tbn_id" toml:"tbn_id" yaml:"tbn_id"`
	TNCItem      int         `boil:"tnc_item" json:"tnc_item" toml:"tnc_item" yaml:"tnc_item"`
	AltiOperador null.Int    `boil:"alti_operador" json:"alti_operador,omitempty" toml:"alti_operador" yaml:"alti_operador,omitempty"`
	AltiValor    null.String `boil:"alti_valor" json:"alti_valor,omitempty" toml:"alti_valor" yaml:"alti_valor,omitempty"`

	R *alertaItenR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L alertaItenL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AlertaItenColumns = struct {
	AltID        string
	AltiItem     string
	TBNID        string
	TNCItem      string
	AltiOperador string
	AltiValor    string
}{
	AltID:        "alt_id",
	AltiItem:     "alti_item",
	TBNID:        "tbn_id",
	TNCItem:      "tnc_item",
	AltiOperador: "alti_operador",
	AltiValor:    "alti_valor",
}

var AlertaItenTableColumns = struct {
	AltID        string
	AltiItem     string
	TBNID        string
	TNCItem      string
	AltiOperador string
	AltiValor    string
}{
	AltID:        "alerta_itens.alt_id",
	AltiItem:     "alerta_itens.alti_item",
	TBNID:        "alerta_itens.tbn_id",
	TNCItem:      "alerta_itens.tnc_item",
	AltiOperador: "alerta_itens.alti_operador",
	AltiValor:    "alerta_itens.alti_valor",
}

// Generated where

var AlertaItenWhere = struct {
	AltID        whereHelperint
	AltiItem     whereHelperint
	TBNID        whereHelperint
	TNCItem      whereHelperint
	AltiOperador whereHelpernull_Int
	AltiValor    whereHelpernull_String
}{
	AltID:        whereHelperint{field: "`alerta_itens`.`alt_id`"},
	AltiItem:     whereHelperint{field: "`alerta_itens`.`alti_item`"},
	TBNID:        whereHelperint{field: "`alerta_itens`.`tbn_id`"},
	TNCItem:      whereHelperint{field: "`alerta_itens`.`tnc_item`"},
	AltiOperador: whereHelpernull_Int{field: "`alerta_itens`.`alti_operador`"},
	AltiValor:    whereHelpernull_String{field: "`alerta_itens`.`alti_valor`"},
}

// AlertaItenRels is where relationship names are stored.
var AlertaItenRels = struct {
	Alt string
}{
	Alt: "Alt",
}

// alertaItenR is where relationships are stored.
type alertaItenR struct {
	Alt *Alertas `boil:"Alt" json:"Alt" toml:"Alt" yaml:"Alt"`
}

// NewStruct creates a new relationship struct
func (*alertaItenR) NewStruct() *alertaItenR {
	return &alertaItenR{}
}

func (r *alertaItenR) GetAlt() *Alertas {
	if r == nil {
		return nil
	}
	return r.Alt
}

// alertaItenL is where Load methods for each relationship are stored.
type alertaItenL struct{}

var (
	alertaItenAllColumns            = []string{"alt_id", "alti_item", "tbn_id", "tnc_item", "alti_operador", "alti_valor"}
	alertaItenColumnsWithoutDefault = []string{"alt_id", "alti_item", "tbn_id", "tnc_item", "alti_operador", "alti_valor"}
	alertaItenColumnsWithDefault    = []string{}
	alertaItenPrimaryKeyColumns     = []string{"alt_id", "alti_item"}
	alertaItenGeneratedColumns      = []string{}
)

type (
	// AlertaItenSlice is an alias for a slice of pointers to AlertaIten.
	// This should almost always be used instead of []AlertaIten.
	AlertaItenSlice []*AlertaIten
	// AlertaItenHook is the signature for custom AlertaIten hook methods
	AlertaItenHook func(context.Context, boil.ContextExecutor, *AlertaIten) error

	alertaItenQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	alertaItenType                 = reflect.TypeOf(&AlertaIten{})
	alertaItenMapping              = queries.MakeStructMapping(alertaItenType)
	alertaItenPrimaryKeyMapping, _ = queries.BindMapping(alertaItenType, alertaItenMapping, alertaItenPrimaryKeyColumns)
	alertaItenInsertCacheMut       sync.RWMutex
	alertaItenInsertCache          = make(map[string]insertCache)
	alertaItenUpdateCacheMut       sync.RWMutex
	alertaItenUpdateCache          = make(map[string]updateCache)
	alertaItenUpsertCacheMut       sync.RWMutex
	alertaItenUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var alertaItenAfterSelectMu sync.Mutex
var alertaItenAfterSelectHooks []AlertaItenHook

var alertaItenBeforeInsertMu sync.Mutex
var alertaItenBeforeInsertHooks []AlertaItenHook
var alertaItenAfterInsertMu sync.Mutex
var alertaItenAfterInsertHooks []AlertaItenHook

var alertaItenBeforeUpdateMu sync.Mutex
var alertaItenBeforeUpdateHooks []AlertaItenHook
var alertaItenAfterUpdateMu sync.Mutex
var alertaItenAfterUpdateHooks []AlertaItenHook

var alertaItenBeforeDeleteMu sync.Mutex
var alertaItenBeforeDeleteHooks []AlertaItenHook
var alertaItenAfterDeleteMu sync.Mutex
var alertaItenAfterDeleteHooks []AlertaItenHook

var alertaItenBeforeUpsertMu sync.Mutex
var alertaItenBeforeUpsertHooks []AlertaItenHook
var alertaItenAfterUpsertMu sync.Mutex
var alertaItenAfterUpsertHooks []AlertaItenHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AlertaIten) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AlertaIten) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AlertaIten) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AlertaIten) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AlertaIten) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AlertaIten) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AlertaIten) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AlertaIten) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AlertaIten) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range alertaItenAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAlertaItenHook registers your hook function for all future operations.
func AddAlertaItenHook(hookPoint boil.HookPoint, alertaItenHook AlertaItenHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		alertaItenAfterSelectMu.Lock()
		alertaItenAfterSelectHooks = append(alertaItenAfterSelectHooks, alertaItenHook)
		alertaItenAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		alertaItenBeforeInsertMu.Lock()
		alertaItenBeforeInsertHooks = append(alertaItenBeforeInsertHooks, alertaItenHook)
		alertaItenBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		alertaItenAfterInsertMu.Lock()
		alertaItenAfterInsertHooks = append(alertaItenAfterInsertHooks, alertaItenHook)
		alertaItenAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		alertaItenBeforeUpdateMu.Lock()
		alertaItenBeforeUpdateHooks = append(alertaItenBeforeUpdateHooks, alertaItenHook)
		alertaItenBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		alertaItenAfterUpdateMu.Lock()
		alertaItenAfterUpdateHooks = append(alertaItenAfterUpdateHooks, alertaItenHook)
		alertaItenAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		alertaItenBeforeDeleteMu.Lock()
		alertaItenBeforeDeleteHooks = append(alertaItenBeforeDeleteHooks, alertaItenHook)
		alertaItenBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		alertaItenAfterDeleteMu.Lock()
		alertaItenAfterDeleteHooks = append(alertaItenAfterDeleteHooks, alertaItenHook)
		alertaItenAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		alertaItenBeforeUpsertMu.Lock()
		alertaItenBeforeUpsertHooks = append(alertaItenBeforeUpsertHooks, alertaItenHook)
		alertaItenBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		alertaItenAfterUpsertMu.Lock()
		alertaItenAfterUpsertHooks = append(alertaItenAfterUpsertHooks, alertaItenHook)
		alertaItenAfterUpsertMu.Unlock()
	}
}

// One returns a single alertaIten record from the query.
func (q alertaItenQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AlertaIten, error) {
	o := &AlertaIten{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for alerta_itens")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AlertaIten records from the query.
func (q alertaItenQuery) All(ctx context.Context, exec boil.ContextExecutor) (AlertaItenSlice, error) {
	var o []*AlertaIten

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AlertaIten slice")
	}

	if len(alertaItenAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AlertaIten records in the query.
func (q alertaItenQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count alerta_itens rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q alertaItenQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if alerta_itens exists")
	}

	return count > 0, nil
}

// Alt pointed to by the foreign key.
func (o *AlertaIten) Alt(mods ...qm.QueryMod) alertasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`alt_id` = ?", o.AltID),
	}

	queryMods = append(queryMods, mods...)

	return Alertas(queryMods...)
}

// LoadAlt allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (alertaItenL) LoadAlt(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAlertaIten interface{}, mods queries.Applicator) error {
	var slice []*AlertaIten
	var object *AlertaIten

	if singular {
		var ok bool
		object, ok = maybeAlertaIten.(*AlertaIten)
		if !ok {
			object = new(AlertaIten)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAlertaIten)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAlertaIten))
			}
		}
	} else {
		s, ok := maybeAlertaIten.(*[]*AlertaIten)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAlertaIten)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAlertaIten))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &alertaItenR{}
		}
		args[object.AltID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &alertaItenR{}
			}

			args[obj.AltID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alertas`),
		qm.WhereIn(`alertas.alt_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Alertas")
	}

	var resultSlice []*Alertas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Alertas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alertas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alertas")
	}

	if len(alertasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Alt = foreign
		if foreign.R == nil {
			foreign.R = &alertasR{}
		}
		foreign.R.AltAlertaItens = append(foreign.R.AltAlertaItens, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AltID == foreign.AltID {
				local.R.Alt = foreign
				if foreign.R == nil {
					foreign.R = &alertasR{}
				}
				foreign.R.AltAlertaItens = append(foreign.R.AltAlertaItens, local)
				break
			}
		}
	}

	return nil
}

// SetAlt of the alertaIten to the related item.
// Sets o.R.Alt to related.
// Adds o to related.R.AltAlertaItens.
func (o *AlertaIten) SetAlt(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Alertas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `alerta_itens` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"alt_id"}),
		strmangle.WhereClause("`", "`", 0, alertaItenPrimaryKeyColumns),
	)
	values := []interface{}{related.AltID, o.AltID, o.AltiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AltID = related.AltID
	if o.R == nil {
		o.R = &alertaItenR{
			Alt: related,
		}
	} else {
		o.R.Alt = related
	}

	if related.R == nil {
		related.R = &alertasR{
			AltAlertaItens: AlertaItenSlice{o},
		}
	} else {
		related.R.AltAlertaItens = append(related.R.AltAlertaItens, o)
	}

	return nil
}

// AlertaItens retrieves all the records using an executor.
func AlertaItens(mods ...qm.QueryMod) alertaItenQuery {
	mods = append(mods, qm.From("`alerta_itens`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`alerta_itens`.*"})
	}

	return alertaItenQuery{q}
}

// FindAlertaIten retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAlertaIten(ctx context.Context, exec boil.ContextExecutor, altID int, altiItem int, selectCols ...string) (*AlertaIten, error) {
	alertaItenObj := &AlertaIten{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `alerta_itens` where `alt_id`=? AND `alti_item`=?", sel,
	)

	q := queries.Raw(query, altID, altiItem)

	err := q.Bind(ctx, exec, alertaItenObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from alerta_itens")
	}

	if err = alertaItenObj.doAfterSelectHooks(ctx, exec); err != nil {
		return alertaItenObj, err
	}

	return alertaItenObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AlertaIten) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no alerta_itens provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(alertaItenColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	alertaItenInsertCacheMut.RLock()
	cache, cached := alertaItenInsertCache[key]
	alertaItenInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			alertaItenAllColumns,
			alertaItenColumnsWithDefault,
			alertaItenColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(alertaItenType, alertaItenMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(alertaItenType, alertaItenMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `alerta_itens` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `alerta_itens` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `alerta_itens` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, alertaItenPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into alerta_itens")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.AltID,
		o.AltiItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for alerta_itens")
	}

CacheNoHooks:
	if !cached {
		alertaItenInsertCacheMut.Lock()
		alertaItenInsertCache[key] = cache
		alertaItenInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AlertaIten.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AlertaIten) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	alertaItenUpdateCacheMut.RLock()
	cache, cached := alertaItenUpdateCache[key]
	alertaItenUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			alertaItenAllColumns,
			alertaItenPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update alerta_itens, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `alerta_itens` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, alertaItenPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(alertaItenType, alertaItenMapping, append(wl, alertaItenPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update alerta_itens row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for alerta_itens")
	}

	if !cached {
		alertaItenUpdateCacheMut.Lock()
		alertaItenUpdateCache[key] = cache
		alertaItenUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q alertaItenQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for alerta_itens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for alerta_itens")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AlertaItenSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertaItenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `alerta_itens` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, alertaItenPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in alertaIten slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all alertaIten")
	}
	return rowsAff, nil
}

var mySQLAlertaItenUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AlertaIten) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no alerta_itens provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(alertaItenColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLAlertaItenUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	alertaItenUpsertCacheMut.RLock()
	cache, cached := alertaItenUpsertCache[key]
	alertaItenUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			alertaItenAllColumns,
			alertaItenColumnsWithDefault,
			alertaItenColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			alertaItenAllColumns,
			alertaItenPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert alerta_itens, could not build update column list")
		}

		ret := strmangle.SetComplement(alertaItenAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`alerta_itens`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `alerta_itens` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(alertaItenType, alertaItenMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(alertaItenType, alertaItenMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for alerta_itens")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(alertaItenType, alertaItenMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for alerta_itens")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for alerta_itens")
	}

CacheNoHooks:
	if !cached {
		alertaItenUpsertCacheMut.Lock()
		alertaItenUpsertCache[key] = cache
		alertaItenUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AlertaIten record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AlertaIten) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AlertaIten provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), alertaItenPrimaryKeyMapping)
	sql := "DELETE FROM `alerta_itens` WHERE `alt_id`=? AND `alti_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from alerta_itens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for alerta_itens")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q alertaItenQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no alertaItenQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from alerta_itens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for alerta_itens")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AlertaItenSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(alertaItenBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertaItenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `alerta_itens` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, alertaItenPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from alertaIten slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for alerta_itens")
	}

	if len(alertaItenAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AlertaIten) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAlertaIten(ctx, exec, o.AltID, o.AltiItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AlertaItenSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AlertaItenSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), alertaItenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `alerta_itens`.* FROM `alerta_itens` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, alertaItenPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AlertaItenSlice")
	}

	*o = slice

	return nil
}

// AlertaItenExists checks if the AlertaIten row exists.
func AlertaItenExists(ctx context.Context, exec boil.ContextExecutor, altID int, altiItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `alerta_itens` where `alt_id`=? AND `alti_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, altID, altiItem)
	}
	row := exec.QueryRowContext(ctx, sql, altID, altiItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if alerta_itens exists")
	}

	return exists, nil
}

// Exists checks if the AlertaIten row exists.
func (o *AlertaIten) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AlertaItenExists(ctx, exec, o.AltID, o.AltiItem)
}
