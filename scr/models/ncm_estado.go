// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// NCMEstado is an object representing the database table.
type NCMEstado struct {
	NCMNumero               int           `boil:"ncm_numero" json:"ncm_numero" toml:"ncm_numero" yaml:"ncm_numero"`
	EstID                   int           `boil:"est_id" json:"est_id" toml:"est_id" yaml:"est_id"`
	NceReducaoBCIcms        types.Decimal `boil:"nce_reducao_bc_icms" json:"nce_reducao_bc_icms" toml:"nce_reducao_bc_icms" yaml:"nce_reducao_bc_icms"`
	NceAliquotamva          types.Decimal `boil:"nce_aliquotamva" json:"nce_aliquotamva" toml:"nce_aliquotamva" yaml:"nce_aliquotamva"`
	NceAliquotamvasimples   types.Decimal `boil:"nce_aliquotamvasimples" json:"nce_aliquotamvasimples" toml:"nce_aliquotamvasimples" yaml:"nce_aliquotamvasimples"`
	NceReducaoBCIcmsSimples types.Decimal `boil:"nce_reducao_bc_icms_simples" json:"nce_reducao_bc_icms_simples" toml:"nce_reducao_bc_icms_simples" yaml:"nce_reducao_bc_icms_simples"`
	NceReducaoBCIcmsST      types.Decimal `boil:"nce_reducao_bc_icms_st" json:"nce_reducao_bc_icms_st" toml:"nce_reducao_bc_icms_st" yaml:"nce_reducao_bc_icms_st"`

	R *ncmEstadoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ncmEstadoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var NCMEstadoColumns = struct {
	NCMNumero               string
	EstID                   string
	NceReducaoBCIcms        string
	NceAliquotamva          string
	NceAliquotamvasimples   string
	NceReducaoBCIcmsSimples string
	NceReducaoBCIcmsST      string
}{
	NCMNumero:               "ncm_numero",
	EstID:                   "est_id",
	NceReducaoBCIcms:        "nce_reducao_bc_icms",
	NceAliquotamva:          "nce_aliquotamva",
	NceAliquotamvasimples:   "nce_aliquotamvasimples",
	NceReducaoBCIcmsSimples: "nce_reducao_bc_icms_simples",
	NceReducaoBCIcmsST:      "nce_reducao_bc_icms_st",
}

var NCMEstadoTableColumns = struct {
	NCMNumero               string
	EstID                   string
	NceReducaoBCIcms        string
	NceAliquotamva          string
	NceAliquotamvasimples   string
	NceReducaoBCIcmsSimples string
	NceReducaoBCIcmsST      string
}{
	NCMNumero:               "ncm_estado.ncm_numero",
	EstID:                   "ncm_estado.est_id",
	NceReducaoBCIcms:        "ncm_estado.nce_reducao_bc_icms",
	NceAliquotamva:          "ncm_estado.nce_aliquotamva",
	NceAliquotamvasimples:   "ncm_estado.nce_aliquotamvasimples",
	NceReducaoBCIcmsSimples: "ncm_estado.nce_reducao_bc_icms_simples",
	NceReducaoBCIcmsST:      "ncm_estado.nce_reducao_bc_icms_st",
}

// Generated where

var NCMEstadoWhere = struct {
	NCMNumero               whereHelperint
	EstID                   whereHelperint
	NceReducaoBCIcms        whereHelpertypes_Decimal
	NceAliquotamva          whereHelpertypes_Decimal
	NceAliquotamvasimples   whereHelpertypes_Decimal
	NceReducaoBCIcmsSimples whereHelpertypes_Decimal
	NceReducaoBCIcmsST      whereHelpertypes_Decimal
}{
	NCMNumero:               whereHelperint{field: "`ncm_estado`.`ncm_numero`"},
	EstID:                   whereHelperint{field: "`ncm_estado`.`est_id`"},
	NceReducaoBCIcms:        whereHelpertypes_Decimal{field: "`ncm_estado`.`nce_reducao_bc_icms`"},
	NceAliquotamva:          whereHelpertypes_Decimal{field: "`ncm_estado`.`nce_aliquotamva`"},
	NceAliquotamvasimples:   whereHelpertypes_Decimal{field: "`ncm_estado`.`nce_aliquotamvasimples`"},
	NceReducaoBCIcmsSimples: whereHelpertypes_Decimal{field: "`ncm_estado`.`nce_reducao_bc_icms_simples`"},
	NceReducaoBCIcmsST:      whereHelpertypes_Decimal{field: "`ncm_estado`.`nce_reducao_bc_icms_st`"},
}

// NCMEstadoRels is where relationship names are stored.
var NCMEstadoRels = struct {
	Est          string
	NCMNumeroNCM string
}{
	Est:          "Est",
	NCMNumeroNCM: "NCMNumeroNCM",
}

// ncmEstadoR is where relationships are stored.
type ncmEstadoR struct {
	Est          *Estado `boil:"Est" json:"Est" toml:"Est" yaml:"Est"`
	NCMNumeroNCM *NCM    `boil:"NCMNumeroNCM" json:"NCMNumeroNCM" toml:"NCMNumeroNCM" yaml:"NCMNumeroNCM"`
}

// NewStruct creates a new relationship struct
func (*ncmEstadoR) NewStruct() *ncmEstadoR {
	return &ncmEstadoR{}
}

func (r *ncmEstadoR) GetEst() *Estado {
	if r == nil {
		return nil
	}
	return r.Est
}

func (r *ncmEstadoR) GetNCMNumeroNCM() *NCM {
	if r == nil {
		return nil
	}
	return r.NCMNumeroNCM
}

// ncmEstadoL is where Load methods for each relationship are stored.
type ncmEstadoL struct{}

var (
	ncmEstadoAllColumns            = []string{"ncm_numero", "est_id", "nce_reducao_bc_icms", "nce_aliquotamva", "nce_aliquotamvasimples", "nce_reducao_bc_icms_simples", "nce_reducao_bc_icms_st"}
	ncmEstadoColumnsWithoutDefault = []string{"ncm_numero", "est_id", "nce_aliquotamva", "nce_aliquotamvasimples", "nce_reducao_bc_icms_simples", "nce_reducao_bc_icms_st"}
	ncmEstadoColumnsWithDefault    = []string{"nce_reducao_bc_icms"}
	ncmEstadoPrimaryKeyColumns     = []string{"ncm_numero", "est_id"}
	ncmEstadoGeneratedColumns      = []string{}
)

type (
	// NCMEstadoSlice is an alias for a slice of pointers to NCMEstado.
	// This should almost always be used instead of []NCMEstado.
	NCMEstadoSlice []*NCMEstado
	// NCMEstadoHook is the signature for custom NCMEstado hook methods
	NCMEstadoHook func(context.Context, boil.ContextExecutor, *NCMEstado) error

	ncmEstadoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ncmEstadoType                 = reflect.TypeOf(&NCMEstado{})
	ncmEstadoMapping              = queries.MakeStructMapping(ncmEstadoType)
	ncmEstadoPrimaryKeyMapping, _ = queries.BindMapping(ncmEstadoType, ncmEstadoMapping, ncmEstadoPrimaryKeyColumns)
	ncmEstadoInsertCacheMut       sync.RWMutex
	ncmEstadoInsertCache          = make(map[string]insertCache)
	ncmEstadoUpdateCacheMut       sync.RWMutex
	ncmEstadoUpdateCache          = make(map[string]updateCache)
	ncmEstadoUpsertCacheMut       sync.RWMutex
	ncmEstadoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ncmEstadoAfterSelectMu sync.Mutex
var ncmEstadoAfterSelectHooks []NCMEstadoHook

var ncmEstadoBeforeInsertMu sync.Mutex
var ncmEstadoBeforeInsertHooks []NCMEstadoHook
var ncmEstadoAfterInsertMu sync.Mutex
var ncmEstadoAfterInsertHooks []NCMEstadoHook

var ncmEstadoBeforeUpdateMu sync.Mutex
var ncmEstadoBeforeUpdateHooks []NCMEstadoHook
var ncmEstadoAfterUpdateMu sync.Mutex
var ncmEstadoAfterUpdateHooks []NCMEstadoHook

var ncmEstadoBeforeDeleteMu sync.Mutex
var ncmEstadoBeforeDeleteHooks []NCMEstadoHook
var ncmEstadoAfterDeleteMu sync.Mutex
var ncmEstadoAfterDeleteHooks []NCMEstadoHook

var ncmEstadoBeforeUpsertMu sync.Mutex
var ncmEstadoBeforeUpsertHooks []NCMEstadoHook
var ncmEstadoAfterUpsertMu sync.Mutex
var ncmEstadoAfterUpsertHooks []NCMEstadoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *NCMEstado) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *NCMEstado) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *NCMEstado) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *NCMEstado) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *NCMEstado) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *NCMEstado) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *NCMEstado) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *NCMEstado) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *NCMEstado) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmEstadoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddNCMEstadoHook registers your hook function for all future operations.
func AddNCMEstadoHook(hookPoint boil.HookPoint, ncmEstadoHook NCMEstadoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ncmEstadoAfterSelectMu.Lock()
		ncmEstadoAfterSelectHooks = append(ncmEstadoAfterSelectHooks, ncmEstadoHook)
		ncmEstadoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		ncmEstadoBeforeInsertMu.Lock()
		ncmEstadoBeforeInsertHooks = append(ncmEstadoBeforeInsertHooks, ncmEstadoHook)
		ncmEstadoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		ncmEstadoAfterInsertMu.Lock()
		ncmEstadoAfterInsertHooks = append(ncmEstadoAfterInsertHooks, ncmEstadoHook)
		ncmEstadoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		ncmEstadoBeforeUpdateMu.Lock()
		ncmEstadoBeforeUpdateHooks = append(ncmEstadoBeforeUpdateHooks, ncmEstadoHook)
		ncmEstadoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		ncmEstadoAfterUpdateMu.Lock()
		ncmEstadoAfterUpdateHooks = append(ncmEstadoAfterUpdateHooks, ncmEstadoHook)
		ncmEstadoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		ncmEstadoBeforeDeleteMu.Lock()
		ncmEstadoBeforeDeleteHooks = append(ncmEstadoBeforeDeleteHooks, ncmEstadoHook)
		ncmEstadoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		ncmEstadoAfterDeleteMu.Lock()
		ncmEstadoAfterDeleteHooks = append(ncmEstadoAfterDeleteHooks, ncmEstadoHook)
		ncmEstadoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		ncmEstadoBeforeUpsertMu.Lock()
		ncmEstadoBeforeUpsertHooks = append(ncmEstadoBeforeUpsertHooks, ncmEstadoHook)
		ncmEstadoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		ncmEstadoAfterUpsertMu.Lock()
		ncmEstadoAfterUpsertHooks = append(ncmEstadoAfterUpsertHooks, ncmEstadoHook)
		ncmEstadoAfterUpsertMu.Unlock()
	}
}

// One returns a single ncmEstado record from the query.
func (q ncmEstadoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*NCMEstado, error) {
	o := &NCMEstado{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ncm_estado")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all NCMEstado records from the query.
func (q ncmEstadoQuery) All(ctx context.Context, exec boil.ContextExecutor) (NCMEstadoSlice, error) {
	var o []*NCMEstado

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to NCMEstado slice")
	}

	if len(ncmEstadoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all NCMEstado records in the query.
func (q ncmEstadoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ncm_estado rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ncmEstadoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ncm_estado exists")
	}

	return count > 0, nil
}

// Est pointed to by the foreign key.
func (o *NCMEstado) Est(mods ...qm.QueryMod) estadoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`est_id` = ?", o.EstID),
	}

	queryMods = append(queryMods, mods...)

	return Estados(queryMods...)
}

// NCMNumeroNCM pointed to by the foreign key.
func (o *NCMEstado) NCMNumeroNCM(mods ...qm.QueryMod) ncmQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ncm_numero` = ?", o.NCMNumero),
	}

	queryMods = append(queryMods, mods...)

	return NCMS(queryMods...)
}

// LoadEst allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmEstadoL) LoadEst(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMEstado interface{}, mods queries.Applicator) error {
	var slice []*NCMEstado
	var object *NCMEstado

	if singular {
		var ok bool
		object, ok = maybeNCMEstado.(*NCMEstado)
		if !ok {
			object = new(NCMEstado)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMEstado)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMEstado))
			}
		}
	} else {
		s, ok := maybeNCMEstado.(*[]*NCMEstado)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMEstado)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMEstado))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmEstadoR{}
		}
		args[object.EstID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmEstadoR{}
			}

			args[obj.EstID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`estado`),
		qm.WhereIn(`estado.est_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Estado")
	}

	var resultSlice []*Estado
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Estado")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for estado")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for estado")
	}

	if len(estadoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Est = foreign
		if foreign.R == nil {
			foreign.R = &estadoR{}
		}
		foreign.R.EstNCMEstados = append(foreign.R.EstNCMEstados, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EstID == foreign.EstID {
				local.R.Est = foreign
				if foreign.R == nil {
					foreign.R = &estadoR{}
				}
				foreign.R.EstNCMEstados = append(foreign.R.EstNCMEstados, local)
				break
			}
		}
	}

	return nil
}

// LoadNCMNumeroNCM allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmEstadoL) LoadNCMNumeroNCM(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMEstado interface{}, mods queries.Applicator) error {
	var slice []*NCMEstado
	var object *NCMEstado

	if singular {
		var ok bool
		object, ok = maybeNCMEstado.(*NCMEstado)
		if !ok {
			object = new(NCMEstado)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMEstado)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMEstado))
			}
		}
	} else {
		s, ok := maybeNCMEstado.(*[]*NCMEstado)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMEstado)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMEstado))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmEstadoR{}
		}
		args[object.NCMNumero] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmEstadoR{}
			}

			args[obj.NCMNumero] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ncm`),
		qm.WhereIn(`ncm.ncm_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load NCM")
	}

	var resultSlice []*NCM
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice NCM")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ncm")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ncm")
	}

	if len(ncmAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.NCMNumeroNCM = foreign
		if foreign.R == nil {
			foreign.R = &ncmR{}
		}
		foreign.R.NCMNumeroNCMEstados = append(foreign.R.NCMNumeroNCMEstados, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.NCMNumero == foreign.NCMNumero {
				local.R.NCMNumeroNCM = foreign
				if foreign.R == nil {
					foreign.R = &ncmR{}
				}
				foreign.R.NCMNumeroNCMEstados = append(foreign.R.NCMNumeroNCMEstados, local)
				break
			}
		}
	}

	return nil
}

// SetEst of the ncmEstado to the related item.
// Sets o.R.Est to related.
// Adds o to related.R.EstNCMEstados.
func (o *NCMEstado) SetEst(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Estado) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_estado` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"est_id"}),
		strmangle.WhereClause("`", "`", 0, ncmEstadoPrimaryKeyColumns),
	)
	values := []interface{}{related.EstID, o.NCMNumero, o.EstID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EstID = related.EstID
	if o.R == nil {
		o.R = &ncmEstadoR{
			Est: related,
		}
	} else {
		o.R.Est = related
	}

	if related.R == nil {
		related.R = &estadoR{
			EstNCMEstados: NCMEstadoSlice{o},
		}
	} else {
		related.R.EstNCMEstados = append(related.R.EstNCMEstados, o)
	}

	return nil
}

// SetNCMNumeroNCM of the ncmEstado to the related item.
// Sets o.R.NCMNumeroNCM to related.
// Adds o to related.R.NCMNumeroNCMEstados.
func (o *NCMEstado) SetNCMNumeroNCM(ctx context.Context, exec boil.ContextExecutor, insert bool, related *NCM) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_estado` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ncm_numero"}),
		strmangle.WhereClause("`", "`", 0, ncmEstadoPrimaryKeyColumns),
	)
	values := []interface{}{related.NCMNumero, o.NCMNumero, o.EstID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.NCMNumero = related.NCMNumero
	if o.R == nil {
		o.R = &ncmEstadoR{
			NCMNumeroNCM: related,
		}
	} else {
		o.R.NCMNumeroNCM = related
	}

	if related.R == nil {
		related.R = &ncmR{
			NCMNumeroNCMEstados: NCMEstadoSlice{o},
		}
	} else {
		related.R.NCMNumeroNCMEstados = append(related.R.NCMNumeroNCMEstados, o)
	}

	return nil
}

// NCMEstados retrieves all the records using an executor.
func NCMEstados(mods ...qm.QueryMod) ncmEstadoQuery {
	mods = append(mods, qm.From("`ncm_estado`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`ncm_estado`.*"})
	}

	return ncmEstadoQuery{q}
}

// FindNCMEstado retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindNCMEstado(ctx context.Context, exec boil.ContextExecutor, nCMNumero int, estID int, selectCols ...string) (*NCMEstado, error) {
	ncmEstadoObj := &NCMEstado{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ncm_estado` where `ncm_numero`=? AND `est_id`=?", sel,
	)

	q := queries.Raw(query, nCMNumero, estID)

	err := q.Bind(ctx, exec, ncmEstadoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ncm_estado")
	}

	if err = ncmEstadoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ncmEstadoObj, err
	}

	return ncmEstadoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *NCMEstado) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ncm_estado provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ncmEstadoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ncmEstadoInsertCacheMut.RLock()
	cache, cached := ncmEstadoInsertCache[key]
	ncmEstadoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ncmEstadoAllColumns,
			ncmEstadoColumnsWithDefault,
			ncmEstadoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ncmEstadoType, ncmEstadoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ncmEstadoType, ncmEstadoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ncm_estado` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ncm_estado` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ncm_estado` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ncmEstadoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ncm_estado")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.NCMNumero,
		o.EstID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ncm_estado")
	}

CacheNoHooks:
	if !cached {
		ncmEstadoInsertCacheMut.Lock()
		ncmEstadoInsertCache[key] = cache
		ncmEstadoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the NCMEstado.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *NCMEstado) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ncmEstadoUpdateCacheMut.RLock()
	cache, cached := ncmEstadoUpdateCache[key]
	ncmEstadoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ncmEstadoAllColumns,
			ncmEstadoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ncm_estado, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ncm_estado` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ncmEstadoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ncmEstadoType, ncmEstadoMapping, append(wl, ncmEstadoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ncm_estado row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ncm_estado")
	}

	if !cached {
		ncmEstadoUpdateCacheMut.Lock()
		ncmEstadoUpdateCache[key] = cache
		ncmEstadoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ncmEstadoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ncm_estado")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ncm_estado")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o NCMEstadoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmEstadoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ncm_estado` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmEstadoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ncmEstado slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ncmEstado")
	}
	return rowsAff, nil
}

var mySQLNCMEstadoUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *NCMEstado) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ncm_estado provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ncmEstadoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLNCMEstadoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ncmEstadoUpsertCacheMut.RLock()
	cache, cached := ncmEstadoUpsertCache[key]
	ncmEstadoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			ncmEstadoAllColumns,
			ncmEstadoColumnsWithDefault,
			ncmEstadoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ncmEstadoAllColumns,
			ncmEstadoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ncm_estado, could not build update column list")
		}

		ret := strmangle.SetComplement(ncmEstadoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`ncm_estado`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ncm_estado` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ncmEstadoType, ncmEstadoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ncmEstadoType, ncmEstadoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ncm_estado")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ncmEstadoType, ncmEstadoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ncm_estado")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ncm_estado")
	}

CacheNoHooks:
	if !cached {
		ncmEstadoUpsertCacheMut.Lock()
		ncmEstadoUpsertCache[key] = cache
		ncmEstadoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single NCMEstado record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *NCMEstado) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no NCMEstado provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ncmEstadoPrimaryKeyMapping)
	sql := "DELETE FROM `ncm_estado` WHERE `ncm_numero`=? AND `est_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ncm_estado")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ncm_estado")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ncmEstadoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ncmEstadoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ncm_estado")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ncm_estado")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o NCMEstadoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ncmEstadoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmEstadoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ncm_estado` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmEstadoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ncmEstado slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ncm_estado")
	}

	if len(ncmEstadoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *NCMEstado) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindNCMEstado(ctx, exec, o.NCMNumero, o.EstID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *NCMEstadoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := NCMEstadoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmEstadoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ncm_estado`.* FROM `ncm_estado` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmEstadoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in NCMEstadoSlice")
	}

	*o = slice

	return nil
}

// NCMEstadoExists checks if the NCMEstado row exists.
func NCMEstadoExists(ctx context.Context, exec boil.ContextExecutor, nCMNumero int, estID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ncm_estado` where `ncm_numero`=? AND `est_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, nCMNumero, estID)
	}
	row := exec.QueryRowContext(ctx, sql, nCMNumero, estID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ncm_estado exists")
	}

	return exists, nil
}

// Exists checks if the NCMEstado row exists.
func (o *NCMEstado) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return NCMEstadoExists(ctx, exec, o.NCMNumero, o.EstID)
}
