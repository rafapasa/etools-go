// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ManifestoEletronicoUf is an object representing the database table.
type ManifestoEletronicoUf struct {
	MdfeID     int   `boil:"mdfe_id" json:"mdfe_id" toml:"mdfe_id" yaml:"mdfe_id"`
	EstID      int   `boil:"est_id" json:"est_id" toml:"est_id" yaml:"est_id"`
	MdfeufItem int16 `boil:"mdfeuf_item" json:"mdfeuf_item" toml:"mdfeuf_item" yaml:"mdfeuf_item"`

	R *manifestoEletronicoUfR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L manifestoEletronicoUfL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ManifestoEletronicoUfColumns = struct {
	MdfeID     string
	EstID      string
	MdfeufItem string
}{
	MdfeID:     "mdfe_id",
	EstID:      "est_id",
	MdfeufItem: "mdfeuf_item",
}

var ManifestoEletronicoUfTableColumns = struct {
	MdfeID     string
	EstID      string
	MdfeufItem string
}{
	MdfeID:     "manifesto_eletronico_uf.mdfe_id",
	EstID:      "manifesto_eletronico_uf.est_id",
	MdfeufItem: "manifesto_eletronico_uf.mdfeuf_item",
}

// Generated where

var ManifestoEletronicoUfWhere = struct {
	MdfeID     whereHelperint
	EstID      whereHelperint
	MdfeufItem whereHelperint16
}{
	MdfeID:     whereHelperint{field: "`manifesto_eletronico_uf`.`mdfe_id`"},
	EstID:      whereHelperint{field: "`manifesto_eletronico_uf`.`est_id`"},
	MdfeufItem: whereHelperint16{field: "`manifesto_eletronico_uf`.`mdfeuf_item`"},
}

// ManifestoEletronicoUfRels is where relationship names are stored.
var ManifestoEletronicoUfRels = struct {
	Est  string
	Mdfe string
}{
	Est:  "Est",
	Mdfe: "Mdfe",
}

// manifestoEletronicoUfR is where relationships are stored.
type manifestoEletronicoUfR struct {
	Est  *Estado              `boil:"Est" json:"Est" toml:"Est" yaml:"Est"`
	Mdfe *ManifestoEletronico `boil:"Mdfe" json:"Mdfe" toml:"Mdfe" yaml:"Mdfe"`
}

// NewStruct creates a new relationship struct
func (*manifestoEletronicoUfR) NewStruct() *manifestoEletronicoUfR {
	return &manifestoEletronicoUfR{}
}

func (r *manifestoEletronicoUfR) GetEst() *Estado {
	if r == nil {
		return nil
	}
	return r.Est
}

func (r *manifestoEletronicoUfR) GetMdfe() *ManifestoEletronico {
	if r == nil {
		return nil
	}
	return r.Mdfe
}

// manifestoEletronicoUfL is where Load methods for each relationship are stored.
type manifestoEletronicoUfL struct{}

var (
	manifestoEletronicoUfAllColumns            = []string{"mdfe_id", "est_id", "mdfeuf_item"}
	manifestoEletronicoUfColumnsWithoutDefault = []string{"mdfe_id", "est_id", "mdfeuf_item"}
	manifestoEletronicoUfColumnsWithDefault    = []string{}
	manifestoEletronicoUfPrimaryKeyColumns     = []string{"mdfe_id", "est_id", "mdfeuf_item"}
	manifestoEletronicoUfGeneratedColumns      = []string{}
)

type (
	// ManifestoEletronicoUfSlice is an alias for a slice of pointers to ManifestoEletronicoUf.
	// This should almost always be used instead of []ManifestoEletronicoUf.
	ManifestoEletronicoUfSlice []*ManifestoEletronicoUf
	// ManifestoEletronicoUfHook is the signature for custom ManifestoEletronicoUf hook methods
	ManifestoEletronicoUfHook func(context.Context, boil.ContextExecutor, *ManifestoEletronicoUf) error

	manifestoEletronicoUfQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	manifestoEletronicoUfType                 = reflect.TypeOf(&ManifestoEletronicoUf{})
	manifestoEletronicoUfMapping              = queries.MakeStructMapping(manifestoEletronicoUfType)
	manifestoEletronicoUfPrimaryKeyMapping, _ = queries.BindMapping(manifestoEletronicoUfType, manifestoEletronicoUfMapping, manifestoEletronicoUfPrimaryKeyColumns)
	manifestoEletronicoUfInsertCacheMut       sync.RWMutex
	manifestoEletronicoUfInsertCache          = make(map[string]insertCache)
	manifestoEletronicoUfUpdateCacheMut       sync.RWMutex
	manifestoEletronicoUfUpdateCache          = make(map[string]updateCache)
	manifestoEletronicoUfUpsertCacheMut       sync.RWMutex
	manifestoEletronicoUfUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var manifestoEletronicoUfAfterSelectMu sync.Mutex
var manifestoEletronicoUfAfterSelectHooks []ManifestoEletronicoUfHook

var manifestoEletronicoUfBeforeInsertMu sync.Mutex
var manifestoEletronicoUfBeforeInsertHooks []ManifestoEletronicoUfHook
var manifestoEletronicoUfAfterInsertMu sync.Mutex
var manifestoEletronicoUfAfterInsertHooks []ManifestoEletronicoUfHook

var manifestoEletronicoUfBeforeUpdateMu sync.Mutex
var manifestoEletronicoUfBeforeUpdateHooks []ManifestoEletronicoUfHook
var manifestoEletronicoUfAfterUpdateMu sync.Mutex
var manifestoEletronicoUfAfterUpdateHooks []ManifestoEletronicoUfHook

var manifestoEletronicoUfBeforeDeleteMu sync.Mutex
var manifestoEletronicoUfBeforeDeleteHooks []ManifestoEletronicoUfHook
var manifestoEletronicoUfAfterDeleteMu sync.Mutex
var manifestoEletronicoUfAfterDeleteHooks []ManifestoEletronicoUfHook

var manifestoEletronicoUfBeforeUpsertMu sync.Mutex
var manifestoEletronicoUfBeforeUpsertHooks []ManifestoEletronicoUfHook
var manifestoEletronicoUfAfterUpsertMu sync.Mutex
var manifestoEletronicoUfAfterUpsertHooks []ManifestoEletronicoUfHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ManifestoEletronicoUf) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ManifestoEletronicoUf) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ManifestoEletronicoUf) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ManifestoEletronicoUf) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ManifestoEletronicoUf) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ManifestoEletronicoUf) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ManifestoEletronicoUf) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ManifestoEletronicoUf) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ManifestoEletronicoUf) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoUfAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddManifestoEletronicoUfHook registers your hook function for all future operations.
func AddManifestoEletronicoUfHook(hookPoint boil.HookPoint, manifestoEletronicoUfHook ManifestoEletronicoUfHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		manifestoEletronicoUfAfterSelectMu.Lock()
		manifestoEletronicoUfAfterSelectHooks = append(manifestoEletronicoUfAfterSelectHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		manifestoEletronicoUfBeforeInsertMu.Lock()
		manifestoEletronicoUfBeforeInsertHooks = append(manifestoEletronicoUfBeforeInsertHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		manifestoEletronicoUfAfterInsertMu.Lock()
		manifestoEletronicoUfAfterInsertHooks = append(manifestoEletronicoUfAfterInsertHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		manifestoEletronicoUfBeforeUpdateMu.Lock()
		manifestoEletronicoUfBeforeUpdateHooks = append(manifestoEletronicoUfBeforeUpdateHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		manifestoEletronicoUfAfterUpdateMu.Lock()
		manifestoEletronicoUfAfterUpdateHooks = append(manifestoEletronicoUfAfterUpdateHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		manifestoEletronicoUfBeforeDeleteMu.Lock()
		manifestoEletronicoUfBeforeDeleteHooks = append(manifestoEletronicoUfBeforeDeleteHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		manifestoEletronicoUfAfterDeleteMu.Lock()
		manifestoEletronicoUfAfterDeleteHooks = append(manifestoEletronicoUfAfterDeleteHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		manifestoEletronicoUfBeforeUpsertMu.Lock()
		manifestoEletronicoUfBeforeUpsertHooks = append(manifestoEletronicoUfBeforeUpsertHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		manifestoEletronicoUfAfterUpsertMu.Lock()
		manifestoEletronicoUfAfterUpsertHooks = append(manifestoEletronicoUfAfterUpsertHooks, manifestoEletronicoUfHook)
		manifestoEletronicoUfAfterUpsertMu.Unlock()
	}
}

// One returns a single manifestoEletronicoUf record from the query.
func (q manifestoEletronicoUfQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ManifestoEletronicoUf, error) {
	o := &ManifestoEletronicoUf{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for manifesto_eletronico_uf")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ManifestoEletronicoUf records from the query.
func (q manifestoEletronicoUfQuery) All(ctx context.Context, exec boil.ContextExecutor) (ManifestoEletronicoUfSlice, error) {
	var o []*ManifestoEletronicoUf

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ManifestoEletronicoUf slice")
	}

	if len(manifestoEletronicoUfAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ManifestoEletronicoUf records in the query.
func (q manifestoEletronicoUfQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count manifesto_eletronico_uf rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q manifestoEletronicoUfQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if manifesto_eletronico_uf exists")
	}

	return count > 0, nil
}

// Est pointed to by the foreign key.
func (o *ManifestoEletronicoUf) Est(mods ...qm.QueryMod) estadoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`est_id` = ?", o.EstID),
	}

	queryMods = append(queryMods, mods...)

	return Estados(queryMods...)
}

// Mdfe pointed to by the foreign key.
func (o *ManifestoEletronicoUf) Mdfe(mods ...qm.QueryMod) manifestoEletronicoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`mdfe_id` = ?", o.MdfeID),
	}

	queryMods = append(queryMods, mods...)

	return ManifestoEletronicos(queryMods...)
}

// LoadEst allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (manifestoEletronicoUfL) LoadEst(ctx context.Context, e boil.ContextExecutor, singular bool, maybeManifestoEletronicoUf interface{}, mods queries.Applicator) error {
	var slice []*ManifestoEletronicoUf
	var object *ManifestoEletronicoUf

	if singular {
		var ok bool
		object, ok = maybeManifestoEletronicoUf.(*ManifestoEletronicoUf)
		if !ok {
			object = new(ManifestoEletronicoUf)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeManifestoEletronicoUf)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeManifestoEletronicoUf))
			}
		}
	} else {
		s, ok := maybeManifestoEletronicoUf.(*[]*ManifestoEletronicoUf)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeManifestoEletronicoUf)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeManifestoEletronicoUf))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &manifestoEletronicoUfR{}
		}
		args[object.EstID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &manifestoEletronicoUfR{}
			}

			args[obj.EstID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`estado`),
		qm.WhereIn(`estado.est_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Estado")
	}

	var resultSlice []*Estado
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Estado")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for estado")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for estado")
	}

	if len(estadoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Est = foreign
		if foreign.R == nil {
			foreign.R = &estadoR{}
		}
		foreign.R.EstManifestoEletronicoUfs = append(foreign.R.EstManifestoEletronicoUfs, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EstID == foreign.EstID {
				local.R.Est = foreign
				if foreign.R == nil {
					foreign.R = &estadoR{}
				}
				foreign.R.EstManifestoEletronicoUfs = append(foreign.R.EstManifestoEletronicoUfs, local)
				break
			}
		}
	}

	return nil
}

// LoadMdfe allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (manifestoEletronicoUfL) LoadMdfe(ctx context.Context, e boil.ContextExecutor, singular bool, maybeManifestoEletronicoUf interface{}, mods queries.Applicator) error {
	var slice []*ManifestoEletronicoUf
	var object *ManifestoEletronicoUf

	if singular {
		var ok bool
		object, ok = maybeManifestoEletronicoUf.(*ManifestoEletronicoUf)
		if !ok {
			object = new(ManifestoEletronicoUf)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeManifestoEletronicoUf)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeManifestoEletronicoUf))
			}
		}
	} else {
		s, ok := maybeManifestoEletronicoUf.(*[]*ManifestoEletronicoUf)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeManifestoEletronicoUf)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeManifestoEletronicoUf))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &manifestoEletronicoUfR{}
		}
		args[object.MdfeID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &manifestoEletronicoUfR{}
			}

			args[obj.MdfeID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`manifesto_eletronico`),
		qm.WhereIn(`manifesto_eletronico.mdfe_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ManifestoEletronico")
	}

	var resultSlice []*ManifestoEletronico
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ManifestoEletronico")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for manifesto_eletronico")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for manifesto_eletronico")
	}

	if len(manifestoEletronicoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Mdfe = foreign
		if foreign.R == nil {
			foreign.R = &manifestoEletronicoR{}
		}
		foreign.R.MdfeManifestoEletronicoUfs = append(foreign.R.MdfeManifestoEletronicoUfs, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MdfeID == foreign.MdfeID {
				local.R.Mdfe = foreign
				if foreign.R == nil {
					foreign.R = &manifestoEletronicoR{}
				}
				foreign.R.MdfeManifestoEletronicoUfs = append(foreign.R.MdfeManifestoEletronicoUfs, local)
				break
			}
		}
	}

	return nil
}

// SetEst of the manifestoEletronicoUf to the related item.
// Sets o.R.Est to related.
// Adds o to related.R.EstManifestoEletronicoUfs.
func (o *ManifestoEletronicoUf) SetEst(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Estado) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `manifesto_eletronico_uf` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"est_id"}),
		strmangle.WhereClause("`", "`", 0, manifestoEletronicoUfPrimaryKeyColumns),
	)
	values := []interface{}{related.EstID, o.MdfeID, o.EstID, o.MdfeufItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EstID = related.EstID
	if o.R == nil {
		o.R = &manifestoEletronicoUfR{
			Est: related,
		}
	} else {
		o.R.Est = related
	}

	if related.R == nil {
		related.R = &estadoR{
			EstManifestoEletronicoUfs: ManifestoEletronicoUfSlice{o},
		}
	} else {
		related.R.EstManifestoEletronicoUfs = append(related.R.EstManifestoEletronicoUfs, o)
	}

	return nil
}

// SetMdfe of the manifestoEletronicoUf to the related item.
// Sets o.R.Mdfe to related.
// Adds o to related.R.MdfeManifestoEletronicoUfs.
func (o *ManifestoEletronicoUf) SetMdfe(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ManifestoEletronico) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `manifesto_eletronico_uf` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"mdfe_id"}),
		strmangle.WhereClause("`", "`", 0, manifestoEletronicoUfPrimaryKeyColumns),
	)
	values := []interface{}{related.MdfeID, o.MdfeID, o.EstID, o.MdfeufItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MdfeID = related.MdfeID
	if o.R == nil {
		o.R = &manifestoEletronicoUfR{
			Mdfe: related,
		}
	} else {
		o.R.Mdfe = related
	}

	if related.R == nil {
		related.R = &manifestoEletronicoR{
			MdfeManifestoEletronicoUfs: ManifestoEletronicoUfSlice{o},
		}
	} else {
		related.R.MdfeManifestoEletronicoUfs = append(related.R.MdfeManifestoEletronicoUfs, o)
	}

	return nil
}

// ManifestoEletronicoUfs retrieves all the records using an executor.
func ManifestoEletronicoUfs(mods ...qm.QueryMod) manifestoEletronicoUfQuery {
	mods = append(mods, qm.From("`manifesto_eletronico_uf`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`manifesto_eletronico_uf`.*"})
	}

	return manifestoEletronicoUfQuery{q}
}

// FindManifestoEletronicoUf retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindManifestoEletronicoUf(ctx context.Context, exec boil.ContextExecutor, mdfeID int, estID int, mdfeufItem int16, selectCols ...string) (*ManifestoEletronicoUf, error) {
	manifestoEletronicoUfObj := &ManifestoEletronicoUf{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `manifesto_eletronico_uf` where `mdfe_id`=? AND `est_id`=? AND `mdfeuf_item`=?", sel,
	)

	q := queries.Raw(query, mdfeID, estID, mdfeufItem)

	err := q.Bind(ctx, exec, manifestoEletronicoUfObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from manifesto_eletronico_uf")
	}

	if err = manifestoEletronicoUfObj.doAfterSelectHooks(ctx, exec); err != nil {
		return manifestoEletronicoUfObj, err
	}

	return manifestoEletronicoUfObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ManifestoEletronicoUf) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no manifesto_eletronico_uf provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(manifestoEletronicoUfColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	manifestoEletronicoUfInsertCacheMut.RLock()
	cache, cached := manifestoEletronicoUfInsertCache[key]
	manifestoEletronicoUfInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			manifestoEletronicoUfAllColumns,
			manifestoEletronicoUfColumnsWithDefault,
			manifestoEletronicoUfColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(manifestoEletronicoUfType, manifestoEletronicoUfMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(manifestoEletronicoUfType, manifestoEletronicoUfMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `manifesto_eletronico_uf` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `manifesto_eletronico_uf` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `manifesto_eletronico_uf` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, manifestoEletronicoUfPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into manifesto_eletronico_uf")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.MdfeID,
		o.EstID,
		o.MdfeufItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for manifesto_eletronico_uf")
	}

CacheNoHooks:
	if !cached {
		manifestoEletronicoUfInsertCacheMut.Lock()
		manifestoEletronicoUfInsertCache[key] = cache
		manifestoEletronicoUfInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ManifestoEletronicoUf.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ManifestoEletronicoUf) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	manifestoEletronicoUfUpdateCacheMut.RLock()
	cache, cached := manifestoEletronicoUfUpdateCache[key]
	manifestoEletronicoUfUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			manifestoEletronicoUfAllColumns,
			manifestoEletronicoUfPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update manifesto_eletronico_uf, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `manifesto_eletronico_uf` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, manifestoEletronicoUfPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(manifestoEletronicoUfType, manifestoEletronicoUfMapping, append(wl, manifestoEletronicoUfPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update manifesto_eletronico_uf row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for manifesto_eletronico_uf")
	}

	if !cached {
		manifestoEletronicoUfUpdateCacheMut.Lock()
		manifestoEletronicoUfUpdateCache[key] = cache
		manifestoEletronicoUfUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q manifestoEletronicoUfQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for manifesto_eletronico_uf")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for manifesto_eletronico_uf")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ManifestoEletronicoUfSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), manifestoEletronicoUfPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `manifesto_eletronico_uf` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, manifestoEletronicoUfPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in manifestoEletronicoUf slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all manifestoEletronicoUf")
	}
	return rowsAff, nil
}

var mySQLManifestoEletronicoUfUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ManifestoEletronicoUf) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no manifesto_eletronico_uf provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(manifestoEletronicoUfColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLManifestoEletronicoUfUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	manifestoEletronicoUfUpsertCacheMut.RLock()
	cache, cached := manifestoEletronicoUfUpsertCache[key]
	manifestoEletronicoUfUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			manifestoEletronicoUfAllColumns,
			manifestoEletronicoUfColumnsWithDefault,
			manifestoEletronicoUfColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			manifestoEletronicoUfAllColumns,
			manifestoEletronicoUfPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert manifesto_eletronico_uf, could not build update column list")
		}

		ret := strmangle.SetComplement(manifestoEletronicoUfAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`manifesto_eletronico_uf`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `manifesto_eletronico_uf` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(manifestoEletronicoUfType, manifestoEletronicoUfMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(manifestoEletronicoUfType, manifestoEletronicoUfMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for manifesto_eletronico_uf")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(manifestoEletronicoUfType, manifestoEletronicoUfMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for manifesto_eletronico_uf")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for manifesto_eletronico_uf")
	}

CacheNoHooks:
	if !cached {
		manifestoEletronicoUfUpsertCacheMut.Lock()
		manifestoEletronicoUfUpsertCache[key] = cache
		manifestoEletronicoUfUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ManifestoEletronicoUf record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ManifestoEletronicoUf) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ManifestoEletronicoUf provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), manifestoEletronicoUfPrimaryKeyMapping)
	sql := "DELETE FROM `manifesto_eletronico_uf` WHERE `mdfe_id`=? AND `est_id`=? AND `mdfeuf_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from manifesto_eletronico_uf")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for manifesto_eletronico_uf")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q manifestoEletronicoUfQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no manifestoEletronicoUfQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from manifesto_eletronico_uf")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for manifesto_eletronico_uf")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ManifestoEletronicoUfSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(manifestoEletronicoUfBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), manifestoEletronicoUfPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `manifesto_eletronico_uf` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, manifestoEletronicoUfPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from manifestoEletronicoUf slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for manifesto_eletronico_uf")
	}

	if len(manifestoEletronicoUfAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ManifestoEletronicoUf) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindManifestoEletronicoUf(ctx, exec, o.MdfeID, o.EstID, o.MdfeufItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ManifestoEletronicoUfSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ManifestoEletronicoUfSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), manifestoEletronicoUfPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `manifesto_eletronico_uf`.* FROM `manifesto_eletronico_uf` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, manifestoEletronicoUfPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ManifestoEletronicoUfSlice")
	}

	*o = slice

	return nil
}

// ManifestoEletronicoUfExists checks if the ManifestoEletronicoUf row exists.
func ManifestoEletronicoUfExists(ctx context.Context, exec boil.ContextExecutor, mdfeID int, estID int, mdfeufItem int16) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `manifesto_eletronico_uf` where `mdfe_id`=? AND `est_id`=? AND `mdfeuf_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, mdfeID, estID, mdfeufItem)
	}
	row := exec.QueryRowContext(ctx, sql, mdfeID, estID, mdfeufItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if manifesto_eletronico_uf exists")
	}

	return exists, nil
}

// Exists checks if the ManifestoEletronicoUf row exists.
func (o *ManifestoEletronicoUf) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ManifestoEletronicoUfExists(ctx, exec, o.MdfeID, o.EstID, o.MdfeufItem)
}
