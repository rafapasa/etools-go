// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ManifestoEletronicoMun is an object representing the database table.
type ManifestoEletronicoMun struct {
	MunID       int `boil:"mun_id" json:"mun_id" toml:"mun_id" yaml:"mun_id"`
	MdfeID      int `boil:"mdfe_id" json:"mdfe_id" toml:"mdfe_id" yaml:"mdfe_id"`
	MdfemunItem int `boil:"mdfemun_item" json:"mdfemun_item" toml:"mdfemun_item" yaml:"mdfemun_item"`

	R *manifestoEletronicoMunR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L manifestoEletronicoMunL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ManifestoEletronicoMunColumns = struct {
	MunID       string
	MdfeID      string
	MdfemunItem string
}{
	MunID:       "mun_id",
	MdfeID:      "mdfe_id",
	MdfemunItem: "mdfemun_item",
}

var ManifestoEletronicoMunTableColumns = struct {
	MunID       string
	MdfeID      string
	MdfemunItem string
}{
	MunID:       "manifesto_eletronico_mun.mun_id",
	MdfeID:      "manifesto_eletronico_mun.mdfe_id",
	MdfemunItem: "manifesto_eletronico_mun.mdfemun_item",
}

// Generated where

var ManifestoEletronicoMunWhere = struct {
	MunID       whereHelperint
	MdfeID      whereHelperint
	MdfemunItem whereHelperint
}{
	MunID:       whereHelperint{field: "`manifesto_eletronico_mun`.`mun_id`"},
	MdfeID:      whereHelperint{field: "`manifesto_eletronico_mun`.`mdfe_id`"},
	MdfemunItem: whereHelperint{field: "`manifesto_eletronico_mun`.`mdfemun_item`"},
}

// ManifestoEletronicoMunRels is where relationship names are stored.
var ManifestoEletronicoMunRels = struct {
	Mdfe string
	Mun  string
}{
	Mdfe: "Mdfe",
	Mun:  "Mun",
}

// manifestoEletronicoMunR is where relationships are stored.
type manifestoEletronicoMunR struct {
	Mdfe *ManifestoEletronico `boil:"Mdfe" json:"Mdfe" toml:"Mdfe" yaml:"Mdfe"`
	Mun  *Municipio           `boil:"Mun" json:"Mun" toml:"Mun" yaml:"Mun"`
}

// NewStruct creates a new relationship struct
func (*manifestoEletronicoMunR) NewStruct() *manifestoEletronicoMunR {
	return &manifestoEletronicoMunR{}
}

func (r *manifestoEletronicoMunR) GetMdfe() *ManifestoEletronico {
	if r == nil {
		return nil
	}
	return r.Mdfe
}

func (r *manifestoEletronicoMunR) GetMun() *Municipio {
	if r == nil {
		return nil
	}
	return r.Mun
}

// manifestoEletronicoMunL is where Load methods for each relationship are stored.
type manifestoEletronicoMunL struct{}

var (
	manifestoEletronicoMunAllColumns            = []string{"mun_id", "mdfe_id", "mdfemun_item"}
	manifestoEletronicoMunColumnsWithoutDefault = []string{"mun_id", "mdfe_id", "mdfemun_item"}
	manifestoEletronicoMunColumnsWithDefault    = []string{}
	manifestoEletronicoMunPrimaryKeyColumns     = []string{"mun_id", "mdfe_id", "mdfemun_item"}
	manifestoEletronicoMunGeneratedColumns      = []string{}
)

type (
	// ManifestoEletronicoMunSlice is an alias for a slice of pointers to ManifestoEletronicoMun.
	// This should almost always be used instead of []ManifestoEletronicoMun.
	ManifestoEletronicoMunSlice []*ManifestoEletronicoMun
	// ManifestoEletronicoMunHook is the signature for custom ManifestoEletronicoMun hook methods
	ManifestoEletronicoMunHook func(context.Context, boil.ContextExecutor, *ManifestoEletronicoMun) error

	manifestoEletronicoMunQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	manifestoEletronicoMunType                 = reflect.TypeOf(&ManifestoEletronicoMun{})
	manifestoEletronicoMunMapping              = queries.MakeStructMapping(manifestoEletronicoMunType)
	manifestoEletronicoMunPrimaryKeyMapping, _ = queries.BindMapping(manifestoEletronicoMunType, manifestoEletronicoMunMapping, manifestoEletronicoMunPrimaryKeyColumns)
	manifestoEletronicoMunInsertCacheMut       sync.RWMutex
	manifestoEletronicoMunInsertCache          = make(map[string]insertCache)
	manifestoEletronicoMunUpdateCacheMut       sync.RWMutex
	manifestoEletronicoMunUpdateCache          = make(map[string]updateCache)
	manifestoEletronicoMunUpsertCacheMut       sync.RWMutex
	manifestoEletronicoMunUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var manifestoEletronicoMunAfterSelectMu sync.Mutex
var manifestoEletronicoMunAfterSelectHooks []ManifestoEletronicoMunHook

var manifestoEletronicoMunBeforeInsertMu sync.Mutex
var manifestoEletronicoMunBeforeInsertHooks []ManifestoEletronicoMunHook
var manifestoEletronicoMunAfterInsertMu sync.Mutex
var manifestoEletronicoMunAfterInsertHooks []ManifestoEletronicoMunHook

var manifestoEletronicoMunBeforeUpdateMu sync.Mutex
var manifestoEletronicoMunBeforeUpdateHooks []ManifestoEletronicoMunHook
var manifestoEletronicoMunAfterUpdateMu sync.Mutex
var manifestoEletronicoMunAfterUpdateHooks []ManifestoEletronicoMunHook

var manifestoEletronicoMunBeforeDeleteMu sync.Mutex
var manifestoEletronicoMunBeforeDeleteHooks []ManifestoEletronicoMunHook
var manifestoEletronicoMunAfterDeleteMu sync.Mutex
var manifestoEletronicoMunAfterDeleteHooks []ManifestoEletronicoMunHook

var manifestoEletronicoMunBeforeUpsertMu sync.Mutex
var manifestoEletronicoMunBeforeUpsertHooks []ManifestoEletronicoMunHook
var manifestoEletronicoMunAfterUpsertMu sync.Mutex
var manifestoEletronicoMunAfterUpsertHooks []ManifestoEletronicoMunHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ManifestoEletronicoMun) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ManifestoEletronicoMun) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ManifestoEletronicoMun) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ManifestoEletronicoMun) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ManifestoEletronicoMun) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ManifestoEletronicoMun) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ManifestoEletronicoMun) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ManifestoEletronicoMun) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ManifestoEletronicoMun) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range manifestoEletronicoMunAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddManifestoEletronicoMunHook registers your hook function for all future operations.
func AddManifestoEletronicoMunHook(hookPoint boil.HookPoint, manifestoEletronicoMunHook ManifestoEletronicoMunHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		manifestoEletronicoMunAfterSelectMu.Lock()
		manifestoEletronicoMunAfterSelectHooks = append(manifestoEletronicoMunAfterSelectHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		manifestoEletronicoMunBeforeInsertMu.Lock()
		manifestoEletronicoMunBeforeInsertHooks = append(manifestoEletronicoMunBeforeInsertHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		manifestoEletronicoMunAfterInsertMu.Lock()
		manifestoEletronicoMunAfterInsertHooks = append(manifestoEletronicoMunAfterInsertHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		manifestoEletronicoMunBeforeUpdateMu.Lock()
		manifestoEletronicoMunBeforeUpdateHooks = append(manifestoEletronicoMunBeforeUpdateHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		manifestoEletronicoMunAfterUpdateMu.Lock()
		manifestoEletronicoMunAfterUpdateHooks = append(manifestoEletronicoMunAfterUpdateHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		manifestoEletronicoMunBeforeDeleteMu.Lock()
		manifestoEletronicoMunBeforeDeleteHooks = append(manifestoEletronicoMunBeforeDeleteHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		manifestoEletronicoMunAfterDeleteMu.Lock()
		manifestoEletronicoMunAfterDeleteHooks = append(manifestoEletronicoMunAfterDeleteHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		manifestoEletronicoMunBeforeUpsertMu.Lock()
		manifestoEletronicoMunBeforeUpsertHooks = append(manifestoEletronicoMunBeforeUpsertHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		manifestoEletronicoMunAfterUpsertMu.Lock()
		manifestoEletronicoMunAfterUpsertHooks = append(manifestoEletronicoMunAfterUpsertHooks, manifestoEletronicoMunHook)
		manifestoEletronicoMunAfterUpsertMu.Unlock()
	}
}

// One returns a single manifestoEletronicoMun record from the query.
func (q manifestoEletronicoMunQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ManifestoEletronicoMun, error) {
	o := &ManifestoEletronicoMun{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for manifesto_eletronico_mun")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ManifestoEletronicoMun records from the query.
func (q manifestoEletronicoMunQuery) All(ctx context.Context, exec boil.ContextExecutor) (ManifestoEletronicoMunSlice, error) {
	var o []*ManifestoEletronicoMun

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ManifestoEletronicoMun slice")
	}

	if len(manifestoEletronicoMunAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ManifestoEletronicoMun records in the query.
func (q manifestoEletronicoMunQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count manifesto_eletronico_mun rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q manifestoEletronicoMunQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if manifesto_eletronico_mun exists")
	}

	return count > 0, nil
}

// Mdfe pointed to by the foreign key.
func (o *ManifestoEletronicoMun) Mdfe(mods ...qm.QueryMod) manifestoEletronicoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`mdfe_id` = ?", o.MdfeID),
	}

	queryMods = append(queryMods, mods...)

	return ManifestoEletronicos(queryMods...)
}

// Mun pointed to by the foreign key.
func (o *ManifestoEletronicoMun) Mun(mods ...qm.QueryMod) municipioQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`mun_id` = ?", o.MunID),
	}

	queryMods = append(queryMods, mods...)

	return Municipios(queryMods...)
}

// LoadMdfe allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (manifestoEletronicoMunL) LoadMdfe(ctx context.Context, e boil.ContextExecutor, singular bool, maybeManifestoEletronicoMun interface{}, mods queries.Applicator) error {
	var slice []*ManifestoEletronicoMun
	var object *ManifestoEletronicoMun

	if singular {
		var ok bool
		object, ok = maybeManifestoEletronicoMun.(*ManifestoEletronicoMun)
		if !ok {
			object = new(ManifestoEletronicoMun)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeManifestoEletronicoMun)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeManifestoEletronicoMun))
			}
		}
	} else {
		s, ok := maybeManifestoEletronicoMun.(*[]*ManifestoEletronicoMun)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeManifestoEletronicoMun)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeManifestoEletronicoMun))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &manifestoEletronicoMunR{}
		}
		args[object.MdfeID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &manifestoEletronicoMunR{}
			}

			args[obj.MdfeID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`manifesto_eletronico`),
		qm.WhereIn(`manifesto_eletronico.mdfe_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ManifestoEletronico")
	}

	var resultSlice []*ManifestoEletronico
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ManifestoEletronico")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for manifesto_eletronico")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for manifesto_eletronico")
	}

	if len(manifestoEletronicoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Mdfe = foreign
		if foreign.R == nil {
			foreign.R = &manifestoEletronicoR{}
		}
		foreign.R.MdfeManifestoEletronicoMuns = append(foreign.R.MdfeManifestoEletronicoMuns, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MdfeID == foreign.MdfeID {
				local.R.Mdfe = foreign
				if foreign.R == nil {
					foreign.R = &manifestoEletronicoR{}
				}
				foreign.R.MdfeManifestoEletronicoMuns = append(foreign.R.MdfeManifestoEletronicoMuns, local)
				break
			}
		}
	}

	return nil
}

// LoadMun allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (manifestoEletronicoMunL) LoadMun(ctx context.Context, e boil.ContextExecutor, singular bool, maybeManifestoEletronicoMun interface{}, mods queries.Applicator) error {
	var slice []*ManifestoEletronicoMun
	var object *ManifestoEletronicoMun

	if singular {
		var ok bool
		object, ok = maybeManifestoEletronicoMun.(*ManifestoEletronicoMun)
		if !ok {
			object = new(ManifestoEletronicoMun)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeManifestoEletronicoMun)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeManifestoEletronicoMun))
			}
		}
	} else {
		s, ok := maybeManifestoEletronicoMun.(*[]*ManifestoEletronicoMun)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeManifestoEletronicoMun)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeManifestoEletronicoMun))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &manifestoEletronicoMunR{}
		}
		args[object.MunID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &manifestoEletronicoMunR{}
			}

			args[obj.MunID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`municipio`),
		qm.WhereIn(`municipio.mun_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Municipio")
	}

	var resultSlice []*Municipio
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Municipio")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for municipio")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for municipio")
	}

	if len(municipioAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Mun = foreign
		if foreign.R == nil {
			foreign.R = &municipioR{}
		}
		foreign.R.MunManifestoEletronicoMuns = append(foreign.R.MunManifestoEletronicoMuns, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MunID == foreign.MunID {
				local.R.Mun = foreign
				if foreign.R == nil {
					foreign.R = &municipioR{}
				}
				foreign.R.MunManifestoEletronicoMuns = append(foreign.R.MunManifestoEletronicoMuns, local)
				break
			}
		}
	}

	return nil
}

// SetMdfe of the manifestoEletronicoMun to the related item.
// Sets o.R.Mdfe to related.
// Adds o to related.R.MdfeManifestoEletronicoMuns.
func (o *ManifestoEletronicoMun) SetMdfe(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ManifestoEletronico) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `manifesto_eletronico_mun` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"mdfe_id"}),
		strmangle.WhereClause("`", "`", 0, manifestoEletronicoMunPrimaryKeyColumns),
	)
	values := []interface{}{related.MdfeID, o.MunID, o.MdfeID, o.MdfemunItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MdfeID = related.MdfeID
	if o.R == nil {
		o.R = &manifestoEletronicoMunR{
			Mdfe: related,
		}
	} else {
		o.R.Mdfe = related
	}

	if related.R == nil {
		related.R = &manifestoEletronicoR{
			MdfeManifestoEletronicoMuns: ManifestoEletronicoMunSlice{o},
		}
	} else {
		related.R.MdfeManifestoEletronicoMuns = append(related.R.MdfeManifestoEletronicoMuns, o)
	}

	return nil
}

// SetMun of the manifestoEletronicoMun to the related item.
// Sets o.R.Mun to related.
// Adds o to related.R.MunManifestoEletronicoMuns.
func (o *ManifestoEletronicoMun) SetMun(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Municipio) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `manifesto_eletronico_mun` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"mun_id"}),
		strmangle.WhereClause("`", "`", 0, manifestoEletronicoMunPrimaryKeyColumns),
	)
	values := []interface{}{related.MunID, o.MunID, o.MdfeID, o.MdfemunItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MunID = related.MunID
	if o.R == nil {
		o.R = &manifestoEletronicoMunR{
			Mun: related,
		}
	} else {
		o.R.Mun = related
	}

	if related.R == nil {
		related.R = &municipioR{
			MunManifestoEletronicoMuns: ManifestoEletronicoMunSlice{o},
		}
	} else {
		related.R.MunManifestoEletronicoMuns = append(related.R.MunManifestoEletronicoMuns, o)
	}

	return nil
}

// ManifestoEletronicoMuns retrieves all the records using an executor.
func ManifestoEletronicoMuns(mods ...qm.QueryMod) manifestoEletronicoMunQuery {
	mods = append(mods, qm.From("`manifesto_eletronico_mun`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`manifesto_eletronico_mun`.*"})
	}

	return manifestoEletronicoMunQuery{q}
}

// FindManifestoEletronicoMun retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindManifestoEletronicoMun(ctx context.Context, exec boil.ContextExecutor, munID int, mdfeID int, mdfemunItem int, selectCols ...string) (*ManifestoEletronicoMun, error) {
	manifestoEletronicoMunObj := &ManifestoEletronicoMun{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `manifesto_eletronico_mun` where `mun_id`=? AND `mdfe_id`=? AND `mdfemun_item`=?", sel,
	)

	q := queries.Raw(query, munID, mdfeID, mdfemunItem)

	err := q.Bind(ctx, exec, manifestoEletronicoMunObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from manifesto_eletronico_mun")
	}

	if err = manifestoEletronicoMunObj.doAfterSelectHooks(ctx, exec); err != nil {
		return manifestoEletronicoMunObj, err
	}

	return manifestoEletronicoMunObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ManifestoEletronicoMun) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no manifesto_eletronico_mun provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(manifestoEletronicoMunColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	manifestoEletronicoMunInsertCacheMut.RLock()
	cache, cached := manifestoEletronicoMunInsertCache[key]
	manifestoEletronicoMunInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			manifestoEletronicoMunAllColumns,
			manifestoEletronicoMunColumnsWithDefault,
			manifestoEletronicoMunColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(manifestoEletronicoMunType, manifestoEletronicoMunMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(manifestoEletronicoMunType, manifestoEletronicoMunMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `manifesto_eletronico_mun` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `manifesto_eletronico_mun` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `manifesto_eletronico_mun` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, manifestoEletronicoMunPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into manifesto_eletronico_mun")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.MunID,
		o.MdfeID,
		o.MdfemunItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for manifesto_eletronico_mun")
	}

CacheNoHooks:
	if !cached {
		manifestoEletronicoMunInsertCacheMut.Lock()
		manifestoEletronicoMunInsertCache[key] = cache
		manifestoEletronicoMunInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ManifestoEletronicoMun.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ManifestoEletronicoMun) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	manifestoEletronicoMunUpdateCacheMut.RLock()
	cache, cached := manifestoEletronicoMunUpdateCache[key]
	manifestoEletronicoMunUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			manifestoEletronicoMunAllColumns,
			manifestoEletronicoMunPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update manifesto_eletronico_mun, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `manifesto_eletronico_mun` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, manifestoEletronicoMunPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(manifestoEletronicoMunType, manifestoEletronicoMunMapping, append(wl, manifestoEletronicoMunPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update manifesto_eletronico_mun row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for manifesto_eletronico_mun")
	}

	if !cached {
		manifestoEletronicoMunUpdateCacheMut.Lock()
		manifestoEletronicoMunUpdateCache[key] = cache
		manifestoEletronicoMunUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q manifestoEletronicoMunQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for manifesto_eletronico_mun")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for manifesto_eletronico_mun")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ManifestoEletronicoMunSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), manifestoEletronicoMunPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `manifesto_eletronico_mun` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, manifestoEletronicoMunPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in manifestoEletronicoMun slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all manifestoEletronicoMun")
	}
	return rowsAff, nil
}

var mySQLManifestoEletronicoMunUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ManifestoEletronicoMun) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no manifesto_eletronico_mun provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(manifestoEletronicoMunColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLManifestoEletronicoMunUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	manifestoEletronicoMunUpsertCacheMut.RLock()
	cache, cached := manifestoEletronicoMunUpsertCache[key]
	manifestoEletronicoMunUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			manifestoEletronicoMunAllColumns,
			manifestoEletronicoMunColumnsWithDefault,
			manifestoEletronicoMunColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			manifestoEletronicoMunAllColumns,
			manifestoEletronicoMunPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert manifesto_eletronico_mun, could not build update column list")
		}

		ret := strmangle.SetComplement(manifestoEletronicoMunAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`manifesto_eletronico_mun`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `manifesto_eletronico_mun` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(manifestoEletronicoMunType, manifestoEletronicoMunMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(manifestoEletronicoMunType, manifestoEletronicoMunMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for manifesto_eletronico_mun")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(manifestoEletronicoMunType, manifestoEletronicoMunMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for manifesto_eletronico_mun")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for manifesto_eletronico_mun")
	}

CacheNoHooks:
	if !cached {
		manifestoEletronicoMunUpsertCacheMut.Lock()
		manifestoEletronicoMunUpsertCache[key] = cache
		manifestoEletronicoMunUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ManifestoEletronicoMun record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ManifestoEletronicoMun) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ManifestoEletronicoMun provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), manifestoEletronicoMunPrimaryKeyMapping)
	sql := "DELETE FROM `manifesto_eletronico_mun` WHERE `mun_id`=? AND `mdfe_id`=? AND `mdfemun_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from manifesto_eletronico_mun")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for manifesto_eletronico_mun")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q manifestoEletronicoMunQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no manifestoEletronicoMunQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from manifesto_eletronico_mun")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for manifesto_eletronico_mun")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ManifestoEletronicoMunSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(manifestoEletronicoMunBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), manifestoEletronicoMunPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `manifesto_eletronico_mun` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, manifestoEletronicoMunPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from manifestoEletronicoMun slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for manifesto_eletronico_mun")
	}

	if len(manifestoEletronicoMunAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ManifestoEletronicoMun) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindManifestoEletronicoMun(ctx, exec, o.MunID, o.MdfeID, o.MdfemunItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ManifestoEletronicoMunSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ManifestoEletronicoMunSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), manifestoEletronicoMunPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `manifesto_eletronico_mun`.* FROM `manifesto_eletronico_mun` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, manifestoEletronicoMunPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ManifestoEletronicoMunSlice")
	}

	*o = slice

	return nil
}

// ManifestoEletronicoMunExists checks if the ManifestoEletronicoMun row exists.
func ManifestoEletronicoMunExists(ctx context.Context, exec boil.ContextExecutor, munID int, mdfeID int, mdfemunItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `manifesto_eletronico_mun` where `mun_id`=? AND `mdfe_id`=? AND `mdfemun_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, munID, mdfeID, mdfemunItem)
	}
	row := exec.QueryRowContext(ctx, sql, munID, mdfeID, mdfemunItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if manifesto_eletronico_mun exists")
	}

	return exists, nil
}

// Exists checks if the ManifestoEletronicoMun row exists.
func (o *ManifestoEletronicoMun) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ManifestoEletronicoMunExists(ctx, exec, o.MunID, o.MdfeID, o.MdfemunItem)
}
