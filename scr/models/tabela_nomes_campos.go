// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TabelaNomesCampo is an object representing the database table.
type TabelaNomesCampo struct {
	TBNID    int    `boil:"tbn_id" json:"tbn_id" toml:"tbn_id" yaml:"tbn_id"`
	TNCItem  int    `boil:"tnc_item" json:"tnc_item" toml:"tnc_item" yaml:"tnc_item"`
	TNCCampo string `boil:"tnc_campo" json:"tnc_campo" toml:"tnc_campo" yaml:"tnc_campo"`
	TNCNome  string `boil:"tnc_nome" json:"tnc_nome" toml:"tnc_nome" yaml:"tnc_nome"`

	R *tabelaNomesCampoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tabelaNomesCampoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TabelaNomesCampoColumns = struct {
	TBNID    string
	TNCItem  string
	TNCCampo string
	TNCNome  string
}{
	TBNID:    "tbn_id",
	TNCItem:  "tnc_item",
	TNCCampo: "tnc_campo",
	TNCNome:  "tnc_nome",
}

var TabelaNomesCampoTableColumns = struct {
	TBNID    string
	TNCItem  string
	TNCCampo string
	TNCNome  string
}{
	TBNID:    "tabela_nomes_campos.tbn_id",
	TNCItem:  "tabela_nomes_campos.tnc_item",
	TNCCampo: "tabela_nomes_campos.tnc_campo",
	TNCNome:  "tabela_nomes_campos.tnc_nome",
}

// Generated where

var TabelaNomesCampoWhere = struct {
	TBNID    whereHelperint
	TNCItem  whereHelperint
	TNCCampo whereHelperstring
	TNCNome  whereHelperstring
}{
	TBNID:    whereHelperint{field: "`tabela_nomes_campos`.`tbn_id`"},
	TNCItem:  whereHelperint{field: "`tabela_nomes_campos`.`tnc_item`"},
	TNCCampo: whereHelperstring{field: "`tabela_nomes_campos`.`tnc_campo`"},
	TNCNome:  whereHelperstring{field: "`tabela_nomes_campos`.`tnc_nome`"},
}

// TabelaNomesCampoRels is where relationship names are stored.
var TabelaNomesCampoRels = struct {
	TBN string
}{
	TBN: "TBN",
}

// tabelaNomesCampoR is where relationships are stored.
type tabelaNomesCampoR struct {
	TBN *TabelaNome `boil:"TBN" json:"TBN" toml:"TBN" yaml:"TBN"`
}

// NewStruct creates a new relationship struct
func (*tabelaNomesCampoR) NewStruct() *tabelaNomesCampoR {
	return &tabelaNomesCampoR{}
}

func (r *tabelaNomesCampoR) GetTBN() *TabelaNome {
	if r == nil {
		return nil
	}
	return r.TBN
}

// tabelaNomesCampoL is where Load methods for each relationship are stored.
type tabelaNomesCampoL struct{}

var (
	tabelaNomesCampoAllColumns            = []string{"tbn_id", "tnc_item", "tnc_campo", "tnc_nome"}
	tabelaNomesCampoColumnsWithoutDefault = []string{"tbn_id", "tnc_item", "tnc_campo", "tnc_nome"}
	tabelaNomesCampoColumnsWithDefault    = []string{}
	tabelaNomesCampoPrimaryKeyColumns     = []string{"tbn_id", "tnc_item"}
	tabelaNomesCampoGeneratedColumns      = []string{}
)

type (
	// TabelaNomesCampoSlice is an alias for a slice of pointers to TabelaNomesCampo.
	// This should almost always be used instead of []TabelaNomesCampo.
	TabelaNomesCampoSlice []*TabelaNomesCampo
	// TabelaNomesCampoHook is the signature for custom TabelaNomesCampo hook methods
	TabelaNomesCampoHook func(context.Context, boil.ContextExecutor, *TabelaNomesCampo) error

	tabelaNomesCampoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tabelaNomesCampoType                 = reflect.TypeOf(&TabelaNomesCampo{})
	tabelaNomesCampoMapping              = queries.MakeStructMapping(tabelaNomesCampoType)
	tabelaNomesCampoPrimaryKeyMapping, _ = queries.BindMapping(tabelaNomesCampoType, tabelaNomesCampoMapping, tabelaNomesCampoPrimaryKeyColumns)
	tabelaNomesCampoInsertCacheMut       sync.RWMutex
	tabelaNomesCampoInsertCache          = make(map[string]insertCache)
	tabelaNomesCampoUpdateCacheMut       sync.RWMutex
	tabelaNomesCampoUpdateCache          = make(map[string]updateCache)
	tabelaNomesCampoUpsertCacheMut       sync.RWMutex
	tabelaNomesCampoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tabelaNomesCampoAfterSelectMu sync.Mutex
var tabelaNomesCampoAfterSelectHooks []TabelaNomesCampoHook

var tabelaNomesCampoBeforeInsertMu sync.Mutex
var tabelaNomesCampoBeforeInsertHooks []TabelaNomesCampoHook
var tabelaNomesCampoAfterInsertMu sync.Mutex
var tabelaNomesCampoAfterInsertHooks []TabelaNomesCampoHook

var tabelaNomesCampoBeforeUpdateMu sync.Mutex
var tabelaNomesCampoBeforeUpdateHooks []TabelaNomesCampoHook
var tabelaNomesCampoAfterUpdateMu sync.Mutex
var tabelaNomesCampoAfterUpdateHooks []TabelaNomesCampoHook

var tabelaNomesCampoBeforeDeleteMu sync.Mutex
var tabelaNomesCampoBeforeDeleteHooks []TabelaNomesCampoHook
var tabelaNomesCampoAfterDeleteMu sync.Mutex
var tabelaNomesCampoAfterDeleteHooks []TabelaNomesCampoHook

var tabelaNomesCampoBeforeUpsertMu sync.Mutex
var tabelaNomesCampoBeforeUpsertHooks []TabelaNomesCampoHook
var tabelaNomesCampoAfterUpsertMu sync.Mutex
var tabelaNomesCampoAfterUpsertHooks []TabelaNomesCampoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TabelaNomesCampo) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TabelaNomesCampo) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TabelaNomesCampo) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TabelaNomesCampo) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TabelaNomesCampo) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TabelaNomesCampo) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TabelaNomesCampo) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TabelaNomesCampo) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TabelaNomesCampo) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaNomesCampoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTabelaNomesCampoHook registers your hook function for all future operations.
func AddTabelaNomesCampoHook(hookPoint boil.HookPoint, tabelaNomesCampoHook TabelaNomesCampoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tabelaNomesCampoAfterSelectMu.Lock()
		tabelaNomesCampoAfterSelectHooks = append(tabelaNomesCampoAfterSelectHooks, tabelaNomesCampoHook)
		tabelaNomesCampoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tabelaNomesCampoBeforeInsertMu.Lock()
		tabelaNomesCampoBeforeInsertHooks = append(tabelaNomesCampoBeforeInsertHooks, tabelaNomesCampoHook)
		tabelaNomesCampoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tabelaNomesCampoAfterInsertMu.Lock()
		tabelaNomesCampoAfterInsertHooks = append(tabelaNomesCampoAfterInsertHooks, tabelaNomesCampoHook)
		tabelaNomesCampoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tabelaNomesCampoBeforeUpdateMu.Lock()
		tabelaNomesCampoBeforeUpdateHooks = append(tabelaNomesCampoBeforeUpdateHooks, tabelaNomesCampoHook)
		tabelaNomesCampoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tabelaNomesCampoAfterUpdateMu.Lock()
		tabelaNomesCampoAfterUpdateHooks = append(tabelaNomesCampoAfterUpdateHooks, tabelaNomesCampoHook)
		tabelaNomesCampoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tabelaNomesCampoBeforeDeleteMu.Lock()
		tabelaNomesCampoBeforeDeleteHooks = append(tabelaNomesCampoBeforeDeleteHooks, tabelaNomesCampoHook)
		tabelaNomesCampoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tabelaNomesCampoAfterDeleteMu.Lock()
		tabelaNomesCampoAfterDeleteHooks = append(tabelaNomesCampoAfterDeleteHooks, tabelaNomesCampoHook)
		tabelaNomesCampoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tabelaNomesCampoBeforeUpsertMu.Lock()
		tabelaNomesCampoBeforeUpsertHooks = append(tabelaNomesCampoBeforeUpsertHooks, tabelaNomesCampoHook)
		tabelaNomesCampoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tabelaNomesCampoAfterUpsertMu.Lock()
		tabelaNomesCampoAfterUpsertHooks = append(tabelaNomesCampoAfterUpsertHooks, tabelaNomesCampoHook)
		tabelaNomesCampoAfterUpsertMu.Unlock()
	}
}

// One returns a single tabelaNomesCampo record from the query.
func (q tabelaNomesCampoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TabelaNomesCampo, error) {
	o := &TabelaNomesCampo{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for tabela_nomes_campos")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TabelaNomesCampo records from the query.
func (q tabelaNomesCampoQuery) All(ctx context.Context, exec boil.ContextExecutor) (TabelaNomesCampoSlice, error) {
	var o []*TabelaNomesCampo

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TabelaNomesCampo slice")
	}

	if len(tabelaNomesCampoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TabelaNomesCampo records in the query.
func (q tabelaNomesCampoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count tabela_nomes_campos rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tabelaNomesCampoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if tabela_nomes_campos exists")
	}

	return count > 0, nil
}

// TBN pointed to by the foreign key.
func (o *TabelaNomesCampo) TBN(mods ...qm.QueryMod) tabelaNomeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`tbn_id` = ?", o.TBNID),
	}

	queryMods = append(queryMods, mods...)

	return TabelaNomes(queryMods...)
}

// LoadTBN allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tabelaNomesCampoL) LoadTBN(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTabelaNomesCampo interface{}, mods queries.Applicator) error {
	var slice []*TabelaNomesCampo
	var object *TabelaNomesCampo

	if singular {
		var ok bool
		object, ok = maybeTabelaNomesCampo.(*TabelaNomesCampo)
		if !ok {
			object = new(TabelaNomesCampo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTabelaNomesCampo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTabelaNomesCampo))
			}
		}
	} else {
		s, ok := maybeTabelaNomesCampo.(*[]*TabelaNomesCampo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTabelaNomesCampo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTabelaNomesCampo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tabelaNomesCampoR{}
		}
		args[object.TBNID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tabelaNomesCampoR{}
			}

			args[obj.TBNID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`tabela_nomes`),
		qm.WhereIn(`tabela_nomes.tbn_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TabelaNome")
	}

	var resultSlice []*TabelaNome
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TabelaNome")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tabela_nomes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tabela_nomes")
	}

	if len(tabelaNomeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.TBN = foreign
		if foreign.R == nil {
			foreign.R = &tabelaNomeR{}
		}
		foreign.R.TBNTabelaNomesCampos = append(foreign.R.TBNTabelaNomesCampos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TBNID == foreign.TBNID {
				local.R.TBN = foreign
				if foreign.R == nil {
					foreign.R = &tabelaNomeR{}
				}
				foreign.R.TBNTabelaNomesCampos = append(foreign.R.TBNTabelaNomesCampos, local)
				break
			}
		}
	}

	return nil
}

// SetTBN of the tabelaNomesCampo to the related item.
// Sets o.R.TBN to related.
// Adds o to related.R.TBNTabelaNomesCampos.
func (o *TabelaNomesCampo) SetTBN(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TabelaNome) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `tabela_nomes_campos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"tbn_id"}),
		strmangle.WhereClause("`", "`", 0, tabelaNomesCampoPrimaryKeyColumns),
	)
	values := []interface{}{related.TBNID, o.TBNID, o.TNCItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TBNID = related.TBNID
	if o.R == nil {
		o.R = &tabelaNomesCampoR{
			TBN: related,
		}
	} else {
		o.R.TBN = related
	}

	if related.R == nil {
		related.R = &tabelaNomeR{
			TBNTabelaNomesCampos: TabelaNomesCampoSlice{o},
		}
	} else {
		related.R.TBNTabelaNomesCampos = append(related.R.TBNTabelaNomesCampos, o)
	}

	return nil
}

// TabelaNomesCampos retrieves all the records using an executor.
func TabelaNomesCampos(mods ...qm.QueryMod) tabelaNomesCampoQuery {
	mods = append(mods, qm.From("`tabela_nomes_campos`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`tabela_nomes_campos`.*"})
	}

	return tabelaNomesCampoQuery{q}
}

// FindTabelaNomesCampo retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTabelaNomesCampo(ctx context.Context, exec boil.ContextExecutor, tBNID int, tNCItem int, selectCols ...string) (*TabelaNomesCampo, error) {
	tabelaNomesCampoObj := &TabelaNomesCampo{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `tabela_nomes_campos` where `tbn_id`=? AND `tnc_item`=?", sel,
	)

	q := queries.Raw(query, tBNID, tNCItem)

	err := q.Bind(ctx, exec, tabelaNomesCampoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from tabela_nomes_campos")
	}

	if err = tabelaNomesCampoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tabelaNomesCampoObj, err
	}

	return tabelaNomesCampoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TabelaNomesCampo) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tabela_nomes_campos provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tabelaNomesCampoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tabelaNomesCampoInsertCacheMut.RLock()
	cache, cached := tabelaNomesCampoInsertCache[key]
	tabelaNomesCampoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tabelaNomesCampoAllColumns,
			tabelaNomesCampoColumnsWithDefault,
			tabelaNomesCampoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tabelaNomesCampoType, tabelaNomesCampoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tabelaNomesCampoType, tabelaNomesCampoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `tabela_nomes_campos` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `tabela_nomes_campos` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `tabela_nomes_campos` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tabelaNomesCampoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into tabela_nomes_campos")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TBNID,
		o.TNCItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tabela_nomes_campos")
	}

CacheNoHooks:
	if !cached {
		tabelaNomesCampoInsertCacheMut.Lock()
		tabelaNomesCampoInsertCache[key] = cache
		tabelaNomesCampoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TabelaNomesCampo.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TabelaNomesCampo) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tabelaNomesCampoUpdateCacheMut.RLock()
	cache, cached := tabelaNomesCampoUpdateCache[key]
	tabelaNomesCampoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tabelaNomesCampoAllColumns,
			tabelaNomesCampoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update tabela_nomes_campos, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `tabela_nomes_campos` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tabelaNomesCampoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tabelaNomesCampoType, tabelaNomesCampoMapping, append(wl, tabelaNomesCampoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update tabela_nomes_campos row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for tabela_nomes_campos")
	}

	if !cached {
		tabelaNomesCampoUpdateCacheMut.Lock()
		tabelaNomesCampoUpdateCache[key] = cache
		tabelaNomesCampoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tabelaNomesCampoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for tabela_nomes_campos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for tabela_nomes_campos")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TabelaNomesCampoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaNomesCampoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `tabela_nomes_campos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaNomesCampoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tabelaNomesCampo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tabelaNomesCampo")
	}
	return rowsAff, nil
}

var mySQLTabelaNomesCampoUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TabelaNomesCampo) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tabela_nomes_campos provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tabelaNomesCampoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTabelaNomesCampoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tabelaNomesCampoUpsertCacheMut.RLock()
	cache, cached := tabelaNomesCampoUpsertCache[key]
	tabelaNomesCampoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tabelaNomesCampoAllColumns,
			tabelaNomesCampoColumnsWithDefault,
			tabelaNomesCampoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tabelaNomesCampoAllColumns,
			tabelaNomesCampoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert tabela_nomes_campos, could not build update column list")
		}

		ret := strmangle.SetComplement(tabelaNomesCampoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`tabela_nomes_campos`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `tabela_nomes_campos` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tabelaNomesCampoType, tabelaNomesCampoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tabelaNomesCampoType, tabelaNomesCampoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for tabela_nomes_campos")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tabelaNomesCampoType, tabelaNomesCampoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for tabela_nomes_campos")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tabela_nomes_campos")
	}

CacheNoHooks:
	if !cached {
		tabelaNomesCampoUpsertCacheMut.Lock()
		tabelaNomesCampoUpsertCache[key] = cache
		tabelaNomesCampoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TabelaNomesCampo record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TabelaNomesCampo) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TabelaNomesCampo provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tabelaNomesCampoPrimaryKeyMapping)
	sql := "DELETE FROM `tabela_nomes_campos` WHERE `tbn_id`=? AND `tnc_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from tabela_nomes_campos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for tabela_nomes_campos")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tabelaNomesCampoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tabelaNomesCampoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tabela_nomes_campos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tabela_nomes_campos")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TabelaNomesCampoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tabelaNomesCampoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaNomesCampoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `tabela_nomes_campos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaNomesCampoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tabelaNomesCampo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tabela_nomes_campos")
	}

	if len(tabelaNomesCampoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TabelaNomesCampo) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTabelaNomesCampo(ctx, exec, o.TBNID, o.TNCItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TabelaNomesCampoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TabelaNomesCampoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaNomesCampoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `tabela_nomes_campos`.* FROM `tabela_nomes_campos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaNomesCampoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TabelaNomesCampoSlice")
	}

	*o = slice

	return nil
}

// TabelaNomesCampoExists checks if the TabelaNomesCampo row exists.
func TabelaNomesCampoExists(ctx context.Context, exec boil.ContextExecutor, tBNID int, tNCItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `tabela_nomes_campos` where `tbn_id`=? AND `tnc_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, tBNID, tNCItem)
	}
	row := exec.QueryRowContext(ctx, sql, tBNID, tNCItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if tabela_nomes_campos exists")
	}

	return exists, nil
}

// Exists checks if the TabelaNomesCampo row exists.
func (o *TabelaNomesCampo) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TabelaNomesCampoExists(ctx, exec, o.TBNID, o.TNCItem)
}
