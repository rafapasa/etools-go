// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// FluxoSetor is an object representing the database table.
type FluxoSetor struct {
	FluID        int `boil:"flu_id" json:"flu_id" toml:"flu_id" yaml:"flu_id"`
	FLSSequencia int `boil:"fls_sequencia" json:"fls_sequencia" toml:"fls_sequencia" yaml:"fls_sequencia"`
	SetID        int `boil:"set_id" json:"set_id" toml:"set_id" yaml:"set_id"`

	R *fluxoSetorR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L fluxoSetorL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FluxoSetorColumns = struct {
	FluID        string
	FLSSequencia string
	SetID        string
}{
	FluID:        "flu_id",
	FLSSequencia: "fls_sequencia",
	SetID:        "set_id",
}

var FluxoSetorTableColumns = struct {
	FluID        string
	FLSSequencia string
	SetID        string
}{
	FluID:        "fluxo_setor.flu_id",
	FLSSequencia: "fluxo_setor.fls_sequencia",
	SetID:        "fluxo_setor.set_id",
}

// Generated where

var FluxoSetorWhere = struct {
	FluID        whereHelperint
	FLSSequencia whereHelperint
	SetID        whereHelperint
}{
	FluID:        whereHelperint{field: "`fluxo_setor`.`flu_id`"},
	FLSSequencia: whereHelperint{field: "`fluxo_setor`.`fls_sequencia`"},
	SetID:        whereHelperint{field: "`fluxo_setor`.`set_id`"},
}

// FluxoSetorRels is where relationship names are stored.
var FluxoSetorRels = struct {
	Flu string
	Set string
}{
	Flu: "Flu",
	Set: "Set",
}

// fluxoSetorR is where relationships are stored.
type fluxoSetorR struct {
	Flu *Fluxo `boil:"Flu" json:"Flu" toml:"Flu" yaml:"Flu"`
	Set *Setor `boil:"Set" json:"Set" toml:"Set" yaml:"Set"`
}

// NewStruct creates a new relationship struct
func (*fluxoSetorR) NewStruct() *fluxoSetorR {
	return &fluxoSetorR{}
}

func (r *fluxoSetorR) GetFlu() *Fluxo {
	if r == nil {
		return nil
	}
	return r.Flu
}

func (r *fluxoSetorR) GetSet() *Setor {
	if r == nil {
		return nil
	}
	return r.Set
}

// fluxoSetorL is where Load methods for each relationship are stored.
type fluxoSetorL struct{}

var (
	fluxoSetorAllColumns            = []string{"flu_id", "fls_sequencia", "set_id"}
	fluxoSetorColumnsWithoutDefault = []string{"flu_id", "fls_sequencia", "set_id"}
	fluxoSetorColumnsWithDefault    = []string{}
	fluxoSetorPrimaryKeyColumns     = []string{"flu_id", "fls_sequencia"}
	fluxoSetorGeneratedColumns      = []string{}
)

type (
	// FluxoSetorSlice is an alias for a slice of pointers to FluxoSetor.
	// This should almost always be used instead of []FluxoSetor.
	FluxoSetorSlice []*FluxoSetor
	// FluxoSetorHook is the signature for custom FluxoSetor hook methods
	FluxoSetorHook func(context.Context, boil.ContextExecutor, *FluxoSetor) error

	fluxoSetorQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	fluxoSetorType                 = reflect.TypeOf(&FluxoSetor{})
	fluxoSetorMapping              = queries.MakeStructMapping(fluxoSetorType)
	fluxoSetorPrimaryKeyMapping, _ = queries.BindMapping(fluxoSetorType, fluxoSetorMapping, fluxoSetorPrimaryKeyColumns)
	fluxoSetorInsertCacheMut       sync.RWMutex
	fluxoSetorInsertCache          = make(map[string]insertCache)
	fluxoSetorUpdateCacheMut       sync.RWMutex
	fluxoSetorUpdateCache          = make(map[string]updateCache)
	fluxoSetorUpsertCacheMut       sync.RWMutex
	fluxoSetorUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var fluxoSetorAfterSelectMu sync.Mutex
var fluxoSetorAfterSelectHooks []FluxoSetorHook

var fluxoSetorBeforeInsertMu sync.Mutex
var fluxoSetorBeforeInsertHooks []FluxoSetorHook
var fluxoSetorAfterInsertMu sync.Mutex
var fluxoSetorAfterInsertHooks []FluxoSetorHook

var fluxoSetorBeforeUpdateMu sync.Mutex
var fluxoSetorBeforeUpdateHooks []FluxoSetorHook
var fluxoSetorAfterUpdateMu sync.Mutex
var fluxoSetorAfterUpdateHooks []FluxoSetorHook

var fluxoSetorBeforeDeleteMu sync.Mutex
var fluxoSetorBeforeDeleteHooks []FluxoSetorHook
var fluxoSetorAfterDeleteMu sync.Mutex
var fluxoSetorAfterDeleteHooks []FluxoSetorHook

var fluxoSetorBeforeUpsertMu sync.Mutex
var fluxoSetorBeforeUpsertHooks []FluxoSetorHook
var fluxoSetorAfterUpsertMu sync.Mutex
var fluxoSetorAfterUpsertHooks []FluxoSetorHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *FluxoSetor) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *FluxoSetor) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *FluxoSetor) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *FluxoSetor) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *FluxoSetor) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *FluxoSetor) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *FluxoSetor) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *FluxoSetor) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *FluxoSetor) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range fluxoSetorAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddFluxoSetorHook registers your hook function for all future operations.
func AddFluxoSetorHook(hookPoint boil.HookPoint, fluxoSetorHook FluxoSetorHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		fluxoSetorAfterSelectMu.Lock()
		fluxoSetorAfterSelectHooks = append(fluxoSetorAfterSelectHooks, fluxoSetorHook)
		fluxoSetorAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		fluxoSetorBeforeInsertMu.Lock()
		fluxoSetorBeforeInsertHooks = append(fluxoSetorBeforeInsertHooks, fluxoSetorHook)
		fluxoSetorBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		fluxoSetorAfterInsertMu.Lock()
		fluxoSetorAfterInsertHooks = append(fluxoSetorAfterInsertHooks, fluxoSetorHook)
		fluxoSetorAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		fluxoSetorBeforeUpdateMu.Lock()
		fluxoSetorBeforeUpdateHooks = append(fluxoSetorBeforeUpdateHooks, fluxoSetorHook)
		fluxoSetorBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		fluxoSetorAfterUpdateMu.Lock()
		fluxoSetorAfterUpdateHooks = append(fluxoSetorAfterUpdateHooks, fluxoSetorHook)
		fluxoSetorAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		fluxoSetorBeforeDeleteMu.Lock()
		fluxoSetorBeforeDeleteHooks = append(fluxoSetorBeforeDeleteHooks, fluxoSetorHook)
		fluxoSetorBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		fluxoSetorAfterDeleteMu.Lock()
		fluxoSetorAfterDeleteHooks = append(fluxoSetorAfterDeleteHooks, fluxoSetorHook)
		fluxoSetorAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		fluxoSetorBeforeUpsertMu.Lock()
		fluxoSetorBeforeUpsertHooks = append(fluxoSetorBeforeUpsertHooks, fluxoSetorHook)
		fluxoSetorBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		fluxoSetorAfterUpsertMu.Lock()
		fluxoSetorAfterUpsertHooks = append(fluxoSetorAfterUpsertHooks, fluxoSetorHook)
		fluxoSetorAfterUpsertMu.Unlock()
	}
}

// One returns a single fluxoSetor record from the query.
func (q fluxoSetorQuery) One(ctx context.Context, exec boil.ContextExecutor) (*FluxoSetor, error) {
	o := &FluxoSetor{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for fluxo_setor")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all FluxoSetor records from the query.
func (q fluxoSetorQuery) All(ctx context.Context, exec boil.ContextExecutor) (FluxoSetorSlice, error) {
	var o []*FluxoSetor

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to FluxoSetor slice")
	}

	if len(fluxoSetorAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all FluxoSetor records in the query.
func (q fluxoSetorQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count fluxo_setor rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q fluxoSetorQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if fluxo_setor exists")
	}

	return count > 0, nil
}

// Flu pointed to by the foreign key.
func (o *FluxoSetor) Flu(mods ...qm.QueryMod) fluxoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`flu_id` = ?", o.FluID),
	}

	queryMods = append(queryMods, mods...)

	return Fluxos(queryMods...)
}

// Set pointed to by the foreign key.
func (o *FluxoSetor) Set(mods ...qm.QueryMod) setorQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`set_id` = ?", o.SetID),
	}

	queryMods = append(queryMods, mods...)

	return Setors(queryMods...)
}

// LoadFlu allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (fluxoSetorL) LoadFlu(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFluxoSetor interface{}, mods queries.Applicator) error {
	var slice []*FluxoSetor
	var object *FluxoSetor

	if singular {
		var ok bool
		object, ok = maybeFluxoSetor.(*FluxoSetor)
		if !ok {
			object = new(FluxoSetor)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFluxoSetor)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFluxoSetor))
			}
		}
	} else {
		s, ok := maybeFluxoSetor.(*[]*FluxoSetor)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFluxoSetor)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFluxoSetor))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &fluxoSetorR{}
		}
		args[object.FluID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &fluxoSetorR{}
			}

			args[obj.FluID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`fluxo`),
		qm.WhereIn(`fluxo.flu_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Fluxo")
	}

	var resultSlice []*Fluxo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Fluxo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for fluxo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for fluxo")
	}

	if len(fluxoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Flu = foreign
		if foreign.R == nil {
			foreign.R = &fluxoR{}
		}
		foreign.R.FluFluxoSetors = append(foreign.R.FluFluxoSetors, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.FluID == foreign.FluID {
				local.R.Flu = foreign
				if foreign.R == nil {
					foreign.R = &fluxoR{}
				}
				foreign.R.FluFluxoSetors = append(foreign.R.FluFluxoSetors, local)
				break
			}
		}
	}

	return nil
}

// LoadSet allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (fluxoSetorL) LoadSet(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFluxoSetor interface{}, mods queries.Applicator) error {
	var slice []*FluxoSetor
	var object *FluxoSetor

	if singular {
		var ok bool
		object, ok = maybeFluxoSetor.(*FluxoSetor)
		if !ok {
			object = new(FluxoSetor)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFluxoSetor)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFluxoSetor))
			}
		}
	} else {
		s, ok := maybeFluxoSetor.(*[]*FluxoSetor)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFluxoSetor)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFluxoSetor))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &fluxoSetorR{}
		}
		args[object.SetID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &fluxoSetorR{}
			}

			args[obj.SetID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`setor`),
		qm.WhereIn(`setor.set_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Setor")
	}

	var resultSlice []*Setor
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Setor")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for setor")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for setor")
	}

	if len(setorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Set = foreign
		if foreign.R == nil {
			foreign.R = &setorR{}
		}
		foreign.R.SetFluxoSetors = append(foreign.R.SetFluxoSetors, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SetID == foreign.SetID {
				local.R.Set = foreign
				if foreign.R == nil {
					foreign.R = &setorR{}
				}
				foreign.R.SetFluxoSetors = append(foreign.R.SetFluxoSetors, local)
				break
			}
		}
	}

	return nil
}

// SetFlu of the fluxoSetor to the related item.
// Sets o.R.Flu to related.
// Adds o to related.R.FluFluxoSetors.
func (o *FluxoSetor) SetFlu(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Fluxo) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `fluxo_setor` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"flu_id"}),
		strmangle.WhereClause("`", "`", 0, fluxoSetorPrimaryKeyColumns),
	)
	values := []interface{}{related.FluID, o.FluID, o.FLSSequencia}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.FluID = related.FluID
	if o.R == nil {
		o.R = &fluxoSetorR{
			Flu: related,
		}
	} else {
		o.R.Flu = related
	}

	if related.R == nil {
		related.R = &fluxoR{
			FluFluxoSetors: FluxoSetorSlice{o},
		}
	} else {
		related.R.FluFluxoSetors = append(related.R.FluFluxoSetors, o)
	}

	return nil
}

// SetSet of the fluxoSetor to the related item.
// Sets o.R.Set to related.
// Adds o to related.R.SetFluxoSetors.
func (o *FluxoSetor) SetSet(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Setor) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `fluxo_setor` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"set_id"}),
		strmangle.WhereClause("`", "`", 0, fluxoSetorPrimaryKeyColumns),
	)
	values := []interface{}{related.SetID, o.FluID, o.FLSSequencia}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SetID = related.SetID
	if o.R == nil {
		o.R = &fluxoSetorR{
			Set: related,
		}
	} else {
		o.R.Set = related
	}

	if related.R == nil {
		related.R = &setorR{
			SetFluxoSetors: FluxoSetorSlice{o},
		}
	} else {
		related.R.SetFluxoSetors = append(related.R.SetFluxoSetors, o)
	}

	return nil
}

// FluxoSetors retrieves all the records using an executor.
func FluxoSetors(mods ...qm.QueryMod) fluxoSetorQuery {
	mods = append(mods, qm.From("`fluxo_setor`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`fluxo_setor`.*"})
	}

	return fluxoSetorQuery{q}
}

// FindFluxoSetor retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFluxoSetor(ctx context.Context, exec boil.ContextExecutor, fluID int, fLSSequencia int, selectCols ...string) (*FluxoSetor, error) {
	fluxoSetorObj := &FluxoSetor{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `fluxo_setor` where `flu_id`=? AND `fls_sequencia`=?", sel,
	)

	q := queries.Raw(query, fluID, fLSSequencia)

	err := q.Bind(ctx, exec, fluxoSetorObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from fluxo_setor")
	}

	if err = fluxoSetorObj.doAfterSelectHooks(ctx, exec); err != nil {
		return fluxoSetorObj, err
	}

	return fluxoSetorObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *FluxoSetor) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no fluxo_setor provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(fluxoSetorColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	fluxoSetorInsertCacheMut.RLock()
	cache, cached := fluxoSetorInsertCache[key]
	fluxoSetorInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			fluxoSetorAllColumns,
			fluxoSetorColumnsWithDefault,
			fluxoSetorColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(fluxoSetorType, fluxoSetorMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(fluxoSetorType, fluxoSetorMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `fluxo_setor` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `fluxo_setor` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `fluxo_setor` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, fluxoSetorPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into fluxo_setor")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.FluID,
		o.FLSSequencia,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for fluxo_setor")
	}

CacheNoHooks:
	if !cached {
		fluxoSetorInsertCacheMut.Lock()
		fluxoSetorInsertCache[key] = cache
		fluxoSetorInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the FluxoSetor.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *FluxoSetor) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	fluxoSetorUpdateCacheMut.RLock()
	cache, cached := fluxoSetorUpdateCache[key]
	fluxoSetorUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			fluxoSetorAllColumns,
			fluxoSetorPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update fluxo_setor, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `fluxo_setor` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, fluxoSetorPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(fluxoSetorType, fluxoSetorMapping, append(wl, fluxoSetorPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update fluxo_setor row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for fluxo_setor")
	}

	if !cached {
		fluxoSetorUpdateCacheMut.Lock()
		fluxoSetorUpdateCache[key] = cache
		fluxoSetorUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q fluxoSetorQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for fluxo_setor")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for fluxo_setor")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FluxoSetorSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), fluxoSetorPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `fluxo_setor` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, fluxoSetorPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in fluxoSetor slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all fluxoSetor")
	}
	return rowsAff, nil
}

var mySQLFluxoSetorUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *FluxoSetor) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no fluxo_setor provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(fluxoSetorColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLFluxoSetorUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	fluxoSetorUpsertCacheMut.RLock()
	cache, cached := fluxoSetorUpsertCache[key]
	fluxoSetorUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			fluxoSetorAllColumns,
			fluxoSetorColumnsWithDefault,
			fluxoSetorColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			fluxoSetorAllColumns,
			fluxoSetorPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert fluxo_setor, could not build update column list")
		}

		ret := strmangle.SetComplement(fluxoSetorAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`fluxo_setor`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `fluxo_setor` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(fluxoSetorType, fluxoSetorMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(fluxoSetorType, fluxoSetorMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for fluxo_setor")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(fluxoSetorType, fluxoSetorMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for fluxo_setor")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for fluxo_setor")
	}

CacheNoHooks:
	if !cached {
		fluxoSetorUpsertCacheMut.Lock()
		fluxoSetorUpsertCache[key] = cache
		fluxoSetorUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single FluxoSetor record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *FluxoSetor) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no FluxoSetor provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), fluxoSetorPrimaryKeyMapping)
	sql := "DELETE FROM `fluxo_setor` WHERE `flu_id`=? AND `fls_sequencia`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from fluxo_setor")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for fluxo_setor")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q fluxoSetorQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no fluxoSetorQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from fluxo_setor")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for fluxo_setor")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FluxoSetorSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(fluxoSetorBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), fluxoSetorPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `fluxo_setor` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, fluxoSetorPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from fluxoSetor slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for fluxo_setor")
	}

	if len(fluxoSetorAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *FluxoSetor) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindFluxoSetor(ctx, exec, o.FluID, o.FLSSequencia)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FluxoSetorSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FluxoSetorSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), fluxoSetorPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `fluxo_setor`.* FROM `fluxo_setor` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, fluxoSetorPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in FluxoSetorSlice")
	}

	*o = slice

	return nil
}

// FluxoSetorExists checks if the FluxoSetor row exists.
func FluxoSetorExists(ctx context.Context, exec boil.ContextExecutor, fluID int, fLSSequencia int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `fluxo_setor` where `flu_id`=? AND `fls_sequencia`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, fluID, fLSSequencia)
	}
	row := exec.QueryRowContext(ctx, sql, fluID, fLSSequencia)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if fluxo_setor exists")
	}

	return exists, nil
}

// Exists checks if the FluxoSetor row exists.
func (o *FluxoSetor) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return FluxoSetorExists(ctx, exec, o.FluID, o.FLSSequencia)
}
