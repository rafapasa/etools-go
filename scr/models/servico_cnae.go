// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ServicoCnae is an object representing the database table.
type ServicoCnae struct {
	ScnaeID          int    `boil:"scnae_id" json:"scnae_id" toml:"scnae_id" yaml:"scnae_id"`
	ScnaeCnae        string `boil:"scnae_cnae" json:"scnae_cnae" toml:"scnae_cnae" yaml:"scnae_cnae"`
	ScnaeItemservico string `boil:"scnae_itemservico" json:"scnae_itemservico" toml:"scnae_itemservico" yaml:"scnae_itemservico"`
	ScnaeCodigomun   string `boil:"scnae_codigomun" json:"scnae_codigomun" toml:"scnae_codigomun" yaml:"scnae_codigomun"`

	R *servicoCnaeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L servicoCnaeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ServicoCnaeColumns = struct {
	ScnaeID          string
	ScnaeCnae        string
	ScnaeItemservico string
	ScnaeCodigomun   string
}{
	ScnaeID:          "scnae_id",
	ScnaeCnae:        "scnae_cnae",
	ScnaeItemservico: "scnae_itemservico",
	ScnaeCodigomun:   "scnae_codigomun",
}

var ServicoCnaeTableColumns = struct {
	ScnaeID          string
	ScnaeCnae        string
	ScnaeItemservico string
	ScnaeCodigomun   string
}{
	ScnaeID:          "servico_cnae.scnae_id",
	ScnaeCnae:        "servico_cnae.scnae_cnae",
	ScnaeItemservico: "servico_cnae.scnae_itemservico",
	ScnaeCodigomun:   "servico_cnae.scnae_codigomun",
}

// Generated where

var ServicoCnaeWhere = struct {
	ScnaeID          whereHelperint
	ScnaeCnae        whereHelperstring
	ScnaeItemservico whereHelperstring
	ScnaeCodigomun   whereHelperstring
}{
	ScnaeID:          whereHelperint{field: "`servico_cnae`.`scnae_id`"},
	ScnaeCnae:        whereHelperstring{field: "`servico_cnae`.`scnae_cnae`"},
	ScnaeItemservico: whereHelperstring{field: "`servico_cnae`.`scnae_itemservico`"},
	ScnaeCodigomun:   whereHelperstring{field: "`servico_cnae`.`scnae_codigomun`"},
}

// ServicoCnaeRels is where relationship names are stored.
var ServicoCnaeRels = struct {
}{}

// servicoCnaeR is where relationships are stored.
type servicoCnaeR struct {
}

// NewStruct creates a new relationship struct
func (*servicoCnaeR) NewStruct() *servicoCnaeR {
	return &servicoCnaeR{}
}

// servicoCnaeL is where Load methods for each relationship are stored.
type servicoCnaeL struct{}

var (
	servicoCnaeAllColumns            = []string{"scnae_id", "scnae_cnae", "scnae_itemservico", "scnae_codigomun"}
	servicoCnaeColumnsWithoutDefault = []string{"scnae_id", "scnae_cnae", "scnae_itemservico", "scnae_codigomun"}
	servicoCnaeColumnsWithDefault    = []string{}
	servicoCnaePrimaryKeyColumns     = []string{"scnae_id"}
	servicoCnaeGeneratedColumns      = []string{}
)

type (
	// ServicoCnaeSlice is an alias for a slice of pointers to ServicoCnae.
	// This should almost always be used instead of []ServicoCnae.
	ServicoCnaeSlice []*ServicoCnae
	// ServicoCnaeHook is the signature for custom ServicoCnae hook methods
	ServicoCnaeHook func(context.Context, boil.ContextExecutor, *ServicoCnae) error

	servicoCnaeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	servicoCnaeType                 = reflect.TypeOf(&ServicoCnae{})
	servicoCnaeMapping              = queries.MakeStructMapping(servicoCnaeType)
	servicoCnaePrimaryKeyMapping, _ = queries.BindMapping(servicoCnaeType, servicoCnaeMapping, servicoCnaePrimaryKeyColumns)
	servicoCnaeInsertCacheMut       sync.RWMutex
	servicoCnaeInsertCache          = make(map[string]insertCache)
	servicoCnaeUpdateCacheMut       sync.RWMutex
	servicoCnaeUpdateCache          = make(map[string]updateCache)
	servicoCnaeUpsertCacheMut       sync.RWMutex
	servicoCnaeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var servicoCnaeAfterSelectMu sync.Mutex
var servicoCnaeAfterSelectHooks []ServicoCnaeHook

var servicoCnaeBeforeInsertMu sync.Mutex
var servicoCnaeBeforeInsertHooks []ServicoCnaeHook
var servicoCnaeAfterInsertMu sync.Mutex
var servicoCnaeAfterInsertHooks []ServicoCnaeHook

var servicoCnaeBeforeUpdateMu sync.Mutex
var servicoCnaeBeforeUpdateHooks []ServicoCnaeHook
var servicoCnaeAfterUpdateMu sync.Mutex
var servicoCnaeAfterUpdateHooks []ServicoCnaeHook

var servicoCnaeBeforeDeleteMu sync.Mutex
var servicoCnaeBeforeDeleteHooks []ServicoCnaeHook
var servicoCnaeAfterDeleteMu sync.Mutex
var servicoCnaeAfterDeleteHooks []ServicoCnaeHook

var servicoCnaeBeforeUpsertMu sync.Mutex
var servicoCnaeBeforeUpsertHooks []ServicoCnaeHook
var servicoCnaeAfterUpsertMu sync.Mutex
var servicoCnaeAfterUpsertHooks []ServicoCnaeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ServicoCnae) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ServicoCnae) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ServicoCnae) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ServicoCnae) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ServicoCnae) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ServicoCnae) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ServicoCnae) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ServicoCnae) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ServicoCnae) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range servicoCnaeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddServicoCnaeHook registers your hook function for all future operations.
func AddServicoCnaeHook(hookPoint boil.HookPoint, servicoCnaeHook ServicoCnaeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		servicoCnaeAfterSelectMu.Lock()
		servicoCnaeAfterSelectHooks = append(servicoCnaeAfterSelectHooks, servicoCnaeHook)
		servicoCnaeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		servicoCnaeBeforeInsertMu.Lock()
		servicoCnaeBeforeInsertHooks = append(servicoCnaeBeforeInsertHooks, servicoCnaeHook)
		servicoCnaeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		servicoCnaeAfterInsertMu.Lock()
		servicoCnaeAfterInsertHooks = append(servicoCnaeAfterInsertHooks, servicoCnaeHook)
		servicoCnaeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		servicoCnaeBeforeUpdateMu.Lock()
		servicoCnaeBeforeUpdateHooks = append(servicoCnaeBeforeUpdateHooks, servicoCnaeHook)
		servicoCnaeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		servicoCnaeAfterUpdateMu.Lock()
		servicoCnaeAfterUpdateHooks = append(servicoCnaeAfterUpdateHooks, servicoCnaeHook)
		servicoCnaeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		servicoCnaeBeforeDeleteMu.Lock()
		servicoCnaeBeforeDeleteHooks = append(servicoCnaeBeforeDeleteHooks, servicoCnaeHook)
		servicoCnaeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		servicoCnaeAfterDeleteMu.Lock()
		servicoCnaeAfterDeleteHooks = append(servicoCnaeAfterDeleteHooks, servicoCnaeHook)
		servicoCnaeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		servicoCnaeBeforeUpsertMu.Lock()
		servicoCnaeBeforeUpsertHooks = append(servicoCnaeBeforeUpsertHooks, servicoCnaeHook)
		servicoCnaeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		servicoCnaeAfterUpsertMu.Lock()
		servicoCnaeAfterUpsertHooks = append(servicoCnaeAfterUpsertHooks, servicoCnaeHook)
		servicoCnaeAfterUpsertMu.Unlock()
	}
}

// One returns a single servicoCnae record from the query.
func (q servicoCnaeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ServicoCnae, error) {
	o := &ServicoCnae{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for servico_cnae")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ServicoCnae records from the query.
func (q servicoCnaeQuery) All(ctx context.Context, exec boil.ContextExecutor) (ServicoCnaeSlice, error) {
	var o []*ServicoCnae

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ServicoCnae slice")
	}

	if len(servicoCnaeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ServicoCnae records in the query.
func (q servicoCnaeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count servico_cnae rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q servicoCnaeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if servico_cnae exists")
	}

	return count > 0, nil
}

// ServicoCnaes retrieves all the records using an executor.
func ServicoCnaes(mods ...qm.QueryMod) servicoCnaeQuery {
	mods = append(mods, qm.From("`servico_cnae`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`servico_cnae`.*"})
	}

	return servicoCnaeQuery{q}
}

// FindServicoCnae retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindServicoCnae(ctx context.Context, exec boil.ContextExecutor, scnaeID int, selectCols ...string) (*ServicoCnae, error) {
	servicoCnaeObj := &ServicoCnae{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `servico_cnae` where `scnae_id`=?", sel,
	)

	q := queries.Raw(query, scnaeID)

	err := q.Bind(ctx, exec, servicoCnaeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from servico_cnae")
	}

	if err = servicoCnaeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return servicoCnaeObj, err
	}

	return servicoCnaeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ServicoCnae) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no servico_cnae provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(servicoCnaeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	servicoCnaeInsertCacheMut.RLock()
	cache, cached := servicoCnaeInsertCache[key]
	servicoCnaeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			servicoCnaeAllColumns,
			servicoCnaeColumnsWithDefault,
			servicoCnaeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(servicoCnaeType, servicoCnaeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(servicoCnaeType, servicoCnaeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `servico_cnae` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `servico_cnae` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `servico_cnae` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, servicoCnaePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into servico_cnae")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ScnaeID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for servico_cnae")
	}

CacheNoHooks:
	if !cached {
		servicoCnaeInsertCacheMut.Lock()
		servicoCnaeInsertCache[key] = cache
		servicoCnaeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ServicoCnae.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ServicoCnae) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	servicoCnaeUpdateCacheMut.RLock()
	cache, cached := servicoCnaeUpdateCache[key]
	servicoCnaeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			servicoCnaeAllColumns,
			servicoCnaePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update servico_cnae, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `servico_cnae` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, servicoCnaePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(servicoCnaeType, servicoCnaeMapping, append(wl, servicoCnaePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update servico_cnae row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for servico_cnae")
	}

	if !cached {
		servicoCnaeUpdateCacheMut.Lock()
		servicoCnaeUpdateCache[key] = cache
		servicoCnaeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q servicoCnaeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for servico_cnae")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for servico_cnae")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ServicoCnaeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), servicoCnaePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `servico_cnae` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, servicoCnaePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in servicoCnae slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all servicoCnae")
	}
	return rowsAff, nil
}

var mySQLServicoCnaeUniqueColumns = []string{
	"scnae_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ServicoCnae) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no servico_cnae provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(servicoCnaeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLServicoCnaeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	servicoCnaeUpsertCacheMut.RLock()
	cache, cached := servicoCnaeUpsertCache[key]
	servicoCnaeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			servicoCnaeAllColumns,
			servicoCnaeColumnsWithDefault,
			servicoCnaeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			servicoCnaeAllColumns,
			servicoCnaePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert servico_cnae, could not build update column list")
		}

		ret := strmangle.SetComplement(servicoCnaeAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`servico_cnae`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `servico_cnae` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(servicoCnaeType, servicoCnaeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(servicoCnaeType, servicoCnaeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for servico_cnae")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(servicoCnaeType, servicoCnaeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for servico_cnae")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for servico_cnae")
	}

CacheNoHooks:
	if !cached {
		servicoCnaeUpsertCacheMut.Lock()
		servicoCnaeUpsertCache[key] = cache
		servicoCnaeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ServicoCnae record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ServicoCnae) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ServicoCnae provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), servicoCnaePrimaryKeyMapping)
	sql := "DELETE FROM `servico_cnae` WHERE `scnae_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from servico_cnae")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for servico_cnae")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q servicoCnaeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no servicoCnaeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from servico_cnae")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for servico_cnae")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ServicoCnaeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(servicoCnaeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), servicoCnaePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `servico_cnae` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, servicoCnaePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from servicoCnae slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for servico_cnae")
	}

	if len(servicoCnaeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ServicoCnae) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindServicoCnae(ctx, exec, o.ScnaeID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ServicoCnaeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ServicoCnaeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), servicoCnaePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `servico_cnae`.* FROM `servico_cnae` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, servicoCnaePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ServicoCnaeSlice")
	}

	*o = slice

	return nil
}

// ServicoCnaeExists checks if the ServicoCnae row exists.
func ServicoCnaeExists(ctx context.Context, exec boil.ContextExecutor, scnaeID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `servico_cnae` where `scnae_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, scnaeID)
	}
	row := exec.QueryRowContext(ctx, sql, scnaeID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if servico_cnae exists")
	}

	return exists, nil
}

// Exists checks if the ServicoCnae row exists.
func (o *ServicoCnae) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ServicoCnaeExists(ctx, exec, o.ScnaeID)
}
