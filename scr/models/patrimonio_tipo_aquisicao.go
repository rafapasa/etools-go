// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PatrimonioTipoAquisicao is an object representing the database table.
type PatrimonioTipoAquisicao struct {
	PtaID        int    `boil:"pta_id" json:"pta_id" toml:"pta_id" yaml:"pta_id"`
	PtaDescricao string `boil:"pta_descricao" json:"pta_descricao" toml:"pta_descricao" yaml:"pta_descricao"`

	R *patrimonioTipoAquisicaoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L patrimonioTipoAquisicaoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PatrimonioTipoAquisicaoColumns = struct {
	PtaID        string
	PtaDescricao string
}{
	PtaID:        "pta_id",
	PtaDescricao: "pta_descricao",
}

var PatrimonioTipoAquisicaoTableColumns = struct {
	PtaID        string
	PtaDescricao string
}{
	PtaID:        "patrimonio_tipo_aquisicao.pta_id",
	PtaDescricao: "patrimonio_tipo_aquisicao.pta_descricao",
}

// Generated where

var PatrimonioTipoAquisicaoWhere = struct {
	PtaID        whereHelperint
	PtaDescricao whereHelperstring
}{
	PtaID:        whereHelperint{field: "`patrimonio_tipo_aquisicao`.`pta_id`"},
	PtaDescricao: whereHelperstring{field: "`patrimonio_tipo_aquisicao`.`pta_descricao`"},
}

// PatrimonioTipoAquisicaoRels is where relationship names are stored.
var PatrimonioTipoAquisicaoRels = struct {
	PtumPatrimonioBens string
}{
	PtumPatrimonioBens: "PtumPatrimonioBens",
}

// patrimonioTipoAquisicaoR is where relationships are stored.
type patrimonioTipoAquisicaoR struct {
	PtumPatrimonioBens PatrimonioBenSlice `boil:"PtumPatrimonioBens" json:"PtumPatrimonioBens" toml:"PtumPatrimonioBens" yaml:"PtumPatrimonioBens"`
}

// NewStruct creates a new relationship struct
func (*patrimonioTipoAquisicaoR) NewStruct() *patrimonioTipoAquisicaoR {
	return &patrimonioTipoAquisicaoR{}
}

func (r *patrimonioTipoAquisicaoR) GetPtumPatrimonioBens() PatrimonioBenSlice {
	if r == nil {
		return nil
	}
	return r.PtumPatrimonioBens
}

// patrimonioTipoAquisicaoL is where Load methods for each relationship are stored.
type patrimonioTipoAquisicaoL struct{}

var (
	patrimonioTipoAquisicaoAllColumns            = []string{"pta_id", "pta_descricao"}
	patrimonioTipoAquisicaoColumnsWithoutDefault = []string{"pta_id", "pta_descricao"}
	patrimonioTipoAquisicaoColumnsWithDefault    = []string{}
	patrimonioTipoAquisicaoPrimaryKeyColumns     = []string{"pta_id"}
	patrimonioTipoAquisicaoGeneratedColumns      = []string{}
)

type (
	// PatrimonioTipoAquisicaoSlice is an alias for a slice of pointers to PatrimonioTipoAquisicao.
	// This should almost always be used instead of []PatrimonioTipoAquisicao.
	PatrimonioTipoAquisicaoSlice []*PatrimonioTipoAquisicao
	// PatrimonioTipoAquisicaoHook is the signature for custom PatrimonioTipoAquisicao hook methods
	PatrimonioTipoAquisicaoHook func(context.Context, boil.ContextExecutor, *PatrimonioTipoAquisicao) error

	patrimonioTipoAquisicaoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	patrimonioTipoAquisicaoType                 = reflect.TypeOf(&PatrimonioTipoAquisicao{})
	patrimonioTipoAquisicaoMapping              = queries.MakeStructMapping(patrimonioTipoAquisicaoType)
	patrimonioTipoAquisicaoPrimaryKeyMapping, _ = queries.BindMapping(patrimonioTipoAquisicaoType, patrimonioTipoAquisicaoMapping, patrimonioTipoAquisicaoPrimaryKeyColumns)
	patrimonioTipoAquisicaoInsertCacheMut       sync.RWMutex
	patrimonioTipoAquisicaoInsertCache          = make(map[string]insertCache)
	patrimonioTipoAquisicaoUpdateCacheMut       sync.RWMutex
	patrimonioTipoAquisicaoUpdateCache          = make(map[string]updateCache)
	patrimonioTipoAquisicaoUpsertCacheMut       sync.RWMutex
	patrimonioTipoAquisicaoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var patrimonioTipoAquisicaoAfterSelectMu sync.Mutex
var patrimonioTipoAquisicaoAfterSelectHooks []PatrimonioTipoAquisicaoHook

var patrimonioTipoAquisicaoBeforeInsertMu sync.Mutex
var patrimonioTipoAquisicaoBeforeInsertHooks []PatrimonioTipoAquisicaoHook
var patrimonioTipoAquisicaoAfterInsertMu sync.Mutex
var patrimonioTipoAquisicaoAfterInsertHooks []PatrimonioTipoAquisicaoHook

var patrimonioTipoAquisicaoBeforeUpdateMu sync.Mutex
var patrimonioTipoAquisicaoBeforeUpdateHooks []PatrimonioTipoAquisicaoHook
var patrimonioTipoAquisicaoAfterUpdateMu sync.Mutex
var patrimonioTipoAquisicaoAfterUpdateHooks []PatrimonioTipoAquisicaoHook

var patrimonioTipoAquisicaoBeforeDeleteMu sync.Mutex
var patrimonioTipoAquisicaoBeforeDeleteHooks []PatrimonioTipoAquisicaoHook
var patrimonioTipoAquisicaoAfterDeleteMu sync.Mutex
var patrimonioTipoAquisicaoAfterDeleteHooks []PatrimonioTipoAquisicaoHook

var patrimonioTipoAquisicaoBeforeUpsertMu sync.Mutex
var patrimonioTipoAquisicaoBeforeUpsertHooks []PatrimonioTipoAquisicaoHook
var patrimonioTipoAquisicaoAfterUpsertMu sync.Mutex
var patrimonioTipoAquisicaoAfterUpsertHooks []PatrimonioTipoAquisicaoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PatrimonioTipoAquisicao) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PatrimonioTipoAquisicao) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PatrimonioTipoAquisicao) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PatrimonioTipoAquisicao) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PatrimonioTipoAquisicao) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PatrimonioTipoAquisicao) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PatrimonioTipoAquisicao) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PatrimonioTipoAquisicao) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PatrimonioTipoAquisicao) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range patrimonioTipoAquisicaoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPatrimonioTipoAquisicaoHook registers your hook function for all future operations.
func AddPatrimonioTipoAquisicaoHook(hookPoint boil.HookPoint, patrimonioTipoAquisicaoHook PatrimonioTipoAquisicaoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		patrimonioTipoAquisicaoAfterSelectMu.Lock()
		patrimonioTipoAquisicaoAfterSelectHooks = append(patrimonioTipoAquisicaoAfterSelectHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		patrimonioTipoAquisicaoBeforeInsertMu.Lock()
		patrimonioTipoAquisicaoBeforeInsertHooks = append(patrimonioTipoAquisicaoBeforeInsertHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		patrimonioTipoAquisicaoAfterInsertMu.Lock()
		patrimonioTipoAquisicaoAfterInsertHooks = append(patrimonioTipoAquisicaoAfterInsertHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		patrimonioTipoAquisicaoBeforeUpdateMu.Lock()
		patrimonioTipoAquisicaoBeforeUpdateHooks = append(patrimonioTipoAquisicaoBeforeUpdateHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		patrimonioTipoAquisicaoAfterUpdateMu.Lock()
		patrimonioTipoAquisicaoAfterUpdateHooks = append(patrimonioTipoAquisicaoAfterUpdateHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		patrimonioTipoAquisicaoBeforeDeleteMu.Lock()
		patrimonioTipoAquisicaoBeforeDeleteHooks = append(patrimonioTipoAquisicaoBeforeDeleteHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		patrimonioTipoAquisicaoAfterDeleteMu.Lock()
		patrimonioTipoAquisicaoAfterDeleteHooks = append(patrimonioTipoAquisicaoAfterDeleteHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		patrimonioTipoAquisicaoBeforeUpsertMu.Lock()
		patrimonioTipoAquisicaoBeforeUpsertHooks = append(patrimonioTipoAquisicaoBeforeUpsertHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		patrimonioTipoAquisicaoAfterUpsertMu.Lock()
		patrimonioTipoAquisicaoAfterUpsertHooks = append(patrimonioTipoAquisicaoAfterUpsertHooks, patrimonioTipoAquisicaoHook)
		patrimonioTipoAquisicaoAfterUpsertMu.Unlock()
	}
}

// One returns a single patrimonioTipoAquisicao record from the query.
func (q patrimonioTipoAquisicaoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PatrimonioTipoAquisicao, error) {
	o := &PatrimonioTipoAquisicao{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for patrimonio_tipo_aquisicao")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PatrimonioTipoAquisicao records from the query.
func (q patrimonioTipoAquisicaoQuery) All(ctx context.Context, exec boil.ContextExecutor) (PatrimonioTipoAquisicaoSlice, error) {
	var o []*PatrimonioTipoAquisicao

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PatrimonioTipoAquisicao slice")
	}

	if len(patrimonioTipoAquisicaoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PatrimonioTipoAquisicao records in the query.
func (q patrimonioTipoAquisicaoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count patrimonio_tipo_aquisicao rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q patrimonioTipoAquisicaoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if patrimonio_tipo_aquisicao exists")
	}

	return count > 0, nil
}

// PtumPatrimonioBens retrieves all the patrimonio_ben's PatrimonioBens with an executor via pta_id column.
func (o *PatrimonioTipoAquisicao) PtumPatrimonioBens(mods ...qm.QueryMod) patrimonioBenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`patrimonio_bens`.`pta_id`=?", o.PtaID),
	)

	return PatrimonioBens(queryMods...)
}

// LoadPtumPatrimonioBens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (patrimonioTipoAquisicaoL) LoadPtumPatrimonioBens(ctx context.Context, e boil.ContextExecutor, singular bool, maybePatrimonioTipoAquisicao interface{}, mods queries.Applicator) error {
	var slice []*PatrimonioTipoAquisicao
	var object *PatrimonioTipoAquisicao

	if singular {
		var ok bool
		object, ok = maybePatrimonioTipoAquisicao.(*PatrimonioTipoAquisicao)
		if !ok {
			object = new(PatrimonioTipoAquisicao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePatrimonioTipoAquisicao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePatrimonioTipoAquisicao))
			}
		}
	} else {
		s, ok := maybePatrimonioTipoAquisicao.(*[]*PatrimonioTipoAquisicao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePatrimonioTipoAquisicao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePatrimonioTipoAquisicao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &patrimonioTipoAquisicaoR{}
		}
		args[object.PtaID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &patrimonioTipoAquisicaoR{}
			}
			args[obj.PtaID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`patrimonio_bens`),
		qm.WhereIn(`patrimonio_bens.pta_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load patrimonio_bens")
	}

	var resultSlice []*PatrimonioBen
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice patrimonio_bens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on patrimonio_bens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for patrimonio_bens")
	}

	if len(patrimonioBenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PtumPatrimonioBens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &patrimonioBenR{}
			}
			foreign.R.Ptum = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PtaID == foreign.PtaID {
				local.R.PtumPatrimonioBens = append(local.R.PtumPatrimonioBens, foreign)
				if foreign.R == nil {
					foreign.R = &patrimonioBenR{}
				}
				foreign.R.Ptum = local
				break
			}
		}
	}

	return nil
}

// AddPtumPatrimonioBens adds the given related objects to the existing relationships
// of the patrimonio_tipo_aquisicao, optionally inserting them as new records.
// Appends related to o.R.PtumPatrimonioBens.
// Sets related.R.Ptum appropriately.
func (o *PatrimonioTipoAquisicao) AddPtumPatrimonioBens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PatrimonioBen) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PtaID = o.PtaID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `patrimonio_bens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"pta_id"}),
				strmangle.WhereClause("`", "`", 0, patrimonioBenPrimaryKeyColumns),
			)
			values := []interface{}{o.PtaID, rel.PbenID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PtaID = o.PtaID
		}
	}

	if o.R == nil {
		o.R = &patrimonioTipoAquisicaoR{
			PtumPatrimonioBens: related,
		}
	} else {
		o.R.PtumPatrimonioBens = append(o.R.PtumPatrimonioBens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &patrimonioBenR{
				Ptum: o,
			}
		} else {
			rel.R.Ptum = o
		}
	}
	return nil
}

// PatrimonioTipoAquisicaos retrieves all the records using an executor.
func PatrimonioTipoAquisicaos(mods ...qm.QueryMod) patrimonioTipoAquisicaoQuery {
	mods = append(mods, qm.From("`patrimonio_tipo_aquisicao`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`patrimonio_tipo_aquisicao`.*"})
	}

	return patrimonioTipoAquisicaoQuery{q}
}

// FindPatrimonioTipoAquisicao retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPatrimonioTipoAquisicao(ctx context.Context, exec boil.ContextExecutor, ptaID int, selectCols ...string) (*PatrimonioTipoAquisicao, error) {
	patrimonioTipoAquisicaoObj := &PatrimonioTipoAquisicao{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `patrimonio_tipo_aquisicao` where `pta_id`=?", sel,
	)

	q := queries.Raw(query, ptaID)

	err := q.Bind(ctx, exec, patrimonioTipoAquisicaoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from patrimonio_tipo_aquisicao")
	}

	if err = patrimonioTipoAquisicaoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return patrimonioTipoAquisicaoObj, err
	}

	return patrimonioTipoAquisicaoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PatrimonioTipoAquisicao) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no patrimonio_tipo_aquisicao provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(patrimonioTipoAquisicaoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	patrimonioTipoAquisicaoInsertCacheMut.RLock()
	cache, cached := patrimonioTipoAquisicaoInsertCache[key]
	patrimonioTipoAquisicaoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			patrimonioTipoAquisicaoAllColumns,
			patrimonioTipoAquisicaoColumnsWithDefault,
			patrimonioTipoAquisicaoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(patrimonioTipoAquisicaoType, patrimonioTipoAquisicaoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(patrimonioTipoAquisicaoType, patrimonioTipoAquisicaoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `patrimonio_tipo_aquisicao` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `patrimonio_tipo_aquisicao` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `patrimonio_tipo_aquisicao` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, patrimonioTipoAquisicaoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into patrimonio_tipo_aquisicao")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.PtaID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for patrimonio_tipo_aquisicao")
	}

CacheNoHooks:
	if !cached {
		patrimonioTipoAquisicaoInsertCacheMut.Lock()
		patrimonioTipoAquisicaoInsertCache[key] = cache
		patrimonioTipoAquisicaoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PatrimonioTipoAquisicao.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PatrimonioTipoAquisicao) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	patrimonioTipoAquisicaoUpdateCacheMut.RLock()
	cache, cached := patrimonioTipoAquisicaoUpdateCache[key]
	patrimonioTipoAquisicaoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			patrimonioTipoAquisicaoAllColumns,
			patrimonioTipoAquisicaoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update patrimonio_tipo_aquisicao, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `patrimonio_tipo_aquisicao` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, patrimonioTipoAquisicaoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(patrimonioTipoAquisicaoType, patrimonioTipoAquisicaoMapping, append(wl, patrimonioTipoAquisicaoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update patrimonio_tipo_aquisicao row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for patrimonio_tipo_aquisicao")
	}

	if !cached {
		patrimonioTipoAquisicaoUpdateCacheMut.Lock()
		patrimonioTipoAquisicaoUpdateCache[key] = cache
		patrimonioTipoAquisicaoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q patrimonioTipoAquisicaoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for patrimonio_tipo_aquisicao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for patrimonio_tipo_aquisicao")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PatrimonioTipoAquisicaoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), patrimonioTipoAquisicaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `patrimonio_tipo_aquisicao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, patrimonioTipoAquisicaoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in patrimonioTipoAquisicao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all patrimonioTipoAquisicao")
	}
	return rowsAff, nil
}

var mySQLPatrimonioTipoAquisicaoUniqueColumns = []string{
	"pta_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PatrimonioTipoAquisicao) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no patrimonio_tipo_aquisicao provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(patrimonioTipoAquisicaoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLPatrimonioTipoAquisicaoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	patrimonioTipoAquisicaoUpsertCacheMut.RLock()
	cache, cached := patrimonioTipoAquisicaoUpsertCache[key]
	patrimonioTipoAquisicaoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			patrimonioTipoAquisicaoAllColumns,
			patrimonioTipoAquisicaoColumnsWithDefault,
			patrimonioTipoAquisicaoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			patrimonioTipoAquisicaoAllColumns,
			patrimonioTipoAquisicaoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert patrimonio_tipo_aquisicao, could not build update column list")
		}

		ret := strmangle.SetComplement(patrimonioTipoAquisicaoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`patrimonio_tipo_aquisicao`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `patrimonio_tipo_aquisicao` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(patrimonioTipoAquisicaoType, patrimonioTipoAquisicaoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(patrimonioTipoAquisicaoType, patrimonioTipoAquisicaoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for patrimonio_tipo_aquisicao")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(patrimonioTipoAquisicaoType, patrimonioTipoAquisicaoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for patrimonio_tipo_aquisicao")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for patrimonio_tipo_aquisicao")
	}

CacheNoHooks:
	if !cached {
		patrimonioTipoAquisicaoUpsertCacheMut.Lock()
		patrimonioTipoAquisicaoUpsertCache[key] = cache
		patrimonioTipoAquisicaoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PatrimonioTipoAquisicao record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PatrimonioTipoAquisicao) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PatrimonioTipoAquisicao provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), patrimonioTipoAquisicaoPrimaryKeyMapping)
	sql := "DELETE FROM `patrimonio_tipo_aquisicao` WHERE `pta_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from patrimonio_tipo_aquisicao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for patrimonio_tipo_aquisicao")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q patrimonioTipoAquisicaoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no patrimonioTipoAquisicaoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from patrimonio_tipo_aquisicao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for patrimonio_tipo_aquisicao")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PatrimonioTipoAquisicaoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(patrimonioTipoAquisicaoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), patrimonioTipoAquisicaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `patrimonio_tipo_aquisicao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, patrimonioTipoAquisicaoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from patrimonioTipoAquisicao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for patrimonio_tipo_aquisicao")
	}

	if len(patrimonioTipoAquisicaoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PatrimonioTipoAquisicao) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPatrimonioTipoAquisicao(ctx, exec, o.PtaID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PatrimonioTipoAquisicaoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PatrimonioTipoAquisicaoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), patrimonioTipoAquisicaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `patrimonio_tipo_aquisicao`.* FROM `patrimonio_tipo_aquisicao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, patrimonioTipoAquisicaoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PatrimonioTipoAquisicaoSlice")
	}

	*o = slice

	return nil
}

// PatrimonioTipoAquisicaoExists checks if the PatrimonioTipoAquisicao row exists.
func PatrimonioTipoAquisicaoExists(ctx context.Context, exec boil.ContextExecutor, ptaID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `patrimonio_tipo_aquisicao` where `pta_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, ptaID)
	}
	row := exec.QueryRowContext(ctx, sql, ptaID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if patrimonio_tipo_aquisicao exists")
	}

	return exists, nil
}

// Exists checks if the PatrimonioTipoAquisicao row exists.
func (o *PatrimonioTipoAquisicao) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PatrimonioTipoAquisicaoExists(ctx, exec, o.PtaID)
}
