// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Unidademedidas is an object representing the database table.
type Unidademedidas struct {
	UndID        int    `boil:"und_id" json:"und_id" toml:"und_id" yaml:"und_id"`
	EmfID        int    `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	UndSigla     string `boil:"und_sigla" json:"und_sigla" toml:"und_sigla" yaml:"und_sigla"`
	UndDescricao string `boil:"und_descricao" json:"und_descricao" toml:"und_descricao" yaml:"und_descricao"`

	R *unidademedidasR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L unidademedidasL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UnidademedidasColumns = struct {
	UndID        string
	EmfID        string
	UndSigla     string
	UndDescricao string
}{
	UndID:        "und_id",
	EmfID:        "emf_id",
	UndSigla:     "und_sigla",
	UndDescricao: "und_descricao",
}

var UnidademedidasTableColumns = struct {
	UndID        string
	EmfID        string
	UndSigla     string
	UndDescricao string
}{
	UndID:        "unidademedidas.und_id",
	EmfID:        "unidademedidas.emf_id",
	UndSigla:     "unidademedidas.und_sigla",
	UndDescricao: "unidademedidas.und_descricao",
}

// Generated where

var UnidademedidasWhere = struct {
	UndID        whereHelperint
	EmfID        whereHelperint
	UndSigla     whereHelperstring
	UndDescricao whereHelperstring
}{
	UndID:        whereHelperint{field: "`unidademedidas`.`und_id`"},
	EmfID:        whereHelperint{field: "`unidademedidas`.`emf_id`"},
	UndSigla:     whereHelperstring{field: "`unidademedidas`.`und_sigla`"},
	UndDescricao: whereHelperstring{field: "`unidademedidas`.`und_descricao`"},
}

// UnidademedidasRels is where relationship names are stored.
var UnidademedidasRels = struct {
	Emf                                string
	UndManifestoEletronicos            string
	UndIDTribNCMS                      string
	UndNCMS                            string
	UndNotaFiscalDestinadaItens        string
	UndIDVendaNotaFiscalDestinadaItens string
	UndIDCompraProdutos                string
	UndIDVendaProdutos                 string
	UndIDVendaProdutoEntidades         string
	UndIDCompraProdutoEntidades        string
}{
	Emf:                                "Emf",
	UndManifestoEletronicos:            "UndManifestoEletronicos",
	UndIDTribNCMS:                      "UndIDTribNCMS",
	UndNCMS:                            "UndNCMS",
	UndNotaFiscalDestinadaItens:        "UndNotaFiscalDestinadaItens",
	UndIDVendaNotaFiscalDestinadaItens: "UndIDVendaNotaFiscalDestinadaItens",
	UndIDCompraProdutos:                "UndIDCompraProdutos",
	UndIDVendaProdutos:                 "UndIDVendaProdutos",
	UndIDVendaProdutoEntidades:         "UndIDVendaProdutoEntidades",
	UndIDCompraProdutoEntidades:        "UndIDCompraProdutoEntidades",
}

// unidademedidasR is where relationships are stored.
type unidademedidasR struct {
	Emf                                *EmpresaFilial               `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	UndManifestoEletronicos            ManifestoEletronicoSlice     `boil:"UndManifestoEletronicos" json:"UndManifestoEletronicos" toml:"UndManifestoEletronicos" yaml:"UndManifestoEletronicos"`
	UndIDTribNCMS                      NCMSlice                     `boil:"UndIDTribNCMS" json:"UndIDTribNCMS" toml:"UndIDTribNCMS" yaml:"UndIDTribNCMS"`
	UndNCMS                            NCMSlice                     `boil:"UndNCMS" json:"UndNCMS" toml:"UndNCMS" yaml:"UndNCMS"`
	UndNotaFiscalDestinadaItens        NotaFiscalDestinadaItenSlice `boil:"UndNotaFiscalDestinadaItens" json:"UndNotaFiscalDestinadaItens" toml:"UndNotaFiscalDestinadaItens" yaml:"UndNotaFiscalDestinadaItens"`
	UndIDVendaNotaFiscalDestinadaItens NotaFiscalDestinadaItenSlice `boil:"UndIDVendaNotaFiscalDestinadaItens" json:"UndIDVendaNotaFiscalDestinadaItens" toml:"UndIDVendaNotaFiscalDestinadaItens" yaml:"UndIDVendaNotaFiscalDestinadaItens"`
	UndIDCompraProdutos                ProdutoSlice                 `boil:"UndIDCompraProdutos" json:"UndIDCompraProdutos" toml:"UndIDCompraProdutos" yaml:"UndIDCompraProdutos"`
	UndIDVendaProdutos                 ProdutoSlice                 `boil:"UndIDVendaProdutos" json:"UndIDVendaProdutos" toml:"UndIDVendaProdutos" yaml:"UndIDVendaProdutos"`
	UndIDVendaProdutoEntidades         ProdutoEntidadeSlice         `boil:"UndIDVendaProdutoEntidades" json:"UndIDVendaProdutoEntidades" toml:"UndIDVendaProdutoEntidades" yaml:"UndIDVendaProdutoEntidades"`
	UndIDCompraProdutoEntidades        ProdutoEntidadeSlice         `boil:"UndIDCompraProdutoEntidades" json:"UndIDCompraProdutoEntidades" toml:"UndIDCompraProdutoEntidades" yaml:"UndIDCompraProdutoEntidades"`
}

// NewStruct creates a new relationship struct
func (*unidademedidasR) NewStruct() *unidademedidasR {
	return &unidademedidasR{}
}

func (r *unidademedidasR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *unidademedidasR) GetUndManifestoEletronicos() ManifestoEletronicoSlice {
	if r == nil {
		return nil
	}
	return r.UndManifestoEletronicos
}

func (r *unidademedidasR) GetUndIDTribNCMS() NCMSlice {
	if r == nil {
		return nil
	}
	return r.UndIDTribNCMS
}

func (r *unidademedidasR) GetUndNCMS() NCMSlice {
	if r == nil {
		return nil
	}
	return r.UndNCMS
}

func (r *unidademedidasR) GetUndNotaFiscalDestinadaItens() NotaFiscalDestinadaItenSlice {
	if r == nil {
		return nil
	}
	return r.UndNotaFiscalDestinadaItens
}

func (r *unidademedidasR) GetUndIDVendaNotaFiscalDestinadaItens() NotaFiscalDestinadaItenSlice {
	if r == nil {
		return nil
	}
	return r.UndIDVendaNotaFiscalDestinadaItens
}

func (r *unidademedidasR) GetUndIDCompraProdutos() ProdutoSlice {
	if r == nil {
		return nil
	}
	return r.UndIDCompraProdutos
}

func (r *unidademedidasR) GetUndIDVendaProdutos() ProdutoSlice {
	if r == nil {
		return nil
	}
	return r.UndIDVendaProdutos
}

func (r *unidademedidasR) GetUndIDVendaProdutoEntidades() ProdutoEntidadeSlice {
	if r == nil {
		return nil
	}
	return r.UndIDVendaProdutoEntidades
}

func (r *unidademedidasR) GetUndIDCompraProdutoEntidades() ProdutoEntidadeSlice {
	if r == nil {
		return nil
	}
	return r.UndIDCompraProdutoEntidades
}

// unidademedidasL is where Load methods for each relationship are stored.
type unidademedidasL struct{}

var (
	unidademedidasAllColumns            = []string{"und_id", "emf_id", "und_sigla", "und_descricao"}
	unidademedidasColumnsWithoutDefault = []string{"und_id", "emf_id", "und_sigla", "und_descricao"}
	unidademedidasColumnsWithDefault    = []string{}
	unidademedidasPrimaryKeyColumns     = []string{"und_id"}
	unidademedidasGeneratedColumns      = []string{}
)

type (
	// UnidademedidasSlice is an alias for a slice of pointers to Unidademedidas.
	// This should almost always be used instead of []Unidademedidas.
	UnidademedidasSlice []*Unidademedidas
	// UnidademedidasHook is the signature for custom Unidademedidas hook methods
	UnidademedidasHook func(context.Context, boil.ContextExecutor, *Unidademedidas) error

	unidademedidasQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	unidademedidasType                 = reflect.TypeOf(&Unidademedidas{})
	unidademedidasMapping              = queries.MakeStructMapping(unidademedidasType)
	unidademedidasPrimaryKeyMapping, _ = queries.BindMapping(unidademedidasType, unidademedidasMapping, unidademedidasPrimaryKeyColumns)
	unidademedidasInsertCacheMut       sync.RWMutex
	unidademedidasInsertCache          = make(map[string]insertCache)
	unidademedidasUpdateCacheMut       sync.RWMutex
	unidademedidasUpdateCache          = make(map[string]updateCache)
	unidademedidasUpsertCacheMut       sync.RWMutex
	unidademedidasUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var unidademedidasAfterSelectMu sync.Mutex
var unidademedidasAfterSelectHooks []UnidademedidasHook

var unidademedidasBeforeInsertMu sync.Mutex
var unidademedidasBeforeInsertHooks []UnidademedidasHook
var unidademedidasAfterInsertMu sync.Mutex
var unidademedidasAfterInsertHooks []UnidademedidasHook

var unidademedidasBeforeUpdateMu sync.Mutex
var unidademedidasBeforeUpdateHooks []UnidademedidasHook
var unidademedidasAfterUpdateMu sync.Mutex
var unidademedidasAfterUpdateHooks []UnidademedidasHook

var unidademedidasBeforeDeleteMu sync.Mutex
var unidademedidasBeforeDeleteHooks []UnidademedidasHook
var unidademedidasAfterDeleteMu sync.Mutex
var unidademedidasAfterDeleteHooks []UnidademedidasHook

var unidademedidasBeforeUpsertMu sync.Mutex
var unidademedidasBeforeUpsertHooks []UnidademedidasHook
var unidademedidasAfterUpsertMu sync.Mutex
var unidademedidasAfterUpsertHooks []UnidademedidasHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Unidademedidas) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Unidademedidas) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Unidademedidas) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Unidademedidas) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Unidademedidas) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Unidademedidas) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Unidademedidas) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Unidademedidas) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Unidademedidas) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range unidademedidasAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUnidademedidasHook registers your hook function for all future operations.
func AddUnidademedidasHook(hookPoint boil.HookPoint, unidademedidasHook UnidademedidasHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		unidademedidasAfterSelectMu.Lock()
		unidademedidasAfterSelectHooks = append(unidademedidasAfterSelectHooks, unidademedidasHook)
		unidademedidasAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		unidademedidasBeforeInsertMu.Lock()
		unidademedidasBeforeInsertHooks = append(unidademedidasBeforeInsertHooks, unidademedidasHook)
		unidademedidasBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		unidademedidasAfterInsertMu.Lock()
		unidademedidasAfterInsertHooks = append(unidademedidasAfterInsertHooks, unidademedidasHook)
		unidademedidasAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		unidademedidasBeforeUpdateMu.Lock()
		unidademedidasBeforeUpdateHooks = append(unidademedidasBeforeUpdateHooks, unidademedidasHook)
		unidademedidasBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		unidademedidasAfterUpdateMu.Lock()
		unidademedidasAfterUpdateHooks = append(unidademedidasAfterUpdateHooks, unidademedidasHook)
		unidademedidasAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		unidademedidasBeforeDeleteMu.Lock()
		unidademedidasBeforeDeleteHooks = append(unidademedidasBeforeDeleteHooks, unidademedidasHook)
		unidademedidasBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		unidademedidasAfterDeleteMu.Lock()
		unidademedidasAfterDeleteHooks = append(unidademedidasAfterDeleteHooks, unidademedidasHook)
		unidademedidasAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		unidademedidasBeforeUpsertMu.Lock()
		unidademedidasBeforeUpsertHooks = append(unidademedidasBeforeUpsertHooks, unidademedidasHook)
		unidademedidasBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		unidademedidasAfterUpsertMu.Lock()
		unidademedidasAfterUpsertHooks = append(unidademedidasAfterUpsertHooks, unidademedidasHook)
		unidademedidasAfterUpsertMu.Unlock()
	}
}

// One returns a single unidademedidas record from the query.
func (q unidademedidasQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Unidademedidas, error) {
	o := &Unidademedidas{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for unidademedidas")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Unidademedidas records from the query.
func (q unidademedidasQuery) All(ctx context.Context, exec boil.ContextExecutor) (UnidademedidasSlice, error) {
	var o []*Unidademedidas

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Unidademedidas slice")
	}

	if len(unidademedidasAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Unidademedidas records in the query.
func (q unidademedidasQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count unidademedidas rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q unidademedidasQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if unidademedidas exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *Unidademedidas) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// UndManifestoEletronicos retrieves all the manifesto_eletronico's ManifestoEletronicos with an executor via und_id column.
func (o *Unidademedidas) UndManifestoEletronicos(mods ...qm.QueryMod) manifestoEletronicoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`manifesto_eletronico`.`und_id`=?", o.UndID),
	)

	return ManifestoEletronicos(queryMods...)
}

// UndIDTribNCMS retrieves all the ncm's NCMS with an executor via und_id_trib column.
func (o *Unidademedidas) UndIDTribNCMS(mods ...qm.QueryMod) ncmQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`ncm`.`und_id_trib`=?", o.UndID),
	)

	return NCMS(queryMods...)
}

// UndNCMS retrieves all the ncm's NCMS with an executor via und_id column.
func (o *Unidademedidas) UndNCMS(mods ...qm.QueryMod) ncmQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`ncm`.`und_id`=?", o.UndID),
	)

	return NCMS(queryMods...)
}

// UndNotaFiscalDestinadaItens retrieves all the nota_fiscal_destinada_iten's NotaFiscalDestinadaItens with an executor via und_id column.
func (o *Unidademedidas) UndNotaFiscalDestinadaItens(mods ...qm.QueryMod) notaFiscalDestinadaItenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal_destinada_itens`.`und_id`=?", o.UndID),
	)

	return NotaFiscalDestinadaItens(queryMods...)
}

// UndIDVendaNotaFiscalDestinadaItens retrieves all the nota_fiscal_destinada_iten's NotaFiscalDestinadaItens with an executor via und_id_venda column.
func (o *Unidademedidas) UndIDVendaNotaFiscalDestinadaItens(mods ...qm.QueryMod) notaFiscalDestinadaItenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal_destinada_itens`.`und_id_venda`=?", o.UndID),
	)

	return NotaFiscalDestinadaItens(queryMods...)
}

// UndIDCompraProdutos retrieves all the produto's Produtos with an executor via und_id_compra column.
func (o *Unidademedidas) UndIDCompraProdutos(mods ...qm.QueryMod) produtoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto`.`und_id_compra`=?", o.UndID),
	)

	return Produtos(queryMods...)
}

// UndIDVendaProdutos retrieves all the produto's Produtos with an executor via und_id_venda column.
func (o *Unidademedidas) UndIDVendaProdutos(mods ...qm.QueryMod) produtoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto`.`und_id_venda`=?", o.UndID),
	)

	return Produtos(queryMods...)
}

// UndIDVendaProdutoEntidades retrieves all the produto_entidade's ProdutoEntidades with an executor via und_id_venda column.
func (o *Unidademedidas) UndIDVendaProdutoEntidades(mods ...qm.QueryMod) produtoEntidadeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto_entidade`.`und_id_venda`=?", o.UndID),
	)

	return ProdutoEntidades(queryMods...)
}

// UndIDCompraProdutoEntidades retrieves all the produto_entidade's ProdutoEntidades with an executor via und_id_compra column.
func (o *Unidademedidas) UndIDCompraProdutoEntidades(mods ...qm.QueryMod) produtoEntidadeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto_entidade`.`und_id_compra`=?", o.UndID),
	)

	return ProdutoEntidades(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (unidademedidasL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfUnidademedidas = append(foreign.R.EmfUnidademedidas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfUnidademedidas = append(foreign.R.EmfUnidademedidas, local)
				break
			}
		}
	}

	return nil
}

// LoadUndManifestoEletronicos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndManifestoEletronicos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`manifesto_eletronico`),
		qm.WhereIn(`manifesto_eletronico.und_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load manifesto_eletronico")
	}

	var resultSlice []*ManifestoEletronico
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice manifesto_eletronico")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on manifesto_eletronico")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for manifesto_eletronico")
	}

	if len(manifestoEletronicoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndManifestoEletronicos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &manifestoEletronicoR{}
			}
			foreign.R.Und = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.UndID == foreign.UndID {
				local.R.UndManifestoEletronicos = append(local.R.UndManifestoEletronicos, foreign)
				if foreign.R == nil {
					foreign.R = &manifestoEletronicoR{}
				}
				foreign.R.Und = local
				break
			}
		}
	}

	return nil
}

// LoadUndIDTribNCMS allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndIDTribNCMS(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ncm`),
		qm.WhereIn(`ncm.und_id_trib in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ncm")
	}

	var resultSlice []*NCM
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ncm")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on ncm")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ncm")
	}

	if len(ncmAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndIDTribNCMS = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &ncmR{}
			}
			foreign.R.UndIDTribUnidademedidas = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.UndID, foreign.UndIDTrib) {
				local.R.UndIDTribNCMS = append(local.R.UndIDTribNCMS, foreign)
				if foreign.R == nil {
					foreign.R = &ncmR{}
				}
				foreign.R.UndIDTribUnidademedidas = local
				break
			}
		}
	}

	return nil
}

// LoadUndNCMS allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndNCMS(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ncm`),
		qm.WhereIn(`ncm.und_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ncm")
	}

	var resultSlice []*NCM
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ncm")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on ncm")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ncm")
	}

	if len(ncmAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndNCMS = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &ncmR{}
			}
			foreign.R.Und = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.UndID == foreign.UndID {
				local.R.UndNCMS = append(local.R.UndNCMS, foreign)
				if foreign.R == nil {
					foreign.R = &ncmR{}
				}
				foreign.R.Und = local
				break
			}
		}
	}

	return nil
}

// LoadUndNotaFiscalDestinadaItens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndNotaFiscalDestinadaItens(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal_destinada_itens`),
		qm.WhereIn(`nota_fiscal_destinada_itens.und_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal_destinada_itens")
	}

	var resultSlice []*NotaFiscalDestinadaIten
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal_destinada_itens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal_destinada_itens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal_destinada_itens")
	}

	if len(notaFiscalDestinadaItenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndNotaFiscalDestinadaItens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalDestinadaItenR{}
			}
			foreign.R.Und = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.UndID, foreign.UndID) {
				local.R.UndNotaFiscalDestinadaItens = append(local.R.UndNotaFiscalDestinadaItens, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalDestinadaItenR{}
				}
				foreign.R.Und = local
				break
			}
		}
	}

	return nil
}

// LoadUndIDVendaNotaFiscalDestinadaItens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndIDVendaNotaFiscalDestinadaItens(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal_destinada_itens`),
		qm.WhereIn(`nota_fiscal_destinada_itens.und_id_venda in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal_destinada_itens")
	}

	var resultSlice []*NotaFiscalDestinadaIten
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal_destinada_itens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal_destinada_itens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal_destinada_itens")
	}

	if len(notaFiscalDestinadaItenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndIDVendaNotaFiscalDestinadaItens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalDestinadaItenR{}
			}
			foreign.R.UndIDVendaUnidademedidas = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.UndID, foreign.UndIDVenda) {
				local.R.UndIDVendaNotaFiscalDestinadaItens = append(local.R.UndIDVendaNotaFiscalDestinadaItens, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalDestinadaItenR{}
				}
				foreign.R.UndIDVendaUnidademedidas = local
				break
			}
		}
	}

	return nil
}

// LoadUndIDCompraProdutos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndIDCompraProdutos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.und_id_compra in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndIDCompraProdutos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoR{}
			}
			foreign.R.UndIDCompraUnidademedidas = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.UndID, foreign.UndIDCompra) {
				local.R.UndIDCompraProdutos = append(local.R.UndIDCompraProdutos, foreign)
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.UndIDCompraUnidademedidas = local
				break
			}
		}
	}

	return nil
}

// LoadUndIDVendaProdutos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndIDVendaProdutos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.und_id_venda in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndIDVendaProdutos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoR{}
			}
			foreign.R.UndIDVendaUnidademedidas = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.UndID, foreign.UndIDVenda) {
				local.R.UndIDVendaProdutos = append(local.R.UndIDVendaProdutos, foreign)
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.UndIDVendaUnidademedidas = local
				break
			}
		}
	}

	return nil
}

// LoadUndIDVendaProdutoEntidades allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndIDVendaProdutoEntidades(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_entidade`),
		qm.WhereIn(`produto_entidade.und_id_venda in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto_entidade")
	}

	var resultSlice []*ProdutoEntidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto_entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto_entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_entidade")
	}

	if len(produtoEntidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndIDVendaProdutoEntidades = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoEntidadeR{}
			}
			foreign.R.UndIDVendaUnidademedidas = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.UndID, foreign.UndIDVenda) {
				local.R.UndIDVendaProdutoEntidades = append(local.R.UndIDVendaProdutoEntidades, foreign)
				if foreign.R == nil {
					foreign.R = &produtoEntidadeR{}
				}
				foreign.R.UndIDVendaUnidademedidas = local
				break
			}
		}
	}

	return nil
}

// LoadUndIDCompraProdutoEntidades allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (unidademedidasL) LoadUndIDCompraProdutoEntidades(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUnidademedidas interface{}, mods queries.Applicator) error {
	var slice []*Unidademedidas
	var object *Unidademedidas

	if singular {
		var ok bool
		object, ok = maybeUnidademedidas.(*Unidademedidas)
		if !ok {
			object = new(Unidademedidas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUnidademedidas))
			}
		}
	} else {
		s, ok := maybeUnidademedidas.(*[]*Unidademedidas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUnidademedidas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUnidademedidas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &unidademedidasR{}
		}
		args[object.UndID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &unidademedidasR{}
			}
			args[obj.UndID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_entidade`),
		qm.WhereIn(`produto_entidade.und_id_compra in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto_entidade")
	}

	var resultSlice []*ProdutoEntidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto_entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto_entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_entidade")
	}

	if len(produtoEntidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UndIDCompraProdutoEntidades = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoEntidadeR{}
			}
			foreign.R.UndIDCompraUnidademedidas = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.UndID, foreign.UndIDCompra) {
				local.R.UndIDCompraProdutoEntidades = append(local.R.UndIDCompraProdutoEntidades, foreign)
				if foreign.R == nil {
					foreign.R = &produtoEntidadeR{}
				}
				foreign.R.UndIDCompraUnidademedidas = local
				break
			}
		}
	}

	return nil
}

// SetEmf of the unidademedidas to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfUnidademedidas.
func (o *Unidademedidas) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `unidademedidas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, unidademedidasPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.UndID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &unidademedidasR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfUnidademedidas: UnidademedidasSlice{o},
		}
	} else {
		related.R.EmfUnidademedidas = append(related.R.EmfUnidademedidas, o)
	}

	return nil
}

// AddUndManifestoEletronicos adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndManifestoEletronicos.
// Sets related.R.Und appropriately.
func (o *Unidademedidas) AddUndManifestoEletronicos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ManifestoEletronico) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.UndID = o.UndID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `manifesto_eletronico` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id"}),
				strmangle.WhereClause("`", "`", 0, manifestoEletronicoPrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.MdfeID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.UndID = o.UndID
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndManifestoEletronicos: related,
		}
	} else {
		o.R.UndManifestoEletronicos = append(o.R.UndManifestoEletronicos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &manifestoEletronicoR{
				Und: o,
			}
		} else {
			rel.R.Und = o
		}
	}
	return nil
}

// AddUndIDTribNCMS adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndIDTribNCMS.
// Sets related.R.UndIDTribUnidademedidas appropriately.
func (o *Unidademedidas) AddUndIDTribNCMS(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NCM) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UndIDTrib, o.UndID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `ncm` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id_trib"}),
				strmangle.WhereClause("`", "`", 0, ncmPrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.NCMNumero}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UndIDTrib, o.UndID)
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndIDTribNCMS: related,
		}
	} else {
		o.R.UndIDTribNCMS = append(o.R.UndIDTribNCMS, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &ncmR{
				UndIDTribUnidademedidas: o,
			}
		} else {
			rel.R.UndIDTribUnidademedidas = o
		}
	}
	return nil
}

// SetUndIDTribNCMS removes all previously related items of the
// unidademedidas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UndIDTribUnidademedidas's UndIDTribNCMS accordingly.
// Replaces o.R.UndIDTribNCMS with related.
// Sets related.R.UndIDTribUnidademedidas's UndIDTribNCMS accordingly.
func (o *Unidademedidas) SetUndIDTribNCMS(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NCM) error {
	query := "update `ncm` set `und_id_trib` = null where `und_id_trib` = ?"
	values := []interface{}{o.UndID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UndIDTribNCMS {
			queries.SetScanner(&rel.UndIDTrib, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UndIDTribUnidademedidas = nil
		}
		o.R.UndIDTribNCMS = nil
	}

	return o.AddUndIDTribNCMS(ctx, exec, insert, related...)
}

// RemoveUndIDTribNCMS relationships from objects passed in.
// Removes related items from R.UndIDTribNCMS (uses pointer comparison, removal does not keep order)
// Sets related.R.UndIDTribUnidademedidas.
func (o *Unidademedidas) RemoveUndIDTribNCMS(ctx context.Context, exec boil.ContextExecutor, related ...*NCM) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UndIDTrib, nil)
		if rel.R != nil {
			rel.R.UndIDTribUnidademedidas = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("und_id_trib")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UndIDTribNCMS {
			if rel != ri {
				continue
			}

			ln := len(o.R.UndIDTribNCMS)
			if ln > 1 && i < ln-1 {
				o.R.UndIDTribNCMS[i] = o.R.UndIDTribNCMS[ln-1]
			}
			o.R.UndIDTribNCMS = o.R.UndIDTribNCMS[:ln-1]
			break
		}
	}

	return nil
}

// AddUndNCMS adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndNCMS.
// Sets related.R.Und appropriately.
func (o *Unidademedidas) AddUndNCMS(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NCM) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.UndID = o.UndID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `ncm` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id"}),
				strmangle.WhereClause("`", "`", 0, ncmPrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.NCMNumero}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.UndID = o.UndID
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndNCMS: related,
		}
	} else {
		o.R.UndNCMS = append(o.R.UndNCMS, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &ncmR{
				Und: o,
			}
		} else {
			rel.R.Und = o
		}
	}
	return nil
}

// AddUndNotaFiscalDestinadaItens adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndNotaFiscalDestinadaItens.
// Sets related.R.Und appropriately.
func (o *Unidademedidas) AddUndNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalDestinadaIten) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UndID, o.UndID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal_destinada_itens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalDestinadaItenPrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.NFDID, rel.NfdiItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UndID, o.UndID)
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndNotaFiscalDestinadaItens: related,
		}
	} else {
		o.R.UndNotaFiscalDestinadaItens = append(o.R.UndNotaFiscalDestinadaItens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalDestinadaItenR{
				Und: o,
			}
		} else {
			rel.R.Und = o
		}
	}
	return nil
}

// SetUndNotaFiscalDestinadaItens removes all previously related items of the
// unidademedidas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Und's UndNotaFiscalDestinadaItens accordingly.
// Replaces o.R.UndNotaFiscalDestinadaItens with related.
// Sets related.R.Und's UndNotaFiscalDestinadaItens accordingly.
func (o *Unidademedidas) SetUndNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalDestinadaIten) error {
	query := "update `nota_fiscal_destinada_itens` set `und_id` = null where `und_id` = ?"
	values := []interface{}{o.UndID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UndNotaFiscalDestinadaItens {
			queries.SetScanner(&rel.UndID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Und = nil
		}
		o.R.UndNotaFiscalDestinadaItens = nil
	}

	return o.AddUndNotaFiscalDestinadaItens(ctx, exec, insert, related...)
}

// RemoveUndNotaFiscalDestinadaItens relationships from objects passed in.
// Removes related items from R.UndNotaFiscalDestinadaItens (uses pointer comparison, removal does not keep order)
// Sets related.R.Und.
func (o *Unidademedidas) RemoveUndNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, related ...*NotaFiscalDestinadaIten) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UndID, nil)
		if rel.R != nil {
			rel.R.Und = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("und_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UndNotaFiscalDestinadaItens {
			if rel != ri {
				continue
			}

			ln := len(o.R.UndNotaFiscalDestinadaItens)
			if ln > 1 && i < ln-1 {
				o.R.UndNotaFiscalDestinadaItens[i] = o.R.UndNotaFiscalDestinadaItens[ln-1]
			}
			o.R.UndNotaFiscalDestinadaItens = o.R.UndNotaFiscalDestinadaItens[:ln-1]
			break
		}
	}

	return nil
}

// AddUndIDVendaNotaFiscalDestinadaItens adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndIDVendaNotaFiscalDestinadaItens.
// Sets related.R.UndIDVendaUnidademedidas appropriately.
func (o *Unidademedidas) AddUndIDVendaNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalDestinadaIten) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UndIDVenda, o.UndID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal_destinada_itens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id_venda"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalDestinadaItenPrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.NFDID, rel.NfdiItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UndIDVenda, o.UndID)
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndIDVendaNotaFiscalDestinadaItens: related,
		}
	} else {
		o.R.UndIDVendaNotaFiscalDestinadaItens = append(o.R.UndIDVendaNotaFiscalDestinadaItens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalDestinadaItenR{
				UndIDVendaUnidademedidas: o,
			}
		} else {
			rel.R.UndIDVendaUnidademedidas = o
		}
	}
	return nil
}

// SetUndIDVendaNotaFiscalDestinadaItens removes all previously related items of the
// unidademedidas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UndIDVendaUnidademedidas's UndIDVendaNotaFiscalDestinadaItens accordingly.
// Replaces o.R.UndIDVendaNotaFiscalDestinadaItens with related.
// Sets related.R.UndIDVendaUnidademedidas's UndIDVendaNotaFiscalDestinadaItens accordingly.
func (o *Unidademedidas) SetUndIDVendaNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalDestinadaIten) error {
	query := "update `nota_fiscal_destinada_itens` set `und_id_venda` = null where `und_id_venda` = ?"
	values := []interface{}{o.UndID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UndIDVendaNotaFiscalDestinadaItens {
			queries.SetScanner(&rel.UndIDVenda, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UndIDVendaUnidademedidas = nil
		}
		o.R.UndIDVendaNotaFiscalDestinadaItens = nil
	}

	return o.AddUndIDVendaNotaFiscalDestinadaItens(ctx, exec, insert, related...)
}

// RemoveUndIDVendaNotaFiscalDestinadaItens relationships from objects passed in.
// Removes related items from R.UndIDVendaNotaFiscalDestinadaItens (uses pointer comparison, removal does not keep order)
// Sets related.R.UndIDVendaUnidademedidas.
func (o *Unidademedidas) RemoveUndIDVendaNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, related ...*NotaFiscalDestinadaIten) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UndIDVenda, nil)
		if rel.R != nil {
			rel.R.UndIDVendaUnidademedidas = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("und_id_venda")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UndIDVendaNotaFiscalDestinadaItens {
			if rel != ri {
				continue
			}

			ln := len(o.R.UndIDVendaNotaFiscalDestinadaItens)
			if ln > 1 && i < ln-1 {
				o.R.UndIDVendaNotaFiscalDestinadaItens[i] = o.R.UndIDVendaNotaFiscalDestinadaItens[ln-1]
			}
			o.R.UndIDVendaNotaFiscalDestinadaItens = o.R.UndIDVendaNotaFiscalDestinadaItens[:ln-1]
			break
		}
	}

	return nil
}

// AddUndIDCompraProdutos adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndIDCompraProdutos.
// Sets related.R.UndIDCompraUnidademedidas appropriately.
func (o *Unidademedidas) AddUndIDCompraProdutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Produto) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UndIDCompra, o.UndID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id_compra"}),
				strmangle.WhereClause("`", "`", 0, produtoPrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.ProID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UndIDCompra, o.UndID)
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndIDCompraProdutos: related,
		}
	} else {
		o.R.UndIDCompraProdutos = append(o.R.UndIDCompraProdutos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoR{
				UndIDCompraUnidademedidas: o,
			}
		} else {
			rel.R.UndIDCompraUnidademedidas = o
		}
	}
	return nil
}

// SetUndIDCompraProdutos removes all previously related items of the
// unidademedidas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UndIDCompraUnidademedidas's UndIDCompraProdutos accordingly.
// Replaces o.R.UndIDCompraProdutos with related.
// Sets related.R.UndIDCompraUnidademedidas's UndIDCompraProdutos accordingly.
func (o *Unidademedidas) SetUndIDCompraProdutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Produto) error {
	query := "update `produto` set `und_id_compra` = null where `und_id_compra` = ?"
	values := []interface{}{o.UndID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UndIDCompraProdutos {
			queries.SetScanner(&rel.UndIDCompra, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UndIDCompraUnidademedidas = nil
		}
		o.R.UndIDCompraProdutos = nil
	}

	return o.AddUndIDCompraProdutos(ctx, exec, insert, related...)
}

// RemoveUndIDCompraProdutos relationships from objects passed in.
// Removes related items from R.UndIDCompraProdutos (uses pointer comparison, removal does not keep order)
// Sets related.R.UndIDCompraUnidademedidas.
func (o *Unidademedidas) RemoveUndIDCompraProdutos(ctx context.Context, exec boil.ContextExecutor, related ...*Produto) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UndIDCompra, nil)
		if rel.R != nil {
			rel.R.UndIDCompraUnidademedidas = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("und_id_compra")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UndIDCompraProdutos {
			if rel != ri {
				continue
			}

			ln := len(o.R.UndIDCompraProdutos)
			if ln > 1 && i < ln-1 {
				o.R.UndIDCompraProdutos[i] = o.R.UndIDCompraProdutos[ln-1]
			}
			o.R.UndIDCompraProdutos = o.R.UndIDCompraProdutos[:ln-1]
			break
		}
	}

	return nil
}

// AddUndIDVendaProdutos adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndIDVendaProdutos.
// Sets related.R.UndIDVendaUnidademedidas appropriately.
func (o *Unidademedidas) AddUndIDVendaProdutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Produto) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UndIDVenda, o.UndID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id_venda"}),
				strmangle.WhereClause("`", "`", 0, produtoPrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.ProID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UndIDVenda, o.UndID)
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndIDVendaProdutos: related,
		}
	} else {
		o.R.UndIDVendaProdutos = append(o.R.UndIDVendaProdutos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoR{
				UndIDVendaUnidademedidas: o,
			}
		} else {
			rel.R.UndIDVendaUnidademedidas = o
		}
	}
	return nil
}

// SetUndIDVendaProdutos removes all previously related items of the
// unidademedidas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UndIDVendaUnidademedidas's UndIDVendaProdutos accordingly.
// Replaces o.R.UndIDVendaProdutos with related.
// Sets related.R.UndIDVendaUnidademedidas's UndIDVendaProdutos accordingly.
func (o *Unidademedidas) SetUndIDVendaProdutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Produto) error {
	query := "update `produto` set `und_id_venda` = null where `und_id_venda` = ?"
	values := []interface{}{o.UndID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UndIDVendaProdutos {
			queries.SetScanner(&rel.UndIDVenda, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UndIDVendaUnidademedidas = nil
		}
		o.R.UndIDVendaProdutos = nil
	}

	return o.AddUndIDVendaProdutos(ctx, exec, insert, related...)
}

// RemoveUndIDVendaProdutos relationships from objects passed in.
// Removes related items from R.UndIDVendaProdutos (uses pointer comparison, removal does not keep order)
// Sets related.R.UndIDVendaUnidademedidas.
func (o *Unidademedidas) RemoveUndIDVendaProdutos(ctx context.Context, exec boil.ContextExecutor, related ...*Produto) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UndIDVenda, nil)
		if rel.R != nil {
			rel.R.UndIDVendaUnidademedidas = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("und_id_venda")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UndIDVendaProdutos {
			if rel != ri {
				continue
			}

			ln := len(o.R.UndIDVendaProdutos)
			if ln > 1 && i < ln-1 {
				o.R.UndIDVendaProdutos[i] = o.R.UndIDVendaProdutos[ln-1]
			}
			o.R.UndIDVendaProdutos = o.R.UndIDVendaProdutos[:ln-1]
			break
		}
	}

	return nil
}

// AddUndIDVendaProdutoEntidades adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndIDVendaProdutoEntidades.
// Sets related.R.UndIDVendaUnidademedidas appropriately.
func (o *Unidademedidas) AddUndIDVendaProdutoEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoEntidade) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UndIDVenda, o.UndID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto_entidade` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id_venda"}),
				strmangle.WhereClause("`", "`", 0, produtoEntidadePrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.ProID, rel.EntID, rel.ProentCodigo}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UndIDVenda, o.UndID)
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndIDVendaProdutoEntidades: related,
		}
	} else {
		o.R.UndIDVendaProdutoEntidades = append(o.R.UndIDVendaProdutoEntidades, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoEntidadeR{
				UndIDVendaUnidademedidas: o,
			}
		} else {
			rel.R.UndIDVendaUnidademedidas = o
		}
	}
	return nil
}

// SetUndIDVendaProdutoEntidades removes all previously related items of the
// unidademedidas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UndIDVendaUnidademedidas's UndIDVendaProdutoEntidades accordingly.
// Replaces o.R.UndIDVendaProdutoEntidades with related.
// Sets related.R.UndIDVendaUnidademedidas's UndIDVendaProdutoEntidades accordingly.
func (o *Unidademedidas) SetUndIDVendaProdutoEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoEntidade) error {
	query := "update `produto_entidade` set `und_id_venda` = null where `und_id_venda` = ?"
	values := []interface{}{o.UndID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UndIDVendaProdutoEntidades {
			queries.SetScanner(&rel.UndIDVenda, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UndIDVendaUnidademedidas = nil
		}
		o.R.UndIDVendaProdutoEntidades = nil
	}

	return o.AddUndIDVendaProdutoEntidades(ctx, exec, insert, related...)
}

// RemoveUndIDVendaProdutoEntidades relationships from objects passed in.
// Removes related items from R.UndIDVendaProdutoEntidades (uses pointer comparison, removal does not keep order)
// Sets related.R.UndIDVendaUnidademedidas.
func (o *Unidademedidas) RemoveUndIDVendaProdutoEntidades(ctx context.Context, exec boil.ContextExecutor, related ...*ProdutoEntidade) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UndIDVenda, nil)
		if rel.R != nil {
			rel.R.UndIDVendaUnidademedidas = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("und_id_venda")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UndIDVendaProdutoEntidades {
			if rel != ri {
				continue
			}

			ln := len(o.R.UndIDVendaProdutoEntidades)
			if ln > 1 && i < ln-1 {
				o.R.UndIDVendaProdutoEntidades[i] = o.R.UndIDVendaProdutoEntidades[ln-1]
			}
			o.R.UndIDVendaProdutoEntidades = o.R.UndIDVendaProdutoEntidades[:ln-1]
			break
		}
	}

	return nil
}

// AddUndIDCompraProdutoEntidades adds the given related objects to the existing relationships
// of the unidademedidas, optionally inserting them as new records.
// Appends related to o.R.UndIDCompraProdutoEntidades.
// Sets related.R.UndIDCompraUnidademedidas appropriately.
func (o *Unidademedidas) AddUndIDCompraProdutoEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoEntidade) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UndIDCompra, o.UndID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto_entidade` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"und_id_compra"}),
				strmangle.WhereClause("`", "`", 0, produtoEntidadePrimaryKeyColumns),
			)
			values := []interface{}{o.UndID, rel.ProID, rel.EntID, rel.ProentCodigo}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UndIDCompra, o.UndID)
		}
	}

	if o.R == nil {
		o.R = &unidademedidasR{
			UndIDCompraProdutoEntidades: related,
		}
	} else {
		o.R.UndIDCompraProdutoEntidades = append(o.R.UndIDCompraProdutoEntidades, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoEntidadeR{
				UndIDCompraUnidademedidas: o,
			}
		} else {
			rel.R.UndIDCompraUnidademedidas = o
		}
	}
	return nil
}

// SetUndIDCompraProdutoEntidades removes all previously related items of the
// unidademedidas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UndIDCompraUnidademedidas's UndIDCompraProdutoEntidades accordingly.
// Replaces o.R.UndIDCompraProdutoEntidades with related.
// Sets related.R.UndIDCompraUnidademedidas's UndIDCompraProdutoEntidades accordingly.
func (o *Unidademedidas) SetUndIDCompraProdutoEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoEntidade) error {
	query := "update `produto_entidade` set `und_id_compra` = null where `und_id_compra` = ?"
	values := []interface{}{o.UndID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UndIDCompraProdutoEntidades {
			queries.SetScanner(&rel.UndIDCompra, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UndIDCompraUnidademedidas = nil
		}
		o.R.UndIDCompraProdutoEntidades = nil
	}

	return o.AddUndIDCompraProdutoEntidades(ctx, exec, insert, related...)
}

// RemoveUndIDCompraProdutoEntidades relationships from objects passed in.
// Removes related items from R.UndIDCompraProdutoEntidades (uses pointer comparison, removal does not keep order)
// Sets related.R.UndIDCompraUnidademedidas.
func (o *Unidademedidas) RemoveUndIDCompraProdutoEntidades(ctx context.Context, exec boil.ContextExecutor, related ...*ProdutoEntidade) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UndIDCompra, nil)
		if rel.R != nil {
			rel.R.UndIDCompraUnidademedidas = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("und_id_compra")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UndIDCompraProdutoEntidades {
			if rel != ri {
				continue
			}

			ln := len(o.R.UndIDCompraProdutoEntidades)
			if ln > 1 && i < ln-1 {
				o.R.UndIDCompraProdutoEntidades[i] = o.R.UndIDCompraProdutoEntidades[ln-1]
			}
			o.R.UndIDCompraProdutoEntidades = o.R.UndIDCompraProdutoEntidades[:ln-1]
			break
		}
	}

	return nil
}

// Unidademedidas retrieves all the records using an executor.
func Unidademedidas(mods ...qm.QueryMod) unidademedidasQuery {
	mods = append(mods, qm.From("`unidademedidas`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`unidademedidas`.*"})
	}

	return unidademedidasQuery{q}
}

// FindUnidademedidas retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUnidademedidas(ctx context.Context, exec boil.ContextExecutor, undID int, selectCols ...string) (*Unidademedidas, error) {
	unidademedidasObj := &Unidademedidas{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `unidademedidas` where `und_id`=?", sel,
	)

	q := queries.Raw(query, undID)

	err := q.Bind(ctx, exec, unidademedidasObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from unidademedidas")
	}

	if err = unidademedidasObj.doAfterSelectHooks(ctx, exec); err != nil {
		return unidademedidasObj, err
	}

	return unidademedidasObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Unidademedidas) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no unidademedidas provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(unidademedidasColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	unidademedidasInsertCacheMut.RLock()
	cache, cached := unidademedidasInsertCache[key]
	unidademedidasInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			unidademedidasAllColumns,
			unidademedidasColumnsWithDefault,
			unidademedidasColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(unidademedidasType, unidademedidasMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(unidademedidasType, unidademedidasMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `unidademedidas` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `unidademedidas` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `unidademedidas` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, unidademedidasPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into unidademedidas")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.UndID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for unidademedidas")
	}

CacheNoHooks:
	if !cached {
		unidademedidasInsertCacheMut.Lock()
		unidademedidasInsertCache[key] = cache
		unidademedidasInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Unidademedidas.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Unidademedidas) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	unidademedidasUpdateCacheMut.RLock()
	cache, cached := unidademedidasUpdateCache[key]
	unidademedidasUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			unidademedidasAllColumns,
			unidademedidasPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update unidademedidas, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `unidademedidas` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, unidademedidasPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(unidademedidasType, unidademedidasMapping, append(wl, unidademedidasPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update unidademedidas row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for unidademedidas")
	}

	if !cached {
		unidademedidasUpdateCacheMut.Lock()
		unidademedidasUpdateCache[key] = cache
		unidademedidasUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q unidademedidasQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for unidademedidas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for unidademedidas")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UnidademedidasSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), unidademedidasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `unidademedidas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, unidademedidasPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in unidademedidas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all unidademedidas")
	}
	return rowsAff, nil
}

var mySQLUnidademedidasUniqueColumns = []string{
	"und_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Unidademedidas) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no unidademedidas provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(unidademedidasColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLUnidademedidasUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	unidademedidasUpsertCacheMut.RLock()
	cache, cached := unidademedidasUpsertCache[key]
	unidademedidasUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			unidademedidasAllColumns,
			unidademedidasColumnsWithDefault,
			unidademedidasColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			unidademedidasAllColumns,
			unidademedidasPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert unidademedidas, could not build update column list")
		}

		ret := strmangle.SetComplement(unidademedidasAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`unidademedidas`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `unidademedidas` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(unidademedidasType, unidademedidasMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(unidademedidasType, unidademedidasMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for unidademedidas")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(unidademedidasType, unidademedidasMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for unidademedidas")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for unidademedidas")
	}

CacheNoHooks:
	if !cached {
		unidademedidasUpsertCacheMut.Lock()
		unidademedidasUpsertCache[key] = cache
		unidademedidasUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Unidademedidas record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Unidademedidas) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Unidademedidas provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), unidademedidasPrimaryKeyMapping)
	sql := "DELETE FROM `unidademedidas` WHERE `und_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from unidademedidas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for unidademedidas")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q unidademedidasQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no unidademedidasQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from unidademedidas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for unidademedidas")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UnidademedidasSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(unidademedidasBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), unidademedidasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `unidademedidas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, unidademedidasPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from unidademedidas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for unidademedidas")
	}

	if len(unidademedidasAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Unidademedidas) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUnidademedidas(ctx, exec, o.UndID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UnidademedidasSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UnidademedidasSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), unidademedidasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `unidademedidas`.* FROM `unidademedidas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, unidademedidasPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UnidademedidasSlice")
	}

	*o = slice

	return nil
}

// UnidademedidasExists checks if the Unidademedidas row exists.
func UnidademedidasExists(ctx context.Context, exec boil.ContextExecutor, undID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `unidademedidas` where `und_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, undID)
	}
	row := exec.QueryRowContext(ctx, sql, undID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if unidademedidas exists")
	}

	return exists, nil
}

// Exists checks if the Unidademedidas row exists.
func (o *Unidademedidas) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UnidademedidasExists(ctx, exec, o.UndID)
}
