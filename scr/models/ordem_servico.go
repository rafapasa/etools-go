// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// OrdemServico is an object representing the database table.
type OrdemServico struct {
	OsID               int         `boil:"os_id" json:"os_id" toml:"os_id" yaml:"os_id"`
	EntID              int         `boil:"ent_id" json:"ent_id" toml:"ent_id" yaml:"ent_id"`
	VeiID              null.Int    `boil:"vei_id" json:"vei_id,omitempty" toml:"vei_id" yaml:"vei_id,omitempty"`
	OsDatacadastro     time.Time   `boil:"os_datacadastro" json:"os_datacadastro" toml:"os_datacadastro" yaml:"os_datacadastro"`
	OsDataprevisao     null.Time   `boil:"os_dataprevisao" json:"os_dataprevisao,omitempty" toml:"os_dataprevisao" yaml:"os_dataprevisao,omitempty"`
	OsDatainicio       null.Time   `boil:"os_datainicio" json:"os_datainicio,omitempty" toml:"os_datainicio" yaml:"os_datainicio,omitempty"`
	OsDatatermino      null.Time   `boil:"os_datatermino" json:"os_datatermino,omitempty" toml:"os_datatermino" yaml:"os_datatermino,omitempty"`
	OsSituacao         int16       `boil:"os_situacao" json:"os_situacao" toml:"os_situacao" yaml:"os_situacao"`
	OsDescricaocliente null.String `boil:"os_descricaocliente" json:"os_descricaocliente,omitempty" toml:"os_descricaocliente" yaml:"os_descricaocliente,omitempty"`
	OsDescricaotecnica null.String `boil:"os_descricaotecnica" json:"os_descricaotecnica,omitempty" toml:"os_descricaotecnica" yaml:"os_descricaotecnica,omitempty"`

	R *ordemServicoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ordemServicoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var OrdemServicoColumns = struct {
	OsID               string
	EntID              string
	VeiID              string
	OsDatacadastro     string
	OsDataprevisao     string
	OsDatainicio       string
	OsDatatermino      string
	OsSituacao         string
	OsDescricaocliente string
	OsDescricaotecnica string
}{
	OsID:               "os_id",
	EntID:              "ent_id",
	VeiID:              "vei_id",
	OsDatacadastro:     "os_datacadastro",
	OsDataprevisao:     "os_dataprevisao",
	OsDatainicio:       "os_datainicio",
	OsDatatermino:      "os_datatermino",
	OsSituacao:         "os_situacao",
	OsDescricaocliente: "os_descricaocliente",
	OsDescricaotecnica: "os_descricaotecnica",
}

var OrdemServicoTableColumns = struct {
	OsID               string
	EntID              string
	VeiID              string
	OsDatacadastro     string
	OsDataprevisao     string
	OsDatainicio       string
	OsDatatermino      string
	OsSituacao         string
	OsDescricaocliente string
	OsDescricaotecnica string
}{
	OsID:               "ordem_servico.os_id",
	EntID:              "ordem_servico.ent_id",
	VeiID:              "ordem_servico.vei_id",
	OsDatacadastro:     "ordem_servico.os_datacadastro",
	OsDataprevisao:     "ordem_servico.os_dataprevisao",
	OsDatainicio:       "ordem_servico.os_datainicio",
	OsDatatermino:      "ordem_servico.os_datatermino",
	OsSituacao:         "ordem_servico.os_situacao",
	OsDescricaocliente: "ordem_servico.os_descricaocliente",
	OsDescricaotecnica: "ordem_servico.os_descricaotecnica",
}

// Generated where

var OrdemServicoWhere = struct {
	OsID               whereHelperint
	EntID              whereHelperint
	VeiID              whereHelpernull_Int
	OsDatacadastro     whereHelpertime_Time
	OsDataprevisao     whereHelpernull_Time
	OsDatainicio       whereHelpernull_Time
	OsDatatermino      whereHelpernull_Time
	OsSituacao         whereHelperint16
	OsDescricaocliente whereHelpernull_String
	OsDescricaotecnica whereHelpernull_String
}{
	OsID:               whereHelperint{field: "`ordem_servico`.`os_id`"},
	EntID:              whereHelperint{field: "`ordem_servico`.`ent_id`"},
	VeiID:              whereHelpernull_Int{field: "`ordem_servico`.`vei_id`"},
	OsDatacadastro:     whereHelpertime_Time{field: "`ordem_servico`.`os_datacadastro`"},
	OsDataprevisao:     whereHelpernull_Time{field: "`ordem_servico`.`os_dataprevisao`"},
	OsDatainicio:       whereHelpernull_Time{field: "`ordem_servico`.`os_datainicio`"},
	OsDatatermino:      whereHelpernull_Time{field: "`ordem_servico`.`os_datatermino`"},
	OsSituacao:         whereHelperint16{field: "`ordem_servico`.`os_situacao`"},
	OsDescricaocliente: whereHelpernull_String{field: "`ordem_servico`.`os_descricaocliente`"},
	OsDescricaotecnica: whereHelpernull_String{field: "`ordem_servico`.`os_descricaotecnica`"},
}

// OrdemServicoRels is where relationship names are stored.
var OrdemServicoRels = struct {
	Ent                string
	Vei                string
	DDVDocumentoVendas string
}{
	Ent:                "Ent",
	Vei:                "Vei",
	DDVDocumentoVendas: "DDVDocumentoVendas",
}

// ordemServicoR is where relationships are stored.
type ordemServicoR struct {
	Ent                *Entidade           `boil:"Ent" json:"Ent" toml:"Ent" yaml:"Ent"`
	Vei                *Veiculo            `boil:"Vei" json:"Vei" toml:"Vei" yaml:"Vei"`
	DDVDocumentoVendas DocumentoVendaSlice `boil:"DDVDocumentoVendas" json:"DDVDocumentoVendas" toml:"DDVDocumentoVendas" yaml:"DDVDocumentoVendas"`
}

// NewStruct creates a new relationship struct
func (*ordemServicoR) NewStruct() *ordemServicoR {
	return &ordemServicoR{}
}

func (r *ordemServicoR) GetEnt() *Entidade {
	if r == nil {
		return nil
	}
	return r.Ent
}

func (r *ordemServicoR) GetVei() *Veiculo {
	if r == nil {
		return nil
	}
	return r.Vei
}

func (r *ordemServicoR) GetDDVDocumentoVendas() DocumentoVendaSlice {
	if r == nil {
		return nil
	}
	return r.DDVDocumentoVendas
}

// ordemServicoL is where Load methods for each relationship are stored.
type ordemServicoL struct{}

var (
	ordemServicoAllColumns            = []string{"os_id", "ent_id", "vei_id", "os_datacadastro", "os_dataprevisao", "os_datainicio", "os_datatermino", "os_situacao", "os_descricaocliente", "os_descricaotecnica"}
	ordemServicoColumnsWithoutDefault = []string{"os_id", "ent_id", "vei_id", "os_datacadastro", "os_dataprevisao", "os_datainicio", "os_datatermino", "os_descricaocliente", "os_descricaotecnica"}
	ordemServicoColumnsWithDefault    = []string{"os_situacao"}
	ordemServicoPrimaryKeyColumns     = []string{"os_id"}
	ordemServicoGeneratedColumns      = []string{}
)

type (
	// OrdemServicoSlice is an alias for a slice of pointers to OrdemServico.
	// This should almost always be used instead of []OrdemServico.
	OrdemServicoSlice []*OrdemServico
	// OrdemServicoHook is the signature for custom OrdemServico hook methods
	OrdemServicoHook func(context.Context, boil.ContextExecutor, *OrdemServico) error

	ordemServicoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ordemServicoType                 = reflect.TypeOf(&OrdemServico{})
	ordemServicoMapping              = queries.MakeStructMapping(ordemServicoType)
	ordemServicoPrimaryKeyMapping, _ = queries.BindMapping(ordemServicoType, ordemServicoMapping, ordemServicoPrimaryKeyColumns)
	ordemServicoInsertCacheMut       sync.RWMutex
	ordemServicoInsertCache          = make(map[string]insertCache)
	ordemServicoUpdateCacheMut       sync.RWMutex
	ordemServicoUpdateCache          = make(map[string]updateCache)
	ordemServicoUpsertCacheMut       sync.RWMutex
	ordemServicoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ordemServicoAfterSelectMu sync.Mutex
var ordemServicoAfterSelectHooks []OrdemServicoHook

var ordemServicoBeforeInsertMu sync.Mutex
var ordemServicoBeforeInsertHooks []OrdemServicoHook
var ordemServicoAfterInsertMu sync.Mutex
var ordemServicoAfterInsertHooks []OrdemServicoHook

var ordemServicoBeforeUpdateMu sync.Mutex
var ordemServicoBeforeUpdateHooks []OrdemServicoHook
var ordemServicoAfterUpdateMu sync.Mutex
var ordemServicoAfterUpdateHooks []OrdemServicoHook

var ordemServicoBeforeDeleteMu sync.Mutex
var ordemServicoBeforeDeleteHooks []OrdemServicoHook
var ordemServicoAfterDeleteMu sync.Mutex
var ordemServicoAfterDeleteHooks []OrdemServicoHook

var ordemServicoBeforeUpsertMu sync.Mutex
var ordemServicoBeforeUpsertHooks []OrdemServicoHook
var ordemServicoAfterUpsertMu sync.Mutex
var ordemServicoAfterUpsertHooks []OrdemServicoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *OrdemServico) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *OrdemServico) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *OrdemServico) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *OrdemServico) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *OrdemServico) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *OrdemServico) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *OrdemServico) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *OrdemServico) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *OrdemServico) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemServicoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddOrdemServicoHook registers your hook function for all future operations.
func AddOrdemServicoHook(hookPoint boil.HookPoint, ordemServicoHook OrdemServicoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ordemServicoAfterSelectMu.Lock()
		ordemServicoAfterSelectHooks = append(ordemServicoAfterSelectHooks, ordemServicoHook)
		ordemServicoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		ordemServicoBeforeInsertMu.Lock()
		ordemServicoBeforeInsertHooks = append(ordemServicoBeforeInsertHooks, ordemServicoHook)
		ordemServicoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		ordemServicoAfterInsertMu.Lock()
		ordemServicoAfterInsertHooks = append(ordemServicoAfterInsertHooks, ordemServicoHook)
		ordemServicoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		ordemServicoBeforeUpdateMu.Lock()
		ordemServicoBeforeUpdateHooks = append(ordemServicoBeforeUpdateHooks, ordemServicoHook)
		ordemServicoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		ordemServicoAfterUpdateMu.Lock()
		ordemServicoAfterUpdateHooks = append(ordemServicoAfterUpdateHooks, ordemServicoHook)
		ordemServicoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		ordemServicoBeforeDeleteMu.Lock()
		ordemServicoBeforeDeleteHooks = append(ordemServicoBeforeDeleteHooks, ordemServicoHook)
		ordemServicoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		ordemServicoAfterDeleteMu.Lock()
		ordemServicoAfterDeleteHooks = append(ordemServicoAfterDeleteHooks, ordemServicoHook)
		ordemServicoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		ordemServicoBeforeUpsertMu.Lock()
		ordemServicoBeforeUpsertHooks = append(ordemServicoBeforeUpsertHooks, ordemServicoHook)
		ordemServicoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		ordemServicoAfterUpsertMu.Lock()
		ordemServicoAfterUpsertHooks = append(ordemServicoAfterUpsertHooks, ordemServicoHook)
		ordemServicoAfterUpsertMu.Unlock()
	}
}

// One returns a single ordemServico record from the query.
func (q ordemServicoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*OrdemServico, error) {
	o := &OrdemServico{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ordem_servico")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all OrdemServico records from the query.
func (q ordemServicoQuery) All(ctx context.Context, exec boil.ContextExecutor) (OrdemServicoSlice, error) {
	var o []*OrdemServico

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to OrdemServico slice")
	}

	if len(ordemServicoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all OrdemServico records in the query.
func (q ordemServicoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ordem_servico rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ordemServicoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ordem_servico exists")
	}

	return count > 0, nil
}

// Ent pointed to by the foreign key.
func (o *OrdemServico) Ent(mods ...qm.QueryMod) entidadeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ent_id` = ?", o.EntID),
	}

	queryMods = append(queryMods, mods...)

	return Entidades(queryMods...)
}

// Vei pointed to by the foreign key.
func (o *OrdemServico) Vei(mods ...qm.QueryMod) veiculoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`vei_id` = ?", o.VeiID),
	}

	queryMods = append(queryMods, mods...)

	return Veiculos(queryMods...)
}

// DDVDocumentoVendas retrieves all the documento_venda's DocumentoVendas with an executor via ddv_id column.
func (o *OrdemServico) DDVDocumentoVendas(mods ...qm.QueryMod) documentoVendaQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("`documento_venda_ordem_servico` on `documento_venda`.`ddv_id` = `documento_venda_ordem_servico`.`ddv_id`"),
		qm.Where("`documento_venda_ordem_servico`.`os_id`=?", o.OsID),
	)

	return DocumentoVendas(queryMods...)
}

// LoadEnt allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ordemServicoL) LoadEnt(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOrdemServico interface{}, mods queries.Applicator) error {
	var slice []*OrdemServico
	var object *OrdemServico

	if singular {
		var ok bool
		object, ok = maybeOrdemServico.(*OrdemServico)
		if !ok {
			object = new(OrdemServico)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeOrdemServico)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeOrdemServico))
			}
		}
	} else {
		s, ok := maybeOrdemServico.(*[]*OrdemServico)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeOrdemServico)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeOrdemServico))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ordemServicoR{}
		}
		args[object.EntID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ordemServicoR{}
			}

			args[obj.EntID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade`),
		qm.WhereIn(`entidade.ent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Entidade")
	}

	var resultSlice []*Entidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade")
	}

	if len(entidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ent = foreign
		if foreign.R == nil {
			foreign.R = &entidadeR{}
		}
		foreign.R.EntOrdemServicos = append(foreign.R.EntOrdemServicos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EntID == foreign.EntID {
				local.R.Ent = foreign
				if foreign.R == nil {
					foreign.R = &entidadeR{}
				}
				foreign.R.EntOrdemServicos = append(foreign.R.EntOrdemServicos, local)
				break
			}
		}
	}

	return nil
}

// LoadVei allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ordemServicoL) LoadVei(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOrdemServico interface{}, mods queries.Applicator) error {
	var slice []*OrdemServico
	var object *OrdemServico

	if singular {
		var ok bool
		object, ok = maybeOrdemServico.(*OrdemServico)
		if !ok {
			object = new(OrdemServico)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeOrdemServico)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeOrdemServico))
			}
		}
	} else {
		s, ok := maybeOrdemServico.(*[]*OrdemServico)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeOrdemServico)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeOrdemServico))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ordemServicoR{}
		}
		if !queries.IsNil(object.VeiID) {
			args[object.VeiID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ordemServicoR{}
			}

			if !queries.IsNil(obj.VeiID) {
				args[obj.VeiID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`veiculos`),
		qm.WhereIn(`veiculos.vei_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Veiculo")
	}

	var resultSlice []*Veiculo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Veiculo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for veiculos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for veiculos")
	}

	if len(veiculoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Vei = foreign
		if foreign.R == nil {
			foreign.R = &veiculoR{}
		}
		foreign.R.VeiOrdemServicos = append(foreign.R.VeiOrdemServicos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.VeiID, foreign.VeiID) {
				local.R.Vei = foreign
				if foreign.R == nil {
					foreign.R = &veiculoR{}
				}
				foreign.R.VeiOrdemServicos = append(foreign.R.VeiOrdemServicos, local)
				break
			}
		}
	}

	return nil
}

// LoadDDVDocumentoVendas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (ordemServicoL) LoadDDVDocumentoVendas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOrdemServico interface{}, mods queries.Applicator) error {
	var slice []*OrdemServico
	var object *OrdemServico

	if singular {
		var ok bool
		object, ok = maybeOrdemServico.(*OrdemServico)
		if !ok {
			object = new(OrdemServico)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeOrdemServico)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeOrdemServico))
			}
		}
	} else {
		s, ok := maybeOrdemServico.(*[]*OrdemServico)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeOrdemServico)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeOrdemServico))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ordemServicoR{}
		}
		args[object.OsID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ordemServicoR{}
			}
			args[obj.OsID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.Select("`documento_venda`.`ddv_id`, `documento_venda`.`ent_id`, `documento_venda`.`prc_id`, `documento_venda`.`flu_id`, `documento_venda`.`pai_id`, `documento_venda`.`tbp_id`, `documento_venda`.`emf_id`, `documento_venda`.`est_id`, `documento_venda`.`mun_id`, `documento_venda`.`rep_ent_id`, `documento_venda`.`codpgt_id`, `documento_venda`.`ddv_numero`, `documento_venda`.`ddv_tipodocumeto`, `documento_venda`.`ddv_tipooperacao`, `documento_venda`.`ddv_datadocumento`, `documento_venda`.`ddv_datavalidade`, `documento_venda`.`ddv_dataprevisao`, `documento_venda`.`ddv_dataexpedicao`, `documento_venda`.`ddv_tipofrete`, `documento_venda`.`ddv_numerorepresentante`, `documento_venda`.`ddv_situacao`, `documento_venda`.`ddv_localizacao`, `documento_venda`.`ddv_percentualdesconto`, `documento_venda`.`ddv_valordesconto`, `documento_venda`.`ddv_valorfrete`, `documento_venda`.`ddv_valoracessorias`, `documento_venda`.`ddv_observacoes_adm`, `documento_venda`.`ddv_obervacoes_especiais`, `documento_venda`.`ddv_totalprodutos`, `documento_venda`.`ddv_totaldescontos`, `documento_venda`.`ddv_totalpesobruto`, `documento_venda`.`ddv_totalpesoliquido`, `documento_venda`.`ddv_totalliquido`, `documento_venda`.`ddv_baseicms`, `documento_venda`.`ddv_valoricms`, `documento_venda`.`ddv_baseipi`, `documento_venda`.`ddv_valoripi`, `documento_venda`.`ddv_basepis`, `documento_venda`.`ddv_valorpis`, `documento_venda`.`ddv_basecofins`, `documento_venda`.`ddv_valorcofins`, `documento_venda`.`ddv_baseicms_st`, `documento_venda`.`ddv_valoricms_st`, `documento_venda`.`ddv_usodestinatario`, `documento_venda`.`ddv_end_logradouro`, `documento_venda`.`ddv_end_numero`, `documento_venda`.`ddv_end_compl`, `documento_venda`.`ddv_end_bairro`, `documento_venda`.`ddv_trasn_id`, `documento_venda`.`ddv_end_distancia`, `documento_venda`.`ddv_dataprevcarregamento`, `documento_venda`.`ddv_end_cep`, `documento_venda`.`usu_id`, `documento_venda`.`ddv_datahorasistema`, `documento_venda`.`ddv_pdv_id`, `documento_venda`.`ddv_valorpago`, `documento_venda`.`ddv_valortroco`, `documento_venda`.`gpe_id`, `documento_venda`.`ddv_vei_placa`, `documento_venda`.`ddv_vei_modelo`, `documento_venda`.`ddv_vei_marca`, `documento_venda`.`ddv_totalcusto`, `documento_venda`.`ddv_kmveiculo`, `documento_venda`.`ddv_pdv_cxindice`, `documento_venda`.`ddv_ent_inscricaofederal`, `documento_venda`.`ddv_ent_inscricaoestadual`, `documento_venda`.`ddv_frc_fone`, `documento_venda`.`ddv_cod_turno`, `documento_venda`.`ddv_codecf`, `documento_venda`.`ddv_coo`, `documento_venda`.`ddv_modelodoc`, `documento_venda`.`ddv_numerocupom`, `documento_venda`.`ddv_seriecupom`, `documento_venda`.`ddv_motivocancelamento`, `documento_venda`.`ddv_numeromesa`, `documento_venda`.`ddv_taxaentrega`, `documento_venda`.`ddv_nomemotorista`, `documento_venda`.`tat_id`, `documento_venda`.`ddv_total_parc_produtos`, `documento_venda`.`ddv_total_parc_servico`, `documento_venda`.`ddv_ent_razaosocial`, `documento_venda`.`ddv_observacoes_interna`, `documento_venda`.`ddv_pecas_terceiros`, `a`.`os_id`"),
		qm.From("`documento_venda`"),
		qm.InnerJoin("`documento_venda_ordem_servico` as `a` on `documento_venda`.`ddv_id` = `a`.`ddv_id`"),
		qm.WhereIn("`a`.`os_id` in ?", argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load documento_venda")
	}

	var resultSlice []*DocumentoVenda

	var localJoinCols []int
	for results.Next() {
		one := new(DocumentoVenda)
		var localJoinCol int

		err = results.Scan(&one.DDVID, &one.EntID, &one.PRCID, &one.FluID, &one.PaiID, &one.TBPID, &one.EmfID, &one.EstID, &one.MunID, &one.RepEntID, &one.CodpgtID, &one.DDVNumero, &one.DDVTipodocumeto, &one.DDVTipooperacao, &one.DDVDatadocumento, &one.DDVDatavalidade, &one.DDVDataprevisao, &one.DDVDataexpedicao, &one.DDVTipofrete, &one.DDVNumerorepresentante, &one.DDVSituacao, &one.DDVLocalizacao, &one.DDVPercentualdesconto, &one.DDVValordesconto, &one.DDVValorfrete, &one.DDVValoracessorias, &one.DDVObservacoesAdm, &one.DDVObervacoesEspeciais, &one.DDVTotalprodutos, &one.DDVTotaldescontos, &one.DDVTotalpesobruto, &one.DDVTotalpesoliquido, &one.DDVTotalliquido, &one.DDVBaseicms, &one.DDVValoricms, &one.DDVBaseipi, &one.DDVValoripi, &one.DDVBasepis, &one.DDVValorpis, &one.DDVBasecofins, &one.DDVValorcofins, &one.DDVBaseicmsST, &one.DDVValoricmsST, &one.DDVUsodestinatario, &one.DDVEndLogradouro, &one.DDVEndNumero, &one.DDVEndCompl, &one.DDVEndBairro, &one.DDVTrasnID, &one.DDVEndDistancia, &one.DDVDataprevcarregamento, &one.DDVEndCep, &one.UsuID, &one.DDVDatahorasistema, &one.DDVPDVID, &one.DDVValorpago, &one.DDVValortroco, &one.GpeID, &one.DDVVeiPlaca, &one.DDVVeiModelo, &one.DDVVeiMarca, &one.DDVTotalcusto, &one.DDVKmveiculo, &one.DDVPDVCxindice, &one.DDVEntInscricaofederal, &one.DDVEntInscricaoestadual, &one.DDVFRCFone, &one.DDVCodTurno, &one.DDVCodecf, &one.DDVCoo, &one.DDVModelodoc, &one.DDVNumerocupom, &one.DDVSeriecupom, &one.DDVMotivocancelamento, &one.DDVNumeromesa, &one.DDVTaxaentrega, &one.DDVNomemotorista, &one.TatID, &one.DDVTotalParcProdutos, &one.DDVTotalParcServico, &one.DDVEntRazaosocial, &one.DDVObservacoesInterna, &one.DDVPecasTerceiros, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for documento_venda")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice documento_venda")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on documento_venda")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documento_venda")
	}

	if len(documentoVendaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DDVDocumentoVendas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentoVendaR{}
			}
			foreign.R.OOrdemServicos = append(foreign.R.OOrdemServicos, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.OsID == localJoinCol {
				local.R.DDVDocumentoVendas = append(local.R.DDVDocumentoVendas, foreign)
				if foreign.R == nil {
					foreign.R = &documentoVendaR{}
				}
				foreign.R.OOrdemServicos = append(foreign.R.OOrdemServicos, local)
				break
			}
		}
	}

	return nil
}

// SetEnt of the ordemServico to the related item.
// Sets o.R.Ent to related.
// Adds o to related.R.EntOrdemServicos.
func (o *OrdemServico) SetEnt(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Entidade) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ordem_servico` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ent_id"}),
		strmangle.WhereClause("`", "`", 0, ordemServicoPrimaryKeyColumns),
	)
	values := []interface{}{related.EntID, o.OsID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EntID = related.EntID
	if o.R == nil {
		o.R = &ordemServicoR{
			Ent: related,
		}
	} else {
		o.R.Ent = related
	}

	if related.R == nil {
		related.R = &entidadeR{
			EntOrdemServicos: OrdemServicoSlice{o},
		}
	} else {
		related.R.EntOrdemServicos = append(related.R.EntOrdemServicos, o)
	}

	return nil
}

// SetVei of the ordemServico to the related item.
// Sets o.R.Vei to related.
// Adds o to related.R.VeiOrdemServicos.
func (o *OrdemServico) SetVei(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Veiculo) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ordem_servico` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"vei_id"}),
		strmangle.WhereClause("`", "`", 0, ordemServicoPrimaryKeyColumns),
	)
	values := []interface{}{related.VeiID, o.OsID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.VeiID, related.VeiID)
	if o.R == nil {
		o.R = &ordemServicoR{
			Vei: related,
		}
	} else {
		o.R.Vei = related
	}

	if related.R == nil {
		related.R = &veiculoR{
			VeiOrdemServicos: OrdemServicoSlice{o},
		}
	} else {
		related.R.VeiOrdemServicos = append(related.R.VeiOrdemServicos, o)
	}

	return nil
}

// RemoveVei relationship.
// Sets o.R.Vei to nil.
// Removes o from all passed in related items' relationships struct.
func (o *OrdemServico) RemoveVei(ctx context.Context, exec boil.ContextExecutor, related *Veiculo) error {
	var err error

	queries.SetScanner(&o.VeiID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("vei_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Vei = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.VeiOrdemServicos {
		if queries.Equal(o.VeiID, ri.VeiID) {
			continue
		}

		ln := len(related.R.VeiOrdemServicos)
		if ln > 1 && i < ln-1 {
			related.R.VeiOrdemServicos[i] = related.R.VeiOrdemServicos[ln-1]
		}
		related.R.VeiOrdemServicos = related.R.VeiOrdemServicos[:ln-1]
		break
	}
	return nil
}

// AddDDVDocumentoVendas adds the given related objects to the existing relationships
// of the ordem_servico, optionally inserting them as new records.
// Appends related to o.R.DDVDocumentoVendas.
// Sets related.R.OOrdemServicos appropriately.
func (o *OrdemServico) AddDDVDocumentoVendas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVenda) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into `documento_venda_ordem_servico` (`os_id`, `ddv_id`) values (?, ?)"
		values := []interface{}{o.OsID, rel.DDVID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &ordemServicoR{
			DDVDocumentoVendas: related,
		}
	} else {
		o.R.DDVDocumentoVendas = append(o.R.DDVDocumentoVendas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentoVendaR{
				OOrdemServicos: OrdemServicoSlice{o},
			}
		} else {
			rel.R.OOrdemServicos = append(rel.R.OOrdemServicos, o)
		}
	}
	return nil
}

// SetDDVDocumentoVendas removes all previously related items of the
// ordem_servico replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.OOrdemServicos's DDVDocumentoVendas accordingly.
// Replaces o.R.DDVDocumentoVendas with related.
// Sets related.R.OOrdemServicos's DDVDocumentoVendas accordingly.
func (o *OrdemServico) SetDDVDocumentoVendas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVenda) error {
	query := "delete from `documento_venda_ordem_servico` where `os_id` = ?"
	values := []interface{}{o.OsID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeDDVDocumentoVendasFromOOrdemServicosSlice(o, related)
	if o.R != nil {
		o.R.DDVDocumentoVendas = nil
	}

	return o.AddDDVDocumentoVendas(ctx, exec, insert, related...)
}

// RemoveDDVDocumentoVendas relationships from objects passed in.
// Removes related items from R.DDVDocumentoVendas (uses pointer comparison, removal does not keep order)
// Sets related.R.OOrdemServicos.
func (o *OrdemServico) RemoveDDVDocumentoVendas(ctx context.Context, exec boil.ContextExecutor, related ...*DocumentoVenda) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from `documento_venda_ordem_servico` where `os_id` = ? and `ddv_id` in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.OsID}
	for _, rel := range related {
		values = append(values, rel.DDVID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeDDVDocumentoVendasFromOOrdemServicosSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.DDVDocumentoVendas {
			if rel != ri {
				continue
			}

			ln := len(o.R.DDVDocumentoVendas)
			if ln > 1 && i < ln-1 {
				o.R.DDVDocumentoVendas[i] = o.R.DDVDocumentoVendas[ln-1]
			}
			o.R.DDVDocumentoVendas = o.R.DDVDocumentoVendas[:ln-1]
			break
		}
	}

	return nil
}

func removeDDVDocumentoVendasFromOOrdemServicosSlice(o *OrdemServico, related []*DocumentoVenda) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.OOrdemServicos {
			if o.OsID != ri.OsID {
				continue
			}

			ln := len(rel.R.OOrdemServicos)
			if ln > 1 && i < ln-1 {
				rel.R.OOrdemServicos[i] = rel.R.OOrdemServicos[ln-1]
			}
			rel.R.OOrdemServicos = rel.R.OOrdemServicos[:ln-1]
			break
		}
	}
}

// OrdemServicos retrieves all the records using an executor.
func OrdemServicos(mods ...qm.QueryMod) ordemServicoQuery {
	mods = append(mods, qm.From("`ordem_servico`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`ordem_servico`.*"})
	}

	return ordemServicoQuery{q}
}

// FindOrdemServico retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindOrdemServico(ctx context.Context, exec boil.ContextExecutor, osID int, selectCols ...string) (*OrdemServico, error) {
	ordemServicoObj := &OrdemServico{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ordem_servico` where `os_id`=?", sel,
	)

	q := queries.Raw(query, osID)

	err := q.Bind(ctx, exec, ordemServicoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ordem_servico")
	}

	if err = ordemServicoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ordemServicoObj, err
	}

	return ordemServicoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *OrdemServico) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ordem_servico provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ordemServicoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ordemServicoInsertCacheMut.RLock()
	cache, cached := ordemServicoInsertCache[key]
	ordemServicoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ordemServicoAllColumns,
			ordemServicoColumnsWithDefault,
			ordemServicoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ordemServicoType, ordemServicoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ordemServicoType, ordemServicoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ordem_servico` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ordem_servico` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ordem_servico` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ordemServicoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ordem_servico")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.OsID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ordem_servico")
	}

CacheNoHooks:
	if !cached {
		ordemServicoInsertCacheMut.Lock()
		ordemServicoInsertCache[key] = cache
		ordemServicoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the OrdemServico.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *OrdemServico) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ordemServicoUpdateCacheMut.RLock()
	cache, cached := ordemServicoUpdateCache[key]
	ordemServicoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ordemServicoAllColumns,
			ordemServicoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ordem_servico, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ordem_servico` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ordemServicoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ordemServicoType, ordemServicoMapping, append(wl, ordemServicoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ordem_servico row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ordem_servico")
	}

	if !cached {
		ordemServicoUpdateCacheMut.Lock()
		ordemServicoUpdateCache[key] = cache
		ordemServicoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ordemServicoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ordem_servico")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ordem_servico")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o OrdemServicoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ordemServicoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ordem_servico` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ordemServicoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ordemServico slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ordemServico")
	}
	return rowsAff, nil
}

var mySQLOrdemServicoUniqueColumns = []string{
	"os_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *OrdemServico) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ordem_servico provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ordemServicoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLOrdemServicoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ordemServicoUpsertCacheMut.RLock()
	cache, cached := ordemServicoUpsertCache[key]
	ordemServicoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			ordemServicoAllColumns,
			ordemServicoColumnsWithDefault,
			ordemServicoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ordemServicoAllColumns,
			ordemServicoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ordem_servico, could not build update column list")
		}

		ret := strmangle.SetComplement(ordemServicoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`ordem_servico`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ordem_servico` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ordemServicoType, ordemServicoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ordemServicoType, ordemServicoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ordem_servico")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ordemServicoType, ordemServicoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ordem_servico")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ordem_servico")
	}

CacheNoHooks:
	if !cached {
		ordemServicoUpsertCacheMut.Lock()
		ordemServicoUpsertCache[key] = cache
		ordemServicoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single OrdemServico record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *OrdemServico) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no OrdemServico provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ordemServicoPrimaryKeyMapping)
	sql := "DELETE FROM `ordem_servico` WHERE `os_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ordem_servico")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ordem_servico")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ordemServicoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ordemServicoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ordem_servico")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ordem_servico")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o OrdemServicoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ordemServicoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ordemServicoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ordem_servico` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ordemServicoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ordemServico slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ordem_servico")
	}

	if len(ordemServicoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *OrdemServico) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindOrdemServico(ctx, exec, o.OsID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *OrdemServicoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := OrdemServicoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ordemServicoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ordem_servico`.* FROM `ordem_servico` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ordemServicoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in OrdemServicoSlice")
	}

	*o = slice

	return nil
}

// OrdemServicoExists checks if the OrdemServico row exists.
func OrdemServicoExists(ctx context.Context, exec boil.ContextExecutor, osID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ordem_servico` where `os_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, osID)
	}
	row := exec.QueryRowContext(ctx, sql, osID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ordem_servico exists")
	}

	return exists, nil
}

// Exists checks if the OrdemServico row exists.
func (o *OrdemServico) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return OrdemServicoExists(ctx, exec, o.OsID)
}
