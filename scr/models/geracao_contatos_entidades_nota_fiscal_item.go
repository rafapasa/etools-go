// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// GeracaoContatosEntidadesNotaFiscalItem is an object representing the database table.
type GeracaoContatosEntidadesNotaFiscalItem struct {
	GceID               int           `boil:"gce_id" json:"gce_id" toml:"gce_id" yaml:"gce_id"`
	GcenfID             int           `boil:"gcenf_id" json:"gcenf_id" toml:"gcenf_id" yaml:"gcenf_id"`
	GcenfiItem          int           `boil:"gcenfi_item" json:"gcenfi_item" toml:"gcenfi_item" yaml:"gcenfi_item"`
	ProID               int           `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	GcenfiQuantidade    types.Decimal `boil:"gcenfi_quantidade" json:"gcenfi_quantidade" toml:"gcenfi_quantidade" yaml:"gcenfi_quantidade"`
	GcenfiValorunitario types.Decimal `boil:"gcenfi_valorunitario" json:"gcenfi_valorunitario" toml:"gcenfi_valorunitario" yaml:"gcenfi_valorunitario"`
	GcenfiValortotal    types.Decimal `boil:"gcenfi_valortotal" json:"gcenfi_valortotal" toml:"gcenfi_valortotal" yaml:"gcenfi_valortotal"`

	R *geracaoContatosEntidadesNotaFiscalItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L geracaoContatosEntidadesNotaFiscalItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var GeracaoContatosEntidadesNotaFiscalItemColumns = struct {
	GceID               string
	GcenfID             string
	GcenfiItem          string
	ProID               string
	GcenfiQuantidade    string
	GcenfiValorunitario string
	GcenfiValortotal    string
}{
	GceID:               "gce_id",
	GcenfID:             "gcenf_id",
	GcenfiItem:          "gcenfi_item",
	ProID:               "pro_id",
	GcenfiQuantidade:    "gcenfi_quantidade",
	GcenfiValorunitario: "gcenfi_valorunitario",
	GcenfiValortotal:    "gcenfi_valortotal",
}

var GeracaoContatosEntidadesNotaFiscalItemTableColumns = struct {
	GceID               string
	GcenfID             string
	GcenfiItem          string
	ProID               string
	GcenfiQuantidade    string
	GcenfiValorunitario string
	GcenfiValortotal    string
}{
	GceID:               "geracao_contatos_entidades_nota_fiscal_item.gce_id",
	GcenfID:             "geracao_contatos_entidades_nota_fiscal_item.gcenf_id",
	GcenfiItem:          "geracao_contatos_entidades_nota_fiscal_item.gcenfi_item",
	ProID:               "geracao_contatos_entidades_nota_fiscal_item.pro_id",
	GcenfiQuantidade:    "geracao_contatos_entidades_nota_fiscal_item.gcenfi_quantidade",
	GcenfiValorunitario: "geracao_contatos_entidades_nota_fiscal_item.gcenfi_valorunitario",
	GcenfiValortotal:    "geracao_contatos_entidades_nota_fiscal_item.gcenfi_valortotal",
}

// Generated where

var GeracaoContatosEntidadesNotaFiscalItemWhere = struct {
	GceID               whereHelperint
	GcenfID             whereHelperint
	GcenfiItem          whereHelperint
	ProID               whereHelperint
	GcenfiQuantidade    whereHelpertypes_Decimal
	GcenfiValorunitario whereHelpertypes_Decimal
	GcenfiValortotal    whereHelpertypes_Decimal
}{
	GceID:               whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal_item`.`gce_id`"},
	GcenfID:             whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal_item`.`gcenf_id`"},
	GcenfiItem:          whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal_item`.`gcenfi_item`"},
	ProID:               whereHelperint{field: "`geracao_contatos_entidades_nota_fiscal_item`.`pro_id`"},
	GcenfiQuantidade:    whereHelpertypes_Decimal{field: "`geracao_contatos_entidades_nota_fiscal_item`.`gcenfi_quantidade`"},
	GcenfiValorunitario: whereHelpertypes_Decimal{field: "`geracao_contatos_entidades_nota_fiscal_item`.`gcenfi_valorunitario`"},
	GcenfiValortotal:    whereHelpertypes_Decimal{field: "`geracao_contatos_entidades_nota_fiscal_item`.`gcenfi_valortotal`"},
}

// GeracaoContatosEntidadesNotaFiscalItemRels is where relationship names are stored.
var GeracaoContatosEntidadesNotaFiscalItemRels = struct {
	Pro string
}{
	Pro: "Pro",
}

// geracaoContatosEntidadesNotaFiscalItemR is where relationships are stored.
type geracaoContatosEntidadesNotaFiscalItemR struct {
	Pro *Produto `boil:"Pro" json:"Pro" toml:"Pro" yaml:"Pro"`
}

// NewStruct creates a new relationship struct
func (*geracaoContatosEntidadesNotaFiscalItemR) NewStruct() *geracaoContatosEntidadesNotaFiscalItemR {
	return &geracaoContatosEntidadesNotaFiscalItemR{}
}

func (r *geracaoContatosEntidadesNotaFiscalItemR) GetPro() *Produto {
	if r == nil {
		return nil
	}
	return r.Pro
}

// geracaoContatosEntidadesNotaFiscalItemL is where Load methods for each relationship are stored.
type geracaoContatosEntidadesNotaFiscalItemL struct{}

var (
	geracaoContatosEntidadesNotaFiscalItemAllColumns            = []string{"gce_id", "gcenf_id", "gcenfi_item", "pro_id", "gcenfi_quantidade", "gcenfi_valorunitario", "gcenfi_valortotal"}
	geracaoContatosEntidadesNotaFiscalItemColumnsWithoutDefault = []string{"gce_id", "gcenf_id", "gcenfi_item", "pro_id"}
	geracaoContatosEntidadesNotaFiscalItemColumnsWithDefault    = []string{"gcenfi_quantidade", "gcenfi_valorunitario", "gcenfi_valortotal"}
	geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns     = []string{"gce_id", "gcenf_id", "gcenfi_item"}
	geracaoContatosEntidadesNotaFiscalItemGeneratedColumns      = []string{}
)

type (
	// GeracaoContatosEntidadesNotaFiscalItemSlice is an alias for a slice of pointers to GeracaoContatosEntidadesNotaFiscalItem.
	// This should almost always be used instead of []GeracaoContatosEntidadesNotaFiscalItem.
	GeracaoContatosEntidadesNotaFiscalItemSlice []*GeracaoContatosEntidadesNotaFiscalItem
	// GeracaoContatosEntidadesNotaFiscalItemHook is the signature for custom GeracaoContatosEntidadesNotaFiscalItem hook methods
	GeracaoContatosEntidadesNotaFiscalItemHook func(context.Context, boil.ContextExecutor, *GeracaoContatosEntidadesNotaFiscalItem) error

	geracaoContatosEntidadesNotaFiscalItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	geracaoContatosEntidadesNotaFiscalItemType                 = reflect.TypeOf(&GeracaoContatosEntidadesNotaFiscalItem{})
	geracaoContatosEntidadesNotaFiscalItemMapping              = queries.MakeStructMapping(geracaoContatosEntidadesNotaFiscalItemType)
	geracaoContatosEntidadesNotaFiscalItemPrimaryKeyMapping, _ = queries.BindMapping(geracaoContatosEntidadesNotaFiscalItemType, geracaoContatosEntidadesNotaFiscalItemMapping, geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns)
	geracaoContatosEntidadesNotaFiscalItemInsertCacheMut       sync.RWMutex
	geracaoContatosEntidadesNotaFiscalItemInsertCache          = make(map[string]insertCache)
	geracaoContatosEntidadesNotaFiscalItemUpdateCacheMut       sync.RWMutex
	geracaoContatosEntidadesNotaFiscalItemUpdateCache          = make(map[string]updateCache)
	geracaoContatosEntidadesNotaFiscalItemUpsertCacheMut       sync.RWMutex
	geracaoContatosEntidadesNotaFiscalItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var geracaoContatosEntidadesNotaFiscalItemAfterSelectMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemAfterSelectHooks []GeracaoContatosEntidadesNotaFiscalItemHook

var geracaoContatosEntidadesNotaFiscalItemBeforeInsertMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemBeforeInsertHooks []GeracaoContatosEntidadesNotaFiscalItemHook
var geracaoContatosEntidadesNotaFiscalItemAfterInsertMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemAfterInsertHooks []GeracaoContatosEntidadesNotaFiscalItemHook

var geracaoContatosEntidadesNotaFiscalItemBeforeUpdateMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemBeforeUpdateHooks []GeracaoContatosEntidadesNotaFiscalItemHook
var geracaoContatosEntidadesNotaFiscalItemAfterUpdateMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemAfterUpdateHooks []GeracaoContatosEntidadesNotaFiscalItemHook

var geracaoContatosEntidadesNotaFiscalItemBeforeDeleteMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemBeforeDeleteHooks []GeracaoContatosEntidadesNotaFiscalItemHook
var geracaoContatosEntidadesNotaFiscalItemAfterDeleteMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemAfterDeleteHooks []GeracaoContatosEntidadesNotaFiscalItemHook

var geracaoContatosEntidadesNotaFiscalItemBeforeUpsertMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemBeforeUpsertHooks []GeracaoContatosEntidadesNotaFiscalItemHook
var geracaoContatosEntidadesNotaFiscalItemAfterUpsertMu sync.Mutex
var geracaoContatosEntidadesNotaFiscalItemAfterUpsertHooks []GeracaoContatosEntidadesNotaFiscalItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *GeracaoContatosEntidadesNotaFiscalItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadesNotaFiscalItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddGeracaoContatosEntidadesNotaFiscalItemHook registers your hook function for all future operations.
func AddGeracaoContatosEntidadesNotaFiscalItemHook(hookPoint boil.HookPoint, geracaoContatosEntidadesNotaFiscalItemHook GeracaoContatosEntidadesNotaFiscalItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		geracaoContatosEntidadesNotaFiscalItemAfterSelectMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemAfterSelectHooks = append(geracaoContatosEntidadesNotaFiscalItemAfterSelectHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		geracaoContatosEntidadesNotaFiscalItemBeforeInsertMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemBeforeInsertHooks = append(geracaoContatosEntidadesNotaFiscalItemBeforeInsertHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		geracaoContatosEntidadesNotaFiscalItemAfterInsertMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemAfterInsertHooks = append(geracaoContatosEntidadesNotaFiscalItemAfterInsertHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		geracaoContatosEntidadesNotaFiscalItemBeforeUpdateMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemBeforeUpdateHooks = append(geracaoContatosEntidadesNotaFiscalItemBeforeUpdateHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		geracaoContatosEntidadesNotaFiscalItemAfterUpdateMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemAfterUpdateHooks = append(geracaoContatosEntidadesNotaFiscalItemAfterUpdateHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		geracaoContatosEntidadesNotaFiscalItemBeforeDeleteMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemBeforeDeleteHooks = append(geracaoContatosEntidadesNotaFiscalItemBeforeDeleteHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		geracaoContatosEntidadesNotaFiscalItemAfterDeleteMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemAfterDeleteHooks = append(geracaoContatosEntidadesNotaFiscalItemAfterDeleteHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		geracaoContatosEntidadesNotaFiscalItemBeforeUpsertMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemBeforeUpsertHooks = append(geracaoContatosEntidadesNotaFiscalItemBeforeUpsertHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		geracaoContatosEntidadesNotaFiscalItemAfterUpsertMu.Lock()
		geracaoContatosEntidadesNotaFiscalItemAfterUpsertHooks = append(geracaoContatosEntidadesNotaFiscalItemAfterUpsertHooks, geracaoContatosEntidadesNotaFiscalItemHook)
		geracaoContatosEntidadesNotaFiscalItemAfterUpsertMu.Unlock()
	}
}

// One returns a single geracaoContatosEntidadesNotaFiscalItem record from the query.
func (q geracaoContatosEntidadesNotaFiscalItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*GeracaoContatosEntidadesNotaFiscalItem, error) {
	o := &GeracaoContatosEntidadesNotaFiscalItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for geracao_contatos_entidades_nota_fiscal_item")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all GeracaoContatosEntidadesNotaFiscalItem records from the query.
func (q geracaoContatosEntidadesNotaFiscalItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (GeracaoContatosEntidadesNotaFiscalItemSlice, error) {
	var o []*GeracaoContatosEntidadesNotaFiscalItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to GeracaoContatosEntidadesNotaFiscalItem slice")
	}

	if len(geracaoContatosEntidadesNotaFiscalItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all GeracaoContatosEntidadesNotaFiscalItem records in the query.
func (q geracaoContatosEntidadesNotaFiscalItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count geracao_contatos_entidades_nota_fiscal_item rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q geracaoContatosEntidadesNotaFiscalItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if geracao_contatos_entidades_nota_fiscal_item exists")
	}

	return count > 0, nil
}

// Pro pointed to by the foreign key.
func (o *GeracaoContatosEntidadesNotaFiscalItem) Pro(mods ...qm.QueryMod) produtoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pro_id` = ?", o.ProID),
	}

	queryMods = append(queryMods, mods...)

	return Produtos(queryMods...)
}

// LoadPro allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (geracaoContatosEntidadesNotaFiscalItemL) LoadPro(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeracaoContatosEntidadesNotaFiscalItem interface{}, mods queries.Applicator) error {
	var slice []*GeracaoContatosEntidadesNotaFiscalItem
	var object *GeracaoContatosEntidadesNotaFiscalItem

	if singular {
		var ok bool
		object, ok = maybeGeracaoContatosEntidadesNotaFiscalItem.(*GeracaoContatosEntidadesNotaFiscalItem)
		if !ok {
			object = new(GeracaoContatosEntidadesNotaFiscalItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeracaoContatosEntidadesNotaFiscalItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeracaoContatosEntidadesNotaFiscalItem))
			}
		}
	} else {
		s, ok := maybeGeracaoContatosEntidadesNotaFiscalItem.(*[]*GeracaoContatosEntidadesNotaFiscalItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeracaoContatosEntidadesNotaFiscalItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeracaoContatosEntidadesNotaFiscalItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &geracaoContatosEntidadesNotaFiscalItemR{}
		}
		args[object.ProID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geracaoContatosEntidadesNotaFiscalItemR{}
			}

			args[obj.ProID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.pro_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pro = foreign
		if foreign.R == nil {
			foreign.R = &produtoR{}
		}
		foreign.R.ProGeracaoContatosEntidadesNotaFiscalItems = append(foreign.R.ProGeracaoContatosEntidadesNotaFiscalItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProID == foreign.ProID {
				local.R.Pro = foreign
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.ProGeracaoContatosEntidadesNotaFiscalItems = append(foreign.R.ProGeracaoContatosEntidadesNotaFiscalItems, local)
				break
			}
		}
	}

	return nil
}

// SetPro of the geracaoContatosEntidadesNotaFiscalItem to the related item.
// Sets o.R.Pro to related.
// Adds o to related.R.ProGeracaoContatosEntidadesNotaFiscalItems.
func (o *GeracaoContatosEntidadesNotaFiscalItem) SetPro(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Produto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `geracao_contatos_entidades_nota_fiscal_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pro_id"}),
		strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ProID, o.GceID, o.GcenfID, o.GcenfiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProID = related.ProID
	if o.R == nil {
		o.R = &geracaoContatosEntidadesNotaFiscalItemR{
			Pro: related,
		}
	} else {
		o.R.Pro = related
	}

	if related.R == nil {
		related.R = &produtoR{
			ProGeracaoContatosEntidadesNotaFiscalItems: GeracaoContatosEntidadesNotaFiscalItemSlice{o},
		}
	} else {
		related.R.ProGeracaoContatosEntidadesNotaFiscalItems = append(related.R.ProGeracaoContatosEntidadesNotaFiscalItems, o)
	}

	return nil
}

// GeracaoContatosEntidadesNotaFiscalItems retrieves all the records using an executor.
func GeracaoContatosEntidadesNotaFiscalItems(mods ...qm.QueryMod) geracaoContatosEntidadesNotaFiscalItemQuery {
	mods = append(mods, qm.From("`geracao_contatos_entidades_nota_fiscal_item`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`geracao_contatos_entidades_nota_fiscal_item`.*"})
	}

	return geracaoContatosEntidadesNotaFiscalItemQuery{q}
}

// FindGeracaoContatosEntidadesNotaFiscalItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindGeracaoContatosEntidadesNotaFiscalItem(ctx context.Context, exec boil.ContextExecutor, gceID int, gcenfID int, gcenfiItem int, selectCols ...string) (*GeracaoContatosEntidadesNotaFiscalItem, error) {
	geracaoContatosEntidadesNotaFiscalItemObj := &GeracaoContatosEntidadesNotaFiscalItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `geracao_contatos_entidades_nota_fiscal_item` where `gce_id`=? AND `gcenf_id`=? AND `gcenfi_item`=?", sel,
	)

	q := queries.Raw(query, gceID, gcenfID, gcenfiItem)

	err := q.Bind(ctx, exec, geracaoContatosEntidadesNotaFiscalItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from geracao_contatos_entidades_nota_fiscal_item")
	}

	if err = geracaoContatosEntidadesNotaFiscalItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return geracaoContatosEntidadesNotaFiscalItemObj, err
	}

	return geracaoContatosEntidadesNotaFiscalItemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *GeracaoContatosEntidadesNotaFiscalItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no geracao_contatos_entidades_nota_fiscal_item provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(geracaoContatosEntidadesNotaFiscalItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	geracaoContatosEntidadesNotaFiscalItemInsertCacheMut.RLock()
	cache, cached := geracaoContatosEntidadesNotaFiscalItemInsertCache[key]
	geracaoContatosEntidadesNotaFiscalItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			geracaoContatosEntidadesNotaFiscalItemAllColumns,
			geracaoContatosEntidadesNotaFiscalItemColumnsWithDefault,
			geracaoContatosEntidadesNotaFiscalItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalItemType, geracaoContatosEntidadesNotaFiscalItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalItemType, geracaoContatosEntidadesNotaFiscalItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `geracao_contatos_entidades_nota_fiscal_item` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `geracao_contatos_entidades_nota_fiscal_item` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `geracao_contatos_entidades_nota_fiscal_item` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into geracao_contatos_entidades_nota_fiscal_item")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.GceID,
		o.GcenfID,
		o.GcenfiItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for geracao_contatos_entidades_nota_fiscal_item")
	}

CacheNoHooks:
	if !cached {
		geracaoContatosEntidadesNotaFiscalItemInsertCacheMut.Lock()
		geracaoContatosEntidadesNotaFiscalItemInsertCache[key] = cache
		geracaoContatosEntidadesNotaFiscalItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the GeracaoContatosEntidadesNotaFiscalItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *GeracaoContatosEntidadesNotaFiscalItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	geracaoContatosEntidadesNotaFiscalItemUpdateCacheMut.RLock()
	cache, cached := geracaoContatosEntidadesNotaFiscalItemUpdateCache[key]
	geracaoContatosEntidadesNotaFiscalItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			geracaoContatosEntidadesNotaFiscalItemAllColumns,
			geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update geracao_contatos_entidades_nota_fiscal_item, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `geracao_contatos_entidades_nota_fiscal_item` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalItemType, geracaoContatosEntidadesNotaFiscalItemMapping, append(wl, geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update geracao_contatos_entidades_nota_fiscal_item row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for geracao_contatos_entidades_nota_fiscal_item")
	}

	if !cached {
		geracaoContatosEntidadesNotaFiscalItemUpdateCacheMut.Lock()
		geracaoContatosEntidadesNotaFiscalItemUpdateCache[key] = cache
		geracaoContatosEntidadesNotaFiscalItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q geracaoContatosEntidadesNotaFiscalItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for geracao_contatos_entidades_nota_fiscal_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for geracao_contatos_entidades_nota_fiscal_item")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o GeracaoContatosEntidadesNotaFiscalItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadesNotaFiscalItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `geracao_contatos_entidades_nota_fiscal_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in geracaoContatosEntidadesNotaFiscalItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all geracaoContatosEntidadesNotaFiscalItem")
	}
	return rowsAff, nil
}

var mySQLGeracaoContatosEntidadesNotaFiscalItemUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *GeracaoContatosEntidadesNotaFiscalItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no geracao_contatos_entidades_nota_fiscal_item provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(geracaoContatosEntidadesNotaFiscalItemColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLGeracaoContatosEntidadesNotaFiscalItemUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	geracaoContatosEntidadesNotaFiscalItemUpsertCacheMut.RLock()
	cache, cached := geracaoContatosEntidadesNotaFiscalItemUpsertCache[key]
	geracaoContatosEntidadesNotaFiscalItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			geracaoContatosEntidadesNotaFiscalItemAllColumns,
			geracaoContatosEntidadesNotaFiscalItemColumnsWithDefault,
			geracaoContatosEntidadesNotaFiscalItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			geracaoContatosEntidadesNotaFiscalItemAllColumns,
			geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert geracao_contatos_entidades_nota_fiscal_item, could not build update column list")
		}

		ret := strmangle.SetComplement(geracaoContatosEntidadesNotaFiscalItemAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`geracao_contatos_entidades_nota_fiscal_item`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `geracao_contatos_entidades_nota_fiscal_item` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalItemType, geracaoContatosEntidadesNotaFiscalItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalItemType, geracaoContatosEntidadesNotaFiscalItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for geracao_contatos_entidades_nota_fiscal_item")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(geracaoContatosEntidadesNotaFiscalItemType, geracaoContatosEntidadesNotaFiscalItemMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for geracao_contatos_entidades_nota_fiscal_item")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for geracao_contatos_entidades_nota_fiscal_item")
	}

CacheNoHooks:
	if !cached {
		geracaoContatosEntidadesNotaFiscalItemUpsertCacheMut.Lock()
		geracaoContatosEntidadesNotaFiscalItemUpsertCache[key] = cache
		geracaoContatosEntidadesNotaFiscalItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single GeracaoContatosEntidadesNotaFiscalItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *GeracaoContatosEntidadesNotaFiscalItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no GeracaoContatosEntidadesNotaFiscalItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), geracaoContatosEntidadesNotaFiscalItemPrimaryKeyMapping)
	sql := "DELETE FROM `geracao_contatos_entidades_nota_fiscal_item` WHERE `gce_id`=? AND `gcenf_id`=? AND `gcenfi_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from geracao_contatos_entidades_nota_fiscal_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for geracao_contatos_entidades_nota_fiscal_item")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q geracaoContatosEntidadesNotaFiscalItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no geracaoContatosEntidadesNotaFiscalItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from geracao_contatos_entidades_nota_fiscal_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for geracao_contatos_entidades_nota_fiscal_item")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o GeracaoContatosEntidadesNotaFiscalItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(geracaoContatosEntidadesNotaFiscalItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadesNotaFiscalItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `geracao_contatos_entidades_nota_fiscal_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from geracaoContatosEntidadesNotaFiscalItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for geracao_contatos_entidades_nota_fiscal_item")
	}

	if len(geracaoContatosEntidadesNotaFiscalItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *GeracaoContatosEntidadesNotaFiscalItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindGeracaoContatosEntidadesNotaFiscalItem(ctx, exec, o.GceID, o.GcenfID, o.GcenfiItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GeracaoContatosEntidadesNotaFiscalItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := GeracaoContatosEntidadesNotaFiscalItemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadesNotaFiscalItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `geracao_contatos_entidades_nota_fiscal_item`.* FROM `geracao_contatos_entidades_nota_fiscal_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadesNotaFiscalItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in GeracaoContatosEntidadesNotaFiscalItemSlice")
	}

	*o = slice

	return nil
}

// GeracaoContatosEntidadesNotaFiscalItemExists checks if the GeracaoContatosEntidadesNotaFiscalItem row exists.
func GeracaoContatosEntidadesNotaFiscalItemExists(ctx context.Context, exec boil.ContextExecutor, gceID int, gcenfID int, gcenfiItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `geracao_contatos_entidades_nota_fiscal_item` where `gce_id`=? AND `gcenf_id`=? AND `gcenfi_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, gceID, gcenfID, gcenfiItem)
	}
	row := exec.QueryRowContext(ctx, sql, gceID, gcenfID, gcenfiItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if geracao_contatos_entidades_nota_fiscal_item exists")
	}

	return exists, nil
}

// Exists checks if the GeracaoContatosEntidadesNotaFiscalItem row exists.
func (o *GeracaoContatosEntidadesNotaFiscalItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return GeracaoContatosEntidadesNotaFiscalItemExists(ctx, exec, o.GceID, o.GcenfID, o.GcenfiItem)
}
