// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UsuarioFilial is an object representing the database table.
type UsuarioFilial struct {
	UfiID int `boil:"ufi_id" json:"ufi_id" toml:"ufi_id" yaml:"ufi_id"`
	UsuID int `boil:"usu_id" json:"usu_id" toml:"usu_id" yaml:"usu_id"`
	EmfID int `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`

	R *usuarioFilialR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L usuarioFilialL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UsuarioFilialColumns = struct {
	UfiID string
	UsuID string
	EmfID string
}{
	UfiID: "ufi_id",
	UsuID: "usu_id",
	EmfID: "emf_id",
}

var UsuarioFilialTableColumns = struct {
	UfiID string
	UsuID string
	EmfID string
}{
	UfiID: "usuario_filial.ufi_id",
	UsuID: "usuario_filial.usu_id",
	EmfID: "usuario_filial.emf_id",
}

// Generated where

var UsuarioFilialWhere = struct {
	UfiID whereHelperint
	UsuID whereHelperint
	EmfID whereHelperint
}{
	UfiID: whereHelperint{field: "`usuario_filial`.`ufi_id`"},
	UsuID: whereHelperint{field: "`usuario_filial`.`usu_id`"},
	EmfID: whereHelperint{field: "`usuario_filial`.`emf_id`"},
}

// UsuarioFilialRels is where relationship names are stored.
var UsuarioFilialRels = struct {
	Emf                        string
	Usu                        string
	PracPermissaoRecursoAcaos  string
	PRCPPermissaoRecursoCampos string
	PrePermissaoRecursos       string
}{
	Emf:                        "Emf",
	Usu:                        "Usu",
	PracPermissaoRecursoAcaos:  "PracPermissaoRecursoAcaos",
	PRCPPermissaoRecursoCampos: "PRCPPermissaoRecursoCampos",
	PrePermissaoRecursos:       "PrePermissaoRecursos",
}

// usuarioFilialR is where relationships are stored.
type usuarioFilialR struct {
	Emf                        *EmpresaFilial             `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	Usu                        *Usuario                   `boil:"Usu" json:"Usu" toml:"Usu" yaml:"Usu"`
	PracPermissaoRecursoAcaos  PermissaoRecursoAcaoSlice  `boil:"PracPermissaoRecursoAcaos" json:"PracPermissaoRecursoAcaos" toml:"PracPermissaoRecursoAcaos" yaml:"PracPermissaoRecursoAcaos"`
	PRCPPermissaoRecursoCampos PermissaoRecursoCampoSlice `boil:"PRCPPermissaoRecursoCampos" json:"PRCPPermissaoRecursoCampos" toml:"PRCPPermissaoRecursoCampos" yaml:"PRCPPermissaoRecursoCampos"`
	PrePermissaoRecursos       PermissaoRecursoSlice      `boil:"PrePermissaoRecursos" json:"PrePermissaoRecursos" toml:"PrePermissaoRecursos" yaml:"PrePermissaoRecursos"`
}

// NewStruct creates a new relationship struct
func (*usuarioFilialR) NewStruct() *usuarioFilialR {
	return &usuarioFilialR{}
}

func (r *usuarioFilialR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *usuarioFilialR) GetUsu() *Usuario {
	if r == nil {
		return nil
	}
	return r.Usu
}

func (r *usuarioFilialR) GetPracPermissaoRecursoAcaos() PermissaoRecursoAcaoSlice {
	if r == nil {
		return nil
	}
	return r.PracPermissaoRecursoAcaos
}

func (r *usuarioFilialR) GetPRCPPermissaoRecursoCampos() PermissaoRecursoCampoSlice {
	if r == nil {
		return nil
	}
	return r.PRCPPermissaoRecursoCampos
}

func (r *usuarioFilialR) GetPrePermissaoRecursos() PermissaoRecursoSlice {
	if r == nil {
		return nil
	}
	return r.PrePermissaoRecursos
}

// usuarioFilialL is where Load methods for each relationship are stored.
type usuarioFilialL struct{}

var (
	usuarioFilialAllColumns            = []string{"ufi_id", "usu_id", "emf_id"}
	usuarioFilialColumnsWithoutDefault = []string{"ufi_id", "usu_id", "emf_id"}
	usuarioFilialColumnsWithDefault    = []string{}
	usuarioFilialPrimaryKeyColumns     = []string{"ufi_id"}
	usuarioFilialGeneratedColumns      = []string{}
)

type (
	// UsuarioFilialSlice is an alias for a slice of pointers to UsuarioFilial.
	// This should almost always be used instead of []UsuarioFilial.
	UsuarioFilialSlice []*UsuarioFilial
	// UsuarioFilialHook is the signature for custom UsuarioFilial hook methods
	UsuarioFilialHook func(context.Context, boil.ContextExecutor, *UsuarioFilial) error

	usuarioFilialQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	usuarioFilialType                 = reflect.TypeOf(&UsuarioFilial{})
	usuarioFilialMapping              = queries.MakeStructMapping(usuarioFilialType)
	usuarioFilialPrimaryKeyMapping, _ = queries.BindMapping(usuarioFilialType, usuarioFilialMapping, usuarioFilialPrimaryKeyColumns)
	usuarioFilialInsertCacheMut       sync.RWMutex
	usuarioFilialInsertCache          = make(map[string]insertCache)
	usuarioFilialUpdateCacheMut       sync.RWMutex
	usuarioFilialUpdateCache          = make(map[string]updateCache)
	usuarioFilialUpsertCacheMut       sync.RWMutex
	usuarioFilialUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var usuarioFilialAfterSelectMu sync.Mutex
var usuarioFilialAfterSelectHooks []UsuarioFilialHook

var usuarioFilialBeforeInsertMu sync.Mutex
var usuarioFilialBeforeInsertHooks []UsuarioFilialHook
var usuarioFilialAfterInsertMu sync.Mutex
var usuarioFilialAfterInsertHooks []UsuarioFilialHook

var usuarioFilialBeforeUpdateMu sync.Mutex
var usuarioFilialBeforeUpdateHooks []UsuarioFilialHook
var usuarioFilialAfterUpdateMu sync.Mutex
var usuarioFilialAfterUpdateHooks []UsuarioFilialHook

var usuarioFilialBeforeDeleteMu sync.Mutex
var usuarioFilialBeforeDeleteHooks []UsuarioFilialHook
var usuarioFilialAfterDeleteMu sync.Mutex
var usuarioFilialAfterDeleteHooks []UsuarioFilialHook

var usuarioFilialBeforeUpsertMu sync.Mutex
var usuarioFilialBeforeUpsertHooks []UsuarioFilialHook
var usuarioFilialAfterUpsertMu sync.Mutex
var usuarioFilialAfterUpsertHooks []UsuarioFilialHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UsuarioFilial) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UsuarioFilial) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UsuarioFilial) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UsuarioFilial) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UsuarioFilial) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UsuarioFilial) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UsuarioFilial) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UsuarioFilial) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UsuarioFilial) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range usuarioFilialAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUsuarioFilialHook registers your hook function for all future operations.
func AddUsuarioFilialHook(hookPoint boil.HookPoint, usuarioFilialHook UsuarioFilialHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		usuarioFilialAfterSelectMu.Lock()
		usuarioFilialAfterSelectHooks = append(usuarioFilialAfterSelectHooks, usuarioFilialHook)
		usuarioFilialAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		usuarioFilialBeforeInsertMu.Lock()
		usuarioFilialBeforeInsertHooks = append(usuarioFilialBeforeInsertHooks, usuarioFilialHook)
		usuarioFilialBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		usuarioFilialAfterInsertMu.Lock()
		usuarioFilialAfterInsertHooks = append(usuarioFilialAfterInsertHooks, usuarioFilialHook)
		usuarioFilialAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		usuarioFilialBeforeUpdateMu.Lock()
		usuarioFilialBeforeUpdateHooks = append(usuarioFilialBeforeUpdateHooks, usuarioFilialHook)
		usuarioFilialBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		usuarioFilialAfterUpdateMu.Lock()
		usuarioFilialAfterUpdateHooks = append(usuarioFilialAfterUpdateHooks, usuarioFilialHook)
		usuarioFilialAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		usuarioFilialBeforeDeleteMu.Lock()
		usuarioFilialBeforeDeleteHooks = append(usuarioFilialBeforeDeleteHooks, usuarioFilialHook)
		usuarioFilialBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		usuarioFilialAfterDeleteMu.Lock()
		usuarioFilialAfterDeleteHooks = append(usuarioFilialAfterDeleteHooks, usuarioFilialHook)
		usuarioFilialAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		usuarioFilialBeforeUpsertMu.Lock()
		usuarioFilialBeforeUpsertHooks = append(usuarioFilialBeforeUpsertHooks, usuarioFilialHook)
		usuarioFilialBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		usuarioFilialAfterUpsertMu.Lock()
		usuarioFilialAfterUpsertHooks = append(usuarioFilialAfterUpsertHooks, usuarioFilialHook)
		usuarioFilialAfterUpsertMu.Unlock()
	}
}

// One returns a single usuarioFilial record from the query.
func (q usuarioFilialQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UsuarioFilial, error) {
	o := &UsuarioFilial{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for usuario_filial")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UsuarioFilial records from the query.
func (q usuarioFilialQuery) All(ctx context.Context, exec boil.ContextExecutor) (UsuarioFilialSlice, error) {
	var o []*UsuarioFilial

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to UsuarioFilial slice")
	}

	if len(usuarioFilialAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UsuarioFilial records in the query.
func (q usuarioFilialQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count usuario_filial rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q usuarioFilialQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if usuario_filial exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *UsuarioFilial) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// Usu pointed to by the foreign key.
func (o *UsuarioFilial) Usu(mods ...qm.QueryMod) usuarioQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`usu_id` = ?", o.UsuID),
	}

	queryMods = append(queryMods, mods...)

	return Usuarios(queryMods...)
}

// PracPermissaoRecursoAcaos retrieves all the permissao_recurso_acao's PermissaoRecursoAcaos with an executor via prac_id column.
func (o *UsuarioFilial) PracPermissaoRecursoAcaos(mods ...qm.QueryMod) permissaoRecursoAcaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("`usuario_filial_rec_acao` on `permissao_recurso_acao`.`prac_id` = `usuario_filial_rec_acao`.`prac_id`"),
		qm.Where("`usuario_filial_rec_acao`.`ufi_id`=?", o.UfiID),
	)

	return PermissaoRecursoAcaos(queryMods...)
}

// PRCPPermissaoRecursoCampos retrieves all the permissao_recurso_campo's PermissaoRecursoCampos with an executor via prcp_id column.
func (o *UsuarioFilial) PRCPPermissaoRecursoCampos(mods ...qm.QueryMod) permissaoRecursoCampoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("`usuario_filial_rec_campos` on `permissao_recurso_campos`.`prcp_id` = `usuario_filial_rec_campos`.`prcp_id`"),
		qm.Where("`usuario_filial_rec_campos`.`ufi_id`=?", o.UfiID),
	)

	return PermissaoRecursoCampos(queryMods...)
}

// PrePermissaoRecursos retrieves all the permissao_recurso's PermissaoRecursos with an executor via pre_id column.
func (o *UsuarioFilial) PrePermissaoRecursos(mods ...qm.QueryMod) permissaoRecursoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("`usuario_filial_recurso` on `permissao_recurso`.`pre_id` = `usuario_filial_recurso`.`pre_id`"),
		qm.Where("`usuario_filial_recurso`.`ufi_id`=?", o.UfiID),
	)

	return PermissaoRecursos(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (usuarioFilialL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUsuarioFilial interface{}, mods queries.Applicator) error {
	var slice []*UsuarioFilial
	var object *UsuarioFilial

	if singular {
		var ok bool
		object, ok = maybeUsuarioFilial.(*UsuarioFilial)
		if !ok {
			object = new(UsuarioFilial)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUsuarioFilial))
			}
		}
	} else {
		s, ok := maybeUsuarioFilial.(*[]*UsuarioFilial)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUsuarioFilial))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &usuarioFilialR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &usuarioFilialR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfUsuarioFilials = append(foreign.R.EmfUsuarioFilials, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfUsuarioFilials = append(foreign.R.EmfUsuarioFilials, local)
				break
			}
		}
	}

	return nil
}

// LoadUsu allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (usuarioFilialL) LoadUsu(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUsuarioFilial interface{}, mods queries.Applicator) error {
	var slice []*UsuarioFilial
	var object *UsuarioFilial

	if singular {
		var ok bool
		object, ok = maybeUsuarioFilial.(*UsuarioFilial)
		if !ok {
			object = new(UsuarioFilial)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUsuarioFilial))
			}
		}
	} else {
		s, ok := maybeUsuarioFilial.(*[]*UsuarioFilial)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUsuarioFilial))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &usuarioFilialR{}
		}
		args[object.UsuID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &usuarioFilialR{}
			}

			args[obj.UsuID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`usuario`),
		qm.WhereIn(`usuario.usu_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Usuario")
	}

	var resultSlice []*Usuario
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Usuario")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for usuario")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for usuario")
	}

	if len(usuarioAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Usu = foreign
		if foreign.R == nil {
			foreign.R = &usuarioR{}
		}
		foreign.R.UsuUsuarioFilials = append(foreign.R.UsuUsuarioFilials, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UsuID == foreign.UsuID {
				local.R.Usu = foreign
				if foreign.R == nil {
					foreign.R = &usuarioR{}
				}
				foreign.R.UsuUsuarioFilials = append(foreign.R.UsuUsuarioFilials, local)
				break
			}
		}
	}

	return nil
}

// LoadPracPermissaoRecursoAcaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (usuarioFilialL) LoadPracPermissaoRecursoAcaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUsuarioFilial interface{}, mods queries.Applicator) error {
	var slice []*UsuarioFilial
	var object *UsuarioFilial

	if singular {
		var ok bool
		object, ok = maybeUsuarioFilial.(*UsuarioFilial)
		if !ok {
			object = new(UsuarioFilial)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUsuarioFilial))
			}
		}
	} else {
		s, ok := maybeUsuarioFilial.(*[]*UsuarioFilial)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUsuarioFilial))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &usuarioFilialR{}
		}
		args[object.UfiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &usuarioFilialR{}
			}
			args[obj.UfiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.Select("`permissao_recurso_acao`.`prac_id`, `permissao_recurso_acao`.`pre_id`, `permissao_recurso_acao`.`prac_nome`, `permissao_recurso_acao`.`prac_descricao`, `a`.`ufi_id`"),
		qm.From("`permissao_recurso_acao`"),
		qm.InnerJoin("`usuario_filial_rec_acao` as `a` on `permissao_recurso_acao`.`prac_id` = `a`.`prac_id`"),
		qm.WhereIn("`a`.`ufi_id` in ?", argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load permissao_recurso_acao")
	}

	var resultSlice []*PermissaoRecursoAcao

	var localJoinCols []int
	for results.Next() {
		one := new(PermissaoRecursoAcao)
		var localJoinCol int

		err = results.Scan(&one.PracID, &one.PreID, &one.PracNome, &one.PracDescricao, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for permissao_recurso_acao")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice permissao_recurso_acao")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on permissao_recurso_acao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permissao_recurso_acao")
	}

	if len(permissaoRecursoAcaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PracPermissaoRecursoAcaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &permissaoRecursoAcaoR{}
			}
			foreign.R.UfiUsuarioFilials = append(foreign.R.UfiUsuarioFilials, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.UfiID == localJoinCol {
				local.R.PracPermissaoRecursoAcaos = append(local.R.PracPermissaoRecursoAcaos, foreign)
				if foreign.R == nil {
					foreign.R = &permissaoRecursoAcaoR{}
				}
				foreign.R.UfiUsuarioFilials = append(foreign.R.UfiUsuarioFilials, local)
				break
			}
		}
	}

	return nil
}

// LoadPRCPPermissaoRecursoCampos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (usuarioFilialL) LoadPRCPPermissaoRecursoCampos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUsuarioFilial interface{}, mods queries.Applicator) error {
	var slice []*UsuarioFilial
	var object *UsuarioFilial

	if singular {
		var ok bool
		object, ok = maybeUsuarioFilial.(*UsuarioFilial)
		if !ok {
			object = new(UsuarioFilial)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUsuarioFilial))
			}
		}
	} else {
		s, ok := maybeUsuarioFilial.(*[]*UsuarioFilial)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUsuarioFilial))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &usuarioFilialR{}
		}
		args[object.UfiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &usuarioFilialR{}
			}
			args[obj.UfiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.Select("`permissao_recurso_campos`.`prcp_id`, `permissao_recurso_campos`.`pre_id`, `permissao_recurso_campos`.`prcp_nome`, `permissao_recurso_campos`.`prcp_descricao`, `a`.`ufi_id`"),
		qm.From("`permissao_recurso_campos`"),
		qm.InnerJoin("`usuario_filial_rec_campos` as `a` on `permissao_recurso_campos`.`prcp_id` = `a`.`prcp_id`"),
		qm.WhereIn("`a`.`ufi_id` in ?", argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load permissao_recurso_campos")
	}

	var resultSlice []*PermissaoRecursoCampo

	var localJoinCols []int
	for results.Next() {
		one := new(PermissaoRecursoCampo)
		var localJoinCol int

		err = results.Scan(&one.PRCPID, &one.PreID, &one.PRCPNome, &one.PRCPDescricao, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for permissao_recurso_campos")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice permissao_recurso_campos")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on permissao_recurso_campos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permissao_recurso_campos")
	}

	if len(permissaoRecursoCampoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PRCPPermissaoRecursoCampos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &permissaoRecursoCampoR{}
			}
			foreign.R.UfiUsuarioFilials = append(foreign.R.UfiUsuarioFilials, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.UfiID == localJoinCol {
				local.R.PRCPPermissaoRecursoCampos = append(local.R.PRCPPermissaoRecursoCampos, foreign)
				if foreign.R == nil {
					foreign.R = &permissaoRecursoCampoR{}
				}
				foreign.R.UfiUsuarioFilials = append(foreign.R.UfiUsuarioFilials, local)
				break
			}
		}
	}

	return nil
}

// LoadPrePermissaoRecursos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (usuarioFilialL) LoadPrePermissaoRecursos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUsuarioFilial interface{}, mods queries.Applicator) error {
	var slice []*UsuarioFilial
	var object *UsuarioFilial

	if singular {
		var ok bool
		object, ok = maybeUsuarioFilial.(*UsuarioFilial)
		if !ok {
			object = new(UsuarioFilial)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUsuarioFilial))
			}
		}
	} else {
		s, ok := maybeUsuarioFilial.(*[]*UsuarioFilial)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUsuarioFilial)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUsuarioFilial))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &usuarioFilialR{}
		}
		args[object.UfiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &usuarioFilialR{}
			}
			args[obj.UfiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.Select("`permissao_recurso`.`pre_id`, `permissao_recurso`.`pre_nome`, `permissao_recurso`.`pre_descricao`, `a`.`ufi_id`"),
		qm.From("`permissao_recurso`"),
		qm.InnerJoin("`usuario_filial_recurso` as `a` on `permissao_recurso`.`pre_id` = `a`.`pre_id`"),
		qm.WhereIn("`a`.`ufi_id` in ?", argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load permissao_recurso")
	}

	var resultSlice []*PermissaoRecurso

	var localJoinCols []int
	for results.Next() {
		one := new(PermissaoRecurso)
		var localJoinCol int

		err = results.Scan(&one.PreID, &one.PreNome, &one.PreDescricao, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for permissao_recurso")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice permissao_recurso")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on permissao_recurso")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permissao_recurso")
	}

	if len(permissaoRecursoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PrePermissaoRecursos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &permissaoRecursoR{}
			}
			foreign.R.UfiUsuarioFilials = append(foreign.R.UfiUsuarioFilials, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.UfiID == localJoinCol {
				local.R.PrePermissaoRecursos = append(local.R.PrePermissaoRecursos, foreign)
				if foreign.R == nil {
					foreign.R = &permissaoRecursoR{}
				}
				foreign.R.UfiUsuarioFilials = append(foreign.R.UfiUsuarioFilials, local)
				break
			}
		}
	}

	return nil
}

// SetEmf of the usuarioFilial to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfUsuarioFilials.
func (o *UsuarioFilial) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `usuario_filial` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, usuarioFilialPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.UfiID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &usuarioFilialR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfUsuarioFilials: UsuarioFilialSlice{o},
		}
	} else {
		related.R.EmfUsuarioFilials = append(related.R.EmfUsuarioFilials, o)
	}

	return nil
}

// SetUsu of the usuarioFilial to the related item.
// Sets o.R.Usu to related.
// Adds o to related.R.UsuUsuarioFilials.
func (o *UsuarioFilial) SetUsu(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Usuario) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `usuario_filial` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"usu_id"}),
		strmangle.WhereClause("`", "`", 0, usuarioFilialPrimaryKeyColumns),
	)
	values := []interface{}{related.UsuID, o.UfiID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UsuID = related.UsuID
	if o.R == nil {
		o.R = &usuarioFilialR{
			Usu: related,
		}
	} else {
		o.R.Usu = related
	}

	if related.R == nil {
		related.R = &usuarioR{
			UsuUsuarioFilials: UsuarioFilialSlice{o},
		}
	} else {
		related.R.UsuUsuarioFilials = append(related.R.UsuUsuarioFilials, o)
	}

	return nil
}

// AddPracPermissaoRecursoAcaos adds the given related objects to the existing relationships
// of the usuario_filial, optionally inserting them as new records.
// Appends related to o.R.PracPermissaoRecursoAcaos.
// Sets related.R.UfiUsuarioFilials appropriately.
func (o *UsuarioFilial) AddPracPermissaoRecursoAcaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissaoRecursoAcao) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into `usuario_filial_rec_acao` (`ufi_id`, `prac_id`) values (?, ?)"
		values := []interface{}{o.UfiID, rel.PracID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &usuarioFilialR{
			PracPermissaoRecursoAcaos: related,
		}
	} else {
		o.R.PracPermissaoRecursoAcaos = append(o.R.PracPermissaoRecursoAcaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &permissaoRecursoAcaoR{
				UfiUsuarioFilials: UsuarioFilialSlice{o},
			}
		} else {
			rel.R.UfiUsuarioFilials = append(rel.R.UfiUsuarioFilials, o)
		}
	}
	return nil
}

// SetPracPermissaoRecursoAcaos removes all previously related items of the
// usuario_filial replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UfiUsuarioFilials's PracPermissaoRecursoAcaos accordingly.
// Replaces o.R.PracPermissaoRecursoAcaos with related.
// Sets related.R.UfiUsuarioFilials's PracPermissaoRecursoAcaos accordingly.
func (o *UsuarioFilial) SetPracPermissaoRecursoAcaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissaoRecursoAcao) error {
	query := "delete from `usuario_filial_rec_acao` where `ufi_id` = ?"
	values := []interface{}{o.UfiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removePracPermissaoRecursoAcaosFromUfiUsuarioFilialsSlice(o, related)
	if o.R != nil {
		o.R.PracPermissaoRecursoAcaos = nil
	}

	return o.AddPracPermissaoRecursoAcaos(ctx, exec, insert, related...)
}

// RemovePracPermissaoRecursoAcaos relationships from objects passed in.
// Removes related items from R.PracPermissaoRecursoAcaos (uses pointer comparison, removal does not keep order)
// Sets related.R.UfiUsuarioFilials.
func (o *UsuarioFilial) RemovePracPermissaoRecursoAcaos(ctx context.Context, exec boil.ContextExecutor, related ...*PermissaoRecursoAcao) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from `usuario_filial_rec_acao` where `ufi_id` = ? and `prac_id` in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.UfiID}
	for _, rel := range related {
		values = append(values, rel.PracID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removePracPermissaoRecursoAcaosFromUfiUsuarioFilialsSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.PracPermissaoRecursoAcaos {
			if rel != ri {
				continue
			}

			ln := len(o.R.PracPermissaoRecursoAcaos)
			if ln > 1 && i < ln-1 {
				o.R.PracPermissaoRecursoAcaos[i] = o.R.PracPermissaoRecursoAcaos[ln-1]
			}
			o.R.PracPermissaoRecursoAcaos = o.R.PracPermissaoRecursoAcaos[:ln-1]
			break
		}
	}

	return nil
}

func removePracPermissaoRecursoAcaosFromUfiUsuarioFilialsSlice(o *UsuarioFilial, related []*PermissaoRecursoAcao) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.UfiUsuarioFilials {
			if o.UfiID != ri.UfiID {
				continue
			}

			ln := len(rel.R.UfiUsuarioFilials)
			if ln > 1 && i < ln-1 {
				rel.R.UfiUsuarioFilials[i] = rel.R.UfiUsuarioFilials[ln-1]
			}
			rel.R.UfiUsuarioFilials = rel.R.UfiUsuarioFilials[:ln-1]
			break
		}
	}
}

// AddPRCPPermissaoRecursoCampos adds the given related objects to the existing relationships
// of the usuario_filial, optionally inserting them as new records.
// Appends related to o.R.PRCPPermissaoRecursoCampos.
// Sets related.R.UfiUsuarioFilials appropriately.
func (o *UsuarioFilial) AddPRCPPermissaoRecursoCampos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissaoRecursoCampo) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into `usuario_filial_rec_campos` (`ufi_id`, `prcp_id`) values (?, ?)"
		values := []interface{}{o.UfiID, rel.PRCPID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &usuarioFilialR{
			PRCPPermissaoRecursoCampos: related,
		}
	} else {
		o.R.PRCPPermissaoRecursoCampos = append(o.R.PRCPPermissaoRecursoCampos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &permissaoRecursoCampoR{
				UfiUsuarioFilials: UsuarioFilialSlice{o},
			}
		} else {
			rel.R.UfiUsuarioFilials = append(rel.R.UfiUsuarioFilials, o)
		}
	}
	return nil
}

// SetPRCPPermissaoRecursoCampos removes all previously related items of the
// usuario_filial replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UfiUsuarioFilials's PRCPPermissaoRecursoCampos accordingly.
// Replaces o.R.PRCPPermissaoRecursoCampos with related.
// Sets related.R.UfiUsuarioFilials's PRCPPermissaoRecursoCampos accordingly.
func (o *UsuarioFilial) SetPRCPPermissaoRecursoCampos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissaoRecursoCampo) error {
	query := "delete from `usuario_filial_rec_campos` where `ufi_id` = ?"
	values := []interface{}{o.UfiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removePRCPPermissaoRecursoCamposFromUfiUsuarioFilialsSlice(o, related)
	if o.R != nil {
		o.R.PRCPPermissaoRecursoCampos = nil
	}

	return o.AddPRCPPermissaoRecursoCampos(ctx, exec, insert, related...)
}

// RemovePRCPPermissaoRecursoCampos relationships from objects passed in.
// Removes related items from R.PRCPPermissaoRecursoCampos (uses pointer comparison, removal does not keep order)
// Sets related.R.UfiUsuarioFilials.
func (o *UsuarioFilial) RemovePRCPPermissaoRecursoCampos(ctx context.Context, exec boil.ContextExecutor, related ...*PermissaoRecursoCampo) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from `usuario_filial_rec_campos` where `ufi_id` = ? and `prcp_id` in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.UfiID}
	for _, rel := range related {
		values = append(values, rel.PRCPID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removePRCPPermissaoRecursoCamposFromUfiUsuarioFilialsSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.PRCPPermissaoRecursoCampos {
			if rel != ri {
				continue
			}

			ln := len(o.R.PRCPPermissaoRecursoCampos)
			if ln > 1 && i < ln-1 {
				o.R.PRCPPermissaoRecursoCampos[i] = o.R.PRCPPermissaoRecursoCampos[ln-1]
			}
			o.R.PRCPPermissaoRecursoCampos = o.R.PRCPPermissaoRecursoCampos[:ln-1]
			break
		}
	}

	return nil
}

func removePRCPPermissaoRecursoCamposFromUfiUsuarioFilialsSlice(o *UsuarioFilial, related []*PermissaoRecursoCampo) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.UfiUsuarioFilials {
			if o.UfiID != ri.UfiID {
				continue
			}

			ln := len(rel.R.UfiUsuarioFilials)
			if ln > 1 && i < ln-1 {
				rel.R.UfiUsuarioFilials[i] = rel.R.UfiUsuarioFilials[ln-1]
			}
			rel.R.UfiUsuarioFilials = rel.R.UfiUsuarioFilials[:ln-1]
			break
		}
	}
}

// AddPrePermissaoRecursos adds the given related objects to the existing relationships
// of the usuario_filial, optionally inserting them as new records.
// Appends related to o.R.PrePermissaoRecursos.
// Sets related.R.UfiUsuarioFilials appropriately.
func (o *UsuarioFilial) AddPrePermissaoRecursos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissaoRecurso) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into `usuario_filial_recurso` (`ufi_id`, `pre_id`) values (?, ?)"
		values := []interface{}{o.UfiID, rel.PreID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &usuarioFilialR{
			PrePermissaoRecursos: related,
		}
	} else {
		o.R.PrePermissaoRecursos = append(o.R.PrePermissaoRecursos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &permissaoRecursoR{
				UfiUsuarioFilials: UsuarioFilialSlice{o},
			}
		} else {
			rel.R.UfiUsuarioFilials = append(rel.R.UfiUsuarioFilials, o)
		}
	}
	return nil
}

// SetPrePermissaoRecursos removes all previously related items of the
// usuario_filial replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UfiUsuarioFilials's PrePermissaoRecursos accordingly.
// Replaces o.R.PrePermissaoRecursos with related.
// Sets related.R.UfiUsuarioFilials's PrePermissaoRecursos accordingly.
func (o *UsuarioFilial) SetPrePermissaoRecursos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissaoRecurso) error {
	query := "delete from `usuario_filial_recurso` where `ufi_id` = ?"
	values := []interface{}{o.UfiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removePrePermissaoRecursosFromUfiUsuarioFilialsSlice(o, related)
	if o.R != nil {
		o.R.PrePermissaoRecursos = nil
	}

	return o.AddPrePermissaoRecursos(ctx, exec, insert, related...)
}

// RemovePrePermissaoRecursos relationships from objects passed in.
// Removes related items from R.PrePermissaoRecursos (uses pointer comparison, removal does not keep order)
// Sets related.R.UfiUsuarioFilials.
func (o *UsuarioFilial) RemovePrePermissaoRecursos(ctx context.Context, exec boil.ContextExecutor, related ...*PermissaoRecurso) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from `usuario_filial_recurso` where `ufi_id` = ? and `pre_id` in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.UfiID}
	for _, rel := range related {
		values = append(values, rel.PreID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removePrePermissaoRecursosFromUfiUsuarioFilialsSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.PrePermissaoRecursos {
			if rel != ri {
				continue
			}

			ln := len(o.R.PrePermissaoRecursos)
			if ln > 1 && i < ln-1 {
				o.R.PrePermissaoRecursos[i] = o.R.PrePermissaoRecursos[ln-1]
			}
			o.R.PrePermissaoRecursos = o.R.PrePermissaoRecursos[:ln-1]
			break
		}
	}

	return nil
}

func removePrePermissaoRecursosFromUfiUsuarioFilialsSlice(o *UsuarioFilial, related []*PermissaoRecurso) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.UfiUsuarioFilials {
			if o.UfiID != ri.UfiID {
				continue
			}

			ln := len(rel.R.UfiUsuarioFilials)
			if ln > 1 && i < ln-1 {
				rel.R.UfiUsuarioFilials[i] = rel.R.UfiUsuarioFilials[ln-1]
			}
			rel.R.UfiUsuarioFilials = rel.R.UfiUsuarioFilials[:ln-1]
			break
		}
	}
}

// UsuarioFilials retrieves all the records using an executor.
func UsuarioFilials(mods ...qm.QueryMod) usuarioFilialQuery {
	mods = append(mods, qm.From("`usuario_filial`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`usuario_filial`.*"})
	}

	return usuarioFilialQuery{q}
}

// FindUsuarioFilial retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUsuarioFilial(ctx context.Context, exec boil.ContextExecutor, ufiID int, selectCols ...string) (*UsuarioFilial, error) {
	usuarioFilialObj := &UsuarioFilial{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `usuario_filial` where `ufi_id`=?", sel,
	)

	q := queries.Raw(query, ufiID)

	err := q.Bind(ctx, exec, usuarioFilialObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from usuario_filial")
	}

	if err = usuarioFilialObj.doAfterSelectHooks(ctx, exec); err != nil {
		return usuarioFilialObj, err
	}

	return usuarioFilialObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UsuarioFilial) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no usuario_filial provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(usuarioFilialColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	usuarioFilialInsertCacheMut.RLock()
	cache, cached := usuarioFilialInsertCache[key]
	usuarioFilialInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			usuarioFilialAllColumns,
			usuarioFilialColumnsWithDefault,
			usuarioFilialColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(usuarioFilialType, usuarioFilialMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(usuarioFilialType, usuarioFilialMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `usuario_filial` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `usuario_filial` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `usuario_filial` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, usuarioFilialPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into usuario_filial")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.UfiID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for usuario_filial")
	}

CacheNoHooks:
	if !cached {
		usuarioFilialInsertCacheMut.Lock()
		usuarioFilialInsertCache[key] = cache
		usuarioFilialInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UsuarioFilial.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UsuarioFilial) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	usuarioFilialUpdateCacheMut.RLock()
	cache, cached := usuarioFilialUpdateCache[key]
	usuarioFilialUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			usuarioFilialAllColumns,
			usuarioFilialPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update usuario_filial, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `usuario_filial` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, usuarioFilialPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(usuarioFilialType, usuarioFilialMapping, append(wl, usuarioFilialPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update usuario_filial row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for usuario_filial")
	}

	if !cached {
		usuarioFilialUpdateCacheMut.Lock()
		usuarioFilialUpdateCache[key] = cache
		usuarioFilialUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q usuarioFilialQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for usuario_filial")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for usuario_filial")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UsuarioFilialSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usuarioFilialPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `usuario_filial` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, usuarioFilialPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in usuarioFilial slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all usuarioFilial")
	}
	return rowsAff, nil
}

var mySQLUsuarioFilialUniqueColumns = []string{
	"ufi_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UsuarioFilial) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no usuario_filial provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(usuarioFilialColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLUsuarioFilialUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	usuarioFilialUpsertCacheMut.RLock()
	cache, cached := usuarioFilialUpsertCache[key]
	usuarioFilialUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			usuarioFilialAllColumns,
			usuarioFilialColumnsWithDefault,
			usuarioFilialColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			usuarioFilialAllColumns,
			usuarioFilialPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert usuario_filial, could not build update column list")
		}

		ret := strmangle.SetComplement(usuarioFilialAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`usuario_filial`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `usuario_filial` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(usuarioFilialType, usuarioFilialMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(usuarioFilialType, usuarioFilialMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for usuario_filial")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(usuarioFilialType, usuarioFilialMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for usuario_filial")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for usuario_filial")
	}

CacheNoHooks:
	if !cached {
		usuarioFilialUpsertCacheMut.Lock()
		usuarioFilialUpsertCache[key] = cache
		usuarioFilialUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UsuarioFilial record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UsuarioFilial) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UsuarioFilial provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), usuarioFilialPrimaryKeyMapping)
	sql := "DELETE FROM `usuario_filial` WHERE `ufi_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from usuario_filial")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for usuario_filial")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q usuarioFilialQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no usuarioFilialQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from usuario_filial")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for usuario_filial")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UsuarioFilialSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(usuarioFilialBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usuarioFilialPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `usuario_filial` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, usuarioFilialPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from usuarioFilial slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for usuario_filial")
	}

	if len(usuarioFilialAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UsuarioFilial) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUsuarioFilial(ctx, exec, o.UfiID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UsuarioFilialSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UsuarioFilialSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), usuarioFilialPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `usuario_filial`.* FROM `usuario_filial` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, usuarioFilialPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UsuarioFilialSlice")
	}

	*o = slice

	return nil
}

// UsuarioFilialExists checks if the UsuarioFilial row exists.
func UsuarioFilialExists(ctx context.Context, exec boil.ContextExecutor, ufiID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `usuario_filial` where `ufi_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, ufiID)
	}
	row := exec.QueryRowContext(ctx, sql, ufiID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if usuario_filial exists")
	}

	return exists, nil
}

// Exists checks if the UsuarioFilial row exists.
func (o *UsuarioFilial) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UsuarioFilialExists(ctx, exec, o.UfiID)
}
