// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// EcfAliquotum is an object representing the database table.
type EcfAliquotum struct {
	EcaID                   int           `boil:"eca_id" json:"eca_id" toml:"eca_id" yaml:"eca_id"`
	NCMNumero               null.Int      `boil:"ncm_numero" json:"ncm_numero,omitempty" toml:"ncm_numero" yaml:"ncm_numero,omitempty"`
	EcaAliquota             types.Decimal `boil:"eca_aliquota" json:"eca_aliquota" toml:"eca_aliquota" yaml:"eca_aliquota"`
	EcaTotalizador          string        `boil:"eca_totalizador" json:"eca_totalizador" toml:"eca_totalizador" yaml:"eca_totalizador"`
	EcaTotalizadorDescricao string        `boil:"eca_totalizador_descricao" json:"eca_totalizador_descricao" toml:"eca_totalizador_descricao" yaml:"eca_totalizador_descricao"`
	EcaTotalizadorNumero    int16         `boil:"eca_totalizador_numero" json:"eca_totalizador_numero" toml:"eca_totalizador_numero" yaml:"eca_totalizador_numero"`

	R *ecfAliquotumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ecfAliquotumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EcfAliquotumColumns = struct {
	EcaID                   string
	NCMNumero               string
	EcaAliquota             string
	EcaTotalizador          string
	EcaTotalizadorDescricao string
	EcaTotalizadorNumero    string
}{
	EcaID:                   "eca_id",
	NCMNumero:               "ncm_numero",
	EcaAliquota:             "eca_aliquota",
	EcaTotalizador:          "eca_totalizador",
	EcaTotalizadorDescricao: "eca_totalizador_descricao",
	EcaTotalizadorNumero:    "eca_totalizador_numero",
}

var EcfAliquotumTableColumns = struct {
	EcaID                   string
	NCMNumero               string
	EcaAliquota             string
	EcaTotalizador          string
	EcaTotalizadorDescricao string
	EcaTotalizadorNumero    string
}{
	EcaID:                   "ecf_aliquota.eca_id",
	NCMNumero:               "ecf_aliquota.ncm_numero",
	EcaAliquota:             "ecf_aliquota.eca_aliquota",
	EcaTotalizador:          "ecf_aliquota.eca_totalizador",
	EcaTotalizadorDescricao: "ecf_aliquota.eca_totalizador_descricao",
	EcaTotalizadorNumero:    "ecf_aliquota.eca_totalizador_numero",
}

// Generated where

var EcfAliquotumWhere = struct {
	EcaID                   whereHelperint
	NCMNumero               whereHelpernull_Int
	EcaAliquota             whereHelpertypes_Decimal
	EcaTotalizador          whereHelperstring
	EcaTotalizadorDescricao whereHelperstring
	EcaTotalizadorNumero    whereHelperint16
}{
	EcaID:                   whereHelperint{field: "`ecf_aliquota`.`eca_id`"},
	NCMNumero:               whereHelpernull_Int{field: "`ecf_aliquota`.`ncm_numero`"},
	EcaAliquota:             whereHelpertypes_Decimal{field: "`ecf_aliquota`.`eca_aliquota`"},
	EcaTotalizador:          whereHelperstring{field: "`ecf_aliquota`.`eca_totalizador`"},
	EcaTotalizadorDescricao: whereHelperstring{field: "`ecf_aliquota`.`eca_totalizador_descricao`"},
	EcaTotalizadorNumero:    whereHelperint16{field: "`ecf_aliquota`.`eca_totalizador_numero`"},
}

// EcfAliquotumRels is where relationship names are stored.
var EcfAliquotumRels = struct {
	NCMNumeroNCM string
}{
	NCMNumeroNCM: "NCMNumeroNCM",
}

// ecfAliquotumR is where relationships are stored.
type ecfAliquotumR struct {
	NCMNumeroNCM *NCM `boil:"NCMNumeroNCM" json:"NCMNumeroNCM" toml:"NCMNumeroNCM" yaml:"NCMNumeroNCM"`
}

// NewStruct creates a new relationship struct
func (*ecfAliquotumR) NewStruct() *ecfAliquotumR {
	return &ecfAliquotumR{}
}

func (r *ecfAliquotumR) GetNCMNumeroNCM() *NCM {
	if r == nil {
		return nil
	}
	return r.NCMNumeroNCM
}

// ecfAliquotumL is where Load methods for each relationship are stored.
type ecfAliquotumL struct{}

var (
	ecfAliquotumAllColumns            = []string{"eca_id", "ncm_numero", "eca_aliquota", "eca_totalizador", "eca_totalizador_descricao", "eca_totalizador_numero"}
	ecfAliquotumColumnsWithoutDefault = []string{"eca_id", "ncm_numero", "eca_aliquota", "eca_totalizador", "eca_totalizador_descricao", "eca_totalizador_numero"}
	ecfAliquotumColumnsWithDefault    = []string{}
	ecfAliquotumPrimaryKeyColumns     = []string{"eca_id"}
	ecfAliquotumGeneratedColumns      = []string{}
)

type (
	// EcfAliquotumSlice is an alias for a slice of pointers to EcfAliquotum.
	// This should almost always be used instead of []EcfAliquotum.
	EcfAliquotumSlice []*EcfAliquotum
	// EcfAliquotumHook is the signature for custom EcfAliquotum hook methods
	EcfAliquotumHook func(context.Context, boil.ContextExecutor, *EcfAliquotum) error

	ecfAliquotumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ecfAliquotumType                 = reflect.TypeOf(&EcfAliquotum{})
	ecfAliquotumMapping              = queries.MakeStructMapping(ecfAliquotumType)
	ecfAliquotumPrimaryKeyMapping, _ = queries.BindMapping(ecfAliquotumType, ecfAliquotumMapping, ecfAliquotumPrimaryKeyColumns)
	ecfAliquotumInsertCacheMut       sync.RWMutex
	ecfAliquotumInsertCache          = make(map[string]insertCache)
	ecfAliquotumUpdateCacheMut       sync.RWMutex
	ecfAliquotumUpdateCache          = make(map[string]updateCache)
	ecfAliquotumUpsertCacheMut       sync.RWMutex
	ecfAliquotumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ecfAliquotumAfterSelectMu sync.Mutex
var ecfAliquotumAfterSelectHooks []EcfAliquotumHook

var ecfAliquotumBeforeInsertMu sync.Mutex
var ecfAliquotumBeforeInsertHooks []EcfAliquotumHook
var ecfAliquotumAfterInsertMu sync.Mutex
var ecfAliquotumAfterInsertHooks []EcfAliquotumHook

var ecfAliquotumBeforeUpdateMu sync.Mutex
var ecfAliquotumBeforeUpdateHooks []EcfAliquotumHook
var ecfAliquotumAfterUpdateMu sync.Mutex
var ecfAliquotumAfterUpdateHooks []EcfAliquotumHook

var ecfAliquotumBeforeDeleteMu sync.Mutex
var ecfAliquotumBeforeDeleteHooks []EcfAliquotumHook
var ecfAliquotumAfterDeleteMu sync.Mutex
var ecfAliquotumAfterDeleteHooks []EcfAliquotumHook

var ecfAliquotumBeforeUpsertMu sync.Mutex
var ecfAliquotumBeforeUpsertHooks []EcfAliquotumHook
var ecfAliquotumAfterUpsertMu sync.Mutex
var ecfAliquotumAfterUpsertHooks []EcfAliquotumHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *EcfAliquotum) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *EcfAliquotum) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *EcfAliquotum) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *EcfAliquotum) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *EcfAliquotum) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *EcfAliquotum) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *EcfAliquotum) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *EcfAliquotum) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *EcfAliquotum) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ecfAliquotumAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEcfAliquotumHook registers your hook function for all future operations.
func AddEcfAliquotumHook(hookPoint boil.HookPoint, ecfAliquotumHook EcfAliquotumHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ecfAliquotumAfterSelectMu.Lock()
		ecfAliquotumAfterSelectHooks = append(ecfAliquotumAfterSelectHooks, ecfAliquotumHook)
		ecfAliquotumAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		ecfAliquotumBeforeInsertMu.Lock()
		ecfAliquotumBeforeInsertHooks = append(ecfAliquotumBeforeInsertHooks, ecfAliquotumHook)
		ecfAliquotumBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		ecfAliquotumAfterInsertMu.Lock()
		ecfAliquotumAfterInsertHooks = append(ecfAliquotumAfterInsertHooks, ecfAliquotumHook)
		ecfAliquotumAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		ecfAliquotumBeforeUpdateMu.Lock()
		ecfAliquotumBeforeUpdateHooks = append(ecfAliquotumBeforeUpdateHooks, ecfAliquotumHook)
		ecfAliquotumBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		ecfAliquotumAfterUpdateMu.Lock()
		ecfAliquotumAfterUpdateHooks = append(ecfAliquotumAfterUpdateHooks, ecfAliquotumHook)
		ecfAliquotumAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		ecfAliquotumBeforeDeleteMu.Lock()
		ecfAliquotumBeforeDeleteHooks = append(ecfAliquotumBeforeDeleteHooks, ecfAliquotumHook)
		ecfAliquotumBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		ecfAliquotumAfterDeleteMu.Lock()
		ecfAliquotumAfterDeleteHooks = append(ecfAliquotumAfterDeleteHooks, ecfAliquotumHook)
		ecfAliquotumAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		ecfAliquotumBeforeUpsertMu.Lock()
		ecfAliquotumBeforeUpsertHooks = append(ecfAliquotumBeforeUpsertHooks, ecfAliquotumHook)
		ecfAliquotumBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		ecfAliquotumAfterUpsertMu.Lock()
		ecfAliquotumAfterUpsertHooks = append(ecfAliquotumAfterUpsertHooks, ecfAliquotumHook)
		ecfAliquotumAfterUpsertMu.Unlock()
	}
}

// One returns a single ecfAliquotum record from the query.
func (q ecfAliquotumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EcfAliquotum, error) {
	o := &EcfAliquotum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ecf_aliquota")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all EcfAliquotum records from the query.
func (q ecfAliquotumQuery) All(ctx context.Context, exec boil.ContextExecutor) (EcfAliquotumSlice, error) {
	var o []*EcfAliquotum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to EcfAliquotum slice")
	}

	if len(ecfAliquotumAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all EcfAliquotum records in the query.
func (q ecfAliquotumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ecf_aliquota rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ecfAliquotumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ecf_aliquota exists")
	}

	return count > 0, nil
}

// NCMNumeroNCM pointed to by the foreign key.
func (o *EcfAliquotum) NCMNumeroNCM(mods ...qm.QueryMod) ncmQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ncm_numero` = ?", o.NCMNumero),
	}

	queryMods = append(queryMods, mods...)

	return NCMS(queryMods...)
}

// LoadNCMNumeroNCM allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ecfAliquotumL) LoadNCMNumeroNCM(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEcfAliquotum interface{}, mods queries.Applicator) error {
	var slice []*EcfAliquotum
	var object *EcfAliquotum

	if singular {
		var ok bool
		object, ok = maybeEcfAliquotum.(*EcfAliquotum)
		if !ok {
			object = new(EcfAliquotum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEcfAliquotum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEcfAliquotum))
			}
		}
	} else {
		s, ok := maybeEcfAliquotum.(*[]*EcfAliquotum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEcfAliquotum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEcfAliquotum))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ecfAliquotumR{}
		}
		if !queries.IsNil(object.NCMNumero) {
			args[object.NCMNumero] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ecfAliquotumR{}
			}

			if !queries.IsNil(obj.NCMNumero) {
				args[obj.NCMNumero] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ncm`),
		qm.WhereIn(`ncm.ncm_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load NCM")
	}

	var resultSlice []*NCM
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice NCM")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ncm")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ncm")
	}

	if len(ncmAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.NCMNumeroNCM = foreign
		if foreign.R == nil {
			foreign.R = &ncmR{}
		}
		foreign.R.NCMNumeroEcfAliquota = append(foreign.R.NCMNumeroEcfAliquota, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.NCMNumero, foreign.NCMNumero) {
				local.R.NCMNumeroNCM = foreign
				if foreign.R == nil {
					foreign.R = &ncmR{}
				}
				foreign.R.NCMNumeroEcfAliquota = append(foreign.R.NCMNumeroEcfAliquota, local)
				break
			}
		}
	}

	return nil
}

// SetNCMNumeroNCM of the ecfAliquotum to the related item.
// Sets o.R.NCMNumeroNCM to related.
// Adds o to related.R.NCMNumeroEcfAliquota.
func (o *EcfAliquotum) SetNCMNumeroNCM(ctx context.Context, exec boil.ContextExecutor, insert bool, related *NCM) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ecf_aliquota` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ncm_numero"}),
		strmangle.WhereClause("`", "`", 0, ecfAliquotumPrimaryKeyColumns),
	)
	values := []interface{}{related.NCMNumero, o.EcaID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.NCMNumero, related.NCMNumero)
	if o.R == nil {
		o.R = &ecfAliquotumR{
			NCMNumeroNCM: related,
		}
	} else {
		o.R.NCMNumeroNCM = related
	}

	if related.R == nil {
		related.R = &ncmR{
			NCMNumeroEcfAliquota: EcfAliquotumSlice{o},
		}
	} else {
		related.R.NCMNumeroEcfAliquota = append(related.R.NCMNumeroEcfAliquota, o)
	}

	return nil
}

// RemoveNCMNumeroNCM relationship.
// Sets o.R.NCMNumeroNCM to nil.
// Removes o from all passed in related items' relationships struct.
func (o *EcfAliquotum) RemoveNCMNumeroNCM(ctx context.Context, exec boil.ContextExecutor, related *NCM) error {
	var err error

	queries.SetScanner(&o.NCMNumero, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("ncm_numero")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.NCMNumeroNCM = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.NCMNumeroEcfAliquota {
		if queries.Equal(o.NCMNumero, ri.NCMNumero) {
			continue
		}

		ln := len(related.R.NCMNumeroEcfAliquota)
		if ln > 1 && i < ln-1 {
			related.R.NCMNumeroEcfAliquota[i] = related.R.NCMNumeroEcfAliquota[ln-1]
		}
		related.R.NCMNumeroEcfAliquota = related.R.NCMNumeroEcfAliquota[:ln-1]
		break
	}
	return nil
}

// EcfAliquota retrieves all the records using an executor.
func EcfAliquota(mods ...qm.QueryMod) ecfAliquotumQuery {
	mods = append(mods, qm.From("`ecf_aliquota`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`ecf_aliquota`.*"})
	}

	return ecfAliquotumQuery{q}
}

// FindEcfAliquotum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEcfAliquotum(ctx context.Context, exec boil.ContextExecutor, ecaID int, selectCols ...string) (*EcfAliquotum, error) {
	ecfAliquotumObj := &EcfAliquotum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ecf_aliquota` where `eca_id`=?", sel,
	)

	q := queries.Raw(query, ecaID)

	err := q.Bind(ctx, exec, ecfAliquotumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ecf_aliquota")
	}

	if err = ecfAliquotumObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ecfAliquotumObj, err
	}

	return ecfAliquotumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EcfAliquotum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ecf_aliquota provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ecfAliquotumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ecfAliquotumInsertCacheMut.RLock()
	cache, cached := ecfAliquotumInsertCache[key]
	ecfAliquotumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ecfAliquotumAllColumns,
			ecfAliquotumColumnsWithDefault,
			ecfAliquotumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ecfAliquotumType, ecfAliquotumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ecfAliquotumType, ecfAliquotumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ecf_aliquota` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ecf_aliquota` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ecf_aliquota` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ecfAliquotumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ecf_aliquota")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.EcaID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ecf_aliquota")
	}

CacheNoHooks:
	if !cached {
		ecfAliquotumInsertCacheMut.Lock()
		ecfAliquotumInsertCache[key] = cache
		ecfAliquotumInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the EcfAliquotum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EcfAliquotum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ecfAliquotumUpdateCacheMut.RLock()
	cache, cached := ecfAliquotumUpdateCache[key]
	ecfAliquotumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ecfAliquotumAllColumns,
			ecfAliquotumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ecf_aliquota, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ecf_aliquota` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ecfAliquotumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ecfAliquotumType, ecfAliquotumMapping, append(wl, ecfAliquotumPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ecf_aliquota row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ecf_aliquota")
	}

	if !cached {
		ecfAliquotumUpdateCacheMut.Lock()
		ecfAliquotumUpdateCache[key] = cache
		ecfAliquotumUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ecfAliquotumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ecf_aliquota")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ecf_aliquota")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EcfAliquotumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ecfAliquotumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ecf_aliquota` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ecfAliquotumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ecfAliquotum slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ecfAliquotum")
	}
	return rowsAff, nil
}

var mySQLEcfAliquotumUniqueColumns = []string{
	"eca_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *EcfAliquotum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ecf_aliquota provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ecfAliquotumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLEcfAliquotumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ecfAliquotumUpsertCacheMut.RLock()
	cache, cached := ecfAliquotumUpsertCache[key]
	ecfAliquotumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			ecfAliquotumAllColumns,
			ecfAliquotumColumnsWithDefault,
			ecfAliquotumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ecfAliquotumAllColumns,
			ecfAliquotumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ecf_aliquota, could not build update column list")
		}

		ret := strmangle.SetComplement(ecfAliquotumAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`ecf_aliquota`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ecf_aliquota` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ecfAliquotumType, ecfAliquotumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ecfAliquotumType, ecfAliquotumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ecf_aliquota")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ecfAliquotumType, ecfAliquotumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ecf_aliquota")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ecf_aliquota")
	}

CacheNoHooks:
	if !cached {
		ecfAliquotumUpsertCacheMut.Lock()
		ecfAliquotumUpsertCache[key] = cache
		ecfAliquotumUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single EcfAliquotum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EcfAliquotum) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no EcfAliquotum provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ecfAliquotumPrimaryKeyMapping)
	sql := "DELETE FROM `ecf_aliquota` WHERE `eca_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ecf_aliquota")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ecf_aliquota")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ecfAliquotumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ecfAliquotumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ecf_aliquota")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ecf_aliquota")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EcfAliquotumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ecfAliquotumBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ecfAliquotumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ecf_aliquota` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ecfAliquotumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ecfAliquotum slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ecf_aliquota")
	}

	if len(ecfAliquotumAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EcfAliquotum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEcfAliquotum(ctx, exec, o.EcaID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EcfAliquotumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EcfAliquotumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ecfAliquotumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ecf_aliquota`.* FROM `ecf_aliquota` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ecfAliquotumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in EcfAliquotumSlice")
	}

	*o = slice

	return nil
}

// EcfAliquotumExists checks if the EcfAliquotum row exists.
func EcfAliquotumExists(ctx context.Context, exec boil.ContextExecutor, ecaID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ecf_aliquota` where `eca_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, ecaID)
	}
	row := exec.QueryRowContext(ctx, sql, ecaID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ecf_aliquota exists")
	}

	return exists, nil
}

// Exists checks if the EcfAliquotum row exists.
func (o *EcfAliquotum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return EcfAliquotumExists(ctx, exec, o.EcaID)
}
