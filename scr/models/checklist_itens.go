// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ChecklistIten is an object representing the database table.
type ChecklistIten struct {
	CKLID           int    `boil:"ckl_id" json:"ckl_id" toml:"ckl_id" yaml:"ckl_id"`
	CkliItem        int    `boil:"ckli_item" json:"ckli_item" toml:"ckli_item" yaml:"ckli_item"`
	CkliTexto       string `boil:"ckli_texto" json:"ckli_texto" toml:"ckli_texto" yaml:"ckli_texto"`
	ChliObrigatorio int16  `boil:"chli_obrigatorio" json:"chli_obrigatorio" toml:"chli_obrigatorio" yaml:"chli_obrigatorio"`

	R *checklistItenR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L checklistItenL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ChecklistItenColumns = struct {
	CKLID           string
	CkliItem        string
	CkliTexto       string
	ChliObrigatorio string
}{
	CKLID:           "ckl_id",
	CkliItem:        "ckli_item",
	CkliTexto:       "ckli_texto",
	ChliObrigatorio: "chli_obrigatorio",
}

var ChecklistItenTableColumns = struct {
	CKLID           string
	CkliItem        string
	CkliTexto       string
	ChliObrigatorio string
}{
	CKLID:           "checklist_itens.ckl_id",
	CkliItem:        "checklist_itens.ckli_item",
	CkliTexto:       "checklist_itens.ckli_texto",
	ChliObrigatorio: "checklist_itens.chli_obrigatorio",
}

// Generated where

var ChecklistItenWhere = struct {
	CKLID           whereHelperint
	CkliItem        whereHelperint
	CkliTexto       whereHelperstring
	ChliObrigatorio whereHelperint16
}{
	CKLID:           whereHelperint{field: "`checklist_itens`.`ckl_id`"},
	CkliItem:        whereHelperint{field: "`checklist_itens`.`ckli_item`"},
	CkliTexto:       whereHelperstring{field: "`checklist_itens`.`ckli_texto`"},
	ChliObrigatorio: whereHelperint16{field: "`checklist_itens`.`chli_obrigatorio`"},
}

// ChecklistItenRels is where relationship names are stored.
var ChecklistItenRels = struct {
	CKL string
}{
	CKL: "CKL",
}

// checklistItenR is where relationships are stored.
type checklistItenR struct {
	CKL *Checklist `boil:"CKL" json:"CKL" toml:"CKL" yaml:"CKL"`
}

// NewStruct creates a new relationship struct
func (*checklistItenR) NewStruct() *checklistItenR {
	return &checklistItenR{}
}

func (r *checklistItenR) GetCKL() *Checklist {
	if r == nil {
		return nil
	}
	return r.CKL
}

// checklistItenL is where Load methods for each relationship are stored.
type checklistItenL struct{}

var (
	checklistItenAllColumns            = []string{"ckl_id", "ckli_item", "ckli_texto", "chli_obrigatorio"}
	checklistItenColumnsWithoutDefault = []string{"ckl_id", "ckli_item", "ckli_texto"}
	checklistItenColumnsWithDefault    = []string{"chli_obrigatorio"}
	checklistItenPrimaryKeyColumns     = []string{"ckl_id", "ckli_item"}
	checklistItenGeneratedColumns      = []string{}
)

type (
	// ChecklistItenSlice is an alias for a slice of pointers to ChecklistIten.
	// This should almost always be used instead of []ChecklistIten.
	ChecklistItenSlice []*ChecklistIten
	// ChecklistItenHook is the signature for custom ChecklistIten hook methods
	ChecklistItenHook func(context.Context, boil.ContextExecutor, *ChecklistIten) error

	checklistItenQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	checklistItenType                 = reflect.TypeOf(&ChecklistIten{})
	checklistItenMapping              = queries.MakeStructMapping(checklistItenType)
	checklistItenPrimaryKeyMapping, _ = queries.BindMapping(checklistItenType, checklistItenMapping, checklistItenPrimaryKeyColumns)
	checklistItenInsertCacheMut       sync.RWMutex
	checklistItenInsertCache          = make(map[string]insertCache)
	checklistItenUpdateCacheMut       sync.RWMutex
	checklistItenUpdateCache          = make(map[string]updateCache)
	checklistItenUpsertCacheMut       sync.RWMutex
	checklistItenUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var checklistItenAfterSelectMu sync.Mutex
var checklistItenAfterSelectHooks []ChecklistItenHook

var checklistItenBeforeInsertMu sync.Mutex
var checklistItenBeforeInsertHooks []ChecklistItenHook
var checklistItenAfterInsertMu sync.Mutex
var checklistItenAfterInsertHooks []ChecklistItenHook

var checklistItenBeforeUpdateMu sync.Mutex
var checklistItenBeforeUpdateHooks []ChecklistItenHook
var checklistItenAfterUpdateMu sync.Mutex
var checklistItenAfterUpdateHooks []ChecklistItenHook

var checklistItenBeforeDeleteMu sync.Mutex
var checklistItenBeforeDeleteHooks []ChecklistItenHook
var checklistItenAfterDeleteMu sync.Mutex
var checklistItenAfterDeleteHooks []ChecklistItenHook

var checklistItenBeforeUpsertMu sync.Mutex
var checklistItenBeforeUpsertHooks []ChecklistItenHook
var checklistItenAfterUpsertMu sync.Mutex
var checklistItenAfterUpsertHooks []ChecklistItenHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ChecklistIten) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ChecklistIten) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ChecklistIten) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ChecklistIten) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ChecklistIten) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ChecklistIten) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ChecklistIten) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ChecklistIten) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ChecklistIten) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range checklistItenAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddChecklistItenHook registers your hook function for all future operations.
func AddChecklistItenHook(hookPoint boil.HookPoint, checklistItenHook ChecklistItenHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		checklistItenAfterSelectMu.Lock()
		checklistItenAfterSelectHooks = append(checklistItenAfterSelectHooks, checklistItenHook)
		checklistItenAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		checklistItenBeforeInsertMu.Lock()
		checklistItenBeforeInsertHooks = append(checklistItenBeforeInsertHooks, checklistItenHook)
		checklistItenBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		checklistItenAfterInsertMu.Lock()
		checklistItenAfterInsertHooks = append(checklistItenAfterInsertHooks, checklistItenHook)
		checklistItenAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		checklistItenBeforeUpdateMu.Lock()
		checklistItenBeforeUpdateHooks = append(checklistItenBeforeUpdateHooks, checklistItenHook)
		checklistItenBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		checklistItenAfterUpdateMu.Lock()
		checklistItenAfterUpdateHooks = append(checklistItenAfterUpdateHooks, checklistItenHook)
		checklistItenAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		checklistItenBeforeDeleteMu.Lock()
		checklistItenBeforeDeleteHooks = append(checklistItenBeforeDeleteHooks, checklistItenHook)
		checklistItenBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		checklistItenAfterDeleteMu.Lock()
		checklistItenAfterDeleteHooks = append(checklistItenAfterDeleteHooks, checklistItenHook)
		checklistItenAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		checklistItenBeforeUpsertMu.Lock()
		checklistItenBeforeUpsertHooks = append(checklistItenBeforeUpsertHooks, checklistItenHook)
		checklistItenBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		checklistItenAfterUpsertMu.Lock()
		checklistItenAfterUpsertHooks = append(checklistItenAfterUpsertHooks, checklistItenHook)
		checklistItenAfterUpsertMu.Unlock()
	}
}

// One returns a single checklistIten record from the query.
func (q checklistItenQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ChecklistIten, error) {
	o := &ChecklistIten{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for checklist_itens")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ChecklistIten records from the query.
func (q checklistItenQuery) All(ctx context.Context, exec boil.ContextExecutor) (ChecklistItenSlice, error) {
	var o []*ChecklistIten

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ChecklistIten slice")
	}

	if len(checklistItenAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ChecklistIten records in the query.
func (q checklistItenQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count checklist_itens rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q checklistItenQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if checklist_itens exists")
	}

	return count > 0, nil
}

// CKL pointed to by the foreign key.
func (o *ChecklistIten) CKL(mods ...qm.QueryMod) checklistQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ckl_id` = ?", o.CKLID),
	}

	queryMods = append(queryMods, mods...)

	return Checklists(queryMods...)
}

// LoadCKL allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (checklistItenL) LoadCKL(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChecklistIten interface{}, mods queries.Applicator) error {
	var slice []*ChecklistIten
	var object *ChecklistIten

	if singular {
		var ok bool
		object, ok = maybeChecklistIten.(*ChecklistIten)
		if !ok {
			object = new(ChecklistIten)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChecklistIten)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChecklistIten))
			}
		}
	} else {
		s, ok := maybeChecklistIten.(*[]*ChecklistIten)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChecklistIten)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChecklistIten))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &checklistItenR{}
		}
		args[object.CKLID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &checklistItenR{}
			}

			args[obj.CKLID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`checklist`),
		qm.WhereIn(`checklist.ckl_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Checklist")
	}

	var resultSlice []*Checklist
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Checklist")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for checklist")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for checklist")
	}

	if len(checklistAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CKL = foreign
		if foreign.R == nil {
			foreign.R = &checklistR{}
		}
		foreign.R.CKLChecklistItens = append(foreign.R.CKLChecklistItens, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CKLID == foreign.CKLID {
				local.R.CKL = foreign
				if foreign.R == nil {
					foreign.R = &checklistR{}
				}
				foreign.R.CKLChecklistItens = append(foreign.R.CKLChecklistItens, local)
				break
			}
		}
	}

	return nil
}

// SetCKL of the checklistIten to the related item.
// Sets o.R.CKL to related.
// Adds o to related.R.CKLChecklistItens.
func (o *ChecklistIten) SetCKL(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Checklist) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `checklist_itens` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ckl_id"}),
		strmangle.WhereClause("`", "`", 0, checklistItenPrimaryKeyColumns),
	)
	values := []interface{}{related.CKLID, o.CKLID, o.CkliItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CKLID = related.CKLID
	if o.R == nil {
		o.R = &checklistItenR{
			CKL: related,
		}
	} else {
		o.R.CKL = related
	}

	if related.R == nil {
		related.R = &checklistR{
			CKLChecklistItens: ChecklistItenSlice{o},
		}
	} else {
		related.R.CKLChecklistItens = append(related.R.CKLChecklistItens, o)
	}

	return nil
}

// ChecklistItens retrieves all the records using an executor.
func ChecklistItens(mods ...qm.QueryMod) checklistItenQuery {
	mods = append(mods, qm.From("`checklist_itens`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`checklist_itens`.*"})
	}

	return checklistItenQuery{q}
}

// FindChecklistIten retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindChecklistIten(ctx context.Context, exec boil.ContextExecutor, cKLID int, ckliItem int, selectCols ...string) (*ChecklistIten, error) {
	checklistItenObj := &ChecklistIten{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `checklist_itens` where `ckl_id`=? AND `ckli_item`=?", sel,
	)

	q := queries.Raw(query, cKLID, ckliItem)

	err := q.Bind(ctx, exec, checklistItenObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from checklist_itens")
	}

	if err = checklistItenObj.doAfterSelectHooks(ctx, exec); err != nil {
		return checklistItenObj, err
	}

	return checklistItenObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ChecklistIten) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no checklist_itens provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(checklistItenColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	checklistItenInsertCacheMut.RLock()
	cache, cached := checklistItenInsertCache[key]
	checklistItenInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			checklistItenAllColumns,
			checklistItenColumnsWithDefault,
			checklistItenColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(checklistItenType, checklistItenMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(checklistItenType, checklistItenMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `checklist_itens` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `checklist_itens` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `checklist_itens` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, checklistItenPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into checklist_itens")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CKLID,
		o.CkliItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for checklist_itens")
	}

CacheNoHooks:
	if !cached {
		checklistItenInsertCacheMut.Lock()
		checklistItenInsertCache[key] = cache
		checklistItenInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ChecklistIten.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ChecklistIten) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	checklistItenUpdateCacheMut.RLock()
	cache, cached := checklistItenUpdateCache[key]
	checklistItenUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			checklistItenAllColumns,
			checklistItenPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update checklist_itens, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `checklist_itens` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, checklistItenPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(checklistItenType, checklistItenMapping, append(wl, checklistItenPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update checklist_itens row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for checklist_itens")
	}

	if !cached {
		checklistItenUpdateCacheMut.Lock()
		checklistItenUpdateCache[key] = cache
		checklistItenUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q checklistItenQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for checklist_itens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for checklist_itens")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ChecklistItenSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), checklistItenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `checklist_itens` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, checklistItenPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in checklistIten slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all checklistIten")
	}
	return rowsAff, nil
}

var mySQLChecklistItenUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ChecklistIten) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no checklist_itens provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(checklistItenColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLChecklistItenUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	checklistItenUpsertCacheMut.RLock()
	cache, cached := checklistItenUpsertCache[key]
	checklistItenUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			checklistItenAllColumns,
			checklistItenColumnsWithDefault,
			checklistItenColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			checklistItenAllColumns,
			checklistItenPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert checklist_itens, could not build update column list")
		}

		ret := strmangle.SetComplement(checklistItenAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`checklist_itens`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `checklist_itens` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(checklistItenType, checklistItenMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(checklistItenType, checklistItenMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for checklist_itens")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(checklistItenType, checklistItenMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for checklist_itens")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for checklist_itens")
	}

CacheNoHooks:
	if !cached {
		checklistItenUpsertCacheMut.Lock()
		checklistItenUpsertCache[key] = cache
		checklistItenUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ChecklistIten record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ChecklistIten) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ChecklistIten provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), checklistItenPrimaryKeyMapping)
	sql := "DELETE FROM `checklist_itens` WHERE `ckl_id`=? AND `ckli_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from checklist_itens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for checklist_itens")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q checklistItenQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no checklistItenQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from checklist_itens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for checklist_itens")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ChecklistItenSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(checklistItenBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), checklistItenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `checklist_itens` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, checklistItenPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from checklistIten slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for checklist_itens")
	}

	if len(checklistItenAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ChecklistIten) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindChecklistIten(ctx, exec, o.CKLID, o.CkliItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ChecklistItenSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ChecklistItenSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), checklistItenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `checklist_itens`.* FROM `checklist_itens` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, checklistItenPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ChecklistItenSlice")
	}

	*o = slice

	return nil
}

// ChecklistItenExists checks if the ChecklistIten row exists.
func ChecklistItenExists(ctx context.Context, exec boil.ContextExecutor, cKLID int, ckliItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `checklist_itens` where `ckl_id`=? AND `ckli_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cKLID, ckliItem)
	}
	row := exec.QueryRowContext(ctx, sql, cKLID, ckliItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if checklist_itens exists")
	}

	return exists, nil
}

// Exists checks if the ChecklistIten row exists.
func (o *ChecklistIten) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ChecklistItenExists(ctx, exec, o.CKLID, o.CkliItem)
}
