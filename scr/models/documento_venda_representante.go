// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// DocumentoVendaRepresentante is an object representing the database table.
type DocumentoVendaRepresentante struct {
	DDVID                  int           `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	EntID                  int           `boil:"ent_id" json:"ent_id" toml:"ent_id" yaml:"ent_id"`
	DDVRPercentualcomissao types.Decimal `boil:"ddvr_percentualcomissao" json:"ddvr_percentualcomissao" toml:"ddvr_percentualcomissao" yaml:"ddvr_percentualcomissao"`

	R *documentoVendaRepresentanteR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L documentoVendaRepresentanteL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DocumentoVendaRepresentanteColumns = struct {
	DDVID                  string
	EntID                  string
	DDVRPercentualcomissao string
}{
	DDVID:                  "ddv_id",
	EntID:                  "ent_id",
	DDVRPercentualcomissao: "ddvr_percentualcomissao",
}

var DocumentoVendaRepresentanteTableColumns = struct {
	DDVID                  string
	EntID                  string
	DDVRPercentualcomissao string
}{
	DDVID:                  "documento_venda_representante.ddv_id",
	EntID:                  "documento_venda_representante.ent_id",
	DDVRPercentualcomissao: "documento_venda_representante.ddvr_percentualcomissao",
}

// Generated where

var DocumentoVendaRepresentanteWhere = struct {
	DDVID                  whereHelperint
	EntID                  whereHelperint
	DDVRPercentualcomissao whereHelpertypes_Decimal
}{
	DDVID:                  whereHelperint{field: "`documento_venda_representante`.`ddv_id`"},
	EntID:                  whereHelperint{field: "`documento_venda_representante`.`ent_id`"},
	DDVRPercentualcomissao: whereHelpertypes_Decimal{field: "`documento_venda_representante`.`ddvr_percentualcomissao`"},
}

// DocumentoVendaRepresentanteRels is where relationship names are stored.
var DocumentoVendaRepresentanteRels = struct {
	DDV string
	Ent string
}{
	DDV: "DDV",
	Ent: "Ent",
}

// documentoVendaRepresentanteR is where relationships are stored.
type documentoVendaRepresentanteR struct {
	DDV *DocumentoVenda `boil:"DDV" json:"DDV" toml:"DDV" yaml:"DDV"`
	Ent *Entidade       `boil:"Ent" json:"Ent" toml:"Ent" yaml:"Ent"`
}

// NewStruct creates a new relationship struct
func (*documentoVendaRepresentanteR) NewStruct() *documentoVendaRepresentanteR {
	return &documentoVendaRepresentanteR{}
}

func (r *documentoVendaRepresentanteR) GetDDV() *DocumentoVenda {
	if r == nil {
		return nil
	}
	return r.DDV
}

func (r *documentoVendaRepresentanteR) GetEnt() *Entidade {
	if r == nil {
		return nil
	}
	return r.Ent
}

// documentoVendaRepresentanteL is where Load methods for each relationship are stored.
type documentoVendaRepresentanteL struct{}

var (
	documentoVendaRepresentanteAllColumns            = []string{"ddv_id", "ent_id", "ddvr_percentualcomissao"}
	documentoVendaRepresentanteColumnsWithoutDefault = []string{"ddv_id", "ent_id", "ddvr_percentualcomissao"}
	documentoVendaRepresentanteColumnsWithDefault    = []string{}
	documentoVendaRepresentantePrimaryKeyColumns     = []string{"ddv_id", "ent_id"}
	documentoVendaRepresentanteGeneratedColumns      = []string{}
)

type (
	// DocumentoVendaRepresentanteSlice is an alias for a slice of pointers to DocumentoVendaRepresentante.
	// This should almost always be used instead of []DocumentoVendaRepresentante.
	DocumentoVendaRepresentanteSlice []*DocumentoVendaRepresentante
	// DocumentoVendaRepresentanteHook is the signature for custom DocumentoVendaRepresentante hook methods
	DocumentoVendaRepresentanteHook func(context.Context, boil.ContextExecutor, *DocumentoVendaRepresentante) error

	documentoVendaRepresentanteQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	documentoVendaRepresentanteType                 = reflect.TypeOf(&DocumentoVendaRepresentante{})
	documentoVendaRepresentanteMapping              = queries.MakeStructMapping(documentoVendaRepresentanteType)
	documentoVendaRepresentantePrimaryKeyMapping, _ = queries.BindMapping(documentoVendaRepresentanteType, documentoVendaRepresentanteMapping, documentoVendaRepresentantePrimaryKeyColumns)
	documentoVendaRepresentanteInsertCacheMut       sync.RWMutex
	documentoVendaRepresentanteInsertCache          = make(map[string]insertCache)
	documentoVendaRepresentanteUpdateCacheMut       sync.RWMutex
	documentoVendaRepresentanteUpdateCache          = make(map[string]updateCache)
	documentoVendaRepresentanteUpsertCacheMut       sync.RWMutex
	documentoVendaRepresentanteUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var documentoVendaRepresentanteAfterSelectMu sync.Mutex
var documentoVendaRepresentanteAfterSelectHooks []DocumentoVendaRepresentanteHook

var documentoVendaRepresentanteBeforeInsertMu sync.Mutex
var documentoVendaRepresentanteBeforeInsertHooks []DocumentoVendaRepresentanteHook
var documentoVendaRepresentanteAfterInsertMu sync.Mutex
var documentoVendaRepresentanteAfterInsertHooks []DocumentoVendaRepresentanteHook

var documentoVendaRepresentanteBeforeUpdateMu sync.Mutex
var documentoVendaRepresentanteBeforeUpdateHooks []DocumentoVendaRepresentanteHook
var documentoVendaRepresentanteAfterUpdateMu sync.Mutex
var documentoVendaRepresentanteAfterUpdateHooks []DocumentoVendaRepresentanteHook

var documentoVendaRepresentanteBeforeDeleteMu sync.Mutex
var documentoVendaRepresentanteBeforeDeleteHooks []DocumentoVendaRepresentanteHook
var documentoVendaRepresentanteAfterDeleteMu sync.Mutex
var documentoVendaRepresentanteAfterDeleteHooks []DocumentoVendaRepresentanteHook

var documentoVendaRepresentanteBeforeUpsertMu sync.Mutex
var documentoVendaRepresentanteBeforeUpsertHooks []DocumentoVendaRepresentanteHook
var documentoVendaRepresentanteAfterUpsertMu sync.Mutex
var documentoVendaRepresentanteAfterUpsertHooks []DocumentoVendaRepresentanteHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DocumentoVendaRepresentante) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DocumentoVendaRepresentante) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DocumentoVendaRepresentante) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DocumentoVendaRepresentante) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DocumentoVendaRepresentante) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DocumentoVendaRepresentante) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DocumentoVendaRepresentante) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DocumentoVendaRepresentante) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DocumentoVendaRepresentante) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaRepresentanteAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDocumentoVendaRepresentanteHook registers your hook function for all future operations.
func AddDocumentoVendaRepresentanteHook(hookPoint boil.HookPoint, documentoVendaRepresentanteHook DocumentoVendaRepresentanteHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		documentoVendaRepresentanteAfterSelectMu.Lock()
		documentoVendaRepresentanteAfterSelectHooks = append(documentoVendaRepresentanteAfterSelectHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		documentoVendaRepresentanteBeforeInsertMu.Lock()
		documentoVendaRepresentanteBeforeInsertHooks = append(documentoVendaRepresentanteBeforeInsertHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		documentoVendaRepresentanteAfterInsertMu.Lock()
		documentoVendaRepresentanteAfterInsertHooks = append(documentoVendaRepresentanteAfterInsertHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		documentoVendaRepresentanteBeforeUpdateMu.Lock()
		documentoVendaRepresentanteBeforeUpdateHooks = append(documentoVendaRepresentanteBeforeUpdateHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		documentoVendaRepresentanteAfterUpdateMu.Lock()
		documentoVendaRepresentanteAfterUpdateHooks = append(documentoVendaRepresentanteAfterUpdateHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		documentoVendaRepresentanteBeforeDeleteMu.Lock()
		documentoVendaRepresentanteBeforeDeleteHooks = append(documentoVendaRepresentanteBeforeDeleteHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		documentoVendaRepresentanteAfterDeleteMu.Lock()
		documentoVendaRepresentanteAfterDeleteHooks = append(documentoVendaRepresentanteAfterDeleteHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		documentoVendaRepresentanteBeforeUpsertMu.Lock()
		documentoVendaRepresentanteBeforeUpsertHooks = append(documentoVendaRepresentanteBeforeUpsertHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		documentoVendaRepresentanteAfterUpsertMu.Lock()
		documentoVendaRepresentanteAfterUpsertHooks = append(documentoVendaRepresentanteAfterUpsertHooks, documentoVendaRepresentanteHook)
		documentoVendaRepresentanteAfterUpsertMu.Unlock()
	}
}

// One returns a single documentoVendaRepresentante record from the query.
func (q documentoVendaRepresentanteQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DocumentoVendaRepresentante, error) {
	o := &DocumentoVendaRepresentante{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for documento_venda_representante")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DocumentoVendaRepresentante records from the query.
func (q documentoVendaRepresentanteQuery) All(ctx context.Context, exec boil.ContextExecutor) (DocumentoVendaRepresentanteSlice, error) {
	var o []*DocumentoVendaRepresentante

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DocumentoVendaRepresentante slice")
	}

	if len(documentoVendaRepresentanteAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DocumentoVendaRepresentante records in the query.
func (q documentoVendaRepresentanteQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count documento_venda_representante rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q documentoVendaRepresentanteQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if documento_venda_representante exists")
	}

	return count > 0, nil
}

// DDV pointed to by the foreign key.
func (o *DocumentoVendaRepresentante) DDV(mods ...qm.QueryMod) documentoVendaQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ddv_id` = ?", o.DDVID),
	}

	queryMods = append(queryMods, mods...)

	return DocumentoVendas(queryMods...)
}

// Ent pointed to by the foreign key.
func (o *DocumentoVendaRepresentante) Ent(mods ...qm.QueryMod) entidadeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ent_id` = ?", o.EntID),
	}

	queryMods = append(queryMods, mods...)

	return Entidades(queryMods...)
}

// LoadDDV allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaRepresentanteL) LoadDDV(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaRepresentante interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaRepresentante
	var object *DocumentoVendaRepresentante

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaRepresentante.(*DocumentoVendaRepresentante)
		if !ok {
			object = new(DocumentoVendaRepresentante)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaRepresentante)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaRepresentante))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaRepresentante.(*[]*DocumentoVendaRepresentante)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaRepresentante)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaRepresentante))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaRepresentanteR{}
		}
		args[object.DDVID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaRepresentanteR{}
			}

			args[obj.DDVID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documento_venda`),
		qm.WhereIn(`documento_venda.ddv_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DocumentoVenda")
	}

	var resultSlice []*DocumentoVenda
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DocumentoVenda")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for documento_venda")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documento_venda")
	}

	if len(documentoVendaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.DDV = foreign
		if foreign.R == nil {
			foreign.R = &documentoVendaR{}
		}
		foreign.R.DDVDocumentoVendaRepresentantes = append(foreign.R.DDVDocumentoVendaRepresentantes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.DDVID == foreign.DDVID {
				local.R.DDV = foreign
				if foreign.R == nil {
					foreign.R = &documentoVendaR{}
				}
				foreign.R.DDVDocumentoVendaRepresentantes = append(foreign.R.DDVDocumentoVendaRepresentantes, local)
				break
			}
		}
	}

	return nil
}

// LoadEnt allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaRepresentanteL) LoadEnt(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaRepresentante interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaRepresentante
	var object *DocumentoVendaRepresentante

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaRepresentante.(*DocumentoVendaRepresentante)
		if !ok {
			object = new(DocumentoVendaRepresentante)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaRepresentante)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaRepresentante))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaRepresentante.(*[]*DocumentoVendaRepresentante)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaRepresentante)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaRepresentante))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaRepresentanteR{}
		}
		args[object.EntID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaRepresentanteR{}
			}

			args[obj.EntID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade`),
		qm.WhereIn(`entidade.ent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Entidade")
	}

	var resultSlice []*Entidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade")
	}

	if len(entidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ent = foreign
		if foreign.R == nil {
			foreign.R = &entidadeR{}
		}
		foreign.R.EntDocumentoVendaRepresentantes = append(foreign.R.EntDocumentoVendaRepresentantes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EntID == foreign.EntID {
				local.R.Ent = foreign
				if foreign.R == nil {
					foreign.R = &entidadeR{}
				}
				foreign.R.EntDocumentoVendaRepresentantes = append(foreign.R.EntDocumentoVendaRepresentantes, local)
				break
			}
		}
	}

	return nil
}

// SetDDV of the documentoVendaRepresentante to the related item.
// Sets o.R.DDV to related.
// Adds o to related.R.DDVDocumentoVendaRepresentantes.
func (o *DocumentoVendaRepresentante) SetDDV(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DocumentoVenda) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_representante` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ddv_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaRepresentantePrimaryKeyColumns),
	)
	values := []interface{}{related.DDVID, o.DDVID, o.EntID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.DDVID = related.DDVID
	if o.R == nil {
		o.R = &documentoVendaRepresentanteR{
			DDV: related,
		}
	} else {
		o.R.DDV = related
	}

	if related.R == nil {
		related.R = &documentoVendaR{
			DDVDocumentoVendaRepresentantes: DocumentoVendaRepresentanteSlice{o},
		}
	} else {
		related.R.DDVDocumentoVendaRepresentantes = append(related.R.DDVDocumentoVendaRepresentantes, o)
	}

	return nil
}

// SetEnt of the documentoVendaRepresentante to the related item.
// Sets o.R.Ent to related.
// Adds o to related.R.EntDocumentoVendaRepresentantes.
func (o *DocumentoVendaRepresentante) SetEnt(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Entidade) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_representante` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ent_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaRepresentantePrimaryKeyColumns),
	)
	values := []interface{}{related.EntID, o.DDVID, o.EntID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EntID = related.EntID
	if o.R == nil {
		o.R = &documentoVendaRepresentanteR{
			Ent: related,
		}
	} else {
		o.R.Ent = related
	}

	if related.R == nil {
		related.R = &entidadeR{
			EntDocumentoVendaRepresentantes: DocumentoVendaRepresentanteSlice{o},
		}
	} else {
		related.R.EntDocumentoVendaRepresentantes = append(related.R.EntDocumentoVendaRepresentantes, o)
	}

	return nil
}

// DocumentoVendaRepresentantes retrieves all the records using an executor.
func DocumentoVendaRepresentantes(mods ...qm.QueryMod) documentoVendaRepresentanteQuery {
	mods = append(mods, qm.From("`documento_venda_representante`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`documento_venda_representante`.*"})
	}

	return documentoVendaRepresentanteQuery{q}
}

// FindDocumentoVendaRepresentante retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDocumentoVendaRepresentante(ctx context.Context, exec boil.ContextExecutor, dDVID int, entID int, selectCols ...string) (*DocumentoVendaRepresentante, error) {
	documentoVendaRepresentanteObj := &DocumentoVendaRepresentante{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `documento_venda_representante` where `ddv_id`=? AND `ent_id`=?", sel,
	)

	q := queries.Raw(query, dDVID, entID)

	err := q.Bind(ctx, exec, documentoVendaRepresentanteObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from documento_venda_representante")
	}

	if err = documentoVendaRepresentanteObj.doAfterSelectHooks(ctx, exec); err != nil {
		return documentoVendaRepresentanteObj, err
	}

	return documentoVendaRepresentanteObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DocumentoVendaRepresentante) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_representante provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaRepresentanteColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	documentoVendaRepresentanteInsertCacheMut.RLock()
	cache, cached := documentoVendaRepresentanteInsertCache[key]
	documentoVendaRepresentanteInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			documentoVendaRepresentanteAllColumns,
			documentoVendaRepresentanteColumnsWithDefault,
			documentoVendaRepresentanteColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaRepresentanteType, documentoVendaRepresentanteMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(documentoVendaRepresentanteType, documentoVendaRepresentanteMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `documento_venda_representante` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `documento_venda_representante` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `documento_venda_representante` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, documentoVendaRepresentantePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into documento_venda_representante")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DDVID,
		o.EntID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_representante")
	}

CacheNoHooks:
	if !cached {
		documentoVendaRepresentanteInsertCacheMut.Lock()
		documentoVendaRepresentanteInsertCache[key] = cache
		documentoVendaRepresentanteInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DocumentoVendaRepresentante.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DocumentoVendaRepresentante) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	documentoVendaRepresentanteUpdateCacheMut.RLock()
	cache, cached := documentoVendaRepresentanteUpdateCache[key]
	documentoVendaRepresentanteUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			documentoVendaRepresentanteAllColumns,
			documentoVendaRepresentantePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update documento_venda_representante, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `documento_venda_representante` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, documentoVendaRepresentantePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(documentoVendaRepresentanteType, documentoVendaRepresentanteMapping, append(wl, documentoVendaRepresentantePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update documento_venda_representante row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for documento_venda_representante")
	}

	if !cached {
		documentoVendaRepresentanteUpdateCacheMut.Lock()
		documentoVendaRepresentanteUpdateCache[key] = cache
		documentoVendaRepresentanteUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q documentoVendaRepresentanteQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for documento_venda_representante")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for documento_venda_representante")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DocumentoVendaRepresentanteSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaRepresentantePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `documento_venda_representante` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaRepresentantePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in documentoVendaRepresentante slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all documentoVendaRepresentante")
	}
	return rowsAff, nil
}

var mySQLDocumentoVendaRepresentanteUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DocumentoVendaRepresentante) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_representante provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaRepresentanteColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDocumentoVendaRepresentanteUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	documentoVendaRepresentanteUpsertCacheMut.RLock()
	cache, cached := documentoVendaRepresentanteUpsertCache[key]
	documentoVendaRepresentanteUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			documentoVendaRepresentanteAllColumns,
			documentoVendaRepresentanteColumnsWithDefault,
			documentoVendaRepresentanteColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			documentoVendaRepresentanteAllColumns,
			documentoVendaRepresentantePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert documento_venda_representante, could not build update column list")
		}

		ret := strmangle.SetComplement(documentoVendaRepresentanteAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`documento_venda_representante`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `documento_venda_representante` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaRepresentanteType, documentoVendaRepresentanteMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(documentoVendaRepresentanteType, documentoVendaRepresentanteMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for documento_venda_representante")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(documentoVendaRepresentanteType, documentoVendaRepresentanteMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for documento_venda_representante")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_representante")
	}

CacheNoHooks:
	if !cached {
		documentoVendaRepresentanteUpsertCacheMut.Lock()
		documentoVendaRepresentanteUpsertCache[key] = cache
		documentoVendaRepresentanteUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DocumentoVendaRepresentante record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DocumentoVendaRepresentante) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DocumentoVendaRepresentante provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), documentoVendaRepresentantePrimaryKeyMapping)
	sql := "DELETE FROM `documento_venda_representante` WHERE `ddv_id`=? AND `ent_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from documento_venda_representante")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for documento_venda_representante")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q documentoVendaRepresentanteQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no documentoVendaRepresentanteQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documento_venda_representante")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_representante")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DocumentoVendaRepresentanteSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(documentoVendaRepresentanteBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaRepresentantePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `documento_venda_representante` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaRepresentantePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documentoVendaRepresentante slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_representante")
	}

	if len(documentoVendaRepresentanteAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DocumentoVendaRepresentante) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDocumentoVendaRepresentante(ctx, exec, o.DDVID, o.EntID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DocumentoVendaRepresentanteSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DocumentoVendaRepresentanteSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaRepresentantePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `documento_venda_representante`.* FROM `documento_venda_representante` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaRepresentantePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DocumentoVendaRepresentanteSlice")
	}

	*o = slice

	return nil
}

// DocumentoVendaRepresentanteExists checks if the DocumentoVendaRepresentante row exists.
func DocumentoVendaRepresentanteExists(ctx context.Context, exec boil.ContextExecutor, dDVID int, entID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `documento_venda_representante` where `ddv_id`=? AND `ent_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, dDVID, entID)
	}
	row := exec.QueryRowContext(ctx, sql, dDVID, entID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if documento_venda_representante exists")
	}

	return exists, nil
}

// Exists checks if the DocumentoVendaRepresentante row exists.
func (o *DocumentoVendaRepresentante) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DocumentoVendaRepresentanteExists(ctx, exec, o.DDVID, o.EntID)
}
