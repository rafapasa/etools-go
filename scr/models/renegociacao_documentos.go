// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// RenegociacaoDocumento is an object representing the database table.
type RenegociacaoDocumento struct {
	RengID              int      `boil:"reng_id" json:"reng_id" toml:"reng_id" yaml:"reng_id"`
	RemgdocItem         int      `boil:"remgdoc_item" json:"remgdoc_item" toml:"remgdoc_item" yaml:"remgdoc_item"`
	RocID               null.Int `boil:"roc_id" json:"roc_id,omitempty" toml:"roc_id" yaml:"roc_id,omitempty"`
	RengdocIddocgerador int      `boil:"rengdoc_iddocgerador" json:"rengdoc_iddocgerador" toml:"rengdoc_iddocgerador" yaml:"rengdoc_iddocgerador"`
	// 5 - titulos
	//             7 - chaques
	RengdocOrigem         int16             `boil:"rengdoc_origem" json:"rengdoc_origem" toml:"rengdoc_origem" yaml:"rengdoc_origem"`
	RengdocValordocumento types.Decimal     `boil:"rengdoc_valordocumento" json:"rengdoc_valordocumento" toml:"rengdoc_valordocumento" yaml:"rengdoc_valordocumento"`
	RengdocDatavencimento time.Time         `boil:"rengdoc_datavencimento" json:"rengdoc_datavencimento" toml:"rengdoc_datavencimento" yaml:"rengdoc_datavencimento"`
	RengdocValorjuros     types.NullDecimal `boil:"rengdoc_valorjuros" json:"rengdoc_valorjuros,omitempty" toml:"rengdoc_valorjuros" yaml:"rengdoc_valorjuros,omitempty"`

	R *renegociacaoDocumentoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L renegociacaoDocumentoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RenegociacaoDocumentoColumns = struct {
	RengID                string
	RemgdocItem           string
	RocID                 string
	RengdocIddocgerador   string
	RengdocOrigem         string
	RengdocValordocumento string
	RengdocDatavencimento string
	RengdocValorjuros     string
}{
	RengID:                "reng_id",
	RemgdocItem:           "remgdoc_item",
	RocID:                 "roc_id",
	RengdocIddocgerador:   "rengdoc_iddocgerador",
	RengdocOrigem:         "rengdoc_origem",
	RengdocValordocumento: "rengdoc_valordocumento",
	RengdocDatavencimento: "rengdoc_datavencimento",
	RengdocValorjuros:     "rengdoc_valorjuros",
}

var RenegociacaoDocumentoTableColumns = struct {
	RengID                string
	RemgdocItem           string
	RocID                 string
	RengdocIddocgerador   string
	RengdocOrigem         string
	RengdocValordocumento string
	RengdocDatavencimento string
	RengdocValorjuros     string
}{
	RengID:                "renegociacao_documentos.reng_id",
	RemgdocItem:           "renegociacao_documentos.remgdoc_item",
	RocID:                 "renegociacao_documentos.roc_id",
	RengdocIddocgerador:   "renegociacao_documentos.rengdoc_iddocgerador",
	RengdocOrigem:         "renegociacao_documentos.rengdoc_origem",
	RengdocValordocumento: "renegociacao_documentos.rengdoc_valordocumento",
	RengdocDatavencimento: "renegociacao_documentos.rengdoc_datavencimento",
	RengdocValorjuros:     "renegociacao_documentos.rengdoc_valorjuros",
}

// Generated where

var RenegociacaoDocumentoWhere = struct {
	RengID                whereHelperint
	RemgdocItem           whereHelperint
	RocID                 whereHelpernull_Int
	RengdocIddocgerador   whereHelperint
	RengdocOrigem         whereHelperint16
	RengdocValordocumento whereHelpertypes_Decimal
	RengdocDatavencimento whereHelpertime_Time
	RengdocValorjuros     whereHelpertypes_NullDecimal
}{
	RengID:                whereHelperint{field: "`renegociacao_documentos`.`reng_id`"},
	RemgdocItem:           whereHelperint{field: "`renegociacao_documentos`.`remgdoc_item`"},
	RocID:                 whereHelpernull_Int{field: "`renegociacao_documentos`.`roc_id`"},
	RengdocIddocgerador:   whereHelperint{field: "`renegociacao_documentos`.`rengdoc_iddocgerador`"},
	RengdocOrigem:         whereHelperint16{field: "`renegociacao_documentos`.`rengdoc_origem`"},
	RengdocValordocumento: whereHelpertypes_Decimal{field: "`renegociacao_documentos`.`rengdoc_valordocumento`"},
	RengdocDatavencimento: whereHelpertime_Time{field: "`renegociacao_documentos`.`rengdoc_datavencimento`"},
	RengdocValorjuros:     whereHelpertypes_NullDecimal{field: "`renegociacao_documentos`.`rengdoc_valorjuros`"},
}

// RenegociacaoDocumentoRels is where relationship names are stored.
var RenegociacaoDocumentoRels = struct {
	Reng string
	Roc  string
}{
	Reng: "Reng",
	Roc:  "Roc",
}

// renegociacaoDocumentoR is where relationships are stored.
type renegociacaoDocumentoR struct {
	Reng *Renegociacao   `boil:"Reng" json:"Reng" toml:"Reng" yaml:"Reng"`
	Roc  *RotinaContabil `boil:"Roc" json:"Roc" toml:"Roc" yaml:"Roc"`
}

// NewStruct creates a new relationship struct
func (*renegociacaoDocumentoR) NewStruct() *renegociacaoDocumentoR {
	return &renegociacaoDocumentoR{}
}

func (r *renegociacaoDocumentoR) GetReng() *Renegociacao {
	if r == nil {
		return nil
	}
	return r.Reng
}

func (r *renegociacaoDocumentoR) GetRoc() *RotinaContabil {
	if r == nil {
		return nil
	}
	return r.Roc
}

// renegociacaoDocumentoL is where Load methods for each relationship are stored.
type renegociacaoDocumentoL struct{}

var (
	renegociacaoDocumentoAllColumns            = []string{"reng_id", "remgdoc_item", "roc_id", "rengdoc_iddocgerador", "rengdoc_origem", "rengdoc_valordocumento", "rengdoc_datavencimento", "rengdoc_valorjuros"}
	renegociacaoDocumentoColumnsWithoutDefault = []string{"reng_id", "remgdoc_item", "roc_id", "rengdoc_iddocgerador", "rengdoc_origem", "rengdoc_valordocumento", "rengdoc_datavencimento", "rengdoc_valorjuros"}
	renegociacaoDocumentoColumnsWithDefault    = []string{}
	renegociacaoDocumentoPrimaryKeyColumns     = []string{"reng_id", "remgdoc_item"}
	renegociacaoDocumentoGeneratedColumns      = []string{}
)

type (
	// RenegociacaoDocumentoSlice is an alias for a slice of pointers to RenegociacaoDocumento.
	// This should almost always be used instead of []RenegociacaoDocumento.
	RenegociacaoDocumentoSlice []*RenegociacaoDocumento
	// RenegociacaoDocumentoHook is the signature for custom RenegociacaoDocumento hook methods
	RenegociacaoDocumentoHook func(context.Context, boil.ContextExecutor, *RenegociacaoDocumento) error

	renegociacaoDocumentoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	renegociacaoDocumentoType                 = reflect.TypeOf(&RenegociacaoDocumento{})
	renegociacaoDocumentoMapping              = queries.MakeStructMapping(renegociacaoDocumentoType)
	renegociacaoDocumentoPrimaryKeyMapping, _ = queries.BindMapping(renegociacaoDocumentoType, renegociacaoDocumentoMapping, renegociacaoDocumentoPrimaryKeyColumns)
	renegociacaoDocumentoInsertCacheMut       sync.RWMutex
	renegociacaoDocumentoInsertCache          = make(map[string]insertCache)
	renegociacaoDocumentoUpdateCacheMut       sync.RWMutex
	renegociacaoDocumentoUpdateCache          = make(map[string]updateCache)
	renegociacaoDocumentoUpsertCacheMut       sync.RWMutex
	renegociacaoDocumentoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var renegociacaoDocumentoAfterSelectMu sync.Mutex
var renegociacaoDocumentoAfterSelectHooks []RenegociacaoDocumentoHook

var renegociacaoDocumentoBeforeInsertMu sync.Mutex
var renegociacaoDocumentoBeforeInsertHooks []RenegociacaoDocumentoHook
var renegociacaoDocumentoAfterInsertMu sync.Mutex
var renegociacaoDocumentoAfterInsertHooks []RenegociacaoDocumentoHook

var renegociacaoDocumentoBeforeUpdateMu sync.Mutex
var renegociacaoDocumentoBeforeUpdateHooks []RenegociacaoDocumentoHook
var renegociacaoDocumentoAfterUpdateMu sync.Mutex
var renegociacaoDocumentoAfterUpdateHooks []RenegociacaoDocumentoHook

var renegociacaoDocumentoBeforeDeleteMu sync.Mutex
var renegociacaoDocumentoBeforeDeleteHooks []RenegociacaoDocumentoHook
var renegociacaoDocumentoAfterDeleteMu sync.Mutex
var renegociacaoDocumentoAfterDeleteHooks []RenegociacaoDocumentoHook

var renegociacaoDocumentoBeforeUpsertMu sync.Mutex
var renegociacaoDocumentoBeforeUpsertHooks []RenegociacaoDocumentoHook
var renegociacaoDocumentoAfterUpsertMu sync.Mutex
var renegociacaoDocumentoAfterUpsertHooks []RenegociacaoDocumentoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RenegociacaoDocumento) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RenegociacaoDocumento) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RenegociacaoDocumento) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RenegociacaoDocumento) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RenegociacaoDocumento) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RenegociacaoDocumento) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RenegociacaoDocumento) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RenegociacaoDocumento) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RenegociacaoDocumento) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range renegociacaoDocumentoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRenegociacaoDocumentoHook registers your hook function for all future operations.
func AddRenegociacaoDocumentoHook(hookPoint boil.HookPoint, renegociacaoDocumentoHook RenegociacaoDocumentoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		renegociacaoDocumentoAfterSelectMu.Lock()
		renegociacaoDocumentoAfterSelectHooks = append(renegociacaoDocumentoAfterSelectHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		renegociacaoDocumentoBeforeInsertMu.Lock()
		renegociacaoDocumentoBeforeInsertHooks = append(renegociacaoDocumentoBeforeInsertHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		renegociacaoDocumentoAfterInsertMu.Lock()
		renegociacaoDocumentoAfterInsertHooks = append(renegociacaoDocumentoAfterInsertHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		renegociacaoDocumentoBeforeUpdateMu.Lock()
		renegociacaoDocumentoBeforeUpdateHooks = append(renegociacaoDocumentoBeforeUpdateHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		renegociacaoDocumentoAfterUpdateMu.Lock()
		renegociacaoDocumentoAfterUpdateHooks = append(renegociacaoDocumentoAfterUpdateHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		renegociacaoDocumentoBeforeDeleteMu.Lock()
		renegociacaoDocumentoBeforeDeleteHooks = append(renegociacaoDocumentoBeforeDeleteHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		renegociacaoDocumentoAfterDeleteMu.Lock()
		renegociacaoDocumentoAfterDeleteHooks = append(renegociacaoDocumentoAfterDeleteHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		renegociacaoDocumentoBeforeUpsertMu.Lock()
		renegociacaoDocumentoBeforeUpsertHooks = append(renegociacaoDocumentoBeforeUpsertHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		renegociacaoDocumentoAfterUpsertMu.Lock()
		renegociacaoDocumentoAfterUpsertHooks = append(renegociacaoDocumentoAfterUpsertHooks, renegociacaoDocumentoHook)
		renegociacaoDocumentoAfterUpsertMu.Unlock()
	}
}

// One returns a single renegociacaoDocumento record from the query.
func (q renegociacaoDocumentoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RenegociacaoDocumento, error) {
	o := &RenegociacaoDocumento{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for renegociacao_documentos")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RenegociacaoDocumento records from the query.
func (q renegociacaoDocumentoQuery) All(ctx context.Context, exec boil.ContextExecutor) (RenegociacaoDocumentoSlice, error) {
	var o []*RenegociacaoDocumento

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RenegociacaoDocumento slice")
	}

	if len(renegociacaoDocumentoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RenegociacaoDocumento records in the query.
func (q renegociacaoDocumentoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count renegociacao_documentos rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q renegociacaoDocumentoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if renegociacao_documentos exists")
	}

	return count > 0, nil
}

// Reng pointed to by the foreign key.
func (o *RenegociacaoDocumento) Reng(mods ...qm.QueryMod) renegociacaoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`reng_id` = ?", o.RengID),
	}

	queryMods = append(queryMods, mods...)

	return Renegociacaos(queryMods...)
}

// Roc pointed to by the foreign key.
func (o *RenegociacaoDocumento) Roc(mods ...qm.QueryMod) rotinaContabilQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`roc_id` = ?", o.RocID),
	}

	queryMods = append(queryMods, mods...)

	return RotinaContabils(queryMods...)
}

// LoadReng allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (renegociacaoDocumentoL) LoadReng(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRenegociacaoDocumento interface{}, mods queries.Applicator) error {
	var slice []*RenegociacaoDocumento
	var object *RenegociacaoDocumento

	if singular {
		var ok bool
		object, ok = maybeRenegociacaoDocumento.(*RenegociacaoDocumento)
		if !ok {
			object = new(RenegociacaoDocumento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRenegociacaoDocumento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRenegociacaoDocumento))
			}
		}
	} else {
		s, ok := maybeRenegociacaoDocumento.(*[]*RenegociacaoDocumento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRenegociacaoDocumento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRenegociacaoDocumento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &renegociacaoDocumentoR{}
		}
		args[object.RengID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &renegociacaoDocumentoR{}
			}

			args[obj.RengID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`renegociacao`),
		qm.WhereIn(`renegociacao.reng_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Renegociacao")
	}

	var resultSlice []*Renegociacao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Renegociacao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for renegociacao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for renegociacao")
	}

	if len(renegociacaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Reng = foreign
		if foreign.R == nil {
			foreign.R = &renegociacaoR{}
		}
		foreign.R.RengRenegociacaoDocumentos = append(foreign.R.RengRenegociacaoDocumentos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.RengID == foreign.RengID {
				local.R.Reng = foreign
				if foreign.R == nil {
					foreign.R = &renegociacaoR{}
				}
				foreign.R.RengRenegociacaoDocumentos = append(foreign.R.RengRenegociacaoDocumentos, local)
				break
			}
		}
	}

	return nil
}

// LoadRoc allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (renegociacaoDocumentoL) LoadRoc(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRenegociacaoDocumento interface{}, mods queries.Applicator) error {
	var slice []*RenegociacaoDocumento
	var object *RenegociacaoDocumento

	if singular {
		var ok bool
		object, ok = maybeRenegociacaoDocumento.(*RenegociacaoDocumento)
		if !ok {
			object = new(RenegociacaoDocumento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRenegociacaoDocumento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRenegociacaoDocumento))
			}
		}
	} else {
		s, ok := maybeRenegociacaoDocumento.(*[]*RenegociacaoDocumento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRenegociacaoDocumento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRenegociacaoDocumento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &renegociacaoDocumentoR{}
		}
		if !queries.IsNil(object.RocID) {
			args[object.RocID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &renegociacaoDocumentoR{}
			}

			if !queries.IsNil(obj.RocID) {
				args[obj.RocID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil`),
		qm.WhereIn(`rotina_contabil.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RotinaContabil")
	}

	var resultSlice []*RotinaContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RotinaContabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for rotina_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil")
	}

	if len(rotinaContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Roc = foreign
		if foreign.R == nil {
			foreign.R = &rotinaContabilR{}
		}
		foreign.R.RocRenegociacaoDocumentos = append(foreign.R.RocRenegociacaoDocumentos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.Roc = foreign
				if foreign.R == nil {
					foreign.R = &rotinaContabilR{}
				}
				foreign.R.RocRenegociacaoDocumentos = append(foreign.R.RocRenegociacaoDocumentos, local)
				break
			}
		}
	}

	return nil
}

// SetReng of the renegociacaoDocumento to the related item.
// Sets o.R.Reng to related.
// Adds o to related.R.RengRenegociacaoDocumentos.
func (o *RenegociacaoDocumento) SetReng(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Renegociacao) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `renegociacao_documentos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"reng_id"}),
		strmangle.WhereClause("`", "`", 0, renegociacaoDocumentoPrimaryKeyColumns),
	)
	values := []interface{}{related.RengID, o.RengID, o.RemgdocItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.RengID = related.RengID
	if o.R == nil {
		o.R = &renegociacaoDocumentoR{
			Reng: related,
		}
	} else {
		o.R.Reng = related
	}

	if related.R == nil {
		related.R = &renegociacaoR{
			RengRenegociacaoDocumentos: RenegociacaoDocumentoSlice{o},
		}
	} else {
		related.R.RengRenegociacaoDocumentos = append(related.R.RengRenegociacaoDocumentos, o)
	}

	return nil
}

// SetRoc of the renegociacaoDocumento to the related item.
// Sets o.R.Roc to related.
// Adds o to related.R.RocRenegociacaoDocumentos.
func (o *RenegociacaoDocumento) SetRoc(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RotinaContabil) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `renegociacao_documentos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
		strmangle.WhereClause("`", "`", 0, renegociacaoDocumentoPrimaryKeyColumns),
	)
	values := []interface{}{related.RocID, o.RengID, o.RemgdocItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RocID, related.RocID)
	if o.R == nil {
		o.R = &renegociacaoDocumentoR{
			Roc: related,
		}
	} else {
		o.R.Roc = related
	}

	if related.R == nil {
		related.R = &rotinaContabilR{
			RocRenegociacaoDocumentos: RenegociacaoDocumentoSlice{o},
		}
	} else {
		related.R.RocRenegociacaoDocumentos = append(related.R.RocRenegociacaoDocumentos, o)
	}

	return nil
}

// RemoveRoc relationship.
// Sets o.R.Roc to nil.
// Removes o from all passed in related items' relationships struct.
func (o *RenegociacaoDocumento) RemoveRoc(ctx context.Context, exec boil.ContextExecutor, related *RotinaContabil) error {
	var err error

	queries.SetScanner(&o.RocID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Roc = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RocRenegociacaoDocumentos {
		if queries.Equal(o.RocID, ri.RocID) {
			continue
		}

		ln := len(related.R.RocRenegociacaoDocumentos)
		if ln > 1 && i < ln-1 {
			related.R.RocRenegociacaoDocumentos[i] = related.R.RocRenegociacaoDocumentos[ln-1]
		}
		related.R.RocRenegociacaoDocumentos = related.R.RocRenegociacaoDocumentos[:ln-1]
		break
	}
	return nil
}

// RenegociacaoDocumentos retrieves all the records using an executor.
func RenegociacaoDocumentos(mods ...qm.QueryMod) renegociacaoDocumentoQuery {
	mods = append(mods, qm.From("`renegociacao_documentos`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`renegociacao_documentos`.*"})
	}

	return renegociacaoDocumentoQuery{q}
}

// FindRenegociacaoDocumento retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRenegociacaoDocumento(ctx context.Context, exec boil.ContextExecutor, rengID int, remgdocItem int, selectCols ...string) (*RenegociacaoDocumento, error) {
	renegociacaoDocumentoObj := &RenegociacaoDocumento{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `renegociacao_documentos` where `reng_id`=? AND `remgdoc_item`=?", sel,
	)

	q := queries.Raw(query, rengID, remgdocItem)

	err := q.Bind(ctx, exec, renegociacaoDocumentoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from renegociacao_documentos")
	}

	if err = renegociacaoDocumentoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return renegociacaoDocumentoObj, err
	}

	return renegociacaoDocumentoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RenegociacaoDocumento) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no renegociacao_documentos provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(renegociacaoDocumentoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	renegociacaoDocumentoInsertCacheMut.RLock()
	cache, cached := renegociacaoDocumentoInsertCache[key]
	renegociacaoDocumentoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			renegociacaoDocumentoAllColumns,
			renegociacaoDocumentoColumnsWithDefault,
			renegociacaoDocumentoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(renegociacaoDocumentoType, renegociacaoDocumentoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(renegociacaoDocumentoType, renegociacaoDocumentoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `renegociacao_documentos` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `renegociacao_documentos` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `renegociacao_documentos` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, renegociacaoDocumentoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into renegociacao_documentos")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.RengID,
		o.RemgdocItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for renegociacao_documentos")
	}

CacheNoHooks:
	if !cached {
		renegociacaoDocumentoInsertCacheMut.Lock()
		renegociacaoDocumentoInsertCache[key] = cache
		renegociacaoDocumentoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RenegociacaoDocumento.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RenegociacaoDocumento) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	renegociacaoDocumentoUpdateCacheMut.RLock()
	cache, cached := renegociacaoDocumentoUpdateCache[key]
	renegociacaoDocumentoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			renegociacaoDocumentoAllColumns,
			renegociacaoDocumentoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update renegociacao_documentos, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `renegociacao_documentos` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, renegociacaoDocumentoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(renegociacaoDocumentoType, renegociacaoDocumentoMapping, append(wl, renegociacaoDocumentoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update renegociacao_documentos row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for renegociacao_documentos")
	}

	if !cached {
		renegociacaoDocumentoUpdateCacheMut.Lock()
		renegociacaoDocumentoUpdateCache[key] = cache
		renegociacaoDocumentoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q renegociacaoDocumentoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for renegociacao_documentos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for renegociacao_documentos")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RenegociacaoDocumentoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), renegociacaoDocumentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `renegociacao_documentos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, renegociacaoDocumentoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in renegociacaoDocumento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all renegociacaoDocumento")
	}
	return rowsAff, nil
}

var mySQLRenegociacaoDocumentoUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RenegociacaoDocumento) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no renegociacao_documentos provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(renegociacaoDocumentoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRenegociacaoDocumentoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	renegociacaoDocumentoUpsertCacheMut.RLock()
	cache, cached := renegociacaoDocumentoUpsertCache[key]
	renegociacaoDocumentoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			renegociacaoDocumentoAllColumns,
			renegociacaoDocumentoColumnsWithDefault,
			renegociacaoDocumentoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			renegociacaoDocumentoAllColumns,
			renegociacaoDocumentoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert renegociacao_documentos, could not build update column list")
		}

		ret := strmangle.SetComplement(renegociacaoDocumentoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`renegociacao_documentos`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `renegociacao_documentos` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(renegociacaoDocumentoType, renegociacaoDocumentoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(renegociacaoDocumentoType, renegociacaoDocumentoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for renegociacao_documentos")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(renegociacaoDocumentoType, renegociacaoDocumentoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for renegociacao_documentos")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for renegociacao_documentos")
	}

CacheNoHooks:
	if !cached {
		renegociacaoDocumentoUpsertCacheMut.Lock()
		renegociacaoDocumentoUpsertCache[key] = cache
		renegociacaoDocumentoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RenegociacaoDocumento record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RenegociacaoDocumento) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RenegociacaoDocumento provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), renegociacaoDocumentoPrimaryKeyMapping)
	sql := "DELETE FROM `renegociacao_documentos` WHERE `reng_id`=? AND `remgdoc_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from renegociacao_documentos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for renegociacao_documentos")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q renegociacaoDocumentoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no renegociacaoDocumentoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from renegociacao_documentos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for renegociacao_documentos")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RenegociacaoDocumentoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(renegociacaoDocumentoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), renegociacaoDocumentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `renegociacao_documentos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, renegociacaoDocumentoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from renegociacaoDocumento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for renegociacao_documentos")
	}

	if len(renegociacaoDocumentoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RenegociacaoDocumento) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRenegociacaoDocumento(ctx, exec, o.RengID, o.RemgdocItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RenegociacaoDocumentoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RenegociacaoDocumentoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), renegociacaoDocumentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `renegociacao_documentos`.* FROM `renegociacao_documentos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, renegociacaoDocumentoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RenegociacaoDocumentoSlice")
	}

	*o = slice

	return nil
}

// RenegociacaoDocumentoExists checks if the RenegociacaoDocumento row exists.
func RenegociacaoDocumentoExists(ctx context.Context, exec boil.ContextExecutor, rengID int, remgdocItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `renegociacao_documentos` where `reng_id`=? AND `remgdoc_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, rengID, remgdocItem)
	}
	row := exec.QueryRowContext(ctx, sql, rengID, remgdocItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if renegociacao_documentos exists")
	}

	return exists, nil
}

// Exists checks if the RenegociacaoDocumento row exists.
func (o *RenegociacaoDocumento) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RenegociacaoDocumentoExists(ctx, exec, o.RengID, o.RemgdocItem)
}
