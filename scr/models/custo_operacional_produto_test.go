// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testCustoOperacionalProdutos(t *testing.T) {
	t.Parallel()

	query := CustoOperacionalProdutos()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testCustoOperacionalProdutosDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCustoOperacionalProdutosQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := CustoOperacionalProdutos().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCustoOperacionalProdutosSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CustoOperacionalProdutoSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCustoOperacionalProdutosExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := CustoOperacionalProdutoExists(ctx, tx, o.CopID, o.DespID)
	if err != nil {
		t.Errorf("Unable to check if CustoOperacionalProduto exists: %s", err)
	}
	if !e {
		t.Errorf("Expected CustoOperacionalProdutoExists to return true, but got false.")
	}
}

func testCustoOperacionalProdutosFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	custoOperacionalProdutoFound, err := FindCustoOperacionalProduto(ctx, tx, o.CopID, o.DespID)
	if err != nil {
		t.Error(err)
	}

	if custoOperacionalProdutoFound == nil {
		t.Error("want a record, got nil")
	}
}

func testCustoOperacionalProdutosBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = CustoOperacionalProdutos().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testCustoOperacionalProdutosOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := CustoOperacionalProdutos().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testCustoOperacionalProdutosAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	custoOperacionalProdutoOne := &CustoOperacionalProduto{}
	custoOperacionalProdutoTwo := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, custoOperacionalProdutoOne, custoOperacionalProdutoDBTypes, false, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}
	if err = randomize.Struct(seed, custoOperacionalProdutoTwo, custoOperacionalProdutoDBTypes, false, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = custoOperacionalProdutoOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = custoOperacionalProdutoTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := CustoOperacionalProdutos().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testCustoOperacionalProdutosCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	custoOperacionalProdutoOne := &CustoOperacionalProduto{}
	custoOperacionalProdutoTwo := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, custoOperacionalProdutoOne, custoOperacionalProdutoDBTypes, false, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}
	if err = randomize.Struct(seed, custoOperacionalProdutoTwo, custoOperacionalProdutoDBTypes, false, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = custoOperacionalProdutoOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = custoOperacionalProdutoTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func custoOperacionalProdutoBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func custoOperacionalProdutoAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func custoOperacionalProdutoAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func custoOperacionalProdutoBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func custoOperacionalProdutoAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func custoOperacionalProdutoBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func custoOperacionalProdutoAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func custoOperacionalProdutoBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func custoOperacionalProdutoAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *CustoOperacionalProduto) error {
	*o = CustoOperacionalProduto{}
	return nil
}

func testCustoOperacionalProdutosHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &CustoOperacionalProduto{}
	o := &CustoOperacionalProduto{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, false); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto object: %s", err)
	}

	AddCustoOperacionalProdutoHook(boil.BeforeInsertHook, custoOperacionalProdutoBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoBeforeInsertHooks = []CustoOperacionalProdutoHook{}

	AddCustoOperacionalProdutoHook(boil.AfterInsertHook, custoOperacionalProdutoAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoAfterInsertHooks = []CustoOperacionalProdutoHook{}

	AddCustoOperacionalProdutoHook(boil.AfterSelectHook, custoOperacionalProdutoAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoAfterSelectHooks = []CustoOperacionalProdutoHook{}

	AddCustoOperacionalProdutoHook(boil.BeforeUpdateHook, custoOperacionalProdutoBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoBeforeUpdateHooks = []CustoOperacionalProdutoHook{}

	AddCustoOperacionalProdutoHook(boil.AfterUpdateHook, custoOperacionalProdutoAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoAfterUpdateHooks = []CustoOperacionalProdutoHook{}

	AddCustoOperacionalProdutoHook(boil.BeforeDeleteHook, custoOperacionalProdutoBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoBeforeDeleteHooks = []CustoOperacionalProdutoHook{}

	AddCustoOperacionalProdutoHook(boil.AfterDeleteHook, custoOperacionalProdutoAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoAfterDeleteHooks = []CustoOperacionalProdutoHook{}

	AddCustoOperacionalProdutoHook(boil.BeforeUpsertHook, custoOperacionalProdutoBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoBeforeUpsertHooks = []CustoOperacionalProdutoHook{}

	AddCustoOperacionalProdutoHook(boil.AfterUpsertHook, custoOperacionalProdutoAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	custoOperacionalProdutoAfterUpsertHooks = []CustoOperacionalProdutoHook{}
}

func testCustoOperacionalProdutosInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCustoOperacionalProdutosInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(custoOperacionalProdutoColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCustoOperacionalProdutoToOneCustoOperacionalUsingCop(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local CustoOperacionalProduto
	var foreign CustoOperacional

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, custoOperacionalProdutoDBTypes, false, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, custoOperacionalDBTypes, false, custoOperacionalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacional struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.CopID = foreign.CopID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Cop().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.CopID != foreign.CopID {
		t.Errorf("want: %v, got %v", foreign.CopID, check.CopID)
	}

	ranAfterSelectHook := false
	AddCustoOperacionalHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *CustoOperacional) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := CustoOperacionalProdutoSlice{&local}
	if err = local.L.LoadCop(ctx, tx, false, (*[]*CustoOperacionalProduto)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Cop == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Cop = nil
	if err = local.L.LoadCop(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Cop == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testCustoOperacionalProdutoToOneDespesasUsingDesp(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local CustoOperacionalProduto
	var foreign Despesas

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, custoOperacionalProdutoDBTypes, false, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, despesasDBTypes, false, despesasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Despesas struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.DespID = foreign.DespID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Desp().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.DespID != foreign.DespID {
		t.Errorf("want: %v, got %v", foreign.DespID, check.DespID)
	}

	ranAfterSelectHook := false
	AddDespesasHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Despesas) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := CustoOperacionalProdutoSlice{&local}
	if err = local.L.LoadDesp(ctx, tx, false, (*[]*CustoOperacionalProduto)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Desp == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Desp = nil
	if err = local.L.LoadDesp(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Desp == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testCustoOperacionalProdutoToOneSetOpCustoOperacionalUsingCop(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a CustoOperacionalProduto
	var b, c CustoOperacional

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, custoOperacionalProdutoDBTypes, false, strmangle.SetComplement(custoOperacionalProdutoPrimaryKeyColumns, custoOperacionalProdutoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, custoOperacionalDBTypes, false, strmangle.SetComplement(custoOperacionalPrimaryKeyColumns, custoOperacionalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, custoOperacionalDBTypes, false, strmangle.SetComplement(custoOperacionalPrimaryKeyColumns, custoOperacionalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*CustoOperacional{&b, &c} {
		err = a.SetCop(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Cop != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.CopCustoOperacionalProdutos[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.CopID != x.CopID {
			t.Error("foreign key was wrong value", a.CopID)
		}

		if exists, err := CustoOperacionalProdutoExists(ctx, tx, a.CopID, a.DespID); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}
func testCustoOperacionalProdutoToOneSetOpDespesasUsingDesp(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a CustoOperacionalProduto
	var b, c Despesas

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, custoOperacionalProdutoDBTypes, false, strmangle.SetComplement(custoOperacionalProdutoPrimaryKeyColumns, custoOperacionalProdutoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, despesasDBTypes, false, strmangle.SetComplement(despesasPrimaryKeyColumns, despesasColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, despesasDBTypes, false, strmangle.SetComplement(despesasPrimaryKeyColumns, despesasColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Despesas{&b, &c} {
		err = a.SetDesp(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Desp != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.DespCustoOperacionalProdutos[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.DespID != x.DespID {
			t.Error("foreign key was wrong value", a.DespID)
		}

		if exists, err := CustoOperacionalProdutoExists(ctx, tx, a.CopID, a.DespID); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}

func testCustoOperacionalProdutosReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testCustoOperacionalProdutosReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CustoOperacionalProdutoSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testCustoOperacionalProdutosSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := CustoOperacionalProdutos().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	custoOperacionalProdutoDBTypes = map[string]string{`CopID`: `int`, `DespID`: `int`, `CoppValor`: `decimal`}
	_                              = bytes.MinRead
)

func testCustoOperacionalProdutosUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(custoOperacionalProdutoPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(custoOperacionalProdutoAllColumns) == len(custoOperacionalProdutoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testCustoOperacionalProdutosSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(custoOperacionalProdutoAllColumns) == len(custoOperacionalProdutoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &CustoOperacionalProduto{}
	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, custoOperacionalProdutoDBTypes, true, custoOperacionalProdutoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(custoOperacionalProdutoAllColumns, custoOperacionalProdutoPrimaryKeyColumns) {
		fields = custoOperacionalProdutoAllColumns
	} else {
		fields = strmangle.SetComplement(
			custoOperacionalProdutoAllColumns,
			custoOperacionalProdutoPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := CustoOperacionalProdutoSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testCustoOperacionalProdutosUpsert(t *testing.T) {
	t.Parallel()

	if len(custoOperacionalProdutoAllColumns) == len(custoOperacionalProdutoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLCustoOperacionalProdutoUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := CustoOperacionalProduto{}
	if err = randomize.Struct(seed, &o, custoOperacionalProdutoDBTypes, false); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert CustoOperacionalProduto: %s", err)
	}

	count, err := CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, custoOperacionalProdutoDBTypes, false, custoOperacionalProdutoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CustoOperacionalProduto struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert CustoOperacionalProduto: %s", err)
	}

	count, err = CustoOperacionalProdutos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
