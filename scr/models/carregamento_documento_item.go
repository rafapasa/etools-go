// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CarregamentoDocumentoItem is an object representing the database table.
type CarregamentoDocumentoItem struct {
	DDVID                 int               `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	CarID                 int               `boil:"car_id" json:"car_id" toml:"car_id" yaml:"car_id"`
	ProID                 int               `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	CardciQuantsolicitada types.Decimal     `boil:"cardci_quantsolicitada" json:"cardci_quantsolicitada" toml:"cardci_quantsolicitada" yaml:"cardci_quantsolicitada"`
	CardciQuantlida       types.NullDecimal `boil:"cardci_quantlida" json:"cardci_quantlida,omitempty" toml:"cardci_quantlida" yaml:"cardci_quantlida,omitempty"`
	CardciPeso            types.NullDecimal `boil:"cardci_peso" json:"cardci_peso,omitempty" toml:"cardci_peso" yaml:"cardci_peso,omitempty"`
	CardciCubagem         types.NullDecimal `boil:"cardci_cubagem" json:"cardci_cubagem,omitempty" toml:"cardci_cubagem" yaml:"cardci_cubagem,omitempty"`

	R *carregamentoDocumentoItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L carregamentoDocumentoItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CarregamentoDocumentoItemColumns = struct {
	DDVID                 string
	CarID                 string
	ProID                 string
	CardciQuantsolicitada string
	CardciQuantlida       string
	CardciPeso            string
	CardciCubagem         string
}{
	DDVID:                 "ddv_id",
	CarID:                 "car_id",
	ProID:                 "pro_id",
	CardciQuantsolicitada: "cardci_quantsolicitada",
	CardciQuantlida:       "cardci_quantlida",
	CardciPeso:            "cardci_peso",
	CardciCubagem:         "cardci_cubagem",
}

var CarregamentoDocumentoItemTableColumns = struct {
	DDVID                 string
	CarID                 string
	ProID                 string
	CardciQuantsolicitada string
	CardciQuantlida       string
	CardciPeso            string
	CardciCubagem         string
}{
	DDVID:                 "carregamento_documento_item.ddv_id",
	CarID:                 "carregamento_documento_item.car_id",
	ProID:                 "carregamento_documento_item.pro_id",
	CardciQuantsolicitada: "carregamento_documento_item.cardci_quantsolicitada",
	CardciQuantlida:       "carregamento_documento_item.cardci_quantlida",
	CardciPeso:            "carregamento_documento_item.cardci_peso",
	CardciCubagem:         "carregamento_documento_item.cardci_cubagem",
}

// Generated where

var CarregamentoDocumentoItemWhere = struct {
	DDVID                 whereHelperint
	CarID                 whereHelperint
	ProID                 whereHelperint
	CardciQuantsolicitada whereHelpertypes_Decimal
	CardciQuantlida       whereHelpertypes_NullDecimal
	CardciPeso            whereHelpertypes_NullDecimal
	CardciCubagem         whereHelpertypes_NullDecimal
}{
	DDVID:                 whereHelperint{field: "`carregamento_documento_item`.`ddv_id`"},
	CarID:                 whereHelperint{field: "`carregamento_documento_item`.`car_id`"},
	ProID:                 whereHelperint{field: "`carregamento_documento_item`.`pro_id`"},
	CardciQuantsolicitada: whereHelpertypes_Decimal{field: "`carregamento_documento_item`.`cardci_quantsolicitada`"},
	CardciQuantlida:       whereHelpertypes_NullDecimal{field: "`carregamento_documento_item`.`cardci_quantlida`"},
	CardciPeso:            whereHelpertypes_NullDecimal{field: "`carregamento_documento_item`.`cardci_peso`"},
	CardciCubagem:         whereHelpertypes_NullDecimal{field: "`carregamento_documento_item`.`cardci_cubagem`"},
}

// CarregamentoDocumentoItemRels is where relationship names are stored.
var CarregamentoDocumentoItemRels = struct {
	Pro string
}{
	Pro: "Pro",
}

// carregamentoDocumentoItemR is where relationships are stored.
type carregamentoDocumentoItemR struct {
	Pro *Produto `boil:"Pro" json:"Pro" toml:"Pro" yaml:"Pro"`
}

// NewStruct creates a new relationship struct
func (*carregamentoDocumentoItemR) NewStruct() *carregamentoDocumentoItemR {
	return &carregamentoDocumentoItemR{}
}

func (r *carregamentoDocumentoItemR) GetPro() *Produto {
	if r == nil {
		return nil
	}
	return r.Pro
}

// carregamentoDocumentoItemL is where Load methods for each relationship are stored.
type carregamentoDocumentoItemL struct{}

var (
	carregamentoDocumentoItemAllColumns            = []string{"ddv_id", "car_id", "pro_id", "cardci_quantsolicitada", "cardci_quantlida", "cardci_peso", "cardci_cubagem"}
	carregamentoDocumentoItemColumnsWithoutDefault = []string{"ddv_id", "car_id", "pro_id", "cardci_quantsolicitada", "cardci_quantlida", "cardci_peso", "cardci_cubagem"}
	carregamentoDocumentoItemColumnsWithDefault    = []string{}
	carregamentoDocumentoItemPrimaryKeyColumns     = []string{"ddv_id", "car_id", "pro_id"}
	carregamentoDocumentoItemGeneratedColumns      = []string{}
)

type (
	// CarregamentoDocumentoItemSlice is an alias for a slice of pointers to CarregamentoDocumentoItem.
	// This should almost always be used instead of []CarregamentoDocumentoItem.
	CarregamentoDocumentoItemSlice []*CarregamentoDocumentoItem
	// CarregamentoDocumentoItemHook is the signature for custom CarregamentoDocumentoItem hook methods
	CarregamentoDocumentoItemHook func(context.Context, boil.ContextExecutor, *CarregamentoDocumentoItem) error

	carregamentoDocumentoItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	carregamentoDocumentoItemType                 = reflect.TypeOf(&CarregamentoDocumentoItem{})
	carregamentoDocumentoItemMapping              = queries.MakeStructMapping(carregamentoDocumentoItemType)
	carregamentoDocumentoItemPrimaryKeyMapping, _ = queries.BindMapping(carregamentoDocumentoItemType, carregamentoDocumentoItemMapping, carregamentoDocumentoItemPrimaryKeyColumns)
	carregamentoDocumentoItemInsertCacheMut       sync.RWMutex
	carregamentoDocumentoItemInsertCache          = make(map[string]insertCache)
	carregamentoDocumentoItemUpdateCacheMut       sync.RWMutex
	carregamentoDocumentoItemUpdateCache          = make(map[string]updateCache)
	carregamentoDocumentoItemUpsertCacheMut       sync.RWMutex
	carregamentoDocumentoItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var carregamentoDocumentoItemAfterSelectMu sync.Mutex
var carregamentoDocumentoItemAfterSelectHooks []CarregamentoDocumentoItemHook

var carregamentoDocumentoItemBeforeInsertMu sync.Mutex
var carregamentoDocumentoItemBeforeInsertHooks []CarregamentoDocumentoItemHook
var carregamentoDocumentoItemAfterInsertMu sync.Mutex
var carregamentoDocumentoItemAfterInsertHooks []CarregamentoDocumentoItemHook

var carregamentoDocumentoItemBeforeUpdateMu sync.Mutex
var carregamentoDocumentoItemBeforeUpdateHooks []CarregamentoDocumentoItemHook
var carregamentoDocumentoItemAfterUpdateMu sync.Mutex
var carregamentoDocumentoItemAfterUpdateHooks []CarregamentoDocumentoItemHook

var carregamentoDocumentoItemBeforeDeleteMu sync.Mutex
var carregamentoDocumentoItemBeforeDeleteHooks []CarregamentoDocumentoItemHook
var carregamentoDocumentoItemAfterDeleteMu sync.Mutex
var carregamentoDocumentoItemAfterDeleteHooks []CarregamentoDocumentoItemHook

var carregamentoDocumentoItemBeforeUpsertMu sync.Mutex
var carregamentoDocumentoItemBeforeUpsertHooks []CarregamentoDocumentoItemHook
var carregamentoDocumentoItemAfterUpsertMu sync.Mutex
var carregamentoDocumentoItemAfterUpsertHooks []CarregamentoDocumentoItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CarregamentoDocumentoItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CarregamentoDocumentoItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CarregamentoDocumentoItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CarregamentoDocumentoItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CarregamentoDocumentoItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CarregamentoDocumentoItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CarregamentoDocumentoItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CarregamentoDocumentoItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CarregamentoDocumentoItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoDocumentoItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCarregamentoDocumentoItemHook registers your hook function for all future operations.
func AddCarregamentoDocumentoItemHook(hookPoint boil.HookPoint, carregamentoDocumentoItemHook CarregamentoDocumentoItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		carregamentoDocumentoItemAfterSelectMu.Lock()
		carregamentoDocumentoItemAfterSelectHooks = append(carregamentoDocumentoItemAfterSelectHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		carregamentoDocumentoItemBeforeInsertMu.Lock()
		carregamentoDocumentoItemBeforeInsertHooks = append(carregamentoDocumentoItemBeforeInsertHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		carregamentoDocumentoItemAfterInsertMu.Lock()
		carregamentoDocumentoItemAfterInsertHooks = append(carregamentoDocumentoItemAfterInsertHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		carregamentoDocumentoItemBeforeUpdateMu.Lock()
		carregamentoDocumentoItemBeforeUpdateHooks = append(carregamentoDocumentoItemBeforeUpdateHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		carregamentoDocumentoItemAfterUpdateMu.Lock()
		carregamentoDocumentoItemAfterUpdateHooks = append(carregamentoDocumentoItemAfterUpdateHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		carregamentoDocumentoItemBeforeDeleteMu.Lock()
		carregamentoDocumentoItemBeforeDeleteHooks = append(carregamentoDocumentoItemBeforeDeleteHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		carregamentoDocumentoItemAfterDeleteMu.Lock()
		carregamentoDocumentoItemAfterDeleteHooks = append(carregamentoDocumentoItemAfterDeleteHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		carregamentoDocumentoItemBeforeUpsertMu.Lock()
		carregamentoDocumentoItemBeforeUpsertHooks = append(carregamentoDocumentoItemBeforeUpsertHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		carregamentoDocumentoItemAfterUpsertMu.Lock()
		carregamentoDocumentoItemAfterUpsertHooks = append(carregamentoDocumentoItemAfterUpsertHooks, carregamentoDocumentoItemHook)
		carregamentoDocumentoItemAfterUpsertMu.Unlock()
	}
}

// One returns a single carregamentoDocumentoItem record from the query.
func (q carregamentoDocumentoItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CarregamentoDocumentoItem, error) {
	o := &CarregamentoDocumentoItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for carregamento_documento_item")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CarregamentoDocumentoItem records from the query.
func (q carregamentoDocumentoItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (CarregamentoDocumentoItemSlice, error) {
	var o []*CarregamentoDocumentoItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CarregamentoDocumentoItem slice")
	}

	if len(carregamentoDocumentoItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CarregamentoDocumentoItem records in the query.
func (q carregamentoDocumentoItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count carregamento_documento_item rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q carregamentoDocumentoItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if carregamento_documento_item exists")
	}

	return count > 0, nil
}

// Pro pointed to by the foreign key.
func (o *CarregamentoDocumentoItem) Pro(mods ...qm.QueryMod) produtoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pro_id` = ?", o.ProID),
	}

	queryMods = append(queryMods, mods...)

	return Produtos(queryMods...)
}

// LoadPro allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (carregamentoDocumentoItemL) LoadPro(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCarregamentoDocumentoItem interface{}, mods queries.Applicator) error {
	var slice []*CarregamentoDocumentoItem
	var object *CarregamentoDocumentoItem

	if singular {
		var ok bool
		object, ok = maybeCarregamentoDocumentoItem.(*CarregamentoDocumentoItem)
		if !ok {
			object = new(CarregamentoDocumentoItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCarregamentoDocumentoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCarregamentoDocumentoItem))
			}
		}
	} else {
		s, ok := maybeCarregamentoDocumentoItem.(*[]*CarregamentoDocumentoItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCarregamentoDocumentoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCarregamentoDocumentoItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &carregamentoDocumentoItemR{}
		}
		args[object.ProID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &carregamentoDocumentoItemR{}
			}

			args[obj.ProID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.pro_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pro = foreign
		if foreign.R == nil {
			foreign.R = &produtoR{}
		}
		foreign.R.ProCarregamentoDocumentoItems = append(foreign.R.ProCarregamentoDocumentoItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProID == foreign.ProID {
				local.R.Pro = foreign
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.ProCarregamentoDocumentoItems = append(foreign.R.ProCarregamentoDocumentoItems, local)
				break
			}
		}
	}

	return nil
}

// SetPro of the carregamentoDocumentoItem to the related item.
// Sets o.R.Pro to related.
// Adds o to related.R.ProCarregamentoDocumentoItems.
func (o *CarregamentoDocumentoItem) SetPro(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Produto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `carregamento_documento_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pro_id"}),
		strmangle.WhereClause("`", "`", 0, carregamentoDocumentoItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ProID, o.DDVID, o.CarID, o.ProID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProID = related.ProID
	if o.R == nil {
		o.R = &carregamentoDocumentoItemR{
			Pro: related,
		}
	} else {
		o.R.Pro = related
	}

	if related.R == nil {
		related.R = &produtoR{
			ProCarregamentoDocumentoItems: CarregamentoDocumentoItemSlice{o},
		}
	} else {
		related.R.ProCarregamentoDocumentoItems = append(related.R.ProCarregamentoDocumentoItems, o)
	}

	return nil
}

// CarregamentoDocumentoItems retrieves all the records using an executor.
func CarregamentoDocumentoItems(mods ...qm.QueryMod) carregamentoDocumentoItemQuery {
	mods = append(mods, qm.From("`carregamento_documento_item`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`carregamento_documento_item`.*"})
	}

	return carregamentoDocumentoItemQuery{q}
}

// FindCarregamentoDocumentoItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCarregamentoDocumentoItem(ctx context.Context, exec boil.ContextExecutor, dDVID int, carID int, proID int, selectCols ...string) (*CarregamentoDocumentoItem, error) {
	carregamentoDocumentoItemObj := &CarregamentoDocumentoItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `carregamento_documento_item` where `ddv_id`=? AND `car_id`=? AND `pro_id`=?", sel,
	)

	q := queries.Raw(query, dDVID, carID, proID)

	err := q.Bind(ctx, exec, carregamentoDocumentoItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from carregamento_documento_item")
	}

	if err = carregamentoDocumentoItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return carregamentoDocumentoItemObj, err
	}

	return carregamentoDocumentoItemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CarregamentoDocumentoItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no carregamento_documento_item provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(carregamentoDocumentoItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	carregamentoDocumentoItemInsertCacheMut.RLock()
	cache, cached := carregamentoDocumentoItemInsertCache[key]
	carregamentoDocumentoItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			carregamentoDocumentoItemAllColumns,
			carregamentoDocumentoItemColumnsWithDefault,
			carregamentoDocumentoItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(carregamentoDocumentoItemType, carregamentoDocumentoItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(carregamentoDocumentoItemType, carregamentoDocumentoItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `carregamento_documento_item` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `carregamento_documento_item` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `carregamento_documento_item` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, carregamentoDocumentoItemPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into carregamento_documento_item")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DDVID,
		o.CarID,
		o.ProID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for carregamento_documento_item")
	}

CacheNoHooks:
	if !cached {
		carregamentoDocumentoItemInsertCacheMut.Lock()
		carregamentoDocumentoItemInsertCache[key] = cache
		carregamentoDocumentoItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CarregamentoDocumentoItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CarregamentoDocumentoItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	carregamentoDocumentoItemUpdateCacheMut.RLock()
	cache, cached := carregamentoDocumentoItemUpdateCache[key]
	carregamentoDocumentoItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			carregamentoDocumentoItemAllColumns,
			carregamentoDocumentoItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update carregamento_documento_item, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `carregamento_documento_item` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, carregamentoDocumentoItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(carregamentoDocumentoItemType, carregamentoDocumentoItemMapping, append(wl, carregamentoDocumentoItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update carregamento_documento_item row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for carregamento_documento_item")
	}

	if !cached {
		carregamentoDocumentoItemUpdateCacheMut.Lock()
		carregamentoDocumentoItemUpdateCache[key] = cache
		carregamentoDocumentoItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q carregamentoDocumentoItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for carregamento_documento_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for carregamento_documento_item")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CarregamentoDocumentoItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), carregamentoDocumentoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `carregamento_documento_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, carregamentoDocumentoItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in carregamentoDocumentoItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all carregamentoDocumentoItem")
	}
	return rowsAff, nil
}

var mySQLCarregamentoDocumentoItemUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CarregamentoDocumentoItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no carregamento_documento_item provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(carregamentoDocumentoItemColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCarregamentoDocumentoItemUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	carregamentoDocumentoItemUpsertCacheMut.RLock()
	cache, cached := carregamentoDocumentoItemUpsertCache[key]
	carregamentoDocumentoItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			carregamentoDocumentoItemAllColumns,
			carregamentoDocumentoItemColumnsWithDefault,
			carregamentoDocumentoItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			carregamentoDocumentoItemAllColumns,
			carregamentoDocumentoItemPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert carregamento_documento_item, could not build update column list")
		}

		ret := strmangle.SetComplement(carregamentoDocumentoItemAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`carregamento_documento_item`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `carregamento_documento_item` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(carregamentoDocumentoItemType, carregamentoDocumentoItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(carregamentoDocumentoItemType, carregamentoDocumentoItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for carregamento_documento_item")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(carregamentoDocumentoItemType, carregamentoDocumentoItemMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for carregamento_documento_item")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for carregamento_documento_item")
	}

CacheNoHooks:
	if !cached {
		carregamentoDocumentoItemUpsertCacheMut.Lock()
		carregamentoDocumentoItemUpsertCache[key] = cache
		carregamentoDocumentoItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CarregamentoDocumentoItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CarregamentoDocumentoItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CarregamentoDocumentoItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), carregamentoDocumentoItemPrimaryKeyMapping)
	sql := "DELETE FROM `carregamento_documento_item` WHERE `ddv_id`=? AND `car_id`=? AND `pro_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from carregamento_documento_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for carregamento_documento_item")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q carregamentoDocumentoItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no carregamentoDocumentoItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from carregamento_documento_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for carregamento_documento_item")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CarregamentoDocumentoItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(carregamentoDocumentoItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), carregamentoDocumentoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `carregamento_documento_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, carregamentoDocumentoItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from carregamentoDocumentoItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for carregamento_documento_item")
	}

	if len(carregamentoDocumentoItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CarregamentoDocumentoItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCarregamentoDocumentoItem(ctx, exec, o.DDVID, o.CarID, o.ProID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CarregamentoDocumentoItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CarregamentoDocumentoItemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), carregamentoDocumentoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `carregamento_documento_item`.* FROM `carregamento_documento_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, carregamentoDocumentoItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CarregamentoDocumentoItemSlice")
	}

	*o = slice

	return nil
}

// CarregamentoDocumentoItemExists checks if the CarregamentoDocumentoItem row exists.
func CarregamentoDocumentoItemExists(ctx context.Context, exec boil.ContextExecutor, dDVID int, carID int, proID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `carregamento_documento_item` where `ddv_id`=? AND `car_id`=? AND `pro_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, dDVID, carID, proID)
	}
	row := exec.QueryRowContext(ctx, sql, dDVID, carID, proID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if carregamento_documento_item exists")
	}

	return exists, nil
}

// Exists checks if the CarregamentoDocumentoItem row exists.
func (o *CarregamentoDocumentoItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CarregamentoDocumentoItemExists(ctx, exec, o.DDVID, o.CarID, o.ProID)
}
