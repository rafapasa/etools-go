// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testGeracaoContatosEntidadesNotaFiscals(t *testing.T) {
	t.Parallel()

	query := GeracaoContatosEntidadesNotaFiscals()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testGeracaoContatosEntidadesNotaFiscalsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := GeracaoContatosEntidadesNotaFiscals().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := GeracaoContatosEntidadesNotaFiscalSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := GeracaoContatosEntidadesNotaFiscalExists(ctx, tx, o.GceID, o.GcenfID)
	if err != nil {
		t.Errorf("Unable to check if GeracaoContatosEntidadesNotaFiscal exists: %s", err)
	}
	if !e {
		t.Errorf("Expected GeracaoContatosEntidadesNotaFiscalExists to return true, but got false.")
	}
}

func testGeracaoContatosEntidadesNotaFiscalsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	geracaoContatosEntidadesNotaFiscalFound, err := FindGeracaoContatosEntidadesNotaFiscal(ctx, tx, o.GceID, o.GcenfID)
	if err != nil {
		t.Error(err)
	}

	if geracaoContatosEntidadesNotaFiscalFound == nil {
		t.Error("want a record, got nil")
	}
}

func testGeracaoContatosEntidadesNotaFiscalsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = GeracaoContatosEntidadesNotaFiscals().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := GeracaoContatosEntidadesNotaFiscals().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testGeracaoContatosEntidadesNotaFiscalsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	geracaoContatosEntidadesNotaFiscalOne := &GeracaoContatosEntidadesNotaFiscal{}
	geracaoContatosEntidadesNotaFiscalTwo := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, geracaoContatosEntidadesNotaFiscalOne, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}
	if err = randomize.Struct(seed, geracaoContatosEntidadesNotaFiscalTwo, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = geracaoContatosEntidadesNotaFiscalOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = geracaoContatosEntidadesNotaFiscalTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := GeracaoContatosEntidadesNotaFiscals().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testGeracaoContatosEntidadesNotaFiscalsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	geracaoContatosEntidadesNotaFiscalOne := &GeracaoContatosEntidadesNotaFiscal{}
	geracaoContatosEntidadesNotaFiscalTwo := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, geracaoContatosEntidadesNotaFiscalOne, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}
	if err = randomize.Struct(seed, geracaoContatosEntidadesNotaFiscalTwo, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = geracaoContatosEntidadesNotaFiscalOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = geracaoContatosEntidadesNotaFiscalTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func geracaoContatosEntidadesNotaFiscalBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func geracaoContatosEntidadesNotaFiscalAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func geracaoContatosEntidadesNotaFiscalAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func geracaoContatosEntidadesNotaFiscalBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func geracaoContatosEntidadesNotaFiscalAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func geracaoContatosEntidadesNotaFiscalBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func geracaoContatosEntidadesNotaFiscalAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func geracaoContatosEntidadesNotaFiscalBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func geracaoContatosEntidadesNotaFiscalAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidadesNotaFiscal) error {
	*o = GeracaoContatosEntidadesNotaFiscal{}
	return nil
}

func testGeracaoContatosEntidadesNotaFiscalsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &GeracaoContatosEntidadesNotaFiscal{}
	o := &GeracaoContatosEntidadesNotaFiscal{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, false); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal object: %s", err)
	}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.BeforeInsertHook, geracaoContatosEntidadesNotaFiscalBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalBeforeInsertHooks = []GeracaoContatosEntidadesNotaFiscalHook{}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.AfterInsertHook, geracaoContatosEntidadesNotaFiscalAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalAfterInsertHooks = []GeracaoContatosEntidadesNotaFiscalHook{}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.AfterSelectHook, geracaoContatosEntidadesNotaFiscalAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalAfterSelectHooks = []GeracaoContatosEntidadesNotaFiscalHook{}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.BeforeUpdateHook, geracaoContatosEntidadesNotaFiscalBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalBeforeUpdateHooks = []GeracaoContatosEntidadesNotaFiscalHook{}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.AfterUpdateHook, geracaoContatosEntidadesNotaFiscalAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalAfterUpdateHooks = []GeracaoContatosEntidadesNotaFiscalHook{}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.BeforeDeleteHook, geracaoContatosEntidadesNotaFiscalBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalBeforeDeleteHooks = []GeracaoContatosEntidadesNotaFiscalHook{}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.AfterDeleteHook, geracaoContatosEntidadesNotaFiscalAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalAfterDeleteHooks = []GeracaoContatosEntidadesNotaFiscalHook{}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.BeforeUpsertHook, geracaoContatosEntidadesNotaFiscalBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalBeforeUpsertHooks = []GeracaoContatosEntidadesNotaFiscalHook{}

	AddGeracaoContatosEntidadesNotaFiscalHook(boil.AfterUpsertHook, geracaoContatosEntidadesNotaFiscalAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadesNotaFiscalAfterUpsertHooks = []GeracaoContatosEntidadesNotaFiscalHook{}
}

func testGeracaoContatosEntidadesNotaFiscalsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testGeracaoContatosEntidadesNotaFiscalToOneContratoEntidadeUsingCent(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local GeracaoContatosEntidadesNotaFiscal
	var foreign ContratoEntidade

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, contratoEntidadeDBTypes, false, contratoEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ContratoEntidade struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.CentID = foreign.CentID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Cent().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.CentID != foreign.CentID {
		t.Errorf("want: %v, got %v", foreign.CentID, check.CentID)
	}

	ranAfterSelectHook := false
	AddContratoEntidadeHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *ContratoEntidade) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := GeracaoContatosEntidadesNotaFiscalSlice{&local}
	if err = local.L.LoadCent(ctx, tx, false, (*[]*GeracaoContatosEntidadesNotaFiscal)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Cent == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Cent = nil
	if err = local.L.LoadCent(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Cent == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testGeracaoContatosEntidadesNotaFiscalToOneEntidadeUsingEnt(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local GeracaoContatosEntidadesNotaFiscal
	var foreign Entidade

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, entidadeDBTypes, false, entidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Entidade struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.EntID = foreign.EntID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Ent().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.EntID != foreign.EntID {
		t.Errorf("want: %v, got %v", foreign.EntID, check.EntID)
	}

	ranAfterSelectHook := false
	AddEntidadeHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Entidade) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := GeracaoContatosEntidadesNotaFiscalSlice{&local}
	if err = local.L.LoadEnt(ctx, tx, false, (*[]*GeracaoContatosEntidadesNotaFiscal)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Ent == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Ent = nil
	if err = local.L.LoadEnt(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Ent == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testGeracaoContatosEntidadesNotaFiscalToOneGeracaoContatosEntidadeUsingGce(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local GeracaoContatosEntidadesNotaFiscal
	var foreign GeracaoContatosEntidade

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, geracaoContatosEntidadeDBTypes, false, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.GceID = foreign.GceID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Gce().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.GceID != foreign.GceID {
		t.Errorf("want: %v, got %v", foreign.GceID, check.GceID)
	}

	ranAfterSelectHook := false
	AddGeracaoContatosEntidadeHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := GeracaoContatosEntidadesNotaFiscalSlice{&local}
	if err = local.L.LoadGce(ctx, tx, false, (*[]*GeracaoContatosEntidadesNotaFiscal)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Gce == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Gce = nil
	if err = local.L.LoadGce(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Gce == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testGeracaoContatosEntidadesNotaFiscalToOneNotaFiscalUsingNTF(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local GeracaoContatosEntidadesNotaFiscal
	var foreign NotaFiscal

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, notaFiscalDBTypes, false, notaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize NotaFiscal struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.NTFID, foreign.NTFID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.NTF().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.NTFID, foreign.NTFID) {
		t.Errorf("want: %v, got %v", foreign.NTFID, check.NTFID)
	}

	ranAfterSelectHook := false
	AddNotaFiscalHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *NotaFiscal) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := GeracaoContatosEntidadesNotaFiscalSlice{&local}
	if err = local.L.LoadNTF(ctx, tx, false, (*[]*GeracaoContatosEntidadesNotaFiscal)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.NTF == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.NTF = nil
	if err = local.L.LoadNTF(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.NTF == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testGeracaoContatosEntidadesNotaFiscalToOneSetOpContratoEntidadeUsingCent(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidadesNotaFiscal
	var b, c ContratoEntidade

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadesNotaFiscalDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, contratoEntidadeDBTypes, false, strmangle.SetComplement(contratoEntidadePrimaryKeyColumns, contratoEntidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, contratoEntidadeDBTypes, false, strmangle.SetComplement(contratoEntidadePrimaryKeyColumns, contratoEntidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*ContratoEntidade{&b, &c} {
		err = a.SetCent(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Cent != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.CentGeracaoContatosEntidadesNotaFiscals[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.CentID != x.CentID {
			t.Error("foreign key was wrong value", a.CentID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.CentID))
		reflect.Indirect(reflect.ValueOf(&a.CentID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.CentID != x.CentID {
			t.Error("foreign key was wrong value", a.CentID, x.CentID)
		}
	}
}
func testGeracaoContatosEntidadesNotaFiscalToOneSetOpEntidadeUsingEnt(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidadesNotaFiscal
	var b, c Entidade

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadesNotaFiscalDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, entidadeDBTypes, false, strmangle.SetComplement(entidadePrimaryKeyColumns, entidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, entidadeDBTypes, false, strmangle.SetComplement(entidadePrimaryKeyColumns, entidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Entidade{&b, &c} {
		err = a.SetEnt(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Ent != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.EntGeracaoContatosEntidadesNotaFiscals[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.EntID != x.EntID {
			t.Error("foreign key was wrong value", a.EntID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.EntID))
		reflect.Indirect(reflect.ValueOf(&a.EntID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.EntID != x.EntID {
			t.Error("foreign key was wrong value", a.EntID, x.EntID)
		}
	}
}
func testGeracaoContatosEntidadesNotaFiscalToOneSetOpGeracaoContatosEntidadeUsingGce(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidadesNotaFiscal
	var b, c GeracaoContatosEntidade

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadesNotaFiscalDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, geracaoContatosEntidadeDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadePrimaryKeyColumns, geracaoContatosEntidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, geracaoContatosEntidadeDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadePrimaryKeyColumns, geracaoContatosEntidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*GeracaoContatosEntidade{&b, &c} {
		err = a.SetGce(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Gce != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.GceGeracaoContatosEntidadesNotaFiscals[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.GceID != x.GceID {
			t.Error("foreign key was wrong value", a.GceID)
		}

		if exists, err := GeracaoContatosEntidadesNotaFiscalExists(ctx, tx, a.GceID, a.GcenfID); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}
func testGeracaoContatosEntidadesNotaFiscalToOneSetOpNotaFiscalUsingNTF(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidadesNotaFiscal
	var b, c NotaFiscal

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadesNotaFiscalDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, notaFiscalDBTypes, false, strmangle.SetComplement(notaFiscalPrimaryKeyColumns, notaFiscalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, notaFiscalDBTypes, false, strmangle.SetComplement(notaFiscalPrimaryKeyColumns, notaFiscalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*NotaFiscal{&b, &c} {
		err = a.SetNTF(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.NTF != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.NTFGeracaoContatosEntidadesNotaFiscals[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.NTFID, x.NTFID) {
			t.Error("foreign key was wrong value", a.NTFID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.NTFID))
		reflect.Indirect(reflect.ValueOf(&a.NTFID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.NTFID, x.NTFID) {
			t.Error("foreign key was wrong value", a.NTFID, x.NTFID)
		}
	}
}

func testGeracaoContatosEntidadesNotaFiscalToOneRemoveOpNotaFiscalUsingNTF(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidadesNotaFiscal
	var b NotaFiscal

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadesNotaFiscalDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, notaFiscalDBTypes, false, strmangle.SetComplement(notaFiscalPrimaryKeyColumns, notaFiscalColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetNTF(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveNTF(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.NTF().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.NTF != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.NTFID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.NTFGeracaoContatosEntidadesNotaFiscals) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testGeracaoContatosEntidadesNotaFiscalsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := GeracaoContatosEntidadesNotaFiscalSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := GeracaoContatosEntidadesNotaFiscals().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	geracaoContatosEntidadesNotaFiscalDBTypes = map[string]string{`GceID`: `int`, `GcenfID`: `int`, `EntID`: `int`, `NTFID`: `int`, `CentID`: `int`, `GcenfTipo`: `int`, `GcenfValorbruto`: `decimal`, `GcenfValorextra`: `decimal`, `GcenfValorliquido`: `decimal`}
	_                                         = bytes.MinRead
)

func testGeracaoContatosEntidadesNotaFiscalsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(geracaoContatosEntidadesNotaFiscalAllColumns) == len(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(geracaoContatosEntidadesNotaFiscalAllColumns) == len(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, geracaoContatosEntidadesNotaFiscalDBTypes, true, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(geracaoContatosEntidadesNotaFiscalAllColumns, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns) {
		fields = geracaoContatosEntidadesNotaFiscalAllColumns
	} else {
		fields = strmangle.SetComplement(
			geracaoContatosEntidadesNotaFiscalAllColumns,
			geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := GeracaoContatosEntidadesNotaFiscalSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testGeracaoContatosEntidadesNotaFiscalsUpsert(t *testing.T) {
	t.Parallel()

	if len(geracaoContatosEntidadesNotaFiscalAllColumns) == len(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLGeracaoContatosEntidadesNotaFiscalUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := GeracaoContatosEntidadesNotaFiscal{}
	if err = randomize.Struct(seed, &o, geracaoContatosEntidadesNotaFiscalDBTypes, false); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert GeracaoContatosEntidadesNotaFiscal: %s", err)
	}

	count, err := GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidadesNotaFiscal struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert GeracaoContatosEntidadesNotaFiscal: %s", err)
	}

	count, err = GeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
