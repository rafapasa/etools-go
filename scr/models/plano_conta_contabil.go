// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PlanoContaContabil is an object representing the database table.
type PlanoContaContabil struct {
	PCCID        int    `boil:"pcc_id" json:"pcc_id" toml:"pcc_id" yaml:"pcc_id"`
	EmfID        int    `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	PCCDescricao string `boil:"pcc_descricao" json:"pcc_descricao" toml:"pcc_descricao" yaml:"pcc_descricao"`
	// 1-tipo sitetica
	//             2-tipo analitica
	PCCTipo   int    `boil:"pcc_tipo" json:"pcc_tipo" toml:"pcc_tipo" yaml:"pcc_tipo"`
	PCCClasse string `boil:"pcc_classe" json:"pcc_classe" toml:"pcc_classe" yaml:"pcc_classe"`
	// 1 - crédito
	//             -1 - débito
	PCCOperadorconta int `boil:"pcc_operadorconta" json:"pcc_operadorconta" toml:"pcc_operadorconta" yaml:"pcc_operadorconta"`
	// 1 - ativo
	//             2 - passivo
	//             3 - dre
	PCCOrigemconta int `boil:"pcc_origemconta" json:"pcc_origemconta" toml:"pcc_origemconta" yaml:"pcc_origemconta"`

	R *planoContaContabilR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L planoContaContabilL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PlanoContaContabilColumns = struct {
	PCCID            string
	EmfID            string
	PCCDescricao     string
	PCCTipo          string
	PCCClasse        string
	PCCOperadorconta string
	PCCOrigemconta   string
}{
	PCCID:            "pcc_id",
	EmfID:            "emf_id",
	PCCDescricao:     "pcc_descricao",
	PCCTipo:          "pcc_tipo",
	PCCClasse:        "pcc_classe",
	PCCOperadorconta: "pcc_operadorconta",
	PCCOrigemconta:   "pcc_origemconta",
}

var PlanoContaContabilTableColumns = struct {
	PCCID            string
	EmfID            string
	PCCDescricao     string
	PCCTipo          string
	PCCClasse        string
	PCCOperadorconta string
	PCCOrigemconta   string
}{
	PCCID:            "plano_conta_contabil.pcc_id",
	EmfID:            "plano_conta_contabil.emf_id",
	PCCDescricao:     "plano_conta_contabil.pcc_descricao",
	PCCTipo:          "plano_conta_contabil.pcc_tipo",
	PCCClasse:        "plano_conta_contabil.pcc_classe",
	PCCOperadorconta: "plano_conta_contabil.pcc_operadorconta",
	PCCOrigemconta:   "plano_conta_contabil.pcc_origemconta",
}

// Generated where

var PlanoContaContabilWhere = struct {
	PCCID            whereHelperint
	EmfID            whereHelperint
	PCCDescricao     whereHelperstring
	PCCTipo          whereHelperint
	PCCClasse        whereHelperstring
	PCCOperadorconta whereHelperint
	PCCOrigemconta   whereHelperint
}{
	PCCID:            whereHelperint{field: "`plano_conta_contabil`.`pcc_id`"},
	EmfID:            whereHelperint{field: "`plano_conta_contabil`.`emf_id`"},
	PCCDescricao:     whereHelperstring{field: "`plano_conta_contabil`.`pcc_descricao`"},
	PCCTipo:          whereHelperint{field: "`plano_conta_contabil`.`pcc_tipo`"},
	PCCClasse:        whereHelperstring{field: "`plano_conta_contabil`.`pcc_classe`"},
	PCCOperadorconta: whereHelperint{field: "`plano_conta_contabil`.`pcc_operadorconta`"},
	PCCOrigemconta:   whereHelperint{field: "`plano_conta_contabil`.`pcc_origemconta`"},
}

// PlanoContaContabilRels is where relationship names are stored.
var PlanoContaContabilRels = struct {
	Emf                             string
	PCCIDCreditoLancamentoContabils string
	PCCIDDebitoLancamentoContabils  string
	PCCIDCreditoRotinaContabilItens string
	PCCIDDebitoRotinaContabilItens  string
}{
	Emf:                             "Emf",
	PCCIDCreditoLancamentoContabils: "PCCIDCreditoLancamentoContabils",
	PCCIDDebitoLancamentoContabils:  "PCCIDDebitoLancamentoContabils",
	PCCIDCreditoRotinaContabilItens: "PCCIDCreditoRotinaContabilItens",
	PCCIDDebitoRotinaContabilItens:  "PCCIDDebitoRotinaContabilItens",
}

// planoContaContabilR is where relationships are stored.
type planoContaContabilR struct {
	Emf                             *EmpresaFilial          `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	PCCIDCreditoLancamentoContabils LancamentoContabilSlice `boil:"PCCIDCreditoLancamentoContabils" json:"PCCIDCreditoLancamentoContabils" toml:"PCCIDCreditoLancamentoContabils" yaml:"PCCIDCreditoLancamentoContabils"`
	PCCIDDebitoLancamentoContabils  LancamentoContabilSlice `boil:"PCCIDDebitoLancamentoContabils" json:"PCCIDDebitoLancamentoContabils" toml:"PCCIDDebitoLancamentoContabils" yaml:"PCCIDDebitoLancamentoContabils"`
	PCCIDCreditoRotinaContabilItens RotinaContabilItenSlice `boil:"PCCIDCreditoRotinaContabilItens" json:"PCCIDCreditoRotinaContabilItens" toml:"PCCIDCreditoRotinaContabilItens" yaml:"PCCIDCreditoRotinaContabilItens"`
	PCCIDDebitoRotinaContabilItens  RotinaContabilItenSlice `boil:"PCCIDDebitoRotinaContabilItens" json:"PCCIDDebitoRotinaContabilItens" toml:"PCCIDDebitoRotinaContabilItens" yaml:"PCCIDDebitoRotinaContabilItens"`
}

// NewStruct creates a new relationship struct
func (*planoContaContabilR) NewStruct() *planoContaContabilR {
	return &planoContaContabilR{}
}

func (r *planoContaContabilR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *planoContaContabilR) GetPCCIDCreditoLancamentoContabils() LancamentoContabilSlice {
	if r == nil {
		return nil
	}
	return r.PCCIDCreditoLancamentoContabils
}

func (r *planoContaContabilR) GetPCCIDDebitoLancamentoContabils() LancamentoContabilSlice {
	if r == nil {
		return nil
	}
	return r.PCCIDDebitoLancamentoContabils
}

func (r *planoContaContabilR) GetPCCIDCreditoRotinaContabilItens() RotinaContabilItenSlice {
	if r == nil {
		return nil
	}
	return r.PCCIDCreditoRotinaContabilItens
}

func (r *planoContaContabilR) GetPCCIDDebitoRotinaContabilItens() RotinaContabilItenSlice {
	if r == nil {
		return nil
	}
	return r.PCCIDDebitoRotinaContabilItens
}

// planoContaContabilL is where Load methods for each relationship are stored.
type planoContaContabilL struct{}

var (
	planoContaContabilAllColumns            = []string{"pcc_id", "emf_id", "pcc_descricao", "pcc_tipo", "pcc_classe", "pcc_operadorconta", "pcc_origemconta"}
	planoContaContabilColumnsWithoutDefault = []string{"pcc_id", "emf_id", "pcc_descricao", "pcc_tipo", "pcc_classe", "pcc_operadorconta", "pcc_origemconta"}
	planoContaContabilColumnsWithDefault    = []string{}
	planoContaContabilPrimaryKeyColumns     = []string{"pcc_id"}
	planoContaContabilGeneratedColumns      = []string{}
)

type (
	// PlanoContaContabilSlice is an alias for a slice of pointers to PlanoContaContabil.
	// This should almost always be used instead of []PlanoContaContabil.
	PlanoContaContabilSlice []*PlanoContaContabil
	// PlanoContaContabilHook is the signature for custom PlanoContaContabil hook methods
	PlanoContaContabilHook func(context.Context, boil.ContextExecutor, *PlanoContaContabil) error

	planoContaContabilQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	planoContaContabilType                 = reflect.TypeOf(&PlanoContaContabil{})
	planoContaContabilMapping              = queries.MakeStructMapping(planoContaContabilType)
	planoContaContabilPrimaryKeyMapping, _ = queries.BindMapping(planoContaContabilType, planoContaContabilMapping, planoContaContabilPrimaryKeyColumns)
	planoContaContabilInsertCacheMut       sync.RWMutex
	planoContaContabilInsertCache          = make(map[string]insertCache)
	planoContaContabilUpdateCacheMut       sync.RWMutex
	planoContaContabilUpdateCache          = make(map[string]updateCache)
	planoContaContabilUpsertCacheMut       sync.RWMutex
	planoContaContabilUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var planoContaContabilAfterSelectMu sync.Mutex
var planoContaContabilAfterSelectHooks []PlanoContaContabilHook

var planoContaContabilBeforeInsertMu sync.Mutex
var planoContaContabilBeforeInsertHooks []PlanoContaContabilHook
var planoContaContabilAfterInsertMu sync.Mutex
var planoContaContabilAfterInsertHooks []PlanoContaContabilHook

var planoContaContabilBeforeUpdateMu sync.Mutex
var planoContaContabilBeforeUpdateHooks []PlanoContaContabilHook
var planoContaContabilAfterUpdateMu sync.Mutex
var planoContaContabilAfterUpdateHooks []PlanoContaContabilHook

var planoContaContabilBeforeDeleteMu sync.Mutex
var planoContaContabilBeforeDeleteHooks []PlanoContaContabilHook
var planoContaContabilAfterDeleteMu sync.Mutex
var planoContaContabilAfterDeleteHooks []PlanoContaContabilHook

var planoContaContabilBeforeUpsertMu sync.Mutex
var planoContaContabilBeforeUpsertHooks []PlanoContaContabilHook
var planoContaContabilAfterUpsertMu sync.Mutex
var planoContaContabilAfterUpsertHooks []PlanoContaContabilHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PlanoContaContabil) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PlanoContaContabil) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PlanoContaContabil) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PlanoContaContabil) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PlanoContaContabil) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PlanoContaContabil) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PlanoContaContabil) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PlanoContaContabil) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PlanoContaContabil) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planoContaContabilAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPlanoContaContabilHook registers your hook function for all future operations.
func AddPlanoContaContabilHook(hookPoint boil.HookPoint, planoContaContabilHook PlanoContaContabilHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		planoContaContabilAfterSelectMu.Lock()
		planoContaContabilAfterSelectHooks = append(planoContaContabilAfterSelectHooks, planoContaContabilHook)
		planoContaContabilAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		planoContaContabilBeforeInsertMu.Lock()
		planoContaContabilBeforeInsertHooks = append(planoContaContabilBeforeInsertHooks, planoContaContabilHook)
		planoContaContabilBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		planoContaContabilAfterInsertMu.Lock()
		planoContaContabilAfterInsertHooks = append(planoContaContabilAfterInsertHooks, planoContaContabilHook)
		planoContaContabilAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		planoContaContabilBeforeUpdateMu.Lock()
		planoContaContabilBeforeUpdateHooks = append(planoContaContabilBeforeUpdateHooks, planoContaContabilHook)
		planoContaContabilBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		planoContaContabilAfterUpdateMu.Lock()
		planoContaContabilAfterUpdateHooks = append(planoContaContabilAfterUpdateHooks, planoContaContabilHook)
		planoContaContabilAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		planoContaContabilBeforeDeleteMu.Lock()
		planoContaContabilBeforeDeleteHooks = append(planoContaContabilBeforeDeleteHooks, planoContaContabilHook)
		planoContaContabilBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		planoContaContabilAfterDeleteMu.Lock()
		planoContaContabilAfterDeleteHooks = append(planoContaContabilAfterDeleteHooks, planoContaContabilHook)
		planoContaContabilAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		planoContaContabilBeforeUpsertMu.Lock()
		planoContaContabilBeforeUpsertHooks = append(planoContaContabilBeforeUpsertHooks, planoContaContabilHook)
		planoContaContabilBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		planoContaContabilAfterUpsertMu.Lock()
		planoContaContabilAfterUpsertHooks = append(planoContaContabilAfterUpsertHooks, planoContaContabilHook)
		planoContaContabilAfterUpsertMu.Unlock()
	}
}

// One returns a single planoContaContabil record from the query.
func (q planoContaContabilQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PlanoContaContabil, error) {
	o := &PlanoContaContabil{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for plano_conta_contabil")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PlanoContaContabil records from the query.
func (q planoContaContabilQuery) All(ctx context.Context, exec boil.ContextExecutor) (PlanoContaContabilSlice, error) {
	var o []*PlanoContaContabil

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PlanoContaContabil slice")
	}

	if len(planoContaContabilAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PlanoContaContabil records in the query.
func (q planoContaContabilQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count plano_conta_contabil rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q planoContaContabilQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if plano_conta_contabil exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *PlanoContaContabil) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// PCCIDCreditoLancamentoContabils retrieves all the lancamento_contabil's LancamentoContabils with an executor via pcc_id_credito column.
func (o *PlanoContaContabil) PCCIDCreditoLancamentoContabils(mods ...qm.QueryMod) lancamentoContabilQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`lancamento_contabil`.`pcc_id_credito`=?", o.PCCID),
	)

	return LancamentoContabils(queryMods...)
}

// PCCIDDebitoLancamentoContabils retrieves all the lancamento_contabil's LancamentoContabils with an executor via pcc_id_debito column.
func (o *PlanoContaContabil) PCCIDDebitoLancamentoContabils(mods ...qm.QueryMod) lancamentoContabilQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`lancamento_contabil`.`pcc_id_debito`=?", o.PCCID),
	)

	return LancamentoContabils(queryMods...)
}

// PCCIDCreditoRotinaContabilItens retrieves all the rotina_contabil_iten's RotinaContabilItens with an executor via pcc_id_credito column.
func (o *PlanoContaContabil) PCCIDCreditoRotinaContabilItens(mods ...qm.QueryMod) rotinaContabilItenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`rotina_contabil_itens`.`pcc_id_credito`=?", o.PCCID),
	)

	return RotinaContabilItens(queryMods...)
}

// PCCIDDebitoRotinaContabilItens retrieves all the rotina_contabil_iten's RotinaContabilItens with an executor via pcc_id_debito column.
func (o *PlanoContaContabil) PCCIDDebitoRotinaContabilItens(mods ...qm.QueryMod) rotinaContabilItenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`rotina_contabil_itens`.`pcc_id_debito`=?", o.PCCID),
	)

	return RotinaContabilItens(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (planoContaContabilL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlanoContaContabil interface{}, mods queries.Applicator) error {
	var slice []*PlanoContaContabil
	var object *PlanoContaContabil

	if singular {
		var ok bool
		object, ok = maybePlanoContaContabil.(*PlanoContaContabil)
		if !ok {
			object = new(PlanoContaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlanoContaContabil))
			}
		}
	} else {
		s, ok := maybePlanoContaContabil.(*[]*PlanoContaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlanoContaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &planoContaContabilR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &planoContaContabilR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfPlanoContaContabils = append(foreign.R.EmfPlanoContaContabils, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfPlanoContaContabils = append(foreign.R.EmfPlanoContaContabils, local)
				break
			}
		}
	}

	return nil
}

// LoadPCCIDCreditoLancamentoContabils allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (planoContaContabilL) LoadPCCIDCreditoLancamentoContabils(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlanoContaContabil interface{}, mods queries.Applicator) error {
	var slice []*PlanoContaContabil
	var object *PlanoContaContabil

	if singular {
		var ok bool
		object, ok = maybePlanoContaContabil.(*PlanoContaContabil)
		if !ok {
			object = new(PlanoContaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlanoContaContabil))
			}
		}
	} else {
		s, ok := maybePlanoContaContabil.(*[]*PlanoContaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlanoContaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &planoContaContabilR{}
		}
		args[object.PCCID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &planoContaContabilR{}
			}
			args[obj.PCCID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`lancamento_contabil`),
		qm.WhereIn(`lancamento_contabil.pcc_id_credito in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load lancamento_contabil")
	}

	var resultSlice []*LancamentoContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice lancamento_contabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on lancamento_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for lancamento_contabil")
	}

	if len(lancamentoContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PCCIDCreditoLancamentoContabils = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &lancamentoContabilR{}
			}
			foreign.R.PCCIDCreditoPlanoContaContabil = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PCCID == foreign.PCCIDCredito {
				local.R.PCCIDCreditoLancamentoContabils = append(local.R.PCCIDCreditoLancamentoContabils, foreign)
				if foreign.R == nil {
					foreign.R = &lancamentoContabilR{}
				}
				foreign.R.PCCIDCreditoPlanoContaContabil = local
				break
			}
		}
	}

	return nil
}

// LoadPCCIDDebitoLancamentoContabils allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (planoContaContabilL) LoadPCCIDDebitoLancamentoContabils(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlanoContaContabil interface{}, mods queries.Applicator) error {
	var slice []*PlanoContaContabil
	var object *PlanoContaContabil

	if singular {
		var ok bool
		object, ok = maybePlanoContaContabil.(*PlanoContaContabil)
		if !ok {
			object = new(PlanoContaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlanoContaContabil))
			}
		}
	} else {
		s, ok := maybePlanoContaContabil.(*[]*PlanoContaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlanoContaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &planoContaContabilR{}
		}
		args[object.PCCID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &planoContaContabilR{}
			}
			args[obj.PCCID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`lancamento_contabil`),
		qm.WhereIn(`lancamento_contabil.pcc_id_debito in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load lancamento_contabil")
	}

	var resultSlice []*LancamentoContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice lancamento_contabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on lancamento_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for lancamento_contabil")
	}

	if len(lancamentoContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PCCIDDebitoLancamentoContabils = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &lancamentoContabilR{}
			}
			foreign.R.PCCIDDebitoPlanoContaContabil = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PCCID == foreign.PCCIDDebito {
				local.R.PCCIDDebitoLancamentoContabils = append(local.R.PCCIDDebitoLancamentoContabils, foreign)
				if foreign.R == nil {
					foreign.R = &lancamentoContabilR{}
				}
				foreign.R.PCCIDDebitoPlanoContaContabil = local
				break
			}
		}
	}

	return nil
}

// LoadPCCIDCreditoRotinaContabilItens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (planoContaContabilL) LoadPCCIDCreditoRotinaContabilItens(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlanoContaContabil interface{}, mods queries.Applicator) error {
	var slice []*PlanoContaContabil
	var object *PlanoContaContabil

	if singular {
		var ok bool
		object, ok = maybePlanoContaContabil.(*PlanoContaContabil)
		if !ok {
			object = new(PlanoContaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlanoContaContabil))
			}
		}
	} else {
		s, ok := maybePlanoContaContabil.(*[]*PlanoContaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlanoContaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &planoContaContabilR{}
		}
		args[object.PCCID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &planoContaContabilR{}
			}
			args[obj.PCCID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil_itens`),
		qm.WhereIn(`rotina_contabil_itens.pcc_id_credito in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load rotina_contabil_itens")
	}

	var resultSlice []*RotinaContabilIten
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice rotina_contabil_itens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on rotina_contabil_itens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil_itens")
	}

	if len(rotinaContabilItenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PCCIDCreditoRotinaContabilItens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &rotinaContabilItenR{}
			}
			foreign.R.PCCIDCreditoPlanoContaContabil = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PCCID == foreign.PCCIDCredito {
				local.R.PCCIDCreditoRotinaContabilItens = append(local.R.PCCIDCreditoRotinaContabilItens, foreign)
				if foreign.R == nil {
					foreign.R = &rotinaContabilItenR{}
				}
				foreign.R.PCCIDCreditoPlanoContaContabil = local
				break
			}
		}
	}

	return nil
}

// LoadPCCIDDebitoRotinaContabilItens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (planoContaContabilL) LoadPCCIDDebitoRotinaContabilItens(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlanoContaContabil interface{}, mods queries.Applicator) error {
	var slice []*PlanoContaContabil
	var object *PlanoContaContabil

	if singular {
		var ok bool
		object, ok = maybePlanoContaContabil.(*PlanoContaContabil)
		if !ok {
			object = new(PlanoContaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlanoContaContabil))
			}
		}
	} else {
		s, ok := maybePlanoContaContabil.(*[]*PlanoContaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlanoContaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlanoContaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &planoContaContabilR{}
		}
		args[object.PCCID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &planoContaContabilR{}
			}
			args[obj.PCCID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil_itens`),
		qm.WhereIn(`rotina_contabil_itens.pcc_id_debito in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load rotina_contabil_itens")
	}

	var resultSlice []*RotinaContabilIten
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice rotina_contabil_itens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on rotina_contabil_itens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil_itens")
	}

	if len(rotinaContabilItenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PCCIDDebitoRotinaContabilItens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &rotinaContabilItenR{}
			}
			foreign.R.PCCIDDebitoPlanoContaContabil = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PCCID == foreign.PCCIDDebito {
				local.R.PCCIDDebitoRotinaContabilItens = append(local.R.PCCIDDebitoRotinaContabilItens, foreign)
				if foreign.R == nil {
					foreign.R = &rotinaContabilItenR{}
				}
				foreign.R.PCCIDDebitoPlanoContaContabil = local
				break
			}
		}
	}

	return nil
}

// SetEmf of the planoContaContabil to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfPlanoContaContabils.
func (o *PlanoContaContabil) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `plano_conta_contabil` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, planoContaContabilPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.PCCID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &planoContaContabilR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfPlanoContaContabils: PlanoContaContabilSlice{o},
		}
	} else {
		related.R.EmfPlanoContaContabils = append(related.R.EmfPlanoContaContabils, o)
	}

	return nil
}

// AddPCCIDCreditoLancamentoContabils adds the given related objects to the existing relationships
// of the plano_conta_contabil, optionally inserting them as new records.
// Appends related to o.R.PCCIDCreditoLancamentoContabils.
// Sets related.R.PCCIDCreditoPlanoContaContabil appropriately.
func (o *PlanoContaContabil) AddPCCIDCreditoLancamentoContabils(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoContabil) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PCCIDCredito = o.PCCID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `lancamento_contabil` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"pcc_id_credito"}),
				strmangle.WhereClause("`", "`", 0, lancamentoContabilPrimaryKeyColumns),
			)
			values := []interface{}{o.PCCID, rel.LCTBID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PCCIDCredito = o.PCCID
		}
	}

	if o.R == nil {
		o.R = &planoContaContabilR{
			PCCIDCreditoLancamentoContabils: related,
		}
	} else {
		o.R.PCCIDCreditoLancamentoContabils = append(o.R.PCCIDCreditoLancamentoContabils, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &lancamentoContabilR{
				PCCIDCreditoPlanoContaContabil: o,
			}
		} else {
			rel.R.PCCIDCreditoPlanoContaContabil = o
		}
	}
	return nil
}

// AddPCCIDDebitoLancamentoContabils adds the given related objects to the existing relationships
// of the plano_conta_contabil, optionally inserting them as new records.
// Appends related to o.R.PCCIDDebitoLancamentoContabils.
// Sets related.R.PCCIDDebitoPlanoContaContabil appropriately.
func (o *PlanoContaContabil) AddPCCIDDebitoLancamentoContabils(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoContabil) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PCCIDDebito = o.PCCID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `lancamento_contabil` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"pcc_id_debito"}),
				strmangle.WhereClause("`", "`", 0, lancamentoContabilPrimaryKeyColumns),
			)
			values := []interface{}{o.PCCID, rel.LCTBID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PCCIDDebito = o.PCCID
		}
	}

	if o.R == nil {
		o.R = &planoContaContabilR{
			PCCIDDebitoLancamentoContabils: related,
		}
	} else {
		o.R.PCCIDDebitoLancamentoContabils = append(o.R.PCCIDDebitoLancamentoContabils, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &lancamentoContabilR{
				PCCIDDebitoPlanoContaContabil: o,
			}
		} else {
			rel.R.PCCIDDebitoPlanoContaContabil = o
		}
	}
	return nil
}

// AddPCCIDCreditoRotinaContabilItens adds the given related objects to the existing relationships
// of the plano_conta_contabil, optionally inserting them as new records.
// Appends related to o.R.PCCIDCreditoRotinaContabilItens.
// Sets related.R.PCCIDCreditoPlanoContaContabil appropriately.
func (o *PlanoContaContabil) AddPCCIDCreditoRotinaContabilItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RotinaContabilIten) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PCCIDCredito = o.PCCID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `rotina_contabil_itens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"pcc_id_credito"}),
				strmangle.WhereClause("`", "`", 0, rotinaContabilItenPrimaryKeyColumns),
			)
			values := []interface{}{o.PCCID, rel.RocID, rel.RociItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PCCIDCredito = o.PCCID
		}
	}

	if o.R == nil {
		o.R = &planoContaContabilR{
			PCCIDCreditoRotinaContabilItens: related,
		}
	} else {
		o.R.PCCIDCreditoRotinaContabilItens = append(o.R.PCCIDCreditoRotinaContabilItens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &rotinaContabilItenR{
				PCCIDCreditoPlanoContaContabil: o,
			}
		} else {
			rel.R.PCCIDCreditoPlanoContaContabil = o
		}
	}
	return nil
}

// AddPCCIDDebitoRotinaContabilItens adds the given related objects to the existing relationships
// of the plano_conta_contabil, optionally inserting them as new records.
// Appends related to o.R.PCCIDDebitoRotinaContabilItens.
// Sets related.R.PCCIDDebitoPlanoContaContabil appropriately.
func (o *PlanoContaContabil) AddPCCIDDebitoRotinaContabilItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RotinaContabilIten) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PCCIDDebito = o.PCCID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `rotina_contabil_itens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"pcc_id_debito"}),
				strmangle.WhereClause("`", "`", 0, rotinaContabilItenPrimaryKeyColumns),
			)
			values := []interface{}{o.PCCID, rel.RocID, rel.RociItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PCCIDDebito = o.PCCID
		}
	}

	if o.R == nil {
		o.R = &planoContaContabilR{
			PCCIDDebitoRotinaContabilItens: related,
		}
	} else {
		o.R.PCCIDDebitoRotinaContabilItens = append(o.R.PCCIDDebitoRotinaContabilItens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &rotinaContabilItenR{
				PCCIDDebitoPlanoContaContabil: o,
			}
		} else {
			rel.R.PCCIDDebitoPlanoContaContabil = o
		}
	}
	return nil
}

// PlanoContaContabils retrieves all the records using an executor.
func PlanoContaContabils(mods ...qm.QueryMod) planoContaContabilQuery {
	mods = append(mods, qm.From("`plano_conta_contabil`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`plano_conta_contabil`.*"})
	}

	return planoContaContabilQuery{q}
}

// FindPlanoContaContabil retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPlanoContaContabil(ctx context.Context, exec boil.ContextExecutor, pCCID int, selectCols ...string) (*PlanoContaContabil, error) {
	planoContaContabilObj := &PlanoContaContabil{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `plano_conta_contabil` where `pcc_id`=?", sel,
	)

	q := queries.Raw(query, pCCID)

	err := q.Bind(ctx, exec, planoContaContabilObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from plano_conta_contabil")
	}

	if err = planoContaContabilObj.doAfterSelectHooks(ctx, exec); err != nil {
		return planoContaContabilObj, err
	}

	return planoContaContabilObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PlanoContaContabil) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no plano_conta_contabil provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(planoContaContabilColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	planoContaContabilInsertCacheMut.RLock()
	cache, cached := planoContaContabilInsertCache[key]
	planoContaContabilInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			planoContaContabilAllColumns,
			planoContaContabilColumnsWithDefault,
			planoContaContabilColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(planoContaContabilType, planoContaContabilMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(planoContaContabilType, planoContaContabilMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `plano_conta_contabil` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `plano_conta_contabil` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `plano_conta_contabil` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, planoContaContabilPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into plano_conta_contabil")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.PCCID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for plano_conta_contabil")
	}

CacheNoHooks:
	if !cached {
		planoContaContabilInsertCacheMut.Lock()
		planoContaContabilInsertCache[key] = cache
		planoContaContabilInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PlanoContaContabil.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PlanoContaContabil) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	planoContaContabilUpdateCacheMut.RLock()
	cache, cached := planoContaContabilUpdateCache[key]
	planoContaContabilUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			planoContaContabilAllColumns,
			planoContaContabilPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update plano_conta_contabil, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `plano_conta_contabil` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, planoContaContabilPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(planoContaContabilType, planoContaContabilMapping, append(wl, planoContaContabilPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update plano_conta_contabil row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for plano_conta_contabil")
	}

	if !cached {
		planoContaContabilUpdateCacheMut.Lock()
		planoContaContabilUpdateCache[key] = cache
		planoContaContabilUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q planoContaContabilQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for plano_conta_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for plano_conta_contabil")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PlanoContaContabilSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), planoContaContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `plano_conta_contabil` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, planoContaContabilPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in planoContaContabil slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all planoContaContabil")
	}
	return rowsAff, nil
}

var mySQLPlanoContaContabilUniqueColumns = []string{
	"pcc_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PlanoContaContabil) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no plano_conta_contabil provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(planoContaContabilColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLPlanoContaContabilUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	planoContaContabilUpsertCacheMut.RLock()
	cache, cached := planoContaContabilUpsertCache[key]
	planoContaContabilUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			planoContaContabilAllColumns,
			planoContaContabilColumnsWithDefault,
			planoContaContabilColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			planoContaContabilAllColumns,
			planoContaContabilPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert plano_conta_contabil, could not build update column list")
		}

		ret := strmangle.SetComplement(planoContaContabilAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`plano_conta_contabil`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `plano_conta_contabil` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(planoContaContabilType, planoContaContabilMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(planoContaContabilType, planoContaContabilMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for plano_conta_contabil")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(planoContaContabilType, planoContaContabilMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for plano_conta_contabil")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for plano_conta_contabil")
	}

CacheNoHooks:
	if !cached {
		planoContaContabilUpsertCacheMut.Lock()
		planoContaContabilUpsertCache[key] = cache
		planoContaContabilUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PlanoContaContabil record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PlanoContaContabil) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PlanoContaContabil provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), planoContaContabilPrimaryKeyMapping)
	sql := "DELETE FROM `plano_conta_contabil` WHERE `pcc_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from plano_conta_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for plano_conta_contabil")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q planoContaContabilQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no planoContaContabilQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from plano_conta_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for plano_conta_contabil")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PlanoContaContabilSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(planoContaContabilBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), planoContaContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `plano_conta_contabil` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, planoContaContabilPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from planoContaContabil slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for plano_conta_contabil")
	}

	if len(planoContaContabilAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PlanoContaContabil) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPlanoContaContabil(ctx, exec, o.PCCID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PlanoContaContabilSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PlanoContaContabilSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), planoContaContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `plano_conta_contabil`.* FROM `plano_conta_contabil` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, planoContaContabilPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PlanoContaContabilSlice")
	}

	*o = slice

	return nil
}

// PlanoContaContabilExists checks if the PlanoContaContabil row exists.
func PlanoContaContabilExists(ctx context.Context, exec boil.ContextExecutor, pCCID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `plano_conta_contabil` where `pcc_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, pCCID)
	}
	row := exec.QueryRowContext(ctx, sql, pCCID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if plano_conta_contabil exists")
	}

	return exists, nil
}

// Exists checks if the PlanoContaContabil row exists.
func (o *PlanoContaContabil) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PlanoContaContabilExists(ctx, exec, o.PCCID)
}
