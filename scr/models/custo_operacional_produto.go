// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CustoOperacionalProduto is an object representing the database table.
type CustoOperacionalProduto struct {
	CopID     int           `boil:"cop_id" json:"cop_id" toml:"cop_id" yaml:"cop_id"`
	DespID    int           `boil:"desp_id" json:"desp_id" toml:"desp_id" yaml:"desp_id"`
	CoppValor types.Decimal `boil:"copp_valor" json:"copp_valor" toml:"copp_valor" yaml:"copp_valor"`

	R *custoOperacionalProdutoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L custoOperacionalProdutoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CustoOperacionalProdutoColumns = struct {
	CopID     string
	DespID    string
	CoppValor string
}{
	CopID:     "cop_id",
	DespID:    "desp_id",
	CoppValor: "copp_valor",
}

var CustoOperacionalProdutoTableColumns = struct {
	CopID     string
	DespID    string
	CoppValor string
}{
	CopID:     "custo_operacional_produto.cop_id",
	DespID:    "custo_operacional_produto.desp_id",
	CoppValor: "custo_operacional_produto.copp_valor",
}

// Generated where

var CustoOperacionalProdutoWhere = struct {
	CopID     whereHelperint
	DespID    whereHelperint
	CoppValor whereHelpertypes_Decimal
}{
	CopID:     whereHelperint{field: "`custo_operacional_produto`.`cop_id`"},
	DespID:    whereHelperint{field: "`custo_operacional_produto`.`desp_id`"},
	CoppValor: whereHelpertypes_Decimal{field: "`custo_operacional_produto`.`copp_valor`"},
}

// CustoOperacionalProdutoRels is where relationship names are stored.
var CustoOperacionalProdutoRels = struct {
	Cop  string
	Desp string
}{
	Cop:  "Cop",
	Desp: "Desp",
}

// custoOperacionalProdutoR is where relationships are stored.
type custoOperacionalProdutoR struct {
	Cop  *CustoOperacional `boil:"Cop" json:"Cop" toml:"Cop" yaml:"Cop"`
	Desp *Despesas         `boil:"Desp" json:"Desp" toml:"Desp" yaml:"Desp"`
}

// NewStruct creates a new relationship struct
func (*custoOperacionalProdutoR) NewStruct() *custoOperacionalProdutoR {
	return &custoOperacionalProdutoR{}
}

func (r *custoOperacionalProdutoR) GetCop() *CustoOperacional {
	if r == nil {
		return nil
	}
	return r.Cop
}

func (r *custoOperacionalProdutoR) GetDesp() *Despesas {
	if r == nil {
		return nil
	}
	return r.Desp
}

// custoOperacionalProdutoL is where Load methods for each relationship are stored.
type custoOperacionalProdutoL struct{}

var (
	custoOperacionalProdutoAllColumns            = []string{"cop_id", "desp_id", "copp_valor"}
	custoOperacionalProdutoColumnsWithoutDefault = []string{"cop_id", "desp_id"}
	custoOperacionalProdutoColumnsWithDefault    = []string{"copp_valor"}
	custoOperacionalProdutoPrimaryKeyColumns     = []string{"cop_id", "desp_id"}
	custoOperacionalProdutoGeneratedColumns      = []string{}
)

type (
	// CustoOperacionalProdutoSlice is an alias for a slice of pointers to CustoOperacionalProduto.
	// This should almost always be used instead of []CustoOperacionalProduto.
	CustoOperacionalProdutoSlice []*CustoOperacionalProduto
	// CustoOperacionalProdutoHook is the signature for custom CustoOperacionalProduto hook methods
	CustoOperacionalProdutoHook func(context.Context, boil.ContextExecutor, *CustoOperacionalProduto) error

	custoOperacionalProdutoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	custoOperacionalProdutoType                 = reflect.TypeOf(&CustoOperacionalProduto{})
	custoOperacionalProdutoMapping              = queries.MakeStructMapping(custoOperacionalProdutoType)
	custoOperacionalProdutoPrimaryKeyMapping, _ = queries.BindMapping(custoOperacionalProdutoType, custoOperacionalProdutoMapping, custoOperacionalProdutoPrimaryKeyColumns)
	custoOperacionalProdutoInsertCacheMut       sync.RWMutex
	custoOperacionalProdutoInsertCache          = make(map[string]insertCache)
	custoOperacionalProdutoUpdateCacheMut       sync.RWMutex
	custoOperacionalProdutoUpdateCache          = make(map[string]updateCache)
	custoOperacionalProdutoUpsertCacheMut       sync.RWMutex
	custoOperacionalProdutoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var custoOperacionalProdutoAfterSelectMu sync.Mutex
var custoOperacionalProdutoAfterSelectHooks []CustoOperacionalProdutoHook

var custoOperacionalProdutoBeforeInsertMu sync.Mutex
var custoOperacionalProdutoBeforeInsertHooks []CustoOperacionalProdutoHook
var custoOperacionalProdutoAfterInsertMu sync.Mutex
var custoOperacionalProdutoAfterInsertHooks []CustoOperacionalProdutoHook

var custoOperacionalProdutoBeforeUpdateMu sync.Mutex
var custoOperacionalProdutoBeforeUpdateHooks []CustoOperacionalProdutoHook
var custoOperacionalProdutoAfterUpdateMu sync.Mutex
var custoOperacionalProdutoAfterUpdateHooks []CustoOperacionalProdutoHook

var custoOperacionalProdutoBeforeDeleteMu sync.Mutex
var custoOperacionalProdutoBeforeDeleteHooks []CustoOperacionalProdutoHook
var custoOperacionalProdutoAfterDeleteMu sync.Mutex
var custoOperacionalProdutoAfterDeleteHooks []CustoOperacionalProdutoHook

var custoOperacionalProdutoBeforeUpsertMu sync.Mutex
var custoOperacionalProdutoBeforeUpsertHooks []CustoOperacionalProdutoHook
var custoOperacionalProdutoAfterUpsertMu sync.Mutex
var custoOperacionalProdutoAfterUpsertHooks []CustoOperacionalProdutoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CustoOperacionalProduto) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CustoOperacionalProduto) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CustoOperacionalProduto) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CustoOperacionalProduto) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CustoOperacionalProduto) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CustoOperacionalProduto) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CustoOperacionalProduto) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CustoOperacionalProduto) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CustoOperacionalProduto) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalProdutoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCustoOperacionalProdutoHook registers your hook function for all future operations.
func AddCustoOperacionalProdutoHook(hookPoint boil.HookPoint, custoOperacionalProdutoHook CustoOperacionalProdutoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		custoOperacionalProdutoAfterSelectMu.Lock()
		custoOperacionalProdutoAfterSelectHooks = append(custoOperacionalProdutoAfterSelectHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		custoOperacionalProdutoBeforeInsertMu.Lock()
		custoOperacionalProdutoBeforeInsertHooks = append(custoOperacionalProdutoBeforeInsertHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		custoOperacionalProdutoAfterInsertMu.Lock()
		custoOperacionalProdutoAfterInsertHooks = append(custoOperacionalProdutoAfterInsertHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		custoOperacionalProdutoBeforeUpdateMu.Lock()
		custoOperacionalProdutoBeforeUpdateHooks = append(custoOperacionalProdutoBeforeUpdateHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		custoOperacionalProdutoAfterUpdateMu.Lock()
		custoOperacionalProdutoAfterUpdateHooks = append(custoOperacionalProdutoAfterUpdateHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		custoOperacionalProdutoBeforeDeleteMu.Lock()
		custoOperacionalProdutoBeforeDeleteHooks = append(custoOperacionalProdutoBeforeDeleteHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		custoOperacionalProdutoAfterDeleteMu.Lock()
		custoOperacionalProdutoAfterDeleteHooks = append(custoOperacionalProdutoAfterDeleteHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		custoOperacionalProdutoBeforeUpsertMu.Lock()
		custoOperacionalProdutoBeforeUpsertHooks = append(custoOperacionalProdutoBeforeUpsertHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		custoOperacionalProdutoAfterUpsertMu.Lock()
		custoOperacionalProdutoAfterUpsertHooks = append(custoOperacionalProdutoAfterUpsertHooks, custoOperacionalProdutoHook)
		custoOperacionalProdutoAfterUpsertMu.Unlock()
	}
}

// One returns a single custoOperacionalProduto record from the query.
func (q custoOperacionalProdutoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CustoOperacionalProduto, error) {
	o := &CustoOperacionalProduto{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for custo_operacional_produto")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CustoOperacionalProduto records from the query.
func (q custoOperacionalProdutoQuery) All(ctx context.Context, exec boil.ContextExecutor) (CustoOperacionalProdutoSlice, error) {
	var o []*CustoOperacionalProduto

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CustoOperacionalProduto slice")
	}

	if len(custoOperacionalProdutoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CustoOperacionalProduto records in the query.
func (q custoOperacionalProdutoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count custo_operacional_produto rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q custoOperacionalProdutoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if custo_operacional_produto exists")
	}

	return count > 0, nil
}

// Cop pointed to by the foreign key.
func (o *CustoOperacionalProduto) Cop(mods ...qm.QueryMod) custoOperacionalQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cop_id` = ?", o.CopID),
	}

	queryMods = append(queryMods, mods...)

	return CustoOperacionals(queryMods...)
}

// Desp pointed to by the foreign key.
func (o *CustoOperacionalProduto) Desp(mods ...qm.QueryMod) despesasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`desp_id` = ?", o.DespID),
	}

	queryMods = append(queryMods, mods...)

	return Despesas(queryMods...)
}

// LoadCop allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (custoOperacionalProdutoL) LoadCop(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCustoOperacionalProduto interface{}, mods queries.Applicator) error {
	var slice []*CustoOperacionalProduto
	var object *CustoOperacionalProduto

	if singular {
		var ok bool
		object, ok = maybeCustoOperacionalProduto.(*CustoOperacionalProduto)
		if !ok {
			object = new(CustoOperacionalProduto)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCustoOperacionalProduto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCustoOperacionalProduto))
			}
		}
	} else {
		s, ok := maybeCustoOperacionalProduto.(*[]*CustoOperacionalProduto)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCustoOperacionalProduto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCustoOperacionalProduto))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &custoOperacionalProdutoR{}
		}
		args[object.CopID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &custoOperacionalProdutoR{}
			}

			args[obj.CopID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`custo_operacional`),
		qm.WhereIn(`custo_operacional.cop_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load CustoOperacional")
	}

	var resultSlice []*CustoOperacional
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice CustoOperacional")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for custo_operacional")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for custo_operacional")
	}

	if len(custoOperacionalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cop = foreign
		if foreign.R == nil {
			foreign.R = &custoOperacionalR{}
		}
		foreign.R.CopCustoOperacionalProdutos = append(foreign.R.CopCustoOperacionalProdutos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CopID == foreign.CopID {
				local.R.Cop = foreign
				if foreign.R == nil {
					foreign.R = &custoOperacionalR{}
				}
				foreign.R.CopCustoOperacionalProdutos = append(foreign.R.CopCustoOperacionalProdutos, local)
				break
			}
		}
	}

	return nil
}

// LoadDesp allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (custoOperacionalProdutoL) LoadDesp(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCustoOperacionalProduto interface{}, mods queries.Applicator) error {
	var slice []*CustoOperacionalProduto
	var object *CustoOperacionalProduto

	if singular {
		var ok bool
		object, ok = maybeCustoOperacionalProduto.(*CustoOperacionalProduto)
		if !ok {
			object = new(CustoOperacionalProduto)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCustoOperacionalProduto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCustoOperacionalProduto))
			}
		}
	} else {
		s, ok := maybeCustoOperacionalProduto.(*[]*CustoOperacionalProduto)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCustoOperacionalProduto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCustoOperacionalProduto))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &custoOperacionalProdutoR{}
		}
		args[object.DespID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &custoOperacionalProdutoR{}
			}

			args[obj.DespID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`despesas`),
		qm.WhereIn(`despesas.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Despesas")
	}

	var resultSlice []*Despesas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Despesas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for despesas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for despesas")
	}

	if len(despesasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Desp = foreign
		if foreign.R == nil {
			foreign.R = &despesasR{}
		}
		foreign.R.DespCustoOperacionalProdutos = append(foreign.R.DespCustoOperacionalProdutos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.DespID == foreign.DespID {
				local.R.Desp = foreign
				if foreign.R == nil {
					foreign.R = &despesasR{}
				}
				foreign.R.DespCustoOperacionalProdutos = append(foreign.R.DespCustoOperacionalProdutos, local)
				break
			}
		}
	}

	return nil
}

// SetCop of the custoOperacionalProduto to the related item.
// Sets o.R.Cop to related.
// Adds o to related.R.CopCustoOperacionalProdutos.
func (o *CustoOperacionalProduto) SetCop(ctx context.Context, exec boil.ContextExecutor, insert bool, related *CustoOperacional) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `custo_operacional_produto` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cop_id"}),
		strmangle.WhereClause("`", "`", 0, custoOperacionalProdutoPrimaryKeyColumns),
	)
	values := []interface{}{related.CopID, o.CopID, o.DespID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CopID = related.CopID
	if o.R == nil {
		o.R = &custoOperacionalProdutoR{
			Cop: related,
		}
	} else {
		o.R.Cop = related
	}

	if related.R == nil {
		related.R = &custoOperacionalR{
			CopCustoOperacionalProdutos: CustoOperacionalProdutoSlice{o},
		}
	} else {
		related.R.CopCustoOperacionalProdutos = append(related.R.CopCustoOperacionalProdutos, o)
	}

	return nil
}

// SetDesp of the custoOperacionalProduto to the related item.
// Sets o.R.Desp to related.
// Adds o to related.R.DespCustoOperacionalProdutos.
func (o *CustoOperacionalProduto) SetDesp(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Despesas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `custo_operacional_produto` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
		strmangle.WhereClause("`", "`", 0, custoOperacionalProdutoPrimaryKeyColumns),
	)
	values := []interface{}{related.DespID, o.CopID, o.DespID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.DespID = related.DespID
	if o.R == nil {
		o.R = &custoOperacionalProdutoR{
			Desp: related,
		}
	} else {
		o.R.Desp = related
	}

	if related.R == nil {
		related.R = &despesasR{
			DespCustoOperacionalProdutos: CustoOperacionalProdutoSlice{o},
		}
	} else {
		related.R.DespCustoOperacionalProdutos = append(related.R.DespCustoOperacionalProdutos, o)
	}

	return nil
}

// CustoOperacionalProdutos retrieves all the records using an executor.
func CustoOperacionalProdutos(mods ...qm.QueryMod) custoOperacionalProdutoQuery {
	mods = append(mods, qm.From("`custo_operacional_produto`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`custo_operacional_produto`.*"})
	}

	return custoOperacionalProdutoQuery{q}
}

// FindCustoOperacionalProduto retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCustoOperacionalProduto(ctx context.Context, exec boil.ContextExecutor, copID int, despID int, selectCols ...string) (*CustoOperacionalProduto, error) {
	custoOperacionalProdutoObj := &CustoOperacionalProduto{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `custo_operacional_produto` where `cop_id`=? AND `desp_id`=?", sel,
	)

	q := queries.Raw(query, copID, despID)

	err := q.Bind(ctx, exec, custoOperacionalProdutoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from custo_operacional_produto")
	}

	if err = custoOperacionalProdutoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return custoOperacionalProdutoObj, err
	}

	return custoOperacionalProdutoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CustoOperacionalProduto) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no custo_operacional_produto provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(custoOperacionalProdutoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	custoOperacionalProdutoInsertCacheMut.RLock()
	cache, cached := custoOperacionalProdutoInsertCache[key]
	custoOperacionalProdutoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			custoOperacionalProdutoAllColumns,
			custoOperacionalProdutoColumnsWithDefault,
			custoOperacionalProdutoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(custoOperacionalProdutoType, custoOperacionalProdutoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(custoOperacionalProdutoType, custoOperacionalProdutoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `custo_operacional_produto` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `custo_operacional_produto` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `custo_operacional_produto` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, custoOperacionalProdutoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into custo_operacional_produto")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CopID,
		o.DespID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for custo_operacional_produto")
	}

CacheNoHooks:
	if !cached {
		custoOperacionalProdutoInsertCacheMut.Lock()
		custoOperacionalProdutoInsertCache[key] = cache
		custoOperacionalProdutoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CustoOperacionalProduto.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CustoOperacionalProduto) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	custoOperacionalProdutoUpdateCacheMut.RLock()
	cache, cached := custoOperacionalProdutoUpdateCache[key]
	custoOperacionalProdutoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			custoOperacionalProdutoAllColumns,
			custoOperacionalProdutoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update custo_operacional_produto, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `custo_operacional_produto` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, custoOperacionalProdutoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(custoOperacionalProdutoType, custoOperacionalProdutoMapping, append(wl, custoOperacionalProdutoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update custo_operacional_produto row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for custo_operacional_produto")
	}

	if !cached {
		custoOperacionalProdutoUpdateCacheMut.Lock()
		custoOperacionalProdutoUpdateCache[key] = cache
		custoOperacionalProdutoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q custoOperacionalProdutoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for custo_operacional_produto")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for custo_operacional_produto")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CustoOperacionalProdutoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), custoOperacionalProdutoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `custo_operacional_produto` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, custoOperacionalProdutoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in custoOperacionalProduto slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all custoOperacionalProduto")
	}
	return rowsAff, nil
}

var mySQLCustoOperacionalProdutoUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CustoOperacionalProduto) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no custo_operacional_produto provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(custoOperacionalProdutoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCustoOperacionalProdutoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	custoOperacionalProdutoUpsertCacheMut.RLock()
	cache, cached := custoOperacionalProdutoUpsertCache[key]
	custoOperacionalProdutoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			custoOperacionalProdutoAllColumns,
			custoOperacionalProdutoColumnsWithDefault,
			custoOperacionalProdutoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			custoOperacionalProdutoAllColumns,
			custoOperacionalProdutoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert custo_operacional_produto, could not build update column list")
		}

		ret := strmangle.SetComplement(custoOperacionalProdutoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`custo_operacional_produto`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `custo_operacional_produto` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(custoOperacionalProdutoType, custoOperacionalProdutoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(custoOperacionalProdutoType, custoOperacionalProdutoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for custo_operacional_produto")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(custoOperacionalProdutoType, custoOperacionalProdutoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for custo_operacional_produto")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for custo_operacional_produto")
	}

CacheNoHooks:
	if !cached {
		custoOperacionalProdutoUpsertCacheMut.Lock()
		custoOperacionalProdutoUpsertCache[key] = cache
		custoOperacionalProdutoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CustoOperacionalProduto record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CustoOperacionalProduto) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CustoOperacionalProduto provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), custoOperacionalProdutoPrimaryKeyMapping)
	sql := "DELETE FROM `custo_operacional_produto` WHERE `cop_id`=? AND `desp_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from custo_operacional_produto")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for custo_operacional_produto")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q custoOperacionalProdutoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no custoOperacionalProdutoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from custo_operacional_produto")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for custo_operacional_produto")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CustoOperacionalProdutoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(custoOperacionalProdutoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), custoOperacionalProdutoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `custo_operacional_produto` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, custoOperacionalProdutoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from custoOperacionalProduto slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for custo_operacional_produto")
	}

	if len(custoOperacionalProdutoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CustoOperacionalProduto) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCustoOperacionalProduto(ctx, exec, o.CopID, o.DespID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CustoOperacionalProdutoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CustoOperacionalProdutoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), custoOperacionalProdutoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `custo_operacional_produto`.* FROM `custo_operacional_produto` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, custoOperacionalProdutoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CustoOperacionalProdutoSlice")
	}

	*o = slice

	return nil
}

// CustoOperacionalProdutoExists checks if the CustoOperacionalProduto row exists.
func CustoOperacionalProdutoExists(ctx context.Context, exec boil.ContextExecutor, copID int, despID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `custo_operacional_produto` where `cop_id`=? AND `desp_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, copID, despID)
	}
	row := exec.QueryRowContext(ctx, sql, copID, despID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if custo_operacional_produto exists")
	}

	return exists, nil
}

// Exists checks if the CustoOperacionalProduto row exists.
func (o *CustoOperacionalProduto) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CustoOperacionalProdutoExists(ctx, exec, o.CopID, o.DespID)
}
