// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testTituloOcorrenciaTipos(t *testing.T) {
	t.Parallel()

	query := TituloOcorrenciaTipos()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testTituloOcorrenciaTiposDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTituloOcorrenciaTiposQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := TituloOcorrenciaTipos().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTituloOcorrenciaTiposSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TituloOcorrenciaTipoSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTituloOcorrenciaTiposExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := TituloOcorrenciaTipoExists(ctx, tx, o.TotID)
	if err != nil {
		t.Errorf("Unable to check if TituloOcorrenciaTipo exists: %s", err)
	}
	if !e {
		t.Errorf("Expected TituloOcorrenciaTipoExists to return true, but got false.")
	}
}

func testTituloOcorrenciaTiposFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	tituloOcorrenciaTipoFound, err := FindTituloOcorrenciaTipo(ctx, tx, o.TotID)
	if err != nil {
		t.Error(err)
	}

	if tituloOcorrenciaTipoFound == nil {
		t.Error("want a record, got nil")
	}
}

func testTituloOcorrenciaTiposBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = TituloOcorrenciaTipos().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testTituloOcorrenciaTiposOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := TituloOcorrenciaTipos().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testTituloOcorrenciaTiposAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	tituloOcorrenciaTipoOne := &TituloOcorrenciaTipo{}
	tituloOcorrenciaTipoTwo := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, tituloOcorrenciaTipoOne, tituloOcorrenciaTipoDBTypes, false, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}
	if err = randomize.Struct(seed, tituloOcorrenciaTipoTwo, tituloOcorrenciaTipoDBTypes, false, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = tituloOcorrenciaTipoOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = tituloOcorrenciaTipoTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := TituloOcorrenciaTipos().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testTituloOcorrenciaTiposCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	tituloOcorrenciaTipoOne := &TituloOcorrenciaTipo{}
	tituloOcorrenciaTipoTwo := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, tituloOcorrenciaTipoOne, tituloOcorrenciaTipoDBTypes, false, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}
	if err = randomize.Struct(seed, tituloOcorrenciaTipoTwo, tituloOcorrenciaTipoDBTypes, false, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = tituloOcorrenciaTipoOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = tituloOcorrenciaTipoTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func tituloOcorrenciaTipoBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func tituloOcorrenciaTipoAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func tituloOcorrenciaTipoAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func tituloOcorrenciaTipoBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func tituloOcorrenciaTipoAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func tituloOcorrenciaTipoBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func tituloOcorrenciaTipoAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func tituloOcorrenciaTipoBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func tituloOcorrenciaTipoAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
	*o = TituloOcorrenciaTipo{}
	return nil
}

func testTituloOcorrenciaTiposHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &TituloOcorrenciaTipo{}
	o := &TituloOcorrenciaTipo{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, false); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo object: %s", err)
	}

	AddTituloOcorrenciaTipoHook(boil.BeforeInsertHook, tituloOcorrenciaTipoBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoBeforeInsertHooks = []TituloOcorrenciaTipoHook{}

	AddTituloOcorrenciaTipoHook(boil.AfterInsertHook, tituloOcorrenciaTipoAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoAfterInsertHooks = []TituloOcorrenciaTipoHook{}

	AddTituloOcorrenciaTipoHook(boil.AfterSelectHook, tituloOcorrenciaTipoAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoAfterSelectHooks = []TituloOcorrenciaTipoHook{}

	AddTituloOcorrenciaTipoHook(boil.BeforeUpdateHook, tituloOcorrenciaTipoBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoBeforeUpdateHooks = []TituloOcorrenciaTipoHook{}

	AddTituloOcorrenciaTipoHook(boil.AfterUpdateHook, tituloOcorrenciaTipoAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoAfterUpdateHooks = []TituloOcorrenciaTipoHook{}

	AddTituloOcorrenciaTipoHook(boil.BeforeDeleteHook, tituloOcorrenciaTipoBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoBeforeDeleteHooks = []TituloOcorrenciaTipoHook{}

	AddTituloOcorrenciaTipoHook(boil.AfterDeleteHook, tituloOcorrenciaTipoAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoAfterDeleteHooks = []TituloOcorrenciaTipoHook{}

	AddTituloOcorrenciaTipoHook(boil.BeforeUpsertHook, tituloOcorrenciaTipoBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoBeforeUpsertHooks = []TituloOcorrenciaTipoHook{}

	AddTituloOcorrenciaTipoHook(boil.AfterUpsertHook, tituloOcorrenciaTipoAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciaTipoAfterUpsertHooks = []TituloOcorrenciaTipoHook{}
}

func testTituloOcorrenciaTiposInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTituloOcorrenciaTiposInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(tituloOcorrenciaTipoColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTituloOcorrenciaTipoToManyTotTituloOcorrencia(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a TituloOcorrenciaTipo
	var b, c TituloOcorrencium

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, tituloOcorrenciumDBTypes, false, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, tituloOcorrenciumDBTypes, false, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.TotID = a.TotID
	c.TotID = a.TotID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.TotTituloOcorrencia().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.TotID == b.TotID {
			bFound = true
		}
		if v.TotID == c.TotID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := TituloOcorrenciaTipoSlice{&a}
	if err = a.L.LoadTotTituloOcorrencia(ctx, tx, false, (*[]*TituloOcorrenciaTipo)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.TotTituloOcorrencia); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.TotTituloOcorrencia = nil
	if err = a.L.LoadTotTituloOcorrencia(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.TotTituloOcorrencia); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testTituloOcorrenciaTipoToManyAddOpTotTituloOcorrencia(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a TituloOcorrenciaTipo
	var b, c, d, e TituloOcorrencium

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, tituloOcorrenciaTipoDBTypes, false, strmangle.SetComplement(tituloOcorrenciaTipoPrimaryKeyColumns, tituloOcorrenciaTipoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*TituloOcorrencium{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, tituloOcorrenciumDBTypes, false, strmangle.SetComplement(tituloOcorrenciumPrimaryKeyColumns, tituloOcorrenciumColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*TituloOcorrencium{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddTotTituloOcorrencia(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.TotID != first.TotID {
			t.Error("foreign key was wrong value", a.TotID, first.TotID)
		}
		if a.TotID != second.TotID {
			t.Error("foreign key was wrong value", a.TotID, second.TotID)
		}

		if first.R.Tot != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Tot != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.TotTituloOcorrencia[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.TotTituloOcorrencia[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.TotTituloOcorrencia().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testTituloOcorrenciaTiposReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTituloOcorrenciaTiposReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TituloOcorrenciaTipoSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTituloOcorrenciaTiposSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := TituloOcorrenciaTipos().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	tituloOcorrenciaTipoDBTypes = map[string]string{`TotID`: `int`, `TotDescricao`: `varchar`}
	_                           = bytes.MinRead
)

func testTituloOcorrenciaTiposUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(tituloOcorrenciaTipoPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(tituloOcorrenciaTipoAllColumns) == len(tituloOcorrenciaTipoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testTituloOcorrenciaTiposSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(tituloOcorrenciaTipoAllColumns) == len(tituloOcorrenciaTipoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, tituloOcorrenciaTipoDBTypes, true, tituloOcorrenciaTipoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(tituloOcorrenciaTipoAllColumns, tituloOcorrenciaTipoPrimaryKeyColumns) {
		fields = tituloOcorrenciaTipoAllColumns
	} else {
		fields = strmangle.SetComplement(
			tituloOcorrenciaTipoAllColumns,
			tituloOcorrenciaTipoPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := TituloOcorrenciaTipoSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testTituloOcorrenciaTiposUpsert(t *testing.T) {
	t.Parallel()

	if len(tituloOcorrenciaTipoAllColumns) == len(tituloOcorrenciaTipoPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLTituloOcorrenciaTipoUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := TituloOcorrenciaTipo{}
	if err = randomize.Struct(seed, &o, tituloOcorrenciaTipoDBTypes, false); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert TituloOcorrenciaTipo: %s", err)
	}

	count, err := TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, tituloOcorrenciaTipoDBTypes, false, tituloOcorrenciaTipoPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert TituloOcorrenciaTipo: %s", err)
	}

	count, err = TituloOcorrenciaTipos().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
