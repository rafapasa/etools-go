// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// NotaFiscalDominio is an object representing the database table.
type NotaFiscalDominio struct {
	NTFID            int         `boil:"ntf_id" json:"ntf_id" toml:"ntf_id" yaml:"ntf_id"`
	NTFDItem         int         `boil:"ntfd_item" json:"ntfd_item" toml:"ntfd_item" yaml:"ntfd_item"`
	NTFDIDIntegracao null.String `boil:"ntfd_id_integracao" json:"ntfd_id_integracao,omitempty" toml:"ntfd_id_integracao" yaml:"ntfd_id_integracao,omitempty"`
	NTFDTipo         null.String `boil:"ntfd_tipo" json:"ntfd_tipo,omitempty" toml:"ntfd_tipo" yaml:"ntfd_tipo,omitempty"`
	NTFDUltimoStatus null.String `boil:"ntfd_ultimo_status" json:"ntfd_ultimo_status,omitempty" toml:"ntfd_ultimo_status" yaml:"ntfd_ultimo_status,omitempty"`
	NTFDVersaoAPI    null.String `boil:"ntfd_versao_api" json:"ntfd_versao_api,omitempty" toml:"ntfd_versao_api" yaml:"ntfd_versao_api,omitempty"`
	NTFDBoxFile      null.Int16  `boil:"ntfd_box_file" json:"ntfd_box_file,omitempty" toml:"ntfd_box_file" yaml:"ntfd_box_file,omitempty"`

	R *notaFiscalDominioR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L notaFiscalDominioL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var NotaFiscalDominioColumns = struct {
	NTFID            string
	NTFDItem         string
	NTFDIDIntegracao string
	NTFDTipo         string
	NTFDUltimoStatus string
	NTFDVersaoAPI    string
	NTFDBoxFile      string
}{
	NTFID:            "ntf_id",
	NTFDItem:         "ntfd_item",
	NTFDIDIntegracao: "ntfd_id_integracao",
	NTFDTipo:         "ntfd_tipo",
	NTFDUltimoStatus: "ntfd_ultimo_status",
	NTFDVersaoAPI:    "ntfd_versao_api",
	NTFDBoxFile:      "ntfd_box_file",
}

var NotaFiscalDominioTableColumns = struct {
	NTFID            string
	NTFDItem         string
	NTFDIDIntegracao string
	NTFDTipo         string
	NTFDUltimoStatus string
	NTFDVersaoAPI    string
	NTFDBoxFile      string
}{
	NTFID:            "nota_fiscal_dominio.ntf_id",
	NTFDItem:         "nota_fiscal_dominio.ntfd_item",
	NTFDIDIntegracao: "nota_fiscal_dominio.ntfd_id_integracao",
	NTFDTipo:         "nota_fiscal_dominio.ntfd_tipo",
	NTFDUltimoStatus: "nota_fiscal_dominio.ntfd_ultimo_status",
	NTFDVersaoAPI:    "nota_fiscal_dominio.ntfd_versao_api",
	NTFDBoxFile:      "nota_fiscal_dominio.ntfd_box_file",
}

// Generated where

var NotaFiscalDominioWhere = struct {
	NTFID            whereHelperint
	NTFDItem         whereHelperint
	NTFDIDIntegracao whereHelpernull_String
	NTFDTipo         whereHelpernull_String
	NTFDUltimoStatus whereHelpernull_String
	NTFDVersaoAPI    whereHelpernull_String
	NTFDBoxFile      whereHelpernull_Int16
}{
	NTFID:            whereHelperint{field: "`nota_fiscal_dominio`.`ntf_id`"},
	NTFDItem:         whereHelperint{field: "`nota_fiscal_dominio`.`ntfd_item`"},
	NTFDIDIntegracao: whereHelpernull_String{field: "`nota_fiscal_dominio`.`ntfd_id_integracao`"},
	NTFDTipo:         whereHelpernull_String{field: "`nota_fiscal_dominio`.`ntfd_tipo`"},
	NTFDUltimoStatus: whereHelpernull_String{field: "`nota_fiscal_dominio`.`ntfd_ultimo_status`"},
	NTFDVersaoAPI:    whereHelpernull_String{field: "`nota_fiscal_dominio`.`ntfd_versao_api`"},
	NTFDBoxFile:      whereHelpernull_Int16{field: "`nota_fiscal_dominio`.`ntfd_box_file`"},
}

// NotaFiscalDominioRels is where relationship names are stored.
var NotaFiscalDominioRels = struct {
	NTF string
}{
	NTF: "NTF",
}

// notaFiscalDominioR is where relationships are stored.
type notaFiscalDominioR struct {
	NTF *NotaFiscal `boil:"NTF" json:"NTF" toml:"NTF" yaml:"NTF"`
}

// NewStruct creates a new relationship struct
func (*notaFiscalDominioR) NewStruct() *notaFiscalDominioR {
	return &notaFiscalDominioR{}
}

func (r *notaFiscalDominioR) GetNTF() *NotaFiscal {
	if r == nil {
		return nil
	}
	return r.NTF
}

// notaFiscalDominioL is where Load methods for each relationship are stored.
type notaFiscalDominioL struct{}

var (
	notaFiscalDominioAllColumns            = []string{"ntf_id", "ntfd_item", "ntfd_id_integracao", "ntfd_tipo", "ntfd_ultimo_status", "ntfd_versao_api", "ntfd_box_file"}
	notaFiscalDominioColumnsWithoutDefault = []string{"ntf_id", "ntfd_item", "ntfd_id_integracao", "ntfd_tipo", "ntfd_ultimo_status", "ntfd_versao_api", "ntfd_box_file"}
	notaFiscalDominioColumnsWithDefault    = []string{}
	notaFiscalDominioPrimaryKeyColumns     = []string{"ntf_id", "ntfd_item"}
	notaFiscalDominioGeneratedColumns      = []string{}
)

type (
	// NotaFiscalDominioSlice is an alias for a slice of pointers to NotaFiscalDominio.
	// This should almost always be used instead of []NotaFiscalDominio.
	NotaFiscalDominioSlice []*NotaFiscalDominio
	// NotaFiscalDominioHook is the signature for custom NotaFiscalDominio hook methods
	NotaFiscalDominioHook func(context.Context, boil.ContextExecutor, *NotaFiscalDominio) error

	notaFiscalDominioQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	notaFiscalDominioType                 = reflect.TypeOf(&NotaFiscalDominio{})
	notaFiscalDominioMapping              = queries.MakeStructMapping(notaFiscalDominioType)
	notaFiscalDominioPrimaryKeyMapping, _ = queries.BindMapping(notaFiscalDominioType, notaFiscalDominioMapping, notaFiscalDominioPrimaryKeyColumns)
	notaFiscalDominioInsertCacheMut       sync.RWMutex
	notaFiscalDominioInsertCache          = make(map[string]insertCache)
	notaFiscalDominioUpdateCacheMut       sync.RWMutex
	notaFiscalDominioUpdateCache          = make(map[string]updateCache)
	notaFiscalDominioUpsertCacheMut       sync.RWMutex
	notaFiscalDominioUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var notaFiscalDominioAfterSelectMu sync.Mutex
var notaFiscalDominioAfterSelectHooks []NotaFiscalDominioHook

var notaFiscalDominioBeforeInsertMu sync.Mutex
var notaFiscalDominioBeforeInsertHooks []NotaFiscalDominioHook
var notaFiscalDominioAfterInsertMu sync.Mutex
var notaFiscalDominioAfterInsertHooks []NotaFiscalDominioHook

var notaFiscalDominioBeforeUpdateMu sync.Mutex
var notaFiscalDominioBeforeUpdateHooks []NotaFiscalDominioHook
var notaFiscalDominioAfterUpdateMu sync.Mutex
var notaFiscalDominioAfterUpdateHooks []NotaFiscalDominioHook

var notaFiscalDominioBeforeDeleteMu sync.Mutex
var notaFiscalDominioBeforeDeleteHooks []NotaFiscalDominioHook
var notaFiscalDominioAfterDeleteMu sync.Mutex
var notaFiscalDominioAfterDeleteHooks []NotaFiscalDominioHook

var notaFiscalDominioBeforeUpsertMu sync.Mutex
var notaFiscalDominioBeforeUpsertHooks []NotaFiscalDominioHook
var notaFiscalDominioAfterUpsertMu sync.Mutex
var notaFiscalDominioAfterUpsertHooks []NotaFiscalDominioHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *NotaFiscalDominio) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *NotaFiscalDominio) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *NotaFiscalDominio) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *NotaFiscalDominio) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *NotaFiscalDominio) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *NotaFiscalDominio) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *NotaFiscalDominio) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *NotaFiscalDominio) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *NotaFiscalDominio) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFiscalDominioAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddNotaFiscalDominioHook registers your hook function for all future operations.
func AddNotaFiscalDominioHook(hookPoint boil.HookPoint, notaFiscalDominioHook NotaFiscalDominioHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		notaFiscalDominioAfterSelectMu.Lock()
		notaFiscalDominioAfterSelectHooks = append(notaFiscalDominioAfterSelectHooks, notaFiscalDominioHook)
		notaFiscalDominioAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		notaFiscalDominioBeforeInsertMu.Lock()
		notaFiscalDominioBeforeInsertHooks = append(notaFiscalDominioBeforeInsertHooks, notaFiscalDominioHook)
		notaFiscalDominioBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		notaFiscalDominioAfterInsertMu.Lock()
		notaFiscalDominioAfterInsertHooks = append(notaFiscalDominioAfterInsertHooks, notaFiscalDominioHook)
		notaFiscalDominioAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		notaFiscalDominioBeforeUpdateMu.Lock()
		notaFiscalDominioBeforeUpdateHooks = append(notaFiscalDominioBeforeUpdateHooks, notaFiscalDominioHook)
		notaFiscalDominioBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		notaFiscalDominioAfterUpdateMu.Lock()
		notaFiscalDominioAfterUpdateHooks = append(notaFiscalDominioAfterUpdateHooks, notaFiscalDominioHook)
		notaFiscalDominioAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		notaFiscalDominioBeforeDeleteMu.Lock()
		notaFiscalDominioBeforeDeleteHooks = append(notaFiscalDominioBeforeDeleteHooks, notaFiscalDominioHook)
		notaFiscalDominioBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		notaFiscalDominioAfterDeleteMu.Lock()
		notaFiscalDominioAfterDeleteHooks = append(notaFiscalDominioAfterDeleteHooks, notaFiscalDominioHook)
		notaFiscalDominioAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		notaFiscalDominioBeforeUpsertMu.Lock()
		notaFiscalDominioBeforeUpsertHooks = append(notaFiscalDominioBeforeUpsertHooks, notaFiscalDominioHook)
		notaFiscalDominioBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		notaFiscalDominioAfterUpsertMu.Lock()
		notaFiscalDominioAfterUpsertHooks = append(notaFiscalDominioAfterUpsertHooks, notaFiscalDominioHook)
		notaFiscalDominioAfterUpsertMu.Unlock()
	}
}

// One returns a single notaFiscalDominio record from the query.
func (q notaFiscalDominioQuery) One(ctx context.Context, exec boil.ContextExecutor) (*NotaFiscalDominio, error) {
	o := &NotaFiscalDominio{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for nota_fiscal_dominio")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all NotaFiscalDominio records from the query.
func (q notaFiscalDominioQuery) All(ctx context.Context, exec boil.ContextExecutor) (NotaFiscalDominioSlice, error) {
	var o []*NotaFiscalDominio

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to NotaFiscalDominio slice")
	}

	if len(notaFiscalDominioAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all NotaFiscalDominio records in the query.
func (q notaFiscalDominioQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count nota_fiscal_dominio rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q notaFiscalDominioQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if nota_fiscal_dominio exists")
	}

	return count > 0, nil
}

// NTF pointed to by the foreign key.
func (o *NotaFiscalDominio) NTF(mods ...qm.QueryMod) notaFiscalQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ntf_id` = ?", o.NTFID),
	}

	queryMods = append(queryMods, mods...)

	return NotaFiscals(queryMods...)
}

// LoadNTF allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (notaFiscalDominioL) LoadNTF(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNotaFiscalDominio interface{}, mods queries.Applicator) error {
	var slice []*NotaFiscalDominio
	var object *NotaFiscalDominio

	if singular {
		var ok bool
		object, ok = maybeNotaFiscalDominio.(*NotaFiscalDominio)
		if !ok {
			object = new(NotaFiscalDominio)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNotaFiscalDominio)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNotaFiscalDominio))
			}
		}
	} else {
		s, ok := maybeNotaFiscalDominio.(*[]*NotaFiscalDominio)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNotaFiscalDominio)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNotaFiscalDominio))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &notaFiscalDominioR{}
		}
		args[object.NTFID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &notaFiscalDominioR{}
			}

			args[obj.NTFID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal`),
		qm.WhereIn(`nota_fiscal.ntf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load NotaFiscal")
	}

	var resultSlice []*NotaFiscal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice NotaFiscal")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for nota_fiscal")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal")
	}

	if len(notaFiscalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.NTF = foreign
		if foreign.R == nil {
			foreign.R = &notaFiscalR{}
		}
		foreign.R.NTFNotaFiscalDominios = append(foreign.R.NTFNotaFiscalDominios, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.NTFID == foreign.NTFID {
				local.R.NTF = foreign
				if foreign.R == nil {
					foreign.R = &notaFiscalR{}
				}
				foreign.R.NTFNotaFiscalDominios = append(foreign.R.NTFNotaFiscalDominios, local)
				break
			}
		}
	}

	return nil
}

// SetNTF of the notaFiscalDominio to the related item.
// Sets o.R.NTF to related.
// Adds o to related.R.NTFNotaFiscalDominios.
func (o *NotaFiscalDominio) SetNTF(ctx context.Context, exec boil.ContextExecutor, insert bool, related *NotaFiscal) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `nota_fiscal_dominio` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ntf_id"}),
		strmangle.WhereClause("`", "`", 0, notaFiscalDominioPrimaryKeyColumns),
	)
	values := []interface{}{related.NTFID, o.NTFID, o.NTFDItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.NTFID = related.NTFID
	if o.R == nil {
		o.R = &notaFiscalDominioR{
			NTF: related,
		}
	} else {
		o.R.NTF = related
	}

	if related.R == nil {
		related.R = &notaFiscalR{
			NTFNotaFiscalDominios: NotaFiscalDominioSlice{o},
		}
	} else {
		related.R.NTFNotaFiscalDominios = append(related.R.NTFNotaFiscalDominios, o)
	}

	return nil
}

// NotaFiscalDominios retrieves all the records using an executor.
func NotaFiscalDominios(mods ...qm.QueryMod) notaFiscalDominioQuery {
	mods = append(mods, qm.From("`nota_fiscal_dominio`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`nota_fiscal_dominio`.*"})
	}

	return notaFiscalDominioQuery{q}
}

// FindNotaFiscalDominio retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindNotaFiscalDominio(ctx context.Context, exec boil.ContextExecutor, nTFID int, nTFDItem int, selectCols ...string) (*NotaFiscalDominio, error) {
	notaFiscalDominioObj := &NotaFiscalDominio{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `nota_fiscal_dominio` where `ntf_id`=? AND `ntfd_item`=?", sel,
	)

	q := queries.Raw(query, nTFID, nTFDItem)

	err := q.Bind(ctx, exec, notaFiscalDominioObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from nota_fiscal_dominio")
	}

	if err = notaFiscalDominioObj.doAfterSelectHooks(ctx, exec); err != nil {
		return notaFiscalDominioObj, err
	}

	return notaFiscalDominioObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *NotaFiscalDominio) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no nota_fiscal_dominio provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(notaFiscalDominioColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	notaFiscalDominioInsertCacheMut.RLock()
	cache, cached := notaFiscalDominioInsertCache[key]
	notaFiscalDominioInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			notaFiscalDominioAllColumns,
			notaFiscalDominioColumnsWithDefault,
			notaFiscalDominioColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(notaFiscalDominioType, notaFiscalDominioMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(notaFiscalDominioType, notaFiscalDominioMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `nota_fiscal_dominio` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `nota_fiscal_dominio` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `nota_fiscal_dominio` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, notaFiscalDominioPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into nota_fiscal_dominio")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.NTFID,
		o.NTFDItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for nota_fiscal_dominio")
	}

CacheNoHooks:
	if !cached {
		notaFiscalDominioInsertCacheMut.Lock()
		notaFiscalDominioInsertCache[key] = cache
		notaFiscalDominioInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the NotaFiscalDominio.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *NotaFiscalDominio) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	notaFiscalDominioUpdateCacheMut.RLock()
	cache, cached := notaFiscalDominioUpdateCache[key]
	notaFiscalDominioUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			notaFiscalDominioAllColumns,
			notaFiscalDominioPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update nota_fiscal_dominio, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `nota_fiscal_dominio` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, notaFiscalDominioPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(notaFiscalDominioType, notaFiscalDominioMapping, append(wl, notaFiscalDominioPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update nota_fiscal_dominio row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for nota_fiscal_dominio")
	}

	if !cached {
		notaFiscalDominioUpdateCacheMut.Lock()
		notaFiscalDominioUpdateCache[key] = cache
		notaFiscalDominioUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q notaFiscalDominioQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for nota_fiscal_dominio")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for nota_fiscal_dominio")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o NotaFiscalDominioSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFiscalDominioPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `nota_fiscal_dominio` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFiscalDominioPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in notaFiscalDominio slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all notaFiscalDominio")
	}
	return rowsAff, nil
}

var mySQLNotaFiscalDominioUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *NotaFiscalDominio) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no nota_fiscal_dominio provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(notaFiscalDominioColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLNotaFiscalDominioUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	notaFiscalDominioUpsertCacheMut.RLock()
	cache, cached := notaFiscalDominioUpsertCache[key]
	notaFiscalDominioUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			notaFiscalDominioAllColumns,
			notaFiscalDominioColumnsWithDefault,
			notaFiscalDominioColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			notaFiscalDominioAllColumns,
			notaFiscalDominioPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert nota_fiscal_dominio, could not build update column list")
		}

		ret := strmangle.SetComplement(notaFiscalDominioAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`nota_fiscal_dominio`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `nota_fiscal_dominio` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(notaFiscalDominioType, notaFiscalDominioMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(notaFiscalDominioType, notaFiscalDominioMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for nota_fiscal_dominio")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(notaFiscalDominioType, notaFiscalDominioMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for nota_fiscal_dominio")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for nota_fiscal_dominio")
	}

CacheNoHooks:
	if !cached {
		notaFiscalDominioUpsertCacheMut.Lock()
		notaFiscalDominioUpsertCache[key] = cache
		notaFiscalDominioUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single NotaFiscalDominio record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *NotaFiscalDominio) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no NotaFiscalDominio provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), notaFiscalDominioPrimaryKeyMapping)
	sql := "DELETE FROM `nota_fiscal_dominio` WHERE `ntf_id`=? AND `ntfd_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from nota_fiscal_dominio")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for nota_fiscal_dominio")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q notaFiscalDominioQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no notaFiscalDominioQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from nota_fiscal_dominio")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for nota_fiscal_dominio")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o NotaFiscalDominioSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(notaFiscalDominioBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFiscalDominioPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `nota_fiscal_dominio` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFiscalDominioPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from notaFiscalDominio slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for nota_fiscal_dominio")
	}

	if len(notaFiscalDominioAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *NotaFiscalDominio) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindNotaFiscalDominio(ctx, exec, o.NTFID, o.NTFDItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *NotaFiscalDominioSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := NotaFiscalDominioSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFiscalDominioPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `nota_fiscal_dominio`.* FROM `nota_fiscal_dominio` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFiscalDominioPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in NotaFiscalDominioSlice")
	}

	*o = slice

	return nil
}

// NotaFiscalDominioExists checks if the NotaFiscalDominio row exists.
func NotaFiscalDominioExists(ctx context.Context, exec boil.ContextExecutor, nTFID int, nTFDItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `nota_fiscal_dominio` where `ntf_id`=? AND `ntfd_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, nTFID, nTFDItem)
	}
	row := exec.QueryRowContext(ctx, sql, nTFID, nTFDItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if nota_fiscal_dominio exists")
	}

	return exists, nil
}

// Exists checks if the NotaFiscalDominio row exists.
func (o *NotaFiscalDominio) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return NotaFiscalDominioExists(ctx, exec, o.NTFID, o.NTFDItem)
}
