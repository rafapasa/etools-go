// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testTituloOcorrencia(t *testing.T) {
	t.Parallel()

	query := TituloOcorrencia()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testTituloOcorrenciaDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTituloOcorrenciaQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := TituloOcorrencia().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTituloOcorrenciaSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TituloOcorrenciumSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTituloOcorrenciaExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := TituloOcorrenciumExists(ctx, tx, o.TitID, o.TitoItem)
	if err != nil {
		t.Errorf("Unable to check if TituloOcorrencium exists: %s", err)
	}
	if !e {
		t.Errorf("Expected TituloOcorrenciumExists to return true, but got false.")
	}
}

func testTituloOcorrenciaFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	tituloOcorrenciumFound, err := FindTituloOcorrencium(ctx, tx, o.TitID, o.TitoItem)
	if err != nil {
		t.Error(err)
	}

	if tituloOcorrenciumFound == nil {
		t.Error("want a record, got nil")
	}
}

func testTituloOcorrenciaBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = TituloOcorrencia().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testTituloOcorrenciaOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := TituloOcorrencia().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testTituloOcorrenciaAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	tituloOcorrenciumOne := &TituloOcorrencium{}
	tituloOcorrenciumTwo := &TituloOcorrencium{}
	if err = randomize.Struct(seed, tituloOcorrenciumOne, tituloOcorrenciumDBTypes, false, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}
	if err = randomize.Struct(seed, tituloOcorrenciumTwo, tituloOcorrenciumDBTypes, false, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = tituloOcorrenciumOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = tituloOcorrenciumTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := TituloOcorrencia().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testTituloOcorrenciaCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	tituloOcorrenciumOne := &TituloOcorrencium{}
	tituloOcorrenciumTwo := &TituloOcorrencium{}
	if err = randomize.Struct(seed, tituloOcorrenciumOne, tituloOcorrenciumDBTypes, false, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}
	if err = randomize.Struct(seed, tituloOcorrenciumTwo, tituloOcorrenciumDBTypes, false, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = tituloOcorrenciumOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = tituloOcorrenciumTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func tituloOcorrenciumBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func tituloOcorrenciumAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func tituloOcorrenciumAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func tituloOcorrenciumBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func tituloOcorrenciumAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func tituloOcorrenciumBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func tituloOcorrenciumAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func tituloOcorrenciumBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func tituloOcorrenciumAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrencium) error {
	*o = TituloOcorrencium{}
	return nil
}

func testTituloOcorrenciaHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &TituloOcorrencium{}
	o := &TituloOcorrencium{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, false); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium object: %s", err)
	}

	AddTituloOcorrenciumHook(boil.BeforeInsertHook, tituloOcorrenciumBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumBeforeInsertHooks = []TituloOcorrenciumHook{}

	AddTituloOcorrenciumHook(boil.AfterInsertHook, tituloOcorrenciumAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumAfterInsertHooks = []TituloOcorrenciumHook{}

	AddTituloOcorrenciumHook(boil.AfterSelectHook, tituloOcorrenciumAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumAfterSelectHooks = []TituloOcorrenciumHook{}

	AddTituloOcorrenciumHook(boil.BeforeUpdateHook, tituloOcorrenciumBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumBeforeUpdateHooks = []TituloOcorrenciumHook{}

	AddTituloOcorrenciumHook(boil.AfterUpdateHook, tituloOcorrenciumAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumAfterUpdateHooks = []TituloOcorrenciumHook{}

	AddTituloOcorrenciumHook(boil.BeforeDeleteHook, tituloOcorrenciumBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumBeforeDeleteHooks = []TituloOcorrenciumHook{}

	AddTituloOcorrenciumHook(boil.AfterDeleteHook, tituloOcorrenciumAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumAfterDeleteHooks = []TituloOcorrenciumHook{}

	AddTituloOcorrenciumHook(boil.BeforeUpsertHook, tituloOcorrenciumBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumBeforeUpsertHooks = []TituloOcorrenciumHook{}

	AddTituloOcorrenciumHook(boil.AfterUpsertHook, tituloOcorrenciumAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	tituloOcorrenciumAfterUpsertHooks = []TituloOcorrenciumHook{}
}

func testTituloOcorrenciaInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTituloOcorrenciaInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(tituloOcorrenciumColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTituloOcorrenciumToOneTituloUsingTit(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local TituloOcorrencium
	var foreign Titulo

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, tituloOcorrenciumDBTypes, false, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, tituloDBTypes, false, tituloColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Titulo struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.TitID = foreign.TitID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Tit().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.TitID != foreign.TitID {
		t.Errorf("want: %v, got %v", foreign.TitID, check.TitID)
	}

	ranAfterSelectHook := false
	AddTituloHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Titulo) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := TituloOcorrenciumSlice{&local}
	if err = local.L.LoadTit(ctx, tx, false, (*[]*TituloOcorrencium)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Tit == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Tit = nil
	if err = local.L.LoadTit(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Tit == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testTituloOcorrenciumToOneTituloOcorrenciaTipoUsingTot(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local TituloOcorrencium
	var foreign TituloOcorrenciaTipo

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, tituloOcorrenciumDBTypes, false, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, tituloOcorrenciaTipoDBTypes, false, tituloOcorrenciaTipoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrenciaTipo struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.TotID = foreign.TotID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Tot().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.TotID != foreign.TotID {
		t.Errorf("want: %v, got %v", foreign.TotID, check.TotID)
	}

	ranAfterSelectHook := false
	AddTituloOcorrenciaTipoHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *TituloOcorrenciaTipo) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := TituloOcorrenciumSlice{&local}
	if err = local.L.LoadTot(ctx, tx, false, (*[]*TituloOcorrencium)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Tot == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Tot = nil
	if err = local.L.LoadTot(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Tot == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testTituloOcorrenciumToOneSetOpTituloUsingTit(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a TituloOcorrencium
	var b, c Titulo

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, tituloOcorrenciumDBTypes, false, strmangle.SetComplement(tituloOcorrenciumPrimaryKeyColumns, tituloOcorrenciumColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, tituloDBTypes, false, strmangle.SetComplement(tituloPrimaryKeyColumns, tituloColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, tituloDBTypes, false, strmangle.SetComplement(tituloPrimaryKeyColumns, tituloColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Titulo{&b, &c} {
		err = a.SetTit(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Tit != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.TitTituloOcorrencia[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.TitID != x.TitID {
			t.Error("foreign key was wrong value", a.TitID)
		}

		if exists, err := TituloOcorrenciumExists(ctx, tx, a.TitID, a.TitoItem); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}
func testTituloOcorrenciumToOneSetOpTituloOcorrenciaTipoUsingTot(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a TituloOcorrencium
	var b, c TituloOcorrenciaTipo

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, tituloOcorrenciumDBTypes, false, strmangle.SetComplement(tituloOcorrenciumPrimaryKeyColumns, tituloOcorrenciumColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, tituloOcorrenciaTipoDBTypes, false, strmangle.SetComplement(tituloOcorrenciaTipoPrimaryKeyColumns, tituloOcorrenciaTipoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, tituloOcorrenciaTipoDBTypes, false, strmangle.SetComplement(tituloOcorrenciaTipoPrimaryKeyColumns, tituloOcorrenciaTipoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*TituloOcorrenciaTipo{&b, &c} {
		err = a.SetTot(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Tot != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.TotTituloOcorrencia[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.TotID != x.TotID {
			t.Error("foreign key was wrong value", a.TotID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.TotID))
		reflect.Indirect(reflect.ValueOf(&a.TotID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.TotID != x.TotID {
			t.Error("foreign key was wrong value", a.TotID, x.TotID)
		}
	}
}

func testTituloOcorrenciaReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTituloOcorrenciaReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TituloOcorrenciumSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTituloOcorrenciaSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := TituloOcorrencia().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	tituloOcorrenciumDBTypes = map[string]string{`TitID`: `int`, `TitoItem`: `int`, `TotID`: `int`, `TitoData`: `date`, `TitoObservacao`: `varchar`}
	_                        = bytes.MinRead
)

func testTituloOcorrenciaUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(tituloOcorrenciumPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(tituloOcorrenciumAllColumns) == len(tituloOcorrenciumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testTituloOcorrenciaSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(tituloOcorrenciumAllColumns) == len(tituloOcorrenciumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &TituloOcorrencium{}
	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, tituloOcorrenciumDBTypes, true, tituloOcorrenciumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(tituloOcorrenciumAllColumns, tituloOcorrenciumPrimaryKeyColumns) {
		fields = tituloOcorrenciumAllColumns
	} else {
		fields = strmangle.SetComplement(
			tituloOcorrenciumAllColumns,
			tituloOcorrenciumPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := TituloOcorrenciumSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testTituloOcorrenciaUpsert(t *testing.T) {
	t.Parallel()

	if len(tituloOcorrenciumAllColumns) == len(tituloOcorrenciumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLTituloOcorrenciumUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := TituloOcorrencium{}
	if err = randomize.Struct(seed, &o, tituloOcorrenciumDBTypes, false); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert TituloOcorrencium: %s", err)
	}

	count, err := TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, tituloOcorrenciumDBTypes, false, tituloOcorrenciumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TituloOcorrencium struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert TituloOcorrencium: %s", err)
	}

	count, err = TituloOcorrencia().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
