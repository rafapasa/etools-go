// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CustoOperacional is an object representing the database table.
type CustoOperacional struct {
	CopID       int       `boil:"cop_id" json:"cop_id" toml:"cop_id" yaml:"cop_id"`
	CopDataini  time.Time `boil:"cop_dataini" json:"cop_dataini" toml:"cop_dataini" yaml:"cop_dataini"`
	CopDatafim  time.Time `boil:"cop_datafim" json:"cop_datafim" toml:"cop_datafim" yaml:"cop_datafim"`
	CopSituacao int       `boil:"cop_situacao" json:"cop_situacao" toml:"cop_situacao" yaml:"cop_situacao"`

	R *custoOperacionalR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L custoOperacionalL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CustoOperacionalColumns = struct {
	CopID       string
	CopDataini  string
	CopDatafim  string
	CopSituacao string
}{
	CopID:       "cop_id",
	CopDataini:  "cop_dataini",
	CopDatafim:  "cop_datafim",
	CopSituacao: "cop_situacao",
}

var CustoOperacionalTableColumns = struct {
	CopID       string
	CopDataini  string
	CopDatafim  string
	CopSituacao string
}{
	CopID:       "custo_operacional.cop_id",
	CopDataini:  "custo_operacional.cop_dataini",
	CopDatafim:  "custo_operacional.cop_datafim",
	CopSituacao: "custo_operacional.cop_situacao",
}

// Generated where

var CustoOperacionalWhere = struct {
	CopID       whereHelperint
	CopDataini  whereHelpertime_Time
	CopDatafim  whereHelpertime_Time
	CopSituacao whereHelperint
}{
	CopID:       whereHelperint{field: "`custo_operacional`.`cop_id`"},
	CopDataini:  whereHelpertime_Time{field: "`custo_operacional`.`cop_dataini`"},
	CopDatafim:  whereHelpertime_Time{field: "`custo_operacional`.`cop_datafim`"},
	CopSituacao: whereHelperint{field: "`custo_operacional`.`cop_situacao`"},
}

// CustoOperacionalRels is where relationship names are stored.
var CustoOperacionalRels = struct {
	CopCustoOperacionalProdutos string
}{
	CopCustoOperacionalProdutos: "CopCustoOperacionalProdutos",
}

// custoOperacionalR is where relationships are stored.
type custoOperacionalR struct {
	CopCustoOperacionalProdutos CustoOperacionalProdutoSlice `boil:"CopCustoOperacionalProdutos" json:"CopCustoOperacionalProdutos" toml:"CopCustoOperacionalProdutos" yaml:"CopCustoOperacionalProdutos"`
}

// NewStruct creates a new relationship struct
func (*custoOperacionalR) NewStruct() *custoOperacionalR {
	return &custoOperacionalR{}
}

func (r *custoOperacionalR) GetCopCustoOperacionalProdutos() CustoOperacionalProdutoSlice {
	if r == nil {
		return nil
	}
	return r.CopCustoOperacionalProdutos
}

// custoOperacionalL is where Load methods for each relationship are stored.
type custoOperacionalL struct{}

var (
	custoOperacionalAllColumns            = []string{"cop_id", "cop_dataini", "cop_datafim", "cop_situacao"}
	custoOperacionalColumnsWithoutDefault = []string{"cop_id", "cop_dataini", "cop_datafim"}
	custoOperacionalColumnsWithDefault    = []string{"cop_situacao"}
	custoOperacionalPrimaryKeyColumns     = []string{"cop_id"}
	custoOperacionalGeneratedColumns      = []string{}
)

type (
	// CustoOperacionalSlice is an alias for a slice of pointers to CustoOperacional.
	// This should almost always be used instead of []CustoOperacional.
	CustoOperacionalSlice []*CustoOperacional
	// CustoOperacionalHook is the signature for custom CustoOperacional hook methods
	CustoOperacionalHook func(context.Context, boil.ContextExecutor, *CustoOperacional) error

	custoOperacionalQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	custoOperacionalType                 = reflect.TypeOf(&CustoOperacional{})
	custoOperacionalMapping              = queries.MakeStructMapping(custoOperacionalType)
	custoOperacionalPrimaryKeyMapping, _ = queries.BindMapping(custoOperacionalType, custoOperacionalMapping, custoOperacionalPrimaryKeyColumns)
	custoOperacionalInsertCacheMut       sync.RWMutex
	custoOperacionalInsertCache          = make(map[string]insertCache)
	custoOperacionalUpdateCacheMut       sync.RWMutex
	custoOperacionalUpdateCache          = make(map[string]updateCache)
	custoOperacionalUpsertCacheMut       sync.RWMutex
	custoOperacionalUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var custoOperacionalAfterSelectMu sync.Mutex
var custoOperacionalAfterSelectHooks []CustoOperacionalHook

var custoOperacionalBeforeInsertMu sync.Mutex
var custoOperacionalBeforeInsertHooks []CustoOperacionalHook
var custoOperacionalAfterInsertMu sync.Mutex
var custoOperacionalAfterInsertHooks []CustoOperacionalHook

var custoOperacionalBeforeUpdateMu sync.Mutex
var custoOperacionalBeforeUpdateHooks []CustoOperacionalHook
var custoOperacionalAfterUpdateMu sync.Mutex
var custoOperacionalAfterUpdateHooks []CustoOperacionalHook

var custoOperacionalBeforeDeleteMu sync.Mutex
var custoOperacionalBeforeDeleteHooks []CustoOperacionalHook
var custoOperacionalAfterDeleteMu sync.Mutex
var custoOperacionalAfterDeleteHooks []CustoOperacionalHook

var custoOperacionalBeforeUpsertMu sync.Mutex
var custoOperacionalBeforeUpsertHooks []CustoOperacionalHook
var custoOperacionalAfterUpsertMu sync.Mutex
var custoOperacionalAfterUpsertHooks []CustoOperacionalHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CustoOperacional) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CustoOperacional) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CustoOperacional) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CustoOperacional) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CustoOperacional) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CustoOperacional) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CustoOperacional) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CustoOperacional) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CustoOperacional) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range custoOperacionalAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCustoOperacionalHook registers your hook function for all future operations.
func AddCustoOperacionalHook(hookPoint boil.HookPoint, custoOperacionalHook CustoOperacionalHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		custoOperacionalAfterSelectMu.Lock()
		custoOperacionalAfterSelectHooks = append(custoOperacionalAfterSelectHooks, custoOperacionalHook)
		custoOperacionalAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		custoOperacionalBeforeInsertMu.Lock()
		custoOperacionalBeforeInsertHooks = append(custoOperacionalBeforeInsertHooks, custoOperacionalHook)
		custoOperacionalBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		custoOperacionalAfterInsertMu.Lock()
		custoOperacionalAfterInsertHooks = append(custoOperacionalAfterInsertHooks, custoOperacionalHook)
		custoOperacionalAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		custoOperacionalBeforeUpdateMu.Lock()
		custoOperacionalBeforeUpdateHooks = append(custoOperacionalBeforeUpdateHooks, custoOperacionalHook)
		custoOperacionalBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		custoOperacionalAfterUpdateMu.Lock()
		custoOperacionalAfterUpdateHooks = append(custoOperacionalAfterUpdateHooks, custoOperacionalHook)
		custoOperacionalAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		custoOperacionalBeforeDeleteMu.Lock()
		custoOperacionalBeforeDeleteHooks = append(custoOperacionalBeforeDeleteHooks, custoOperacionalHook)
		custoOperacionalBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		custoOperacionalAfterDeleteMu.Lock()
		custoOperacionalAfterDeleteHooks = append(custoOperacionalAfterDeleteHooks, custoOperacionalHook)
		custoOperacionalAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		custoOperacionalBeforeUpsertMu.Lock()
		custoOperacionalBeforeUpsertHooks = append(custoOperacionalBeforeUpsertHooks, custoOperacionalHook)
		custoOperacionalBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		custoOperacionalAfterUpsertMu.Lock()
		custoOperacionalAfterUpsertHooks = append(custoOperacionalAfterUpsertHooks, custoOperacionalHook)
		custoOperacionalAfterUpsertMu.Unlock()
	}
}

// One returns a single custoOperacional record from the query.
func (q custoOperacionalQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CustoOperacional, error) {
	o := &CustoOperacional{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for custo_operacional")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CustoOperacional records from the query.
func (q custoOperacionalQuery) All(ctx context.Context, exec boil.ContextExecutor) (CustoOperacionalSlice, error) {
	var o []*CustoOperacional

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CustoOperacional slice")
	}

	if len(custoOperacionalAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CustoOperacional records in the query.
func (q custoOperacionalQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count custo_operacional rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q custoOperacionalQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if custo_operacional exists")
	}

	return count > 0, nil
}

// CopCustoOperacionalProdutos retrieves all the custo_operacional_produto's CustoOperacionalProdutos with an executor via cop_id column.
func (o *CustoOperacional) CopCustoOperacionalProdutos(mods ...qm.QueryMod) custoOperacionalProdutoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`custo_operacional_produto`.`cop_id`=?", o.CopID),
	)

	return CustoOperacionalProdutos(queryMods...)
}

// LoadCopCustoOperacionalProdutos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (custoOperacionalL) LoadCopCustoOperacionalProdutos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCustoOperacional interface{}, mods queries.Applicator) error {
	var slice []*CustoOperacional
	var object *CustoOperacional

	if singular {
		var ok bool
		object, ok = maybeCustoOperacional.(*CustoOperacional)
		if !ok {
			object = new(CustoOperacional)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCustoOperacional)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCustoOperacional))
			}
		}
	} else {
		s, ok := maybeCustoOperacional.(*[]*CustoOperacional)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCustoOperacional)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCustoOperacional))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &custoOperacionalR{}
		}
		args[object.CopID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &custoOperacionalR{}
			}
			args[obj.CopID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`custo_operacional_produto`),
		qm.WhereIn(`custo_operacional_produto.cop_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load custo_operacional_produto")
	}

	var resultSlice []*CustoOperacionalProduto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice custo_operacional_produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on custo_operacional_produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for custo_operacional_produto")
	}

	if len(custoOperacionalProdutoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CopCustoOperacionalProdutos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &custoOperacionalProdutoR{}
			}
			foreign.R.Cop = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.CopID == foreign.CopID {
				local.R.CopCustoOperacionalProdutos = append(local.R.CopCustoOperacionalProdutos, foreign)
				if foreign.R == nil {
					foreign.R = &custoOperacionalProdutoR{}
				}
				foreign.R.Cop = local
				break
			}
		}
	}

	return nil
}

// AddCopCustoOperacionalProdutos adds the given related objects to the existing relationships
// of the custo_operacional, optionally inserting them as new records.
// Appends related to o.R.CopCustoOperacionalProdutos.
// Sets related.R.Cop appropriately.
func (o *CustoOperacional) AddCopCustoOperacionalProdutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CustoOperacionalProduto) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CopID = o.CopID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `custo_operacional_produto` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"cop_id"}),
				strmangle.WhereClause("`", "`", 0, custoOperacionalProdutoPrimaryKeyColumns),
			)
			values := []interface{}{o.CopID, rel.CopID, rel.DespID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CopID = o.CopID
		}
	}

	if o.R == nil {
		o.R = &custoOperacionalR{
			CopCustoOperacionalProdutos: related,
		}
	} else {
		o.R.CopCustoOperacionalProdutos = append(o.R.CopCustoOperacionalProdutos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &custoOperacionalProdutoR{
				Cop: o,
			}
		} else {
			rel.R.Cop = o
		}
	}
	return nil
}

// CustoOperacionals retrieves all the records using an executor.
func CustoOperacionals(mods ...qm.QueryMod) custoOperacionalQuery {
	mods = append(mods, qm.From("`custo_operacional`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`custo_operacional`.*"})
	}

	return custoOperacionalQuery{q}
}

// FindCustoOperacional retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCustoOperacional(ctx context.Context, exec boil.ContextExecutor, copID int, selectCols ...string) (*CustoOperacional, error) {
	custoOperacionalObj := &CustoOperacional{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `custo_operacional` where `cop_id`=?", sel,
	)

	q := queries.Raw(query, copID)

	err := q.Bind(ctx, exec, custoOperacionalObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from custo_operacional")
	}

	if err = custoOperacionalObj.doAfterSelectHooks(ctx, exec); err != nil {
		return custoOperacionalObj, err
	}

	return custoOperacionalObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CustoOperacional) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no custo_operacional provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(custoOperacionalColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	custoOperacionalInsertCacheMut.RLock()
	cache, cached := custoOperacionalInsertCache[key]
	custoOperacionalInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			custoOperacionalAllColumns,
			custoOperacionalColumnsWithDefault,
			custoOperacionalColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(custoOperacionalType, custoOperacionalMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(custoOperacionalType, custoOperacionalMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `custo_operacional` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `custo_operacional` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `custo_operacional` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, custoOperacionalPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into custo_operacional")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CopID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for custo_operacional")
	}

CacheNoHooks:
	if !cached {
		custoOperacionalInsertCacheMut.Lock()
		custoOperacionalInsertCache[key] = cache
		custoOperacionalInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CustoOperacional.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CustoOperacional) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	custoOperacionalUpdateCacheMut.RLock()
	cache, cached := custoOperacionalUpdateCache[key]
	custoOperacionalUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			custoOperacionalAllColumns,
			custoOperacionalPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update custo_operacional, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `custo_operacional` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, custoOperacionalPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(custoOperacionalType, custoOperacionalMapping, append(wl, custoOperacionalPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update custo_operacional row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for custo_operacional")
	}

	if !cached {
		custoOperacionalUpdateCacheMut.Lock()
		custoOperacionalUpdateCache[key] = cache
		custoOperacionalUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q custoOperacionalQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for custo_operacional")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for custo_operacional")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CustoOperacionalSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), custoOperacionalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `custo_operacional` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, custoOperacionalPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in custoOperacional slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all custoOperacional")
	}
	return rowsAff, nil
}

var mySQLCustoOperacionalUniqueColumns = []string{
	"cop_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CustoOperacional) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no custo_operacional provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(custoOperacionalColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCustoOperacionalUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	custoOperacionalUpsertCacheMut.RLock()
	cache, cached := custoOperacionalUpsertCache[key]
	custoOperacionalUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			custoOperacionalAllColumns,
			custoOperacionalColumnsWithDefault,
			custoOperacionalColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			custoOperacionalAllColumns,
			custoOperacionalPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert custo_operacional, could not build update column list")
		}

		ret := strmangle.SetComplement(custoOperacionalAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`custo_operacional`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `custo_operacional` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(custoOperacionalType, custoOperacionalMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(custoOperacionalType, custoOperacionalMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for custo_operacional")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(custoOperacionalType, custoOperacionalMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for custo_operacional")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for custo_operacional")
	}

CacheNoHooks:
	if !cached {
		custoOperacionalUpsertCacheMut.Lock()
		custoOperacionalUpsertCache[key] = cache
		custoOperacionalUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CustoOperacional record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CustoOperacional) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CustoOperacional provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), custoOperacionalPrimaryKeyMapping)
	sql := "DELETE FROM `custo_operacional` WHERE `cop_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from custo_operacional")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for custo_operacional")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q custoOperacionalQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no custoOperacionalQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from custo_operacional")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for custo_operacional")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CustoOperacionalSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(custoOperacionalBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), custoOperacionalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `custo_operacional` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, custoOperacionalPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from custoOperacional slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for custo_operacional")
	}

	if len(custoOperacionalAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CustoOperacional) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCustoOperacional(ctx, exec, o.CopID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CustoOperacionalSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CustoOperacionalSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), custoOperacionalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `custo_operacional`.* FROM `custo_operacional` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, custoOperacionalPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CustoOperacionalSlice")
	}

	*o = slice

	return nil
}

// CustoOperacionalExists checks if the CustoOperacional row exists.
func CustoOperacionalExists(ctx context.Context, exec boil.ContextExecutor, copID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `custo_operacional` where `cop_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, copID)
	}
	row := exec.QueryRowContext(ctx, sql, copID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if custo_operacional exists")
	}

	return exists, nil
}

// Exists checks if the CustoOperacional row exists.
func (o *CustoOperacional) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CustoOperacionalExists(ctx, exec, o.CopID)
}
