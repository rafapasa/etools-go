// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// MargemLucro is an object representing the database table.
type MargemLucro struct {
	MGLID           int           `boil:"mgl_id" json:"mgl_id" toml:"mgl_id" yaml:"mgl_id"`
	EmfID           int           `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	MGLValorinicial types.Decimal `boil:"mgl_valorinicial" json:"mgl_valorinicial" toml:"mgl_valorinicial" yaml:"mgl_valorinicial"`
	MGLValorfinal   types.Decimal `boil:"mgl_valorfinal" json:"mgl_valorfinal" toml:"mgl_valorfinal" yaml:"mgl_valorfinal"`
	MGLMargem       types.Decimal `boil:"mgl_margem" json:"mgl_margem" toml:"mgl_margem" yaml:"mgl_margem"`

	R *margemLucroR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L margemLucroL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MargemLucroColumns = struct {
	MGLID           string
	EmfID           string
	MGLValorinicial string
	MGLValorfinal   string
	MGLMargem       string
}{
	MGLID:           "mgl_id",
	EmfID:           "emf_id",
	MGLValorinicial: "mgl_valorinicial",
	MGLValorfinal:   "mgl_valorfinal",
	MGLMargem:       "mgl_margem",
}

var MargemLucroTableColumns = struct {
	MGLID           string
	EmfID           string
	MGLValorinicial string
	MGLValorfinal   string
	MGLMargem       string
}{
	MGLID:           "margem_lucro.mgl_id",
	EmfID:           "margem_lucro.emf_id",
	MGLValorinicial: "margem_lucro.mgl_valorinicial",
	MGLValorfinal:   "margem_lucro.mgl_valorfinal",
	MGLMargem:       "margem_lucro.mgl_margem",
}

// Generated where

var MargemLucroWhere = struct {
	MGLID           whereHelperint
	EmfID           whereHelperint
	MGLValorinicial whereHelpertypes_Decimal
	MGLValorfinal   whereHelpertypes_Decimal
	MGLMargem       whereHelpertypes_Decimal
}{
	MGLID:           whereHelperint{field: "`margem_lucro`.`mgl_id`"},
	EmfID:           whereHelperint{field: "`margem_lucro`.`emf_id`"},
	MGLValorinicial: whereHelpertypes_Decimal{field: "`margem_lucro`.`mgl_valorinicial`"},
	MGLValorfinal:   whereHelpertypes_Decimal{field: "`margem_lucro`.`mgl_valorfinal`"},
	MGLMargem:       whereHelpertypes_Decimal{field: "`margem_lucro`.`mgl_margem`"},
}

// MargemLucroRels is where relationship names are stored.
var MargemLucroRels = struct {
	Emf string
}{
	Emf: "Emf",
}

// margemLucroR is where relationships are stored.
type margemLucroR struct {
	Emf *EmpresaFilial `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
}

// NewStruct creates a new relationship struct
func (*margemLucroR) NewStruct() *margemLucroR {
	return &margemLucroR{}
}

func (r *margemLucroR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

// margemLucroL is where Load methods for each relationship are stored.
type margemLucroL struct{}

var (
	margemLucroAllColumns            = []string{"mgl_id", "emf_id", "mgl_valorinicial", "mgl_valorfinal", "mgl_margem"}
	margemLucroColumnsWithoutDefault = []string{"mgl_id", "emf_id", "mgl_valorinicial", "mgl_valorfinal", "mgl_margem"}
	margemLucroColumnsWithDefault    = []string{}
	margemLucroPrimaryKeyColumns     = []string{"mgl_id"}
	margemLucroGeneratedColumns      = []string{}
)

type (
	// MargemLucroSlice is an alias for a slice of pointers to MargemLucro.
	// This should almost always be used instead of []MargemLucro.
	MargemLucroSlice []*MargemLucro
	// MargemLucroHook is the signature for custom MargemLucro hook methods
	MargemLucroHook func(context.Context, boil.ContextExecutor, *MargemLucro) error

	margemLucroQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	margemLucroType                 = reflect.TypeOf(&MargemLucro{})
	margemLucroMapping              = queries.MakeStructMapping(margemLucroType)
	margemLucroPrimaryKeyMapping, _ = queries.BindMapping(margemLucroType, margemLucroMapping, margemLucroPrimaryKeyColumns)
	margemLucroInsertCacheMut       sync.RWMutex
	margemLucroInsertCache          = make(map[string]insertCache)
	margemLucroUpdateCacheMut       sync.RWMutex
	margemLucroUpdateCache          = make(map[string]updateCache)
	margemLucroUpsertCacheMut       sync.RWMutex
	margemLucroUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var margemLucroAfterSelectMu sync.Mutex
var margemLucroAfterSelectHooks []MargemLucroHook

var margemLucroBeforeInsertMu sync.Mutex
var margemLucroBeforeInsertHooks []MargemLucroHook
var margemLucroAfterInsertMu sync.Mutex
var margemLucroAfterInsertHooks []MargemLucroHook

var margemLucroBeforeUpdateMu sync.Mutex
var margemLucroBeforeUpdateHooks []MargemLucroHook
var margemLucroAfterUpdateMu sync.Mutex
var margemLucroAfterUpdateHooks []MargemLucroHook

var margemLucroBeforeDeleteMu sync.Mutex
var margemLucroBeforeDeleteHooks []MargemLucroHook
var margemLucroAfterDeleteMu sync.Mutex
var margemLucroAfterDeleteHooks []MargemLucroHook

var margemLucroBeforeUpsertMu sync.Mutex
var margemLucroBeforeUpsertHooks []MargemLucroHook
var margemLucroAfterUpsertMu sync.Mutex
var margemLucroAfterUpsertHooks []MargemLucroHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MargemLucro) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MargemLucro) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MargemLucro) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MargemLucro) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MargemLucro) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MargemLucro) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MargemLucro) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MargemLucro) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MargemLucro) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range margemLucroAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMargemLucroHook registers your hook function for all future operations.
func AddMargemLucroHook(hookPoint boil.HookPoint, margemLucroHook MargemLucroHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		margemLucroAfterSelectMu.Lock()
		margemLucroAfterSelectHooks = append(margemLucroAfterSelectHooks, margemLucroHook)
		margemLucroAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		margemLucroBeforeInsertMu.Lock()
		margemLucroBeforeInsertHooks = append(margemLucroBeforeInsertHooks, margemLucroHook)
		margemLucroBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		margemLucroAfterInsertMu.Lock()
		margemLucroAfterInsertHooks = append(margemLucroAfterInsertHooks, margemLucroHook)
		margemLucroAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		margemLucroBeforeUpdateMu.Lock()
		margemLucroBeforeUpdateHooks = append(margemLucroBeforeUpdateHooks, margemLucroHook)
		margemLucroBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		margemLucroAfterUpdateMu.Lock()
		margemLucroAfterUpdateHooks = append(margemLucroAfterUpdateHooks, margemLucroHook)
		margemLucroAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		margemLucroBeforeDeleteMu.Lock()
		margemLucroBeforeDeleteHooks = append(margemLucroBeforeDeleteHooks, margemLucroHook)
		margemLucroBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		margemLucroAfterDeleteMu.Lock()
		margemLucroAfterDeleteHooks = append(margemLucroAfterDeleteHooks, margemLucroHook)
		margemLucroAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		margemLucroBeforeUpsertMu.Lock()
		margemLucroBeforeUpsertHooks = append(margemLucroBeforeUpsertHooks, margemLucroHook)
		margemLucroBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		margemLucroAfterUpsertMu.Lock()
		margemLucroAfterUpsertHooks = append(margemLucroAfterUpsertHooks, margemLucroHook)
		margemLucroAfterUpsertMu.Unlock()
	}
}

// One returns a single margemLucro record from the query.
func (q margemLucroQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MargemLucro, error) {
	o := &MargemLucro{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for margem_lucro")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MargemLucro records from the query.
func (q margemLucroQuery) All(ctx context.Context, exec boil.ContextExecutor) (MargemLucroSlice, error) {
	var o []*MargemLucro

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MargemLucro slice")
	}

	if len(margemLucroAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MargemLucro records in the query.
func (q margemLucroQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count margem_lucro rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q margemLucroQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if margem_lucro exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *MargemLucro) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (margemLucroL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMargemLucro interface{}, mods queries.Applicator) error {
	var slice []*MargemLucro
	var object *MargemLucro

	if singular {
		var ok bool
		object, ok = maybeMargemLucro.(*MargemLucro)
		if !ok {
			object = new(MargemLucro)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeMargemLucro)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeMargemLucro))
			}
		}
	} else {
		s, ok := maybeMargemLucro.(*[]*MargemLucro)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeMargemLucro)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeMargemLucro))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &margemLucroR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &margemLucroR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfMargemLucros = append(foreign.R.EmfMargemLucros, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfMargemLucros = append(foreign.R.EmfMargemLucros, local)
				break
			}
		}
	}

	return nil
}

// SetEmf of the margemLucro to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfMargemLucros.
func (o *MargemLucro) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `margem_lucro` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, margemLucroPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.MGLID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &margemLucroR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfMargemLucros: MargemLucroSlice{o},
		}
	} else {
		related.R.EmfMargemLucros = append(related.R.EmfMargemLucros, o)
	}

	return nil
}

// MargemLucros retrieves all the records using an executor.
func MargemLucros(mods ...qm.QueryMod) margemLucroQuery {
	mods = append(mods, qm.From("`margem_lucro`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`margem_lucro`.*"})
	}

	return margemLucroQuery{q}
}

// FindMargemLucro retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMargemLucro(ctx context.Context, exec boil.ContextExecutor, mGLID int, selectCols ...string) (*MargemLucro, error) {
	margemLucroObj := &MargemLucro{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `margem_lucro` where `mgl_id`=?", sel,
	)

	q := queries.Raw(query, mGLID)

	err := q.Bind(ctx, exec, margemLucroObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from margem_lucro")
	}

	if err = margemLucroObj.doAfterSelectHooks(ctx, exec); err != nil {
		return margemLucroObj, err
	}

	return margemLucroObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MargemLucro) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no margem_lucro provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(margemLucroColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	margemLucroInsertCacheMut.RLock()
	cache, cached := margemLucroInsertCache[key]
	margemLucroInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			margemLucroAllColumns,
			margemLucroColumnsWithDefault,
			margemLucroColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(margemLucroType, margemLucroMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(margemLucroType, margemLucroMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `margem_lucro` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `margem_lucro` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `margem_lucro` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, margemLucroPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into margem_lucro")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.MGLID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for margem_lucro")
	}

CacheNoHooks:
	if !cached {
		margemLucroInsertCacheMut.Lock()
		margemLucroInsertCache[key] = cache
		margemLucroInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MargemLucro.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MargemLucro) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	margemLucroUpdateCacheMut.RLock()
	cache, cached := margemLucroUpdateCache[key]
	margemLucroUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			margemLucroAllColumns,
			margemLucroPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update margem_lucro, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `margem_lucro` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, margemLucroPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(margemLucroType, margemLucroMapping, append(wl, margemLucroPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update margem_lucro row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for margem_lucro")
	}

	if !cached {
		margemLucroUpdateCacheMut.Lock()
		margemLucroUpdateCache[key] = cache
		margemLucroUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q margemLucroQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for margem_lucro")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for margem_lucro")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MargemLucroSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), margemLucroPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `margem_lucro` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, margemLucroPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in margemLucro slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all margemLucro")
	}
	return rowsAff, nil
}

var mySQLMargemLucroUniqueColumns = []string{
	"mgl_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MargemLucro) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no margem_lucro provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(margemLucroColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMargemLucroUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	margemLucroUpsertCacheMut.RLock()
	cache, cached := margemLucroUpsertCache[key]
	margemLucroUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			margemLucroAllColumns,
			margemLucroColumnsWithDefault,
			margemLucroColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			margemLucroAllColumns,
			margemLucroPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert margem_lucro, could not build update column list")
		}

		ret := strmangle.SetComplement(margemLucroAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`margem_lucro`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `margem_lucro` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(margemLucroType, margemLucroMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(margemLucroType, margemLucroMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for margem_lucro")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(margemLucroType, margemLucroMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for margem_lucro")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for margem_lucro")
	}

CacheNoHooks:
	if !cached {
		margemLucroUpsertCacheMut.Lock()
		margemLucroUpsertCache[key] = cache
		margemLucroUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MargemLucro record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MargemLucro) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MargemLucro provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), margemLucroPrimaryKeyMapping)
	sql := "DELETE FROM `margem_lucro` WHERE `mgl_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from margem_lucro")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for margem_lucro")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q margemLucroQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no margemLucroQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from margem_lucro")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for margem_lucro")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MargemLucroSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(margemLucroBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), margemLucroPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `margem_lucro` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, margemLucroPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from margemLucro slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for margem_lucro")
	}

	if len(margemLucroAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MargemLucro) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMargemLucro(ctx, exec, o.MGLID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MargemLucroSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MargemLucroSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), margemLucroPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `margem_lucro`.* FROM `margem_lucro` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, margemLucroPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MargemLucroSlice")
	}

	*o = slice

	return nil
}

// MargemLucroExists checks if the MargemLucro row exists.
func MargemLucroExists(ctx context.Context, exec boil.ContextExecutor, mGLID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `margem_lucro` where `mgl_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, mGLID)
	}
	row := exec.QueryRowContext(ctx, sql, mGLID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if margem_lucro exists")
	}

	return exists, nil
}

// Exists checks if the MargemLucro row exists.
func (o *MargemLucro) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return MargemLucroExists(ctx, exec, o.MGLID)
}
