// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// OrdemProducaoItem is an object representing the database table.
type OrdemProducaoItem struct {
	OrpID              int           `boil:"orp_id" json:"orp_id" toml:"orp_id" yaml:"orp_id"`
	OpiItem            int           `boil:"opi_item" json:"opi_item" toml:"opi_item" yaml:"opi_item"`
	ProID              int           `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	OpiQuantsolicitada types.Decimal `boil:"opi_quantsolicitada" json:"opi_quantsolicitada" toml:"opi_quantsolicitada" yaml:"opi_quantsolicitada"`
	OpiQuantproduzida  types.Decimal `boil:"opi_quantproduzida" json:"opi_quantproduzida" toml:"opi_quantproduzida" yaml:"opi_quantproduzida"`
	OpiLote            int           `boil:"opi_lote" json:"opi_lote" toml:"opi_lote" yaml:"opi_lote"`

	R *ordemProducaoItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ordemProducaoItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var OrdemProducaoItemColumns = struct {
	OrpID              string
	OpiItem            string
	ProID              string
	OpiQuantsolicitada string
	OpiQuantproduzida  string
	OpiLote            string
}{
	OrpID:              "orp_id",
	OpiItem:            "opi_item",
	ProID:              "pro_id",
	OpiQuantsolicitada: "opi_quantsolicitada",
	OpiQuantproduzida:  "opi_quantproduzida",
	OpiLote:            "opi_lote",
}

var OrdemProducaoItemTableColumns = struct {
	OrpID              string
	OpiItem            string
	ProID              string
	OpiQuantsolicitada string
	OpiQuantproduzida  string
	OpiLote            string
}{
	OrpID:              "ordem_producao_item.orp_id",
	OpiItem:            "ordem_producao_item.opi_item",
	ProID:              "ordem_producao_item.pro_id",
	OpiQuantsolicitada: "ordem_producao_item.opi_quantsolicitada",
	OpiQuantproduzida:  "ordem_producao_item.opi_quantproduzida",
	OpiLote:            "ordem_producao_item.opi_lote",
}

// Generated where

var OrdemProducaoItemWhere = struct {
	OrpID              whereHelperint
	OpiItem            whereHelperint
	ProID              whereHelperint
	OpiQuantsolicitada whereHelpertypes_Decimal
	OpiQuantproduzida  whereHelpertypes_Decimal
	OpiLote            whereHelperint
}{
	OrpID:              whereHelperint{field: "`ordem_producao_item`.`orp_id`"},
	OpiItem:            whereHelperint{field: "`ordem_producao_item`.`opi_item`"},
	ProID:              whereHelperint{field: "`ordem_producao_item`.`pro_id`"},
	OpiQuantsolicitada: whereHelpertypes_Decimal{field: "`ordem_producao_item`.`opi_quantsolicitada`"},
	OpiQuantproduzida:  whereHelpertypes_Decimal{field: "`ordem_producao_item`.`opi_quantproduzida`"},
	OpiLote:            whereHelperint{field: "`ordem_producao_item`.`opi_lote`"},
}

// OrdemProducaoItemRels is where relationship names are stored.
var OrdemProducaoItemRels = struct {
	Orp string
	Pro string
}{
	Orp: "Orp",
	Pro: "Pro",
}

// ordemProducaoItemR is where relationships are stored.
type ordemProducaoItemR struct {
	Orp *OrdemProducao `boil:"Orp" json:"Orp" toml:"Orp" yaml:"Orp"`
	Pro *Produto       `boil:"Pro" json:"Pro" toml:"Pro" yaml:"Pro"`
}

// NewStruct creates a new relationship struct
func (*ordemProducaoItemR) NewStruct() *ordemProducaoItemR {
	return &ordemProducaoItemR{}
}

func (r *ordemProducaoItemR) GetOrp() *OrdemProducao {
	if r == nil {
		return nil
	}
	return r.Orp
}

func (r *ordemProducaoItemR) GetPro() *Produto {
	if r == nil {
		return nil
	}
	return r.Pro
}

// ordemProducaoItemL is where Load methods for each relationship are stored.
type ordemProducaoItemL struct{}

var (
	ordemProducaoItemAllColumns            = []string{"orp_id", "opi_item", "pro_id", "opi_quantsolicitada", "opi_quantproduzida", "opi_lote"}
	ordemProducaoItemColumnsWithoutDefault = []string{"orp_id", "opi_item", "pro_id", "opi_quantsolicitada", "opi_quantproduzida", "opi_lote"}
	ordemProducaoItemColumnsWithDefault    = []string{}
	ordemProducaoItemPrimaryKeyColumns     = []string{"orp_id", "opi_item"}
	ordemProducaoItemGeneratedColumns      = []string{}
)

type (
	// OrdemProducaoItemSlice is an alias for a slice of pointers to OrdemProducaoItem.
	// This should almost always be used instead of []OrdemProducaoItem.
	OrdemProducaoItemSlice []*OrdemProducaoItem
	// OrdemProducaoItemHook is the signature for custom OrdemProducaoItem hook methods
	OrdemProducaoItemHook func(context.Context, boil.ContextExecutor, *OrdemProducaoItem) error

	ordemProducaoItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ordemProducaoItemType                 = reflect.TypeOf(&OrdemProducaoItem{})
	ordemProducaoItemMapping              = queries.MakeStructMapping(ordemProducaoItemType)
	ordemProducaoItemPrimaryKeyMapping, _ = queries.BindMapping(ordemProducaoItemType, ordemProducaoItemMapping, ordemProducaoItemPrimaryKeyColumns)
	ordemProducaoItemInsertCacheMut       sync.RWMutex
	ordemProducaoItemInsertCache          = make(map[string]insertCache)
	ordemProducaoItemUpdateCacheMut       sync.RWMutex
	ordemProducaoItemUpdateCache          = make(map[string]updateCache)
	ordemProducaoItemUpsertCacheMut       sync.RWMutex
	ordemProducaoItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ordemProducaoItemAfterSelectMu sync.Mutex
var ordemProducaoItemAfterSelectHooks []OrdemProducaoItemHook

var ordemProducaoItemBeforeInsertMu sync.Mutex
var ordemProducaoItemBeforeInsertHooks []OrdemProducaoItemHook
var ordemProducaoItemAfterInsertMu sync.Mutex
var ordemProducaoItemAfterInsertHooks []OrdemProducaoItemHook

var ordemProducaoItemBeforeUpdateMu sync.Mutex
var ordemProducaoItemBeforeUpdateHooks []OrdemProducaoItemHook
var ordemProducaoItemAfterUpdateMu sync.Mutex
var ordemProducaoItemAfterUpdateHooks []OrdemProducaoItemHook

var ordemProducaoItemBeforeDeleteMu sync.Mutex
var ordemProducaoItemBeforeDeleteHooks []OrdemProducaoItemHook
var ordemProducaoItemAfterDeleteMu sync.Mutex
var ordemProducaoItemAfterDeleteHooks []OrdemProducaoItemHook

var ordemProducaoItemBeforeUpsertMu sync.Mutex
var ordemProducaoItemBeforeUpsertHooks []OrdemProducaoItemHook
var ordemProducaoItemAfterUpsertMu sync.Mutex
var ordemProducaoItemAfterUpsertHooks []OrdemProducaoItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *OrdemProducaoItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *OrdemProducaoItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *OrdemProducaoItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *OrdemProducaoItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *OrdemProducaoItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *OrdemProducaoItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *OrdemProducaoItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *OrdemProducaoItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *OrdemProducaoItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ordemProducaoItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddOrdemProducaoItemHook registers your hook function for all future operations.
func AddOrdemProducaoItemHook(hookPoint boil.HookPoint, ordemProducaoItemHook OrdemProducaoItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ordemProducaoItemAfterSelectMu.Lock()
		ordemProducaoItemAfterSelectHooks = append(ordemProducaoItemAfterSelectHooks, ordemProducaoItemHook)
		ordemProducaoItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		ordemProducaoItemBeforeInsertMu.Lock()
		ordemProducaoItemBeforeInsertHooks = append(ordemProducaoItemBeforeInsertHooks, ordemProducaoItemHook)
		ordemProducaoItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		ordemProducaoItemAfterInsertMu.Lock()
		ordemProducaoItemAfterInsertHooks = append(ordemProducaoItemAfterInsertHooks, ordemProducaoItemHook)
		ordemProducaoItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		ordemProducaoItemBeforeUpdateMu.Lock()
		ordemProducaoItemBeforeUpdateHooks = append(ordemProducaoItemBeforeUpdateHooks, ordemProducaoItemHook)
		ordemProducaoItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		ordemProducaoItemAfterUpdateMu.Lock()
		ordemProducaoItemAfterUpdateHooks = append(ordemProducaoItemAfterUpdateHooks, ordemProducaoItemHook)
		ordemProducaoItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		ordemProducaoItemBeforeDeleteMu.Lock()
		ordemProducaoItemBeforeDeleteHooks = append(ordemProducaoItemBeforeDeleteHooks, ordemProducaoItemHook)
		ordemProducaoItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		ordemProducaoItemAfterDeleteMu.Lock()
		ordemProducaoItemAfterDeleteHooks = append(ordemProducaoItemAfterDeleteHooks, ordemProducaoItemHook)
		ordemProducaoItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		ordemProducaoItemBeforeUpsertMu.Lock()
		ordemProducaoItemBeforeUpsertHooks = append(ordemProducaoItemBeforeUpsertHooks, ordemProducaoItemHook)
		ordemProducaoItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		ordemProducaoItemAfterUpsertMu.Lock()
		ordemProducaoItemAfterUpsertHooks = append(ordemProducaoItemAfterUpsertHooks, ordemProducaoItemHook)
		ordemProducaoItemAfterUpsertMu.Unlock()
	}
}

// One returns a single ordemProducaoItem record from the query.
func (q ordemProducaoItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*OrdemProducaoItem, error) {
	o := &OrdemProducaoItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ordem_producao_item")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all OrdemProducaoItem records from the query.
func (q ordemProducaoItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (OrdemProducaoItemSlice, error) {
	var o []*OrdemProducaoItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to OrdemProducaoItem slice")
	}

	if len(ordemProducaoItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all OrdemProducaoItem records in the query.
func (q ordemProducaoItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ordem_producao_item rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ordemProducaoItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ordem_producao_item exists")
	}

	return count > 0, nil
}

// Orp pointed to by the foreign key.
func (o *OrdemProducaoItem) Orp(mods ...qm.QueryMod) ordemProducaoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`orp_id` = ?", o.OrpID),
	}

	queryMods = append(queryMods, mods...)

	return OrdemProducaos(queryMods...)
}

// Pro pointed to by the foreign key.
func (o *OrdemProducaoItem) Pro(mods ...qm.QueryMod) produtoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pro_id` = ?", o.ProID),
	}

	queryMods = append(queryMods, mods...)

	return Produtos(queryMods...)
}

// LoadOrp allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ordemProducaoItemL) LoadOrp(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOrdemProducaoItem interface{}, mods queries.Applicator) error {
	var slice []*OrdemProducaoItem
	var object *OrdemProducaoItem

	if singular {
		var ok bool
		object, ok = maybeOrdemProducaoItem.(*OrdemProducaoItem)
		if !ok {
			object = new(OrdemProducaoItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeOrdemProducaoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeOrdemProducaoItem))
			}
		}
	} else {
		s, ok := maybeOrdemProducaoItem.(*[]*OrdemProducaoItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeOrdemProducaoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeOrdemProducaoItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ordemProducaoItemR{}
		}
		args[object.OrpID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ordemProducaoItemR{}
			}

			args[obj.OrpID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ordem_producao`),
		qm.WhereIn(`ordem_producao.orp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load OrdemProducao")
	}

	var resultSlice []*OrdemProducao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice OrdemProducao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ordem_producao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ordem_producao")
	}

	if len(ordemProducaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Orp = foreign
		if foreign.R == nil {
			foreign.R = &ordemProducaoR{}
		}
		foreign.R.OrpOrdemProducaoItems = append(foreign.R.OrpOrdemProducaoItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.OrpID == foreign.OrpID {
				local.R.Orp = foreign
				if foreign.R == nil {
					foreign.R = &ordemProducaoR{}
				}
				foreign.R.OrpOrdemProducaoItems = append(foreign.R.OrpOrdemProducaoItems, local)
				break
			}
		}
	}

	return nil
}

// LoadPro allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ordemProducaoItemL) LoadPro(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOrdemProducaoItem interface{}, mods queries.Applicator) error {
	var slice []*OrdemProducaoItem
	var object *OrdemProducaoItem

	if singular {
		var ok bool
		object, ok = maybeOrdemProducaoItem.(*OrdemProducaoItem)
		if !ok {
			object = new(OrdemProducaoItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeOrdemProducaoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeOrdemProducaoItem))
			}
		}
	} else {
		s, ok := maybeOrdemProducaoItem.(*[]*OrdemProducaoItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeOrdemProducaoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeOrdemProducaoItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ordemProducaoItemR{}
		}
		args[object.ProID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ordemProducaoItemR{}
			}

			args[obj.ProID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.pro_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pro = foreign
		if foreign.R == nil {
			foreign.R = &produtoR{}
		}
		foreign.R.ProOrdemProducaoItems = append(foreign.R.ProOrdemProducaoItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProID == foreign.ProID {
				local.R.Pro = foreign
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.ProOrdemProducaoItems = append(foreign.R.ProOrdemProducaoItems, local)
				break
			}
		}
	}

	return nil
}

// SetOrp of the ordemProducaoItem to the related item.
// Sets o.R.Orp to related.
// Adds o to related.R.OrpOrdemProducaoItems.
func (o *OrdemProducaoItem) SetOrp(ctx context.Context, exec boil.ContextExecutor, insert bool, related *OrdemProducao) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ordem_producao_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"orp_id"}),
		strmangle.WhereClause("`", "`", 0, ordemProducaoItemPrimaryKeyColumns),
	)
	values := []interface{}{related.OrpID, o.OrpID, o.OpiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.OrpID = related.OrpID
	if o.R == nil {
		o.R = &ordemProducaoItemR{
			Orp: related,
		}
	} else {
		o.R.Orp = related
	}

	if related.R == nil {
		related.R = &ordemProducaoR{
			OrpOrdemProducaoItems: OrdemProducaoItemSlice{o},
		}
	} else {
		related.R.OrpOrdemProducaoItems = append(related.R.OrpOrdemProducaoItems, o)
	}

	return nil
}

// SetPro of the ordemProducaoItem to the related item.
// Sets o.R.Pro to related.
// Adds o to related.R.ProOrdemProducaoItems.
func (o *OrdemProducaoItem) SetPro(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Produto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ordem_producao_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pro_id"}),
		strmangle.WhereClause("`", "`", 0, ordemProducaoItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ProID, o.OrpID, o.OpiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProID = related.ProID
	if o.R == nil {
		o.R = &ordemProducaoItemR{
			Pro: related,
		}
	} else {
		o.R.Pro = related
	}

	if related.R == nil {
		related.R = &produtoR{
			ProOrdemProducaoItems: OrdemProducaoItemSlice{o},
		}
	} else {
		related.R.ProOrdemProducaoItems = append(related.R.ProOrdemProducaoItems, o)
	}

	return nil
}

// OrdemProducaoItems retrieves all the records using an executor.
func OrdemProducaoItems(mods ...qm.QueryMod) ordemProducaoItemQuery {
	mods = append(mods, qm.From("`ordem_producao_item`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`ordem_producao_item`.*"})
	}

	return ordemProducaoItemQuery{q}
}

// FindOrdemProducaoItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindOrdemProducaoItem(ctx context.Context, exec boil.ContextExecutor, orpID int, opiItem int, selectCols ...string) (*OrdemProducaoItem, error) {
	ordemProducaoItemObj := &OrdemProducaoItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ordem_producao_item` where `orp_id`=? AND `opi_item`=?", sel,
	)

	q := queries.Raw(query, orpID, opiItem)

	err := q.Bind(ctx, exec, ordemProducaoItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ordem_producao_item")
	}

	if err = ordemProducaoItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ordemProducaoItemObj, err
	}

	return ordemProducaoItemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *OrdemProducaoItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ordem_producao_item provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ordemProducaoItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ordemProducaoItemInsertCacheMut.RLock()
	cache, cached := ordemProducaoItemInsertCache[key]
	ordemProducaoItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ordemProducaoItemAllColumns,
			ordemProducaoItemColumnsWithDefault,
			ordemProducaoItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ordemProducaoItemType, ordemProducaoItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ordemProducaoItemType, ordemProducaoItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ordem_producao_item` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ordem_producao_item` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ordem_producao_item` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ordemProducaoItemPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ordem_producao_item")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.OrpID,
		o.OpiItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ordem_producao_item")
	}

CacheNoHooks:
	if !cached {
		ordemProducaoItemInsertCacheMut.Lock()
		ordemProducaoItemInsertCache[key] = cache
		ordemProducaoItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the OrdemProducaoItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *OrdemProducaoItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ordemProducaoItemUpdateCacheMut.RLock()
	cache, cached := ordemProducaoItemUpdateCache[key]
	ordemProducaoItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ordemProducaoItemAllColumns,
			ordemProducaoItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ordem_producao_item, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ordem_producao_item` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ordemProducaoItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ordemProducaoItemType, ordemProducaoItemMapping, append(wl, ordemProducaoItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ordem_producao_item row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ordem_producao_item")
	}

	if !cached {
		ordemProducaoItemUpdateCacheMut.Lock()
		ordemProducaoItemUpdateCache[key] = cache
		ordemProducaoItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ordemProducaoItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ordem_producao_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ordem_producao_item")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o OrdemProducaoItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ordemProducaoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ordem_producao_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ordemProducaoItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ordemProducaoItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ordemProducaoItem")
	}
	return rowsAff, nil
}

var mySQLOrdemProducaoItemUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *OrdemProducaoItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ordem_producao_item provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ordemProducaoItemColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLOrdemProducaoItemUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ordemProducaoItemUpsertCacheMut.RLock()
	cache, cached := ordemProducaoItemUpsertCache[key]
	ordemProducaoItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			ordemProducaoItemAllColumns,
			ordemProducaoItemColumnsWithDefault,
			ordemProducaoItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ordemProducaoItemAllColumns,
			ordemProducaoItemPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ordem_producao_item, could not build update column list")
		}

		ret := strmangle.SetComplement(ordemProducaoItemAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`ordem_producao_item`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ordem_producao_item` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ordemProducaoItemType, ordemProducaoItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ordemProducaoItemType, ordemProducaoItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ordem_producao_item")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ordemProducaoItemType, ordemProducaoItemMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ordem_producao_item")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ordem_producao_item")
	}

CacheNoHooks:
	if !cached {
		ordemProducaoItemUpsertCacheMut.Lock()
		ordemProducaoItemUpsertCache[key] = cache
		ordemProducaoItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single OrdemProducaoItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *OrdemProducaoItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no OrdemProducaoItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ordemProducaoItemPrimaryKeyMapping)
	sql := "DELETE FROM `ordem_producao_item` WHERE `orp_id`=? AND `opi_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ordem_producao_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ordem_producao_item")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ordemProducaoItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ordemProducaoItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ordem_producao_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ordem_producao_item")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o OrdemProducaoItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ordemProducaoItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ordemProducaoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ordem_producao_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ordemProducaoItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ordemProducaoItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ordem_producao_item")
	}

	if len(ordemProducaoItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *OrdemProducaoItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindOrdemProducaoItem(ctx, exec, o.OrpID, o.OpiItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *OrdemProducaoItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := OrdemProducaoItemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ordemProducaoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ordem_producao_item`.* FROM `ordem_producao_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ordemProducaoItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in OrdemProducaoItemSlice")
	}

	*o = slice

	return nil
}

// OrdemProducaoItemExists checks if the OrdemProducaoItem row exists.
func OrdemProducaoItemExists(ctx context.Context, exec boil.ContextExecutor, orpID int, opiItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ordem_producao_item` where `orp_id`=? AND `opi_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, orpID, opiItem)
	}
	row := exec.QueryRowContext(ctx, sql, orpID, opiItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ordem_producao_item exists")
	}

	return exists, nil
}

// Exists checks if the OrdemProducaoItem row exists.
func (o *OrdemProducaoItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return OrdemProducaoItemExists(ctx, exec, o.OrpID, o.OpiItem)
}
