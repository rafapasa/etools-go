// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// DocumentoVendaPagamentoCDC is an object representing the database table.
type DocumentoVendaPagamentoCDC struct {
	CDCID            int               `boil:"cdc_id" json:"cdc_id" toml:"cdc_id" yaml:"cdc_id"`
	DDVID            int               `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	DVPItem          int               `boil:"dvp_item" json:"dvp_item" toml:"dvp_item" yaml:"dvp_item"`
	DVPCDCValor      types.NullDecimal `boil:"dvpcdc_valor" json:"dvpcdc_valor,omitempty" toml:"dvpcdc_valor" yaml:"dvpcdc_valor,omitempty"`
	DVPCDCPercentual types.NullDecimal `boil:"dvpcdc_percentual" json:"dvpcdc_percentual,omitempty" toml:"dvpcdc_percentual" yaml:"dvpcdc_percentual,omitempty"`

	R *documentoVendaPagamentoCDCR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L documentoVendaPagamentoCDCL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DocumentoVendaPagamentoCDCColumns = struct {
	CDCID            string
	DDVID            string
	DVPItem          string
	DVPCDCValor      string
	DVPCDCPercentual string
}{
	CDCID:            "cdc_id",
	DDVID:            "ddv_id",
	DVPItem:          "dvp_item",
	DVPCDCValor:      "dvpcdc_valor",
	DVPCDCPercentual: "dvpcdc_percentual",
}

var DocumentoVendaPagamentoCDCTableColumns = struct {
	CDCID            string
	DDVID            string
	DVPItem          string
	DVPCDCValor      string
	DVPCDCPercentual string
}{
	CDCID:            "documento_venda_pagamento_cdc.cdc_id",
	DDVID:            "documento_venda_pagamento_cdc.ddv_id",
	DVPItem:          "documento_venda_pagamento_cdc.dvp_item",
	DVPCDCValor:      "documento_venda_pagamento_cdc.dvpcdc_valor",
	DVPCDCPercentual: "documento_venda_pagamento_cdc.dvpcdc_percentual",
}

// Generated where

var DocumentoVendaPagamentoCDCWhere = struct {
	CDCID            whereHelperint
	DDVID            whereHelperint
	DVPItem          whereHelperint
	DVPCDCValor      whereHelpertypes_NullDecimal
	DVPCDCPercentual whereHelpertypes_NullDecimal
}{
	CDCID:            whereHelperint{field: "`documento_venda_pagamento_cdc`.`cdc_id`"},
	DDVID:            whereHelperint{field: "`documento_venda_pagamento_cdc`.`ddv_id`"},
	DVPItem:          whereHelperint{field: "`documento_venda_pagamento_cdc`.`dvp_item`"},
	DVPCDCValor:      whereHelpertypes_NullDecimal{field: "`documento_venda_pagamento_cdc`.`dvpcdc_valor`"},
	DVPCDCPercentual: whereHelpertypes_NullDecimal{field: "`documento_venda_pagamento_cdc`.`dvpcdc_percentual`"},
}

// DocumentoVendaPagamentoCDCRels is where relationship names are stored.
var DocumentoVendaPagamentoCDCRels = struct {
	CDC string
}{
	CDC: "CDC",
}

// documentoVendaPagamentoCDCR is where relationships are stored.
type documentoVendaPagamentoCDCR struct {
	CDC *CentroDeCusto `boil:"CDC" json:"CDC" toml:"CDC" yaml:"CDC"`
}

// NewStruct creates a new relationship struct
func (*documentoVendaPagamentoCDCR) NewStruct() *documentoVendaPagamentoCDCR {
	return &documentoVendaPagamentoCDCR{}
}

func (r *documentoVendaPagamentoCDCR) GetCDC() *CentroDeCusto {
	if r == nil {
		return nil
	}
	return r.CDC
}

// documentoVendaPagamentoCDCL is where Load methods for each relationship are stored.
type documentoVendaPagamentoCDCL struct{}

var (
	documentoVendaPagamentoCDCAllColumns            = []string{"cdc_id", "ddv_id", "dvp_item", "dvpcdc_valor", "dvpcdc_percentual"}
	documentoVendaPagamentoCDCColumnsWithoutDefault = []string{"cdc_id", "ddv_id", "dvp_item", "dvpcdc_valor", "dvpcdc_percentual"}
	documentoVendaPagamentoCDCColumnsWithDefault    = []string{}
	documentoVendaPagamentoCDCPrimaryKeyColumns     = []string{"ddv_id", "cdc_id", "dvp_item"}
	documentoVendaPagamentoCDCGeneratedColumns      = []string{}
)

type (
	// DocumentoVendaPagamentoCDCSlice is an alias for a slice of pointers to DocumentoVendaPagamentoCDC.
	// This should almost always be used instead of []DocumentoVendaPagamentoCDC.
	DocumentoVendaPagamentoCDCSlice []*DocumentoVendaPagamentoCDC
	// DocumentoVendaPagamentoCDCHook is the signature for custom DocumentoVendaPagamentoCDC hook methods
	DocumentoVendaPagamentoCDCHook func(context.Context, boil.ContextExecutor, *DocumentoVendaPagamentoCDC) error

	documentoVendaPagamentoCDCQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	documentoVendaPagamentoCDCType                 = reflect.TypeOf(&DocumentoVendaPagamentoCDC{})
	documentoVendaPagamentoCDCMapping              = queries.MakeStructMapping(documentoVendaPagamentoCDCType)
	documentoVendaPagamentoCDCPrimaryKeyMapping, _ = queries.BindMapping(documentoVendaPagamentoCDCType, documentoVendaPagamentoCDCMapping, documentoVendaPagamentoCDCPrimaryKeyColumns)
	documentoVendaPagamentoCDCInsertCacheMut       sync.RWMutex
	documentoVendaPagamentoCDCInsertCache          = make(map[string]insertCache)
	documentoVendaPagamentoCDCUpdateCacheMut       sync.RWMutex
	documentoVendaPagamentoCDCUpdateCache          = make(map[string]updateCache)
	documentoVendaPagamentoCDCUpsertCacheMut       sync.RWMutex
	documentoVendaPagamentoCDCUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var documentoVendaPagamentoCDCAfterSelectMu sync.Mutex
var documentoVendaPagamentoCDCAfterSelectHooks []DocumentoVendaPagamentoCDCHook

var documentoVendaPagamentoCDCBeforeInsertMu sync.Mutex
var documentoVendaPagamentoCDCBeforeInsertHooks []DocumentoVendaPagamentoCDCHook
var documentoVendaPagamentoCDCAfterInsertMu sync.Mutex
var documentoVendaPagamentoCDCAfterInsertHooks []DocumentoVendaPagamentoCDCHook

var documentoVendaPagamentoCDCBeforeUpdateMu sync.Mutex
var documentoVendaPagamentoCDCBeforeUpdateHooks []DocumentoVendaPagamentoCDCHook
var documentoVendaPagamentoCDCAfterUpdateMu sync.Mutex
var documentoVendaPagamentoCDCAfterUpdateHooks []DocumentoVendaPagamentoCDCHook

var documentoVendaPagamentoCDCBeforeDeleteMu sync.Mutex
var documentoVendaPagamentoCDCBeforeDeleteHooks []DocumentoVendaPagamentoCDCHook
var documentoVendaPagamentoCDCAfterDeleteMu sync.Mutex
var documentoVendaPagamentoCDCAfterDeleteHooks []DocumentoVendaPagamentoCDCHook

var documentoVendaPagamentoCDCBeforeUpsertMu sync.Mutex
var documentoVendaPagamentoCDCBeforeUpsertHooks []DocumentoVendaPagamentoCDCHook
var documentoVendaPagamentoCDCAfterUpsertMu sync.Mutex
var documentoVendaPagamentoCDCAfterUpsertHooks []DocumentoVendaPagamentoCDCHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DocumentoVendaPagamentoCDC) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DocumentoVendaPagamentoCDC) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DocumentoVendaPagamentoCDC) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DocumentoVendaPagamentoCDC) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DocumentoVendaPagamentoCDC) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DocumentoVendaPagamentoCDC) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DocumentoVendaPagamentoCDC) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DocumentoVendaPagamentoCDC) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DocumentoVendaPagamentoCDC) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaPagamentoCDCAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDocumentoVendaPagamentoCDCHook registers your hook function for all future operations.
func AddDocumentoVendaPagamentoCDCHook(hookPoint boil.HookPoint, documentoVendaPagamentoCDCHook DocumentoVendaPagamentoCDCHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		documentoVendaPagamentoCDCAfterSelectMu.Lock()
		documentoVendaPagamentoCDCAfterSelectHooks = append(documentoVendaPagamentoCDCAfterSelectHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		documentoVendaPagamentoCDCBeforeInsertMu.Lock()
		documentoVendaPagamentoCDCBeforeInsertHooks = append(documentoVendaPagamentoCDCBeforeInsertHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		documentoVendaPagamentoCDCAfterInsertMu.Lock()
		documentoVendaPagamentoCDCAfterInsertHooks = append(documentoVendaPagamentoCDCAfterInsertHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		documentoVendaPagamentoCDCBeforeUpdateMu.Lock()
		documentoVendaPagamentoCDCBeforeUpdateHooks = append(documentoVendaPagamentoCDCBeforeUpdateHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		documentoVendaPagamentoCDCAfterUpdateMu.Lock()
		documentoVendaPagamentoCDCAfterUpdateHooks = append(documentoVendaPagamentoCDCAfterUpdateHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		documentoVendaPagamentoCDCBeforeDeleteMu.Lock()
		documentoVendaPagamentoCDCBeforeDeleteHooks = append(documentoVendaPagamentoCDCBeforeDeleteHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		documentoVendaPagamentoCDCAfterDeleteMu.Lock()
		documentoVendaPagamentoCDCAfterDeleteHooks = append(documentoVendaPagamentoCDCAfterDeleteHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		documentoVendaPagamentoCDCBeforeUpsertMu.Lock()
		documentoVendaPagamentoCDCBeforeUpsertHooks = append(documentoVendaPagamentoCDCBeforeUpsertHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		documentoVendaPagamentoCDCAfterUpsertMu.Lock()
		documentoVendaPagamentoCDCAfterUpsertHooks = append(documentoVendaPagamentoCDCAfterUpsertHooks, documentoVendaPagamentoCDCHook)
		documentoVendaPagamentoCDCAfterUpsertMu.Unlock()
	}
}

// One returns a single documentoVendaPagamentoCDC record from the query.
func (q documentoVendaPagamentoCDCQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DocumentoVendaPagamentoCDC, error) {
	o := &DocumentoVendaPagamentoCDC{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for documento_venda_pagamento_cdc")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DocumentoVendaPagamentoCDC records from the query.
func (q documentoVendaPagamentoCDCQuery) All(ctx context.Context, exec boil.ContextExecutor) (DocumentoVendaPagamentoCDCSlice, error) {
	var o []*DocumentoVendaPagamentoCDC

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DocumentoVendaPagamentoCDC slice")
	}

	if len(documentoVendaPagamentoCDCAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DocumentoVendaPagamentoCDC records in the query.
func (q documentoVendaPagamentoCDCQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count documento_venda_pagamento_cdc rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q documentoVendaPagamentoCDCQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if documento_venda_pagamento_cdc exists")
	}

	return count > 0, nil
}

// CDC pointed to by the foreign key.
func (o *DocumentoVendaPagamentoCDC) CDC(mods ...qm.QueryMod) centroDeCustoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cdc_id` = ?", o.CDCID),
	}

	queryMods = append(queryMods, mods...)

	return CentroDeCustos(queryMods...)
}

// LoadCDC allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaPagamentoCDCL) LoadCDC(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaPagamentoCDC interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaPagamentoCDC
	var object *DocumentoVendaPagamentoCDC

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaPagamentoCDC.(*DocumentoVendaPagamentoCDC)
		if !ok {
			object = new(DocumentoVendaPagamentoCDC)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaPagamentoCDC)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaPagamentoCDC))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaPagamentoCDC.(*[]*DocumentoVendaPagamentoCDC)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaPagamentoCDC)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaPagamentoCDC))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaPagamentoCDCR{}
		}
		args[object.CDCID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaPagamentoCDCR{}
			}

			args[obj.CDCID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`centro_de_custo`),
		qm.WhereIn(`centro_de_custo.cdc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load CentroDeCusto")
	}

	var resultSlice []*CentroDeCusto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice CentroDeCusto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for centro_de_custo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for centro_de_custo")
	}

	if len(centroDeCustoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CDC = foreign
		if foreign.R == nil {
			foreign.R = &centroDeCustoR{}
		}
		foreign.R.CDCDocumentoVendaPagamentoCDCS = append(foreign.R.CDCDocumentoVendaPagamentoCDCS, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CDCID == foreign.CDCID {
				local.R.CDC = foreign
				if foreign.R == nil {
					foreign.R = &centroDeCustoR{}
				}
				foreign.R.CDCDocumentoVendaPagamentoCDCS = append(foreign.R.CDCDocumentoVendaPagamentoCDCS, local)
				break
			}
		}
	}

	return nil
}

// SetCDC of the documentoVendaPagamentoCDC to the related item.
// Sets o.R.CDC to related.
// Adds o to related.R.CDCDocumentoVendaPagamentoCDCS.
func (o *DocumentoVendaPagamentoCDC) SetCDC(ctx context.Context, exec boil.ContextExecutor, insert bool, related *CentroDeCusto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_pagamento_cdc` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cdc_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoCDCPrimaryKeyColumns),
	)
	values := []interface{}{related.CDCID, o.DDVID, o.CDCID, o.DVPItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CDCID = related.CDCID
	if o.R == nil {
		o.R = &documentoVendaPagamentoCDCR{
			CDC: related,
		}
	} else {
		o.R.CDC = related
	}

	if related.R == nil {
		related.R = &centroDeCustoR{
			CDCDocumentoVendaPagamentoCDCS: DocumentoVendaPagamentoCDCSlice{o},
		}
	} else {
		related.R.CDCDocumentoVendaPagamentoCDCS = append(related.R.CDCDocumentoVendaPagamentoCDCS, o)
	}

	return nil
}

// DocumentoVendaPagamentoCDCS retrieves all the records using an executor.
func DocumentoVendaPagamentoCDCS(mods ...qm.QueryMod) documentoVendaPagamentoCDCQuery {
	mods = append(mods, qm.From("`documento_venda_pagamento_cdc`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`documento_venda_pagamento_cdc`.*"})
	}

	return documentoVendaPagamentoCDCQuery{q}
}

// FindDocumentoVendaPagamentoCDC retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDocumentoVendaPagamentoCDC(ctx context.Context, exec boil.ContextExecutor, dDVID int, cDCID int, dVPItem int, selectCols ...string) (*DocumentoVendaPagamentoCDC, error) {
	documentoVendaPagamentoCDCObj := &DocumentoVendaPagamentoCDC{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `documento_venda_pagamento_cdc` where `ddv_id`=? AND `cdc_id`=? AND `dvp_item`=?", sel,
	)

	q := queries.Raw(query, dDVID, cDCID, dVPItem)

	err := q.Bind(ctx, exec, documentoVendaPagamentoCDCObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from documento_venda_pagamento_cdc")
	}

	if err = documentoVendaPagamentoCDCObj.doAfterSelectHooks(ctx, exec); err != nil {
		return documentoVendaPagamentoCDCObj, err
	}

	return documentoVendaPagamentoCDCObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DocumentoVendaPagamentoCDC) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_pagamento_cdc provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaPagamentoCDCColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	documentoVendaPagamentoCDCInsertCacheMut.RLock()
	cache, cached := documentoVendaPagamentoCDCInsertCache[key]
	documentoVendaPagamentoCDCInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			documentoVendaPagamentoCDCAllColumns,
			documentoVendaPagamentoCDCColumnsWithDefault,
			documentoVendaPagamentoCDCColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaPagamentoCDCType, documentoVendaPagamentoCDCMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(documentoVendaPagamentoCDCType, documentoVendaPagamentoCDCMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `documento_venda_pagamento_cdc` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `documento_venda_pagamento_cdc` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `documento_venda_pagamento_cdc` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoCDCPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into documento_venda_pagamento_cdc")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DDVID,
		o.CDCID,
		o.DVPItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_pagamento_cdc")
	}

CacheNoHooks:
	if !cached {
		documentoVendaPagamentoCDCInsertCacheMut.Lock()
		documentoVendaPagamentoCDCInsertCache[key] = cache
		documentoVendaPagamentoCDCInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DocumentoVendaPagamentoCDC.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DocumentoVendaPagamentoCDC) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	documentoVendaPagamentoCDCUpdateCacheMut.RLock()
	cache, cached := documentoVendaPagamentoCDCUpdateCache[key]
	documentoVendaPagamentoCDCUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			documentoVendaPagamentoCDCAllColumns,
			documentoVendaPagamentoCDCPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update documento_venda_pagamento_cdc, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `documento_venda_pagamento_cdc` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoCDCPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(documentoVendaPagamentoCDCType, documentoVendaPagamentoCDCMapping, append(wl, documentoVendaPagamentoCDCPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update documento_venda_pagamento_cdc row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for documento_venda_pagamento_cdc")
	}

	if !cached {
		documentoVendaPagamentoCDCUpdateCacheMut.Lock()
		documentoVendaPagamentoCDCUpdateCache[key] = cache
		documentoVendaPagamentoCDCUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q documentoVendaPagamentoCDCQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for documento_venda_pagamento_cdc")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for documento_venda_pagamento_cdc")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DocumentoVendaPagamentoCDCSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaPagamentoCDCPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `documento_venda_pagamento_cdc` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaPagamentoCDCPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in documentoVendaPagamentoCDC slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all documentoVendaPagamentoCDC")
	}
	return rowsAff, nil
}

var mySQLDocumentoVendaPagamentoCDCUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DocumentoVendaPagamentoCDC) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_pagamento_cdc provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaPagamentoCDCColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDocumentoVendaPagamentoCDCUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	documentoVendaPagamentoCDCUpsertCacheMut.RLock()
	cache, cached := documentoVendaPagamentoCDCUpsertCache[key]
	documentoVendaPagamentoCDCUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			documentoVendaPagamentoCDCAllColumns,
			documentoVendaPagamentoCDCColumnsWithDefault,
			documentoVendaPagamentoCDCColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			documentoVendaPagamentoCDCAllColumns,
			documentoVendaPagamentoCDCPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert documento_venda_pagamento_cdc, could not build update column list")
		}

		ret := strmangle.SetComplement(documentoVendaPagamentoCDCAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`documento_venda_pagamento_cdc`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `documento_venda_pagamento_cdc` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaPagamentoCDCType, documentoVendaPagamentoCDCMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(documentoVendaPagamentoCDCType, documentoVendaPagamentoCDCMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for documento_venda_pagamento_cdc")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(documentoVendaPagamentoCDCType, documentoVendaPagamentoCDCMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for documento_venda_pagamento_cdc")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_pagamento_cdc")
	}

CacheNoHooks:
	if !cached {
		documentoVendaPagamentoCDCUpsertCacheMut.Lock()
		documentoVendaPagamentoCDCUpsertCache[key] = cache
		documentoVendaPagamentoCDCUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DocumentoVendaPagamentoCDC record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DocumentoVendaPagamentoCDC) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DocumentoVendaPagamentoCDC provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), documentoVendaPagamentoCDCPrimaryKeyMapping)
	sql := "DELETE FROM `documento_venda_pagamento_cdc` WHERE `ddv_id`=? AND `cdc_id`=? AND `dvp_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from documento_venda_pagamento_cdc")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for documento_venda_pagamento_cdc")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q documentoVendaPagamentoCDCQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no documentoVendaPagamentoCDCQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documento_venda_pagamento_cdc")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_pagamento_cdc")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DocumentoVendaPagamentoCDCSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(documentoVendaPagamentoCDCBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaPagamentoCDCPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `documento_venda_pagamento_cdc` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaPagamentoCDCPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documentoVendaPagamentoCDC slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_pagamento_cdc")
	}

	if len(documentoVendaPagamentoCDCAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DocumentoVendaPagamentoCDC) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDocumentoVendaPagamentoCDC(ctx, exec, o.DDVID, o.CDCID, o.DVPItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DocumentoVendaPagamentoCDCSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DocumentoVendaPagamentoCDCSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaPagamentoCDCPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `documento_venda_pagamento_cdc`.* FROM `documento_venda_pagamento_cdc` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaPagamentoCDCPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DocumentoVendaPagamentoCDCSlice")
	}

	*o = slice

	return nil
}

// DocumentoVendaPagamentoCDCExists checks if the DocumentoVendaPagamentoCDC row exists.
func DocumentoVendaPagamentoCDCExists(ctx context.Context, exec boil.ContextExecutor, dDVID int, cDCID int, dVPItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `documento_venda_pagamento_cdc` where `ddv_id`=? AND `cdc_id`=? AND `dvp_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, dDVID, cDCID, dVPItem)
	}
	row := exec.QueryRowContext(ctx, sql, dDVID, cDCID, dVPItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if documento_venda_pagamento_cdc exists")
	}

	return exists, nil
}

// Exists checks if the DocumentoVendaPagamentoCDC row exists.
func (o *DocumentoVendaPagamentoCDC) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DocumentoVendaPagamentoCDCExists(ctx, exec, o.DDVID, o.CDCID, o.DVPItem)
}
