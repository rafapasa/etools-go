// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testCompartilhamentoTabelas(t *testing.T) {
	t.Parallel()

	query := CompartilhamentoTabelas()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testCompartilhamentoTabelasDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCompartilhamentoTabelasQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := CompartilhamentoTabelas().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCompartilhamentoTabelasSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CompartilhamentoTabelasSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCompartilhamentoTabelasExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := CompartilhamentoTabelasExists(ctx, tx, o.CptabID)
	if err != nil {
		t.Errorf("Unable to check if CompartilhamentoTabelas exists: %s", err)
	}
	if !e {
		t.Errorf("Expected CompartilhamentoTabelasExists to return true, but got false.")
	}
}

func testCompartilhamentoTabelasFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	compartilhamentoTabelasFound, err := FindCompartilhamentoTabelas(ctx, tx, o.CptabID)
	if err != nil {
		t.Error(err)
	}

	if compartilhamentoTabelasFound == nil {
		t.Error("want a record, got nil")
	}
}

func testCompartilhamentoTabelasBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = CompartilhamentoTabelas().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testCompartilhamentoTabelasOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := CompartilhamentoTabelas().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testCompartilhamentoTabelasAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	compartilhamentoTabelasOne := &CompartilhamentoTabelas{}
	compartilhamentoTabelasTwo := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, compartilhamentoTabelasOne, compartilhamentoTabelasDBTypes, false, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}
	if err = randomize.Struct(seed, compartilhamentoTabelasTwo, compartilhamentoTabelasDBTypes, false, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = compartilhamentoTabelasOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = compartilhamentoTabelasTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := CompartilhamentoTabelas().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testCompartilhamentoTabelasCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	compartilhamentoTabelasOne := &CompartilhamentoTabelas{}
	compartilhamentoTabelasTwo := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, compartilhamentoTabelasOne, compartilhamentoTabelasDBTypes, false, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}
	if err = randomize.Struct(seed, compartilhamentoTabelasTwo, compartilhamentoTabelasDBTypes, false, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = compartilhamentoTabelasOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = compartilhamentoTabelasTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func compartilhamentoTabelasBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func compartilhamentoTabelasAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func compartilhamentoTabelasAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func compartilhamentoTabelasBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func compartilhamentoTabelasAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func compartilhamentoTabelasBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func compartilhamentoTabelasAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func compartilhamentoTabelasBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func compartilhamentoTabelasAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *CompartilhamentoTabelas) error {
	*o = CompartilhamentoTabelas{}
	return nil
}

func testCompartilhamentoTabelasHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &CompartilhamentoTabelas{}
	o := &CompartilhamentoTabelas{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, false); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas object: %s", err)
	}

	AddCompartilhamentoTabelasHook(boil.BeforeInsertHook, compartilhamentoTabelasBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasBeforeInsertHooks = []CompartilhamentoTabelasHook{}

	AddCompartilhamentoTabelasHook(boil.AfterInsertHook, compartilhamentoTabelasAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasAfterInsertHooks = []CompartilhamentoTabelasHook{}

	AddCompartilhamentoTabelasHook(boil.AfterSelectHook, compartilhamentoTabelasAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasAfterSelectHooks = []CompartilhamentoTabelasHook{}

	AddCompartilhamentoTabelasHook(boil.BeforeUpdateHook, compartilhamentoTabelasBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasBeforeUpdateHooks = []CompartilhamentoTabelasHook{}

	AddCompartilhamentoTabelasHook(boil.AfterUpdateHook, compartilhamentoTabelasAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasAfterUpdateHooks = []CompartilhamentoTabelasHook{}

	AddCompartilhamentoTabelasHook(boil.BeforeDeleteHook, compartilhamentoTabelasBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasBeforeDeleteHooks = []CompartilhamentoTabelasHook{}

	AddCompartilhamentoTabelasHook(boil.AfterDeleteHook, compartilhamentoTabelasAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasAfterDeleteHooks = []CompartilhamentoTabelasHook{}

	AddCompartilhamentoTabelasHook(boil.BeforeUpsertHook, compartilhamentoTabelasBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasBeforeUpsertHooks = []CompartilhamentoTabelasHook{}

	AddCompartilhamentoTabelasHook(boil.AfterUpsertHook, compartilhamentoTabelasAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	compartilhamentoTabelasAfterUpsertHooks = []CompartilhamentoTabelasHook{}
}

func testCompartilhamentoTabelasInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCompartilhamentoTabelasInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(compartilhamentoTabelasColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCompartilhamentoTabelasToOneEmpresaFilialUsingEmf(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local CompartilhamentoTabelas
	var foreign EmpresaFilial

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, compartilhamentoTabelasDBTypes, false, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, empresaFilialDBTypes, false, empresaFilialColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize EmpresaFilial struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.EmfID = foreign.EmfID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Emf().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.EmfID != foreign.EmfID {
		t.Errorf("want: %v, got %v", foreign.EmfID, check.EmfID)
	}

	ranAfterSelectHook := false
	AddEmpresaFilialHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *EmpresaFilial) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := CompartilhamentoTabelasSlice{&local}
	if err = local.L.LoadEmf(ctx, tx, false, (*[]*CompartilhamentoTabelas)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Emf == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Emf = nil
	if err = local.L.LoadEmf(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Emf == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testCompartilhamentoTabelasToOneEmpresaFilialUsingEmfIDCompaEmpresaFilial(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local CompartilhamentoTabelas
	var foreign EmpresaFilial

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, compartilhamentoTabelasDBTypes, false, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, empresaFilialDBTypes, false, empresaFilialColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize EmpresaFilial struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.EmfIDCompa = foreign.EmfID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.EmfIDCompaEmpresaFilial().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.EmfID != foreign.EmfID {
		t.Errorf("want: %v, got %v", foreign.EmfID, check.EmfID)
	}

	ranAfterSelectHook := false
	AddEmpresaFilialHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *EmpresaFilial) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := CompartilhamentoTabelasSlice{&local}
	if err = local.L.LoadEmfIDCompaEmpresaFilial(ctx, tx, false, (*[]*CompartilhamentoTabelas)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.EmfIDCompaEmpresaFilial == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.EmfIDCompaEmpresaFilial = nil
	if err = local.L.LoadEmfIDCompaEmpresaFilial(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.EmfIDCompaEmpresaFilial == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testCompartilhamentoTabelasToOneSetOpEmpresaFilialUsingEmf(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a CompartilhamentoTabelas
	var b, c EmpresaFilial

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, compartilhamentoTabelasDBTypes, false, strmangle.SetComplement(compartilhamentoTabelasPrimaryKeyColumns, compartilhamentoTabelasColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, empresaFilialDBTypes, false, strmangle.SetComplement(empresaFilialPrimaryKeyColumns, empresaFilialColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, empresaFilialDBTypes, false, strmangle.SetComplement(empresaFilialPrimaryKeyColumns, empresaFilialColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*EmpresaFilial{&b, &c} {
		err = a.SetEmf(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Emf != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.EmfCompartilhamentoTabelas[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.EmfID != x.EmfID {
			t.Error("foreign key was wrong value", a.EmfID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.EmfID))
		reflect.Indirect(reflect.ValueOf(&a.EmfID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.EmfID != x.EmfID {
			t.Error("foreign key was wrong value", a.EmfID, x.EmfID)
		}
	}
}
func testCompartilhamentoTabelasToOneSetOpEmpresaFilialUsingEmfIDCompaEmpresaFilial(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a CompartilhamentoTabelas
	var b, c EmpresaFilial

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, compartilhamentoTabelasDBTypes, false, strmangle.SetComplement(compartilhamentoTabelasPrimaryKeyColumns, compartilhamentoTabelasColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, empresaFilialDBTypes, false, strmangle.SetComplement(empresaFilialPrimaryKeyColumns, empresaFilialColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, empresaFilialDBTypes, false, strmangle.SetComplement(empresaFilialPrimaryKeyColumns, empresaFilialColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*EmpresaFilial{&b, &c} {
		err = a.SetEmfIDCompaEmpresaFilial(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.EmfIDCompaEmpresaFilial != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.EmfIDCompaCompartilhamentoTabelas[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.EmfIDCompa != x.EmfID {
			t.Error("foreign key was wrong value", a.EmfIDCompa)
		}

		zero := reflect.Zero(reflect.TypeOf(a.EmfIDCompa))
		reflect.Indirect(reflect.ValueOf(&a.EmfIDCompa)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.EmfIDCompa != x.EmfID {
			t.Error("foreign key was wrong value", a.EmfIDCompa, x.EmfID)
		}
	}
}

func testCompartilhamentoTabelasReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testCompartilhamentoTabelasReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CompartilhamentoTabelasSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testCompartilhamentoTabelasSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := CompartilhamentoTabelas().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	compartilhamentoTabelasDBTypes = map[string]string{`CptabID`: `int`, `EmfID`: `int`, `EmfIDCompa`: `int`, `CptabClass`: `varchar`}
	_                              = bytes.MinRead
)

func testCompartilhamentoTabelasUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(compartilhamentoTabelasPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(compartilhamentoTabelasAllColumns) == len(compartilhamentoTabelasPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testCompartilhamentoTabelasSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(compartilhamentoTabelasAllColumns) == len(compartilhamentoTabelasPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, compartilhamentoTabelasDBTypes, true, compartilhamentoTabelasPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(compartilhamentoTabelasAllColumns, compartilhamentoTabelasPrimaryKeyColumns) {
		fields = compartilhamentoTabelasAllColumns
	} else {
		fields = strmangle.SetComplement(
			compartilhamentoTabelasAllColumns,
			compartilhamentoTabelasPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := CompartilhamentoTabelasSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testCompartilhamentoTabelasUpsert(t *testing.T) {
	t.Parallel()

	if len(compartilhamentoTabelasAllColumns) == len(compartilhamentoTabelasPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLCompartilhamentoTabelasUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := CompartilhamentoTabelas{}
	if err = randomize.Struct(seed, &o, compartilhamentoTabelasDBTypes, false); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert CompartilhamentoTabelas: %s", err)
	}

	count, err := CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, compartilhamentoTabelasDBTypes, false, compartilhamentoTabelasPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CompartilhamentoTabelas struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert CompartilhamentoTabelas: %s", err)
	}

	count, err = CompartilhamentoTabelas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
