// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// ChequeTituloBaixa is an object representing the database table.
type ChequeTituloBaixa struct {
	TitID            int           `boil:"tit_id" json:"tit_id" toml:"tit_id" yaml:"tit_id"`
	TBXItem          int           `boil:"tbx_item" json:"tbx_item" toml:"tbx_item" yaml:"tbx_item"`
	ChqrecID         int           `boil:"chqrec_id" json:"chqrec_id" toml:"chqrec_id" yaml:"chqrec_id"`
	CHQTBXValorusado types.Decimal `boil:"chqtbx_valorusado" json:"chqtbx_valorusado" toml:"chqtbx_valorusado" yaml:"chqtbx_valorusado"`
	CHQTBXCompensado int16         `boil:"chqtbx_compensado" json:"chqtbx_compensado" toml:"chqtbx_compensado" yaml:"chqtbx_compensado"`

	R *chequeTituloBaixaR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L chequeTituloBaixaL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ChequeTituloBaixaColumns = struct {
	TitID            string
	TBXItem          string
	ChqrecID         string
	CHQTBXValorusado string
	CHQTBXCompensado string
}{
	TitID:            "tit_id",
	TBXItem:          "tbx_item",
	ChqrecID:         "chqrec_id",
	CHQTBXValorusado: "chqtbx_valorusado",
	CHQTBXCompensado: "chqtbx_compensado",
}

var ChequeTituloBaixaTableColumns = struct {
	TitID            string
	TBXItem          string
	ChqrecID         string
	CHQTBXValorusado string
	CHQTBXCompensado string
}{
	TitID:            "cheque_titulo_baixa.tit_id",
	TBXItem:          "cheque_titulo_baixa.tbx_item",
	ChqrecID:         "cheque_titulo_baixa.chqrec_id",
	CHQTBXValorusado: "cheque_titulo_baixa.chqtbx_valorusado",
	CHQTBXCompensado: "cheque_titulo_baixa.chqtbx_compensado",
}

// Generated where

var ChequeTituloBaixaWhere = struct {
	TitID            whereHelperint
	TBXItem          whereHelperint
	ChqrecID         whereHelperint
	CHQTBXValorusado whereHelpertypes_Decimal
	CHQTBXCompensado whereHelperint16
}{
	TitID:            whereHelperint{field: "`cheque_titulo_baixa`.`tit_id`"},
	TBXItem:          whereHelperint{field: "`cheque_titulo_baixa`.`tbx_item`"},
	ChqrecID:         whereHelperint{field: "`cheque_titulo_baixa`.`chqrec_id`"},
	CHQTBXValorusado: whereHelpertypes_Decimal{field: "`cheque_titulo_baixa`.`chqtbx_valorusado`"},
	CHQTBXCompensado: whereHelperint16{field: "`cheque_titulo_baixa`.`chqtbx_compensado`"},
}

// ChequeTituloBaixaRels is where relationship names are stored.
var ChequeTituloBaixaRels = struct {
	Chqrec string
}{
	Chqrec: "Chqrec",
}

// chequeTituloBaixaR is where relationships are stored.
type chequeTituloBaixaR struct {
	Chqrec *ChequesRecebido `boil:"Chqrec" json:"Chqrec" toml:"Chqrec" yaml:"Chqrec"`
}

// NewStruct creates a new relationship struct
func (*chequeTituloBaixaR) NewStruct() *chequeTituloBaixaR {
	return &chequeTituloBaixaR{}
}

func (r *chequeTituloBaixaR) GetChqrec() *ChequesRecebido {
	if r == nil {
		return nil
	}
	return r.Chqrec
}

// chequeTituloBaixaL is where Load methods for each relationship are stored.
type chequeTituloBaixaL struct{}

var (
	chequeTituloBaixaAllColumns            = []string{"tit_id", "tbx_item", "chqrec_id", "chqtbx_valorusado", "chqtbx_compensado"}
	chequeTituloBaixaColumnsWithoutDefault = []string{"tit_id", "tbx_item", "chqrec_id", "chqtbx_valorusado"}
	chequeTituloBaixaColumnsWithDefault    = []string{"chqtbx_compensado"}
	chequeTituloBaixaPrimaryKeyColumns     = []string{"tit_id", "tbx_item", "chqrec_id"}
	chequeTituloBaixaGeneratedColumns      = []string{}
)

type (
	// ChequeTituloBaixaSlice is an alias for a slice of pointers to ChequeTituloBaixa.
	// This should almost always be used instead of []ChequeTituloBaixa.
	ChequeTituloBaixaSlice []*ChequeTituloBaixa
	// ChequeTituloBaixaHook is the signature for custom ChequeTituloBaixa hook methods
	ChequeTituloBaixaHook func(context.Context, boil.ContextExecutor, *ChequeTituloBaixa) error

	chequeTituloBaixaQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	chequeTituloBaixaType                 = reflect.TypeOf(&ChequeTituloBaixa{})
	chequeTituloBaixaMapping              = queries.MakeStructMapping(chequeTituloBaixaType)
	chequeTituloBaixaPrimaryKeyMapping, _ = queries.BindMapping(chequeTituloBaixaType, chequeTituloBaixaMapping, chequeTituloBaixaPrimaryKeyColumns)
	chequeTituloBaixaInsertCacheMut       sync.RWMutex
	chequeTituloBaixaInsertCache          = make(map[string]insertCache)
	chequeTituloBaixaUpdateCacheMut       sync.RWMutex
	chequeTituloBaixaUpdateCache          = make(map[string]updateCache)
	chequeTituloBaixaUpsertCacheMut       sync.RWMutex
	chequeTituloBaixaUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var chequeTituloBaixaAfterSelectMu sync.Mutex
var chequeTituloBaixaAfterSelectHooks []ChequeTituloBaixaHook

var chequeTituloBaixaBeforeInsertMu sync.Mutex
var chequeTituloBaixaBeforeInsertHooks []ChequeTituloBaixaHook
var chequeTituloBaixaAfterInsertMu sync.Mutex
var chequeTituloBaixaAfterInsertHooks []ChequeTituloBaixaHook

var chequeTituloBaixaBeforeUpdateMu sync.Mutex
var chequeTituloBaixaBeforeUpdateHooks []ChequeTituloBaixaHook
var chequeTituloBaixaAfterUpdateMu sync.Mutex
var chequeTituloBaixaAfterUpdateHooks []ChequeTituloBaixaHook

var chequeTituloBaixaBeforeDeleteMu sync.Mutex
var chequeTituloBaixaBeforeDeleteHooks []ChequeTituloBaixaHook
var chequeTituloBaixaAfterDeleteMu sync.Mutex
var chequeTituloBaixaAfterDeleteHooks []ChequeTituloBaixaHook

var chequeTituloBaixaBeforeUpsertMu sync.Mutex
var chequeTituloBaixaBeforeUpsertHooks []ChequeTituloBaixaHook
var chequeTituloBaixaAfterUpsertMu sync.Mutex
var chequeTituloBaixaAfterUpsertHooks []ChequeTituloBaixaHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ChequeTituloBaixa) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ChequeTituloBaixa) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ChequeTituloBaixa) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ChequeTituloBaixa) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ChequeTituloBaixa) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ChequeTituloBaixa) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ChequeTituloBaixa) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ChequeTituloBaixa) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ChequeTituloBaixa) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeTituloBaixaAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddChequeTituloBaixaHook registers your hook function for all future operations.
func AddChequeTituloBaixaHook(hookPoint boil.HookPoint, chequeTituloBaixaHook ChequeTituloBaixaHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		chequeTituloBaixaAfterSelectMu.Lock()
		chequeTituloBaixaAfterSelectHooks = append(chequeTituloBaixaAfterSelectHooks, chequeTituloBaixaHook)
		chequeTituloBaixaAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		chequeTituloBaixaBeforeInsertMu.Lock()
		chequeTituloBaixaBeforeInsertHooks = append(chequeTituloBaixaBeforeInsertHooks, chequeTituloBaixaHook)
		chequeTituloBaixaBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		chequeTituloBaixaAfterInsertMu.Lock()
		chequeTituloBaixaAfterInsertHooks = append(chequeTituloBaixaAfterInsertHooks, chequeTituloBaixaHook)
		chequeTituloBaixaAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		chequeTituloBaixaBeforeUpdateMu.Lock()
		chequeTituloBaixaBeforeUpdateHooks = append(chequeTituloBaixaBeforeUpdateHooks, chequeTituloBaixaHook)
		chequeTituloBaixaBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		chequeTituloBaixaAfterUpdateMu.Lock()
		chequeTituloBaixaAfterUpdateHooks = append(chequeTituloBaixaAfterUpdateHooks, chequeTituloBaixaHook)
		chequeTituloBaixaAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		chequeTituloBaixaBeforeDeleteMu.Lock()
		chequeTituloBaixaBeforeDeleteHooks = append(chequeTituloBaixaBeforeDeleteHooks, chequeTituloBaixaHook)
		chequeTituloBaixaBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		chequeTituloBaixaAfterDeleteMu.Lock()
		chequeTituloBaixaAfterDeleteHooks = append(chequeTituloBaixaAfterDeleteHooks, chequeTituloBaixaHook)
		chequeTituloBaixaAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		chequeTituloBaixaBeforeUpsertMu.Lock()
		chequeTituloBaixaBeforeUpsertHooks = append(chequeTituloBaixaBeforeUpsertHooks, chequeTituloBaixaHook)
		chequeTituloBaixaBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		chequeTituloBaixaAfterUpsertMu.Lock()
		chequeTituloBaixaAfterUpsertHooks = append(chequeTituloBaixaAfterUpsertHooks, chequeTituloBaixaHook)
		chequeTituloBaixaAfterUpsertMu.Unlock()
	}
}

// One returns a single chequeTituloBaixa record from the query.
func (q chequeTituloBaixaQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ChequeTituloBaixa, error) {
	o := &ChequeTituloBaixa{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cheque_titulo_baixa")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ChequeTituloBaixa records from the query.
func (q chequeTituloBaixaQuery) All(ctx context.Context, exec boil.ContextExecutor) (ChequeTituloBaixaSlice, error) {
	var o []*ChequeTituloBaixa

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ChequeTituloBaixa slice")
	}

	if len(chequeTituloBaixaAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ChequeTituloBaixa records in the query.
func (q chequeTituloBaixaQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cheque_titulo_baixa rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q chequeTituloBaixaQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cheque_titulo_baixa exists")
	}

	return count > 0, nil
}

// Chqrec pointed to by the foreign key.
func (o *ChequeTituloBaixa) Chqrec(mods ...qm.QueryMod) chequesRecebidoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`chqrec_id` = ?", o.ChqrecID),
	}

	queryMods = append(queryMods, mods...)

	return ChequesRecebidos(queryMods...)
}

// LoadChqrec allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequeTituloBaixaL) LoadChqrec(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequeTituloBaixa interface{}, mods queries.Applicator) error {
	var slice []*ChequeTituloBaixa
	var object *ChequeTituloBaixa

	if singular {
		var ok bool
		object, ok = maybeChequeTituloBaixa.(*ChequeTituloBaixa)
		if !ok {
			object = new(ChequeTituloBaixa)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequeTituloBaixa)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequeTituloBaixa))
			}
		}
	} else {
		s, ok := maybeChequeTituloBaixa.(*[]*ChequeTituloBaixa)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequeTituloBaixa)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequeTituloBaixa))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequeTituloBaixaR{}
		}
		args[object.ChqrecID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequeTituloBaixaR{}
			}

			args[obj.ChqrecID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cheques_recebidos`),
		qm.WhereIn(`cheques_recebidos.chqrec_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ChequesRecebido")
	}

	var resultSlice []*ChequesRecebido
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ChequesRecebido")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for cheques_recebidos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cheques_recebidos")
	}

	if len(chequesRecebidoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Chqrec = foreign
		if foreign.R == nil {
			foreign.R = &chequesRecebidoR{}
		}
		foreign.R.ChqrecChequeTituloBaixas = append(foreign.R.ChqrecChequeTituloBaixas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ChqrecID == foreign.ChqrecID {
				local.R.Chqrec = foreign
				if foreign.R == nil {
					foreign.R = &chequesRecebidoR{}
				}
				foreign.R.ChqrecChequeTituloBaixas = append(foreign.R.ChqrecChequeTituloBaixas, local)
				break
			}
		}
	}

	return nil
}

// SetChqrec of the chequeTituloBaixa to the related item.
// Sets o.R.Chqrec to related.
// Adds o to related.R.ChqrecChequeTituloBaixas.
func (o *ChequeTituloBaixa) SetChqrec(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ChequesRecebido) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheque_titulo_baixa` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"chqrec_id"}),
		strmangle.WhereClause("`", "`", 0, chequeTituloBaixaPrimaryKeyColumns),
	)
	values := []interface{}{related.ChqrecID, o.TitID, o.TBXItem, o.ChqrecID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ChqrecID = related.ChqrecID
	if o.R == nil {
		o.R = &chequeTituloBaixaR{
			Chqrec: related,
		}
	} else {
		o.R.Chqrec = related
	}

	if related.R == nil {
		related.R = &chequesRecebidoR{
			ChqrecChequeTituloBaixas: ChequeTituloBaixaSlice{o},
		}
	} else {
		related.R.ChqrecChequeTituloBaixas = append(related.R.ChqrecChequeTituloBaixas, o)
	}

	return nil
}

// ChequeTituloBaixas retrieves all the records using an executor.
func ChequeTituloBaixas(mods ...qm.QueryMod) chequeTituloBaixaQuery {
	mods = append(mods, qm.From("`cheque_titulo_baixa`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`cheque_titulo_baixa`.*"})
	}

	return chequeTituloBaixaQuery{q}
}

// FindChequeTituloBaixa retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindChequeTituloBaixa(ctx context.Context, exec boil.ContextExecutor, titID int, tBXItem int, chqrecID int, selectCols ...string) (*ChequeTituloBaixa, error) {
	chequeTituloBaixaObj := &ChequeTituloBaixa{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cheque_titulo_baixa` where `tit_id`=? AND `tbx_item`=? AND `chqrec_id`=?", sel,
	)

	q := queries.Raw(query, titID, tBXItem, chqrecID)

	err := q.Bind(ctx, exec, chequeTituloBaixaObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cheque_titulo_baixa")
	}

	if err = chequeTituloBaixaObj.doAfterSelectHooks(ctx, exec); err != nil {
		return chequeTituloBaixaObj, err
	}

	return chequeTituloBaixaObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ChequeTituloBaixa) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cheque_titulo_baixa provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(chequeTituloBaixaColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	chequeTituloBaixaInsertCacheMut.RLock()
	cache, cached := chequeTituloBaixaInsertCache[key]
	chequeTituloBaixaInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			chequeTituloBaixaAllColumns,
			chequeTituloBaixaColumnsWithDefault,
			chequeTituloBaixaColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(chequeTituloBaixaType, chequeTituloBaixaMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(chequeTituloBaixaType, chequeTituloBaixaMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cheque_titulo_baixa` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cheque_titulo_baixa` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cheque_titulo_baixa` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, chequeTituloBaixaPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cheque_titulo_baixa")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TitID,
		o.TBXItem,
		o.ChqrecID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cheque_titulo_baixa")
	}

CacheNoHooks:
	if !cached {
		chequeTituloBaixaInsertCacheMut.Lock()
		chequeTituloBaixaInsertCache[key] = cache
		chequeTituloBaixaInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ChequeTituloBaixa.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ChequeTituloBaixa) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	chequeTituloBaixaUpdateCacheMut.RLock()
	cache, cached := chequeTituloBaixaUpdateCache[key]
	chequeTituloBaixaUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			chequeTituloBaixaAllColumns,
			chequeTituloBaixaPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cheque_titulo_baixa, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cheque_titulo_baixa` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, chequeTituloBaixaPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(chequeTituloBaixaType, chequeTituloBaixaMapping, append(wl, chequeTituloBaixaPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cheque_titulo_baixa row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cheque_titulo_baixa")
	}

	if !cached {
		chequeTituloBaixaUpdateCacheMut.Lock()
		chequeTituloBaixaUpdateCache[key] = cache
		chequeTituloBaixaUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q chequeTituloBaixaQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cheque_titulo_baixa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cheque_titulo_baixa")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ChequeTituloBaixaSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequeTituloBaixaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cheque_titulo_baixa` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequeTituloBaixaPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in chequeTituloBaixa slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all chequeTituloBaixa")
	}
	return rowsAff, nil
}

var mySQLChequeTituloBaixaUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ChequeTituloBaixa) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cheque_titulo_baixa provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(chequeTituloBaixaColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLChequeTituloBaixaUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	chequeTituloBaixaUpsertCacheMut.RLock()
	cache, cached := chequeTituloBaixaUpsertCache[key]
	chequeTituloBaixaUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			chequeTituloBaixaAllColumns,
			chequeTituloBaixaColumnsWithDefault,
			chequeTituloBaixaColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			chequeTituloBaixaAllColumns,
			chequeTituloBaixaPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cheque_titulo_baixa, could not build update column list")
		}

		ret := strmangle.SetComplement(chequeTituloBaixaAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`cheque_titulo_baixa`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cheque_titulo_baixa` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(chequeTituloBaixaType, chequeTituloBaixaMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(chequeTituloBaixaType, chequeTituloBaixaMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cheque_titulo_baixa")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(chequeTituloBaixaType, chequeTituloBaixaMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cheque_titulo_baixa")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cheque_titulo_baixa")
	}

CacheNoHooks:
	if !cached {
		chequeTituloBaixaUpsertCacheMut.Lock()
		chequeTituloBaixaUpsertCache[key] = cache
		chequeTituloBaixaUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ChequeTituloBaixa record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ChequeTituloBaixa) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ChequeTituloBaixa provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), chequeTituloBaixaPrimaryKeyMapping)
	sql := "DELETE FROM `cheque_titulo_baixa` WHERE `tit_id`=? AND `tbx_item`=? AND `chqrec_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cheque_titulo_baixa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cheque_titulo_baixa")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q chequeTituloBaixaQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no chequeTituloBaixaQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cheque_titulo_baixa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cheque_titulo_baixa")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ChequeTituloBaixaSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(chequeTituloBaixaBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequeTituloBaixaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cheque_titulo_baixa` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequeTituloBaixaPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from chequeTituloBaixa slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cheque_titulo_baixa")
	}

	if len(chequeTituloBaixaAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ChequeTituloBaixa) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindChequeTituloBaixa(ctx, exec, o.TitID, o.TBXItem, o.ChqrecID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ChequeTituloBaixaSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ChequeTituloBaixaSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequeTituloBaixaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cheque_titulo_baixa`.* FROM `cheque_titulo_baixa` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequeTituloBaixaPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ChequeTituloBaixaSlice")
	}

	*o = slice

	return nil
}

// ChequeTituloBaixaExists checks if the ChequeTituloBaixa row exists.
func ChequeTituloBaixaExists(ctx context.Context, exec boil.ContextExecutor, titID int, tBXItem int, chqrecID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cheque_titulo_baixa` where `tit_id`=? AND `tbx_item`=? AND `chqrec_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, titID, tBXItem, chqrecID)
	}
	row := exec.QueryRowContext(ctx, sql, titID, tBXItem, chqrecID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cheque_titulo_baixa exists")
	}

	return exists, nil
}

// Exists checks if the ChequeTituloBaixa row exists.
func (o *ChequeTituloBaixa) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ChequeTituloBaixaExists(ctx, exec, o.TitID, o.TBXItem, o.ChqrecID)
}
