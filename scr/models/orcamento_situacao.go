// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// OrcamentoSituacao is an object representing the database table.
type OrcamentoSituacao struct {
	OrsID        int16  `boil:"ors_id" json:"ors_id" toml:"ors_id" yaml:"ors_id"`
	OrsDescricao string `boil:"ors_descricao" json:"ors_descricao" toml:"ors_descricao" yaml:"ors_descricao"`

	R *orcamentoSituacaoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L orcamentoSituacaoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var OrcamentoSituacaoColumns = struct {
	OrsID        string
	OrsDescricao string
}{
	OrsID:        "ors_id",
	OrsDescricao: "ors_descricao",
}

var OrcamentoSituacaoTableColumns = struct {
	OrsID        string
	OrsDescricao string
}{
	OrsID:        "orcamento_situacao.ors_id",
	OrsDescricao: "orcamento_situacao.ors_descricao",
}

// Generated where

var OrcamentoSituacaoWhere = struct {
	OrsID        whereHelperint16
	OrsDescricao whereHelperstring
}{
	OrsID:        whereHelperint16{field: "`orcamento_situacao`.`ors_id`"},
	OrsDescricao: whereHelperstring{field: "`orcamento_situacao`.`ors_descricao`"},
}

// OrcamentoSituacaoRels is where relationship names are stored.
var OrcamentoSituacaoRels = struct {
	OrOrcamentoCompras string
}{
	OrOrcamentoCompras: "OrOrcamentoCompras",
}

// orcamentoSituacaoR is where relationships are stored.
type orcamentoSituacaoR struct {
	OrOrcamentoCompras OrcamentoCompraSlice `boil:"OrOrcamentoCompras" json:"OrOrcamentoCompras" toml:"OrOrcamentoCompras" yaml:"OrOrcamentoCompras"`
}

// NewStruct creates a new relationship struct
func (*orcamentoSituacaoR) NewStruct() *orcamentoSituacaoR {
	return &orcamentoSituacaoR{}
}

func (r *orcamentoSituacaoR) GetOrOrcamentoCompras() OrcamentoCompraSlice {
	if r == nil {
		return nil
	}
	return r.OrOrcamentoCompras
}

// orcamentoSituacaoL is where Load methods for each relationship are stored.
type orcamentoSituacaoL struct{}

var (
	orcamentoSituacaoAllColumns            = []string{"ors_id", "ors_descricao"}
	orcamentoSituacaoColumnsWithoutDefault = []string{"ors_id", "ors_descricao"}
	orcamentoSituacaoColumnsWithDefault    = []string{}
	orcamentoSituacaoPrimaryKeyColumns     = []string{"ors_id"}
	orcamentoSituacaoGeneratedColumns      = []string{}
)

type (
	// OrcamentoSituacaoSlice is an alias for a slice of pointers to OrcamentoSituacao.
	// This should almost always be used instead of []OrcamentoSituacao.
	OrcamentoSituacaoSlice []*OrcamentoSituacao
	// OrcamentoSituacaoHook is the signature for custom OrcamentoSituacao hook methods
	OrcamentoSituacaoHook func(context.Context, boil.ContextExecutor, *OrcamentoSituacao) error

	orcamentoSituacaoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	orcamentoSituacaoType                 = reflect.TypeOf(&OrcamentoSituacao{})
	orcamentoSituacaoMapping              = queries.MakeStructMapping(orcamentoSituacaoType)
	orcamentoSituacaoPrimaryKeyMapping, _ = queries.BindMapping(orcamentoSituacaoType, orcamentoSituacaoMapping, orcamentoSituacaoPrimaryKeyColumns)
	orcamentoSituacaoInsertCacheMut       sync.RWMutex
	orcamentoSituacaoInsertCache          = make(map[string]insertCache)
	orcamentoSituacaoUpdateCacheMut       sync.RWMutex
	orcamentoSituacaoUpdateCache          = make(map[string]updateCache)
	orcamentoSituacaoUpsertCacheMut       sync.RWMutex
	orcamentoSituacaoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var orcamentoSituacaoAfterSelectMu sync.Mutex
var orcamentoSituacaoAfterSelectHooks []OrcamentoSituacaoHook

var orcamentoSituacaoBeforeInsertMu sync.Mutex
var orcamentoSituacaoBeforeInsertHooks []OrcamentoSituacaoHook
var orcamentoSituacaoAfterInsertMu sync.Mutex
var orcamentoSituacaoAfterInsertHooks []OrcamentoSituacaoHook

var orcamentoSituacaoBeforeUpdateMu sync.Mutex
var orcamentoSituacaoBeforeUpdateHooks []OrcamentoSituacaoHook
var orcamentoSituacaoAfterUpdateMu sync.Mutex
var orcamentoSituacaoAfterUpdateHooks []OrcamentoSituacaoHook

var orcamentoSituacaoBeforeDeleteMu sync.Mutex
var orcamentoSituacaoBeforeDeleteHooks []OrcamentoSituacaoHook
var orcamentoSituacaoAfterDeleteMu sync.Mutex
var orcamentoSituacaoAfterDeleteHooks []OrcamentoSituacaoHook

var orcamentoSituacaoBeforeUpsertMu sync.Mutex
var orcamentoSituacaoBeforeUpsertHooks []OrcamentoSituacaoHook
var orcamentoSituacaoAfterUpsertMu sync.Mutex
var orcamentoSituacaoAfterUpsertHooks []OrcamentoSituacaoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *OrcamentoSituacao) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *OrcamentoSituacao) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *OrcamentoSituacao) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *OrcamentoSituacao) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *OrcamentoSituacao) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *OrcamentoSituacao) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *OrcamentoSituacao) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *OrcamentoSituacao) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *OrcamentoSituacao) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range orcamentoSituacaoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddOrcamentoSituacaoHook registers your hook function for all future operations.
func AddOrcamentoSituacaoHook(hookPoint boil.HookPoint, orcamentoSituacaoHook OrcamentoSituacaoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		orcamentoSituacaoAfterSelectMu.Lock()
		orcamentoSituacaoAfterSelectHooks = append(orcamentoSituacaoAfterSelectHooks, orcamentoSituacaoHook)
		orcamentoSituacaoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		orcamentoSituacaoBeforeInsertMu.Lock()
		orcamentoSituacaoBeforeInsertHooks = append(orcamentoSituacaoBeforeInsertHooks, orcamentoSituacaoHook)
		orcamentoSituacaoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		orcamentoSituacaoAfterInsertMu.Lock()
		orcamentoSituacaoAfterInsertHooks = append(orcamentoSituacaoAfterInsertHooks, orcamentoSituacaoHook)
		orcamentoSituacaoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		orcamentoSituacaoBeforeUpdateMu.Lock()
		orcamentoSituacaoBeforeUpdateHooks = append(orcamentoSituacaoBeforeUpdateHooks, orcamentoSituacaoHook)
		orcamentoSituacaoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		orcamentoSituacaoAfterUpdateMu.Lock()
		orcamentoSituacaoAfterUpdateHooks = append(orcamentoSituacaoAfterUpdateHooks, orcamentoSituacaoHook)
		orcamentoSituacaoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		orcamentoSituacaoBeforeDeleteMu.Lock()
		orcamentoSituacaoBeforeDeleteHooks = append(orcamentoSituacaoBeforeDeleteHooks, orcamentoSituacaoHook)
		orcamentoSituacaoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		orcamentoSituacaoAfterDeleteMu.Lock()
		orcamentoSituacaoAfterDeleteHooks = append(orcamentoSituacaoAfterDeleteHooks, orcamentoSituacaoHook)
		orcamentoSituacaoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		orcamentoSituacaoBeforeUpsertMu.Lock()
		orcamentoSituacaoBeforeUpsertHooks = append(orcamentoSituacaoBeforeUpsertHooks, orcamentoSituacaoHook)
		orcamentoSituacaoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		orcamentoSituacaoAfterUpsertMu.Lock()
		orcamentoSituacaoAfterUpsertHooks = append(orcamentoSituacaoAfterUpsertHooks, orcamentoSituacaoHook)
		orcamentoSituacaoAfterUpsertMu.Unlock()
	}
}

// One returns a single orcamentoSituacao record from the query.
func (q orcamentoSituacaoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*OrcamentoSituacao, error) {
	o := &OrcamentoSituacao{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for orcamento_situacao")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all OrcamentoSituacao records from the query.
func (q orcamentoSituacaoQuery) All(ctx context.Context, exec boil.ContextExecutor) (OrcamentoSituacaoSlice, error) {
	var o []*OrcamentoSituacao

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to OrcamentoSituacao slice")
	}

	if len(orcamentoSituacaoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all OrcamentoSituacao records in the query.
func (q orcamentoSituacaoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count orcamento_situacao rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q orcamentoSituacaoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if orcamento_situacao exists")
	}

	return count > 0, nil
}

// OrOrcamentoCompras retrieves all the orcamento_compra's OrcamentoCompras with an executor via ors_id column.
func (o *OrcamentoSituacao) OrOrcamentoCompras(mods ...qm.QueryMod) orcamentoCompraQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`orcamento_compra`.`ors_id`=?", o.OrsID),
	)

	return OrcamentoCompras(queryMods...)
}

// LoadOrOrcamentoCompras allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (orcamentoSituacaoL) LoadOrOrcamentoCompras(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOrcamentoSituacao interface{}, mods queries.Applicator) error {
	var slice []*OrcamentoSituacao
	var object *OrcamentoSituacao

	if singular {
		var ok bool
		object, ok = maybeOrcamentoSituacao.(*OrcamentoSituacao)
		if !ok {
			object = new(OrcamentoSituacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeOrcamentoSituacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeOrcamentoSituacao))
			}
		}
	} else {
		s, ok := maybeOrcamentoSituacao.(*[]*OrcamentoSituacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeOrcamentoSituacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeOrcamentoSituacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &orcamentoSituacaoR{}
		}
		args[object.OrsID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &orcamentoSituacaoR{}
			}
			args[obj.OrsID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`orcamento_compra`),
		qm.WhereIn(`orcamento_compra.ors_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load orcamento_compra")
	}

	var resultSlice []*OrcamentoCompra
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice orcamento_compra")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on orcamento_compra")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for orcamento_compra")
	}

	if len(orcamentoCompraAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.OrOrcamentoCompras = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &orcamentoCompraR{}
			}
			foreign.R.Or = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.OrsID == foreign.OrsID {
				local.R.OrOrcamentoCompras = append(local.R.OrOrcamentoCompras, foreign)
				if foreign.R == nil {
					foreign.R = &orcamentoCompraR{}
				}
				foreign.R.Or = local
				break
			}
		}
	}

	return nil
}

// AddOrOrcamentoCompras adds the given related objects to the existing relationships
// of the orcamento_situacao, optionally inserting them as new records.
// Appends related to o.R.OrOrcamentoCompras.
// Sets related.R.Or appropriately.
func (o *OrcamentoSituacao) AddOrOrcamentoCompras(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*OrcamentoCompra) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.OrsID = o.OrsID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `orcamento_compra` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"ors_id"}),
				strmangle.WhereClause("`", "`", 0, orcamentoCompraPrimaryKeyColumns),
			)
			values := []interface{}{o.OrsID, rel.OrcID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.OrsID = o.OrsID
		}
	}

	if o.R == nil {
		o.R = &orcamentoSituacaoR{
			OrOrcamentoCompras: related,
		}
	} else {
		o.R.OrOrcamentoCompras = append(o.R.OrOrcamentoCompras, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &orcamentoCompraR{
				Or: o,
			}
		} else {
			rel.R.Or = o
		}
	}
	return nil
}

// OrcamentoSituacaos retrieves all the records using an executor.
func OrcamentoSituacaos(mods ...qm.QueryMod) orcamentoSituacaoQuery {
	mods = append(mods, qm.From("`orcamento_situacao`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`orcamento_situacao`.*"})
	}

	return orcamentoSituacaoQuery{q}
}

// FindOrcamentoSituacao retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindOrcamentoSituacao(ctx context.Context, exec boil.ContextExecutor, orsID int16, selectCols ...string) (*OrcamentoSituacao, error) {
	orcamentoSituacaoObj := &OrcamentoSituacao{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `orcamento_situacao` where `ors_id`=?", sel,
	)

	q := queries.Raw(query, orsID)

	err := q.Bind(ctx, exec, orcamentoSituacaoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from orcamento_situacao")
	}

	if err = orcamentoSituacaoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return orcamentoSituacaoObj, err
	}

	return orcamentoSituacaoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *OrcamentoSituacao) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no orcamento_situacao provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(orcamentoSituacaoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	orcamentoSituacaoInsertCacheMut.RLock()
	cache, cached := orcamentoSituacaoInsertCache[key]
	orcamentoSituacaoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			orcamentoSituacaoAllColumns,
			orcamentoSituacaoColumnsWithDefault,
			orcamentoSituacaoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(orcamentoSituacaoType, orcamentoSituacaoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(orcamentoSituacaoType, orcamentoSituacaoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `orcamento_situacao` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `orcamento_situacao` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `orcamento_situacao` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, orcamentoSituacaoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into orcamento_situacao")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.OrsID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for orcamento_situacao")
	}

CacheNoHooks:
	if !cached {
		orcamentoSituacaoInsertCacheMut.Lock()
		orcamentoSituacaoInsertCache[key] = cache
		orcamentoSituacaoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the OrcamentoSituacao.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *OrcamentoSituacao) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	orcamentoSituacaoUpdateCacheMut.RLock()
	cache, cached := orcamentoSituacaoUpdateCache[key]
	orcamentoSituacaoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			orcamentoSituacaoAllColumns,
			orcamentoSituacaoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update orcamento_situacao, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `orcamento_situacao` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, orcamentoSituacaoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(orcamentoSituacaoType, orcamentoSituacaoMapping, append(wl, orcamentoSituacaoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update orcamento_situacao row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for orcamento_situacao")
	}

	if !cached {
		orcamentoSituacaoUpdateCacheMut.Lock()
		orcamentoSituacaoUpdateCache[key] = cache
		orcamentoSituacaoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q orcamentoSituacaoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for orcamento_situacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for orcamento_situacao")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o OrcamentoSituacaoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orcamentoSituacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `orcamento_situacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, orcamentoSituacaoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in orcamentoSituacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all orcamentoSituacao")
	}
	return rowsAff, nil
}

var mySQLOrcamentoSituacaoUniqueColumns = []string{
	"ors_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *OrcamentoSituacao) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no orcamento_situacao provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(orcamentoSituacaoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLOrcamentoSituacaoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	orcamentoSituacaoUpsertCacheMut.RLock()
	cache, cached := orcamentoSituacaoUpsertCache[key]
	orcamentoSituacaoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			orcamentoSituacaoAllColumns,
			orcamentoSituacaoColumnsWithDefault,
			orcamentoSituacaoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			orcamentoSituacaoAllColumns,
			orcamentoSituacaoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert orcamento_situacao, could not build update column list")
		}

		ret := strmangle.SetComplement(orcamentoSituacaoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`orcamento_situacao`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `orcamento_situacao` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(orcamentoSituacaoType, orcamentoSituacaoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(orcamentoSituacaoType, orcamentoSituacaoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for orcamento_situacao")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(orcamentoSituacaoType, orcamentoSituacaoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for orcamento_situacao")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for orcamento_situacao")
	}

CacheNoHooks:
	if !cached {
		orcamentoSituacaoUpsertCacheMut.Lock()
		orcamentoSituacaoUpsertCache[key] = cache
		orcamentoSituacaoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single OrcamentoSituacao record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *OrcamentoSituacao) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no OrcamentoSituacao provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), orcamentoSituacaoPrimaryKeyMapping)
	sql := "DELETE FROM `orcamento_situacao` WHERE `ors_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from orcamento_situacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for orcamento_situacao")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q orcamentoSituacaoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no orcamentoSituacaoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from orcamento_situacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for orcamento_situacao")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o OrcamentoSituacaoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(orcamentoSituacaoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orcamentoSituacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `orcamento_situacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, orcamentoSituacaoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from orcamentoSituacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for orcamento_situacao")
	}

	if len(orcamentoSituacaoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *OrcamentoSituacao) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindOrcamentoSituacao(ctx, exec, o.OrsID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *OrcamentoSituacaoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := OrcamentoSituacaoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), orcamentoSituacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `orcamento_situacao`.* FROM `orcamento_situacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, orcamentoSituacaoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in OrcamentoSituacaoSlice")
	}

	*o = slice

	return nil
}

// OrcamentoSituacaoExists checks if the OrcamentoSituacao row exists.
func OrcamentoSituacaoExists(ctx context.Context, exec boil.ContextExecutor, orsID int16) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `orcamento_situacao` where `ors_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, orsID)
	}
	row := exec.QueryRowContext(ctx, sql, orsID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if orcamento_situacao exists")
	}

	return exists, nil
}

// Exists checks if the OrcamentoSituacao row exists.
func (o *OrcamentoSituacao) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return OrcamentoSituacaoExists(ctx, exec, o.OrsID)
}
