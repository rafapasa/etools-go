// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// DocumentoVendaItemRep is an object representing the database table.
type DocumentoVendaItemRep struct {
	EntID        int               `boil:"ent_id" json:"ent_id" toml:"ent_id" yaml:"ent_id"`
	DDVID        int               `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	DviItem      int               `boil:"dvi_item" json:"dvi_item" toml:"dvi_item" yaml:"dvi_item"`
	DvirComissao types.NullDecimal `boil:"dvir_comissao" json:"dvir_comissao,omitempty" toml:"dvir_comissao" yaml:"dvir_comissao,omitempty"`

	R *documentoVendaItemRepR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L documentoVendaItemRepL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DocumentoVendaItemRepColumns = struct {
	EntID        string
	DDVID        string
	DviItem      string
	DvirComissao string
}{
	EntID:        "ent_id",
	DDVID:        "ddv_id",
	DviItem:      "dvi_item",
	DvirComissao: "dvir_comissao",
}

var DocumentoVendaItemRepTableColumns = struct {
	EntID        string
	DDVID        string
	DviItem      string
	DvirComissao string
}{
	EntID:        "documento_venda_item_rep.ent_id",
	DDVID:        "documento_venda_item_rep.ddv_id",
	DviItem:      "documento_venda_item_rep.dvi_item",
	DvirComissao: "documento_venda_item_rep.dvir_comissao",
}

// Generated where

var DocumentoVendaItemRepWhere = struct {
	EntID        whereHelperint
	DDVID        whereHelperint
	DviItem      whereHelperint
	DvirComissao whereHelpertypes_NullDecimal
}{
	EntID:        whereHelperint{field: "`documento_venda_item_rep`.`ent_id`"},
	DDVID:        whereHelperint{field: "`documento_venda_item_rep`.`ddv_id`"},
	DviItem:      whereHelperint{field: "`documento_venda_item_rep`.`dvi_item`"},
	DvirComissao: whereHelpertypes_NullDecimal{field: "`documento_venda_item_rep`.`dvir_comissao`"},
}

// DocumentoVendaItemRepRels is where relationship names are stored.
var DocumentoVendaItemRepRels = struct {
	Ent string
}{
	Ent: "Ent",
}

// documentoVendaItemRepR is where relationships are stored.
type documentoVendaItemRepR struct {
	Ent *Entidade `boil:"Ent" json:"Ent" toml:"Ent" yaml:"Ent"`
}

// NewStruct creates a new relationship struct
func (*documentoVendaItemRepR) NewStruct() *documentoVendaItemRepR {
	return &documentoVendaItemRepR{}
}

func (r *documentoVendaItemRepR) GetEnt() *Entidade {
	if r == nil {
		return nil
	}
	return r.Ent
}

// documentoVendaItemRepL is where Load methods for each relationship are stored.
type documentoVendaItemRepL struct{}

var (
	documentoVendaItemRepAllColumns            = []string{"ent_id", "ddv_id", "dvi_item", "dvir_comissao"}
	documentoVendaItemRepColumnsWithoutDefault = []string{"ent_id", "ddv_id", "dvi_item", "dvir_comissao"}
	documentoVendaItemRepColumnsWithDefault    = []string{}
	documentoVendaItemRepPrimaryKeyColumns     = []string{"ddv_id", "ent_id", "dvi_item"}
	documentoVendaItemRepGeneratedColumns      = []string{}
)

type (
	// DocumentoVendaItemRepSlice is an alias for a slice of pointers to DocumentoVendaItemRep.
	// This should almost always be used instead of []DocumentoVendaItemRep.
	DocumentoVendaItemRepSlice []*DocumentoVendaItemRep
	// DocumentoVendaItemRepHook is the signature for custom DocumentoVendaItemRep hook methods
	DocumentoVendaItemRepHook func(context.Context, boil.ContextExecutor, *DocumentoVendaItemRep) error

	documentoVendaItemRepQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	documentoVendaItemRepType                 = reflect.TypeOf(&DocumentoVendaItemRep{})
	documentoVendaItemRepMapping              = queries.MakeStructMapping(documentoVendaItemRepType)
	documentoVendaItemRepPrimaryKeyMapping, _ = queries.BindMapping(documentoVendaItemRepType, documentoVendaItemRepMapping, documentoVendaItemRepPrimaryKeyColumns)
	documentoVendaItemRepInsertCacheMut       sync.RWMutex
	documentoVendaItemRepInsertCache          = make(map[string]insertCache)
	documentoVendaItemRepUpdateCacheMut       sync.RWMutex
	documentoVendaItemRepUpdateCache          = make(map[string]updateCache)
	documentoVendaItemRepUpsertCacheMut       sync.RWMutex
	documentoVendaItemRepUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var documentoVendaItemRepAfterSelectMu sync.Mutex
var documentoVendaItemRepAfterSelectHooks []DocumentoVendaItemRepHook

var documentoVendaItemRepBeforeInsertMu sync.Mutex
var documentoVendaItemRepBeforeInsertHooks []DocumentoVendaItemRepHook
var documentoVendaItemRepAfterInsertMu sync.Mutex
var documentoVendaItemRepAfterInsertHooks []DocumentoVendaItemRepHook

var documentoVendaItemRepBeforeUpdateMu sync.Mutex
var documentoVendaItemRepBeforeUpdateHooks []DocumentoVendaItemRepHook
var documentoVendaItemRepAfterUpdateMu sync.Mutex
var documentoVendaItemRepAfterUpdateHooks []DocumentoVendaItemRepHook

var documentoVendaItemRepBeforeDeleteMu sync.Mutex
var documentoVendaItemRepBeforeDeleteHooks []DocumentoVendaItemRepHook
var documentoVendaItemRepAfterDeleteMu sync.Mutex
var documentoVendaItemRepAfterDeleteHooks []DocumentoVendaItemRepHook

var documentoVendaItemRepBeforeUpsertMu sync.Mutex
var documentoVendaItemRepBeforeUpsertHooks []DocumentoVendaItemRepHook
var documentoVendaItemRepAfterUpsertMu sync.Mutex
var documentoVendaItemRepAfterUpsertHooks []DocumentoVendaItemRepHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DocumentoVendaItemRep) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DocumentoVendaItemRep) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DocumentoVendaItemRep) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DocumentoVendaItemRep) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DocumentoVendaItemRep) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DocumentoVendaItemRep) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DocumentoVendaItemRep) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DocumentoVendaItemRep) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DocumentoVendaItemRep) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemRepAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDocumentoVendaItemRepHook registers your hook function for all future operations.
func AddDocumentoVendaItemRepHook(hookPoint boil.HookPoint, documentoVendaItemRepHook DocumentoVendaItemRepHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		documentoVendaItemRepAfterSelectMu.Lock()
		documentoVendaItemRepAfterSelectHooks = append(documentoVendaItemRepAfterSelectHooks, documentoVendaItemRepHook)
		documentoVendaItemRepAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		documentoVendaItemRepBeforeInsertMu.Lock()
		documentoVendaItemRepBeforeInsertHooks = append(documentoVendaItemRepBeforeInsertHooks, documentoVendaItemRepHook)
		documentoVendaItemRepBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		documentoVendaItemRepAfterInsertMu.Lock()
		documentoVendaItemRepAfterInsertHooks = append(documentoVendaItemRepAfterInsertHooks, documentoVendaItemRepHook)
		documentoVendaItemRepAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		documentoVendaItemRepBeforeUpdateMu.Lock()
		documentoVendaItemRepBeforeUpdateHooks = append(documentoVendaItemRepBeforeUpdateHooks, documentoVendaItemRepHook)
		documentoVendaItemRepBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		documentoVendaItemRepAfterUpdateMu.Lock()
		documentoVendaItemRepAfterUpdateHooks = append(documentoVendaItemRepAfterUpdateHooks, documentoVendaItemRepHook)
		documentoVendaItemRepAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		documentoVendaItemRepBeforeDeleteMu.Lock()
		documentoVendaItemRepBeforeDeleteHooks = append(documentoVendaItemRepBeforeDeleteHooks, documentoVendaItemRepHook)
		documentoVendaItemRepBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		documentoVendaItemRepAfterDeleteMu.Lock()
		documentoVendaItemRepAfterDeleteHooks = append(documentoVendaItemRepAfterDeleteHooks, documentoVendaItemRepHook)
		documentoVendaItemRepAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		documentoVendaItemRepBeforeUpsertMu.Lock()
		documentoVendaItemRepBeforeUpsertHooks = append(documentoVendaItemRepBeforeUpsertHooks, documentoVendaItemRepHook)
		documentoVendaItemRepBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		documentoVendaItemRepAfterUpsertMu.Lock()
		documentoVendaItemRepAfterUpsertHooks = append(documentoVendaItemRepAfterUpsertHooks, documentoVendaItemRepHook)
		documentoVendaItemRepAfterUpsertMu.Unlock()
	}
}

// One returns a single documentoVendaItemRep record from the query.
func (q documentoVendaItemRepQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DocumentoVendaItemRep, error) {
	o := &DocumentoVendaItemRep{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for documento_venda_item_rep")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DocumentoVendaItemRep records from the query.
func (q documentoVendaItemRepQuery) All(ctx context.Context, exec boil.ContextExecutor) (DocumentoVendaItemRepSlice, error) {
	var o []*DocumentoVendaItemRep

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DocumentoVendaItemRep slice")
	}

	if len(documentoVendaItemRepAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DocumentoVendaItemRep records in the query.
func (q documentoVendaItemRepQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count documento_venda_item_rep rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q documentoVendaItemRepQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if documento_venda_item_rep exists")
	}

	return count > 0, nil
}

// Ent pointed to by the foreign key.
func (o *DocumentoVendaItemRep) Ent(mods ...qm.QueryMod) entidadeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ent_id` = ?", o.EntID),
	}

	queryMods = append(queryMods, mods...)

	return Entidades(queryMods...)
}

// LoadEnt allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaItemRepL) LoadEnt(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaItemRep interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaItemRep
	var object *DocumentoVendaItemRep

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaItemRep.(*DocumentoVendaItemRep)
		if !ok {
			object = new(DocumentoVendaItemRep)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaItemRep)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaItemRep))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaItemRep.(*[]*DocumentoVendaItemRep)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaItemRep)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaItemRep))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaItemRepR{}
		}
		args[object.EntID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaItemRepR{}
			}

			args[obj.EntID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade`),
		qm.WhereIn(`entidade.ent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Entidade")
	}

	var resultSlice []*Entidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade")
	}

	if len(entidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ent = foreign
		if foreign.R == nil {
			foreign.R = &entidadeR{}
		}
		foreign.R.EntDocumentoVendaItemReps = append(foreign.R.EntDocumentoVendaItemReps, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EntID == foreign.EntID {
				local.R.Ent = foreign
				if foreign.R == nil {
					foreign.R = &entidadeR{}
				}
				foreign.R.EntDocumentoVendaItemReps = append(foreign.R.EntDocumentoVendaItemReps, local)
				break
			}
		}
	}

	return nil
}

// SetEnt of the documentoVendaItemRep to the related item.
// Sets o.R.Ent to related.
// Adds o to related.R.EntDocumentoVendaItemReps.
func (o *DocumentoVendaItemRep) SetEnt(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Entidade) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_item_rep` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ent_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaItemRepPrimaryKeyColumns),
	)
	values := []interface{}{related.EntID, o.DDVID, o.EntID, o.DviItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EntID = related.EntID
	if o.R == nil {
		o.R = &documentoVendaItemRepR{
			Ent: related,
		}
	} else {
		o.R.Ent = related
	}

	if related.R == nil {
		related.R = &entidadeR{
			EntDocumentoVendaItemReps: DocumentoVendaItemRepSlice{o},
		}
	} else {
		related.R.EntDocumentoVendaItemReps = append(related.R.EntDocumentoVendaItemReps, o)
	}

	return nil
}

// DocumentoVendaItemReps retrieves all the records using an executor.
func DocumentoVendaItemReps(mods ...qm.QueryMod) documentoVendaItemRepQuery {
	mods = append(mods, qm.From("`documento_venda_item_rep`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`documento_venda_item_rep`.*"})
	}

	return documentoVendaItemRepQuery{q}
}

// FindDocumentoVendaItemRep retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDocumentoVendaItemRep(ctx context.Context, exec boil.ContextExecutor, dDVID int, entID int, dviItem int, selectCols ...string) (*DocumentoVendaItemRep, error) {
	documentoVendaItemRepObj := &DocumentoVendaItemRep{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `documento_venda_item_rep` where `ddv_id`=? AND `ent_id`=? AND `dvi_item`=?", sel,
	)

	q := queries.Raw(query, dDVID, entID, dviItem)

	err := q.Bind(ctx, exec, documentoVendaItemRepObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from documento_venda_item_rep")
	}

	if err = documentoVendaItemRepObj.doAfterSelectHooks(ctx, exec); err != nil {
		return documentoVendaItemRepObj, err
	}

	return documentoVendaItemRepObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DocumentoVendaItemRep) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_item_rep provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaItemRepColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	documentoVendaItemRepInsertCacheMut.RLock()
	cache, cached := documentoVendaItemRepInsertCache[key]
	documentoVendaItemRepInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			documentoVendaItemRepAllColumns,
			documentoVendaItemRepColumnsWithDefault,
			documentoVendaItemRepColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaItemRepType, documentoVendaItemRepMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(documentoVendaItemRepType, documentoVendaItemRepMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `documento_venda_item_rep` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `documento_venda_item_rep` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `documento_venda_item_rep` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, documentoVendaItemRepPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into documento_venda_item_rep")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DDVID,
		o.EntID,
		o.DviItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_item_rep")
	}

CacheNoHooks:
	if !cached {
		documentoVendaItemRepInsertCacheMut.Lock()
		documentoVendaItemRepInsertCache[key] = cache
		documentoVendaItemRepInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DocumentoVendaItemRep.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DocumentoVendaItemRep) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	documentoVendaItemRepUpdateCacheMut.RLock()
	cache, cached := documentoVendaItemRepUpdateCache[key]
	documentoVendaItemRepUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			documentoVendaItemRepAllColumns,
			documentoVendaItemRepPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update documento_venda_item_rep, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `documento_venda_item_rep` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, documentoVendaItemRepPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(documentoVendaItemRepType, documentoVendaItemRepMapping, append(wl, documentoVendaItemRepPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update documento_venda_item_rep row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for documento_venda_item_rep")
	}

	if !cached {
		documentoVendaItemRepUpdateCacheMut.Lock()
		documentoVendaItemRepUpdateCache[key] = cache
		documentoVendaItemRepUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q documentoVendaItemRepQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for documento_venda_item_rep")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for documento_venda_item_rep")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DocumentoVendaItemRepSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemRepPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `documento_venda_item_rep` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemRepPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in documentoVendaItemRep slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all documentoVendaItemRep")
	}
	return rowsAff, nil
}

var mySQLDocumentoVendaItemRepUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DocumentoVendaItemRep) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_item_rep provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaItemRepColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDocumentoVendaItemRepUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	documentoVendaItemRepUpsertCacheMut.RLock()
	cache, cached := documentoVendaItemRepUpsertCache[key]
	documentoVendaItemRepUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			documentoVendaItemRepAllColumns,
			documentoVendaItemRepColumnsWithDefault,
			documentoVendaItemRepColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			documentoVendaItemRepAllColumns,
			documentoVendaItemRepPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert documento_venda_item_rep, could not build update column list")
		}

		ret := strmangle.SetComplement(documentoVendaItemRepAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`documento_venda_item_rep`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `documento_venda_item_rep` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaItemRepType, documentoVendaItemRepMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(documentoVendaItemRepType, documentoVendaItemRepMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for documento_venda_item_rep")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(documentoVendaItemRepType, documentoVendaItemRepMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for documento_venda_item_rep")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_item_rep")
	}

CacheNoHooks:
	if !cached {
		documentoVendaItemRepUpsertCacheMut.Lock()
		documentoVendaItemRepUpsertCache[key] = cache
		documentoVendaItemRepUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DocumentoVendaItemRep record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DocumentoVendaItemRep) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DocumentoVendaItemRep provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), documentoVendaItemRepPrimaryKeyMapping)
	sql := "DELETE FROM `documento_venda_item_rep` WHERE `ddv_id`=? AND `ent_id`=? AND `dvi_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from documento_venda_item_rep")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for documento_venda_item_rep")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q documentoVendaItemRepQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no documentoVendaItemRepQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documento_venda_item_rep")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_item_rep")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DocumentoVendaItemRepSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(documentoVendaItemRepBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemRepPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `documento_venda_item_rep` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemRepPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documentoVendaItemRep slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_item_rep")
	}

	if len(documentoVendaItemRepAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DocumentoVendaItemRep) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDocumentoVendaItemRep(ctx, exec, o.DDVID, o.EntID, o.DviItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DocumentoVendaItemRepSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DocumentoVendaItemRepSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemRepPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `documento_venda_item_rep`.* FROM `documento_venda_item_rep` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemRepPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DocumentoVendaItemRepSlice")
	}

	*o = slice

	return nil
}

// DocumentoVendaItemRepExists checks if the DocumentoVendaItemRep row exists.
func DocumentoVendaItemRepExists(ctx context.Context, exec boil.ContextExecutor, dDVID int, entID int, dviItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `documento_venda_item_rep` where `ddv_id`=? AND `ent_id`=? AND `dvi_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, dDVID, entID, dviItem)
	}
	row := exec.QueryRowContext(ctx, sql, dDVID, entID, dviItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if documento_venda_item_rep exists")
	}

	return exists, nil
}

// Exists checks if the DocumentoVendaItemRep row exists.
func (o *DocumentoVendaItemRep) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DocumentoVendaItemRepExists(ctx, exec, o.DDVID, o.EntID, o.DviItem)
}
