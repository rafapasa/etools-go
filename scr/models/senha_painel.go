// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SenhaPainel is an object representing the database table.
type SenhaPainel struct {
	SPNLID    int      `boil:"spnl_id" json:"spnl_id" toml:"spnl_id" yaml:"spnl_id"`
	SPNLSenha null.Int `boil:"spnl_senha" json:"spnl_senha,omitempty" toml:"spnl_senha" yaml:"spnl_senha,omitempty"`

	R *senhaPainelR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L senhaPainelL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SenhaPainelColumns = struct {
	SPNLID    string
	SPNLSenha string
}{
	SPNLID:    "spnl_id",
	SPNLSenha: "spnl_senha",
}

var SenhaPainelTableColumns = struct {
	SPNLID    string
	SPNLSenha string
}{
	SPNLID:    "senha_painel.spnl_id",
	SPNLSenha: "senha_painel.spnl_senha",
}

// Generated where

var SenhaPainelWhere = struct {
	SPNLID    whereHelperint
	SPNLSenha whereHelpernull_Int
}{
	SPNLID:    whereHelperint{field: "`senha_painel`.`spnl_id`"},
	SPNLSenha: whereHelpernull_Int{field: "`senha_painel`.`spnl_senha`"},
}

// SenhaPainelRels is where relationship names are stored.
var SenhaPainelRels = struct {
}{}

// senhaPainelR is where relationships are stored.
type senhaPainelR struct {
}

// NewStruct creates a new relationship struct
func (*senhaPainelR) NewStruct() *senhaPainelR {
	return &senhaPainelR{}
}

// senhaPainelL is where Load methods for each relationship are stored.
type senhaPainelL struct{}

var (
	senhaPainelAllColumns            = []string{"spnl_id", "spnl_senha"}
	senhaPainelColumnsWithoutDefault = []string{"spnl_id", "spnl_senha"}
	senhaPainelColumnsWithDefault    = []string{}
	senhaPainelPrimaryKeyColumns     = []string{"spnl_id"}
	senhaPainelGeneratedColumns      = []string{}
)

type (
	// SenhaPainelSlice is an alias for a slice of pointers to SenhaPainel.
	// This should almost always be used instead of []SenhaPainel.
	SenhaPainelSlice []*SenhaPainel
	// SenhaPainelHook is the signature for custom SenhaPainel hook methods
	SenhaPainelHook func(context.Context, boil.ContextExecutor, *SenhaPainel) error

	senhaPainelQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	senhaPainelType                 = reflect.TypeOf(&SenhaPainel{})
	senhaPainelMapping              = queries.MakeStructMapping(senhaPainelType)
	senhaPainelPrimaryKeyMapping, _ = queries.BindMapping(senhaPainelType, senhaPainelMapping, senhaPainelPrimaryKeyColumns)
	senhaPainelInsertCacheMut       sync.RWMutex
	senhaPainelInsertCache          = make(map[string]insertCache)
	senhaPainelUpdateCacheMut       sync.RWMutex
	senhaPainelUpdateCache          = make(map[string]updateCache)
	senhaPainelUpsertCacheMut       sync.RWMutex
	senhaPainelUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var senhaPainelAfterSelectMu sync.Mutex
var senhaPainelAfterSelectHooks []SenhaPainelHook

var senhaPainelBeforeInsertMu sync.Mutex
var senhaPainelBeforeInsertHooks []SenhaPainelHook
var senhaPainelAfterInsertMu sync.Mutex
var senhaPainelAfterInsertHooks []SenhaPainelHook

var senhaPainelBeforeUpdateMu sync.Mutex
var senhaPainelBeforeUpdateHooks []SenhaPainelHook
var senhaPainelAfterUpdateMu sync.Mutex
var senhaPainelAfterUpdateHooks []SenhaPainelHook

var senhaPainelBeforeDeleteMu sync.Mutex
var senhaPainelBeforeDeleteHooks []SenhaPainelHook
var senhaPainelAfterDeleteMu sync.Mutex
var senhaPainelAfterDeleteHooks []SenhaPainelHook

var senhaPainelBeforeUpsertMu sync.Mutex
var senhaPainelBeforeUpsertHooks []SenhaPainelHook
var senhaPainelAfterUpsertMu sync.Mutex
var senhaPainelAfterUpsertHooks []SenhaPainelHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SenhaPainel) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SenhaPainel) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SenhaPainel) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SenhaPainel) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SenhaPainel) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SenhaPainel) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SenhaPainel) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SenhaPainel) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SenhaPainel) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range senhaPainelAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSenhaPainelHook registers your hook function for all future operations.
func AddSenhaPainelHook(hookPoint boil.HookPoint, senhaPainelHook SenhaPainelHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		senhaPainelAfterSelectMu.Lock()
		senhaPainelAfterSelectHooks = append(senhaPainelAfterSelectHooks, senhaPainelHook)
		senhaPainelAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		senhaPainelBeforeInsertMu.Lock()
		senhaPainelBeforeInsertHooks = append(senhaPainelBeforeInsertHooks, senhaPainelHook)
		senhaPainelBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		senhaPainelAfterInsertMu.Lock()
		senhaPainelAfterInsertHooks = append(senhaPainelAfterInsertHooks, senhaPainelHook)
		senhaPainelAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		senhaPainelBeforeUpdateMu.Lock()
		senhaPainelBeforeUpdateHooks = append(senhaPainelBeforeUpdateHooks, senhaPainelHook)
		senhaPainelBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		senhaPainelAfterUpdateMu.Lock()
		senhaPainelAfterUpdateHooks = append(senhaPainelAfterUpdateHooks, senhaPainelHook)
		senhaPainelAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		senhaPainelBeforeDeleteMu.Lock()
		senhaPainelBeforeDeleteHooks = append(senhaPainelBeforeDeleteHooks, senhaPainelHook)
		senhaPainelBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		senhaPainelAfterDeleteMu.Lock()
		senhaPainelAfterDeleteHooks = append(senhaPainelAfterDeleteHooks, senhaPainelHook)
		senhaPainelAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		senhaPainelBeforeUpsertMu.Lock()
		senhaPainelBeforeUpsertHooks = append(senhaPainelBeforeUpsertHooks, senhaPainelHook)
		senhaPainelBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		senhaPainelAfterUpsertMu.Lock()
		senhaPainelAfterUpsertHooks = append(senhaPainelAfterUpsertHooks, senhaPainelHook)
		senhaPainelAfterUpsertMu.Unlock()
	}
}

// One returns a single senhaPainel record from the query.
func (q senhaPainelQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SenhaPainel, error) {
	o := &SenhaPainel{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for senha_painel")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all SenhaPainel records from the query.
func (q senhaPainelQuery) All(ctx context.Context, exec boil.ContextExecutor) (SenhaPainelSlice, error) {
	var o []*SenhaPainel

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SenhaPainel slice")
	}

	if len(senhaPainelAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all SenhaPainel records in the query.
func (q senhaPainelQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count senha_painel rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q senhaPainelQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if senha_painel exists")
	}

	return count > 0, nil
}

// SenhaPainels retrieves all the records using an executor.
func SenhaPainels(mods ...qm.QueryMod) senhaPainelQuery {
	mods = append(mods, qm.From("`senha_painel`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`senha_painel`.*"})
	}

	return senhaPainelQuery{q}
}

// FindSenhaPainel retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSenhaPainel(ctx context.Context, exec boil.ContextExecutor, sPNLID int, selectCols ...string) (*SenhaPainel, error) {
	senhaPainelObj := &SenhaPainel{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `senha_painel` where `spnl_id`=?", sel,
	)

	q := queries.Raw(query, sPNLID)

	err := q.Bind(ctx, exec, senhaPainelObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from senha_painel")
	}

	if err = senhaPainelObj.doAfterSelectHooks(ctx, exec); err != nil {
		return senhaPainelObj, err
	}

	return senhaPainelObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SenhaPainel) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no senha_painel provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(senhaPainelColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	senhaPainelInsertCacheMut.RLock()
	cache, cached := senhaPainelInsertCache[key]
	senhaPainelInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			senhaPainelAllColumns,
			senhaPainelColumnsWithDefault,
			senhaPainelColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(senhaPainelType, senhaPainelMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(senhaPainelType, senhaPainelMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `senha_painel` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `senha_painel` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `senha_painel` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, senhaPainelPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into senha_painel")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.SPNLID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for senha_painel")
	}

CacheNoHooks:
	if !cached {
		senhaPainelInsertCacheMut.Lock()
		senhaPainelInsertCache[key] = cache
		senhaPainelInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the SenhaPainel.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SenhaPainel) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	senhaPainelUpdateCacheMut.RLock()
	cache, cached := senhaPainelUpdateCache[key]
	senhaPainelUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			senhaPainelAllColumns,
			senhaPainelPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update senha_painel, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `senha_painel` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, senhaPainelPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(senhaPainelType, senhaPainelMapping, append(wl, senhaPainelPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update senha_painel row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for senha_painel")
	}

	if !cached {
		senhaPainelUpdateCacheMut.Lock()
		senhaPainelUpdateCache[key] = cache
		senhaPainelUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q senhaPainelQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for senha_painel")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for senha_painel")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SenhaPainelSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), senhaPainelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `senha_painel` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, senhaPainelPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in senhaPainel slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all senhaPainel")
	}
	return rowsAff, nil
}

var mySQLSenhaPainelUniqueColumns = []string{
	"spnl_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SenhaPainel) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no senha_painel provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(senhaPainelColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLSenhaPainelUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	senhaPainelUpsertCacheMut.RLock()
	cache, cached := senhaPainelUpsertCache[key]
	senhaPainelUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			senhaPainelAllColumns,
			senhaPainelColumnsWithDefault,
			senhaPainelColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			senhaPainelAllColumns,
			senhaPainelPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert senha_painel, could not build update column list")
		}

		ret := strmangle.SetComplement(senhaPainelAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`senha_painel`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `senha_painel` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(senhaPainelType, senhaPainelMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(senhaPainelType, senhaPainelMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for senha_painel")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(senhaPainelType, senhaPainelMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for senha_painel")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for senha_painel")
	}

CacheNoHooks:
	if !cached {
		senhaPainelUpsertCacheMut.Lock()
		senhaPainelUpsertCache[key] = cache
		senhaPainelUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single SenhaPainel record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SenhaPainel) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SenhaPainel provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), senhaPainelPrimaryKeyMapping)
	sql := "DELETE FROM `senha_painel` WHERE `spnl_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from senha_painel")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for senha_painel")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q senhaPainelQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no senhaPainelQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from senha_painel")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for senha_painel")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SenhaPainelSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(senhaPainelBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), senhaPainelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `senha_painel` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, senhaPainelPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from senhaPainel slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for senha_painel")
	}

	if len(senhaPainelAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SenhaPainel) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSenhaPainel(ctx, exec, o.SPNLID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SenhaPainelSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SenhaPainelSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), senhaPainelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `senha_painel`.* FROM `senha_painel` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, senhaPainelPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SenhaPainelSlice")
	}

	*o = slice

	return nil
}

// SenhaPainelExists checks if the SenhaPainel row exists.
func SenhaPainelExists(ctx context.Context, exec boil.ContextExecutor, sPNLID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `senha_painel` where `spnl_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, sPNLID)
	}
	row := exec.QueryRowContext(ctx, sql, sPNLID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if senha_painel exists")
	}

	return exists, nil
}

// Exists checks if the SenhaPainel row exists.
func (o *SenhaPainel) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return SenhaPainelExists(ctx, exec, o.SPNLID)
}
