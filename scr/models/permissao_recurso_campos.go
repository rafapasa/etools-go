// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PermissaoRecursoCampo is an object representing the database table.
type PermissaoRecursoCampo struct {
	PRCPID        int    `boil:"prcp_id" json:"prcp_id" toml:"prcp_id" yaml:"prcp_id"`
	PreID         int    `boil:"pre_id" json:"pre_id" toml:"pre_id" yaml:"pre_id"`
	PRCPNome      string `boil:"prcp_nome" json:"prcp_nome" toml:"prcp_nome" yaml:"prcp_nome"`
	PRCPDescricao string `boil:"prcp_descricao" json:"prcp_descricao" toml:"prcp_descricao" yaml:"prcp_descricao"`

	R *permissaoRecursoCampoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L permissaoRecursoCampoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PermissaoRecursoCampoColumns = struct {
	PRCPID        string
	PreID         string
	PRCPNome      string
	PRCPDescricao string
}{
	PRCPID:        "prcp_id",
	PreID:         "pre_id",
	PRCPNome:      "prcp_nome",
	PRCPDescricao: "prcp_descricao",
}

var PermissaoRecursoCampoTableColumns = struct {
	PRCPID        string
	PreID         string
	PRCPNome      string
	PRCPDescricao string
}{
	PRCPID:        "permissao_recurso_campos.prcp_id",
	PreID:         "permissao_recurso_campos.pre_id",
	PRCPNome:      "permissao_recurso_campos.prcp_nome",
	PRCPDescricao: "permissao_recurso_campos.prcp_descricao",
}

// Generated where

var PermissaoRecursoCampoWhere = struct {
	PRCPID        whereHelperint
	PreID         whereHelperint
	PRCPNome      whereHelperstring
	PRCPDescricao whereHelperstring
}{
	PRCPID:        whereHelperint{field: "`permissao_recurso_campos`.`prcp_id`"},
	PreID:         whereHelperint{field: "`permissao_recurso_campos`.`pre_id`"},
	PRCPNome:      whereHelperstring{field: "`permissao_recurso_campos`.`prcp_nome`"},
	PRCPDescricao: whereHelperstring{field: "`permissao_recurso_campos`.`prcp_descricao`"},
}

// PermissaoRecursoCampoRels is where relationship names are stored.
var PermissaoRecursoCampoRels = struct {
	Pre               string
	UfiUsuarioFilials string
}{
	Pre:               "Pre",
	UfiUsuarioFilials: "UfiUsuarioFilials",
}

// permissaoRecursoCampoR is where relationships are stored.
type permissaoRecursoCampoR struct {
	Pre               *PermissaoRecurso  `boil:"Pre" json:"Pre" toml:"Pre" yaml:"Pre"`
	UfiUsuarioFilials UsuarioFilialSlice `boil:"UfiUsuarioFilials" json:"UfiUsuarioFilials" toml:"UfiUsuarioFilials" yaml:"UfiUsuarioFilials"`
}

// NewStruct creates a new relationship struct
func (*permissaoRecursoCampoR) NewStruct() *permissaoRecursoCampoR {
	return &permissaoRecursoCampoR{}
}

func (r *permissaoRecursoCampoR) GetPre() *PermissaoRecurso {
	if r == nil {
		return nil
	}
	return r.Pre
}

func (r *permissaoRecursoCampoR) GetUfiUsuarioFilials() UsuarioFilialSlice {
	if r == nil {
		return nil
	}
	return r.UfiUsuarioFilials
}

// permissaoRecursoCampoL is where Load methods for each relationship are stored.
type permissaoRecursoCampoL struct{}

var (
	permissaoRecursoCampoAllColumns            = []string{"prcp_id", "pre_id", "prcp_nome", "prcp_descricao"}
	permissaoRecursoCampoColumnsWithoutDefault = []string{"prcp_id", "pre_id", "prcp_nome", "prcp_descricao"}
	permissaoRecursoCampoColumnsWithDefault    = []string{}
	permissaoRecursoCampoPrimaryKeyColumns     = []string{"prcp_id"}
	permissaoRecursoCampoGeneratedColumns      = []string{}
)

type (
	// PermissaoRecursoCampoSlice is an alias for a slice of pointers to PermissaoRecursoCampo.
	// This should almost always be used instead of []PermissaoRecursoCampo.
	PermissaoRecursoCampoSlice []*PermissaoRecursoCampo
	// PermissaoRecursoCampoHook is the signature for custom PermissaoRecursoCampo hook methods
	PermissaoRecursoCampoHook func(context.Context, boil.ContextExecutor, *PermissaoRecursoCampo) error

	permissaoRecursoCampoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	permissaoRecursoCampoType                 = reflect.TypeOf(&PermissaoRecursoCampo{})
	permissaoRecursoCampoMapping              = queries.MakeStructMapping(permissaoRecursoCampoType)
	permissaoRecursoCampoPrimaryKeyMapping, _ = queries.BindMapping(permissaoRecursoCampoType, permissaoRecursoCampoMapping, permissaoRecursoCampoPrimaryKeyColumns)
	permissaoRecursoCampoInsertCacheMut       sync.RWMutex
	permissaoRecursoCampoInsertCache          = make(map[string]insertCache)
	permissaoRecursoCampoUpdateCacheMut       sync.RWMutex
	permissaoRecursoCampoUpdateCache          = make(map[string]updateCache)
	permissaoRecursoCampoUpsertCacheMut       sync.RWMutex
	permissaoRecursoCampoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var permissaoRecursoCampoAfterSelectMu sync.Mutex
var permissaoRecursoCampoAfterSelectHooks []PermissaoRecursoCampoHook

var permissaoRecursoCampoBeforeInsertMu sync.Mutex
var permissaoRecursoCampoBeforeInsertHooks []PermissaoRecursoCampoHook
var permissaoRecursoCampoAfterInsertMu sync.Mutex
var permissaoRecursoCampoAfterInsertHooks []PermissaoRecursoCampoHook

var permissaoRecursoCampoBeforeUpdateMu sync.Mutex
var permissaoRecursoCampoBeforeUpdateHooks []PermissaoRecursoCampoHook
var permissaoRecursoCampoAfterUpdateMu sync.Mutex
var permissaoRecursoCampoAfterUpdateHooks []PermissaoRecursoCampoHook

var permissaoRecursoCampoBeforeDeleteMu sync.Mutex
var permissaoRecursoCampoBeforeDeleteHooks []PermissaoRecursoCampoHook
var permissaoRecursoCampoAfterDeleteMu sync.Mutex
var permissaoRecursoCampoAfterDeleteHooks []PermissaoRecursoCampoHook

var permissaoRecursoCampoBeforeUpsertMu sync.Mutex
var permissaoRecursoCampoBeforeUpsertHooks []PermissaoRecursoCampoHook
var permissaoRecursoCampoAfterUpsertMu sync.Mutex
var permissaoRecursoCampoAfterUpsertHooks []PermissaoRecursoCampoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PermissaoRecursoCampo) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PermissaoRecursoCampo) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PermissaoRecursoCampo) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PermissaoRecursoCampo) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PermissaoRecursoCampo) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PermissaoRecursoCampo) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PermissaoRecursoCampo) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PermissaoRecursoCampo) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PermissaoRecursoCampo) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoCampoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPermissaoRecursoCampoHook registers your hook function for all future operations.
func AddPermissaoRecursoCampoHook(hookPoint boil.HookPoint, permissaoRecursoCampoHook PermissaoRecursoCampoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		permissaoRecursoCampoAfterSelectMu.Lock()
		permissaoRecursoCampoAfterSelectHooks = append(permissaoRecursoCampoAfterSelectHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		permissaoRecursoCampoBeforeInsertMu.Lock()
		permissaoRecursoCampoBeforeInsertHooks = append(permissaoRecursoCampoBeforeInsertHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		permissaoRecursoCampoAfterInsertMu.Lock()
		permissaoRecursoCampoAfterInsertHooks = append(permissaoRecursoCampoAfterInsertHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		permissaoRecursoCampoBeforeUpdateMu.Lock()
		permissaoRecursoCampoBeforeUpdateHooks = append(permissaoRecursoCampoBeforeUpdateHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		permissaoRecursoCampoAfterUpdateMu.Lock()
		permissaoRecursoCampoAfterUpdateHooks = append(permissaoRecursoCampoAfterUpdateHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		permissaoRecursoCampoBeforeDeleteMu.Lock()
		permissaoRecursoCampoBeforeDeleteHooks = append(permissaoRecursoCampoBeforeDeleteHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		permissaoRecursoCampoAfterDeleteMu.Lock()
		permissaoRecursoCampoAfterDeleteHooks = append(permissaoRecursoCampoAfterDeleteHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		permissaoRecursoCampoBeforeUpsertMu.Lock()
		permissaoRecursoCampoBeforeUpsertHooks = append(permissaoRecursoCampoBeforeUpsertHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		permissaoRecursoCampoAfterUpsertMu.Lock()
		permissaoRecursoCampoAfterUpsertHooks = append(permissaoRecursoCampoAfterUpsertHooks, permissaoRecursoCampoHook)
		permissaoRecursoCampoAfterUpsertMu.Unlock()
	}
}

// One returns a single permissaoRecursoCampo record from the query.
func (q permissaoRecursoCampoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PermissaoRecursoCampo, error) {
	o := &PermissaoRecursoCampo{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for permissao_recurso_campos")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PermissaoRecursoCampo records from the query.
func (q permissaoRecursoCampoQuery) All(ctx context.Context, exec boil.ContextExecutor) (PermissaoRecursoCampoSlice, error) {
	var o []*PermissaoRecursoCampo

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PermissaoRecursoCampo slice")
	}

	if len(permissaoRecursoCampoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PermissaoRecursoCampo records in the query.
func (q permissaoRecursoCampoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count permissao_recurso_campos rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q permissaoRecursoCampoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if permissao_recurso_campos exists")
	}

	return count > 0, nil
}

// Pre pointed to by the foreign key.
func (o *PermissaoRecursoCampo) Pre(mods ...qm.QueryMod) permissaoRecursoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pre_id` = ?", o.PreID),
	}

	queryMods = append(queryMods, mods...)

	return PermissaoRecursos(queryMods...)
}

// UfiUsuarioFilials retrieves all the usuario_filial's UsuarioFilials with an executor via ufi_id column.
func (o *PermissaoRecursoCampo) UfiUsuarioFilials(mods ...qm.QueryMod) usuarioFilialQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("`usuario_filial_rec_campos` on `usuario_filial`.`ufi_id` = `usuario_filial_rec_campos`.`ufi_id`"),
		qm.Where("`usuario_filial_rec_campos`.`prcp_id`=?", o.PRCPID),
	)

	return UsuarioFilials(queryMods...)
}

// LoadPre allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (permissaoRecursoCampoL) LoadPre(ctx context.Context, e boil.ContextExecutor, singular bool, maybePermissaoRecursoCampo interface{}, mods queries.Applicator) error {
	var slice []*PermissaoRecursoCampo
	var object *PermissaoRecursoCampo

	if singular {
		var ok bool
		object, ok = maybePermissaoRecursoCampo.(*PermissaoRecursoCampo)
		if !ok {
			object = new(PermissaoRecursoCampo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePermissaoRecursoCampo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePermissaoRecursoCampo))
			}
		}
	} else {
		s, ok := maybePermissaoRecursoCampo.(*[]*PermissaoRecursoCampo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePermissaoRecursoCampo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePermissaoRecursoCampo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &permissaoRecursoCampoR{}
		}
		args[object.PreID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &permissaoRecursoCampoR{}
			}

			args[obj.PreID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`permissao_recurso`),
		qm.WhereIn(`permissao_recurso.pre_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PermissaoRecurso")
	}

	var resultSlice []*PermissaoRecurso
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PermissaoRecurso")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for permissao_recurso")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permissao_recurso")
	}

	if len(permissaoRecursoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pre = foreign
		if foreign.R == nil {
			foreign.R = &permissaoRecursoR{}
		}
		foreign.R.PrePermissaoRecursoCampos = append(foreign.R.PrePermissaoRecursoCampos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PreID == foreign.PreID {
				local.R.Pre = foreign
				if foreign.R == nil {
					foreign.R = &permissaoRecursoR{}
				}
				foreign.R.PrePermissaoRecursoCampos = append(foreign.R.PrePermissaoRecursoCampos, local)
				break
			}
		}
	}

	return nil
}

// LoadUfiUsuarioFilials allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (permissaoRecursoCampoL) LoadUfiUsuarioFilials(ctx context.Context, e boil.ContextExecutor, singular bool, maybePermissaoRecursoCampo interface{}, mods queries.Applicator) error {
	var slice []*PermissaoRecursoCampo
	var object *PermissaoRecursoCampo

	if singular {
		var ok bool
		object, ok = maybePermissaoRecursoCampo.(*PermissaoRecursoCampo)
		if !ok {
			object = new(PermissaoRecursoCampo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePermissaoRecursoCampo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePermissaoRecursoCampo))
			}
		}
	} else {
		s, ok := maybePermissaoRecursoCampo.(*[]*PermissaoRecursoCampo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePermissaoRecursoCampo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePermissaoRecursoCampo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &permissaoRecursoCampoR{}
		}
		args[object.PRCPID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &permissaoRecursoCampoR{}
			}
			args[obj.PRCPID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.Select("`usuario_filial`.`ufi_id`, `usuario_filial`.`usu_id`, `usuario_filial`.`emf_id`, `a`.`prcp_id`"),
		qm.From("`usuario_filial`"),
		qm.InnerJoin("`usuario_filial_rec_campos` as `a` on `usuario_filial`.`ufi_id` = `a`.`ufi_id`"),
		qm.WhereIn("`a`.`prcp_id` in ?", argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load usuario_filial")
	}

	var resultSlice []*UsuarioFilial

	var localJoinCols []int
	for results.Next() {
		one := new(UsuarioFilial)
		var localJoinCol int

		err = results.Scan(&one.UfiID, &one.UsuID, &one.EmfID, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for usuario_filial")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice usuario_filial")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on usuario_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for usuario_filial")
	}

	if len(usuarioFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UfiUsuarioFilials = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &usuarioFilialR{}
			}
			foreign.R.PRCPPermissaoRecursoCampos = append(foreign.R.PRCPPermissaoRecursoCampos, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.PRCPID == localJoinCol {
				local.R.UfiUsuarioFilials = append(local.R.UfiUsuarioFilials, foreign)
				if foreign.R == nil {
					foreign.R = &usuarioFilialR{}
				}
				foreign.R.PRCPPermissaoRecursoCampos = append(foreign.R.PRCPPermissaoRecursoCampos, local)
				break
			}
		}
	}

	return nil
}

// SetPre of the permissaoRecursoCampo to the related item.
// Sets o.R.Pre to related.
// Adds o to related.R.PrePermissaoRecursoCampos.
func (o *PermissaoRecursoCampo) SetPre(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PermissaoRecurso) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `permissao_recurso_campos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pre_id"}),
		strmangle.WhereClause("`", "`", 0, permissaoRecursoCampoPrimaryKeyColumns),
	)
	values := []interface{}{related.PreID, o.PRCPID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PreID = related.PreID
	if o.R == nil {
		o.R = &permissaoRecursoCampoR{
			Pre: related,
		}
	} else {
		o.R.Pre = related
	}

	if related.R == nil {
		related.R = &permissaoRecursoR{
			PrePermissaoRecursoCampos: PermissaoRecursoCampoSlice{o},
		}
	} else {
		related.R.PrePermissaoRecursoCampos = append(related.R.PrePermissaoRecursoCampos, o)
	}

	return nil
}

// AddUfiUsuarioFilials adds the given related objects to the existing relationships
// of the permissao_recurso_campo, optionally inserting them as new records.
// Appends related to o.R.UfiUsuarioFilials.
// Sets related.R.PRCPPermissaoRecursoCampos appropriately.
func (o *PermissaoRecursoCampo) AddUfiUsuarioFilials(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*UsuarioFilial) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into `usuario_filial_rec_campos` (`prcp_id`, `ufi_id`) values (?, ?)"
		values := []interface{}{o.PRCPID, rel.UfiID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &permissaoRecursoCampoR{
			UfiUsuarioFilials: related,
		}
	} else {
		o.R.UfiUsuarioFilials = append(o.R.UfiUsuarioFilials, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &usuarioFilialR{
				PRCPPermissaoRecursoCampos: PermissaoRecursoCampoSlice{o},
			}
		} else {
			rel.R.PRCPPermissaoRecursoCampos = append(rel.R.PRCPPermissaoRecursoCampos, o)
		}
	}
	return nil
}

// SetUfiUsuarioFilials removes all previously related items of the
// permissao_recurso_campo replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.PRCPPermissaoRecursoCampos's UfiUsuarioFilials accordingly.
// Replaces o.R.UfiUsuarioFilials with related.
// Sets related.R.PRCPPermissaoRecursoCampos's UfiUsuarioFilials accordingly.
func (o *PermissaoRecursoCampo) SetUfiUsuarioFilials(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*UsuarioFilial) error {
	query := "delete from `usuario_filial_rec_campos` where `prcp_id` = ?"
	values := []interface{}{o.PRCPID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeUfiUsuarioFilialsFromPRCPPermissaoRecursoCamposSlice(o, related)
	if o.R != nil {
		o.R.UfiUsuarioFilials = nil
	}

	return o.AddUfiUsuarioFilials(ctx, exec, insert, related...)
}

// RemoveUfiUsuarioFilials relationships from objects passed in.
// Removes related items from R.UfiUsuarioFilials (uses pointer comparison, removal does not keep order)
// Sets related.R.PRCPPermissaoRecursoCampos.
func (o *PermissaoRecursoCampo) RemoveUfiUsuarioFilials(ctx context.Context, exec boil.ContextExecutor, related ...*UsuarioFilial) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from `usuario_filial_rec_campos` where `prcp_id` = ? and `ufi_id` in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.PRCPID}
	for _, rel := range related {
		values = append(values, rel.UfiID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeUfiUsuarioFilialsFromPRCPPermissaoRecursoCamposSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UfiUsuarioFilials {
			if rel != ri {
				continue
			}

			ln := len(o.R.UfiUsuarioFilials)
			if ln > 1 && i < ln-1 {
				o.R.UfiUsuarioFilials[i] = o.R.UfiUsuarioFilials[ln-1]
			}
			o.R.UfiUsuarioFilials = o.R.UfiUsuarioFilials[:ln-1]
			break
		}
	}

	return nil
}

func removeUfiUsuarioFilialsFromPRCPPermissaoRecursoCamposSlice(o *PermissaoRecursoCampo, related []*UsuarioFilial) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.PRCPPermissaoRecursoCampos {
			if o.PRCPID != ri.PRCPID {
				continue
			}

			ln := len(rel.R.PRCPPermissaoRecursoCampos)
			if ln > 1 && i < ln-1 {
				rel.R.PRCPPermissaoRecursoCampos[i] = rel.R.PRCPPermissaoRecursoCampos[ln-1]
			}
			rel.R.PRCPPermissaoRecursoCampos = rel.R.PRCPPermissaoRecursoCampos[:ln-1]
			break
		}
	}
}

// PermissaoRecursoCampos retrieves all the records using an executor.
func PermissaoRecursoCampos(mods ...qm.QueryMod) permissaoRecursoCampoQuery {
	mods = append(mods, qm.From("`permissao_recurso_campos`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`permissao_recurso_campos`.*"})
	}

	return permissaoRecursoCampoQuery{q}
}

// FindPermissaoRecursoCampo retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPermissaoRecursoCampo(ctx context.Context, exec boil.ContextExecutor, pRCPID int, selectCols ...string) (*PermissaoRecursoCampo, error) {
	permissaoRecursoCampoObj := &PermissaoRecursoCampo{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `permissao_recurso_campos` where `prcp_id`=?", sel,
	)

	q := queries.Raw(query, pRCPID)

	err := q.Bind(ctx, exec, permissaoRecursoCampoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from permissao_recurso_campos")
	}

	if err = permissaoRecursoCampoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return permissaoRecursoCampoObj, err
	}

	return permissaoRecursoCampoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PermissaoRecursoCampo) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no permissao_recurso_campos provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(permissaoRecursoCampoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	permissaoRecursoCampoInsertCacheMut.RLock()
	cache, cached := permissaoRecursoCampoInsertCache[key]
	permissaoRecursoCampoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			permissaoRecursoCampoAllColumns,
			permissaoRecursoCampoColumnsWithDefault,
			permissaoRecursoCampoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(permissaoRecursoCampoType, permissaoRecursoCampoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(permissaoRecursoCampoType, permissaoRecursoCampoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `permissao_recurso_campos` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `permissao_recurso_campos` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `permissao_recurso_campos` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, permissaoRecursoCampoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into permissao_recurso_campos")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.PRCPID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for permissao_recurso_campos")
	}

CacheNoHooks:
	if !cached {
		permissaoRecursoCampoInsertCacheMut.Lock()
		permissaoRecursoCampoInsertCache[key] = cache
		permissaoRecursoCampoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PermissaoRecursoCampo.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PermissaoRecursoCampo) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	permissaoRecursoCampoUpdateCacheMut.RLock()
	cache, cached := permissaoRecursoCampoUpdateCache[key]
	permissaoRecursoCampoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			permissaoRecursoCampoAllColumns,
			permissaoRecursoCampoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update permissao_recurso_campos, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `permissao_recurso_campos` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, permissaoRecursoCampoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(permissaoRecursoCampoType, permissaoRecursoCampoMapping, append(wl, permissaoRecursoCampoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update permissao_recurso_campos row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for permissao_recurso_campos")
	}

	if !cached {
		permissaoRecursoCampoUpdateCacheMut.Lock()
		permissaoRecursoCampoUpdateCache[key] = cache
		permissaoRecursoCampoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q permissaoRecursoCampoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for permissao_recurso_campos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for permissao_recurso_campos")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PermissaoRecursoCampoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissaoRecursoCampoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `permissao_recurso_campos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, permissaoRecursoCampoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in permissaoRecursoCampo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all permissaoRecursoCampo")
	}
	return rowsAff, nil
}

var mySQLPermissaoRecursoCampoUniqueColumns = []string{
	"prcp_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PermissaoRecursoCampo) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no permissao_recurso_campos provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(permissaoRecursoCampoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLPermissaoRecursoCampoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	permissaoRecursoCampoUpsertCacheMut.RLock()
	cache, cached := permissaoRecursoCampoUpsertCache[key]
	permissaoRecursoCampoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			permissaoRecursoCampoAllColumns,
			permissaoRecursoCampoColumnsWithDefault,
			permissaoRecursoCampoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			permissaoRecursoCampoAllColumns,
			permissaoRecursoCampoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert permissao_recurso_campos, could not build update column list")
		}

		ret := strmangle.SetComplement(permissaoRecursoCampoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`permissao_recurso_campos`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `permissao_recurso_campos` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(permissaoRecursoCampoType, permissaoRecursoCampoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(permissaoRecursoCampoType, permissaoRecursoCampoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for permissao_recurso_campos")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(permissaoRecursoCampoType, permissaoRecursoCampoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for permissao_recurso_campos")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for permissao_recurso_campos")
	}

CacheNoHooks:
	if !cached {
		permissaoRecursoCampoUpsertCacheMut.Lock()
		permissaoRecursoCampoUpsertCache[key] = cache
		permissaoRecursoCampoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PermissaoRecursoCampo record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PermissaoRecursoCampo) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PermissaoRecursoCampo provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), permissaoRecursoCampoPrimaryKeyMapping)
	sql := "DELETE FROM `permissao_recurso_campos` WHERE `prcp_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from permissao_recurso_campos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for permissao_recurso_campos")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q permissaoRecursoCampoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no permissaoRecursoCampoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from permissao_recurso_campos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for permissao_recurso_campos")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PermissaoRecursoCampoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(permissaoRecursoCampoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissaoRecursoCampoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `permissao_recurso_campos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, permissaoRecursoCampoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from permissaoRecursoCampo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for permissao_recurso_campos")
	}

	if len(permissaoRecursoCampoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PermissaoRecursoCampo) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPermissaoRecursoCampo(ctx, exec, o.PRCPID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PermissaoRecursoCampoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PermissaoRecursoCampoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissaoRecursoCampoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `permissao_recurso_campos`.* FROM `permissao_recurso_campos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, permissaoRecursoCampoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PermissaoRecursoCampoSlice")
	}

	*o = slice

	return nil
}

// PermissaoRecursoCampoExists checks if the PermissaoRecursoCampo row exists.
func PermissaoRecursoCampoExists(ctx context.Context, exec boil.ContextExecutor, pRCPID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `permissao_recurso_campos` where `prcp_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, pRCPID)
	}
	row := exec.QueryRowContext(ctx, sql, pRCPID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if permissao_recurso_campos exists")
	}

	return exists, nil
}

// Exists checks if the PermissaoRecursoCampo row exists.
func (o *PermissaoRecursoCampo) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PermissaoRecursoCampoExists(ctx, exec, o.PRCPID)
}
