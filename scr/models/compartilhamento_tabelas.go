// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CompartilhamentoTabelas is an object representing the database table.
type CompartilhamentoTabelas struct {
	CptabID    int    `boil:"cptab_id" json:"cptab_id" toml:"cptab_id" yaml:"cptab_id"`
	EmfID      int    `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	EmfIDCompa int    `boil:"emf_id_compa" json:"emf_id_compa" toml:"emf_id_compa" yaml:"emf_id_compa"`
	CptabClass string `boil:"cptab_class" json:"cptab_class" toml:"cptab_class" yaml:"cptab_class"`

	R *compartilhamentoTabelasR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L compartilhamentoTabelasL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CompartilhamentoTabelasColumns = struct {
	CptabID    string
	EmfID      string
	EmfIDCompa string
	CptabClass string
}{
	CptabID:    "cptab_id",
	EmfID:      "emf_id",
	EmfIDCompa: "emf_id_compa",
	CptabClass: "cptab_class",
}

var CompartilhamentoTabelasTableColumns = struct {
	CptabID    string
	EmfID      string
	EmfIDCompa string
	CptabClass string
}{
	CptabID:    "compartilhamento_tabelas.cptab_id",
	EmfID:      "compartilhamento_tabelas.emf_id",
	EmfIDCompa: "compartilhamento_tabelas.emf_id_compa",
	CptabClass: "compartilhamento_tabelas.cptab_class",
}

// Generated where

var CompartilhamentoTabelasWhere = struct {
	CptabID    whereHelperint
	EmfID      whereHelperint
	EmfIDCompa whereHelperint
	CptabClass whereHelperstring
}{
	CptabID:    whereHelperint{field: "`compartilhamento_tabelas`.`cptab_id`"},
	EmfID:      whereHelperint{field: "`compartilhamento_tabelas`.`emf_id`"},
	EmfIDCompa: whereHelperint{field: "`compartilhamento_tabelas`.`emf_id_compa`"},
	CptabClass: whereHelperstring{field: "`compartilhamento_tabelas`.`cptab_class`"},
}

// CompartilhamentoTabelasRels is where relationship names are stored.
var CompartilhamentoTabelasRels = struct {
	Emf                     string
	EmfIDCompaEmpresaFilial string
}{
	Emf:                     "Emf",
	EmfIDCompaEmpresaFilial: "EmfIDCompaEmpresaFilial",
}

// compartilhamentoTabelasR is where relationships are stored.
type compartilhamentoTabelasR struct {
	Emf                     *EmpresaFilial `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	EmfIDCompaEmpresaFilial *EmpresaFilial `boil:"EmfIDCompaEmpresaFilial" json:"EmfIDCompaEmpresaFilial" toml:"EmfIDCompaEmpresaFilial" yaml:"EmfIDCompaEmpresaFilial"`
}

// NewStruct creates a new relationship struct
func (*compartilhamentoTabelasR) NewStruct() *compartilhamentoTabelasR {
	return &compartilhamentoTabelasR{}
}

func (r *compartilhamentoTabelasR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *compartilhamentoTabelasR) GetEmfIDCompaEmpresaFilial() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.EmfIDCompaEmpresaFilial
}

// compartilhamentoTabelasL is where Load methods for each relationship are stored.
type compartilhamentoTabelasL struct{}

var (
	compartilhamentoTabelasAllColumns            = []string{"cptab_id", "emf_id", "emf_id_compa", "cptab_class"}
	compartilhamentoTabelasColumnsWithoutDefault = []string{"cptab_id", "emf_id", "emf_id_compa", "cptab_class"}
	compartilhamentoTabelasColumnsWithDefault    = []string{}
	compartilhamentoTabelasPrimaryKeyColumns     = []string{"cptab_id"}
	compartilhamentoTabelasGeneratedColumns      = []string{}
)

type (
	// CompartilhamentoTabelasSlice is an alias for a slice of pointers to CompartilhamentoTabelas.
	// This should almost always be used instead of []CompartilhamentoTabelas.
	CompartilhamentoTabelasSlice []*CompartilhamentoTabelas
	// CompartilhamentoTabelasHook is the signature for custom CompartilhamentoTabelas hook methods
	CompartilhamentoTabelasHook func(context.Context, boil.ContextExecutor, *CompartilhamentoTabelas) error

	compartilhamentoTabelasQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	compartilhamentoTabelasType                 = reflect.TypeOf(&CompartilhamentoTabelas{})
	compartilhamentoTabelasMapping              = queries.MakeStructMapping(compartilhamentoTabelasType)
	compartilhamentoTabelasPrimaryKeyMapping, _ = queries.BindMapping(compartilhamentoTabelasType, compartilhamentoTabelasMapping, compartilhamentoTabelasPrimaryKeyColumns)
	compartilhamentoTabelasInsertCacheMut       sync.RWMutex
	compartilhamentoTabelasInsertCache          = make(map[string]insertCache)
	compartilhamentoTabelasUpdateCacheMut       sync.RWMutex
	compartilhamentoTabelasUpdateCache          = make(map[string]updateCache)
	compartilhamentoTabelasUpsertCacheMut       sync.RWMutex
	compartilhamentoTabelasUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var compartilhamentoTabelasAfterSelectMu sync.Mutex
var compartilhamentoTabelasAfterSelectHooks []CompartilhamentoTabelasHook

var compartilhamentoTabelasBeforeInsertMu sync.Mutex
var compartilhamentoTabelasBeforeInsertHooks []CompartilhamentoTabelasHook
var compartilhamentoTabelasAfterInsertMu sync.Mutex
var compartilhamentoTabelasAfterInsertHooks []CompartilhamentoTabelasHook

var compartilhamentoTabelasBeforeUpdateMu sync.Mutex
var compartilhamentoTabelasBeforeUpdateHooks []CompartilhamentoTabelasHook
var compartilhamentoTabelasAfterUpdateMu sync.Mutex
var compartilhamentoTabelasAfterUpdateHooks []CompartilhamentoTabelasHook

var compartilhamentoTabelasBeforeDeleteMu sync.Mutex
var compartilhamentoTabelasBeforeDeleteHooks []CompartilhamentoTabelasHook
var compartilhamentoTabelasAfterDeleteMu sync.Mutex
var compartilhamentoTabelasAfterDeleteHooks []CompartilhamentoTabelasHook

var compartilhamentoTabelasBeforeUpsertMu sync.Mutex
var compartilhamentoTabelasBeforeUpsertHooks []CompartilhamentoTabelasHook
var compartilhamentoTabelasAfterUpsertMu sync.Mutex
var compartilhamentoTabelasAfterUpsertHooks []CompartilhamentoTabelasHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CompartilhamentoTabelas) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CompartilhamentoTabelas) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CompartilhamentoTabelas) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CompartilhamentoTabelas) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CompartilhamentoTabelas) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CompartilhamentoTabelas) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CompartilhamentoTabelas) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CompartilhamentoTabelas) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CompartilhamentoTabelas) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range compartilhamentoTabelasAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCompartilhamentoTabelasHook registers your hook function for all future operations.
func AddCompartilhamentoTabelasHook(hookPoint boil.HookPoint, compartilhamentoTabelasHook CompartilhamentoTabelasHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		compartilhamentoTabelasAfterSelectMu.Lock()
		compartilhamentoTabelasAfterSelectHooks = append(compartilhamentoTabelasAfterSelectHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		compartilhamentoTabelasBeforeInsertMu.Lock()
		compartilhamentoTabelasBeforeInsertHooks = append(compartilhamentoTabelasBeforeInsertHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		compartilhamentoTabelasAfterInsertMu.Lock()
		compartilhamentoTabelasAfterInsertHooks = append(compartilhamentoTabelasAfterInsertHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		compartilhamentoTabelasBeforeUpdateMu.Lock()
		compartilhamentoTabelasBeforeUpdateHooks = append(compartilhamentoTabelasBeforeUpdateHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		compartilhamentoTabelasAfterUpdateMu.Lock()
		compartilhamentoTabelasAfterUpdateHooks = append(compartilhamentoTabelasAfterUpdateHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		compartilhamentoTabelasBeforeDeleteMu.Lock()
		compartilhamentoTabelasBeforeDeleteHooks = append(compartilhamentoTabelasBeforeDeleteHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		compartilhamentoTabelasAfterDeleteMu.Lock()
		compartilhamentoTabelasAfterDeleteHooks = append(compartilhamentoTabelasAfterDeleteHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		compartilhamentoTabelasBeforeUpsertMu.Lock()
		compartilhamentoTabelasBeforeUpsertHooks = append(compartilhamentoTabelasBeforeUpsertHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		compartilhamentoTabelasAfterUpsertMu.Lock()
		compartilhamentoTabelasAfterUpsertHooks = append(compartilhamentoTabelasAfterUpsertHooks, compartilhamentoTabelasHook)
		compartilhamentoTabelasAfterUpsertMu.Unlock()
	}
}

// One returns a single compartilhamentoTabelas record from the query.
func (q compartilhamentoTabelasQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CompartilhamentoTabelas, error) {
	o := &CompartilhamentoTabelas{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for compartilhamento_tabelas")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CompartilhamentoTabelas records from the query.
func (q compartilhamentoTabelasQuery) All(ctx context.Context, exec boil.ContextExecutor) (CompartilhamentoTabelasSlice, error) {
	var o []*CompartilhamentoTabelas

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CompartilhamentoTabelas slice")
	}

	if len(compartilhamentoTabelasAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CompartilhamentoTabelas records in the query.
func (q compartilhamentoTabelasQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count compartilhamento_tabelas rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q compartilhamentoTabelasQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if compartilhamento_tabelas exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *CompartilhamentoTabelas) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// EmfIDCompaEmpresaFilial pointed to by the foreign key.
func (o *CompartilhamentoTabelas) EmfIDCompaEmpresaFilial(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfIDCompa),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (compartilhamentoTabelasL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCompartilhamentoTabelas interface{}, mods queries.Applicator) error {
	var slice []*CompartilhamentoTabelas
	var object *CompartilhamentoTabelas

	if singular {
		var ok bool
		object, ok = maybeCompartilhamentoTabelas.(*CompartilhamentoTabelas)
		if !ok {
			object = new(CompartilhamentoTabelas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCompartilhamentoTabelas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCompartilhamentoTabelas))
			}
		}
	} else {
		s, ok := maybeCompartilhamentoTabelas.(*[]*CompartilhamentoTabelas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCompartilhamentoTabelas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCompartilhamentoTabelas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &compartilhamentoTabelasR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &compartilhamentoTabelasR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfCompartilhamentoTabelas = append(foreign.R.EmfCompartilhamentoTabelas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfCompartilhamentoTabelas = append(foreign.R.EmfCompartilhamentoTabelas, local)
				break
			}
		}
	}

	return nil
}

// LoadEmfIDCompaEmpresaFilial allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (compartilhamentoTabelasL) LoadEmfIDCompaEmpresaFilial(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCompartilhamentoTabelas interface{}, mods queries.Applicator) error {
	var slice []*CompartilhamentoTabelas
	var object *CompartilhamentoTabelas

	if singular {
		var ok bool
		object, ok = maybeCompartilhamentoTabelas.(*CompartilhamentoTabelas)
		if !ok {
			object = new(CompartilhamentoTabelas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCompartilhamentoTabelas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCompartilhamentoTabelas))
			}
		}
	} else {
		s, ok := maybeCompartilhamentoTabelas.(*[]*CompartilhamentoTabelas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCompartilhamentoTabelas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCompartilhamentoTabelas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &compartilhamentoTabelasR{}
		}
		args[object.EmfIDCompa] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &compartilhamentoTabelasR{}
			}

			args[obj.EmfIDCompa] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.EmfIDCompaEmpresaFilial = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfIDCompaCompartilhamentoTabelas = append(foreign.R.EmfIDCompaCompartilhamentoTabelas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfIDCompa == foreign.EmfID {
				local.R.EmfIDCompaEmpresaFilial = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfIDCompaCompartilhamentoTabelas = append(foreign.R.EmfIDCompaCompartilhamentoTabelas, local)
				break
			}
		}
	}

	return nil
}

// SetEmf of the compartilhamentoTabelas to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfCompartilhamentoTabelas.
func (o *CompartilhamentoTabelas) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `compartilhamento_tabelas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, compartilhamentoTabelasPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.CptabID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &compartilhamentoTabelasR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfCompartilhamentoTabelas: CompartilhamentoTabelasSlice{o},
		}
	} else {
		related.R.EmfCompartilhamentoTabelas = append(related.R.EmfCompartilhamentoTabelas, o)
	}

	return nil
}

// SetEmfIDCompaEmpresaFilial of the compartilhamentoTabelas to the related item.
// Sets o.R.EmfIDCompaEmpresaFilial to related.
// Adds o to related.R.EmfIDCompaCompartilhamentoTabelas.
func (o *CompartilhamentoTabelas) SetEmfIDCompaEmpresaFilial(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `compartilhamento_tabelas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id_compa"}),
		strmangle.WhereClause("`", "`", 0, compartilhamentoTabelasPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.CptabID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfIDCompa = related.EmfID
	if o.R == nil {
		o.R = &compartilhamentoTabelasR{
			EmfIDCompaEmpresaFilial: related,
		}
	} else {
		o.R.EmfIDCompaEmpresaFilial = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfIDCompaCompartilhamentoTabelas: CompartilhamentoTabelasSlice{o},
		}
	} else {
		related.R.EmfIDCompaCompartilhamentoTabelas = append(related.R.EmfIDCompaCompartilhamentoTabelas, o)
	}

	return nil
}

// CompartilhamentoTabelas retrieves all the records using an executor.
func CompartilhamentoTabelas(mods ...qm.QueryMod) compartilhamentoTabelasQuery {
	mods = append(mods, qm.From("`compartilhamento_tabelas`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`compartilhamento_tabelas`.*"})
	}

	return compartilhamentoTabelasQuery{q}
}

// FindCompartilhamentoTabelas retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCompartilhamentoTabelas(ctx context.Context, exec boil.ContextExecutor, cptabID int, selectCols ...string) (*CompartilhamentoTabelas, error) {
	compartilhamentoTabelasObj := &CompartilhamentoTabelas{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `compartilhamento_tabelas` where `cptab_id`=?", sel,
	)

	q := queries.Raw(query, cptabID)

	err := q.Bind(ctx, exec, compartilhamentoTabelasObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from compartilhamento_tabelas")
	}

	if err = compartilhamentoTabelasObj.doAfterSelectHooks(ctx, exec); err != nil {
		return compartilhamentoTabelasObj, err
	}

	return compartilhamentoTabelasObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CompartilhamentoTabelas) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no compartilhamento_tabelas provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(compartilhamentoTabelasColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	compartilhamentoTabelasInsertCacheMut.RLock()
	cache, cached := compartilhamentoTabelasInsertCache[key]
	compartilhamentoTabelasInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			compartilhamentoTabelasAllColumns,
			compartilhamentoTabelasColumnsWithDefault,
			compartilhamentoTabelasColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(compartilhamentoTabelasType, compartilhamentoTabelasMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(compartilhamentoTabelasType, compartilhamentoTabelasMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `compartilhamento_tabelas` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `compartilhamento_tabelas` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `compartilhamento_tabelas` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, compartilhamentoTabelasPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into compartilhamento_tabelas")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CptabID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for compartilhamento_tabelas")
	}

CacheNoHooks:
	if !cached {
		compartilhamentoTabelasInsertCacheMut.Lock()
		compartilhamentoTabelasInsertCache[key] = cache
		compartilhamentoTabelasInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CompartilhamentoTabelas.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CompartilhamentoTabelas) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	compartilhamentoTabelasUpdateCacheMut.RLock()
	cache, cached := compartilhamentoTabelasUpdateCache[key]
	compartilhamentoTabelasUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			compartilhamentoTabelasAllColumns,
			compartilhamentoTabelasPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update compartilhamento_tabelas, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `compartilhamento_tabelas` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, compartilhamentoTabelasPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(compartilhamentoTabelasType, compartilhamentoTabelasMapping, append(wl, compartilhamentoTabelasPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update compartilhamento_tabelas row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for compartilhamento_tabelas")
	}

	if !cached {
		compartilhamentoTabelasUpdateCacheMut.Lock()
		compartilhamentoTabelasUpdateCache[key] = cache
		compartilhamentoTabelasUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q compartilhamentoTabelasQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for compartilhamento_tabelas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for compartilhamento_tabelas")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CompartilhamentoTabelasSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), compartilhamentoTabelasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `compartilhamento_tabelas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, compartilhamentoTabelasPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in compartilhamentoTabelas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all compartilhamentoTabelas")
	}
	return rowsAff, nil
}

var mySQLCompartilhamentoTabelasUniqueColumns = []string{
	"cptab_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CompartilhamentoTabelas) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no compartilhamento_tabelas provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(compartilhamentoTabelasColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCompartilhamentoTabelasUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	compartilhamentoTabelasUpsertCacheMut.RLock()
	cache, cached := compartilhamentoTabelasUpsertCache[key]
	compartilhamentoTabelasUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			compartilhamentoTabelasAllColumns,
			compartilhamentoTabelasColumnsWithDefault,
			compartilhamentoTabelasColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			compartilhamentoTabelasAllColumns,
			compartilhamentoTabelasPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert compartilhamento_tabelas, could not build update column list")
		}

		ret := strmangle.SetComplement(compartilhamentoTabelasAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`compartilhamento_tabelas`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `compartilhamento_tabelas` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(compartilhamentoTabelasType, compartilhamentoTabelasMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(compartilhamentoTabelasType, compartilhamentoTabelasMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for compartilhamento_tabelas")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(compartilhamentoTabelasType, compartilhamentoTabelasMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for compartilhamento_tabelas")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for compartilhamento_tabelas")
	}

CacheNoHooks:
	if !cached {
		compartilhamentoTabelasUpsertCacheMut.Lock()
		compartilhamentoTabelasUpsertCache[key] = cache
		compartilhamentoTabelasUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CompartilhamentoTabelas record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CompartilhamentoTabelas) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CompartilhamentoTabelas provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), compartilhamentoTabelasPrimaryKeyMapping)
	sql := "DELETE FROM `compartilhamento_tabelas` WHERE `cptab_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from compartilhamento_tabelas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for compartilhamento_tabelas")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q compartilhamentoTabelasQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no compartilhamentoTabelasQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from compartilhamento_tabelas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for compartilhamento_tabelas")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CompartilhamentoTabelasSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(compartilhamentoTabelasBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), compartilhamentoTabelasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `compartilhamento_tabelas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, compartilhamentoTabelasPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from compartilhamentoTabelas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for compartilhamento_tabelas")
	}

	if len(compartilhamentoTabelasAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CompartilhamentoTabelas) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCompartilhamentoTabelas(ctx, exec, o.CptabID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CompartilhamentoTabelasSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CompartilhamentoTabelasSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), compartilhamentoTabelasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `compartilhamento_tabelas`.* FROM `compartilhamento_tabelas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, compartilhamentoTabelasPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CompartilhamentoTabelasSlice")
	}

	*o = slice

	return nil
}

// CompartilhamentoTabelasExists checks if the CompartilhamentoTabelas row exists.
func CompartilhamentoTabelasExists(ctx context.Context, exec boil.ContextExecutor, cptabID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `compartilhamento_tabelas` where `cptab_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cptabID)
	}
	row := exec.QueryRowContext(ctx, sql, cptabID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if compartilhamento_tabelas exists")
	}

	return exists, nil
}

// Exists checks if the CompartilhamentoTabelas row exists.
func (o *CompartilhamentoTabelas) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CompartilhamentoTabelasExists(ctx, exec, o.CptabID)
}
