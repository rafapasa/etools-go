// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DocumentoVendaChecklist is an object representing the database table.
type DocumentoVendaChecklist struct {
	DDVID          int        `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	CKLID          int        `boil:"ckl_id" json:"ckl_id" toml:"ckl_id" yaml:"ckl_id"`
	CkliItem       int        `boil:"ckli_item" json:"ckli_item" toml:"ckli_item" yaml:"ckli_item"`
	DDVCKLResposta null.Int16 `boil:"ddvckl_resposta" json:"ddvckl_resposta,omitempty" toml:"ddvckl_resposta" yaml:"ddvckl_resposta,omitempty"`

	R *documentoVendaChecklistR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L documentoVendaChecklistL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DocumentoVendaChecklistColumns = struct {
	DDVID          string
	CKLID          string
	CkliItem       string
	DDVCKLResposta string
}{
	DDVID:          "ddv_id",
	CKLID:          "ckl_id",
	CkliItem:       "ckli_item",
	DDVCKLResposta: "ddvckl_resposta",
}

var DocumentoVendaChecklistTableColumns = struct {
	DDVID          string
	CKLID          string
	CkliItem       string
	DDVCKLResposta string
}{
	DDVID:          "documento_venda_checklist.ddv_id",
	CKLID:          "documento_venda_checklist.ckl_id",
	CkliItem:       "documento_venda_checklist.ckli_item",
	DDVCKLResposta: "documento_venda_checklist.ddvckl_resposta",
}

// Generated where

var DocumentoVendaChecklistWhere = struct {
	DDVID          whereHelperint
	CKLID          whereHelperint
	CkliItem       whereHelperint
	DDVCKLResposta whereHelpernull_Int16
}{
	DDVID:          whereHelperint{field: "`documento_venda_checklist`.`ddv_id`"},
	CKLID:          whereHelperint{field: "`documento_venda_checklist`.`ckl_id`"},
	CkliItem:       whereHelperint{field: "`documento_venda_checklist`.`ckli_item`"},
	DDVCKLResposta: whereHelpernull_Int16{field: "`documento_venda_checklist`.`ddvckl_resposta`"},
}

// DocumentoVendaChecklistRels is where relationship names are stored.
var DocumentoVendaChecklistRels = struct {
	DDV string
}{
	DDV: "DDV",
}

// documentoVendaChecklistR is where relationships are stored.
type documentoVendaChecklistR struct {
	DDV *DocumentoVenda `boil:"DDV" json:"DDV" toml:"DDV" yaml:"DDV"`
}

// NewStruct creates a new relationship struct
func (*documentoVendaChecklistR) NewStruct() *documentoVendaChecklistR {
	return &documentoVendaChecklistR{}
}

func (r *documentoVendaChecklistR) GetDDV() *DocumentoVenda {
	if r == nil {
		return nil
	}
	return r.DDV
}

// documentoVendaChecklistL is where Load methods for each relationship are stored.
type documentoVendaChecklistL struct{}

var (
	documentoVendaChecklistAllColumns            = []string{"ddv_id", "ckl_id", "ckli_item", "ddvckl_resposta"}
	documentoVendaChecklistColumnsWithoutDefault = []string{"ddv_id", "ckl_id", "ckli_item", "ddvckl_resposta"}
	documentoVendaChecklistColumnsWithDefault    = []string{}
	documentoVendaChecklistPrimaryKeyColumns     = []string{"ckl_id", "ddv_id", "ckli_item"}
	documentoVendaChecklistGeneratedColumns      = []string{}
)

type (
	// DocumentoVendaChecklistSlice is an alias for a slice of pointers to DocumentoVendaChecklist.
	// This should almost always be used instead of []DocumentoVendaChecklist.
	DocumentoVendaChecklistSlice []*DocumentoVendaChecklist
	// DocumentoVendaChecklistHook is the signature for custom DocumentoVendaChecklist hook methods
	DocumentoVendaChecklistHook func(context.Context, boil.ContextExecutor, *DocumentoVendaChecklist) error

	documentoVendaChecklistQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	documentoVendaChecklistType                 = reflect.TypeOf(&DocumentoVendaChecklist{})
	documentoVendaChecklistMapping              = queries.MakeStructMapping(documentoVendaChecklistType)
	documentoVendaChecklistPrimaryKeyMapping, _ = queries.BindMapping(documentoVendaChecklistType, documentoVendaChecklistMapping, documentoVendaChecklistPrimaryKeyColumns)
	documentoVendaChecklistInsertCacheMut       sync.RWMutex
	documentoVendaChecklistInsertCache          = make(map[string]insertCache)
	documentoVendaChecklistUpdateCacheMut       sync.RWMutex
	documentoVendaChecklistUpdateCache          = make(map[string]updateCache)
	documentoVendaChecklistUpsertCacheMut       sync.RWMutex
	documentoVendaChecklistUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var documentoVendaChecklistAfterSelectMu sync.Mutex
var documentoVendaChecklistAfterSelectHooks []DocumentoVendaChecklistHook

var documentoVendaChecklistBeforeInsertMu sync.Mutex
var documentoVendaChecklistBeforeInsertHooks []DocumentoVendaChecklistHook
var documentoVendaChecklistAfterInsertMu sync.Mutex
var documentoVendaChecklistAfterInsertHooks []DocumentoVendaChecklistHook

var documentoVendaChecklistBeforeUpdateMu sync.Mutex
var documentoVendaChecklistBeforeUpdateHooks []DocumentoVendaChecklistHook
var documentoVendaChecklistAfterUpdateMu sync.Mutex
var documentoVendaChecklistAfterUpdateHooks []DocumentoVendaChecklistHook

var documentoVendaChecklistBeforeDeleteMu sync.Mutex
var documentoVendaChecklistBeforeDeleteHooks []DocumentoVendaChecklistHook
var documentoVendaChecklistAfterDeleteMu sync.Mutex
var documentoVendaChecklistAfterDeleteHooks []DocumentoVendaChecklistHook

var documentoVendaChecklistBeforeUpsertMu sync.Mutex
var documentoVendaChecklistBeforeUpsertHooks []DocumentoVendaChecklistHook
var documentoVendaChecklistAfterUpsertMu sync.Mutex
var documentoVendaChecklistAfterUpsertHooks []DocumentoVendaChecklistHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DocumentoVendaChecklist) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DocumentoVendaChecklist) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DocumentoVendaChecklist) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DocumentoVendaChecklist) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DocumentoVendaChecklist) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DocumentoVendaChecklist) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DocumentoVendaChecklist) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DocumentoVendaChecklist) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DocumentoVendaChecklist) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaChecklistAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDocumentoVendaChecklistHook registers your hook function for all future operations.
func AddDocumentoVendaChecklistHook(hookPoint boil.HookPoint, documentoVendaChecklistHook DocumentoVendaChecklistHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		documentoVendaChecklistAfterSelectMu.Lock()
		documentoVendaChecklistAfterSelectHooks = append(documentoVendaChecklistAfterSelectHooks, documentoVendaChecklistHook)
		documentoVendaChecklistAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		documentoVendaChecklistBeforeInsertMu.Lock()
		documentoVendaChecklistBeforeInsertHooks = append(documentoVendaChecklistBeforeInsertHooks, documentoVendaChecklistHook)
		documentoVendaChecklistBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		documentoVendaChecklistAfterInsertMu.Lock()
		documentoVendaChecklistAfterInsertHooks = append(documentoVendaChecklistAfterInsertHooks, documentoVendaChecklistHook)
		documentoVendaChecklistAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		documentoVendaChecklistBeforeUpdateMu.Lock()
		documentoVendaChecklistBeforeUpdateHooks = append(documentoVendaChecklistBeforeUpdateHooks, documentoVendaChecklistHook)
		documentoVendaChecklistBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		documentoVendaChecklistAfterUpdateMu.Lock()
		documentoVendaChecklistAfterUpdateHooks = append(documentoVendaChecklistAfterUpdateHooks, documentoVendaChecklistHook)
		documentoVendaChecklistAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		documentoVendaChecklistBeforeDeleteMu.Lock()
		documentoVendaChecklistBeforeDeleteHooks = append(documentoVendaChecklistBeforeDeleteHooks, documentoVendaChecklistHook)
		documentoVendaChecklistBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		documentoVendaChecklistAfterDeleteMu.Lock()
		documentoVendaChecklistAfterDeleteHooks = append(documentoVendaChecklistAfterDeleteHooks, documentoVendaChecklistHook)
		documentoVendaChecklistAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		documentoVendaChecklistBeforeUpsertMu.Lock()
		documentoVendaChecklistBeforeUpsertHooks = append(documentoVendaChecklistBeforeUpsertHooks, documentoVendaChecklistHook)
		documentoVendaChecklistBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		documentoVendaChecklistAfterUpsertMu.Lock()
		documentoVendaChecklistAfterUpsertHooks = append(documentoVendaChecklistAfterUpsertHooks, documentoVendaChecklistHook)
		documentoVendaChecklistAfterUpsertMu.Unlock()
	}
}

// One returns a single documentoVendaChecklist record from the query.
func (q documentoVendaChecklistQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DocumentoVendaChecklist, error) {
	o := &DocumentoVendaChecklist{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for documento_venda_checklist")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DocumentoVendaChecklist records from the query.
func (q documentoVendaChecklistQuery) All(ctx context.Context, exec boil.ContextExecutor) (DocumentoVendaChecklistSlice, error) {
	var o []*DocumentoVendaChecklist

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DocumentoVendaChecklist slice")
	}

	if len(documentoVendaChecklistAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DocumentoVendaChecklist records in the query.
func (q documentoVendaChecklistQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count documento_venda_checklist rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q documentoVendaChecklistQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if documento_venda_checklist exists")
	}

	return count > 0, nil
}

// DDV pointed to by the foreign key.
func (o *DocumentoVendaChecklist) DDV(mods ...qm.QueryMod) documentoVendaQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ddv_id` = ?", o.DDVID),
	}

	queryMods = append(queryMods, mods...)

	return DocumentoVendas(queryMods...)
}

// LoadDDV allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaChecklistL) LoadDDV(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaChecklist interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaChecklist
	var object *DocumentoVendaChecklist

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaChecklist.(*DocumentoVendaChecklist)
		if !ok {
			object = new(DocumentoVendaChecklist)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaChecklist)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaChecklist))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaChecklist.(*[]*DocumentoVendaChecklist)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaChecklist)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaChecklist))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaChecklistR{}
		}
		args[object.DDVID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaChecklistR{}
			}

			args[obj.DDVID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documento_venda`),
		qm.WhereIn(`documento_venda.ddv_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DocumentoVenda")
	}

	var resultSlice []*DocumentoVenda
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DocumentoVenda")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for documento_venda")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documento_venda")
	}

	if len(documentoVendaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.DDV = foreign
		if foreign.R == nil {
			foreign.R = &documentoVendaR{}
		}
		foreign.R.DDVDocumentoVendaChecklists = append(foreign.R.DDVDocumentoVendaChecklists, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.DDVID == foreign.DDVID {
				local.R.DDV = foreign
				if foreign.R == nil {
					foreign.R = &documentoVendaR{}
				}
				foreign.R.DDVDocumentoVendaChecklists = append(foreign.R.DDVDocumentoVendaChecklists, local)
				break
			}
		}
	}

	return nil
}

// SetDDV of the documentoVendaChecklist to the related item.
// Sets o.R.DDV to related.
// Adds o to related.R.DDVDocumentoVendaChecklists.
func (o *DocumentoVendaChecklist) SetDDV(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DocumentoVenda) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_checklist` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ddv_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaChecklistPrimaryKeyColumns),
	)
	values := []interface{}{related.DDVID, o.CKLID, o.DDVID, o.CkliItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.DDVID = related.DDVID
	if o.R == nil {
		o.R = &documentoVendaChecklistR{
			DDV: related,
		}
	} else {
		o.R.DDV = related
	}

	if related.R == nil {
		related.R = &documentoVendaR{
			DDVDocumentoVendaChecklists: DocumentoVendaChecklistSlice{o},
		}
	} else {
		related.R.DDVDocumentoVendaChecklists = append(related.R.DDVDocumentoVendaChecklists, o)
	}

	return nil
}

// DocumentoVendaChecklists retrieves all the records using an executor.
func DocumentoVendaChecklists(mods ...qm.QueryMod) documentoVendaChecklistQuery {
	mods = append(mods, qm.From("`documento_venda_checklist`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`documento_venda_checklist`.*"})
	}

	return documentoVendaChecklistQuery{q}
}

// FindDocumentoVendaChecklist retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDocumentoVendaChecklist(ctx context.Context, exec boil.ContextExecutor, cKLID int, dDVID int, ckliItem int, selectCols ...string) (*DocumentoVendaChecklist, error) {
	documentoVendaChecklistObj := &DocumentoVendaChecklist{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `documento_venda_checklist` where `ckl_id`=? AND `ddv_id`=? AND `ckli_item`=?", sel,
	)

	q := queries.Raw(query, cKLID, dDVID, ckliItem)

	err := q.Bind(ctx, exec, documentoVendaChecklistObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from documento_venda_checklist")
	}

	if err = documentoVendaChecklistObj.doAfterSelectHooks(ctx, exec); err != nil {
		return documentoVendaChecklistObj, err
	}

	return documentoVendaChecklistObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DocumentoVendaChecklist) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_checklist provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaChecklistColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	documentoVendaChecklistInsertCacheMut.RLock()
	cache, cached := documentoVendaChecklistInsertCache[key]
	documentoVendaChecklistInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			documentoVendaChecklistAllColumns,
			documentoVendaChecklistColumnsWithDefault,
			documentoVendaChecklistColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaChecklistType, documentoVendaChecklistMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(documentoVendaChecklistType, documentoVendaChecklistMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `documento_venda_checklist` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `documento_venda_checklist` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `documento_venda_checklist` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, documentoVendaChecklistPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into documento_venda_checklist")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CKLID,
		o.DDVID,
		o.CkliItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_checklist")
	}

CacheNoHooks:
	if !cached {
		documentoVendaChecklistInsertCacheMut.Lock()
		documentoVendaChecklistInsertCache[key] = cache
		documentoVendaChecklistInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DocumentoVendaChecklist.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DocumentoVendaChecklist) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	documentoVendaChecklistUpdateCacheMut.RLock()
	cache, cached := documentoVendaChecklistUpdateCache[key]
	documentoVendaChecklistUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			documentoVendaChecklistAllColumns,
			documentoVendaChecklistPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update documento_venda_checklist, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `documento_venda_checklist` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, documentoVendaChecklistPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(documentoVendaChecklistType, documentoVendaChecklistMapping, append(wl, documentoVendaChecklistPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update documento_venda_checklist row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for documento_venda_checklist")
	}

	if !cached {
		documentoVendaChecklistUpdateCacheMut.Lock()
		documentoVendaChecklistUpdateCache[key] = cache
		documentoVendaChecklistUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q documentoVendaChecklistQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for documento_venda_checklist")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for documento_venda_checklist")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DocumentoVendaChecklistSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaChecklistPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `documento_venda_checklist` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaChecklistPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in documentoVendaChecklist slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all documentoVendaChecklist")
	}
	return rowsAff, nil
}

var mySQLDocumentoVendaChecklistUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DocumentoVendaChecklist) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_checklist provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaChecklistColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDocumentoVendaChecklistUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	documentoVendaChecklistUpsertCacheMut.RLock()
	cache, cached := documentoVendaChecklistUpsertCache[key]
	documentoVendaChecklistUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			documentoVendaChecklistAllColumns,
			documentoVendaChecklistColumnsWithDefault,
			documentoVendaChecklistColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			documentoVendaChecklistAllColumns,
			documentoVendaChecklistPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert documento_venda_checklist, could not build update column list")
		}

		ret := strmangle.SetComplement(documentoVendaChecklistAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`documento_venda_checklist`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `documento_venda_checklist` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaChecklistType, documentoVendaChecklistMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(documentoVendaChecklistType, documentoVendaChecklistMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for documento_venda_checklist")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(documentoVendaChecklistType, documentoVendaChecklistMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for documento_venda_checklist")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_checklist")
	}

CacheNoHooks:
	if !cached {
		documentoVendaChecklistUpsertCacheMut.Lock()
		documentoVendaChecklistUpsertCache[key] = cache
		documentoVendaChecklistUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DocumentoVendaChecklist record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DocumentoVendaChecklist) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DocumentoVendaChecklist provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), documentoVendaChecklistPrimaryKeyMapping)
	sql := "DELETE FROM `documento_venda_checklist` WHERE `ckl_id`=? AND `ddv_id`=? AND `ckli_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from documento_venda_checklist")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for documento_venda_checklist")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q documentoVendaChecklistQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no documentoVendaChecklistQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documento_venda_checklist")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_checklist")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DocumentoVendaChecklistSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(documentoVendaChecklistBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaChecklistPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `documento_venda_checklist` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaChecklistPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documentoVendaChecklist slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_checklist")
	}

	if len(documentoVendaChecklistAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DocumentoVendaChecklist) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDocumentoVendaChecklist(ctx, exec, o.CKLID, o.DDVID, o.CkliItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DocumentoVendaChecklistSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DocumentoVendaChecklistSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaChecklistPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `documento_venda_checklist`.* FROM `documento_venda_checklist` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaChecklistPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DocumentoVendaChecklistSlice")
	}

	*o = slice

	return nil
}

// DocumentoVendaChecklistExists checks if the DocumentoVendaChecklist row exists.
func DocumentoVendaChecklistExists(ctx context.Context, exec boil.ContextExecutor, cKLID int, dDVID int, ckliItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `documento_venda_checklist` where `ckl_id`=? AND `ddv_id`=? AND `ckli_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cKLID, dDVID, ckliItem)
	}
	row := exec.QueryRowContext(ctx, sql, cKLID, dDVID, ckliItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if documento_venda_checklist exists")
	}

	return exists, nil
}

// Exists checks if the DocumentoVendaChecklist row exists.
func (o *DocumentoVendaChecklist) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DocumentoVendaChecklistExists(ctx, exec, o.CKLID, o.DDVID, o.CkliItem)
}
