// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ProdutoImagem is an object representing the database table.
type ProdutoImagem struct {
	ProID           int         `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	ProimgItem      int         `boil:"proimg_item" json:"proimg_item" toml:"proimg_item" yaml:"proimg_item"`
	ProimgDescricao null.String `boil:"proimg_descricao" json:"proimg_descricao,omitempty" toml:"proimg_descricao" yaml:"proimg_descricao,omitempty"`
	ProimgImagem    []byte      `boil:"proimg_imagem" json:"proimg_imagem" toml:"proimg_imagem" yaml:"proimg_imagem"`

	R *produtoImagemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L produtoImagemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ProdutoImagemColumns = struct {
	ProID           string
	ProimgItem      string
	ProimgDescricao string
	ProimgImagem    string
}{
	ProID:           "pro_id",
	ProimgItem:      "proimg_item",
	ProimgDescricao: "proimg_descricao",
	ProimgImagem:    "proimg_imagem",
}

var ProdutoImagemTableColumns = struct {
	ProID           string
	ProimgItem      string
	ProimgDescricao string
	ProimgImagem    string
}{
	ProID:           "produto_imagem.pro_id",
	ProimgItem:      "produto_imagem.proimg_item",
	ProimgDescricao: "produto_imagem.proimg_descricao",
	ProimgImagem:    "produto_imagem.proimg_imagem",
}

// Generated where

var ProdutoImagemWhere = struct {
	ProID           whereHelperint
	ProimgItem      whereHelperint
	ProimgDescricao whereHelpernull_String
	ProimgImagem    whereHelper__byte
}{
	ProID:           whereHelperint{field: "`produto_imagem`.`pro_id`"},
	ProimgItem:      whereHelperint{field: "`produto_imagem`.`proimg_item`"},
	ProimgDescricao: whereHelpernull_String{field: "`produto_imagem`.`proimg_descricao`"},
	ProimgImagem:    whereHelper__byte{field: "`produto_imagem`.`proimg_imagem`"},
}

// ProdutoImagemRels is where relationship names are stored.
var ProdutoImagemRels = struct {
	Pro string
}{
	Pro: "Pro",
}

// produtoImagemR is where relationships are stored.
type produtoImagemR struct {
	Pro *Produto `boil:"Pro" json:"Pro" toml:"Pro" yaml:"Pro"`
}

// NewStruct creates a new relationship struct
func (*produtoImagemR) NewStruct() *produtoImagemR {
	return &produtoImagemR{}
}

func (r *produtoImagemR) GetPro() *Produto {
	if r == nil {
		return nil
	}
	return r.Pro
}

// produtoImagemL is where Load methods for each relationship are stored.
type produtoImagemL struct{}

var (
	produtoImagemAllColumns            = []string{"pro_id", "proimg_item", "proimg_descricao", "proimg_imagem"}
	produtoImagemColumnsWithoutDefault = []string{"pro_id", "proimg_item", "proimg_descricao", "proimg_imagem"}
	produtoImagemColumnsWithDefault    = []string{}
	produtoImagemPrimaryKeyColumns     = []string{"pro_id", "proimg_item"}
	produtoImagemGeneratedColumns      = []string{}
)

type (
	// ProdutoImagemSlice is an alias for a slice of pointers to ProdutoImagem.
	// This should almost always be used instead of []ProdutoImagem.
	ProdutoImagemSlice []*ProdutoImagem
	// ProdutoImagemHook is the signature for custom ProdutoImagem hook methods
	ProdutoImagemHook func(context.Context, boil.ContextExecutor, *ProdutoImagem) error

	produtoImagemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	produtoImagemType                 = reflect.TypeOf(&ProdutoImagem{})
	produtoImagemMapping              = queries.MakeStructMapping(produtoImagemType)
	produtoImagemPrimaryKeyMapping, _ = queries.BindMapping(produtoImagemType, produtoImagemMapping, produtoImagemPrimaryKeyColumns)
	produtoImagemInsertCacheMut       sync.RWMutex
	produtoImagemInsertCache          = make(map[string]insertCache)
	produtoImagemUpdateCacheMut       sync.RWMutex
	produtoImagemUpdateCache          = make(map[string]updateCache)
	produtoImagemUpsertCacheMut       sync.RWMutex
	produtoImagemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var produtoImagemAfterSelectMu sync.Mutex
var produtoImagemAfterSelectHooks []ProdutoImagemHook

var produtoImagemBeforeInsertMu sync.Mutex
var produtoImagemBeforeInsertHooks []ProdutoImagemHook
var produtoImagemAfterInsertMu sync.Mutex
var produtoImagemAfterInsertHooks []ProdutoImagemHook

var produtoImagemBeforeUpdateMu sync.Mutex
var produtoImagemBeforeUpdateHooks []ProdutoImagemHook
var produtoImagemAfterUpdateMu sync.Mutex
var produtoImagemAfterUpdateHooks []ProdutoImagemHook

var produtoImagemBeforeDeleteMu sync.Mutex
var produtoImagemBeforeDeleteHooks []ProdutoImagemHook
var produtoImagemAfterDeleteMu sync.Mutex
var produtoImagemAfterDeleteHooks []ProdutoImagemHook

var produtoImagemBeforeUpsertMu sync.Mutex
var produtoImagemBeforeUpsertHooks []ProdutoImagemHook
var produtoImagemAfterUpsertMu sync.Mutex
var produtoImagemAfterUpsertHooks []ProdutoImagemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ProdutoImagem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ProdutoImagem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ProdutoImagem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ProdutoImagem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ProdutoImagem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ProdutoImagem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ProdutoImagem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ProdutoImagem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ProdutoImagem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoImagemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddProdutoImagemHook registers your hook function for all future operations.
func AddProdutoImagemHook(hookPoint boil.HookPoint, produtoImagemHook ProdutoImagemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		produtoImagemAfterSelectMu.Lock()
		produtoImagemAfterSelectHooks = append(produtoImagemAfterSelectHooks, produtoImagemHook)
		produtoImagemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		produtoImagemBeforeInsertMu.Lock()
		produtoImagemBeforeInsertHooks = append(produtoImagemBeforeInsertHooks, produtoImagemHook)
		produtoImagemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		produtoImagemAfterInsertMu.Lock()
		produtoImagemAfterInsertHooks = append(produtoImagemAfterInsertHooks, produtoImagemHook)
		produtoImagemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		produtoImagemBeforeUpdateMu.Lock()
		produtoImagemBeforeUpdateHooks = append(produtoImagemBeforeUpdateHooks, produtoImagemHook)
		produtoImagemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		produtoImagemAfterUpdateMu.Lock()
		produtoImagemAfterUpdateHooks = append(produtoImagemAfterUpdateHooks, produtoImagemHook)
		produtoImagemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		produtoImagemBeforeDeleteMu.Lock()
		produtoImagemBeforeDeleteHooks = append(produtoImagemBeforeDeleteHooks, produtoImagemHook)
		produtoImagemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		produtoImagemAfterDeleteMu.Lock()
		produtoImagemAfterDeleteHooks = append(produtoImagemAfterDeleteHooks, produtoImagemHook)
		produtoImagemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		produtoImagemBeforeUpsertMu.Lock()
		produtoImagemBeforeUpsertHooks = append(produtoImagemBeforeUpsertHooks, produtoImagemHook)
		produtoImagemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		produtoImagemAfterUpsertMu.Lock()
		produtoImagemAfterUpsertHooks = append(produtoImagemAfterUpsertHooks, produtoImagemHook)
		produtoImagemAfterUpsertMu.Unlock()
	}
}

// One returns a single produtoImagem record from the query.
func (q produtoImagemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ProdutoImagem, error) {
	o := &ProdutoImagem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for produto_imagem")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ProdutoImagem records from the query.
func (q produtoImagemQuery) All(ctx context.Context, exec boil.ContextExecutor) (ProdutoImagemSlice, error) {
	var o []*ProdutoImagem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ProdutoImagem slice")
	}

	if len(produtoImagemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ProdutoImagem records in the query.
func (q produtoImagemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count produto_imagem rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q produtoImagemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if produto_imagem exists")
	}

	return count > 0, nil
}

// Pro pointed to by the foreign key.
func (o *ProdutoImagem) Pro(mods ...qm.QueryMod) produtoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pro_id` = ?", o.ProID),
	}

	queryMods = append(queryMods, mods...)

	return Produtos(queryMods...)
}

// LoadPro allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (produtoImagemL) LoadPro(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProdutoImagem interface{}, mods queries.Applicator) error {
	var slice []*ProdutoImagem
	var object *ProdutoImagem

	if singular {
		var ok bool
		object, ok = maybeProdutoImagem.(*ProdutoImagem)
		if !ok {
			object = new(ProdutoImagem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeProdutoImagem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeProdutoImagem))
			}
		}
	} else {
		s, ok := maybeProdutoImagem.(*[]*ProdutoImagem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeProdutoImagem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeProdutoImagem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &produtoImagemR{}
		}
		args[object.ProID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &produtoImagemR{}
			}

			args[obj.ProID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.pro_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pro = foreign
		if foreign.R == nil {
			foreign.R = &produtoR{}
		}
		foreign.R.ProProdutoImagems = append(foreign.R.ProProdutoImagems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProID == foreign.ProID {
				local.R.Pro = foreign
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.ProProdutoImagems = append(foreign.R.ProProdutoImagems, local)
				break
			}
		}
	}

	return nil
}

// SetPro of the produtoImagem to the related item.
// Sets o.R.Pro to related.
// Adds o to related.R.ProProdutoImagems.
func (o *ProdutoImagem) SetPro(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Produto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `produto_imagem` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pro_id"}),
		strmangle.WhereClause("`", "`", 0, produtoImagemPrimaryKeyColumns),
	)
	values := []interface{}{related.ProID, o.ProID, o.ProimgItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProID = related.ProID
	if o.R == nil {
		o.R = &produtoImagemR{
			Pro: related,
		}
	} else {
		o.R.Pro = related
	}

	if related.R == nil {
		related.R = &produtoR{
			ProProdutoImagems: ProdutoImagemSlice{o},
		}
	} else {
		related.R.ProProdutoImagems = append(related.R.ProProdutoImagems, o)
	}

	return nil
}

// ProdutoImagems retrieves all the records using an executor.
func ProdutoImagems(mods ...qm.QueryMod) produtoImagemQuery {
	mods = append(mods, qm.From("`produto_imagem`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`produto_imagem`.*"})
	}

	return produtoImagemQuery{q}
}

// FindProdutoImagem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindProdutoImagem(ctx context.Context, exec boil.ContextExecutor, proID int, proimgItem int, selectCols ...string) (*ProdutoImagem, error) {
	produtoImagemObj := &ProdutoImagem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `produto_imagem` where `pro_id`=? AND `proimg_item`=?", sel,
	)

	q := queries.Raw(query, proID, proimgItem)

	err := q.Bind(ctx, exec, produtoImagemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from produto_imagem")
	}

	if err = produtoImagemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return produtoImagemObj, err
	}

	return produtoImagemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ProdutoImagem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no produto_imagem provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(produtoImagemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	produtoImagemInsertCacheMut.RLock()
	cache, cached := produtoImagemInsertCache[key]
	produtoImagemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			produtoImagemAllColumns,
			produtoImagemColumnsWithDefault,
			produtoImagemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(produtoImagemType, produtoImagemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(produtoImagemType, produtoImagemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `produto_imagem` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `produto_imagem` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `produto_imagem` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, produtoImagemPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into produto_imagem")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ProID,
		o.ProimgItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for produto_imagem")
	}

CacheNoHooks:
	if !cached {
		produtoImagemInsertCacheMut.Lock()
		produtoImagemInsertCache[key] = cache
		produtoImagemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ProdutoImagem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ProdutoImagem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	produtoImagemUpdateCacheMut.RLock()
	cache, cached := produtoImagemUpdateCache[key]
	produtoImagemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			produtoImagemAllColumns,
			produtoImagemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update produto_imagem, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `produto_imagem` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, produtoImagemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(produtoImagemType, produtoImagemMapping, append(wl, produtoImagemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update produto_imagem row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for produto_imagem")
	}

	if !cached {
		produtoImagemUpdateCacheMut.Lock()
		produtoImagemUpdateCache[key] = cache
		produtoImagemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q produtoImagemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for produto_imagem")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for produto_imagem")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ProdutoImagemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), produtoImagemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `produto_imagem` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, produtoImagemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in produtoImagem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all produtoImagem")
	}
	return rowsAff, nil
}

var mySQLProdutoImagemUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ProdutoImagem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no produto_imagem provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(produtoImagemColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLProdutoImagemUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	produtoImagemUpsertCacheMut.RLock()
	cache, cached := produtoImagemUpsertCache[key]
	produtoImagemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			produtoImagemAllColumns,
			produtoImagemColumnsWithDefault,
			produtoImagemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			produtoImagemAllColumns,
			produtoImagemPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert produto_imagem, could not build update column list")
		}

		ret := strmangle.SetComplement(produtoImagemAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`produto_imagem`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `produto_imagem` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(produtoImagemType, produtoImagemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(produtoImagemType, produtoImagemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for produto_imagem")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(produtoImagemType, produtoImagemMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for produto_imagem")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for produto_imagem")
	}

CacheNoHooks:
	if !cached {
		produtoImagemUpsertCacheMut.Lock()
		produtoImagemUpsertCache[key] = cache
		produtoImagemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ProdutoImagem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ProdutoImagem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ProdutoImagem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), produtoImagemPrimaryKeyMapping)
	sql := "DELETE FROM `produto_imagem` WHERE `pro_id`=? AND `proimg_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from produto_imagem")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for produto_imagem")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q produtoImagemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no produtoImagemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from produto_imagem")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for produto_imagem")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ProdutoImagemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(produtoImagemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), produtoImagemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `produto_imagem` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, produtoImagemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from produtoImagem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for produto_imagem")
	}

	if len(produtoImagemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ProdutoImagem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindProdutoImagem(ctx, exec, o.ProID, o.ProimgItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ProdutoImagemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ProdutoImagemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), produtoImagemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `produto_imagem`.* FROM `produto_imagem` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, produtoImagemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ProdutoImagemSlice")
	}

	*o = slice

	return nil
}

// ProdutoImagemExists checks if the ProdutoImagem row exists.
func ProdutoImagemExists(ctx context.Context, exec boil.ContextExecutor, proID int, proimgItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `produto_imagem` where `pro_id`=? AND `proimg_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, proID, proimgItem)
	}
	row := exec.QueryRowContext(ctx, sql, proID, proimgItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if produto_imagem exists")
	}

	return exists, nil
}

// Exists checks if the ProdutoImagem row exists.
func (o *ProdutoImagem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ProdutoImagemExists(ctx, exec, o.ProID, o.ProimgItem)
}
