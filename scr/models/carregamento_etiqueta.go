// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CarregamentoEtiquetum is an object representing the database table.
type CarregamentoEtiquetum struct {
	CaretqID            int64             `boil:"caretq_id" json:"caretq_id" toml:"caretq_id" yaml:"caretq_id"`
	DDVID               int               `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	CarID               int               `boil:"car_id" json:"car_id" toml:"car_id" yaml:"car_id"`
	ProID               int               `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	CaretqDescricao     null.String       `boil:"caretq_descricao" json:"caretq_descricao,omitempty" toml:"caretq_descricao" yaml:"caretq_descricao,omitempty"`
	CaretqNumerodacaixa null.Int          `boil:"caretq_numerodacaixa" json:"caretq_numerodacaixa,omitempty" toml:"caretq_numerodacaixa" yaml:"caretq_numerodacaixa,omitempty"`
	CaretqQuantdecaixas null.Int          `boil:"caretq_quantdecaixas" json:"caretq_quantdecaixas,omitempty" toml:"caretq_quantdecaixas" yaml:"caretq_quantdecaixas,omitempty"`
	CaretqPeso          types.NullDecimal `boil:"caretq_peso" json:"caretq_peso,omitempty" toml:"caretq_peso" yaml:"caretq_peso,omitempty"`
	CaretqCubagem       types.NullDecimal `boil:"caretq_cubagem" json:"caretq_cubagem,omitempty" toml:"caretq_cubagem" yaml:"caretq_cubagem,omitempty"`

	R *carregamentoEtiquetumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L carregamentoEtiquetumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CarregamentoEtiquetumColumns = struct {
	CaretqID            string
	DDVID               string
	CarID               string
	ProID               string
	CaretqDescricao     string
	CaretqNumerodacaixa string
	CaretqQuantdecaixas string
	CaretqPeso          string
	CaretqCubagem       string
}{
	CaretqID:            "caretq_id",
	DDVID:               "ddv_id",
	CarID:               "car_id",
	ProID:               "pro_id",
	CaretqDescricao:     "caretq_descricao",
	CaretqNumerodacaixa: "caretq_numerodacaixa",
	CaretqQuantdecaixas: "caretq_quantdecaixas",
	CaretqPeso:          "caretq_peso",
	CaretqCubagem:       "caretq_cubagem",
}

var CarregamentoEtiquetumTableColumns = struct {
	CaretqID            string
	DDVID               string
	CarID               string
	ProID               string
	CaretqDescricao     string
	CaretqNumerodacaixa string
	CaretqQuantdecaixas string
	CaretqPeso          string
	CaretqCubagem       string
}{
	CaretqID:            "carregamento_etiqueta.caretq_id",
	DDVID:               "carregamento_etiqueta.ddv_id",
	CarID:               "carregamento_etiqueta.car_id",
	ProID:               "carregamento_etiqueta.pro_id",
	CaretqDescricao:     "carregamento_etiqueta.caretq_descricao",
	CaretqNumerodacaixa: "carregamento_etiqueta.caretq_numerodacaixa",
	CaretqQuantdecaixas: "carregamento_etiqueta.caretq_quantdecaixas",
	CaretqPeso:          "carregamento_etiqueta.caretq_peso",
	CaretqCubagem:       "carregamento_etiqueta.caretq_cubagem",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var CarregamentoEtiquetumWhere = struct {
	CaretqID            whereHelperint64
	DDVID               whereHelperint
	CarID               whereHelperint
	ProID               whereHelperint
	CaretqDescricao     whereHelpernull_String
	CaretqNumerodacaixa whereHelpernull_Int
	CaretqQuantdecaixas whereHelpernull_Int
	CaretqPeso          whereHelpertypes_NullDecimal
	CaretqCubagem       whereHelpertypes_NullDecimal
}{
	CaretqID:            whereHelperint64{field: "`carregamento_etiqueta`.`caretq_id`"},
	DDVID:               whereHelperint{field: "`carregamento_etiqueta`.`ddv_id`"},
	CarID:               whereHelperint{field: "`carregamento_etiqueta`.`car_id`"},
	ProID:               whereHelperint{field: "`carregamento_etiqueta`.`pro_id`"},
	CaretqDescricao:     whereHelpernull_String{field: "`carregamento_etiqueta`.`caretq_descricao`"},
	CaretqNumerodacaixa: whereHelpernull_Int{field: "`carregamento_etiqueta`.`caretq_numerodacaixa`"},
	CaretqQuantdecaixas: whereHelpernull_Int{field: "`carregamento_etiqueta`.`caretq_quantdecaixas`"},
	CaretqPeso:          whereHelpertypes_NullDecimal{field: "`carregamento_etiqueta`.`caretq_peso`"},
	CaretqCubagem:       whereHelpertypes_NullDecimal{field: "`carregamento_etiqueta`.`caretq_cubagem`"},
}

// CarregamentoEtiquetumRels is where relationship names are stored.
var CarregamentoEtiquetumRels = struct {
	CaretqCarregamentoEtiquetaItems string
}{
	CaretqCarregamentoEtiquetaItems: "CaretqCarregamentoEtiquetaItems",
}

// carregamentoEtiquetumR is where relationships are stored.
type carregamentoEtiquetumR struct {
	CaretqCarregamentoEtiquetaItems CarregamentoEtiquetaItemSlice `boil:"CaretqCarregamentoEtiquetaItems" json:"CaretqCarregamentoEtiquetaItems" toml:"CaretqCarregamentoEtiquetaItems" yaml:"CaretqCarregamentoEtiquetaItems"`
}

// NewStruct creates a new relationship struct
func (*carregamentoEtiquetumR) NewStruct() *carregamentoEtiquetumR {
	return &carregamentoEtiquetumR{}
}

func (r *carregamentoEtiquetumR) GetCaretqCarregamentoEtiquetaItems() CarregamentoEtiquetaItemSlice {
	if r == nil {
		return nil
	}
	return r.CaretqCarregamentoEtiquetaItems
}

// carregamentoEtiquetumL is where Load methods for each relationship are stored.
type carregamentoEtiquetumL struct{}

var (
	carregamentoEtiquetumAllColumns            = []string{"caretq_id", "ddv_id", "car_id", "pro_id", "caretq_descricao", "caretq_numerodacaixa", "caretq_quantdecaixas", "caretq_peso", "caretq_cubagem"}
	carregamentoEtiquetumColumnsWithoutDefault = []string{"caretq_id", "ddv_id", "car_id", "pro_id", "caretq_descricao", "caretq_numerodacaixa", "caretq_quantdecaixas", "caretq_peso", "caretq_cubagem"}
	carregamentoEtiquetumColumnsWithDefault    = []string{}
	carregamentoEtiquetumPrimaryKeyColumns     = []string{"caretq_id"}
	carregamentoEtiquetumGeneratedColumns      = []string{}
)

type (
	// CarregamentoEtiquetumSlice is an alias for a slice of pointers to CarregamentoEtiquetum.
	// This should almost always be used instead of []CarregamentoEtiquetum.
	CarregamentoEtiquetumSlice []*CarregamentoEtiquetum
	// CarregamentoEtiquetumHook is the signature for custom CarregamentoEtiquetum hook methods
	CarregamentoEtiquetumHook func(context.Context, boil.ContextExecutor, *CarregamentoEtiquetum) error

	carregamentoEtiquetumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	carregamentoEtiquetumType                 = reflect.TypeOf(&CarregamentoEtiquetum{})
	carregamentoEtiquetumMapping              = queries.MakeStructMapping(carregamentoEtiquetumType)
	carregamentoEtiquetumPrimaryKeyMapping, _ = queries.BindMapping(carregamentoEtiquetumType, carregamentoEtiquetumMapping, carregamentoEtiquetumPrimaryKeyColumns)
	carregamentoEtiquetumInsertCacheMut       sync.RWMutex
	carregamentoEtiquetumInsertCache          = make(map[string]insertCache)
	carregamentoEtiquetumUpdateCacheMut       sync.RWMutex
	carregamentoEtiquetumUpdateCache          = make(map[string]updateCache)
	carregamentoEtiquetumUpsertCacheMut       sync.RWMutex
	carregamentoEtiquetumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var carregamentoEtiquetumAfterSelectMu sync.Mutex
var carregamentoEtiquetumAfterSelectHooks []CarregamentoEtiquetumHook

var carregamentoEtiquetumBeforeInsertMu sync.Mutex
var carregamentoEtiquetumBeforeInsertHooks []CarregamentoEtiquetumHook
var carregamentoEtiquetumAfterInsertMu sync.Mutex
var carregamentoEtiquetumAfterInsertHooks []CarregamentoEtiquetumHook

var carregamentoEtiquetumBeforeUpdateMu sync.Mutex
var carregamentoEtiquetumBeforeUpdateHooks []CarregamentoEtiquetumHook
var carregamentoEtiquetumAfterUpdateMu sync.Mutex
var carregamentoEtiquetumAfterUpdateHooks []CarregamentoEtiquetumHook

var carregamentoEtiquetumBeforeDeleteMu sync.Mutex
var carregamentoEtiquetumBeforeDeleteHooks []CarregamentoEtiquetumHook
var carregamentoEtiquetumAfterDeleteMu sync.Mutex
var carregamentoEtiquetumAfterDeleteHooks []CarregamentoEtiquetumHook

var carregamentoEtiquetumBeforeUpsertMu sync.Mutex
var carregamentoEtiquetumBeforeUpsertHooks []CarregamentoEtiquetumHook
var carregamentoEtiquetumAfterUpsertMu sync.Mutex
var carregamentoEtiquetumAfterUpsertHooks []CarregamentoEtiquetumHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CarregamentoEtiquetum) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CarregamentoEtiquetum) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CarregamentoEtiquetum) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CarregamentoEtiquetum) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CarregamentoEtiquetum) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CarregamentoEtiquetum) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CarregamentoEtiquetum) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CarregamentoEtiquetum) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CarregamentoEtiquetum) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range carregamentoEtiquetumAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCarregamentoEtiquetumHook registers your hook function for all future operations.
func AddCarregamentoEtiquetumHook(hookPoint boil.HookPoint, carregamentoEtiquetumHook CarregamentoEtiquetumHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		carregamentoEtiquetumAfterSelectMu.Lock()
		carregamentoEtiquetumAfterSelectHooks = append(carregamentoEtiquetumAfterSelectHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		carregamentoEtiquetumBeforeInsertMu.Lock()
		carregamentoEtiquetumBeforeInsertHooks = append(carregamentoEtiquetumBeforeInsertHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		carregamentoEtiquetumAfterInsertMu.Lock()
		carregamentoEtiquetumAfterInsertHooks = append(carregamentoEtiquetumAfterInsertHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		carregamentoEtiquetumBeforeUpdateMu.Lock()
		carregamentoEtiquetumBeforeUpdateHooks = append(carregamentoEtiquetumBeforeUpdateHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		carregamentoEtiquetumAfterUpdateMu.Lock()
		carregamentoEtiquetumAfterUpdateHooks = append(carregamentoEtiquetumAfterUpdateHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		carregamentoEtiquetumBeforeDeleteMu.Lock()
		carregamentoEtiquetumBeforeDeleteHooks = append(carregamentoEtiquetumBeforeDeleteHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		carregamentoEtiquetumAfterDeleteMu.Lock()
		carregamentoEtiquetumAfterDeleteHooks = append(carregamentoEtiquetumAfterDeleteHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		carregamentoEtiquetumBeforeUpsertMu.Lock()
		carregamentoEtiquetumBeforeUpsertHooks = append(carregamentoEtiquetumBeforeUpsertHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		carregamentoEtiquetumAfterUpsertMu.Lock()
		carregamentoEtiquetumAfterUpsertHooks = append(carregamentoEtiquetumAfterUpsertHooks, carregamentoEtiquetumHook)
		carregamentoEtiquetumAfterUpsertMu.Unlock()
	}
}

// One returns a single carregamentoEtiquetum record from the query.
func (q carregamentoEtiquetumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CarregamentoEtiquetum, error) {
	o := &CarregamentoEtiquetum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for carregamento_etiqueta")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CarregamentoEtiquetum records from the query.
func (q carregamentoEtiquetumQuery) All(ctx context.Context, exec boil.ContextExecutor) (CarregamentoEtiquetumSlice, error) {
	var o []*CarregamentoEtiquetum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CarregamentoEtiquetum slice")
	}

	if len(carregamentoEtiquetumAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CarregamentoEtiquetum records in the query.
func (q carregamentoEtiquetumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count carregamento_etiqueta rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q carregamentoEtiquetumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if carregamento_etiqueta exists")
	}

	return count > 0, nil
}

// CaretqCarregamentoEtiquetaItems retrieves all the carregamento_etiqueta_item's CarregamentoEtiquetaItems with an executor via caretq_id column.
func (o *CarregamentoEtiquetum) CaretqCarregamentoEtiquetaItems(mods ...qm.QueryMod) carregamentoEtiquetaItemQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`carregamento_etiqueta_item`.`caretq_id`=?", o.CaretqID),
	)

	return CarregamentoEtiquetaItems(queryMods...)
}

// LoadCaretqCarregamentoEtiquetaItems allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (carregamentoEtiquetumL) LoadCaretqCarregamentoEtiquetaItems(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCarregamentoEtiquetum interface{}, mods queries.Applicator) error {
	var slice []*CarregamentoEtiquetum
	var object *CarregamentoEtiquetum

	if singular {
		var ok bool
		object, ok = maybeCarregamentoEtiquetum.(*CarregamentoEtiquetum)
		if !ok {
			object = new(CarregamentoEtiquetum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCarregamentoEtiquetum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCarregamentoEtiquetum))
			}
		}
	} else {
		s, ok := maybeCarregamentoEtiquetum.(*[]*CarregamentoEtiquetum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCarregamentoEtiquetum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCarregamentoEtiquetum))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &carregamentoEtiquetumR{}
		}
		args[object.CaretqID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &carregamentoEtiquetumR{}
			}
			args[obj.CaretqID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`carregamento_etiqueta_item`),
		qm.WhereIn(`carregamento_etiqueta_item.caretq_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load carregamento_etiqueta_item")
	}

	var resultSlice []*CarregamentoEtiquetaItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice carregamento_etiqueta_item")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on carregamento_etiqueta_item")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for carregamento_etiqueta_item")
	}

	if len(carregamentoEtiquetaItemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CaretqCarregamentoEtiquetaItems = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &carregamentoEtiquetaItemR{}
			}
			foreign.R.Caretq = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.CaretqID == foreign.CaretqID {
				local.R.CaretqCarregamentoEtiquetaItems = append(local.R.CaretqCarregamentoEtiquetaItems, foreign)
				if foreign.R == nil {
					foreign.R = &carregamentoEtiquetaItemR{}
				}
				foreign.R.Caretq = local
				break
			}
		}
	}

	return nil
}

// AddCaretqCarregamentoEtiquetaItems adds the given related objects to the existing relationships
// of the carregamento_etiquetum, optionally inserting them as new records.
// Appends related to o.R.CaretqCarregamentoEtiquetaItems.
// Sets related.R.Caretq appropriately.
func (o *CarregamentoEtiquetum) AddCaretqCarregamentoEtiquetaItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CarregamentoEtiquetaItem) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CaretqID = o.CaretqID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `carregamento_etiqueta_item` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"caretq_id"}),
				strmangle.WhereClause("`", "`", 0, carregamentoEtiquetaItemPrimaryKeyColumns),
			)
			values := []interface{}{o.CaretqID, rel.CaretqID, rel.ProID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CaretqID = o.CaretqID
		}
	}

	if o.R == nil {
		o.R = &carregamentoEtiquetumR{
			CaretqCarregamentoEtiquetaItems: related,
		}
	} else {
		o.R.CaretqCarregamentoEtiquetaItems = append(o.R.CaretqCarregamentoEtiquetaItems, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &carregamentoEtiquetaItemR{
				Caretq: o,
			}
		} else {
			rel.R.Caretq = o
		}
	}
	return nil
}

// CarregamentoEtiqueta retrieves all the records using an executor.
func CarregamentoEtiqueta(mods ...qm.QueryMod) carregamentoEtiquetumQuery {
	mods = append(mods, qm.From("`carregamento_etiqueta`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`carregamento_etiqueta`.*"})
	}

	return carregamentoEtiquetumQuery{q}
}

// FindCarregamentoEtiquetum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCarregamentoEtiquetum(ctx context.Context, exec boil.ContextExecutor, caretqID int64, selectCols ...string) (*CarregamentoEtiquetum, error) {
	carregamentoEtiquetumObj := &CarregamentoEtiquetum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `carregamento_etiqueta` where `caretq_id`=?", sel,
	)

	q := queries.Raw(query, caretqID)

	err := q.Bind(ctx, exec, carregamentoEtiquetumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from carregamento_etiqueta")
	}

	if err = carregamentoEtiquetumObj.doAfterSelectHooks(ctx, exec); err != nil {
		return carregamentoEtiquetumObj, err
	}

	return carregamentoEtiquetumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CarregamentoEtiquetum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no carregamento_etiqueta provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(carregamentoEtiquetumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	carregamentoEtiquetumInsertCacheMut.RLock()
	cache, cached := carregamentoEtiquetumInsertCache[key]
	carregamentoEtiquetumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			carregamentoEtiquetumAllColumns,
			carregamentoEtiquetumColumnsWithDefault,
			carregamentoEtiquetumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(carregamentoEtiquetumType, carregamentoEtiquetumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(carregamentoEtiquetumType, carregamentoEtiquetumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `carregamento_etiqueta` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `carregamento_etiqueta` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `carregamento_etiqueta` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, carregamentoEtiquetumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into carregamento_etiqueta")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CaretqID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for carregamento_etiqueta")
	}

CacheNoHooks:
	if !cached {
		carregamentoEtiquetumInsertCacheMut.Lock()
		carregamentoEtiquetumInsertCache[key] = cache
		carregamentoEtiquetumInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CarregamentoEtiquetum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CarregamentoEtiquetum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	carregamentoEtiquetumUpdateCacheMut.RLock()
	cache, cached := carregamentoEtiquetumUpdateCache[key]
	carregamentoEtiquetumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			carregamentoEtiquetumAllColumns,
			carregamentoEtiquetumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update carregamento_etiqueta, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `carregamento_etiqueta` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, carregamentoEtiquetumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(carregamentoEtiquetumType, carregamentoEtiquetumMapping, append(wl, carregamentoEtiquetumPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update carregamento_etiqueta row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for carregamento_etiqueta")
	}

	if !cached {
		carregamentoEtiquetumUpdateCacheMut.Lock()
		carregamentoEtiquetumUpdateCache[key] = cache
		carregamentoEtiquetumUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q carregamentoEtiquetumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for carregamento_etiqueta")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for carregamento_etiqueta")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CarregamentoEtiquetumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), carregamentoEtiquetumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `carregamento_etiqueta` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, carregamentoEtiquetumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in carregamentoEtiquetum slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all carregamentoEtiquetum")
	}
	return rowsAff, nil
}

var mySQLCarregamentoEtiquetumUniqueColumns = []string{
	"caretq_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CarregamentoEtiquetum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no carregamento_etiqueta provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(carregamentoEtiquetumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCarregamentoEtiquetumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	carregamentoEtiquetumUpsertCacheMut.RLock()
	cache, cached := carregamentoEtiquetumUpsertCache[key]
	carregamentoEtiquetumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			carregamentoEtiquetumAllColumns,
			carregamentoEtiquetumColumnsWithDefault,
			carregamentoEtiquetumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			carregamentoEtiquetumAllColumns,
			carregamentoEtiquetumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert carregamento_etiqueta, could not build update column list")
		}

		ret := strmangle.SetComplement(carregamentoEtiquetumAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`carregamento_etiqueta`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `carregamento_etiqueta` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(carregamentoEtiquetumType, carregamentoEtiquetumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(carregamentoEtiquetumType, carregamentoEtiquetumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for carregamento_etiqueta")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(carregamentoEtiquetumType, carregamentoEtiquetumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for carregamento_etiqueta")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for carregamento_etiqueta")
	}

CacheNoHooks:
	if !cached {
		carregamentoEtiquetumUpsertCacheMut.Lock()
		carregamentoEtiquetumUpsertCache[key] = cache
		carregamentoEtiquetumUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CarregamentoEtiquetum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CarregamentoEtiquetum) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CarregamentoEtiquetum provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), carregamentoEtiquetumPrimaryKeyMapping)
	sql := "DELETE FROM `carregamento_etiqueta` WHERE `caretq_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from carregamento_etiqueta")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for carregamento_etiqueta")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q carregamentoEtiquetumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no carregamentoEtiquetumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from carregamento_etiqueta")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for carregamento_etiqueta")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CarregamentoEtiquetumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(carregamentoEtiquetumBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), carregamentoEtiquetumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `carregamento_etiqueta` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, carregamentoEtiquetumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from carregamentoEtiquetum slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for carregamento_etiqueta")
	}

	if len(carregamentoEtiquetumAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CarregamentoEtiquetum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCarregamentoEtiquetum(ctx, exec, o.CaretqID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CarregamentoEtiquetumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CarregamentoEtiquetumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), carregamentoEtiquetumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `carregamento_etiqueta`.* FROM `carregamento_etiqueta` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, carregamentoEtiquetumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CarregamentoEtiquetumSlice")
	}

	*o = slice

	return nil
}

// CarregamentoEtiquetumExists checks if the CarregamentoEtiquetum row exists.
func CarregamentoEtiquetumExists(ctx context.Context, exec boil.ContextExecutor, caretqID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `carregamento_etiqueta` where `caretq_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, caretqID)
	}
	row := exec.QueryRowContext(ctx, sql, caretqID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if carregamento_etiqueta exists")
	}

	return exists, nil
}

// Exists checks if the CarregamentoEtiquetum row exists.
func (o *CarregamentoEtiquetum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CarregamentoEtiquetumExists(ctx, exec, o.CaretqID)
}
