// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// AnimalEspecie is an object representing the database table.
type AnimalEspecie struct {
	AnieID        int    `boil:"anie_id" json:"anie_id" toml:"anie_id" yaml:"anie_id"`
	AnieDescricao string `boil:"anie_descricao" json:"anie_descricao" toml:"anie_descricao" yaml:"anie_descricao"`

	R *animalEspecieR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L animalEspecieL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AnimalEspecieColumns = struct {
	AnieID        string
	AnieDescricao string
}{
	AnieID:        "anie_id",
	AnieDescricao: "anie_descricao",
}

var AnimalEspecieTableColumns = struct {
	AnieID        string
	AnieDescricao string
}{
	AnieID:        "animal_especie.anie_id",
	AnieDescricao: "animal_especie.anie_descricao",
}

// Generated where

var AnimalEspecieWhere = struct {
	AnieID        whereHelperint
	AnieDescricao whereHelperstring
}{
	AnieID:        whereHelperint{field: "`animal_especie`.`anie_id`"},
	AnieDescricao: whereHelperstring{field: "`animal_especie`.`anie_descricao`"},
}

// AnimalEspecieRels is where relationship names are stored.
var AnimalEspecieRels = struct {
	AnieAnimalRacas     string
	AnieEntidadeAnimals string
}{
	AnieAnimalRacas:     "AnieAnimalRacas",
	AnieEntidadeAnimals: "AnieEntidadeAnimals",
}

// animalEspecieR is where relationships are stored.
type animalEspecieR struct {
	AnieAnimalRacas     AnimalRacaSlice     `boil:"AnieAnimalRacas" json:"AnieAnimalRacas" toml:"AnieAnimalRacas" yaml:"AnieAnimalRacas"`
	AnieEntidadeAnimals EntidadeAnimalSlice `boil:"AnieEntidadeAnimals" json:"AnieEntidadeAnimals" toml:"AnieEntidadeAnimals" yaml:"AnieEntidadeAnimals"`
}

// NewStruct creates a new relationship struct
func (*animalEspecieR) NewStruct() *animalEspecieR {
	return &animalEspecieR{}
}

func (r *animalEspecieR) GetAnieAnimalRacas() AnimalRacaSlice {
	if r == nil {
		return nil
	}
	return r.AnieAnimalRacas
}

func (r *animalEspecieR) GetAnieEntidadeAnimals() EntidadeAnimalSlice {
	if r == nil {
		return nil
	}
	return r.AnieEntidadeAnimals
}

// animalEspecieL is where Load methods for each relationship are stored.
type animalEspecieL struct{}

var (
	animalEspecieAllColumns            = []string{"anie_id", "anie_descricao"}
	animalEspecieColumnsWithoutDefault = []string{"anie_id", "anie_descricao"}
	animalEspecieColumnsWithDefault    = []string{}
	animalEspeciePrimaryKeyColumns     = []string{"anie_id"}
	animalEspecieGeneratedColumns      = []string{}
)

type (
	// AnimalEspecieSlice is an alias for a slice of pointers to AnimalEspecie.
	// This should almost always be used instead of []AnimalEspecie.
	AnimalEspecieSlice []*AnimalEspecie
	// AnimalEspecieHook is the signature for custom AnimalEspecie hook methods
	AnimalEspecieHook func(context.Context, boil.ContextExecutor, *AnimalEspecie) error

	animalEspecieQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	animalEspecieType                 = reflect.TypeOf(&AnimalEspecie{})
	animalEspecieMapping              = queries.MakeStructMapping(animalEspecieType)
	animalEspeciePrimaryKeyMapping, _ = queries.BindMapping(animalEspecieType, animalEspecieMapping, animalEspeciePrimaryKeyColumns)
	animalEspecieInsertCacheMut       sync.RWMutex
	animalEspecieInsertCache          = make(map[string]insertCache)
	animalEspecieUpdateCacheMut       sync.RWMutex
	animalEspecieUpdateCache          = make(map[string]updateCache)
	animalEspecieUpsertCacheMut       sync.RWMutex
	animalEspecieUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var animalEspecieAfterSelectMu sync.Mutex
var animalEspecieAfterSelectHooks []AnimalEspecieHook

var animalEspecieBeforeInsertMu sync.Mutex
var animalEspecieBeforeInsertHooks []AnimalEspecieHook
var animalEspecieAfterInsertMu sync.Mutex
var animalEspecieAfterInsertHooks []AnimalEspecieHook

var animalEspecieBeforeUpdateMu sync.Mutex
var animalEspecieBeforeUpdateHooks []AnimalEspecieHook
var animalEspecieAfterUpdateMu sync.Mutex
var animalEspecieAfterUpdateHooks []AnimalEspecieHook

var animalEspecieBeforeDeleteMu sync.Mutex
var animalEspecieBeforeDeleteHooks []AnimalEspecieHook
var animalEspecieAfterDeleteMu sync.Mutex
var animalEspecieAfterDeleteHooks []AnimalEspecieHook

var animalEspecieBeforeUpsertMu sync.Mutex
var animalEspecieBeforeUpsertHooks []AnimalEspecieHook
var animalEspecieAfterUpsertMu sync.Mutex
var animalEspecieAfterUpsertHooks []AnimalEspecieHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AnimalEspecie) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AnimalEspecie) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AnimalEspecie) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AnimalEspecie) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AnimalEspecie) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AnimalEspecie) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AnimalEspecie) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AnimalEspecie) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AnimalEspecie) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range animalEspecieAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAnimalEspecieHook registers your hook function for all future operations.
func AddAnimalEspecieHook(hookPoint boil.HookPoint, animalEspecieHook AnimalEspecieHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		animalEspecieAfterSelectMu.Lock()
		animalEspecieAfterSelectHooks = append(animalEspecieAfterSelectHooks, animalEspecieHook)
		animalEspecieAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		animalEspecieBeforeInsertMu.Lock()
		animalEspecieBeforeInsertHooks = append(animalEspecieBeforeInsertHooks, animalEspecieHook)
		animalEspecieBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		animalEspecieAfterInsertMu.Lock()
		animalEspecieAfterInsertHooks = append(animalEspecieAfterInsertHooks, animalEspecieHook)
		animalEspecieAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		animalEspecieBeforeUpdateMu.Lock()
		animalEspecieBeforeUpdateHooks = append(animalEspecieBeforeUpdateHooks, animalEspecieHook)
		animalEspecieBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		animalEspecieAfterUpdateMu.Lock()
		animalEspecieAfterUpdateHooks = append(animalEspecieAfterUpdateHooks, animalEspecieHook)
		animalEspecieAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		animalEspecieBeforeDeleteMu.Lock()
		animalEspecieBeforeDeleteHooks = append(animalEspecieBeforeDeleteHooks, animalEspecieHook)
		animalEspecieBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		animalEspecieAfterDeleteMu.Lock()
		animalEspecieAfterDeleteHooks = append(animalEspecieAfterDeleteHooks, animalEspecieHook)
		animalEspecieAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		animalEspecieBeforeUpsertMu.Lock()
		animalEspecieBeforeUpsertHooks = append(animalEspecieBeforeUpsertHooks, animalEspecieHook)
		animalEspecieBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		animalEspecieAfterUpsertMu.Lock()
		animalEspecieAfterUpsertHooks = append(animalEspecieAfterUpsertHooks, animalEspecieHook)
		animalEspecieAfterUpsertMu.Unlock()
	}
}

// One returns a single animalEspecie record from the query.
func (q animalEspecieQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AnimalEspecie, error) {
	o := &AnimalEspecie{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for animal_especie")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AnimalEspecie records from the query.
func (q animalEspecieQuery) All(ctx context.Context, exec boil.ContextExecutor) (AnimalEspecieSlice, error) {
	var o []*AnimalEspecie

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AnimalEspecie slice")
	}

	if len(animalEspecieAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AnimalEspecie records in the query.
func (q animalEspecieQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count animal_especie rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q animalEspecieQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if animal_especie exists")
	}

	return count > 0, nil
}

// AnieAnimalRacas retrieves all the animal_raca's AnimalRacas with an executor via anie_id column.
func (o *AnimalEspecie) AnieAnimalRacas(mods ...qm.QueryMod) animalRacaQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`animal_raca`.`anie_id`=?", o.AnieID),
	)

	return AnimalRacas(queryMods...)
}

// AnieEntidadeAnimals retrieves all the entidade_animal's EntidadeAnimals with an executor via anie_id column.
func (o *AnimalEspecie) AnieEntidadeAnimals(mods ...qm.QueryMod) entidadeAnimalQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`entidade_animal`.`anie_id`=?", o.AnieID),
	)

	return EntidadeAnimals(queryMods...)
}

// LoadAnieAnimalRacas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (animalEspecieL) LoadAnieAnimalRacas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAnimalEspecie interface{}, mods queries.Applicator) error {
	var slice []*AnimalEspecie
	var object *AnimalEspecie

	if singular {
		var ok bool
		object, ok = maybeAnimalEspecie.(*AnimalEspecie)
		if !ok {
			object = new(AnimalEspecie)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAnimalEspecie)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAnimalEspecie))
			}
		}
	} else {
		s, ok := maybeAnimalEspecie.(*[]*AnimalEspecie)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAnimalEspecie)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAnimalEspecie))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &animalEspecieR{}
		}
		args[object.AnieID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &animalEspecieR{}
			}
			args[obj.AnieID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`animal_raca`),
		qm.WhereIn(`animal_raca.anie_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load animal_raca")
	}

	var resultSlice []*AnimalRaca
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice animal_raca")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on animal_raca")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for animal_raca")
	}

	if len(animalRacaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AnieAnimalRacas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &animalRacaR{}
			}
			foreign.R.Anie = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.AnieID == foreign.AnieID {
				local.R.AnieAnimalRacas = append(local.R.AnieAnimalRacas, foreign)
				if foreign.R == nil {
					foreign.R = &animalRacaR{}
				}
				foreign.R.Anie = local
				break
			}
		}
	}

	return nil
}

// LoadAnieEntidadeAnimals allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (animalEspecieL) LoadAnieEntidadeAnimals(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAnimalEspecie interface{}, mods queries.Applicator) error {
	var slice []*AnimalEspecie
	var object *AnimalEspecie

	if singular {
		var ok bool
		object, ok = maybeAnimalEspecie.(*AnimalEspecie)
		if !ok {
			object = new(AnimalEspecie)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAnimalEspecie)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAnimalEspecie))
			}
		}
	} else {
		s, ok := maybeAnimalEspecie.(*[]*AnimalEspecie)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAnimalEspecie)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAnimalEspecie))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &animalEspecieR{}
		}
		args[object.AnieID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &animalEspecieR{}
			}
			args[obj.AnieID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade_animal`),
		qm.WhereIn(`entidade_animal.anie_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load entidade_animal")
	}

	var resultSlice []*EntidadeAnimal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice entidade_animal")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on entidade_animal")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade_animal")
	}

	if len(entidadeAnimalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AnieEntidadeAnimals = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &entidadeAnimalR{}
			}
			foreign.R.Anie = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.AnieID, foreign.AnieID) {
				local.R.AnieEntidadeAnimals = append(local.R.AnieEntidadeAnimals, foreign)
				if foreign.R == nil {
					foreign.R = &entidadeAnimalR{}
				}
				foreign.R.Anie = local
				break
			}
		}
	}

	return nil
}

// AddAnieAnimalRacas adds the given related objects to the existing relationships
// of the animal_especie, optionally inserting them as new records.
// Appends related to o.R.AnieAnimalRacas.
// Sets related.R.Anie appropriately.
func (o *AnimalEspecie) AddAnieAnimalRacas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AnimalRaca) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AnieID = o.AnieID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `animal_raca` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"anie_id"}),
				strmangle.WhereClause("`", "`", 0, animalRacaPrimaryKeyColumns),
			)
			values := []interface{}{o.AnieID, rel.AnirID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AnieID = o.AnieID
		}
	}

	if o.R == nil {
		o.R = &animalEspecieR{
			AnieAnimalRacas: related,
		}
	} else {
		o.R.AnieAnimalRacas = append(o.R.AnieAnimalRacas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &animalRacaR{
				Anie: o,
			}
		} else {
			rel.R.Anie = o
		}
	}
	return nil
}

// AddAnieEntidadeAnimals adds the given related objects to the existing relationships
// of the animal_especie, optionally inserting them as new records.
// Appends related to o.R.AnieEntidadeAnimals.
// Sets related.R.Anie appropriately.
func (o *AnimalEspecie) AddAnieEntidadeAnimals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EntidadeAnimal) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.AnieID, o.AnieID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `entidade_animal` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"anie_id"}),
				strmangle.WhereClause("`", "`", 0, entidadeAnimalPrimaryKeyColumns),
			)
			values := []interface{}{o.AnieID, rel.EntID, rel.EntaItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.AnieID, o.AnieID)
		}
	}

	if o.R == nil {
		o.R = &animalEspecieR{
			AnieEntidadeAnimals: related,
		}
	} else {
		o.R.AnieEntidadeAnimals = append(o.R.AnieEntidadeAnimals, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &entidadeAnimalR{
				Anie: o,
			}
		} else {
			rel.R.Anie = o
		}
	}
	return nil
}

// SetAnieEntidadeAnimals removes all previously related items of the
// animal_especie replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Anie's AnieEntidadeAnimals accordingly.
// Replaces o.R.AnieEntidadeAnimals with related.
// Sets related.R.Anie's AnieEntidadeAnimals accordingly.
func (o *AnimalEspecie) SetAnieEntidadeAnimals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EntidadeAnimal) error {
	query := "update `entidade_animal` set `anie_id` = null where `anie_id` = ?"
	values := []interface{}{o.AnieID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.AnieEntidadeAnimals {
			queries.SetScanner(&rel.AnieID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Anie = nil
		}
		o.R.AnieEntidadeAnimals = nil
	}

	return o.AddAnieEntidadeAnimals(ctx, exec, insert, related...)
}

// RemoveAnieEntidadeAnimals relationships from objects passed in.
// Removes related items from R.AnieEntidadeAnimals (uses pointer comparison, removal does not keep order)
// Sets related.R.Anie.
func (o *AnimalEspecie) RemoveAnieEntidadeAnimals(ctx context.Context, exec boil.ContextExecutor, related ...*EntidadeAnimal) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.AnieID, nil)
		if rel.R != nil {
			rel.R.Anie = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("anie_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.AnieEntidadeAnimals {
			if rel != ri {
				continue
			}

			ln := len(o.R.AnieEntidadeAnimals)
			if ln > 1 && i < ln-1 {
				o.R.AnieEntidadeAnimals[i] = o.R.AnieEntidadeAnimals[ln-1]
			}
			o.R.AnieEntidadeAnimals = o.R.AnieEntidadeAnimals[:ln-1]
			break
		}
	}

	return nil
}

// AnimalEspecies retrieves all the records using an executor.
func AnimalEspecies(mods ...qm.QueryMod) animalEspecieQuery {
	mods = append(mods, qm.From("`animal_especie`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`animal_especie`.*"})
	}

	return animalEspecieQuery{q}
}

// FindAnimalEspecie retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAnimalEspecie(ctx context.Context, exec boil.ContextExecutor, anieID int, selectCols ...string) (*AnimalEspecie, error) {
	animalEspecieObj := &AnimalEspecie{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `animal_especie` where `anie_id`=?", sel,
	)

	q := queries.Raw(query, anieID)

	err := q.Bind(ctx, exec, animalEspecieObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from animal_especie")
	}

	if err = animalEspecieObj.doAfterSelectHooks(ctx, exec); err != nil {
		return animalEspecieObj, err
	}

	return animalEspecieObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AnimalEspecie) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no animal_especie provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(animalEspecieColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	animalEspecieInsertCacheMut.RLock()
	cache, cached := animalEspecieInsertCache[key]
	animalEspecieInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			animalEspecieAllColumns,
			animalEspecieColumnsWithDefault,
			animalEspecieColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(animalEspecieType, animalEspecieMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(animalEspecieType, animalEspecieMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `animal_especie` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `animal_especie` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `animal_especie` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, animalEspeciePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into animal_especie")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.AnieID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for animal_especie")
	}

CacheNoHooks:
	if !cached {
		animalEspecieInsertCacheMut.Lock()
		animalEspecieInsertCache[key] = cache
		animalEspecieInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AnimalEspecie.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AnimalEspecie) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	animalEspecieUpdateCacheMut.RLock()
	cache, cached := animalEspecieUpdateCache[key]
	animalEspecieUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			animalEspecieAllColumns,
			animalEspeciePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update animal_especie, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `animal_especie` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, animalEspeciePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(animalEspecieType, animalEspecieMapping, append(wl, animalEspeciePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update animal_especie row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for animal_especie")
	}

	if !cached {
		animalEspecieUpdateCacheMut.Lock()
		animalEspecieUpdateCache[key] = cache
		animalEspecieUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q animalEspecieQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for animal_especie")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for animal_especie")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AnimalEspecieSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), animalEspeciePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `animal_especie` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, animalEspeciePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in animalEspecie slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all animalEspecie")
	}
	return rowsAff, nil
}

var mySQLAnimalEspecieUniqueColumns = []string{
	"anie_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AnimalEspecie) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no animal_especie provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(animalEspecieColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLAnimalEspecieUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	animalEspecieUpsertCacheMut.RLock()
	cache, cached := animalEspecieUpsertCache[key]
	animalEspecieUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			animalEspecieAllColumns,
			animalEspecieColumnsWithDefault,
			animalEspecieColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			animalEspecieAllColumns,
			animalEspeciePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert animal_especie, could not build update column list")
		}

		ret := strmangle.SetComplement(animalEspecieAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`animal_especie`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `animal_especie` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(animalEspecieType, animalEspecieMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(animalEspecieType, animalEspecieMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for animal_especie")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(animalEspecieType, animalEspecieMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for animal_especie")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for animal_especie")
	}

CacheNoHooks:
	if !cached {
		animalEspecieUpsertCacheMut.Lock()
		animalEspecieUpsertCache[key] = cache
		animalEspecieUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AnimalEspecie record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AnimalEspecie) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AnimalEspecie provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), animalEspeciePrimaryKeyMapping)
	sql := "DELETE FROM `animal_especie` WHERE `anie_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from animal_especie")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for animal_especie")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q animalEspecieQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no animalEspecieQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from animal_especie")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for animal_especie")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AnimalEspecieSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(animalEspecieBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), animalEspeciePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `animal_especie` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, animalEspeciePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from animalEspecie slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for animal_especie")
	}

	if len(animalEspecieAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AnimalEspecie) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAnimalEspecie(ctx, exec, o.AnieID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AnimalEspecieSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AnimalEspecieSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), animalEspeciePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `animal_especie`.* FROM `animal_especie` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, animalEspeciePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AnimalEspecieSlice")
	}

	*o = slice

	return nil
}

// AnimalEspecieExists checks if the AnimalEspecie row exists.
func AnimalEspecieExists(ctx context.Context, exec boil.ContextExecutor, anieID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `animal_especie` where `anie_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, anieID)
	}
	row := exec.QueryRowContext(ctx, sql, anieID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if animal_especie exists")
	}

	return exists, nil
}

// Exists checks if the AnimalEspecie row exists.
func (o *AnimalEspecie) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AnimalEspecieExists(ctx, exec, o.AnieID)
}
