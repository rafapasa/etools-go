// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ConfigAtualizacao is an object representing the database table.
type ConfigAtualizacao struct {
	CatID       int         `boil:"cat_id" json:"cat_id" toml:"cat_id" yaml:"cat_id"`
	CatScript   string      `boil:"cat_script" json:"cat_script" toml:"cat_script" yaml:"cat_script"`
	CatSituacao int         `boil:"cat_situacao" json:"cat_situacao" toml:"cat_situacao" yaml:"cat_situacao"`
	CatMensagem null.String `boil:"cat_mensagem" json:"cat_mensagem,omitempty" toml:"cat_mensagem" yaml:"cat_mensagem,omitempty"`

	R *configAtualizacaoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L configAtualizacaoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ConfigAtualizacaoColumns = struct {
	CatID       string
	CatScript   string
	CatSituacao string
	CatMensagem string
}{
	CatID:       "cat_id",
	CatScript:   "cat_script",
	CatSituacao: "cat_situacao",
	CatMensagem: "cat_mensagem",
}

var ConfigAtualizacaoTableColumns = struct {
	CatID       string
	CatScript   string
	CatSituacao string
	CatMensagem string
}{
	CatID:       "config_atualizacao.cat_id",
	CatScript:   "config_atualizacao.cat_script",
	CatSituacao: "config_atualizacao.cat_situacao",
	CatMensagem: "config_atualizacao.cat_mensagem",
}

// Generated where

var ConfigAtualizacaoWhere = struct {
	CatID       whereHelperint
	CatScript   whereHelperstring
	CatSituacao whereHelperint
	CatMensagem whereHelpernull_String
}{
	CatID:       whereHelperint{field: "`config_atualizacao`.`cat_id`"},
	CatScript:   whereHelperstring{field: "`config_atualizacao`.`cat_script`"},
	CatSituacao: whereHelperint{field: "`config_atualizacao`.`cat_situacao`"},
	CatMensagem: whereHelpernull_String{field: "`config_atualizacao`.`cat_mensagem`"},
}

// ConfigAtualizacaoRels is where relationship names are stored.
var ConfigAtualizacaoRels = struct {
}{}

// configAtualizacaoR is where relationships are stored.
type configAtualizacaoR struct {
}

// NewStruct creates a new relationship struct
func (*configAtualizacaoR) NewStruct() *configAtualizacaoR {
	return &configAtualizacaoR{}
}

// configAtualizacaoL is where Load methods for each relationship are stored.
type configAtualizacaoL struct{}

var (
	configAtualizacaoAllColumns            = []string{"cat_id", "cat_script", "cat_situacao", "cat_mensagem"}
	configAtualizacaoColumnsWithoutDefault = []string{"cat_id", "cat_script", "cat_mensagem"}
	configAtualizacaoColumnsWithDefault    = []string{"cat_situacao"}
	configAtualizacaoPrimaryKeyColumns     = []string{"cat_id"}
	configAtualizacaoGeneratedColumns      = []string{}
)

type (
	// ConfigAtualizacaoSlice is an alias for a slice of pointers to ConfigAtualizacao.
	// This should almost always be used instead of []ConfigAtualizacao.
	ConfigAtualizacaoSlice []*ConfigAtualizacao
	// ConfigAtualizacaoHook is the signature for custom ConfigAtualizacao hook methods
	ConfigAtualizacaoHook func(context.Context, boil.ContextExecutor, *ConfigAtualizacao) error

	configAtualizacaoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	configAtualizacaoType                 = reflect.TypeOf(&ConfigAtualizacao{})
	configAtualizacaoMapping              = queries.MakeStructMapping(configAtualizacaoType)
	configAtualizacaoPrimaryKeyMapping, _ = queries.BindMapping(configAtualizacaoType, configAtualizacaoMapping, configAtualizacaoPrimaryKeyColumns)
	configAtualizacaoInsertCacheMut       sync.RWMutex
	configAtualizacaoInsertCache          = make(map[string]insertCache)
	configAtualizacaoUpdateCacheMut       sync.RWMutex
	configAtualizacaoUpdateCache          = make(map[string]updateCache)
	configAtualizacaoUpsertCacheMut       sync.RWMutex
	configAtualizacaoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var configAtualizacaoAfterSelectMu sync.Mutex
var configAtualizacaoAfterSelectHooks []ConfigAtualizacaoHook

var configAtualizacaoBeforeInsertMu sync.Mutex
var configAtualizacaoBeforeInsertHooks []ConfigAtualizacaoHook
var configAtualizacaoAfterInsertMu sync.Mutex
var configAtualizacaoAfterInsertHooks []ConfigAtualizacaoHook

var configAtualizacaoBeforeUpdateMu sync.Mutex
var configAtualizacaoBeforeUpdateHooks []ConfigAtualizacaoHook
var configAtualizacaoAfterUpdateMu sync.Mutex
var configAtualizacaoAfterUpdateHooks []ConfigAtualizacaoHook

var configAtualizacaoBeforeDeleteMu sync.Mutex
var configAtualizacaoBeforeDeleteHooks []ConfigAtualizacaoHook
var configAtualizacaoAfterDeleteMu sync.Mutex
var configAtualizacaoAfterDeleteHooks []ConfigAtualizacaoHook

var configAtualizacaoBeforeUpsertMu sync.Mutex
var configAtualizacaoBeforeUpsertHooks []ConfigAtualizacaoHook
var configAtualizacaoAfterUpsertMu sync.Mutex
var configAtualizacaoAfterUpsertHooks []ConfigAtualizacaoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ConfigAtualizacao) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ConfigAtualizacao) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ConfigAtualizacao) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ConfigAtualizacao) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ConfigAtualizacao) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ConfigAtualizacao) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ConfigAtualizacao) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ConfigAtualizacao) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ConfigAtualizacao) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range configAtualizacaoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddConfigAtualizacaoHook registers your hook function for all future operations.
func AddConfigAtualizacaoHook(hookPoint boil.HookPoint, configAtualizacaoHook ConfigAtualizacaoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		configAtualizacaoAfterSelectMu.Lock()
		configAtualizacaoAfterSelectHooks = append(configAtualizacaoAfterSelectHooks, configAtualizacaoHook)
		configAtualizacaoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		configAtualizacaoBeforeInsertMu.Lock()
		configAtualizacaoBeforeInsertHooks = append(configAtualizacaoBeforeInsertHooks, configAtualizacaoHook)
		configAtualizacaoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		configAtualizacaoAfterInsertMu.Lock()
		configAtualizacaoAfterInsertHooks = append(configAtualizacaoAfterInsertHooks, configAtualizacaoHook)
		configAtualizacaoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		configAtualizacaoBeforeUpdateMu.Lock()
		configAtualizacaoBeforeUpdateHooks = append(configAtualizacaoBeforeUpdateHooks, configAtualizacaoHook)
		configAtualizacaoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		configAtualizacaoAfterUpdateMu.Lock()
		configAtualizacaoAfterUpdateHooks = append(configAtualizacaoAfterUpdateHooks, configAtualizacaoHook)
		configAtualizacaoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		configAtualizacaoBeforeDeleteMu.Lock()
		configAtualizacaoBeforeDeleteHooks = append(configAtualizacaoBeforeDeleteHooks, configAtualizacaoHook)
		configAtualizacaoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		configAtualizacaoAfterDeleteMu.Lock()
		configAtualizacaoAfterDeleteHooks = append(configAtualizacaoAfterDeleteHooks, configAtualizacaoHook)
		configAtualizacaoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		configAtualizacaoBeforeUpsertMu.Lock()
		configAtualizacaoBeforeUpsertHooks = append(configAtualizacaoBeforeUpsertHooks, configAtualizacaoHook)
		configAtualizacaoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		configAtualizacaoAfterUpsertMu.Lock()
		configAtualizacaoAfterUpsertHooks = append(configAtualizacaoAfterUpsertHooks, configAtualizacaoHook)
		configAtualizacaoAfterUpsertMu.Unlock()
	}
}

// One returns a single configAtualizacao record from the query.
func (q configAtualizacaoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ConfigAtualizacao, error) {
	o := &ConfigAtualizacao{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for config_atualizacao")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ConfigAtualizacao records from the query.
func (q configAtualizacaoQuery) All(ctx context.Context, exec boil.ContextExecutor) (ConfigAtualizacaoSlice, error) {
	var o []*ConfigAtualizacao

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ConfigAtualizacao slice")
	}

	if len(configAtualizacaoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ConfigAtualizacao records in the query.
func (q configAtualizacaoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count config_atualizacao rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q configAtualizacaoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if config_atualizacao exists")
	}

	return count > 0, nil
}

// ConfigAtualizacaos retrieves all the records using an executor.
func ConfigAtualizacaos(mods ...qm.QueryMod) configAtualizacaoQuery {
	mods = append(mods, qm.From("`config_atualizacao`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`config_atualizacao`.*"})
	}

	return configAtualizacaoQuery{q}
}

// FindConfigAtualizacao retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindConfigAtualizacao(ctx context.Context, exec boil.ContextExecutor, catID int, selectCols ...string) (*ConfigAtualizacao, error) {
	configAtualizacaoObj := &ConfigAtualizacao{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `config_atualizacao` where `cat_id`=?", sel,
	)

	q := queries.Raw(query, catID)

	err := q.Bind(ctx, exec, configAtualizacaoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from config_atualizacao")
	}

	if err = configAtualizacaoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return configAtualizacaoObj, err
	}

	return configAtualizacaoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ConfigAtualizacao) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no config_atualizacao provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(configAtualizacaoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	configAtualizacaoInsertCacheMut.RLock()
	cache, cached := configAtualizacaoInsertCache[key]
	configAtualizacaoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			configAtualizacaoAllColumns,
			configAtualizacaoColumnsWithDefault,
			configAtualizacaoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(configAtualizacaoType, configAtualizacaoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(configAtualizacaoType, configAtualizacaoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `config_atualizacao` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `config_atualizacao` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `config_atualizacao` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, configAtualizacaoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into config_atualizacao")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CatID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for config_atualizacao")
	}

CacheNoHooks:
	if !cached {
		configAtualizacaoInsertCacheMut.Lock()
		configAtualizacaoInsertCache[key] = cache
		configAtualizacaoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ConfigAtualizacao.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ConfigAtualizacao) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	configAtualizacaoUpdateCacheMut.RLock()
	cache, cached := configAtualizacaoUpdateCache[key]
	configAtualizacaoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			configAtualizacaoAllColumns,
			configAtualizacaoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update config_atualizacao, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `config_atualizacao` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, configAtualizacaoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(configAtualizacaoType, configAtualizacaoMapping, append(wl, configAtualizacaoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update config_atualizacao row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for config_atualizacao")
	}

	if !cached {
		configAtualizacaoUpdateCacheMut.Lock()
		configAtualizacaoUpdateCache[key] = cache
		configAtualizacaoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q configAtualizacaoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for config_atualizacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for config_atualizacao")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ConfigAtualizacaoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), configAtualizacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `config_atualizacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, configAtualizacaoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in configAtualizacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all configAtualizacao")
	}
	return rowsAff, nil
}

var mySQLConfigAtualizacaoUniqueColumns = []string{
	"cat_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ConfigAtualizacao) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no config_atualizacao provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(configAtualizacaoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLConfigAtualizacaoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	configAtualizacaoUpsertCacheMut.RLock()
	cache, cached := configAtualizacaoUpsertCache[key]
	configAtualizacaoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			configAtualizacaoAllColumns,
			configAtualizacaoColumnsWithDefault,
			configAtualizacaoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			configAtualizacaoAllColumns,
			configAtualizacaoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert config_atualizacao, could not build update column list")
		}

		ret := strmangle.SetComplement(configAtualizacaoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`config_atualizacao`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `config_atualizacao` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(configAtualizacaoType, configAtualizacaoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(configAtualizacaoType, configAtualizacaoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for config_atualizacao")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(configAtualizacaoType, configAtualizacaoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for config_atualizacao")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for config_atualizacao")
	}

CacheNoHooks:
	if !cached {
		configAtualizacaoUpsertCacheMut.Lock()
		configAtualizacaoUpsertCache[key] = cache
		configAtualizacaoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ConfigAtualizacao record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ConfigAtualizacao) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ConfigAtualizacao provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), configAtualizacaoPrimaryKeyMapping)
	sql := "DELETE FROM `config_atualizacao` WHERE `cat_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from config_atualizacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for config_atualizacao")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q configAtualizacaoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no configAtualizacaoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from config_atualizacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for config_atualizacao")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ConfigAtualizacaoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(configAtualizacaoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), configAtualizacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `config_atualizacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, configAtualizacaoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from configAtualizacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for config_atualizacao")
	}

	if len(configAtualizacaoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ConfigAtualizacao) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindConfigAtualizacao(ctx, exec, o.CatID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ConfigAtualizacaoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ConfigAtualizacaoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), configAtualizacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `config_atualizacao`.* FROM `config_atualizacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, configAtualizacaoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ConfigAtualizacaoSlice")
	}

	*o = slice

	return nil
}

// ConfigAtualizacaoExists checks if the ConfigAtualizacao row exists.
func ConfigAtualizacaoExists(ctx context.Context, exec boil.ContextExecutor, catID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `config_atualizacao` where `cat_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, catID)
	}
	row := exec.QueryRowContext(ctx, sql, catID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if config_atualizacao exists")
	}

	return exists, nil
}

// Exists checks if the ConfigAtualizacao row exists.
func (o *ConfigAtualizacao) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ConfigAtualizacaoExists(ctx, exec, o.CatID)
}
