// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// RequisicaoItem is an object representing the database table.
type RequisicaoItem struct {
	ReqID         int           `boil:"req_id" json:"req_id" toml:"req_id" yaml:"req_id"`
	ReiItem       int           `boil:"rei_item" json:"rei_item" toml:"rei_item" yaml:"rei_item"`
	ProID         int           `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	ReiQuantidade types.Decimal `boil:"rei_quantidade" json:"rei_quantidade" toml:"rei_quantidade" yaml:"rei_quantidade"`
	ReiCusto      types.Decimal `boil:"rei_custo" json:"rei_custo" toml:"rei_custo" yaml:"rei_custo"`
	GradeID       null.Int      `boil:"grade_id" json:"grade_id,omitempty" toml:"grade_id" yaml:"grade_id,omitempty"`
	GraiItem      null.Int      `boil:"grai_item" json:"grai_item,omitempty" toml:"grai_item" yaml:"grai_item,omitempty"`

	R *requisicaoItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L requisicaoItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RequisicaoItemColumns = struct {
	ReqID         string
	ReiItem       string
	ProID         string
	ReiQuantidade string
	ReiCusto      string
	GradeID       string
	GraiItem      string
}{
	ReqID:         "req_id",
	ReiItem:       "rei_item",
	ProID:         "pro_id",
	ReiQuantidade: "rei_quantidade",
	ReiCusto:      "rei_custo",
	GradeID:       "grade_id",
	GraiItem:      "grai_item",
}

var RequisicaoItemTableColumns = struct {
	ReqID         string
	ReiItem       string
	ProID         string
	ReiQuantidade string
	ReiCusto      string
	GradeID       string
	GraiItem      string
}{
	ReqID:         "requisicao_item.req_id",
	ReiItem:       "requisicao_item.rei_item",
	ProID:         "requisicao_item.pro_id",
	ReiQuantidade: "requisicao_item.rei_quantidade",
	ReiCusto:      "requisicao_item.rei_custo",
	GradeID:       "requisicao_item.grade_id",
	GraiItem:      "requisicao_item.grai_item",
}

// Generated where

var RequisicaoItemWhere = struct {
	ReqID         whereHelperint
	ReiItem       whereHelperint
	ProID         whereHelperint
	ReiQuantidade whereHelpertypes_Decimal
	ReiCusto      whereHelpertypes_Decimal
	GradeID       whereHelpernull_Int
	GraiItem      whereHelpernull_Int
}{
	ReqID:         whereHelperint{field: "`requisicao_item`.`req_id`"},
	ReiItem:       whereHelperint{field: "`requisicao_item`.`rei_item`"},
	ProID:         whereHelperint{field: "`requisicao_item`.`pro_id`"},
	ReiQuantidade: whereHelpertypes_Decimal{field: "`requisicao_item`.`rei_quantidade`"},
	ReiCusto:      whereHelpertypes_Decimal{field: "`requisicao_item`.`rei_custo`"},
	GradeID:       whereHelpernull_Int{field: "`requisicao_item`.`grade_id`"},
	GraiItem:      whereHelpernull_Int{field: "`requisicao_item`.`grai_item`"},
}

// RequisicaoItemRels is where relationship names are stored.
var RequisicaoItemRels = struct {
	Pro string
	Req string
}{
	Pro: "Pro",
	Req: "Req",
}

// requisicaoItemR is where relationships are stored.
type requisicaoItemR struct {
	Pro *Produto    `boil:"Pro" json:"Pro" toml:"Pro" yaml:"Pro"`
	Req *Requisicao `boil:"Req" json:"Req" toml:"Req" yaml:"Req"`
}

// NewStruct creates a new relationship struct
func (*requisicaoItemR) NewStruct() *requisicaoItemR {
	return &requisicaoItemR{}
}

func (r *requisicaoItemR) GetPro() *Produto {
	if r == nil {
		return nil
	}
	return r.Pro
}

func (r *requisicaoItemR) GetReq() *Requisicao {
	if r == nil {
		return nil
	}
	return r.Req
}

// requisicaoItemL is where Load methods for each relationship are stored.
type requisicaoItemL struct{}

var (
	requisicaoItemAllColumns            = []string{"req_id", "rei_item", "pro_id", "rei_quantidade", "rei_custo", "grade_id", "grai_item"}
	requisicaoItemColumnsWithoutDefault = []string{"req_id", "rei_item", "pro_id", "rei_quantidade", "rei_custo", "grade_id", "grai_item"}
	requisicaoItemColumnsWithDefault    = []string{}
	requisicaoItemPrimaryKeyColumns     = []string{"req_id", "rei_item"}
	requisicaoItemGeneratedColumns      = []string{}
)

type (
	// RequisicaoItemSlice is an alias for a slice of pointers to RequisicaoItem.
	// This should almost always be used instead of []RequisicaoItem.
	RequisicaoItemSlice []*RequisicaoItem
	// RequisicaoItemHook is the signature for custom RequisicaoItem hook methods
	RequisicaoItemHook func(context.Context, boil.ContextExecutor, *RequisicaoItem) error

	requisicaoItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	requisicaoItemType                 = reflect.TypeOf(&RequisicaoItem{})
	requisicaoItemMapping              = queries.MakeStructMapping(requisicaoItemType)
	requisicaoItemPrimaryKeyMapping, _ = queries.BindMapping(requisicaoItemType, requisicaoItemMapping, requisicaoItemPrimaryKeyColumns)
	requisicaoItemInsertCacheMut       sync.RWMutex
	requisicaoItemInsertCache          = make(map[string]insertCache)
	requisicaoItemUpdateCacheMut       sync.RWMutex
	requisicaoItemUpdateCache          = make(map[string]updateCache)
	requisicaoItemUpsertCacheMut       sync.RWMutex
	requisicaoItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var requisicaoItemAfterSelectMu sync.Mutex
var requisicaoItemAfterSelectHooks []RequisicaoItemHook

var requisicaoItemBeforeInsertMu sync.Mutex
var requisicaoItemBeforeInsertHooks []RequisicaoItemHook
var requisicaoItemAfterInsertMu sync.Mutex
var requisicaoItemAfterInsertHooks []RequisicaoItemHook

var requisicaoItemBeforeUpdateMu sync.Mutex
var requisicaoItemBeforeUpdateHooks []RequisicaoItemHook
var requisicaoItemAfterUpdateMu sync.Mutex
var requisicaoItemAfterUpdateHooks []RequisicaoItemHook

var requisicaoItemBeforeDeleteMu sync.Mutex
var requisicaoItemBeforeDeleteHooks []RequisicaoItemHook
var requisicaoItemAfterDeleteMu sync.Mutex
var requisicaoItemAfterDeleteHooks []RequisicaoItemHook

var requisicaoItemBeforeUpsertMu sync.Mutex
var requisicaoItemBeforeUpsertHooks []RequisicaoItemHook
var requisicaoItemAfterUpsertMu sync.Mutex
var requisicaoItemAfterUpsertHooks []RequisicaoItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RequisicaoItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RequisicaoItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RequisicaoItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RequisicaoItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RequisicaoItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RequisicaoItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RequisicaoItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RequisicaoItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RequisicaoItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRequisicaoItemHook registers your hook function for all future operations.
func AddRequisicaoItemHook(hookPoint boil.HookPoint, requisicaoItemHook RequisicaoItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		requisicaoItemAfterSelectMu.Lock()
		requisicaoItemAfterSelectHooks = append(requisicaoItemAfterSelectHooks, requisicaoItemHook)
		requisicaoItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		requisicaoItemBeforeInsertMu.Lock()
		requisicaoItemBeforeInsertHooks = append(requisicaoItemBeforeInsertHooks, requisicaoItemHook)
		requisicaoItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		requisicaoItemAfterInsertMu.Lock()
		requisicaoItemAfterInsertHooks = append(requisicaoItemAfterInsertHooks, requisicaoItemHook)
		requisicaoItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		requisicaoItemBeforeUpdateMu.Lock()
		requisicaoItemBeforeUpdateHooks = append(requisicaoItemBeforeUpdateHooks, requisicaoItemHook)
		requisicaoItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		requisicaoItemAfterUpdateMu.Lock()
		requisicaoItemAfterUpdateHooks = append(requisicaoItemAfterUpdateHooks, requisicaoItemHook)
		requisicaoItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		requisicaoItemBeforeDeleteMu.Lock()
		requisicaoItemBeforeDeleteHooks = append(requisicaoItemBeforeDeleteHooks, requisicaoItemHook)
		requisicaoItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		requisicaoItemAfterDeleteMu.Lock()
		requisicaoItemAfterDeleteHooks = append(requisicaoItemAfterDeleteHooks, requisicaoItemHook)
		requisicaoItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		requisicaoItemBeforeUpsertMu.Lock()
		requisicaoItemBeforeUpsertHooks = append(requisicaoItemBeforeUpsertHooks, requisicaoItemHook)
		requisicaoItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		requisicaoItemAfterUpsertMu.Lock()
		requisicaoItemAfterUpsertHooks = append(requisicaoItemAfterUpsertHooks, requisicaoItemHook)
		requisicaoItemAfterUpsertMu.Unlock()
	}
}

// One returns a single requisicaoItem record from the query.
func (q requisicaoItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RequisicaoItem, error) {
	o := &RequisicaoItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for requisicao_item")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RequisicaoItem records from the query.
func (q requisicaoItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (RequisicaoItemSlice, error) {
	var o []*RequisicaoItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RequisicaoItem slice")
	}

	if len(requisicaoItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RequisicaoItem records in the query.
func (q requisicaoItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count requisicao_item rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q requisicaoItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if requisicao_item exists")
	}

	return count > 0, nil
}

// Pro pointed to by the foreign key.
func (o *RequisicaoItem) Pro(mods ...qm.QueryMod) produtoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pro_id` = ?", o.ProID),
	}

	queryMods = append(queryMods, mods...)

	return Produtos(queryMods...)
}

// Req pointed to by the foreign key.
func (o *RequisicaoItem) Req(mods ...qm.QueryMod) requisicaoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`req_id` = ?", o.ReqID),
	}

	queryMods = append(queryMods, mods...)

	return Requisicaos(queryMods...)
}

// LoadPro allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (requisicaoItemL) LoadPro(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRequisicaoItem interface{}, mods queries.Applicator) error {
	var slice []*RequisicaoItem
	var object *RequisicaoItem

	if singular {
		var ok bool
		object, ok = maybeRequisicaoItem.(*RequisicaoItem)
		if !ok {
			object = new(RequisicaoItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRequisicaoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRequisicaoItem))
			}
		}
	} else {
		s, ok := maybeRequisicaoItem.(*[]*RequisicaoItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRequisicaoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRequisicaoItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &requisicaoItemR{}
		}
		args[object.ProID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &requisicaoItemR{}
			}

			args[obj.ProID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.pro_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pro = foreign
		if foreign.R == nil {
			foreign.R = &produtoR{}
		}
		foreign.R.ProRequisicaoItems = append(foreign.R.ProRequisicaoItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProID == foreign.ProID {
				local.R.Pro = foreign
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.ProRequisicaoItems = append(foreign.R.ProRequisicaoItems, local)
				break
			}
		}
	}

	return nil
}

// LoadReq allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (requisicaoItemL) LoadReq(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRequisicaoItem interface{}, mods queries.Applicator) error {
	var slice []*RequisicaoItem
	var object *RequisicaoItem

	if singular {
		var ok bool
		object, ok = maybeRequisicaoItem.(*RequisicaoItem)
		if !ok {
			object = new(RequisicaoItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRequisicaoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRequisicaoItem))
			}
		}
	} else {
		s, ok := maybeRequisicaoItem.(*[]*RequisicaoItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRequisicaoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRequisicaoItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &requisicaoItemR{}
		}
		args[object.ReqID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &requisicaoItemR{}
			}

			args[obj.ReqID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`requisicao`),
		qm.WhereIn(`requisicao.req_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Requisicao")
	}

	var resultSlice []*Requisicao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Requisicao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for requisicao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for requisicao")
	}

	if len(requisicaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Req = foreign
		if foreign.R == nil {
			foreign.R = &requisicaoR{}
		}
		foreign.R.ReqRequisicaoItems = append(foreign.R.ReqRequisicaoItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ReqID == foreign.ReqID {
				local.R.Req = foreign
				if foreign.R == nil {
					foreign.R = &requisicaoR{}
				}
				foreign.R.ReqRequisicaoItems = append(foreign.R.ReqRequisicaoItems, local)
				break
			}
		}
	}

	return nil
}

// SetPro of the requisicaoItem to the related item.
// Sets o.R.Pro to related.
// Adds o to related.R.ProRequisicaoItems.
func (o *RequisicaoItem) SetPro(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Produto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `requisicao_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pro_id"}),
		strmangle.WhereClause("`", "`", 0, requisicaoItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ProID, o.ReqID, o.ReiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProID = related.ProID
	if o.R == nil {
		o.R = &requisicaoItemR{
			Pro: related,
		}
	} else {
		o.R.Pro = related
	}

	if related.R == nil {
		related.R = &produtoR{
			ProRequisicaoItems: RequisicaoItemSlice{o},
		}
	} else {
		related.R.ProRequisicaoItems = append(related.R.ProRequisicaoItems, o)
	}

	return nil
}

// SetReq of the requisicaoItem to the related item.
// Sets o.R.Req to related.
// Adds o to related.R.ReqRequisicaoItems.
func (o *RequisicaoItem) SetReq(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Requisicao) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `requisicao_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"req_id"}),
		strmangle.WhereClause("`", "`", 0, requisicaoItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ReqID, o.ReqID, o.ReiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ReqID = related.ReqID
	if o.R == nil {
		o.R = &requisicaoItemR{
			Req: related,
		}
	} else {
		o.R.Req = related
	}

	if related.R == nil {
		related.R = &requisicaoR{
			ReqRequisicaoItems: RequisicaoItemSlice{o},
		}
	} else {
		related.R.ReqRequisicaoItems = append(related.R.ReqRequisicaoItems, o)
	}

	return nil
}

// RequisicaoItems retrieves all the records using an executor.
func RequisicaoItems(mods ...qm.QueryMod) requisicaoItemQuery {
	mods = append(mods, qm.From("`requisicao_item`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`requisicao_item`.*"})
	}

	return requisicaoItemQuery{q}
}

// FindRequisicaoItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRequisicaoItem(ctx context.Context, exec boil.ContextExecutor, reqID int, reiItem int, selectCols ...string) (*RequisicaoItem, error) {
	requisicaoItemObj := &RequisicaoItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `requisicao_item` where `req_id`=? AND `rei_item`=?", sel,
	)

	q := queries.Raw(query, reqID, reiItem)

	err := q.Bind(ctx, exec, requisicaoItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from requisicao_item")
	}

	if err = requisicaoItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return requisicaoItemObj, err
	}

	return requisicaoItemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RequisicaoItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no requisicao_item provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(requisicaoItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	requisicaoItemInsertCacheMut.RLock()
	cache, cached := requisicaoItemInsertCache[key]
	requisicaoItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			requisicaoItemAllColumns,
			requisicaoItemColumnsWithDefault,
			requisicaoItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(requisicaoItemType, requisicaoItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(requisicaoItemType, requisicaoItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `requisicao_item` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `requisicao_item` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `requisicao_item` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, requisicaoItemPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into requisicao_item")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ReqID,
		o.ReiItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for requisicao_item")
	}

CacheNoHooks:
	if !cached {
		requisicaoItemInsertCacheMut.Lock()
		requisicaoItemInsertCache[key] = cache
		requisicaoItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RequisicaoItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RequisicaoItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	requisicaoItemUpdateCacheMut.RLock()
	cache, cached := requisicaoItemUpdateCache[key]
	requisicaoItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			requisicaoItemAllColumns,
			requisicaoItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update requisicao_item, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `requisicao_item` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, requisicaoItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(requisicaoItemType, requisicaoItemMapping, append(wl, requisicaoItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update requisicao_item row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for requisicao_item")
	}

	if !cached {
		requisicaoItemUpdateCacheMut.Lock()
		requisicaoItemUpdateCache[key] = cache
		requisicaoItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q requisicaoItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for requisicao_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for requisicao_item")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RequisicaoItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `requisicao_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in requisicaoItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all requisicaoItem")
	}
	return rowsAff, nil
}

var mySQLRequisicaoItemUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RequisicaoItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no requisicao_item provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(requisicaoItemColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRequisicaoItemUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	requisicaoItemUpsertCacheMut.RLock()
	cache, cached := requisicaoItemUpsertCache[key]
	requisicaoItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			requisicaoItemAllColumns,
			requisicaoItemColumnsWithDefault,
			requisicaoItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			requisicaoItemAllColumns,
			requisicaoItemPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert requisicao_item, could not build update column list")
		}

		ret := strmangle.SetComplement(requisicaoItemAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`requisicao_item`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `requisicao_item` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(requisicaoItemType, requisicaoItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(requisicaoItemType, requisicaoItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for requisicao_item")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(requisicaoItemType, requisicaoItemMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for requisicao_item")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for requisicao_item")
	}

CacheNoHooks:
	if !cached {
		requisicaoItemUpsertCacheMut.Lock()
		requisicaoItemUpsertCache[key] = cache
		requisicaoItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RequisicaoItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RequisicaoItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RequisicaoItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), requisicaoItemPrimaryKeyMapping)
	sql := "DELETE FROM `requisicao_item` WHERE `req_id`=? AND `rei_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from requisicao_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for requisicao_item")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q requisicaoItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no requisicaoItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from requisicao_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for requisicao_item")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RequisicaoItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(requisicaoItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `requisicao_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from requisicaoItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for requisicao_item")
	}

	if len(requisicaoItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RequisicaoItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRequisicaoItem(ctx, exec, o.ReqID, o.ReiItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RequisicaoItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RequisicaoItemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `requisicao_item`.* FROM `requisicao_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RequisicaoItemSlice")
	}

	*o = slice

	return nil
}

// RequisicaoItemExists checks if the RequisicaoItem row exists.
func RequisicaoItemExists(ctx context.Context, exec boil.ContextExecutor, reqID int, reiItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `requisicao_item` where `req_id`=? AND `rei_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, reqID, reiItem)
	}
	row := exec.QueryRowContext(ctx, sql, reqID, reiItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if requisicao_item exists")
	}

	return exists, nil
}

// Exists checks if the RequisicaoItem row exists.
func (o *RequisicaoItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RequisicaoItemExists(ctx, exec, o.ReqID, o.ReiItem)
}
