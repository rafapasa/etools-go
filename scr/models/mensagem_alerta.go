// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MensagemAlertum is an object representing the database table.
type MensagemAlertum struct {
	MsgaltID       int       `boil:"msgalt_id" json:"msgalt_id" toml:"msgalt_id" yaml:"msgalt_id"`
	AltID          int       `boil:"alt_id" json:"alt_id" toml:"alt_id" yaml:"alt_id"`
	EntID          int       `boil:"ent_id" json:"ent_id" toml:"ent_id" yaml:"ent_id"`
	MsgaltMensagem string    `boil:"msgalt_mensagem" json:"msgalt_mensagem" toml:"msgalt_mensagem" yaml:"msgalt_mensagem"`
	MsgaltDatahora time.Time `boil:"msgalt_datahora" json:"msgalt_datahora" toml:"msgalt_datahora" yaml:"msgalt_datahora"`
	// 1 - nao lida 2 - lida
	MsgaltSituacao int `boil:"msgalt_situacao" json:"msgalt_situacao" toml:"msgalt_situacao" yaml:"msgalt_situacao"`
	// 0 - enviar 1 - enviado 2 - não enviar
	MsgaltSMS null.Int `boil:"msgalt_sms" json:"msgalt_sms,omitempty" toml:"msgalt_sms" yaml:"msgalt_sms,omitempty"`
	// 0 - enviar 1 - enviado 2 - não enviar
	MsgaltEmail null.Int `boil:"msgalt_email" json:"msgalt_email,omitempty" toml:"msgalt_email" yaml:"msgalt_email,omitempty"`
	// 0 - enviar 1 - enviado 2 - não enviar
	MsgaltWhatsapp   null.Int    `boil:"msgalt_whatsapp" json:"msgalt_whatsapp,omitempty" toml:"msgalt_whatsapp" yaml:"msgalt_whatsapp,omitempty"`
	MsgaltInformacao null.String `boil:"msgalt_informacao" json:"msgalt_informacao,omitempty" toml:"msgalt_informacao" yaml:"msgalt_informacao,omitempty"`

	R *mensagemAlertumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L mensagemAlertumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MensagemAlertumColumns = struct {
	MsgaltID         string
	AltID            string
	EntID            string
	MsgaltMensagem   string
	MsgaltDatahora   string
	MsgaltSituacao   string
	MsgaltSMS        string
	MsgaltEmail      string
	MsgaltWhatsapp   string
	MsgaltInformacao string
}{
	MsgaltID:         "msgalt_id",
	AltID:            "alt_id",
	EntID:            "ent_id",
	MsgaltMensagem:   "msgalt_mensagem",
	MsgaltDatahora:   "msgalt_datahora",
	MsgaltSituacao:   "msgalt_situacao",
	MsgaltSMS:        "msgalt_sms",
	MsgaltEmail:      "msgalt_email",
	MsgaltWhatsapp:   "msgalt_whatsapp",
	MsgaltInformacao: "msgalt_informacao",
}

var MensagemAlertumTableColumns = struct {
	MsgaltID         string
	AltID            string
	EntID            string
	MsgaltMensagem   string
	MsgaltDatahora   string
	MsgaltSituacao   string
	MsgaltSMS        string
	MsgaltEmail      string
	MsgaltWhatsapp   string
	MsgaltInformacao string
}{
	MsgaltID:         "mensagem_alerta.msgalt_id",
	AltID:            "mensagem_alerta.alt_id",
	EntID:            "mensagem_alerta.ent_id",
	MsgaltMensagem:   "mensagem_alerta.msgalt_mensagem",
	MsgaltDatahora:   "mensagem_alerta.msgalt_datahora",
	MsgaltSituacao:   "mensagem_alerta.msgalt_situacao",
	MsgaltSMS:        "mensagem_alerta.msgalt_sms",
	MsgaltEmail:      "mensagem_alerta.msgalt_email",
	MsgaltWhatsapp:   "mensagem_alerta.msgalt_whatsapp",
	MsgaltInformacao: "mensagem_alerta.msgalt_informacao",
}

// Generated where

var MensagemAlertumWhere = struct {
	MsgaltID         whereHelperint
	AltID            whereHelperint
	EntID            whereHelperint
	MsgaltMensagem   whereHelperstring
	MsgaltDatahora   whereHelpertime_Time
	MsgaltSituacao   whereHelperint
	MsgaltSMS        whereHelpernull_Int
	MsgaltEmail      whereHelpernull_Int
	MsgaltWhatsapp   whereHelpernull_Int
	MsgaltInformacao whereHelpernull_String
}{
	MsgaltID:         whereHelperint{field: "`mensagem_alerta`.`msgalt_id`"},
	AltID:            whereHelperint{field: "`mensagem_alerta`.`alt_id`"},
	EntID:            whereHelperint{field: "`mensagem_alerta`.`ent_id`"},
	MsgaltMensagem:   whereHelperstring{field: "`mensagem_alerta`.`msgalt_mensagem`"},
	MsgaltDatahora:   whereHelpertime_Time{field: "`mensagem_alerta`.`msgalt_datahora`"},
	MsgaltSituacao:   whereHelperint{field: "`mensagem_alerta`.`msgalt_situacao`"},
	MsgaltSMS:        whereHelpernull_Int{field: "`mensagem_alerta`.`msgalt_sms`"},
	MsgaltEmail:      whereHelpernull_Int{field: "`mensagem_alerta`.`msgalt_email`"},
	MsgaltWhatsapp:   whereHelpernull_Int{field: "`mensagem_alerta`.`msgalt_whatsapp`"},
	MsgaltInformacao: whereHelpernull_String{field: "`mensagem_alerta`.`msgalt_informacao`"},
}

// MensagemAlertumRels is where relationship names are stored.
var MensagemAlertumRels = struct {
	Alt string
	Ent string
}{
	Alt: "Alt",
	Ent: "Ent",
}

// mensagemAlertumR is where relationships are stored.
type mensagemAlertumR struct {
	Alt *Alertas  `boil:"Alt" json:"Alt" toml:"Alt" yaml:"Alt"`
	Ent *Entidade `boil:"Ent" json:"Ent" toml:"Ent" yaml:"Ent"`
}

// NewStruct creates a new relationship struct
func (*mensagemAlertumR) NewStruct() *mensagemAlertumR {
	return &mensagemAlertumR{}
}

func (r *mensagemAlertumR) GetAlt() *Alertas {
	if r == nil {
		return nil
	}
	return r.Alt
}

func (r *mensagemAlertumR) GetEnt() *Entidade {
	if r == nil {
		return nil
	}
	return r.Ent
}

// mensagemAlertumL is where Load methods for each relationship are stored.
type mensagemAlertumL struct{}

var (
	mensagemAlertumAllColumns            = []string{"msgalt_id", "alt_id", "ent_id", "msgalt_mensagem", "msgalt_datahora", "msgalt_situacao", "msgalt_sms", "msgalt_email", "msgalt_whatsapp", "msgalt_informacao"}
	mensagemAlertumColumnsWithoutDefault = []string{"msgalt_id", "alt_id", "ent_id", "msgalt_mensagem", "msgalt_datahora", "msgalt_situacao", "msgalt_sms", "msgalt_email", "msgalt_whatsapp", "msgalt_informacao"}
	mensagemAlertumColumnsWithDefault    = []string{}
	mensagemAlertumPrimaryKeyColumns     = []string{"msgalt_id"}
	mensagemAlertumGeneratedColumns      = []string{}
)

type (
	// MensagemAlertumSlice is an alias for a slice of pointers to MensagemAlertum.
	// This should almost always be used instead of []MensagemAlertum.
	MensagemAlertumSlice []*MensagemAlertum
	// MensagemAlertumHook is the signature for custom MensagemAlertum hook methods
	MensagemAlertumHook func(context.Context, boil.ContextExecutor, *MensagemAlertum) error

	mensagemAlertumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	mensagemAlertumType                 = reflect.TypeOf(&MensagemAlertum{})
	mensagemAlertumMapping              = queries.MakeStructMapping(mensagemAlertumType)
	mensagemAlertumPrimaryKeyMapping, _ = queries.BindMapping(mensagemAlertumType, mensagemAlertumMapping, mensagemAlertumPrimaryKeyColumns)
	mensagemAlertumInsertCacheMut       sync.RWMutex
	mensagemAlertumInsertCache          = make(map[string]insertCache)
	mensagemAlertumUpdateCacheMut       sync.RWMutex
	mensagemAlertumUpdateCache          = make(map[string]updateCache)
	mensagemAlertumUpsertCacheMut       sync.RWMutex
	mensagemAlertumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var mensagemAlertumAfterSelectMu sync.Mutex
var mensagemAlertumAfterSelectHooks []MensagemAlertumHook

var mensagemAlertumBeforeInsertMu sync.Mutex
var mensagemAlertumBeforeInsertHooks []MensagemAlertumHook
var mensagemAlertumAfterInsertMu sync.Mutex
var mensagemAlertumAfterInsertHooks []MensagemAlertumHook

var mensagemAlertumBeforeUpdateMu sync.Mutex
var mensagemAlertumBeforeUpdateHooks []MensagemAlertumHook
var mensagemAlertumAfterUpdateMu sync.Mutex
var mensagemAlertumAfterUpdateHooks []MensagemAlertumHook

var mensagemAlertumBeforeDeleteMu sync.Mutex
var mensagemAlertumBeforeDeleteHooks []MensagemAlertumHook
var mensagemAlertumAfterDeleteMu sync.Mutex
var mensagemAlertumAfterDeleteHooks []MensagemAlertumHook

var mensagemAlertumBeforeUpsertMu sync.Mutex
var mensagemAlertumBeforeUpsertHooks []MensagemAlertumHook
var mensagemAlertumAfterUpsertMu sync.Mutex
var mensagemAlertumAfterUpsertHooks []MensagemAlertumHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MensagemAlertum) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MensagemAlertum) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MensagemAlertum) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MensagemAlertum) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MensagemAlertum) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MensagemAlertum) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MensagemAlertum) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MensagemAlertum) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MensagemAlertum) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mensagemAlertumAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMensagemAlertumHook registers your hook function for all future operations.
func AddMensagemAlertumHook(hookPoint boil.HookPoint, mensagemAlertumHook MensagemAlertumHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		mensagemAlertumAfterSelectMu.Lock()
		mensagemAlertumAfterSelectHooks = append(mensagemAlertumAfterSelectHooks, mensagemAlertumHook)
		mensagemAlertumAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		mensagemAlertumBeforeInsertMu.Lock()
		mensagemAlertumBeforeInsertHooks = append(mensagemAlertumBeforeInsertHooks, mensagemAlertumHook)
		mensagemAlertumBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		mensagemAlertumAfterInsertMu.Lock()
		mensagemAlertumAfterInsertHooks = append(mensagemAlertumAfterInsertHooks, mensagemAlertumHook)
		mensagemAlertumAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		mensagemAlertumBeforeUpdateMu.Lock()
		mensagemAlertumBeforeUpdateHooks = append(mensagemAlertumBeforeUpdateHooks, mensagemAlertumHook)
		mensagemAlertumBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		mensagemAlertumAfterUpdateMu.Lock()
		mensagemAlertumAfterUpdateHooks = append(mensagemAlertumAfterUpdateHooks, mensagemAlertumHook)
		mensagemAlertumAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		mensagemAlertumBeforeDeleteMu.Lock()
		mensagemAlertumBeforeDeleteHooks = append(mensagemAlertumBeforeDeleteHooks, mensagemAlertumHook)
		mensagemAlertumBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		mensagemAlertumAfterDeleteMu.Lock()
		mensagemAlertumAfterDeleteHooks = append(mensagemAlertumAfterDeleteHooks, mensagemAlertumHook)
		mensagemAlertumAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		mensagemAlertumBeforeUpsertMu.Lock()
		mensagemAlertumBeforeUpsertHooks = append(mensagemAlertumBeforeUpsertHooks, mensagemAlertumHook)
		mensagemAlertumBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		mensagemAlertumAfterUpsertMu.Lock()
		mensagemAlertumAfterUpsertHooks = append(mensagemAlertumAfterUpsertHooks, mensagemAlertumHook)
		mensagemAlertumAfterUpsertMu.Unlock()
	}
}

// One returns a single mensagemAlertum record from the query.
func (q mensagemAlertumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MensagemAlertum, error) {
	o := &MensagemAlertum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for mensagem_alerta")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MensagemAlertum records from the query.
func (q mensagemAlertumQuery) All(ctx context.Context, exec boil.ContextExecutor) (MensagemAlertumSlice, error) {
	var o []*MensagemAlertum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MensagemAlertum slice")
	}

	if len(mensagemAlertumAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MensagemAlertum records in the query.
func (q mensagemAlertumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count mensagem_alerta rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q mensagemAlertumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if mensagem_alerta exists")
	}

	return count > 0, nil
}

// Alt pointed to by the foreign key.
func (o *MensagemAlertum) Alt(mods ...qm.QueryMod) alertasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`alt_id` = ?", o.AltID),
	}

	queryMods = append(queryMods, mods...)

	return Alertas(queryMods...)
}

// Ent pointed to by the foreign key.
func (o *MensagemAlertum) Ent(mods ...qm.QueryMod) entidadeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ent_id` = ?", o.EntID),
	}

	queryMods = append(queryMods, mods...)

	return Entidades(queryMods...)
}

// LoadAlt allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (mensagemAlertumL) LoadAlt(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMensagemAlertum interface{}, mods queries.Applicator) error {
	var slice []*MensagemAlertum
	var object *MensagemAlertum

	if singular {
		var ok bool
		object, ok = maybeMensagemAlertum.(*MensagemAlertum)
		if !ok {
			object = new(MensagemAlertum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeMensagemAlertum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeMensagemAlertum))
			}
		}
	} else {
		s, ok := maybeMensagemAlertum.(*[]*MensagemAlertum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeMensagemAlertum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeMensagemAlertum))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &mensagemAlertumR{}
		}
		args[object.AltID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &mensagemAlertumR{}
			}

			args[obj.AltID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`alertas`),
		qm.WhereIn(`alertas.alt_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Alertas")
	}

	var resultSlice []*Alertas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Alertas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for alertas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for alertas")
	}

	if len(alertasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Alt = foreign
		if foreign.R == nil {
			foreign.R = &alertasR{}
		}
		foreign.R.AltMensagemAlerta = append(foreign.R.AltMensagemAlerta, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AltID == foreign.AltID {
				local.R.Alt = foreign
				if foreign.R == nil {
					foreign.R = &alertasR{}
				}
				foreign.R.AltMensagemAlerta = append(foreign.R.AltMensagemAlerta, local)
				break
			}
		}
	}

	return nil
}

// LoadEnt allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (mensagemAlertumL) LoadEnt(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMensagemAlertum interface{}, mods queries.Applicator) error {
	var slice []*MensagemAlertum
	var object *MensagemAlertum

	if singular {
		var ok bool
		object, ok = maybeMensagemAlertum.(*MensagemAlertum)
		if !ok {
			object = new(MensagemAlertum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeMensagemAlertum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeMensagemAlertum))
			}
		}
	} else {
		s, ok := maybeMensagemAlertum.(*[]*MensagemAlertum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeMensagemAlertum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeMensagemAlertum))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &mensagemAlertumR{}
		}
		args[object.EntID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &mensagemAlertumR{}
			}

			args[obj.EntID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade`),
		qm.WhereIn(`entidade.ent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Entidade")
	}

	var resultSlice []*Entidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade")
	}

	if len(entidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ent = foreign
		if foreign.R == nil {
			foreign.R = &entidadeR{}
		}
		foreign.R.EntMensagemAlerta = append(foreign.R.EntMensagemAlerta, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EntID == foreign.EntID {
				local.R.Ent = foreign
				if foreign.R == nil {
					foreign.R = &entidadeR{}
				}
				foreign.R.EntMensagemAlerta = append(foreign.R.EntMensagemAlerta, local)
				break
			}
		}
	}

	return nil
}

// SetAlt of the mensagemAlertum to the related item.
// Sets o.R.Alt to related.
// Adds o to related.R.AltMensagemAlerta.
func (o *MensagemAlertum) SetAlt(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Alertas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `mensagem_alerta` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"alt_id"}),
		strmangle.WhereClause("`", "`", 0, mensagemAlertumPrimaryKeyColumns),
	)
	values := []interface{}{related.AltID, o.MsgaltID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AltID = related.AltID
	if o.R == nil {
		o.R = &mensagemAlertumR{
			Alt: related,
		}
	} else {
		o.R.Alt = related
	}

	if related.R == nil {
		related.R = &alertasR{
			AltMensagemAlerta: MensagemAlertumSlice{o},
		}
	} else {
		related.R.AltMensagemAlerta = append(related.R.AltMensagemAlerta, o)
	}

	return nil
}

// SetEnt of the mensagemAlertum to the related item.
// Sets o.R.Ent to related.
// Adds o to related.R.EntMensagemAlerta.
func (o *MensagemAlertum) SetEnt(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Entidade) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `mensagem_alerta` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ent_id"}),
		strmangle.WhereClause("`", "`", 0, mensagemAlertumPrimaryKeyColumns),
	)
	values := []interface{}{related.EntID, o.MsgaltID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EntID = related.EntID
	if o.R == nil {
		o.R = &mensagemAlertumR{
			Ent: related,
		}
	} else {
		o.R.Ent = related
	}

	if related.R == nil {
		related.R = &entidadeR{
			EntMensagemAlerta: MensagemAlertumSlice{o},
		}
	} else {
		related.R.EntMensagemAlerta = append(related.R.EntMensagemAlerta, o)
	}

	return nil
}

// MensagemAlerta retrieves all the records using an executor.
func MensagemAlerta(mods ...qm.QueryMod) mensagemAlertumQuery {
	mods = append(mods, qm.From("`mensagem_alerta`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`mensagem_alerta`.*"})
	}

	return mensagemAlertumQuery{q}
}

// FindMensagemAlertum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMensagemAlertum(ctx context.Context, exec boil.ContextExecutor, msgaltID int, selectCols ...string) (*MensagemAlertum, error) {
	mensagemAlertumObj := &MensagemAlertum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `mensagem_alerta` where `msgalt_id`=?", sel,
	)

	q := queries.Raw(query, msgaltID)

	err := q.Bind(ctx, exec, mensagemAlertumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from mensagem_alerta")
	}

	if err = mensagemAlertumObj.doAfterSelectHooks(ctx, exec); err != nil {
		return mensagemAlertumObj, err
	}

	return mensagemAlertumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MensagemAlertum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no mensagem_alerta provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(mensagemAlertumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	mensagemAlertumInsertCacheMut.RLock()
	cache, cached := mensagemAlertumInsertCache[key]
	mensagemAlertumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			mensagemAlertumAllColumns,
			mensagemAlertumColumnsWithDefault,
			mensagemAlertumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(mensagemAlertumType, mensagemAlertumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(mensagemAlertumType, mensagemAlertumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `mensagem_alerta` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `mensagem_alerta` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `mensagem_alerta` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, mensagemAlertumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into mensagem_alerta")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.MsgaltID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for mensagem_alerta")
	}

CacheNoHooks:
	if !cached {
		mensagemAlertumInsertCacheMut.Lock()
		mensagemAlertumInsertCache[key] = cache
		mensagemAlertumInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MensagemAlertum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MensagemAlertum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	mensagemAlertumUpdateCacheMut.RLock()
	cache, cached := mensagemAlertumUpdateCache[key]
	mensagemAlertumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			mensagemAlertumAllColumns,
			mensagemAlertumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update mensagem_alerta, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `mensagem_alerta` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, mensagemAlertumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(mensagemAlertumType, mensagemAlertumMapping, append(wl, mensagemAlertumPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update mensagem_alerta row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for mensagem_alerta")
	}

	if !cached {
		mensagemAlertumUpdateCacheMut.Lock()
		mensagemAlertumUpdateCache[key] = cache
		mensagemAlertumUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q mensagemAlertumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for mensagem_alerta")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for mensagem_alerta")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MensagemAlertumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mensagemAlertumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `mensagem_alerta` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, mensagemAlertumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in mensagemAlertum slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all mensagemAlertum")
	}
	return rowsAff, nil
}

var mySQLMensagemAlertumUniqueColumns = []string{
	"msgalt_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MensagemAlertum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no mensagem_alerta provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(mensagemAlertumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMensagemAlertumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	mensagemAlertumUpsertCacheMut.RLock()
	cache, cached := mensagemAlertumUpsertCache[key]
	mensagemAlertumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			mensagemAlertumAllColumns,
			mensagemAlertumColumnsWithDefault,
			mensagemAlertumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			mensagemAlertumAllColumns,
			mensagemAlertumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert mensagem_alerta, could not build update column list")
		}

		ret := strmangle.SetComplement(mensagemAlertumAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`mensagem_alerta`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `mensagem_alerta` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(mensagemAlertumType, mensagemAlertumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(mensagemAlertumType, mensagemAlertumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for mensagem_alerta")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(mensagemAlertumType, mensagemAlertumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for mensagem_alerta")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for mensagem_alerta")
	}

CacheNoHooks:
	if !cached {
		mensagemAlertumUpsertCacheMut.Lock()
		mensagemAlertumUpsertCache[key] = cache
		mensagemAlertumUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MensagemAlertum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MensagemAlertum) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MensagemAlertum provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), mensagemAlertumPrimaryKeyMapping)
	sql := "DELETE FROM `mensagem_alerta` WHERE `msgalt_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from mensagem_alerta")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for mensagem_alerta")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q mensagemAlertumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no mensagemAlertumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from mensagem_alerta")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for mensagem_alerta")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MensagemAlertumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(mensagemAlertumBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mensagemAlertumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `mensagem_alerta` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, mensagemAlertumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from mensagemAlertum slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for mensagem_alerta")
	}

	if len(mensagemAlertumAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MensagemAlertum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMensagemAlertum(ctx, exec, o.MsgaltID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MensagemAlertumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MensagemAlertumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mensagemAlertumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `mensagem_alerta`.* FROM `mensagem_alerta` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, mensagemAlertumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MensagemAlertumSlice")
	}

	*o = slice

	return nil
}

// MensagemAlertumExists checks if the MensagemAlertum row exists.
func MensagemAlertumExists(ctx context.Context, exec boil.ContextExecutor, msgaltID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `mensagem_alerta` where `msgalt_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, msgaltID)
	}
	row := exec.QueryRowContext(ctx, sql, msgaltID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if mensagem_alerta exists")
	}

	return exists, nil
}

// Exists checks if the MensagemAlertum row exists.
func (o *MensagemAlertum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return MensagemAlertumExists(ctx, exec, o.MsgaltID)
}
