// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// HorarioTurno is an object representing the database table.
type HorarioTurno struct {
	HortID       int    `boil:"hort_id" json:"hort_id" toml:"hort_id" yaml:"hort_id"`
	HortEscricao string `boil:"hort_escricao" json:"hort_escricao" toml:"hort_escricao" yaml:"hort_escricao"`

	R *horarioTurnoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L horarioTurnoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var HorarioTurnoColumns = struct {
	HortID       string
	HortEscricao string
}{
	HortID:       "hort_id",
	HortEscricao: "hort_escricao",
}

var HorarioTurnoTableColumns = struct {
	HortID       string
	HortEscricao string
}{
	HortID:       "horario_turno.hort_id",
	HortEscricao: "horario_turno.hort_escricao",
}

// Generated where

var HorarioTurnoWhere = struct {
	HortID       whereHelperint
	HortEscricao whereHelperstring
}{
	HortID:       whereHelperint{field: "`horario_turno`.`hort_id`"},
	HortEscricao: whereHelperstring{field: "`horario_turno`.`hort_escricao`"},
}

// HorarioTurnoRels is where relationship names are stored.
var HorarioTurnoRels = struct {
	HortHorarioPontos string
}{
	HortHorarioPontos: "HortHorarioPontos",
}

// horarioTurnoR is where relationships are stored.
type horarioTurnoR struct {
	HortHorarioPontos HorarioPontoSlice `boil:"HortHorarioPontos" json:"HortHorarioPontos" toml:"HortHorarioPontos" yaml:"HortHorarioPontos"`
}

// NewStruct creates a new relationship struct
func (*horarioTurnoR) NewStruct() *horarioTurnoR {
	return &horarioTurnoR{}
}

func (r *horarioTurnoR) GetHortHorarioPontos() HorarioPontoSlice {
	if r == nil {
		return nil
	}
	return r.HortHorarioPontos
}

// horarioTurnoL is where Load methods for each relationship are stored.
type horarioTurnoL struct{}

var (
	horarioTurnoAllColumns            = []string{"hort_id", "hort_escricao"}
	horarioTurnoColumnsWithoutDefault = []string{"hort_id", "hort_escricao"}
	horarioTurnoColumnsWithDefault    = []string{}
	horarioTurnoPrimaryKeyColumns     = []string{"hort_id"}
	horarioTurnoGeneratedColumns      = []string{}
)

type (
	// HorarioTurnoSlice is an alias for a slice of pointers to HorarioTurno.
	// This should almost always be used instead of []HorarioTurno.
	HorarioTurnoSlice []*HorarioTurno
	// HorarioTurnoHook is the signature for custom HorarioTurno hook methods
	HorarioTurnoHook func(context.Context, boil.ContextExecutor, *HorarioTurno) error

	horarioTurnoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	horarioTurnoType                 = reflect.TypeOf(&HorarioTurno{})
	horarioTurnoMapping              = queries.MakeStructMapping(horarioTurnoType)
	horarioTurnoPrimaryKeyMapping, _ = queries.BindMapping(horarioTurnoType, horarioTurnoMapping, horarioTurnoPrimaryKeyColumns)
	horarioTurnoInsertCacheMut       sync.RWMutex
	horarioTurnoInsertCache          = make(map[string]insertCache)
	horarioTurnoUpdateCacheMut       sync.RWMutex
	horarioTurnoUpdateCache          = make(map[string]updateCache)
	horarioTurnoUpsertCacheMut       sync.RWMutex
	horarioTurnoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var horarioTurnoAfterSelectMu sync.Mutex
var horarioTurnoAfterSelectHooks []HorarioTurnoHook

var horarioTurnoBeforeInsertMu sync.Mutex
var horarioTurnoBeforeInsertHooks []HorarioTurnoHook
var horarioTurnoAfterInsertMu sync.Mutex
var horarioTurnoAfterInsertHooks []HorarioTurnoHook

var horarioTurnoBeforeUpdateMu sync.Mutex
var horarioTurnoBeforeUpdateHooks []HorarioTurnoHook
var horarioTurnoAfterUpdateMu sync.Mutex
var horarioTurnoAfterUpdateHooks []HorarioTurnoHook

var horarioTurnoBeforeDeleteMu sync.Mutex
var horarioTurnoBeforeDeleteHooks []HorarioTurnoHook
var horarioTurnoAfterDeleteMu sync.Mutex
var horarioTurnoAfterDeleteHooks []HorarioTurnoHook

var horarioTurnoBeforeUpsertMu sync.Mutex
var horarioTurnoBeforeUpsertHooks []HorarioTurnoHook
var horarioTurnoAfterUpsertMu sync.Mutex
var horarioTurnoAfterUpsertHooks []HorarioTurnoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *HorarioTurno) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *HorarioTurno) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *HorarioTurno) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *HorarioTurno) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *HorarioTurno) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *HorarioTurno) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *HorarioTurno) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *HorarioTurno) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *HorarioTurno) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range horarioTurnoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddHorarioTurnoHook registers your hook function for all future operations.
func AddHorarioTurnoHook(hookPoint boil.HookPoint, horarioTurnoHook HorarioTurnoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		horarioTurnoAfterSelectMu.Lock()
		horarioTurnoAfterSelectHooks = append(horarioTurnoAfterSelectHooks, horarioTurnoHook)
		horarioTurnoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		horarioTurnoBeforeInsertMu.Lock()
		horarioTurnoBeforeInsertHooks = append(horarioTurnoBeforeInsertHooks, horarioTurnoHook)
		horarioTurnoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		horarioTurnoAfterInsertMu.Lock()
		horarioTurnoAfterInsertHooks = append(horarioTurnoAfterInsertHooks, horarioTurnoHook)
		horarioTurnoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		horarioTurnoBeforeUpdateMu.Lock()
		horarioTurnoBeforeUpdateHooks = append(horarioTurnoBeforeUpdateHooks, horarioTurnoHook)
		horarioTurnoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		horarioTurnoAfterUpdateMu.Lock()
		horarioTurnoAfterUpdateHooks = append(horarioTurnoAfterUpdateHooks, horarioTurnoHook)
		horarioTurnoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		horarioTurnoBeforeDeleteMu.Lock()
		horarioTurnoBeforeDeleteHooks = append(horarioTurnoBeforeDeleteHooks, horarioTurnoHook)
		horarioTurnoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		horarioTurnoAfterDeleteMu.Lock()
		horarioTurnoAfterDeleteHooks = append(horarioTurnoAfterDeleteHooks, horarioTurnoHook)
		horarioTurnoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		horarioTurnoBeforeUpsertMu.Lock()
		horarioTurnoBeforeUpsertHooks = append(horarioTurnoBeforeUpsertHooks, horarioTurnoHook)
		horarioTurnoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		horarioTurnoAfterUpsertMu.Lock()
		horarioTurnoAfterUpsertHooks = append(horarioTurnoAfterUpsertHooks, horarioTurnoHook)
		horarioTurnoAfterUpsertMu.Unlock()
	}
}

// One returns a single horarioTurno record from the query.
func (q horarioTurnoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*HorarioTurno, error) {
	o := &HorarioTurno{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for horario_turno")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all HorarioTurno records from the query.
func (q horarioTurnoQuery) All(ctx context.Context, exec boil.ContextExecutor) (HorarioTurnoSlice, error) {
	var o []*HorarioTurno

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to HorarioTurno slice")
	}

	if len(horarioTurnoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all HorarioTurno records in the query.
func (q horarioTurnoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count horario_turno rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q horarioTurnoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if horario_turno exists")
	}

	return count > 0, nil
}

// HortHorarioPontos retrieves all the horario_ponto's HorarioPontos with an executor via hort_id column.
func (o *HorarioTurno) HortHorarioPontos(mods ...qm.QueryMod) horarioPontoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`horario_ponto`.`hort_id`=?", o.HortID),
	)

	return HorarioPontos(queryMods...)
}

// LoadHortHorarioPontos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (horarioTurnoL) LoadHortHorarioPontos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeHorarioTurno interface{}, mods queries.Applicator) error {
	var slice []*HorarioTurno
	var object *HorarioTurno

	if singular {
		var ok bool
		object, ok = maybeHorarioTurno.(*HorarioTurno)
		if !ok {
			object = new(HorarioTurno)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeHorarioTurno)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeHorarioTurno))
			}
		}
	} else {
		s, ok := maybeHorarioTurno.(*[]*HorarioTurno)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeHorarioTurno)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeHorarioTurno))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &horarioTurnoR{}
		}
		args[object.HortID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &horarioTurnoR{}
			}
			args[obj.HortID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`horario_ponto`),
		qm.WhereIn(`horario_ponto.hort_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load horario_ponto")
	}

	var resultSlice []*HorarioPonto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice horario_ponto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on horario_ponto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for horario_ponto")
	}

	if len(horarioPontoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.HortHorarioPontos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &horarioPontoR{}
			}
			foreign.R.Hort = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.HortID == foreign.HortID {
				local.R.HortHorarioPontos = append(local.R.HortHorarioPontos, foreign)
				if foreign.R == nil {
					foreign.R = &horarioPontoR{}
				}
				foreign.R.Hort = local
				break
			}
		}
	}

	return nil
}

// AddHortHorarioPontos adds the given related objects to the existing relationships
// of the horario_turno, optionally inserting them as new records.
// Appends related to o.R.HortHorarioPontos.
// Sets related.R.Hort appropriately.
func (o *HorarioTurno) AddHortHorarioPontos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*HorarioPonto) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.HortID = o.HortID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `horario_ponto` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"hort_id"}),
				strmangle.WhereClause("`", "`", 0, horarioPontoPrimaryKeyColumns),
			)
			values := []interface{}{o.HortID, rel.HorID, rel.HorpItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.HortID = o.HortID
		}
	}

	if o.R == nil {
		o.R = &horarioTurnoR{
			HortHorarioPontos: related,
		}
	} else {
		o.R.HortHorarioPontos = append(o.R.HortHorarioPontos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &horarioPontoR{
				Hort: o,
			}
		} else {
			rel.R.Hort = o
		}
	}
	return nil
}

// HorarioTurnos retrieves all the records using an executor.
func HorarioTurnos(mods ...qm.QueryMod) horarioTurnoQuery {
	mods = append(mods, qm.From("`horario_turno`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`horario_turno`.*"})
	}

	return horarioTurnoQuery{q}
}

// FindHorarioTurno retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindHorarioTurno(ctx context.Context, exec boil.ContextExecutor, hortID int, selectCols ...string) (*HorarioTurno, error) {
	horarioTurnoObj := &HorarioTurno{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `horario_turno` where `hort_id`=?", sel,
	)

	q := queries.Raw(query, hortID)

	err := q.Bind(ctx, exec, horarioTurnoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from horario_turno")
	}

	if err = horarioTurnoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return horarioTurnoObj, err
	}

	return horarioTurnoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *HorarioTurno) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no horario_turno provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(horarioTurnoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	horarioTurnoInsertCacheMut.RLock()
	cache, cached := horarioTurnoInsertCache[key]
	horarioTurnoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			horarioTurnoAllColumns,
			horarioTurnoColumnsWithDefault,
			horarioTurnoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(horarioTurnoType, horarioTurnoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(horarioTurnoType, horarioTurnoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `horario_turno` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `horario_turno` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `horario_turno` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, horarioTurnoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into horario_turno")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.HortID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for horario_turno")
	}

CacheNoHooks:
	if !cached {
		horarioTurnoInsertCacheMut.Lock()
		horarioTurnoInsertCache[key] = cache
		horarioTurnoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the HorarioTurno.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *HorarioTurno) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	horarioTurnoUpdateCacheMut.RLock()
	cache, cached := horarioTurnoUpdateCache[key]
	horarioTurnoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			horarioTurnoAllColumns,
			horarioTurnoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update horario_turno, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `horario_turno` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, horarioTurnoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(horarioTurnoType, horarioTurnoMapping, append(wl, horarioTurnoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update horario_turno row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for horario_turno")
	}

	if !cached {
		horarioTurnoUpdateCacheMut.Lock()
		horarioTurnoUpdateCache[key] = cache
		horarioTurnoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q horarioTurnoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for horario_turno")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for horario_turno")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o HorarioTurnoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), horarioTurnoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `horario_turno` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, horarioTurnoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in horarioTurno slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all horarioTurno")
	}
	return rowsAff, nil
}

var mySQLHorarioTurnoUniqueColumns = []string{
	"hort_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *HorarioTurno) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no horario_turno provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(horarioTurnoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLHorarioTurnoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	horarioTurnoUpsertCacheMut.RLock()
	cache, cached := horarioTurnoUpsertCache[key]
	horarioTurnoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			horarioTurnoAllColumns,
			horarioTurnoColumnsWithDefault,
			horarioTurnoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			horarioTurnoAllColumns,
			horarioTurnoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert horario_turno, could not build update column list")
		}

		ret := strmangle.SetComplement(horarioTurnoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`horario_turno`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `horario_turno` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(horarioTurnoType, horarioTurnoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(horarioTurnoType, horarioTurnoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for horario_turno")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(horarioTurnoType, horarioTurnoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for horario_turno")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for horario_turno")
	}

CacheNoHooks:
	if !cached {
		horarioTurnoUpsertCacheMut.Lock()
		horarioTurnoUpsertCache[key] = cache
		horarioTurnoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single HorarioTurno record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *HorarioTurno) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no HorarioTurno provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), horarioTurnoPrimaryKeyMapping)
	sql := "DELETE FROM `horario_turno` WHERE `hort_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from horario_turno")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for horario_turno")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q horarioTurnoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no horarioTurnoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from horario_turno")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for horario_turno")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o HorarioTurnoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(horarioTurnoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), horarioTurnoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `horario_turno` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, horarioTurnoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from horarioTurno slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for horario_turno")
	}

	if len(horarioTurnoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *HorarioTurno) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindHorarioTurno(ctx, exec, o.HortID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HorarioTurnoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := HorarioTurnoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), horarioTurnoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `horario_turno`.* FROM `horario_turno` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, horarioTurnoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in HorarioTurnoSlice")
	}

	*o = slice

	return nil
}

// HorarioTurnoExists checks if the HorarioTurno row exists.
func HorarioTurnoExists(ctx context.Context, exec boil.ContextExecutor, hortID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `horario_turno` where `hort_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, hortID)
	}
	row := exec.QueryRowContext(ctx, sql, hortID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if horario_turno exists")
	}

	return exists, nil
}

// Exists checks if the HorarioTurno row exists.
func (o *HorarioTurno) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return HorarioTurnoExists(ctx, exec, o.HortID)
}
