// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// GradeProdutoItem is an object representing the database table.
type GradeProdutoItem struct {
	GradeID  int `boil:"grade_id" json:"grade_id" toml:"grade_id" yaml:"grade_id"`
	GraiItem int `boil:"grai_item" json:"grai_item" toml:"grai_item" yaml:"grai_item"`
	CorID    int `boil:"cor_id" json:"cor_id" toml:"cor_id" yaml:"cor_id"`
	PtamID   int `boil:"ptam_id" json:"ptam_id" toml:"ptam_id" yaml:"ptam_id"`

	R *gradeProdutoItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L gradeProdutoItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var GradeProdutoItemColumns = struct {
	GradeID  string
	GraiItem string
	CorID    string
	PtamID   string
}{
	GradeID:  "grade_id",
	GraiItem: "grai_item",
	CorID:    "cor_id",
	PtamID:   "ptam_id",
}

var GradeProdutoItemTableColumns = struct {
	GradeID  string
	GraiItem string
	CorID    string
	PtamID   string
}{
	GradeID:  "grade_produto_item.grade_id",
	GraiItem: "grade_produto_item.grai_item",
	CorID:    "grade_produto_item.cor_id",
	PtamID:   "grade_produto_item.ptam_id",
}

// Generated where

var GradeProdutoItemWhere = struct {
	GradeID  whereHelperint
	GraiItem whereHelperint
	CorID    whereHelperint
	PtamID   whereHelperint
}{
	GradeID:  whereHelperint{field: "`grade_produto_item`.`grade_id`"},
	GraiItem: whereHelperint{field: "`grade_produto_item`.`grai_item`"},
	CorID:    whereHelperint{field: "`grade_produto_item`.`cor_id`"},
	PtamID:   whereHelperint{field: "`grade_produto_item`.`ptam_id`"},
}

// GradeProdutoItemRels is where relationship names are stored.
var GradeProdutoItemRels = struct {
	Cor   string
	Grade string
	Ptam  string
}{
	Cor:   "Cor",
	Grade: "Grade",
	Ptam:  "Ptam",
}

// gradeProdutoItemR is where relationships are stored.
type gradeProdutoItemR struct {
	Cor   *ProdutoCor     `boil:"Cor" json:"Cor" toml:"Cor" yaml:"Cor"`
	Grade *GradeProduto   `boil:"Grade" json:"Grade" toml:"Grade" yaml:"Grade"`
	Ptam  *ProdutoTamanho `boil:"Ptam" json:"Ptam" toml:"Ptam" yaml:"Ptam"`
}

// NewStruct creates a new relationship struct
func (*gradeProdutoItemR) NewStruct() *gradeProdutoItemR {
	return &gradeProdutoItemR{}
}

func (r *gradeProdutoItemR) GetCor() *ProdutoCor {
	if r == nil {
		return nil
	}
	return r.Cor
}

func (r *gradeProdutoItemR) GetGrade() *GradeProduto {
	if r == nil {
		return nil
	}
	return r.Grade
}

func (r *gradeProdutoItemR) GetPtam() *ProdutoTamanho {
	if r == nil {
		return nil
	}
	return r.Ptam
}

// gradeProdutoItemL is where Load methods for each relationship are stored.
type gradeProdutoItemL struct{}

var (
	gradeProdutoItemAllColumns            = []string{"grade_id", "grai_item", "cor_id", "ptam_id"}
	gradeProdutoItemColumnsWithoutDefault = []string{"grade_id", "grai_item", "cor_id", "ptam_id"}
	gradeProdutoItemColumnsWithDefault    = []string{}
	gradeProdutoItemPrimaryKeyColumns     = []string{"grade_id", "grai_item"}
	gradeProdutoItemGeneratedColumns      = []string{}
)

type (
	// GradeProdutoItemSlice is an alias for a slice of pointers to GradeProdutoItem.
	// This should almost always be used instead of []GradeProdutoItem.
	GradeProdutoItemSlice []*GradeProdutoItem
	// GradeProdutoItemHook is the signature for custom GradeProdutoItem hook methods
	GradeProdutoItemHook func(context.Context, boil.ContextExecutor, *GradeProdutoItem) error

	gradeProdutoItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	gradeProdutoItemType                 = reflect.TypeOf(&GradeProdutoItem{})
	gradeProdutoItemMapping              = queries.MakeStructMapping(gradeProdutoItemType)
	gradeProdutoItemPrimaryKeyMapping, _ = queries.BindMapping(gradeProdutoItemType, gradeProdutoItemMapping, gradeProdutoItemPrimaryKeyColumns)
	gradeProdutoItemInsertCacheMut       sync.RWMutex
	gradeProdutoItemInsertCache          = make(map[string]insertCache)
	gradeProdutoItemUpdateCacheMut       sync.RWMutex
	gradeProdutoItemUpdateCache          = make(map[string]updateCache)
	gradeProdutoItemUpsertCacheMut       sync.RWMutex
	gradeProdutoItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var gradeProdutoItemAfterSelectMu sync.Mutex
var gradeProdutoItemAfterSelectHooks []GradeProdutoItemHook

var gradeProdutoItemBeforeInsertMu sync.Mutex
var gradeProdutoItemBeforeInsertHooks []GradeProdutoItemHook
var gradeProdutoItemAfterInsertMu sync.Mutex
var gradeProdutoItemAfterInsertHooks []GradeProdutoItemHook

var gradeProdutoItemBeforeUpdateMu sync.Mutex
var gradeProdutoItemBeforeUpdateHooks []GradeProdutoItemHook
var gradeProdutoItemAfterUpdateMu sync.Mutex
var gradeProdutoItemAfterUpdateHooks []GradeProdutoItemHook

var gradeProdutoItemBeforeDeleteMu sync.Mutex
var gradeProdutoItemBeforeDeleteHooks []GradeProdutoItemHook
var gradeProdutoItemAfterDeleteMu sync.Mutex
var gradeProdutoItemAfterDeleteHooks []GradeProdutoItemHook

var gradeProdutoItemBeforeUpsertMu sync.Mutex
var gradeProdutoItemBeforeUpsertHooks []GradeProdutoItemHook
var gradeProdutoItemAfterUpsertMu sync.Mutex
var gradeProdutoItemAfterUpsertHooks []GradeProdutoItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *GradeProdutoItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *GradeProdutoItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *GradeProdutoItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *GradeProdutoItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *GradeProdutoItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *GradeProdutoItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *GradeProdutoItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *GradeProdutoItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *GradeProdutoItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range gradeProdutoItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddGradeProdutoItemHook registers your hook function for all future operations.
func AddGradeProdutoItemHook(hookPoint boil.HookPoint, gradeProdutoItemHook GradeProdutoItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		gradeProdutoItemAfterSelectMu.Lock()
		gradeProdutoItemAfterSelectHooks = append(gradeProdutoItemAfterSelectHooks, gradeProdutoItemHook)
		gradeProdutoItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		gradeProdutoItemBeforeInsertMu.Lock()
		gradeProdutoItemBeforeInsertHooks = append(gradeProdutoItemBeforeInsertHooks, gradeProdutoItemHook)
		gradeProdutoItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		gradeProdutoItemAfterInsertMu.Lock()
		gradeProdutoItemAfterInsertHooks = append(gradeProdutoItemAfterInsertHooks, gradeProdutoItemHook)
		gradeProdutoItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		gradeProdutoItemBeforeUpdateMu.Lock()
		gradeProdutoItemBeforeUpdateHooks = append(gradeProdutoItemBeforeUpdateHooks, gradeProdutoItemHook)
		gradeProdutoItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		gradeProdutoItemAfterUpdateMu.Lock()
		gradeProdutoItemAfterUpdateHooks = append(gradeProdutoItemAfterUpdateHooks, gradeProdutoItemHook)
		gradeProdutoItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		gradeProdutoItemBeforeDeleteMu.Lock()
		gradeProdutoItemBeforeDeleteHooks = append(gradeProdutoItemBeforeDeleteHooks, gradeProdutoItemHook)
		gradeProdutoItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		gradeProdutoItemAfterDeleteMu.Lock()
		gradeProdutoItemAfterDeleteHooks = append(gradeProdutoItemAfterDeleteHooks, gradeProdutoItemHook)
		gradeProdutoItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		gradeProdutoItemBeforeUpsertMu.Lock()
		gradeProdutoItemBeforeUpsertHooks = append(gradeProdutoItemBeforeUpsertHooks, gradeProdutoItemHook)
		gradeProdutoItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		gradeProdutoItemAfterUpsertMu.Lock()
		gradeProdutoItemAfterUpsertHooks = append(gradeProdutoItemAfterUpsertHooks, gradeProdutoItemHook)
		gradeProdutoItemAfterUpsertMu.Unlock()
	}
}

// One returns a single gradeProdutoItem record from the query.
func (q gradeProdutoItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*GradeProdutoItem, error) {
	o := &GradeProdutoItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for grade_produto_item")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all GradeProdutoItem records from the query.
func (q gradeProdutoItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (GradeProdutoItemSlice, error) {
	var o []*GradeProdutoItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to GradeProdutoItem slice")
	}

	if len(gradeProdutoItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all GradeProdutoItem records in the query.
func (q gradeProdutoItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count grade_produto_item rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q gradeProdutoItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if grade_produto_item exists")
	}

	return count > 0, nil
}

// Cor pointed to by the foreign key.
func (o *GradeProdutoItem) Cor(mods ...qm.QueryMod) produtoCorQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cor_id` = ?", o.CorID),
	}

	queryMods = append(queryMods, mods...)

	return ProdutoCors(queryMods...)
}

// Grade pointed to by the foreign key.
func (o *GradeProdutoItem) Grade(mods ...qm.QueryMod) gradeProdutoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`grade_id` = ?", o.GradeID),
	}

	queryMods = append(queryMods, mods...)

	return GradeProdutos(queryMods...)
}

// Ptam pointed to by the foreign key.
func (o *GradeProdutoItem) Ptam(mods ...qm.QueryMod) produtoTamanhoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ptam_id` = ?", o.PtamID),
	}

	queryMods = append(queryMods, mods...)

	return ProdutoTamanhos(queryMods...)
}

// LoadCor allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (gradeProdutoItemL) LoadCor(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGradeProdutoItem interface{}, mods queries.Applicator) error {
	var slice []*GradeProdutoItem
	var object *GradeProdutoItem

	if singular {
		var ok bool
		object, ok = maybeGradeProdutoItem.(*GradeProdutoItem)
		if !ok {
			object = new(GradeProdutoItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGradeProdutoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGradeProdutoItem))
			}
		}
	} else {
		s, ok := maybeGradeProdutoItem.(*[]*GradeProdutoItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGradeProdutoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGradeProdutoItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &gradeProdutoItemR{}
		}
		args[object.CorID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &gradeProdutoItemR{}
			}

			args[obj.CorID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_cor`),
		qm.WhereIn(`produto_cor.cor_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProdutoCor")
	}

	var resultSlice []*ProdutoCor
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProdutoCor")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto_cor")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_cor")
	}

	if len(produtoCorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cor = foreign
		if foreign.R == nil {
			foreign.R = &produtoCorR{}
		}
		foreign.R.CorGradeProdutoItems = append(foreign.R.CorGradeProdutoItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CorID == foreign.CorID {
				local.R.Cor = foreign
				if foreign.R == nil {
					foreign.R = &produtoCorR{}
				}
				foreign.R.CorGradeProdutoItems = append(foreign.R.CorGradeProdutoItems, local)
				break
			}
		}
	}

	return nil
}

// LoadGrade allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (gradeProdutoItemL) LoadGrade(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGradeProdutoItem interface{}, mods queries.Applicator) error {
	var slice []*GradeProdutoItem
	var object *GradeProdutoItem

	if singular {
		var ok bool
		object, ok = maybeGradeProdutoItem.(*GradeProdutoItem)
		if !ok {
			object = new(GradeProdutoItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGradeProdutoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGradeProdutoItem))
			}
		}
	} else {
		s, ok := maybeGradeProdutoItem.(*[]*GradeProdutoItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGradeProdutoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGradeProdutoItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &gradeProdutoItemR{}
		}
		args[object.GradeID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &gradeProdutoItemR{}
			}

			args[obj.GradeID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`grade_produto`),
		qm.WhereIn(`grade_produto.grade_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load GradeProduto")
	}

	var resultSlice []*GradeProduto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice GradeProduto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for grade_produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for grade_produto")
	}

	if len(gradeProdutoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Grade = foreign
		if foreign.R == nil {
			foreign.R = &gradeProdutoR{}
		}
		foreign.R.GradeGradeProdutoItems = append(foreign.R.GradeGradeProdutoItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.GradeID == foreign.GradeID {
				local.R.Grade = foreign
				if foreign.R == nil {
					foreign.R = &gradeProdutoR{}
				}
				foreign.R.GradeGradeProdutoItems = append(foreign.R.GradeGradeProdutoItems, local)
				break
			}
		}
	}

	return nil
}

// LoadPtam allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (gradeProdutoItemL) LoadPtam(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGradeProdutoItem interface{}, mods queries.Applicator) error {
	var slice []*GradeProdutoItem
	var object *GradeProdutoItem

	if singular {
		var ok bool
		object, ok = maybeGradeProdutoItem.(*GradeProdutoItem)
		if !ok {
			object = new(GradeProdutoItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGradeProdutoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGradeProdutoItem))
			}
		}
	} else {
		s, ok := maybeGradeProdutoItem.(*[]*GradeProdutoItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGradeProdutoItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGradeProdutoItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &gradeProdutoItemR{}
		}
		args[object.PtamID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &gradeProdutoItemR{}
			}

			args[obj.PtamID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_tamanho`),
		qm.WhereIn(`produto_tamanho.ptam_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProdutoTamanho")
	}

	var resultSlice []*ProdutoTamanho
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProdutoTamanho")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto_tamanho")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_tamanho")
	}

	if len(produtoTamanhoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ptam = foreign
		if foreign.R == nil {
			foreign.R = &produtoTamanhoR{}
		}
		foreign.R.PtamGradeProdutoItems = append(foreign.R.PtamGradeProdutoItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PtamID == foreign.PtamID {
				local.R.Ptam = foreign
				if foreign.R == nil {
					foreign.R = &produtoTamanhoR{}
				}
				foreign.R.PtamGradeProdutoItems = append(foreign.R.PtamGradeProdutoItems, local)
				break
			}
		}
	}

	return nil
}

// SetCor of the gradeProdutoItem to the related item.
// Sets o.R.Cor to related.
// Adds o to related.R.CorGradeProdutoItems.
func (o *GradeProdutoItem) SetCor(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ProdutoCor) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `grade_produto_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cor_id"}),
		strmangle.WhereClause("`", "`", 0, gradeProdutoItemPrimaryKeyColumns),
	)
	values := []interface{}{related.CorID, o.GradeID, o.GraiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CorID = related.CorID
	if o.R == nil {
		o.R = &gradeProdutoItemR{
			Cor: related,
		}
	} else {
		o.R.Cor = related
	}

	if related.R == nil {
		related.R = &produtoCorR{
			CorGradeProdutoItems: GradeProdutoItemSlice{o},
		}
	} else {
		related.R.CorGradeProdutoItems = append(related.R.CorGradeProdutoItems, o)
	}

	return nil
}

// SetGrade of the gradeProdutoItem to the related item.
// Sets o.R.Grade to related.
// Adds o to related.R.GradeGradeProdutoItems.
func (o *GradeProdutoItem) SetGrade(ctx context.Context, exec boil.ContextExecutor, insert bool, related *GradeProduto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `grade_produto_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"grade_id"}),
		strmangle.WhereClause("`", "`", 0, gradeProdutoItemPrimaryKeyColumns),
	)
	values := []interface{}{related.GradeID, o.GradeID, o.GraiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.GradeID = related.GradeID
	if o.R == nil {
		o.R = &gradeProdutoItemR{
			Grade: related,
		}
	} else {
		o.R.Grade = related
	}

	if related.R == nil {
		related.R = &gradeProdutoR{
			GradeGradeProdutoItems: GradeProdutoItemSlice{o},
		}
	} else {
		related.R.GradeGradeProdutoItems = append(related.R.GradeGradeProdutoItems, o)
	}

	return nil
}

// SetPtam of the gradeProdutoItem to the related item.
// Sets o.R.Ptam to related.
// Adds o to related.R.PtamGradeProdutoItems.
func (o *GradeProdutoItem) SetPtam(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ProdutoTamanho) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `grade_produto_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ptam_id"}),
		strmangle.WhereClause("`", "`", 0, gradeProdutoItemPrimaryKeyColumns),
	)
	values := []interface{}{related.PtamID, o.GradeID, o.GraiItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PtamID = related.PtamID
	if o.R == nil {
		o.R = &gradeProdutoItemR{
			Ptam: related,
		}
	} else {
		o.R.Ptam = related
	}

	if related.R == nil {
		related.R = &produtoTamanhoR{
			PtamGradeProdutoItems: GradeProdutoItemSlice{o},
		}
	} else {
		related.R.PtamGradeProdutoItems = append(related.R.PtamGradeProdutoItems, o)
	}

	return nil
}

// GradeProdutoItems retrieves all the records using an executor.
func GradeProdutoItems(mods ...qm.QueryMod) gradeProdutoItemQuery {
	mods = append(mods, qm.From("`grade_produto_item`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`grade_produto_item`.*"})
	}

	return gradeProdutoItemQuery{q}
}

// FindGradeProdutoItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindGradeProdutoItem(ctx context.Context, exec boil.ContextExecutor, gradeID int, graiItem int, selectCols ...string) (*GradeProdutoItem, error) {
	gradeProdutoItemObj := &GradeProdutoItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `grade_produto_item` where `grade_id`=? AND `grai_item`=?", sel,
	)

	q := queries.Raw(query, gradeID, graiItem)

	err := q.Bind(ctx, exec, gradeProdutoItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from grade_produto_item")
	}

	if err = gradeProdutoItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return gradeProdutoItemObj, err
	}

	return gradeProdutoItemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *GradeProdutoItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no grade_produto_item provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(gradeProdutoItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	gradeProdutoItemInsertCacheMut.RLock()
	cache, cached := gradeProdutoItemInsertCache[key]
	gradeProdutoItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			gradeProdutoItemAllColumns,
			gradeProdutoItemColumnsWithDefault,
			gradeProdutoItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(gradeProdutoItemType, gradeProdutoItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(gradeProdutoItemType, gradeProdutoItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `grade_produto_item` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `grade_produto_item` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `grade_produto_item` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, gradeProdutoItemPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into grade_produto_item")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.GradeID,
		o.GraiItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for grade_produto_item")
	}

CacheNoHooks:
	if !cached {
		gradeProdutoItemInsertCacheMut.Lock()
		gradeProdutoItemInsertCache[key] = cache
		gradeProdutoItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the GradeProdutoItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *GradeProdutoItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	gradeProdutoItemUpdateCacheMut.RLock()
	cache, cached := gradeProdutoItemUpdateCache[key]
	gradeProdutoItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			gradeProdutoItemAllColumns,
			gradeProdutoItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update grade_produto_item, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `grade_produto_item` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, gradeProdutoItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(gradeProdutoItemType, gradeProdutoItemMapping, append(wl, gradeProdutoItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update grade_produto_item row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for grade_produto_item")
	}

	if !cached {
		gradeProdutoItemUpdateCacheMut.Lock()
		gradeProdutoItemUpdateCache[key] = cache
		gradeProdutoItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q gradeProdutoItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for grade_produto_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for grade_produto_item")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o GradeProdutoItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), gradeProdutoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `grade_produto_item` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, gradeProdutoItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in gradeProdutoItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all gradeProdutoItem")
	}
	return rowsAff, nil
}

var mySQLGradeProdutoItemUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *GradeProdutoItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no grade_produto_item provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(gradeProdutoItemColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLGradeProdutoItemUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	gradeProdutoItemUpsertCacheMut.RLock()
	cache, cached := gradeProdutoItemUpsertCache[key]
	gradeProdutoItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			gradeProdutoItemAllColumns,
			gradeProdutoItemColumnsWithDefault,
			gradeProdutoItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			gradeProdutoItemAllColumns,
			gradeProdutoItemPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert grade_produto_item, could not build update column list")
		}

		ret := strmangle.SetComplement(gradeProdutoItemAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`grade_produto_item`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `grade_produto_item` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(gradeProdutoItemType, gradeProdutoItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(gradeProdutoItemType, gradeProdutoItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for grade_produto_item")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(gradeProdutoItemType, gradeProdutoItemMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for grade_produto_item")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for grade_produto_item")
	}

CacheNoHooks:
	if !cached {
		gradeProdutoItemUpsertCacheMut.Lock()
		gradeProdutoItemUpsertCache[key] = cache
		gradeProdutoItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single GradeProdutoItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *GradeProdutoItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no GradeProdutoItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), gradeProdutoItemPrimaryKeyMapping)
	sql := "DELETE FROM `grade_produto_item` WHERE `grade_id`=? AND `grai_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from grade_produto_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for grade_produto_item")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q gradeProdutoItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no gradeProdutoItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from grade_produto_item")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for grade_produto_item")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o GradeProdutoItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(gradeProdutoItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), gradeProdutoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `grade_produto_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, gradeProdutoItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from gradeProdutoItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for grade_produto_item")
	}

	if len(gradeProdutoItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *GradeProdutoItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindGradeProdutoItem(ctx, exec, o.GradeID, o.GraiItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GradeProdutoItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := GradeProdutoItemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), gradeProdutoItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `grade_produto_item`.* FROM `grade_produto_item` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, gradeProdutoItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in GradeProdutoItemSlice")
	}

	*o = slice

	return nil
}

// GradeProdutoItemExists checks if the GradeProdutoItem row exists.
func GradeProdutoItemExists(ctx context.Context, exec boil.ContextExecutor, gradeID int, graiItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `grade_produto_item` where `grade_id`=? AND `grai_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, gradeID, graiItem)
	}
	row := exec.QueryRowContext(ctx, sql, gradeID, graiItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if grade_produto_item exists")
	}

	return exists, nil
}

// Exists checks if the GradeProdutoItem row exists.
func (o *GradeProdutoItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return GradeProdutoItemExists(ctx, exec, o.GradeID, o.GraiItem)
}
