// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testChequeTituloBaixas(t *testing.T) {
	t.Parallel()

	query := ChequeTituloBaixas()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testChequeTituloBaixasDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testChequeTituloBaixasQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := ChequeTituloBaixas().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testChequeTituloBaixasSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ChequeTituloBaixaSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testChequeTituloBaixasExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := ChequeTituloBaixaExists(ctx, tx, o.TitID, o.TBXItem, o.ChqrecID)
	if err != nil {
		t.Errorf("Unable to check if ChequeTituloBaixa exists: %s", err)
	}
	if !e {
		t.Errorf("Expected ChequeTituloBaixaExists to return true, but got false.")
	}
}

func testChequeTituloBaixasFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	chequeTituloBaixaFound, err := FindChequeTituloBaixa(ctx, tx, o.TitID, o.TBXItem, o.ChqrecID)
	if err != nil {
		t.Error(err)
	}

	if chequeTituloBaixaFound == nil {
		t.Error("want a record, got nil")
	}
}

func testChequeTituloBaixasBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = ChequeTituloBaixas().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testChequeTituloBaixasOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := ChequeTituloBaixas().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testChequeTituloBaixasAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	chequeTituloBaixaOne := &ChequeTituloBaixa{}
	chequeTituloBaixaTwo := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, chequeTituloBaixaOne, chequeTituloBaixaDBTypes, false, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}
	if err = randomize.Struct(seed, chequeTituloBaixaTwo, chequeTituloBaixaDBTypes, false, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = chequeTituloBaixaOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = chequeTituloBaixaTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ChequeTituloBaixas().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testChequeTituloBaixasCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	chequeTituloBaixaOne := &ChequeTituloBaixa{}
	chequeTituloBaixaTwo := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, chequeTituloBaixaOne, chequeTituloBaixaDBTypes, false, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}
	if err = randomize.Struct(seed, chequeTituloBaixaTwo, chequeTituloBaixaDBTypes, false, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = chequeTituloBaixaOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = chequeTituloBaixaTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func chequeTituloBaixaBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func chequeTituloBaixaAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func chequeTituloBaixaAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func chequeTituloBaixaBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func chequeTituloBaixaAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func chequeTituloBaixaBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func chequeTituloBaixaAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func chequeTituloBaixaBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func chequeTituloBaixaAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ChequeTituloBaixa) error {
	*o = ChequeTituloBaixa{}
	return nil
}

func testChequeTituloBaixasHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &ChequeTituloBaixa{}
	o := &ChequeTituloBaixa{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, false); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa object: %s", err)
	}

	AddChequeTituloBaixaHook(boil.BeforeInsertHook, chequeTituloBaixaBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaBeforeInsertHooks = []ChequeTituloBaixaHook{}

	AddChequeTituloBaixaHook(boil.AfterInsertHook, chequeTituloBaixaAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaAfterInsertHooks = []ChequeTituloBaixaHook{}

	AddChequeTituloBaixaHook(boil.AfterSelectHook, chequeTituloBaixaAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaAfterSelectHooks = []ChequeTituloBaixaHook{}

	AddChequeTituloBaixaHook(boil.BeforeUpdateHook, chequeTituloBaixaBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaBeforeUpdateHooks = []ChequeTituloBaixaHook{}

	AddChequeTituloBaixaHook(boil.AfterUpdateHook, chequeTituloBaixaAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaAfterUpdateHooks = []ChequeTituloBaixaHook{}

	AddChequeTituloBaixaHook(boil.BeforeDeleteHook, chequeTituloBaixaBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaBeforeDeleteHooks = []ChequeTituloBaixaHook{}

	AddChequeTituloBaixaHook(boil.AfterDeleteHook, chequeTituloBaixaAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaAfterDeleteHooks = []ChequeTituloBaixaHook{}

	AddChequeTituloBaixaHook(boil.BeforeUpsertHook, chequeTituloBaixaBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaBeforeUpsertHooks = []ChequeTituloBaixaHook{}

	AddChequeTituloBaixaHook(boil.AfterUpsertHook, chequeTituloBaixaAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	chequeTituloBaixaAfterUpsertHooks = []ChequeTituloBaixaHook{}
}

func testChequeTituloBaixasInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testChequeTituloBaixasInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(chequeTituloBaixaColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testChequeTituloBaixaToOneChequesRecebidoUsingChqrec(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local ChequeTituloBaixa
	var foreign ChequesRecebido

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, chequeTituloBaixaDBTypes, false, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, chequesRecebidoDBTypes, false, chequesRecebidoColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequesRecebido struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.ChqrecID = foreign.ChqrecID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Chqrec().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ChqrecID != foreign.ChqrecID {
		t.Errorf("want: %v, got %v", foreign.ChqrecID, check.ChqrecID)
	}

	ranAfterSelectHook := false
	AddChequesRecebidoHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *ChequesRecebido) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := ChequeTituloBaixaSlice{&local}
	if err = local.L.LoadChqrec(ctx, tx, false, (*[]*ChequeTituloBaixa)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Chqrec == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Chqrec = nil
	if err = local.L.LoadChqrec(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Chqrec == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testChequeTituloBaixaToOneSetOpChequesRecebidoUsingChqrec(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a ChequeTituloBaixa
	var b, c ChequesRecebido

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, chequeTituloBaixaDBTypes, false, strmangle.SetComplement(chequeTituloBaixaPrimaryKeyColumns, chequeTituloBaixaColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, chequesRecebidoDBTypes, false, strmangle.SetComplement(chequesRecebidoPrimaryKeyColumns, chequesRecebidoColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*ChequesRecebido{&b, &c} {
		err = a.SetChqrec(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Chqrec != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ChqrecChequeTituloBaixas[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.ChqrecID != x.ChqrecID {
			t.Error("foreign key was wrong value", a.ChqrecID)
		}

		if exists, err := ChequeTituloBaixaExists(ctx, tx, a.TitID, a.TBXItem, a.ChqrecID); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}

func testChequeTituloBaixasReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testChequeTituloBaixasReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ChequeTituloBaixaSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testChequeTituloBaixasSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ChequeTituloBaixas().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	chequeTituloBaixaDBTypes = map[string]string{`TitID`: `int`, `TBXItem`: `int`, `ChqrecID`: `int`, `CHQTBXValorusado`: `decimal`, `CHQTBXCompensado`: `smallint`}
	_                        = bytes.MinRead
)

func testChequeTituloBaixasUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(chequeTituloBaixaPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(chequeTituloBaixaAllColumns) == len(chequeTituloBaixaPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testChequeTituloBaixasSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(chequeTituloBaixaAllColumns) == len(chequeTituloBaixaPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ChequeTituloBaixa{}
	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, chequeTituloBaixaDBTypes, true, chequeTituloBaixaPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(chequeTituloBaixaAllColumns, chequeTituloBaixaPrimaryKeyColumns) {
		fields = chequeTituloBaixaAllColumns
	} else {
		fields = strmangle.SetComplement(
			chequeTituloBaixaAllColumns,
			chequeTituloBaixaPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := ChequeTituloBaixaSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testChequeTituloBaixasUpsert(t *testing.T) {
	t.Parallel()

	if len(chequeTituloBaixaAllColumns) == len(chequeTituloBaixaPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLChequeTituloBaixaUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := ChequeTituloBaixa{}
	if err = randomize.Struct(seed, &o, chequeTituloBaixaDBTypes, false); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ChequeTituloBaixa: %s", err)
	}

	count, err := ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, chequeTituloBaixaDBTypes, false, chequeTituloBaixaPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ChequeTituloBaixa struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ChequeTituloBaixa: %s", err)
	}

	count, err = ChequeTituloBaixas().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
