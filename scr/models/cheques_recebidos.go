// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// ChequesRecebido is an object representing the database table.
type ChequesRecebido struct {
	ChqrecID              int           `boil:"chqrec_id" json:"chqrec_id" toml:"chqrec_id" yaml:"chqrec_id"`
	BanID                 int           `boil:"ban_id" json:"ban_id" toml:"ban_id" yaml:"ban_id"`
	EmfID                 int           `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	EntID                 int           `boil:"ent_id" json:"ent_id" toml:"ent_id" yaml:"ent_id"`
	RocID                 null.Int      `boil:"roc_id" json:"roc_id,omitempty" toml:"roc_id" yaml:"roc_id,omitempty"`
	ChqrecConta           string        `boil:"chqrec_conta" json:"chqrec_conta" toml:"chqrec_conta" yaml:"chqrec_conta"`
	ChqrecAgencia         string        `boil:"chqrec_agencia" json:"chqrec_agencia" toml:"chqrec_agencia" yaml:"chqrec_agencia"`
	ChqrecNumero          string        `boil:"chqrec_numero" json:"chqrec_numero" toml:"chqrec_numero" yaml:"chqrec_numero"`
	ChqrecValorcheque     types.Decimal `boil:"chqrec_valorcheque" json:"chqrec_valorcheque" toml:"chqrec_valorcheque" yaml:"chqrec_valorcheque"`
	ChqrecDataemissao     time.Time     `boil:"chqrec_dataemissao" json:"chqrec_dataemissao" toml:"chqrec_dataemissao" yaml:"chqrec_dataemissao"`
	ChqrecDatapre         null.Time     `boil:"chqrec_datapre" json:"chqrec_datapre,omitempty" toml:"chqrec_datapre" yaml:"chqrec_datapre,omitempty"`
	ChqrecDatacompensacao null.Time     `boil:"chqrec_datacompensacao" json:"chqrec_datacompensacao,omitempty" toml:"chqrec_datacompensacao" yaml:"chqrec_datacompensacao,omitempty"`
	// 1 - carteira
	//             2 - com ocorencia
	//             3 - rejeitado
	//             4 - compensado
	//             5 - cobrança
	//             6 - repassado
	ChqrecSituacao   null.Int16        `boil:"chqrec_situacao" json:"chqrec_situacao,omitempty" toml:"chqrec_situacao" yaml:"chqrec_situacao,omitempty"`
	ChqrecObservacao null.String       `boil:"chqrec_observacao" json:"chqrec_observacao,omitempty" toml:"chqrec_observacao" yaml:"chqrec_observacao,omitempty"`
	ChqrecValorsaldo types.NullDecimal `boil:"chqrec_valorsaldo" json:"chqrec_valorsaldo,omitempty" toml:"chqrec_valorsaldo" yaml:"chqrec_valorsaldo,omitempty"`

	R *chequesRecebidoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L chequesRecebidoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ChequesRecebidoColumns = struct {
	ChqrecID              string
	BanID                 string
	EmfID                 string
	EntID                 string
	RocID                 string
	ChqrecConta           string
	ChqrecAgencia         string
	ChqrecNumero          string
	ChqrecValorcheque     string
	ChqrecDataemissao     string
	ChqrecDatapre         string
	ChqrecDatacompensacao string
	ChqrecSituacao        string
	ChqrecObservacao      string
	ChqrecValorsaldo      string
}{
	ChqrecID:              "chqrec_id",
	BanID:                 "ban_id",
	EmfID:                 "emf_id",
	EntID:                 "ent_id",
	RocID:                 "roc_id",
	ChqrecConta:           "chqrec_conta",
	ChqrecAgencia:         "chqrec_agencia",
	ChqrecNumero:          "chqrec_numero",
	ChqrecValorcheque:     "chqrec_valorcheque",
	ChqrecDataemissao:     "chqrec_dataemissao",
	ChqrecDatapre:         "chqrec_datapre",
	ChqrecDatacompensacao: "chqrec_datacompensacao",
	ChqrecSituacao:        "chqrec_situacao",
	ChqrecObservacao:      "chqrec_observacao",
	ChqrecValorsaldo:      "chqrec_valorsaldo",
}

var ChequesRecebidoTableColumns = struct {
	ChqrecID              string
	BanID                 string
	EmfID                 string
	EntID                 string
	RocID                 string
	ChqrecConta           string
	ChqrecAgencia         string
	ChqrecNumero          string
	ChqrecValorcheque     string
	ChqrecDataemissao     string
	ChqrecDatapre         string
	ChqrecDatacompensacao string
	ChqrecSituacao        string
	ChqrecObservacao      string
	ChqrecValorsaldo      string
}{
	ChqrecID:              "cheques_recebidos.chqrec_id",
	BanID:                 "cheques_recebidos.ban_id",
	EmfID:                 "cheques_recebidos.emf_id",
	EntID:                 "cheques_recebidos.ent_id",
	RocID:                 "cheques_recebidos.roc_id",
	ChqrecConta:           "cheques_recebidos.chqrec_conta",
	ChqrecAgencia:         "cheques_recebidos.chqrec_agencia",
	ChqrecNumero:          "cheques_recebidos.chqrec_numero",
	ChqrecValorcheque:     "cheques_recebidos.chqrec_valorcheque",
	ChqrecDataemissao:     "cheques_recebidos.chqrec_dataemissao",
	ChqrecDatapre:         "cheques_recebidos.chqrec_datapre",
	ChqrecDatacompensacao: "cheques_recebidos.chqrec_datacompensacao",
	ChqrecSituacao:        "cheques_recebidos.chqrec_situacao",
	ChqrecObservacao:      "cheques_recebidos.chqrec_observacao",
	ChqrecValorsaldo:      "cheques_recebidos.chqrec_valorsaldo",
}

// Generated where

var ChequesRecebidoWhere = struct {
	ChqrecID              whereHelperint
	BanID                 whereHelperint
	EmfID                 whereHelperint
	EntID                 whereHelperint
	RocID                 whereHelpernull_Int
	ChqrecConta           whereHelperstring
	ChqrecAgencia         whereHelperstring
	ChqrecNumero          whereHelperstring
	ChqrecValorcheque     whereHelpertypes_Decimal
	ChqrecDataemissao     whereHelpertime_Time
	ChqrecDatapre         whereHelpernull_Time
	ChqrecDatacompensacao whereHelpernull_Time
	ChqrecSituacao        whereHelpernull_Int16
	ChqrecObservacao      whereHelpernull_String
	ChqrecValorsaldo      whereHelpertypes_NullDecimal
}{
	ChqrecID:              whereHelperint{field: "`cheques_recebidos`.`chqrec_id`"},
	BanID:                 whereHelperint{field: "`cheques_recebidos`.`ban_id`"},
	EmfID:                 whereHelperint{field: "`cheques_recebidos`.`emf_id`"},
	EntID:                 whereHelperint{field: "`cheques_recebidos`.`ent_id`"},
	RocID:                 whereHelpernull_Int{field: "`cheques_recebidos`.`roc_id`"},
	ChqrecConta:           whereHelperstring{field: "`cheques_recebidos`.`chqrec_conta`"},
	ChqrecAgencia:         whereHelperstring{field: "`cheques_recebidos`.`chqrec_agencia`"},
	ChqrecNumero:          whereHelperstring{field: "`cheques_recebidos`.`chqrec_numero`"},
	ChqrecValorcheque:     whereHelpertypes_Decimal{field: "`cheques_recebidos`.`chqrec_valorcheque`"},
	ChqrecDataemissao:     whereHelpertime_Time{field: "`cheques_recebidos`.`chqrec_dataemissao`"},
	ChqrecDatapre:         whereHelpernull_Time{field: "`cheques_recebidos`.`chqrec_datapre`"},
	ChqrecDatacompensacao: whereHelpernull_Time{field: "`cheques_recebidos`.`chqrec_datacompensacao`"},
	ChqrecSituacao:        whereHelpernull_Int16{field: "`cheques_recebidos`.`chqrec_situacao`"},
	ChqrecObservacao:      whereHelpernull_String{field: "`cheques_recebidos`.`chqrec_observacao`"},
	ChqrecValorsaldo:      whereHelpertypes_NullDecimal{field: "`cheques_recebidos`.`chqrec_valorsaldo`"},
}

// ChequesRecebidoRels is where relationship names are stored.
var ChequesRecebidoRels = struct {
	Ban                          string
	Emf                          string
	Ent                          string
	Roc                          string
	ChqrecChequeOcorenciaCheques string
	ChqrecChequeTituloBaixas     string
	ChqrecLancamentoFinanceiros  string
}{
	Ban:                          "Ban",
	Emf:                          "Emf",
	Ent:                          "Ent",
	Roc:                          "Roc",
	ChqrecChequeOcorenciaCheques: "ChqrecChequeOcorenciaCheques",
	ChqrecChequeTituloBaixas:     "ChqrecChequeTituloBaixas",
	ChqrecLancamentoFinanceiros:  "ChqrecLancamentoFinanceiros",
}

// chequesRecebidoR is where relationships are stored.
type chequesRecebidoR struct {
	Ban                          *Banco                     `boil:"Ban" json:"Ban" toml:"Ban" yaml:"Ban"`
	Emf                          *EmpresaFilial             `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	Ent                          *Entidade                  `boil:"Ent" json:"Ent" toml:"Ent" yaml:"Ent"`
	Roc                          *RotinaContabil            `boil:"Roc" json:"Roc" toml:"Roc" yaml:"Roc"`
	ChqrecChequeOcorenciaCheques ChequeOcorenciaChequeSlice `boil:"ChqrecChequeOcorenciaCheques" json:"ChqrecChequeOcorenciaCheques" toml:"ChqrecChequeOcorenciaCheques" yaml:"ChqrecChequeOcorenciaCheques"`
	ChqrecChequeTituloBaixas     ChequeTituloBaixaSlice     `boil:"ChqrecChequeTituloBaixas" json:"ChqrecChequeTituloBaixas" toml:"ChqrecChequeTituloBaixas" yaml:"ChqrecChequeTituloBaixas"`
	ChqrecLancamentoFinanceiros  LancamentoFinanceiroSlice  `boil:"ChqrecLancamentoFinanceiros" json:"ChqrecLancamentoFinanceiros" toml:"ChqrecLancamentoFinanceiros" yaml:"ChqrecLancamentoFinanceiros"`
}

// NewStruct creates a new relationship struct
func (*chequesRecebidoR) NewStruct() *chequesRecebidoR {
	return &chequesRecebidoR{}
}

func (r *chequesRecebidoR) GetBan() *Banco {
	if r == nil {
		return nil
	}
	return r.Ban
}

func (r *chequesRecebidoR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *chequesRecebidoR) GetEnt() *Entidade {
	if r == nil {
		return nil
	}
	return r.Ent
}

func (r *chequesRecebidoR) GetRoc() *RotinaContabil {
	if r == nil {
		return nil
	}
	return r.Roc
}

func (r *chequesRecebidoR) GetChqrecChequeOcorenciaCheques() ChequeOcorenciaChequeSlice {
	if r == nil {
		return nil
	}
	return r.ChqrecChequeOcorenciaCheques
}

func (r *chequesRecebidoR) GetChqrecChequeTituloBaixas() ChequeTituloBaixaSlice {
	if r == nil {
		return nil
	}
	return r.ChqrecChequeTituloBaixas
}

func (r *chequesRecebidoR) GetChqrecLancamentoFinanceiros() LancamentoFinanceiroSlice {
	if r == nil {
		return nil
	}
	return r.ChqrecLancamentoFinanceiros
}

// chequesRecebidoL is where Load methods for each relationship are stored.
type chequesRecebidoL struct{}

var (
	chequesRecebidoAllColumns            = []string{"chqrec_id", "ban_id", "emf_id", "ent_id", "roc_id", "chqrec_conta", "chqrec_agencia", "chqrec_numero", "chqrec_valorcheque", "chqrec_dataemissao", "chqrec_datapre", "chqrec_datacompensacao", "chqrec_situacao", "chqrec_observacao", "chqrec_valorsaldo"}
	chequesRecebidoColumnsWithoutDefault = []string{"chqrec_id", "ban_id", "emf_id", "ent_id", "roc_id", "chqrec_conta", "chqrec_agencia", "chqrec_numero", "chqrec_valorcheque", "chqrec_dataemissao", "chqrec_datapre", "chqrec_datacompensacao", "chqrec_situacao", "chqrec_observacao", "chqrec_valorsaldo"}
	chequesRecebidoColumnsWithDefault    = []string{}
	chequesRecebidoPrimaryKeyColumns     = []string{"chqrec_id"}
	chequesRecebidoGeneratedColumns      = []string{}
)

type (
	// ChequesRecebidoSlice is an alias for a slice of pointers to ChequesRecebido.
	// This should almost always be used instead of []ChequesRecebido.
	ChequesRecebidoSlice []*ChequesRecebido
	// ChequesRecebidoHook is the signature for custom ChequesRecebido hook methods
	ChequesRecebidoHook func(context.Context, boil.ContextExecutor, *ChequesRecebido) error

	chequesRecebidoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	chequesRecebidoType                 = reflect.TypeOf(&ChequesRecebido{})
	chequesRecebidoMapping              = queries.MakeStructMapping(chequesRecebidoType)
	chequesRecebidoPrimaryKeyMapping, _ = queries.BindMapping(chequesRecebidoType, chequesRecebidoMapping, chequesRecebidoPrimaryKeyColumns)
	chequesRecebidoInsertCacheMut       sync.RWMutex
	chequesRecebidoInsertCache          = make(map[string]insertCache)
	chequesRecebidoUpdateCacheMut       sync.RWMutex
	chequesRecebidoUpdateCache          = make(map[string]updateCache)
	chequesRecebidoUpsertCacheMut       sync.RWMutex
	chequesRecebidoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var chequesRecebidoAfterSelectMu sync.Mutex
var chequesRecebidoAfterSelectHooks []ChequesRecebidoHook

var chequesRecebidoBeforeInsertMu sync.Mutex
var chequesRecebidoBeforeInsertHooks []ChequesRecebidoHook
var chequesRecebidoAfterInsertMu sync.Mutex
var chequesRecebidoAfterInsertHooks []ChequesRecebidoHook

var chequesRecebidoBeforeUpdateMu sync.Mutex
var chequesRecebidoBeforeUpdateHooks []ChequesRecebidoHook
var chequesRecebidoAfterUpdateMu sync.Mutex
var chequesRecebidoAfterUpdateHooks []ChequesRecebidoHook

var chequesRecebidoBeforeDeleteMu sync.Mutex
var chequesRecebidoBeforeDeleteHooks []ChequesRecebidoHook
var chequesRecebidoAfterDeleteMu sync.Mutex
var chequesRecebidoAfterDeleteHooks []ChequesRecebidoHook

var chequesRecebidoBeforeUpsertMu sync.Mutex
var chequesRecebidoBeforeUpsertHooks []ChequesRecebidoHook
var chequesRecebidoAfterUpsertMu sync.Mutex
var chequesRecebidoAfterUpsertHooks []ChequesRecebidoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ChequesRecebido) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ChequesRecebido) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ChequesRecebido) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ChequesRecebido) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ChequesRecebido) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ChequesRecebido) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ChequesRecebido) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ChequesRecebido) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ChequesRecebido) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequesRecebidoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddChequesRecebidoHook registers your hook function for all future operations.
func AddChequesRecebidoHook(hookPoint boil.HookPoint, chequesRecebidoHook ChequesRecebidoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		chequesRecebidoAfterSelectMu.Lock()
		chequesRecebidoAfterSelectHooks = append(chequesRecebidoAfterSelectHooks, chequesRecebidoHook)
		chequesRecebidoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		chequesRecebidoBeforeInsertMu.Lock()
		chequesRecebidoBeforeInsertHooks = append(chequesRecebidoBeforeInsertHooks, chequesRecebidoHook)
		chequesRecebidoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		chequesRecebidoAfterInsertMu.Lock()
		chequesRecebidoAfterInsertHooks = append(chequesRecebidoAfterInsertHooks, chequesRecebidoHook)
		chequesRecebidoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		chequesRecebidoBeforeUpdateMu.Lock()
		chequesRecebidoBeforeUpdateHooks = append(chequesRecebidoBeforeUpdateHooks, chequesRecebidoHook)
		chequesRecebidoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		chequesRecebidoAfterUpdateMu.Lock()
		chequesRecebidoAfterUpdateHooks = append(chequesRecebidoAfterUpdateHooks, chequesRecebidoHook)
		chequesRecebidoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		chequesRecebidoBeforeDeleteMu.Lock()
		chequesRecebidoBeforeDeleteHooks = append(chequesRecebidoBeforeDeleteHooks, chequesRecebidoHook)
		chequesRecebidoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		chequesRecebidoAfterDeleteMu.Lock()
		chequesRecebidoAfterDeleteHooks = append(chequesRecebidoAfterDeleteHooks, chequesRecebidoHook)
		chequesRecebidoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		chequesRecebidoBeforeUpsertMu.Lock()
		chequesRecebidoBeforeUpsertHooks = append(chequesRecebidoBeforeUpsertHooks, chequesRecebidoHook)
		chequesRecebidoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		chequesRecebidoAfterUpsertMu.Lock()
		chequesRecebidoAfterUpsertHooks = append(chequesRecebidoAfterUpsertHooks, chequesRecebidoHook)
		chequesRecebidoAfterUpsertMu.Unlock()
	}
}

// One returns a single chequesRecebido record from the query.
func (q chequesRecebidoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ChequesRecebido, error) {
	o := &ChequesRecebido{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cheques_recebidos")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ChequesRecebido records from the query.
func (q chequesRecebidoQuery) All(ctx context.Context, exec boil.ContextExecutor) (ChequesRecebidoSlice, error) {
	var o []*ChequesRecebido

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ChequesRecebido slice")
	}

	if len(chequesRecebidoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ChequesRecebido records in the query.
func (q chequesRecebidoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cheques_recebidos rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q chequesRecebidoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cheques_recebidos exists")
	}

	return count > 0, nil
}

// Ban pointed to by the foreign key.
func (o *ChequesRecebido) Ban(mods ...qm.QueryMod) bancoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ban_id` = ?", o.BanID),
	}

	queryMods = append(queryMods, mods...)

	return Bancos(queryMods...)
}

// Emf pointed to by the foreign key.
func (o *ChequesRecebido) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// Ent pointed to by the foreign key.
func (o *ChequesRecebido) Ent(mods ...qm.QueryMod) entidadeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ent_id` = ?", o.EntID),
	}

	queryMods = append(queryMods, mods...)

	return Entidades(queryMods...)
}

// Roc pointed to by the foreign key.
func (o *ChequesRecebido) Roc(mods ...qm.QueryMod) rotinaContabilQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`roc_id` = ?", o.RocID),
	}

	queryMods = append(queryMods, mods...)

	return RotinaContabils(queryMods...)
}

// ChqrecChequeOcorenciaCheques retrieves all the cheque_ocorencia_cheque's ChequeOcorenciaCheques with an executor via chqrec_id column.
func (o *ChequesRecebido) ChqrecChequeOcorenciaCheques(mods ...qm.QueryMod) chequeOcorenciaChequeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`cheque_ocorencia_cheque`.`chqrec_id`=?", o.ChqrecID),
	)

	return ChequeOcorenciaCheques(queryMods...)
}

// ChqrecChequeTituloBaixas retrieves all the cheque_titulo_baixa's ChequeTituloBaixas with an executor via chqrec_id column.
func (o *ChequesRecebido) ChqrecChequeTituloBaixas(mods ...qm.QueryMod) chequeTituloBaixaQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`cheque_titulo_baixa`.`chqrec_id`=?", o.ChqrecID),
	)

	return ChequeTituloBaixas(queryMods...)
}

// ChqrecLancamentoFinanceiros retrieves all the lancamento_financeiro's LancamentoFinanceiros with an executor via chqrec_id column.
func (o *ChequesRecebido) ChqrecLancamentoFinanceiros(mods ...qm.QueryMod) lancamentoFinanceiroQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`lancamento_financeiro`.`chqrec_id`=?", o.ChqrecID),
	)

	return LancamentoFinanceiros(queryMods...)
}

// LoadBan allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequesRecebidoL) LoadBan(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequesRecebido interface{}, mods queries.Applicator) error {
	var slice []*ChequesRecebido
	var object *ChequesRecebido

	if singular {
		var ok bool
		object, ok = maybeChequesRecebido.(*ChequesRecebido)
		if !ok {
			object = new(ChequesRecebido)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequesRecebido))
			}
		}
	} else {
		s, ok := maybeChequesRecebido.(*[]*ChequesRecebido)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequesRecebido))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequesRecebidoR{}
		}
		args[object.BanID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequesRecebidoR{}
			}

			args[obj.BanID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`bancos`),
		qm.WhereIn(`bancos.ban_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Banco")
	}

	var resultSlice []*Banco
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Banco")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for bancos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for bancos")
	}

	if len(bancoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ban = foreign
		if foreign.R == nil {
			foreign.R = &bancoR{}
		}
		foreign.R.BanChequesRecebidos = append(foreign.R.BanChequesRecebidos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BanID == foreign.BanID {
				local.R.Ban = foreign
				if foreign.R == nil {
					foreign.R = &bancoR{}
				}
				foreign.R.BanChequesRecebidos = append(foreign.R.BanChequesRecebidos, local)
				break
			}
		}
	}

	return nil
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequesRecebidoL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequesRecebido interface{}, mods queries.Applicator) error {
	var slice []*ChequesRecebido
	var object *ChequesRecebido

	if singular {
		var ok bool
		object, ok = maybeChequesRecebido.(*ChequesRecebido)
		if !ok {
			object = new(ChequesRecebido)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequesRecebido))
			}
		}
	} else {
		s, ok := maybeChequesRecebido.(*[]*ChequesRecebido)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequesRecebido))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequesRecebidoR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequesRecebidoR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfChequesRecebidos = append(foreign.R.EmfChequesRecebidos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfChequesRecebidos = append(foreign.R.EmfChequesRecebidos, local)
				break
			}
		}
	}

	return nil
}

// LoadEnt allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequesRecebidoL) LoadEnt(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequesRecebido interface{}, mods queries.Applicator) error {
	var slice []*ChequesRecebido
	var object *ChequesRecebido

	if singular {
		var ok bool
		object, ok = maybeChequesRecebido.(*ChequesRecebido)
		if !ok {
			object = new(ChequesRecebido)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequesRecebido))
			}
		}
	} else {
		s, ok := maybeChequesRecebido.(*[]*ChequesRecebido)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequesRecebido))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequesRecebidoR{}
		}
		args[object.EntID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequesRecebidoR{}
			}

			args[obj.EntID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade`),
		qm.WhereIn(`entidade.ent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Entidade")
	}

	var resultSlice []*Entidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade")
	}

	if len(entidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ent = foreign
		if foreign.R == nil {
			foreign.R = &entidadeR{}
		}
		foreign.R.EntChequesRecebidos = append(foreign.R.EntChequesRecebidos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EntID == foreign.EntID {
				local.R.Ent = foreign
				if foreign.R == nil {
					foreign.R = &entidadeR{}
				}
				foreign.R.EntChequesRecebidos = append(foreign.R.EntChequesRecebidos, local)
				break
			}
		}
	}

	return nil
}

// LoadRoc allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequesRecebidoL) LoadRoc(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequesRecebido interface{}, mods queries.Applicator) error {
	var slice []*ChequesRecebido
	var object *ChequesRecebido

	if singular {
		var ok bool
		object, ok = maybeChequesRecebido.(*ChequesRecebido)
		if !ok {
			object = new(ChequesRecebido)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequesRecebido))
			}
		}
	} else {
		s, ok := maybeChequesRecebido.(*[]*ChequesRecebido)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequesRecebido))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequesRecebidoR{}
		}
		if !queries.IsNil(object.RocID) {
			args[object.RocID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequesRecebidoR{}
			}

			if !queries.IsNil(obj.RocID) {
				args[obj.RocID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil`),
		qm.WhereIn(`rotina_contabil.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RotinaContabil")
	}

	var resultSlice []*RotinaContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RotinaContabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for rotina_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil")
	}

	if len(rotinaContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Roc = foreign
		if foreign.R == nil {
			foreign.R = &rotinaContabilR{}
		}
		foreign.R.RocChequesRecebidos = append(foreign.R.RocChequesRecebidos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.Roc = foreign
				if foreign.R == nil {
					foreign.R = &rotinaContabilR{}
				}
				foreign.R.RocChequesRecebidos = append(foreign.R.RocChequesRecebidos, local)
				break
			}
		}
	}

	return nil
}

// LoadChqrecChequeOcorenciaCheques allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (chequesRecebidoL) LoadChqrecChequeOcorenciaCheques(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequesRecebido interface{}, mods queries.Applicator) error {
	var slice []*ChequesRecebido
	var object *ChequesRecebido

	if singular {
		var ok bool
		object, ok = maybeChequesRecebido.(*ChequesRecebido)
		if !ok {
			object = new(ChequesRecebido)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequesRecebido))
			}
		}
	} else {
		s, ok := maybeChequesRecebido.(*[]*ChequesRecebido)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequesRecebido))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequesRecebidoR{}
		}
		args[object.ChqrecID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequesRecebidoR{}
			}
			args[obj.ChqrecID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cheque_ocorencia_cheque`),
		qm.WhereIn(`cheque_ocorencia_cheque.chqrec_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load cheque_ocorencia_cheque")
	}

	var resultSlice []*ChequeOcorenciaCheque
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice cheque_ocorencia_cheque")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on cheque_ocorencia_cheque")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cheque_ocorencia_cheque")
	}

	if len(chequeOcorenciaChequeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ChqrecChequeOcorenciaCheques = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &chequeOcorenciaChequeR{}
			}
			foreign.R.Chqrec = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ChqrecID == foreign.ChqrecID {
				local.R.ChqrecChequeOcorenciaCheques = append(local.R.ChqrecChequeOcorenciaCheques, foreign)
				if foreign.R == nil {
					foreign.R = &chequeOcorenciaChequeR{}
				}
				foreign.R.Chqrec = local
				break
			}
		}
	}

	return nil
}

// LoadChqrecChequeTituloBaixas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (chequesRecebidoL) LoadChqrecChequeTituloBaixas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequesRecebido interface{}, mods queries.Applicator) error {
	var slice []*ChequesRecebido
	var object *ChequesRecebido

	if singular {
		var ok bool
		object, ok = maybeChequesRecebido.(*ChequesRecebido)
		if !ok {
			object = new(ChequesRecebido)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequesRecebido))
			}
		}
	} else {
		s, ok := maybeChequesRecebido.(*[]*ChequesRecebido)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequesRecebido))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequesRecebidoR{}
		}
		args[object.ChqrecID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequesRecebidoR{}
			}
			args[obj.ChqrecID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cheque_titulo_baixa`),
		qm.WhereIn(`cheque_titulo_baixa.chqrec_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load cheque_titulo_baixa")
	}

	var resultSlice []*ChequeTituloBaixa
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice cheque_titulo_baixa")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on cheque_titulo_baixa")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cheque_titulo_baixa")
	}

	if len(chequeTituloBaixaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ChqrecChequeTituloBaixas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &chequeTituloBaixaR{}
			}
			foreign.R.Chqrec = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ChqrecID == foreign.ChqrecID {
				local.R.ChqrecChequeTituloBaixas = append(local.R.ChqrecChequeTituloBaixas, foreign)
				if foreign.R == nil {
					foreign.R = &chequeTituloBaixaR{}
				}
				foreign.R.Chqrec = local
				break
			}
		}
	}

	return nil
}

// LoadChqrecLancamentoFinanceiros allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (chequesRecebidoL) LoadChqrecLancamentoFinanceiros(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequesRecebido interface{}, mods queries.Applicator) error {
	var slice []*ChequesRecebido
	var object *ChequesRecebido

	if singular {
		var ok bool
		object, ok = maybeChequesRecebido.(*ChequesRecebido)
		if !ok {
			object = new(ChequesRecebido)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequesRecebido))
			}
		}
	} else {
		s, ok := maybeChequesRecebido.(*[]*ChequesRecebido)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequesRecebido)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequesRecebido))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequesRecebidoR{}
		}
		args[object.ChqrecID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequesRecebidoR{}
			}
			args[obj.ChqrecID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`lancamento_financeiro`),
		qm.WhereIn(`lancamento_financeiro.chqrec_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load lancamento_financeiro")
	}

	var resultSlice []*LancamentoFinanceiro
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice lancamento_financeiro")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on lancamento_financeiro")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for lancamento_financeiro")
	}

	if len(lancamentoFinanceiroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ChqrecLancamentoFinanceiros = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &lancamentoFinanceiroR{}
			}
			foreign.R.Chqrec = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ChqrecID, foreign.ChqrecID) {
				local.R.ChqrecLancamentoFinanceiros = append(local.R.ChqrecLancamentoFinanceiros, foreign)
				if foreign.R == nil {
					foreign.R = &lancamentoFinanceiroR{}
				}
				foreign.R.Chqrec = local
				break
			}
		}
	}

	return nil
}

// SetBan of the chequesRecebido to the related item.
// Sets o.R.Ban to related.
// Adds o to related.R.BanChequesRecebidos.
func (o *ChequesRecebido) SetBan(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Banco) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheques_recebidos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ban_id"}),
		strmangle.WhereClause("`", "`", 0, chequesRecebidoPrimaryKeyColumns),
	)
	values := []interface{}{related.BanID, o.ChqrecID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BanID = related.BanID
	if o.R == nil {
		o.R = &chequesRecebidoR{
			Ban: related,
		}
	} else {
		o.R.Ban = related
	}

	if related.R == nil {
		related.R = &bancoR{
			BanChequesRecebidos: ChequesRecebidoSlice{o},
		}
	} else {
		related.R.BanChequesRecebidos = append(related.R.BanChequesRecebidos, o)
	}

	return nil
}

// SetEmf of the chequesRecebido to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfChequesRecebidos.
func (o *ChequesRecebido) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheques_recebidos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, chequesRecebidoPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.ChqrecID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &chequesRecebidoR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfChequesRecebidos: ChequesRecebidoSlice{o},
		}
	} else {
		related.R.EmfChequesRecebidos = append(related.R.EmfChequesRecebidos, o)
	}

	return nil
}

// SetEnt of the chequesRecebido to the related item.
// Sets o.R.Ent to related.
// Adds o to related.R.EntChequesRecebidos.
func (o *ChequesRecebido) SetEnt(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Entidade) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheques_recebidos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ent_id"}),
		strmangle.WhereClause("`", "`", 0, chequesRecebidoPrimaryKeyColumns),
	)
	values := []interface{}{related.EntID, o.ChqrecID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EntID = related.EntID
	if o.R == nil {
		o.R = &chequesRecebidoR{
			Ent: related,
		}
	} else {
		o.R.Ent = related
	}

	if related.R == nil {
		related.R = &entidadeR{
			EntChequesRecebidos: ChequesRecebidoSlice{o},
		}
	} else {
		related.R.EntChequesRecebidos = append(related.R.EntChequesRecebidos, o)
	}

	return nil
}

// SetRoc of the chequesRecebido to the related item.
// Sets o.R.Roc to related.
// Adds o to related.R.RocChequesRecebidos.
func (o *ChequesRecebido) SetRoc(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RotinaContabil) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheques_recebidos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
		strmangle.WhereClause("`", "`", 0, chequesRecebidoPrimaryKeyColumns),
	)
	values := []interface{}{related.RocID, o.ChqrecID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RocID, related.RocID)
	if o.R == nil {
		o.R = &chequesRecebidoR{
			Roc: related,
		}
	} else {
		o.R.Roc = related
	}

	if related.R == nil {
		related.R = &rotinaContabilR{
			RocChequesRecebidos: ChequesRecebidoSlice{o},
		}
	} else {
		related.R.RocChequesRecebidos = append(related.R.RocChequesRecebidos, o)
	}

	return nil
}

// RemoveRoc relationship.
// Sets o.R.Roc to nil.
// Removes o from all passed in related items' relationships struct.
func (o *ChequesRecebido) RemoveRoc(ctx context.Context, exec boil.ContextExecutor, related *RotinaContabil) error {
	var err error

	queries.SetScanner(&o.RocID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Roc = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RocChequesRecebidos {
		if queries.Equal(o.RocID, ri.RocID) {
			continue
		}

		ln := len(related.R.RocChequesRecebidos)
		if ln > 1 && i < ln-1 {
			related.R.RocChequesRecebidos[i] = related.R.RocChequesRecebidos[ln-1]
		}
		related.R.RocChequesRecebidos = related.R.RocChequesRecebidos[:ln-1]
		break
	}
	return nil
}

// AddChqrecChequeOcorenciaCheques adds the given related objects to the existing relationships
// of the cheques_recebido, optionally inserting them as new records.
// Appends related to o.R.ChqrecChequeOcorenciaCheques.
// Sets related.R.Chqrec appropriately.
func (o *ChequesRecebido) AddChqrecChequeOcorenciaCheques(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ChequeOcorenciaCheque) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.ChqrecID = o.ChqrecID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `cheque_ocorencia_cheque` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"chqrec_id"}),
				strmangle.WhereClause("`", "`", 0, chequeOcorenciaChequePrimaryKeyColumns),
			)
			values := []interface{}{o.ChqrecID, rel.ChqrecID, rel.ChqochItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.ChqrecID = o.ChqrecID
		}
	}

	if o.R == nil {
		o.R = &chequesRecebidoR{
			ChqrecChequeOcorenciaCheques: related,
		}
	} else {
		o.R.ChqrecChequeOcorenciaCheques = append(o.R.ChqrecChequeOcorenciaCheques, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &chequeOcorenciaChequeR{
				Chqrec: o,
			}
		} else {
			rel.R.Chqrec = o
		}
	}
	return nil
}

// AddChqrecChequeTituloBaixas adds the given related objects to the existing relationships
// of the cheques_recebido, optionally inserting them as new records.
// Appends related to o.R.ChqrecChequeTituloBaixas.
// Sets related.R.Chqrec appropriately.
func (o *ChequesRecebido) AddChqrecChequeTituloBaixas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ChequeTituloBaixa) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.ChqrecID = o.ChqrecID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `cheque_titulo_baixa` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"chqrec_id"}),
				strmangle.WhereClause("`", "`", 0, chequeTituloBaixaPrimaryKeyColumns),
			)
			values := []interface{}{o.ChqrecID, rel.TitID, rel.TBXItem, rel.ChqrecID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.ChqrecID = o.ChqrecID
		}
	}

	if o.R == nil {
		o.R = &chequesRecebidoR{
			ChqrecChequeTituloBaixas: related,
		}
	} else {
		o.R.ChqrecChequeTituloBaixas = append(o.R.ChqrecChequeTituloBaixas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &chequeTituloBaixaR{
				Chqrec: o,
			}
		} else {
			rel.R.Chqrec = o
		}
	}
	return nil
}

// AddChqrecLancamentoFinanceiros adds the given related objects to the existing relationships
// of the cheques_recebido, optionally inserting them as new records.
// Appends related to o.R.ChqrecLancamentoFinanceiros.
// Sets related.R.Chqrec appropriately.
func (o *ChequesRecebido) AddChqrecLancamentoFinanceiros(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoFinanceiro) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.ChqrecID, o.ChqrecID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `lancamento_financeiro` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"chqrec_id"}),
				strmangle.WhereClause("`", "`", 0, lancamentoFinanceiroPrimaryKeyColumns),
			)
			values := []interface{}{o.ChqrecID, rel.LfinID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.ChqrecID, o.ChqrecID)
		}
	}

	if o.R == nil {
		o.R = &chequesRecebidoR{
			ChqrecLancamentoFinanceiros: related,
		}
	} else {
		o.R.ChqrecLancamentoFinanceiros = append(o.R.ChqrecLancamentoFinanceiros, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &lancamentoFinanceiroR{
				Chqrec: o,
			}
		} else {
			rel.R.Chqrec = o
		}
	}
	return nil
}

// SetChqrecLancamentoFinanceiros removes all previously related items of the
// cheques_recebido replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Chqrec's ChqrecLancamentoFinanceiros accordingly.
// Replaces o.R.ChqrecLancamentoFinanceiros with related.
// Sets related.R.Chqrec's ChqrecLancamentoFinanceiros accordingly.
func (o *ChequesRecebido) SetChqrecLancamentoFinanceiros(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoFinanceiro) error {
	query := "update `lancamento_financeiro` set `chqrec_id` = null where `chqrec_id` = ?"
	values := []interface{}{o.ChqrecID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.ChqrecLancamentoFinanceiros {
			queries.SetScanner(&rel.ChqrecID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Chqrec = nil
		}
		o.R.ChqrecLancamentoFinanceiros = nil
	}

	return o.AddChqrecLancamentoFinanceiros(ctx, exec, insert, related...)
}

// RemoveChqrecLancamentoFinanceiros relationships from objects passed in.
// Removes related items from R.ChqrecLancamentoFinanceiros (uses pointer comparison, removal does not keep order)
// Sets related.R.Chqrec.
func (o *ChequesRecebido) RemoveChqrecLancamentoFinanceiros(ctx context.Context, exec boil.ContextExecutor, related ...*LancamentoFinanceiro) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.ChqrecID, nil)
		if rel.R != nil {
			rel.R.Chqrec = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("chqrec_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.ChqrecLancamentoFinanceiros {
			if rel != ri {
				continue
			}

			ln := len(o.R.ChqrecLancamentoFinanceiros)
			if ln > 1 && i < ln-1 {
				o.R.ChqrecLancamentoFinanceiros[i] = o.R.ChqrecLancamentoFinanceiros[ln-1]
			}
			o.R.ChqrecLancamentoFinanceiros = o.R.ChqrecLancamentoFinanceiros[:ln-1]
			break
		}
	}

	return nil
}

// ChequesRecebidos retrieves all the records using an executor.
func ChequesRecebidos(mods ...qm.QueryMod) chequesRecebidoQuery {
	mods = append(mods, qm.From("`cheques_recebidos`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`cheques_recebidos`.*"})
	}

	return chequesRecebidoQuery{q}
}

// FindChequesRecebido retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindChequesRecebido(ctx context.Context, exec boil.ContextExecutor, chqrecID int, selectCols ...string) (*ChequesRecebido, error) {
	chequesRecebidoObj := &ChequesRecebido{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cheques_recebidos` where `chqrec_id`=?", sel,
	)

	q := queries.Raw(query, chqrecID)

	err := q.Bind(ctx, exec, chequesRecebidoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cheques_recebidos")
	}

	if err = chequesRecebidoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return chequesRecebidoObj, err
	}

	return chequesRecebidoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ChequesRecebido) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cheques_recebidos provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(chequesRecebidoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	chequesRecebidoInsertCacheMut.RLock()
	cache, cached := chequesRecebidoInsertCache[key]
	chequesRecebidoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			chequesRecebidoAllColumns,
			chequesRecebidoColumnsWithDefault,
			chequesRecebidoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(chequesRecebidoType, chequesRecebidoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(chequesRecebidoType, chequesRecebidoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cheques_recebidos` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cheques_recebidos` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cheques_recebidos` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, chequesRecebidoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cheques_recebidos")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ChqrecID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cheques_recebidos")
	}

CacheNoHooks:
	if !cached {
		chequesRecebidoInsertCacheMut.Lock()
		chequesRecebidoInsertCache[key] = cache
		chequesRecebidoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ChequesRecebido.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ChequesRecebido) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	chequesRecebidoUpdateCacheMut.RLock()
	cache, cached := chequesRecebidoUpdateCache[key]
	chequesRecebidoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			chequesRecebidoAllColumns,
			chequesRecebidoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cheques_recebidos, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cheques_recebidos` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, chequesRecebidoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(chequesRecebidoType, chequesRecebidoMapping, append(wl, chequesRecebidoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cheques_recebidos row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cheques_recebidos")
	}

	if !cached {
		chequesRecebidoUpdateCacheMut.Lock()
		chequesRecebidoUpdateCache[key] = cache
		chequesRecebidoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q chequesRecebidoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cheques_recebidos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cheques_recebidos")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ChequesRecebidoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequesRecebidoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cheques_recebidos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequesRecebidoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in chequesRecebido slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all chequesRecebido")
	}
	return rowsAff, nil
}

var mySQLChequesRecebidoUniqueColumns = []string{
	"chqrec_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ChequesRecebido) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cheques_recebidos provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(chequesRecebidoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLChequesRecebidoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	chequesRecebidoUpsertCacheMut.RLock()
	cache, cached := chequesRecebidoUpsertCache[key]
	chequesRecebidoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			chequesRecebidoAllColumns,
			chequesRecebidoColumnsWithDefault,
			chequesRecebidoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			chequesRecebidoAllColumns,
			chequesRecebidoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cheques_recebidos, could not build update column list")
		}

		ret := strmangle.SetComplement(chequesRecebidoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`cheques_recebidos`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cheques_recebidos` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(chequesRecebidoType, chequesRecebidoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(chequesRecebidoType, chequesRecebidoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cheques_recebidos")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(chequesRecebidoType, chequesRecebidoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cheques_recebidos")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cheques_recebidos")
	}

CacheNoHooks:
	if !cached {
		chequesRecebidoUpsertCacheMut.Lock()
		chequesRecebidoUpsertCache[key] = cache
		chequesRecebidoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ChequesRecebido record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ChequesRecebido) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ChequesRecebido provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), chequesRecebidoPrimaryKeyMapping)
	sql := "DELETE FROM `cheques_recebidos` WHERE `chqrec_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cheques_recebidos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cheques_recebidos")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q chequesRecebidoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no chequesRecebidoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cheques_recebidos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cheques_recebidos")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ChequesRecebidoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(chequesRecebidoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequesRecebidoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cheques_recebidos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequesRecebidoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from chequesRecebido slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cheques_recebidos")
	}

	if len(chequesRecebidoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ChequesRecebido) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindChequesRecebido(ctx, exec, o.ChqrecID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ChequesRecebidoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ChequesRecebidoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequesRecebidoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cheques_recebidos`.* FROM `cheques_recebidos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequesRecebidoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ChequesRecebidoSlice")
	}

	*o = slice

	return nil
}

// ChequesRecebidoExists checks if the ChequesRecebido row exists.
func ChequesRecebidoExists(ctx context.Context, exec boil.ContextExecutor, chqrecID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cheques_recebidos` where `chqrec_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, chqrecID)
	}
	row := exec.QueryRowContext(ctx, sql, chqrecID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cheques_recebidos exists")
	}

	return exists, nil
}

// Exists checks if the ChequesRecebido row exists.
func (o *ChequesRecebido) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ChequesRecebidoExists(ctx, exec, o.ChqrecID)
}
