// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Despesas is an object representing the database table.
type Despesas struct {
	DespID           int         `boil:"desp_id" json:"desp_id" toml:"desp_id" yaml:"desp_id"`
	PCFID            int         `boil:"pcf_id" json:"pcf_id" toml:"pcf_id" yaml:"pcf_id"`
	DespDescricao    string      `boil:"desp_descricao" json:"desp_descricao" toml:"desp_descricao" yaml:"desp_descricao"`
	DespSituacao     int16       `boil:"desp_situacao" json:"desp_situacao" toml:"desp_situacao" yaml:"desp_situacao"`
	DespFixaVariavel int16       `boil:"desp_fixa_variavel" json:"desp_fixa_variavel" toml:"desp_fixa_variavel" yaml:"desp_fixa_variavel"`
	DespMascara      null.String `boil:"desp_mascara" json:"desp_mascara,omitempty" toml:"desp_mascara" yaml:"desp_mascara,omitempty"`
	// 1 - sitetica 2 - analitica
	DespEspecie null.Int `boil:"desp_especie" json:"desp_especie,omitempty" toml:"desp_especie" yaml:"desp_especie,omitempty"`

	R *despesasR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L despesasL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DespesasColumns = struct {
	DespID           string
	PCFID            string
	DespDescricao    string
	DespSituacao     string
	DespFixaVariavel string
	DespMascara      string
	DespEspecie      string
}{
	DespID:           "desp_id",
	PCFID:            "pcf_id",
	DespDescricao:    "desp_descricao",
	DespSituacao:     "desp_situacao",
	DespFixaVariavel: "desp_fixa_variavel",
	DespMascara:      "desp_mascara",
	DespEspecie:      "desp_especie",
}

var DespesasTableColumns = struct {
	DespID           string
	PCFID            string
	DespDescricao    string
	DespSituacao     string
	DespFixaVariavel string
	DespMascara      string
	DespEspecie      string
}{
	DespID:           "despesas.desp_id",
	PCFID:            "despesas.pcf_id",
	DespDescricao:    "despesas.desp_descricao",
	DespSituacao:     "despesas.desp_situacao",
	DespFixaVariavel: "despesas.desp_fixa_variavel",
	DespMascara:      "despesas.desp_mascara",
	DespEspecie:      "despesas.desp_especie",
}

// Generated where

var DespesasWhere = struct {
	DespID           whereHelperint
	PCFID            whereHelperint
	DespDescricao    whereHelperstring
	DespSituacao     whereHelperint16
	DespFixaVariavel whereHelperint16
	DespMascara      whereHelpernull_String
	DespEspecie      whereHelpernull_Int
}{
	DespID:           whereHelperint{field: "`despesas`.`desp_id`"},
	PCFID:            whereHelperint{field: "`despesas`.`pcf_id`"},
	DespDescricao:    whereHelperstring{field: "`despesas`.`desp_descricao`"},
	DespSituacao:     whereHelperint16{field: "`despesas`.`desp_situacao`"},
	DespFixaVariavel: whereHelperint16{field: "`despesas`.`desp_fixa_variavel`"},
	DespMascara:      whereHelpernull_String{field: "`despesas`.`desp_mascara`"},
	DespEspecie:      whereHelpernull_Int{field: "`despesas`.`desp_especie`"},
}

// DespesasRels is where relationship names are stored.
var DespesasRels = struct {
	PCF                            string
	DespContratoEntidadeTipos      string
	DespCustoOperacionalProdutos   string
	DespDespesaCentroDeCustos      string
	DespLancamentoDespesas         string
	DespOrcamentoVendaOperacionals string
	DespProcessos                  string
	DespProdutoFichaoperacionals   string
	ProsgProdutoSubgrupos          string
	DespProdutoTipoprodutos        string
	DespTituloBaixas               string
	DespTitulos                    string
}{
	PCF:                            "PCF",
	DespContratoEntidadeTipos:      "DespContratoEntidadeTipos",
	DespCustoOperacionalProdutos:   "DespCustoOperacionalProdutos",
	DespDespesaCentroDeCustos:      "DespDespesaCentroDeCustos",
	DespLancamentoDespesas:         "DespLancamentoDespesas",
	DespOrcamentoVendaOperacionals: "DespOrcamentoVendaOperacionals",
	DespProcessos:                  "DespProcessos",
	DespProdutoFichaoperacionals:   "DespProdutoFichaoperacionals",
	ProsgProdutoSubgrupos:          "ProsgProdutoSubgrupos",
	DespProdutoTipoprodutos:        "DespProdutoTipoprodutos",
	DespTituloBaixas:               "DespTituloBaixas",
	DespTitulos:                    "DespTitulos",
}

// despesasR is where relationships are stored.
type despesasR struct {
	PCF                            *PlanoContasFinanceiro         `boil:"PCF" json:"PCF" toml:"PCF" yaml:"PCF"`
	DespContratoEntidadeTipos      ContratoEntidadeTipoSlice      `boil:"DespContratoEntidadeTipos" json:"DespContratoEntidadeTipos" toml:"DespContratoEntidadeTipos" yaml:"DespContratoEntidadeTipos"`
	DespCustoOperacionalProdutos   CustoOperacionalProdutoSlice   `boil:"DespCustoOperacionalProdutos" json:"DespCustoOperacionalProdutos" toml:"DespCustoOperacionalProdutos" yaml:"DespCustoOperacionalProdutos"`
	DespDespesaCentroDeCustos      DespesaCentroDeCustoSlice      `boil:"DespDespesaCentroDeCustos" json:"DespDespesaCentroDeCustos" toml:"DespDespesaCentroDeCustos" yaml:"DespDespesaCentroDeCustos"`
	DespLancamentoDespesas         LancamentoDespesaSlice         `boil:"DespLancamentoDespesas" json:"DespLancamentoDespesas" toml:"DespLancamentoDespesas" yaml:"DespLancamentoDespesas"`
	DespOrcamentoVendaOperacionals OrcamentoVendaOperacionalSlice `boil:"DespOrcamentoVendaOperacionals" json:"DespOrcamentoVendaOperacionals" toml:"DespOrcamentoVendaOperacionals" yaml:"DespOrcamentoVendaOperacionals"`
	DespProcessos                  ProcessoSlice                  `boil:"DespProcessos" json:"DespProcessos" toml:"DespProcessos" yaml:"DespProcessos"`
	DespProdutoFichaoperacionals   ProdutoFichaoperacionalSlice   `boil:"DespProdutoFichaoperacionals" json:"DespProdutoFichaoperacionals" toml:"DespProdutoFichaoperacionals" yaml:"DespProdutoFichaoperacionals"`
	ProsgProdutoSubgrupos          ProdutoSubgrupoSlice           `boil:"ProsgProdutoSubgrupos" json:"ProsgProdutoSubgrupos" toml:"ProsgProdutoSubgrupos" yaml:"ProsgProdutoSubgrupos"`
	DespProdutoTipoprodutos        ProdutoTipoprodutoSlice        `boil:"DespProdutoTipoprodutos" json:"DespProdutoTipoprodutos" toml:"DespProdutoTipoprodutos" yaml:"DespProdutoTipoprodutos"`
	DespTituloBaixas               TituloBaixasSlice              `boil:"DespTituloBaixas" json:"DespTituloBaixas" toml:"DespTituloBaixas" yaml:"DespTituloBaixas"`
	DespTitulos                    TituloSlice                    `boil:"DespTitulos" json:"DespTitulos" toml:"DespTitulos" yaml:"DespTitulos"`
}

// NewStruct creates a new relationship struct
func (*despesasR) NewStruct() *despesasR {
	return &despesasR{}
}

func (r *despesasR) GetPCF() *PlanoContasFinanceiro {
	if r == nil {
		return nil
	}
	return r.PCF
}

func (r *despesasR) GetDespContratoEntidadeTipos() ContratoEntidadeTipoSlice {
	if r == nil {
		return nil
	}
	return r.DespContratoEntidadeTipos
}

func (r *despesasR) GetDespCustoOperacionalProdutos() CustoOperacionalProdutoSlice {
	if r == nil {
		return nil
	}
	return r.DespCustoOperacionalProdutos
}

func (r *despesasR) GetDespDespesaCentroDeCustos() DespesaCentroDeCustoSlice {
	if r == nil {
		return nil
	}
	return r.DespDespesaCentroDeCustos
}

func (r *despesasR) GetDespLancamentoDespesas() LancamentoDespesaSlice {
	if r == nil {
		return nil
	}
	return r.DespLancamentoDespesas
}

func (r *despesasR) GetDespOrcamentoVendaOperacionals() OrcamentoVendaOperacionalSlice {
	if r == nil {
		return nil
	}
	return r.DespOrcamentoVendaOperacionals
}

func (r *despesasR) GetDespProcessos() ProcessoSlice {
	if r == nil {
		return nil
	}
	return r.DespProcessos
}

func (r *despesasR) GetDespProdutoFichaoperacionals() ProdutoFichaoperacionalSlice {
	if r == nil {
		return nil
	}
	return r.DespProdutoFichaoperacionals
}

func (r *despesasR) GetProsgProdutoSubgrupos() ProdutoSubgrupoSlice {
	if r == nil {
		return nil
	}
	return r.ProsgProdutoSubgrupos
}

func (r *despesasR) GetDespProdutoTipoprodutos() ProdutoTipoprodutoSlice {
	if r == nil {
		return nil
	}
	return r.DespProdutoTipoprodutos
}

func (r *despesasR) GetDespTituloBaixas() TituloBaixasSlice {
	if r == nil {
		return nil
	}
	return r.DespTituloBaixas
}

func (r *despesasR) GetDespTitulos() TituloSlice {
	if r == nil {
		return nil
	}
	return r.DespTitulos
}

// despesasL is where Load methods for each relationship are stored.
type despesasL struct{}

var (
	despesasAllColumns            = []string{"desp_id", "pcf_id", "desp_descricao", "desp_situacao", "desp_fixa_variavel", "desp_mascara", "desp_especie"}
	despesasColumnsWithoutDefault = []string{"desp_id", "pcf_id", "desp_descricao", "desp_fixa_variavel", "desp_mascara", "desp_especie"}
	despesasColumnsWithDefault    = []string{"desp_situacao"}
	despesasPrimaryKeyColumns     = []string{"desp_id"}
	despesasGeneratedColumns      = []string{}
)

type (
	// DespesasSlice is an alias for a slice of pointers to Despesas.
	// This should almost always be used instead of []Despesas.
	DespesasSlice []*Despesas
	// DespesasHook is the signature for custom Despesas hook methods
	DespesasHook func(context.Context, boil.ContextExecutor, *Despesas) error

	despesasQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	despesasType                 = reflect.TypeOf(&Despesas{})
	despesasMapping              = queries.MakeStructMapping(despesasType)
	despesasPrimaryKeyMapping, _ = queries.BindMapping(despesasType, despesasMapping, despesasPrimaryKeyColumns)
	despesasInsertCacheMut       sync.RWMutex
	despesasInsertCache          = make(map[string]insertCache)
	despesasUpdateCacheMut       sync.RWMutex
	despesasUpdateCache          = make(map[string]updateCache)
	despesasUpsertCacheMut       sync.RWMutex
	despesasUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var despesasAfterSelectMu sync.Mutex
var despesasAfterSelectHooks []DespesasHook

var despesasBeforeInsertMu sync.Mutex
var despesasBeforeInsertHooks []DespesasHook
var despesasAfterInsertMu sync.Mutex
var despesasAfterInsertHooks []DespesasHook

var despesasBeforeUpdateMu sync.Mutex
var despesasBeforeUpdateHooks []DespesasHook
var despesasAfterUpdateMu sync.Mutex
var despesasAfterUpdateHooks []DespesasHook

var despesasBeforeDeleteMu sync.Mutex
var despesasBeforeDeleteHooks []DespesasHook
var despesasAfterDeleteMu sync.Mutex
var despesasAfterDeleteHooks []DespesasHook

var despesasBeforeUpsertMu sync.Mutex
var despesasBeforeUpsertHooks []DespesasHook
var despesasAfterUpsertMu sync.Mutex
var despesasAfterUpsertHooks []DespesasHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Despesas) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Despesas) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Despesas) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Despesas) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Despesas) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Despesas) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Despesas) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Despesas) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Despesas) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range despesasAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDespesasHook registers your hook function for all future operations.
func AddDespesasHook(hookPoint boil.HookPoint, despesasHook DespesasHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		despesasAfterSelectMu.Lock()
		despesasAfterSelectHooks = append(despesasAfterSelectHooks, despesasHook)
		despesasAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		despesasBeforeInsertMu.Lock()
		despesasBeforeInsertHooks = append(despesasBeforeInsertHooks, despesasHook)
		despesasBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		despesasAfterInsertMu.Lock()
		despesasAfterInsertHooks = append(despesasAfterInsertHooks, despesasHook)
		despesasAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		despesasBeforeUpdateMu.Lock()
		despesasBeforeUpdateHooks = append(despesasBeforeUpdateHooks, despesasHook)
		despesasBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		despesasAfterUpdateMu.Lock()
		despesasAfterUpdateHooks = append(despesasAfterUpdateHooks, despesasHook)
		despesasAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		despesasBeforeDeleteMu.Lock()
		despesasBeforeDeleteHooks = append(despesasBeforeDeleteHooks, despesasHook)
		despesasBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		despesasAfterDeleteMu.Lock()
		despesasAfterDeleteHooks = append(despesasAfterDeleteHooks, despesasHook)
		despesasAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		despesasBeforeUpsertMu.Lock()
		despesasBeforeUpsertHooks = append(despesasBeforeUpsertHooks, despesasHook)
		despesasBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		despesasAfterUpsertMu.Lock()
		despesasAfterUpsertHooks = append(despesasAfterUpsertHooks, despesasHook)
		despesasAfterUpsertMu.Unlock()
	}
}

// One returns a single despesas record from the query.
func (q despesasQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Despesas, error) {
	o := &Despesas{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for despesas")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Despesas records from the query.
func (q despesasQuery) All(ctx context.Context, exec boil.ContextExecutor) (DespesasSlice, error) {
	var o []*Despesas

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Despesas slice")
	}

	if len(despesasAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Despesas records in the query.
func (q despesasQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count despesas rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q despesasQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if despesas exists")
	}

	return count > 0, nil
}

// PCF pointed to by the foreign key.
func (o *Despesas) PCF(mods ...qm.QueryMod) planoContasFinanceiroQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pcf_id` = ?", o.PCFID),
	}

	queryMods = append(queryMods, mods...)

	return PlanoContasFinanceiros(queryMods...)
}

// DespContratoEntidadeTipos retrieves all the contrato_entidade_tipo's ContratoEntidadeTipos with an executor via desp_id column.
func (o *Despesas) DespContratoEntidadeTipos(mods ...qm.QueryMod) contratoEntidadeTipoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`contrato_entidade_tipo`.`desp_id`=?", o.DespID),
	)

	return ContratoEntidadeTipos(queryMods...)
}

// DespCustoOperacionalProdutos retrieves all the custo_operacional_produto's CustoOperacionalProdutos with an executor via desp_id column.
func (o *Despesas) DespCustoOperacionalProdutos(mods ...qm.QueryMod) custoOperacionalProdutoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`custo_operacional_produto`.`desp_id`=?", o.DespID),
	)

	return CustoOperacionalProdutos(queryMods...)
}

// DespDespesaCentroDeCustos retrieves all the despesa_centro_de_custo's DespesaCentroDeCustos with an executor via desp_id column.
func (o *Despesas) DespDespesaCentroDeCustos(mods ...qm.QueryMod) despesaCentroDeCustoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`despesa_centro_de_custo`.`desp_id`=?", o.DespID),
	)

	return DespesaCentroDeCustos(queryMods...)
}

// DespLancamentoDespesas retrieves all the lancamento_despesa's LancamentoDespesas with an executor via desp_id column.
func (o *Despesas) DespLancamentoDespesas(mods ...qm.QueryMod) lancamentoDespesaQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`lancamento_despesa`.`desp_id`=?", o.DespID),
	)

	return LancamentoDespesas(queryMods...)
}

// DespOrcamentoVendaOperacionals retrieves all the orcamento_venda_operacional's OrcamentoVendaOperacionals with an executor via desp_id column.
func (o *Despesas) DespOrcamentoVendaOperacionals(mods ...qm.QueryMod) orcamentoVendaOperacionalQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`orcamento_venda_operacional`.`desp_id`=?", o.DespID),
	)

	return OrcamentoVendaOperacionals(queryMods...)
}

// DespProcessos retrieves all the processo's Processos with an executor via desp_id column.
func (o *Despesas) DespProcessos(mods ...qm.QueryMod) processoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`processo`.`desp_id`=?", o.DespID),
	)

	return Processos(queryMods...)
}

// DespProdutoFichaoperacionals retrieves all the produto_fichaoperacional's ProdutoFichaoperacionals with an executor via desp_id column.
func (o *Despesas) DespProdutoFichaoperacionals(mods ...qm.QueryMod) produtoFichaoperacionalQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto_fichaoperacional`.`desp_id`=?", o.DespID),
	)

	return ProdutoFichaoperacionals(queryMods...)
}

// ProsgProdutoSubgrupos retrieves all the produto_subgrupo's ProdutoSubgrupos with an executor via prosg_id column.
func (o *Despesas) ProsgProdutoSubgrupos(mods ...qm.QueryMod) produtoSubgrupoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("`produto_subgrupo_despesa` on `produto_subgrupo`.`prosg_id` = `produto_subgrupo_despesa`.`prosg_id`"),
		qm.Where("`produto_subgrupo_despesa`.`desp_id`=?", o.DespID),
	)

	return ProdutoSubgrupos(queryMods...)
}

// DespProdutoTipoprodutos retrieves all the produto_tipoproduto's ProdutoTipoprodutos with an executor via desp_id column.
func (o *Despesas) DespProdutoTipoprodutos(mods ...qm.QueryMod) produtoTipoprodutoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto_tipoproduto`.`desp_id`=?", o.DespID),
	)

	return ProdutoTipoprodutos(queryMods...)
}

// DespTituloBaixas retrieves all the titulo_baixas's TituloBaixas with an executor via desp_id column.
func (o *Despesas) DespTituloBaixas(mods ...qm.QueryMod) tituloBaixasQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`titulo_baixas`.`desp_id`=?", o.DespID),
	)

	return TituloBaixas(queryMods...)
}

// DespTitulos retrieves all the titulo's Titulos with an executor via desp_id column.
func (o *Despesas) DespTitulos(mods ...qm.QueryMod) tituloQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`titulos`.`desp_id`=?", o.DespID),
	)

	return Titulos(queryMods...)
}

// LoadPCF allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (despesasL) LoadPCF(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.PCFID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}

			args[obj.PCFID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`plano_contas_financeiro`),
		qm.WhereIn(`plano_contas_financeiro.pcf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PlanoContasFinanceiro")
	}

	var resultSlice []*PlanoContasFinanceiro
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PlanoContasFinanceiro")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for plano_contas_financeiro")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for plano_contas_financeiro")
	}

	if len(planoContasFinanceiroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PCF = foreign
		if foreign.R == nil {
			foreign.R = &planoContasFinanceiroR{}
		}
		foreign.R.PCFDespesas = append(foreign.R.PCFDespesas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PCFID == foreign.PCFID {
				local.R.PCF = foreign
				if foreign.R == nil {
					foreign.R = &planoContasFinanceiroR{}
				}
				foreign.R.PCFDespesas = append(foreign.R.PCFDespesas, local)
				break
			}
		}
	}

	return nil
}

// LoadDespContratoEntidadeTipos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespContratoEntidadeTipos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`contrato_entidade_tipo`),
		qm.WhereIn(`contrato_entidade_tipo.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load contrato_entidade_tipo")
	}

	var resultSlice []*ContratoEntidadeTipo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice contrato_entidade_tipo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on contrato_entidade_tipo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for contrato_entidade_tipo")
	}

	if len(contratoEntidadeTipoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespContratoEntidadeTipos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &contratoEntidadeTipoR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.DespID, foreign.DespID) {
				local.R.DespContratoEntidadeTipos = append(local.R.DespContratoEntidadeTipos, foreign)
				if foreign.R == nil {
					foreign.R = &contratoEntidadeTipoR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadDespCustoOperacionalProdutos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespCustoOperacionalProdutos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`custo_operacional_produto`),
		qm.WhereIn(`custo_operacional_produto.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load custo_operacional_produto")
	}

	var resultSlice []*CustoOperacionalProduto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice custo_operacional_produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on custo_operacional_produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for custo_operacional_produto")
	}

	if len(custoOperacionalProdutoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespCustoOperacionalProdutos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &custoOperacionalProdutoR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.DespID == foreign.DespID {
				local.R.DespCustoOperacionalProdutos = append(local.R.DespCustoOperacionalProdutos, foreign)
				if foreign.R == nil {
					foreign.R = &custoOperacionalProdutoR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadDespDespesaCentroDeCustos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespDespesaCentroDeCustos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`despesa_centro_de_custo`),
		qm.WhereIn(`despesa_centro_de_custo.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load despesa_centro_de_custo")
	}

	var resultSlice []*DespesaCentroDeCusto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice despesa_centro_de_custo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on despesa_centro_de_custo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for despesa_centro_de_custo")
	}

	if len(despesaCentroDeCustoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespDespesaCentroDeCustos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &despesaCentroDeCustoR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.DespID == foreign.DespID {
				local.R.DespDespesaCentroDeCustos = append(local.R.DespDespesaCentroDeCustos, foreign)
				if foreign.R == nil {
					foreign.R = &despesaCentroDeCustoR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadDespLancamentoDespesas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespLancamentoDespesas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`lancamento_despesa`),
		qm.WhereIn(`lancamento_despesa.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load lancamento_despesa")
	}

	var resultSlice []*LancamentoDespesa
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice lancamento_despesa")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on lancamento_despesa")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for lancamento_despesa")
	}

	if len(lancamentoDespesaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespLancamentoDespesas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &lancamentoDespesaR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.DespID == foreign.DespID {
				local.R.DespLancamentoDespesas = append(local.R.DespLancamentoDespesas, foreign)
				if foreign.R == nil {
					foreign.R = &lancamentoDespesaR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadDespOrcamentoVendaOperacionals allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespOrcamentoVendaOperacionals(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`orcamento_venda_operacional`),
		qm.WhereIn(`orcamento_venda_operacional.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load orcamento_venda_operacional")
	}

	var resultSlice []*OrcamentoVendaOperacional
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice orcamento_venda_operacional")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on orcamento_venda_operacional")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for orcamento_venda_operacional")
	}

	if len(orcamentoVendaOperacionalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespOrcamentoVendaOperacionals = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &orcamentoVendaOperacionalR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.DespID == foreign.DespID {
				local.R.DespOrcamentoVendaOperacionals = append(local.R.DespOrcamentoVendaOperacionals, foreign)
				if foreign.R == nil {
					foreign.R = &orcamentoVendaOperacionalR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadDespProcessos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespProcessos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`processo`),
		qm.WhereIn(`processo.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load processo")
	}

	var resultSlice []*Processo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice processo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on processo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for processo")
	}

	if len(processoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespProcessos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &processoR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.DespID, foreign.DespID) {
				local.R.DespProcessos = append(local.R.DespProcessos, foreign)
				if foreign.R == nil {
					foreign.R = &processoR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadDespProdutoFichaoperacionals allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespProdutoFichaoperacionals(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_fichaoperacional`),
		qm.WhereIn(`produto_fichaoperacional.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto_fichaoperacional")
	}

	var resultSlice []*ProdutoFichaoperacional
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto_fichaoperacional")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto_fichaoperacional")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_fichaoperacional")
	}

	if len(produtoFichaoperacionalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespProdutoFichaoperacionals = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoFichaoperacionalR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.DespID == foreign.DespID {
				local.R.DespProdutoFichaoperacionals = append(local.R.DespProdutoFichaoperacionals, foreign)
				if foreign.R == nil {
					foreign.R = &produtoFichaoperacionalR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadProsgProdutoSubgrupos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadProsgProdutoSubgrupos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.Select("`produto_subgrupo`.`prosg_id`, `produto_subgrupo`.`prosg_descricao`, `produto_subgrupo`.`prosg_situacao`, `a`.`desp_id`"),
		qm.From("`produto_subgrupo`"),
		qm.InnerJoin("`produto_subgrupo_despesa` as `a` on `produto_subgrupo`.`prosg_id` = `a`.`prosg_id`"),
		qm.WhereIn("`a`.`desp_id` in ?", argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto_subgrupo")
	}

	var resultSlice []*ProdutoSubgrupo

	var localJoinCols []int
	for results.Next() {
		one := new(ProdutoSubgrupo)
		var localJoinCol int

		err = results.Scan(&one.ProsgID, &one.ProsgDescricao, &one.ProsgSituacao, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for produto_subgrupo")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice produto_subgrupo")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto_subgrupo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_subgrupo")
	}

	if len(produtoSubgrupoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ProsgProdutoSubgrupos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoSubgrupoR{}
			}
			foreign.R.DespDespesas = append(foreign.R.DespDespesas, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.DespID == localJoinCol {
				local.R.ProsgProdutoSubgrupos = append(local.R.ProsgProdutoSubgrupos, foreign)
				if foreign.R == nil {
					foreign.R = &produtoSubgrupoR{}
				}
				foreign.R.DespDespesas = append(foreign.R.DespDespesas, local)
				break
			}
		}
	}

	return nil
}

// LoadDespProdutoTipoprodutos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespProdutoTipoprodutos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_tipoproduto`),
		qm.WhereIn(`produto_tipoproduto.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto_tipoproduto")
	}

	var resultSlice []*ProdutoTipoproduto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto_tipoproduto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto_tipoproduto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_tipoproduto")
	}

	if len(produtoTipoprodutoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespProdutoTipoprodutos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoTipoprodutoR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.DespID, foreign.DespID) {
				local.R.DespProdutoTipoprodutos = append(local.R.DespProdutoTipoprodutos, foreign)
				if foreign.R == nil {
					foreign.R = &produtoTipoprodutoR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadDespTituloBaixas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespTituloBaixas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulo_baixas`),
		qm.WhereIn(`titulo_baixas.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load titulo_baixas")
	}

	var resultSlice []*TituloBaixas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice titulo_baixas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on titulo_baixas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulo_baixas")
	}

	if len(tituloBaixasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespTituloBaixas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &tituloBaixasR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.DespID, foreign.DespID) {
				local.R.DespTituloBaixas = append(local.R.DespTituloBaixas, foreign)
				if foreign.R == nil {
					foreign.R = &tituloBaixasR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// LoadDespTitulos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (despesasL) LoadDespTitulos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDespesas interface{}, mods queries.Applicator) error {
	var slice []*Despesas
	var object *Despesas

	if singular {
		var ok bool
		object, ok = maybeDespesas.(*Despesas)
		if !ok {
			object = new(Despesas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDespesas))
			}
		}
	} else {
		s, ok := maybeDespesas.(*[]*Despesas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDespesas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDespesas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &despesasR{}
		}
		args[object.DespID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &despesasR{}
			}
			args[obj.DespID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulos`),
		qm.WhereIn(`titulos.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load titulos")
	}

	var resultSlice []*Titulo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice titulos")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on titulos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulos")
	}

	if len(tituloAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DespTitulos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &tituloR{}
			}
			foreign.R.Desp = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.DespID, foreign.DespID) {
				local.R.DespTitulos = append(local.R.DespTitulos, foreign)
				if foreign.R == nil {
					foreign.R = &tituloR{}
				}
				foreign.R.Desp = local
				break
			}
		}
	}

	return nil
}

// SetPCF of the despesas to the related item.
// Sets o.R.PCF to related.
// Adds o to related.R.PCFDespesas.
func (o *Despesas) SetPCF(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PlanoContasFinanceiro) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `despesas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pcf_id"}),
		strmangle.WhereClause("`", "`", 0, despesasPrimaryKeyColumns),
	)
	values := []interface{}{related.PCFID, o.DespID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PCFID = related.PCFID
	if o.R == nil {
		o.R = &despesasR{
			PCF: related,
		}
	} else {
		o.R.PCF = related
	}

	if related.R == nil {
		related.R = &planoContasFinanceiroR{
			PCFDespesas: DespesasSlice{o},
		}
	} else {
		related.R.PCFDespesas = append(related.R.PCFDespesas, o)
	}

	return nil
}

// AddDespContratoEntidadeTipos adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespContratoEntidadeTipos.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespContratoEntidadeTipos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ContratoEntidadeTipo) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.DespID, o.DespID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `contrato_entidade_tipo` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, contratoEntidadeTipoPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.CetID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.DespID, o.DespID)
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespContratoEntidadeTipos: related,
		}
	} else {
		o.R.DespContratoEntidadeTipos = append(o.R.DespContratoEntidadeTipos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &contratoEntidadeTipoR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// SetDespContratoEntidadeTipos removes all previously related items of the
// despesas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Desp's DespContratoEntidadeTipos accordingly.
// Replaces o.R.DespContratoEntidadeTipos with related.
// Sets related.R.Desp's DespContratoEntidadeTipos accordingly.
func (o *Despesas) SetDespContratoEntidadeTipos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ContratoEntidadeTipo) error {
	query := "update `contrato_entidade_tipo` set `desp_id` = null where `desp_id` = ?"
	values := []interface{}{o.DespID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.DespContratoEntidadeTipos {
			queries.SetScanner(&rel.DespID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Desp = nil
		}
		o.R.DespContratoEntidadeTipos = nil
	}

	return o.AddDespContratoEntidadeTipos(ctx, exec, insert, related...)
}

// RemoveDespContratoEntidadeTipos relationships from objects passed in.
// Removes related items from R.DespContratoEntidadeTipos (uses pointer comparison, removal does not keep order)
// Sets related.R.Desp.
func (o *Despesas) RemoveDespContratoEntidadeTipos(ctx context.Context, exec boil.ContextExecutor, related ...*ContratoEntidadeTipo) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.DespID, nil)
		if rel.R != nil {
			rel.R.Desp = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("desp_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.DespContratoEntidadeTipos {
			if rel != ri {
				continue
			}

			ln := len(o.R.DespContratoEntidadeTipos)
			if ln > 1 && i < ln-1 {
				o.R.DespContratoEntidadeTipos[i] = o.R.DespContratoEntidadeTipos[ln-1]
			}
			o.R.DespContratoEntidadeTipos = o.R.DespContratoEntidadeTipos[:ln-1]
			break
		}
	}

	return nil
}

// AddDespCustoOperacionalProdutos adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespCustoOperacionalProdutos.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespCustoOperacionalProdutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CustoOperacionalProduto) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.DespID = o.DespID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `custo_operacional_produto` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, custoOperacionalProdutoPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.CopID, rel.DespID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.DespID = o.DespID
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespCustoOperacionalProdutos: related,
		}
	} else {
		o.R.DespCustoOperacionalProdutos = append(o.R.DespCustoOperacionalProdutos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &custoOperacionalProdutoR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// AddDespDespesaCentroDeCustos adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespDespesaCentroDeCustos.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespDespesaCentroDeCustos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DespesaCentroDeCusto) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.DespID = o.DespID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `despesa_centro_de_custo` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, despesaCentroDeCustoPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.CDCID, rel.DespID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.DespID = o.DespID
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespDespesaCentroDeCustos: related,
		}
	} else {
		o.R.DespDespesaCentroDeCustos = append(o.R.DespDespesaCentroDeCustos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &despesaCentroDeCustoR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// AddDespLancamentoDespesas adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespLancamentoDespesas.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespLancamentoDespesas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoDespesa) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.DespID = o.DespID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `lancamento_despesa` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, lancamentoDespesaPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.LdespID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.DespID = o.DespID
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespLancamentoDespesas: related,
		}
	} else {
		o.R.DespLancamentoDespesas = append(o.R.DespLancamentoDespesas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &lancamentoDespesaR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// AddDespOrcamentoVendaOperacionals adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespOrcamentoVendaOperacionals.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespOrcamentoVendaOperacionals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*OrcamentoVendaOperacional) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.DespID = o.DespID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `orcamento_venda_operacional` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, orcamentoVendaOperacionalPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.OrcvID, rel.OrcvoItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.DespID = o.DespID
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespOrcamentoVendaOperacionals: related,
		}
	} else {
		o.R.DespOrcamentoVendaOperacionals = append(o.R.DespOrcamentoVendaOperacionals, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &orcamentoVendaOperacionalR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// AddDespProcessos adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespProcessos.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespProcessos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Processo) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.DespID, o.DespID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `processo` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, processoPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.PRCID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.DespID, o.DespID)
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespProcessos: related,
		}
	} else {
		o.R.DespProcessos = append(o.R.DespProcessos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &processoR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// SetDespProcessos removes all previously related items of the
// despesas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Desp's DespProcessos accordingly.
// Replaces o.R.DespProcessos with related.
// Sets related.R.Desp's DespProcessos accordingly.
func (o *Despesas) SetDespProcessos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Processo) error {
	query := "update `processo` set `desp_id` = null where `desp_id` = ?"
	values := []interface{}{o.DespID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.DespProcessos {
			queries.SetScanner(&rel.DespID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Desp = nil
		}
		o.R.DespProcessos = nil
	}

	return o.AddDespProcessos(ctx, exec, insert, related...)
}

// RemoveDespProcessos relationships from objects passed in.
// Removes related items from R.DespProcessos (uses pointer comparison, removal does not keep order)
// Sets related.R.Desp.
func (o *Despesas) RemoveDespProcessos(ctx context.Context, exec boil.ContextExecutor, related ...*Processo) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.DespID, nil)
		if rel.R != nil {
			rel.R.Desp = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("desp_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.DespProcessos {
			if rel != ri {
				continue
			}

			ln := len(o.R.DespProcessos)
			if ln > 1 && i < ln-1 {
				o.R.DespProcessos[i] = o.R.DespProcessos[ln-1]
			}
			o.R.DespProcessos = o.R.DespProcessos[:ln-1]
			break
		}
	}

	return nil
}

// AddDespProdutoFichaoperacionals adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespProdutoFichaoperacionals.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespProdutoFichaoperacionals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoFichaoperacional) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.DespID = o.DespID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto_fichaoperacional` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, produtoFichaoperacionalPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.ProID, rel.DespID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.DespID = o.DespID
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespProdutoFichaoperacionals: related,
		}
	} else {
		o.R.DespProdutoFichaoperacionals = append(o.R.DespProdutoFichaoperacionals, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoFichaoperacionalR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// AddProsgProdutoSubgrupos adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.ProsgProdutoSubgrupos.
// Sets related.R.DespDespesas appropriately.
func (o *Despesas) AddProsgProdutoSubgrupos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoSubgrupo) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into `produto_subgrupo_despesa` (`desp_id`, `prosg_id`) values (?, ?)"
		values := []interface{}{o.DespID, rel.ProsgID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &despesasR{
			ProsgProdutoSubgrupos: related,
		}
	} else {
		o.R.ProsgProdutoSubgrupos = append(o.R.ProsgProdutoSubgrupos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoSubgrupoR{
				DespDespesas: DespesasSlice{o},
			}
		} else {
			rel.R.DespDespesas = append(rel.R.DespDespesas, o)
		}
	}
	return nil
}

// SetProsgProdutoSubgrupos removes all previously related items of the
// despesas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.DespDespesas's ProsgProdutoSubgrupos accordingly.
// Replaces o.R.ProsgProdutoSubgrupos with related.
// Sets related.R.DespDespesas's ProsgProdutoSubgrupos accordingly.
func (o *Despesas) SetProsgProdutoSubgrupos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoSubgrupo) error {
	query := "delete from `produto_subgrupo_despesa` where `desp_id` = ?"
	values := []interface{}{o.DespID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeProsgProdutoSubgruposFromDespDespesasSlice(o, related)
	if o.R != nil {
		o.R.ProsgProdutoSubgrupos = nil
	}

	return o.AddProsgProdutoSubgrupos(ctx, exec, insert, related...)
}

// RemoveProsgProdutoSubgrupos relationships from objects passed in.
// Removes related items from R.ProsgProdutoSubgrupos (uses pointer comparison, removal does not keep order)
// Sets related.R.DespDespesas.
func (o *Despesas) RemoveProsgProdutoSubgrupos(ctx context.Context, exec boil.ContextExecutor, related ...*ProdutoSubgrupo) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from `produto_subgrupo_despesa` where `desp_id` = ? and `prosg_id` in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.DespID}
	for _, rel := range related {
		values = append(values, rel.ProsgID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeProsgProdutoSubgruposFromDespDespesasSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.ProsgProdutoSubgrupos {
			if rel != ri {
				continue
			}

			ln := len(o.R.ProsgProdutoSubgrupos)
			if ln > 1 && i < ln-1 {
				o.R.ProsgProdutoSubgrupos[i] = o.R.ProsgProdutoSubgrupos[ln-1]
			}
			o.R.ProsgProdutoSubgrupos = o.R.ProsgProdutoSubgrupos[:ln-1]
			break
		}
	}

	return nil
}

func removeProsgProdutoSubgruposFromDespDespesasSlice(o *Despesas, related []*ProdutoSubgrupo) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.DespDespesas {
			if o.DespID != ri.DespID {
				continue
			}

			ln := len(rel.R.DespDespesas)
			if ln > 1 && i < ln-1 {
				rel.R.DespDespesas[i] = rel.R.DespDespesas[ln-1]
			}
			rel.R.DespDespesas = rel.R.DespDespesas[:ln-1]
			break
		}
	}
}

// AddDespProdutoTipoprodutos adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespProdutoTipoprodutos.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespProdutoTipoprodutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoTipoproduto) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.DespID, o.DespID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto_tipoproduto` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, produtoTipoprodutoPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.PTPID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.DespID, o.DespID)
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespProdutoTipoprodutos: related,
		}
	} else {
		o.R.DespProdutoTipoprodutos = append(o.R.DespProdutoTipoprodutos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoTipoprodutoR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// SetDespProdutoTipoprodutos removes all previously related items of the
// despesas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Desp's DespProdutoTipoprodutos accordingly.
// Replaces o.R.DespProdutoTipoprodutos with related.
// Sets related.R.Desp's DespProdutoTipoprodutos accordingly.
func (o *Despesas) SetDespProdutoTipoprodutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoTipoproduto) error {
	query := "update `produto_tipoproduto` set `desp_id` = null where `desp_id` = ?"
	values := []interface{}{o.DespID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.DespProdutoTipoprodutos {
			queries.SetScanner(&rel.DespID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Desp = nil
		}
		o.R.DespProdutoTipoprodutos = nil
	}

	return o.AddDespProdutoTipoprodutos(ctx, exec, insert, related...)
}

// RemoveDespProdutoTipoprodutos relationships from objects passed in.
// Removes related items from R.DespProdutoTipoprodutos (uses pointer comparison, removal does not keep order)
// Sets related.R.Desp.
func (o *Despesas) RemoveDespProdutoTipoprodutos(ctx context.Context, exec boil.ContextExecutor, related ...*ProdutoTipoproduto) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.DespID, nil)
		if rel.R != nil {
			rel.R.Desp = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("desp_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.DespProdutoTipoprodutos {
			if rel != ri {
				continue
			}

			ln := len(o.R.DespProdutoTipoprodutos)
			if ln > 1 && i < ln-1 {
				o.R.DespProdutoTipoprodutos[i] = o.R.DespProdutoTipoprodutos[ln-1]
			}
			o.R.DespProdutoTipoprodutos = o.R.DespProdutoTipoprodutos[:ln-1]
			break
		}
	}

	return nil
}

// AddDespTituloBaixas adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespTituloBaixas.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespTituloBaixas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TituloBaixas) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.DespID, o.DespID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `titulo_baixas` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.TitID, rel.TBXItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.DespID, o.DespID)
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespTituloBaixas: related,
		}
	} else {
		o.R.DespTituloBaixas = append(o.R.DespTituloBaixas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &tituloBaixasR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// SetDespTituloBaixas removes all previously related items of the
// despesas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Desp's DespTituloBaixas accordingly.
// Replaces o.R.DespTituloBaixas with related.
// Sets related.R.Desp's DespTituloBaixas accordingly.
func (o *Despesas) SetDespTituloBaixas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TituloBaixas) error {
	query := "update `titulo_baixas` set `desp_id` = null where `desp_id` = ?"
	values := []interface{}{o.DespID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.DespTituloBaixas {
			queries.SetScanner(&rel.DespID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Desp = nil
		}
		o.R.DespTituloBaixas = nil
	}

	return o.AddDespTituloBaixas(ctx, exec, insert, related...)
}

// RemoveDespTituloBaixas relationships from objects passed in.
// Removes related items from R.DespTituloBaixas (uses pointer comparison, removal does not keep order)
// Sets related.R.Desp.
func (o *Despesas) RemoveDespTituloBaixas(ctx context.Context, exec boil.ContextExecutor, related ...*TituloBaixas) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.DespID, nil)
		if rel.R != nil {
			rel.R.Desp = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("desp_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.DespTituloBaixas {
			if rel != ri {
				continue
			}

			ln := len(o.R.DespTituloBaixas)
			if ln > 1 && i < ln-1 {
				o.R.DespTituloBaixas[i] = o.R.DespTituloBaixas[ln-1]
			}
			o.R.DespTituloBaixas = o.R.DespTituloBaixas[:ln-1]
			break
		}
	}

	return nil
}

// AddDespTitulos adds the given related objects to the existing relationships
// of the despesas, optionally inserting them as new records.
// Appends related to o.R.DespTitulos.
// Sets related.R.Desp appropriately.
func (o *Despesas) AddDespTitulos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Titulo) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.DespID, o.DespID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `titulos` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
				strmangle.WhereClause("`", "`", 0, tituloPrimaryKeyColumns),
			)
			values := []interface{}{o.DespID, rel.TitID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.DespID, o.DespID)
		}
	}

	if o.R == nil {
		o.R = &despesasR{
			DespTitulos: related,
		}
	} else {
		o.R.DespTitulos = append(o.R.DespTitulos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &tituloR{
				Desp: o,
			}
		} else {
			rel.R.Desp = o
		}
	}
	return nil
}

// SetDespTitulos removes all previously related items of the
// despesas replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Desp's DespTitulos accordingly.
// Replaces o.R.DespTitulos with related.
// Sets related.R.Desp's DespTitulos accordingly.
func (o *Despesas) SetDespTitulos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Titulo) error {
	query := "update `titulos` set `desp_id` = null where `desp_id` = ?"
	values := []interface{}{o.DespID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.DespTitulos {
			queries.SetScanner(&rel.DespID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Desp = nil
		}
		o.R.DespTitulos = nil
	}

	return o.AddDespTitulos(ctx, exec, insert, related...)
}

// RemoveDespTitulos relationships from objects passed in.
// Removes related items from R.DespTitulos (uses pointer comparison, removal does not keep order)
// Sets related.R.Desp.
func (o *Despesas) RemoveDespTitulos(ctx context.Context, exec boil.ContextExecutor, related ...*Titulo) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.DespID, nil)
		if rel.R != nil {
			rel.R.Desp = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("desp_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.DespTitulos {
			if rel != ri {
				continue
			}

			ln := len(o.R.DespTitulos)
			if ln > 1 && i < ln-1 {
				o.R.DespTitulos[i] = o.R.DespTitulos[ln-1]
			}
			o.R.DespTitulos = o.R.DespTitulos[:ln-1]
			break
		}
	}

	return nil
}

// Despesas retrieves all the records using an executor.
func Despesas(mods ...qm.QueryMod) despesasQuery {
	mods = append(mods, qm.From("`despesas`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`despesas`.*"})
	}

	return despesasQuery{q}
}

// FindDespesas retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDespesas(ctx context.Context, exec boil.ContextExecutor, despID int, selectCols ...string) (*Despesas, error) {
	despesasObj := &Despesas{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `despesas` where `desp_id`=?", sel,
	)

	q := queries.Raw(query, despID)

	err := q.Bind(ctx, exec, despesasObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from despesas")
	}

	if err = despesasObj.doAfterSelectHooks(ctx, exec); err != nil {
		return despesasObj, err
	}

	return despesasObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Despesas) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no despesas provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(despesasColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	despesasInsertCacheMut.RLock()
	cache, cached := despesasInsertCache[key]
	despesasInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			despesasAllColumns,
			despesasColumnsWithDefault,
			despesasColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(despesasType, despesasMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(despesasType, despesasMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `despesas` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `despesas` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `despesas` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, despesasPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into despesas")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DespID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for despesas")
	}

CacheNoHooks:
	if !cached {
		despesasInsertCacheMut.Lock()
		despesasInsertCache[key] = cache
		despesasInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Despesas.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Despesas) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	despesasUpdateCacheMut.RLock()
	cache, cached := despesasUpdateCache[key]
	despesasUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			despesasAllColumns,
			despesasPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update despesas, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `despesas` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, despesasPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(despesasType, despesasMapping, append(wl, despesasPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update despesas row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for despesas")
	}

	if !cached {
		despesasUpdateCacheMut.Lock()
		despesasUpdateCache[key] = cache
		despesasUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q despesasQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for despesas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for despesas")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DespesasSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), despesasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `despesas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, despesasPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in despesas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all despesas")
	}
	return rowsAff, nil
}

var mySQLDespesasUniqueColumns = []string{
	"desp_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Despesas) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no despesas provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(despesasColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDespesasUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	despesasUpsertCacheMut.RLock()
	cache, cached := despesasUpsertCache[key]
	despesasUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			despesasAllColumns,
			despesasColumnsWithDefault,
			despesasColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			despesasAllColumns,
			despesasPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert despesas, could not build update column list")
		}

		ret := strmangle.SetComplement(despesasAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`despesas`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `despesas` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(despesasType, despesasMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(despesasType, despesasMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for despesas")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(despesasType, despesasMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for despesas")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for despesas")
	}

CacheNoHooks:
	if !cached {
		despesasUpsertCacheMut.Lock()
		despesasUpsertCache[key] = cache
		despesasUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Despesas record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Despesas) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Despesas provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), despesasPrimaryKeyMapping)
	sql := "DELETE FROM `despesas` WHERE `desp_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from despesas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for despesas")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q despesasQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no despesasQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from despesas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for despesas")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DespesasSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(despesasBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), despesasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `despesas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, despesasPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from despesas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for despesas")
	}

	if len(despesasAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Despesas) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDespesas(ctx, exec, o.DespID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DespesasSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DespesasSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), despesasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `despesas`.* FROM `despesas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, despesasPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DespesasSlice")
	}

	*o = slice

	return nil
}

// DespesasExists checks if the Despesas row exists.
func DespesasExists(ctx context.Context, exec boil.ContextExecutor, despID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `despesas` where `desp_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, despID)
	}
	row := exec.QueryRowContext(ctx, sql, despID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if despesas exists")
	}

	return exists, nil
}

// Exists checks if the Despesas row exists.
func (o *Despesas) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DespesasExists(ctx, exec, o.DespID)
}
