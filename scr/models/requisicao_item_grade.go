// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// RequisicaoItemGrade is an object representing the database table.
type RequisicaoItemGrade struct {
	ReqID          int           `boil:"req_id" json:"req_id" toml:"req_id" yaml:"req_id"`
	ReiItem        int           `boil:"rei_item" json:"rei_item" toml:"rei_item" yaml:"rei_item"`
	ReigItem       int           `boil:"reig_item" json:"reig_item" toml:"reig_item" yaml:"reig_item"`
	CorID          int           `boil:"cor_id" json:"cor_id" toml:"cor_id" yaml:"cor_id"`
	PtamID         int           `boil:"ptam_id" json:"ptam_id" toml:"ptam_id" yaml:"ptam_id"`
	ReigQuantidade types.Decimal `boil:"reig_quantidade" json:"reig_quantidade" toml:"reig_quantidade" yaml:"reig_quantidade"`

	R *requisicaoItemGradeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L requisicaoItemGradeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RequisicaoItemGradeColumns = struct {
	ReqID          string
	ReiItem        string
	ReigItem       string
	CorID          string
	PtamID         string
	ReigQuantidade string
}{
	ReqID:          "req_id",
	ReiItem:        "rei_item",
	ReigItem:       "reig_item",
	CorID:          "cor_id",
	PtamID:         "ptam_id",
	ReigQuantidade: "reig_quantidade",
}

var RequisicaoItemGradeTableColumns = struct {
	ReqID          string
	ReiItem        string
	ReigItem       string
	CorID          string
	PtamID         string
	ReigQuantidade string
}{
	ReqID:          "requisicao_item_grade.req_id",
	ReiItem:        "requisicao_item_grade.rei_item",
	ReigItem:       "requisicao_item_grade.reig_item",
	CorID:          "requisicao_item_grade.cor_id",
	PtamID:         "requisicao_item_grade.ptam_id",
	ReigQuantidade: "requisicao_item_grade.reig_quantidade",
}

// Generated where

var RequisicaoItemGradeWhere = struct {
	ReqID          whereHelperint
	ReiItem        whereHelperint
	ReigItem       whereHelperint
	CorID          whereHelperint
	PtamID         whereHelperint
	ReigQuantidade whereHelpertypes_Decimal
}{
	ReqID:          whereHelperint{field: "`requisicao_item_grade`.`req_id`"},
	ReiItem:        whereHelperint{field: "`requisicao_item_grade`.`rei_item`"},
	ReigItem:       whereHelperint{field: "`requisicao_item_grade`.`reig_item`"},
	CorID:          whereHelperint{field: "`requisicao_item_grade`.`cor_id`"},
	PtamID:         whereHelperint{field: "`requisicao_item_grade`.`ptam_id`"},
	ReigQuantidade: whereHelpertypes_Decimal{field: "`requisicao_item_grade`.`reig_quantidade`"},
}

// RequisicaoItemGradeRels is where relationship names are stored.
var RequisicaoItemGradeRels = struct {
	Cor  string
	Ptam string
}{
	Cor:  "Cor",
	Ptam: "Ptam",
}

// requisicaoItemGradeR is where relationships are stored.
type requisicaoItemGradeR struct {
	Cor  *ProdutoCor     `boil:"Cor" json:"Cor" toml:"Cor" yaml:"Cor"`
	Ptam *ProdutoTamanho `boil:"Ptam" json:"Ptam" toml:"Ptam" yaml:"Ptam"`
}

// NewStruct creates a new relationship struct
func (*requisicaoItemGradeR) NewStruct() *requisicaoItemGradeR {
	return &requisicaoItemGradeR{}
}

func (r *requisicaoItemGradeR) GetCor() *ProdutoCor {
	if r == nil {
		return nil
	}
	return r.Cor
}

func (r *requisicaoItemGradeR) GetPtam() *ProdutoTamanho {
	if r == nil {
		return nil
	}
	return r.Ptam
}

// requisicaoItemGradeL is where Load methods for each relationship are stored.
type requisicaoItemGradeL struct{}

var (
	requisicaoItemGradeAllColumns            = []string{"req_id", "rei_item", "reig_item", "cor_id", "ptam_id", "reig_quantidade"}
	requisicaoItemGradeColumnsWithoutDefault = []string{"req_id", "rei_item", "reig_item", "cor_id", "ptam_id"}
	requisicaoItemGradeColumnsWithDefault    = []string{"reig_quantidade"}
	requisicaoItemGradePrimaryKeyColumns     = []string{"req_id", "rei_item", "reig_item"}
	requisicaoItemGradeGeneratedColumns      = []string{}
)

type (
	// RequisicaoItemGradeSlice is an alias for a slice of pointers to RequisicaoItemGrade.
	// This should almost always be used instead of []RequisicaoItemGrade.
	RequisicaoItemGradeSlice []*RequisicaoItemGrade
	// RequisicaoItemGradeHook is the signature for custom RequisicaoItemGrade hook methods
	RequisicaoItemGradeHook func(context.Context, boil.ContextExecutor, *RequisicaoItemGrade) error

	requisicaoItemGradeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	requisicaoItemGradeType                 = reflect.TypeOf(&RequisicaoItemGrade{})
	requisicaoItemGradeMapping              = queries.MakeStructMapping(requisicaoItemGradeType)
	requisicaoItemGradePrimaryKeyMapping, _ = queries.BindMapping(requisicaoItemGradeType, requisicaoItemGradeMapping, requisicaoItemGradePrimaryKeyColumns)
	requisicaoItemGradeInsertCacheMut       sync.RWMutex
	requisicaoItemGradeInsertCache          = make(map[string]insertCache)
	requisicaoItemGradeUpdateCacheMut       sync.RWMutex
	requisicaoItemGradeUpdateCache          = make(map[string]updateCache)
	requisicaoItemGradeUpsertCacheMut       sync.RWMutex
	requisicaoItemGradeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var requisicaoItemGradeAfterSelectMu sync.Mutex
var requisicaoItemGradeAfterSelectHooks []RequisicaoItemGradeHook

var requisicaoItemGradeBeforeInsertMu sync.Mutex
var requisicaoItemGradeBeforeInsertHooks []RequisicaoItemGradeHook
var requisicaoItemGradeAfterInsertMu sync.Mutex
var requisicaoItemGradeAfterInsertHooks []RequisicaoItemGradeHook

var requisicaoItemGradeBeforeUpdateMu sync.Mutex
var requisicaoItemGradeBeforeUpdateHooks []RequisicaoItemGradeHook
var requisicaoItemGradeAfterUpdateMu sync.Mutex
var requisicaoItemGradeAfterUpdateHooks []RequisicaoItemGradeHook

var requisicaoItemGradeBeforeDeleteMu sync.Mutex
var requisicaoItemGradeBeforeDeleteHooks []RequisicaoItemGradeHook
var requisicaoItemGradeAfterDeleteMu sync.Mutex
var requisicaoItemGradeAfterDeleteHooks []RequisicaoItemGradeHook

var requisicaoItemGradeBeforeUpsertMu sync.Mutex
var requisicaoItemGradeBeforeUpsertHooks []RequisicaoItemGradeHook
var requisicaoItemGradeAfterUpsertMu sync.Mutex
var requisicaoItemGradeAfterUpsertHooks []RequisicaoItemGradeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RequisicaoItemGrade) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RequisicaoItemGrade) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RequisicaoItemGrade) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RequisicaoItemGrade) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RequisicaoItemGrade) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RequisicaoItemGrade) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RequisicaoItemGrade) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RequisicaoItemGrade) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RequisicaoItemGrade) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemGradeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRequisicaoItemGradeHook registers your hook function for all future operations.
func AddRequisicaoItemGradeHook(hookPoint boil.HookPoint, requisicaoItemGradeHook RequisicaoItemGradeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		requisicaoItemGradeAfterSelectMu.Lock()
		requisicaoItemGradeAfterSelectHooks = append(requisicaoItemGradeAfterSelectHooks, requisicaoItemGradeHook)
		requisicaoItemGradeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		requisicaoItemGradeBeforeInsertMu.Lock()
		requisicaoItemGradeBeforeInsertHooks = append(requisicaoItemGradeBeforeInsertHooks, requisicaoItemGradeHook)
		requisicaoItemGradeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		requisicaoItemGradeAfterInsertMu.Lock()
		requisicaoItemGradeAfterInsertHooks = append(requisicaoItemGradeAfterInsertHooks, requisicaoItemGradeHook)
		requisicaoItemGradeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		requisicaoItemGradeBeforeUpdateMu.Lock()
		requisicaoItemGradeBeforeUpdateHooks = append(requisicaoItemGradeBeforeUpdateHooks, requisicaoItemGradeHook)
		requisicaoItemGradeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		requisicaoItemGradeAfterUpdateMu.Lock()
		requisicaoItemGradeAfterUpdateHooks = append(requisicaoItemGradeAfterUpdateHooks, requisicaoItemGradeHook)
		requisicaoItemGradeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		requisicaoItemGradeBeforeDeleteMu.Lock()
		requisicaoItemGradeBeforeDeleteHooks = append(requisicaoItemGradeBeforeDeleteHooks, requisicaoItemGradeHook)
		requisicaoItemGradeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		requisicaoItemGradeAfterDeleteMu.Lock()
		requisicaoItemGradeAfterDeleteHooks = append(requisicaoItemGradeAfterDeleteHooks, requisicaoItemGradeHook)
		requisicaoItemGradeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		requisicaoItemGradeBeforeUpsertMu.Lock()
		requisicaoItemGradeBeforeUpsertHooks = append(requisicaoItemGradeBeforeUpsertHooks, requisicaoItemGradeHook)
		requisicaoItemGradeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		requisicaoItemGradeAfterUpsertMu.Lock()
		requisicaoItemGradeAfterUpsertHooks = append(requisicaoItemGradeAfterUpsertHooks, requisicaoItemGradeHook)
		requisicaoItemGradeAfterUpsertMu.Unlock()
	}
}

// One returns a single requisicaoItemGrade record from the query.
func (q requisicaoItemGradeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RequisicaoItemGrade, error) {
	o := &RequisicaoItemGrade{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for requisicao_item_grade")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RequisicaoItemGrade records from the query.
func (q requisicaoItemGradeQuery) All(ctx context.Context, exec boil.ContextExecutor) (RequisicaoItemGradeSlice, error) {
	var o []*RequisicaoItemGrade

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RequisicaoItemGrade slice")
	}

	if len(requisicaoItemGradeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RequisicaoItemGrade records in the query.
func (q requisicaoItemGradeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count requisicao_item_grade rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q requisicaoItemGradeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if requisicao_item_grade exists")
	}

	return count > 0, nil
}

// Cor pointed to by the foreign key.
func (o *RequisicaoItemGrade) Cor(mods ...qm.QueryMod) produtoCorQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cor_id` = ?", o.CorID),
	}

	queryMods = append(queryMods, mods...)

	return ProdutoCors(queryMods...)
}

// Ptam pointed to by the foreign key.
func (o *RequisicaoItemGrade) Ptam(mods ...qm.QueryMod) produtoTamanhoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ptam_id` = ?", o.PtamID),
	}

	queryMods = append(queryMods, mods...)

	return ProdutoTamanhos(queryMods...)
}

// LoadCor allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (requisicaoItemGradeL) LoadCor(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRequisicaoItemGrade interface{}, mods queries.Applicator) error {
	var slice []*RequisicaoItemGrade
	var object *RequisicaoItemGrade

	if singular {
		var ok bool
		object, ok = maybeRequisicaoItemGrade.(*RequisicaoItemGrade)
		if !ok {
			object = new(RequisicaoItemGrade)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRequisicaoItemGrade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRequisicaoItemGrade))
			}
		}
	} else {
		s, ok := maybeRequisicaoItemGrade.(*[]*RequisicaoItemGrade)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRequisicaoItemGrade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRequisicaoItemGrade))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &requisicaoItemGradeR{}
		}
		args[object.CorID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &requisicaoItemGradeR{}
			}

			args[obj.CorID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_cor`),
		qm.WhereIn(`produto_cor.cor_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProdutoCor")
	}

	var resultSlice []*ProdutoCor
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProdutoCor")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto_cor")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_cor")
	}

	if len(produtoCorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cor = foreign
		if foreign.R == nil {
			foreign.R = &produtoCorR{}
		}
		foreign.R.CorRequisicaoItemGrades = append(foreign.R.CorRequisicaoItemGrades, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CorID == foreign.CorID {
				local.R.Cor = foreign
				if foreign.R == nil {
					foreign.R = &produtoCorR{}
				}
				foreign.R.CorRequisicaoItemGrades = append(foreign.R.CorRequisicaoItemGrades, local)
				break
			}
		}
	}

	return nil
}

// LoadPtam allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (requisicaoItemGradeL) LoadPtam(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRequisicaoItemGrade interface{}, mods queries.Applicator) error {
	var slice []*RequisicaoItemGrade
	var object *RequisicaoItemGrade

	if singular {
		var ok bool
		object, ok = maybeRequisicaoItemGrade.(*RequisicaoItemGrade)
		if !ok {
			object = new(RequisicaoItemGrade)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRequisicaoItemGrade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRequisicaoItemGrade))
			}
		}
	} else {
		s, ok := maybeRequisicaoItemGrade.(*[]*RequisicaoItemGrade)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRequisicaoItemGrade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRequisicaoItemGrade))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &requisicaoItemGradeR{}
		}
		args[object.PtamID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &requisicaoItemGradeR{}
			}

			args[obj.PtamID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_tamanho`),
		qm.WhereIn(`produto_tamanho.ptam_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProdutoTamanho")
	}

	var resultSlice []*ProdutoTamanho
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProdutoTamanho")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto_tamanho")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_tamanho")
	}

	if len(produtoTamanhoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ptam = foreign
		if foreign.R == nil {
			foreign.R = &produtoTamanhoR{}
		}
		foreign.R.PtamRequisicaoItemGrades = append(foreign.R.PtamRequisicaoItemGrades, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PtamID == foreign.PtamID {
				local.R.Ptam = foreign
				if foreign.R == nil {
					foreign.R = &produtoTamanhoR{}
				}
				foreign.R.PtamRequisicaoItemGrades = append(foreign.R.PtamRequisicaoItemGrades, local)
				break
			}
		}
	}

	return nil
}

// SetCor of the requisicaoItemGrade to the related item.
// Sets o.R.Cor to related.
// Adds o to related.R.CorRequisicaoItemGrades.
func (o *RequisicaoItemGrade) SetCor(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ProdutoCor) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `requisicao_item_grade` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cor_id"}),
		strmangle.WhereClause("`", "`", 0, requisicaoItemGradePrimaryKeyColumns),
	)
	values := []interface{}{related.CorID, o.ReqID, o.ReiItem, o.ReigItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CorID = related.CorID
	if o.R == nil {
		o.R = &requisicaoItemGradeR{
			Cor: related,
		}
	} else {
		o.R.Cor = related
	}

	if related.R == nil {
		related.R = &produtoCorR{
			CorRequisicaoItemGrades: RequisicaoItemGradeSlice{o},
		}
	} else {
		related.R.CorRequisicaoItemGrades = append(related.R.CorRequisicaoItemGrades, o)
	}

	return nil
}

// SetPtam of the requisicaoItemGrade to the related item.
// Sets o.R.Ptam to related.
// Adds o to related.R.PtamRequisicaoItemGrades.
func (o *RequisicaoItemGrade) SetPtam(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ProdutoTamanho) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `requisicao_item_grade` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ptam_id"}),
		strmangle.WhereClause("`", "`", 0, requisicaoItemGradePrimaryKeyColumns),
	)
	values := []interface{}{related.PtamID, o.ReqID, o.ReiItem, o.ReigItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PtamID = related.PtamID
	if o.R == nil {
		o.R = &requisicaoItemGradeR{
			Ptam: related,
		}
	} else {
		o.R.Ptam = related
	}

	if related.R == nil {
		related.R = &produtoTamanhoR{
			PtamRequisicaoItemGrades: RequisicaoItemGradeSlice{o},
		}
	} else {
		related.R.PtamRequisicaoItemGrades = append(related.R.PtamRequisicaoItemGrades, o)
	}

	return nil
}

// RequisicaoItemGrades retrieves all the records using an executor.
func RequisicaoItemGrades(mods ...qm.QueryMod) requisicaoItemGradeQuery {
	mods = append(mods, qm.From("`requisicao_item_grade`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`requisicao_item_grade`.*"})
	}

	return requisicaoItemGradeQuery{q}
}

// FindRequisicaoItemGrade retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRequisicaoItemGrade(ctx context.Context, exec boil.ContextExecutor, reqID int, reiItem int, reigItem int, selectCols ...string) (*RequisicaoItemGrade, error) {
	requisicaoItemGradeObj := &RequisicaoItemGrade{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `requisicao_item_grade` where `req_id`=? AND `rei_item`=? AND `reig_item`=?", sel,
	)

	q := queries.Raw(query, reqID, reiItem, reigItem)

	err := q.Bind(ctx, exec, requisicaoItemGradeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from requisicao_item_grade")
	}

	if err = requisicaoItemGradeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return requisicaoItemGradeObj, err
	}

	return requisicaoItemGradeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RequisicaoItemGrade) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no requisicao_item_grade provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(requisicaoItemGradeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	requisicaoItemGradeInsertCacheMut.RLock()
	cache, cached := requisicaoItemGradeInsertCache[key]
	requisicaoItemGradeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			requisicaoItemGradeAllColumns,
			requisicaoItemGradeColumnsWithDefault,
			requisicaoItemGradeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(requisicaoItemGradeType, requisicaoItemGradeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(requisicaoItemGradeType, requisicaoItemGradeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `requisicao_item_grade` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `requisicao_item_grade` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `requisicao_item_grade` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, requisicaoItemGradePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into requisicao_item_grade")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ReqID,
		o.ReiItem,
		o.ReigItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for requisicao_item_grade")
	}

CacheNoHooks:
	if !cached {
		requisicaoItemGradeInsertCacheMut.Lock()
		requisicaoItemGradeInsertCache[key] = cache
		requisicaoItemGradeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RequisicaoItemGrade.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RequisicaoItemGrade) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	requisicaoItemGradeUpdateCacheMut.RLock()
	cache, cached := requisicaoItemGradeUpdateCache[key]
	requisicaoItemGradeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			requisicaoItemGradeAllColumns,
			requisicaoItemGradePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update requisicao_item_grade, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `requisicao_item_grade` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, requisicaoItemGradePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(requisicaoItemGradeType, requisicaoItemGradeMapping, append(wl, requisicaoItemGradePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update requisicao_item_grade row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for requisicao_item_grade")
	}

	if !cached {
		requisicaoItemGradeUpdateCacheMut.Lock()
		requisicaoItemGradeUpdateCache[key] = cache
		requisicaoItemGradeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q requisicaoItemGradeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for requisicao_item_grade")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for requisicao_item_grade")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RequisicaoItemGradeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemGradePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `requisicao_item_grade` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemGradePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in requisicaoItemGrade slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all requisicaoItemGrade")
	}
	return rowsAff, nil
}

var mySQLRequisicaoItemGradeUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RequisicaoItemGrade) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no requisicao_item_grade provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(requisicaoItemGradeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRequisicaoItemGradeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	requisicaoItemGradeUpsertCacheMut.RLock()
	cache, cached := requisicaoItemGradeUpsertCache[key]
	requisicaoItemGradeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			requisicaoItemGradeAllColumns,
			requisicaoItemGradeColumnsWithDefault,
			requisicaoItemGradeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			requisicaoItemGradeAllColumns,
			requisicaoItemGradePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert requisicao_item_grade, could not build update column list")
		}

		ret := strmangle.SetComplement(requisicaoItemGradeAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`requisicao_item_grade`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `requisicao_item_grade` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(requisicaoItemGradeType, requisicaoItemGradeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(requisicaoItemGradeType, requisicaoItemGradeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for requisicao_item_grade")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(requisicaoItemGradeType, requisicaoItemGradeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for requisicao_item_grade")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for requisicao_item_grade")
	}

CacheNoHooks:
	if !cached {
		requisicaoItemGradeUpsertCacheMut.Lock()
		requisicaoItemGradeUpsertCache[key] = cache
		requisicaoItemGradeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RequisicaoItemGrade record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RequisicaoItemGrade) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RequisicaoItemGrade provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), requisicaoItemGradePrimaryKeyMapping)
	sql := "DELETE FROM `requisicao_item_grade` WHERE `req_id`=? AND `rei_item`=? AND `reig_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from requisicao_item_grade")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for requisicao_item_grade")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q requisicaoItemGradeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no requisicaoItemGradeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from requisicao_item_grade")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for requisicao_item_grade")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RequisicaoItemGradeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(requisicaoItemGradeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemGradePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `requisicao_item_grade` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemGradePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from requisicaoItemGrade slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for requisicao_item_grade")
	}

	if len(requisicaoItemGradeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RequisicaoItemGrade) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRequisicaoItemGrade(ctx, exec, o.ReqID, o.ReiItem, o.ReigItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RequisicaoItemGradeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RequisicaoItemGradeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemGradePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `requisicao_item_grade`.* FROM `requisicao_item_grade` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemGradePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RequisicaoItemGradeSlice")
	}

	*o = slice

	return nil
}

// RequisicaoItemGradeExists checks if the RequisicaoItemGrade row exists.
func RequisicaoItemGradeExists(ctx context.Context, exec boil.ContextExecutor, reqID int, reiItem int, reigItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `requisicao_item_grade` where `req_id`=? AND `rei_item`=? AND `reig_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, reqID, reiItem, reigItem)
	}
	row := exec.QueryRowContext(ctx, sql, reqID, reiItem, reigItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if requisicao_item_grade exists")
	}

	return exists, nil
}

// Exists checks if the RequisicaoItemGrade row exists.
func (o *RequisicaoItemGrade) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RequisicaoItemGradeExists(ctx, exec, o.ReqID, o.ReiItem, o.ReigItem)
}
