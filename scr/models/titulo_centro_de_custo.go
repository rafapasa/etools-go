// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// TituloCentroDeCusto is an object representing the database table.
type TituloCentroDeCusto struct {
	CDCID          int               `boil:"cdc_id" json:"cdc_id" toml:"cdc_id" yaml:"cdc_id"`
	TitID          int               `boil:"tit_id" json:"tit_id" toml:"tit_id" yaml:"tit_id"`
	TCDCItem       int               `boil:"tcdc_item" json:"tcdc_item" toml:"tcdc_item" yaml:"tcdc_item"`
	TCDCPercentual types.NullDecimal `boil:"tcdc_percentual" json:"tcdc_percentual,omitempty" toml:"tcdc_percentual" yaml:"tcdc_percentual,omitempty"`
	TCDCValor      types.NullDecimal `boil:"tcdc_valor" json:"tcdc_valor,omitempty" toml:"tcdc_valor" yaml:"tcdc_valor,omitempty"`

	R *tituloCentroDeCustoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tituloCentroDeCustoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TituloCentroDeCustoColumns = struct {
	CDCID          string
	TitID          string
	TCDCItem       string
	TCDCPercentual string
	TCDCValor      string
}{
	CDCID:          "cdc_id",
	TitID:          "tit_id",
	TCDCItem:       "tcdc_item",
	TCDCPercentual: "tcdc_percentual",
	TCDCValor:      "tcdc_valor",
}

var TituloCentroDeCustoTableColumns = struct {
	CDCID          string
	TitID          string
	TCDCItem       string
	TCDCPercentual string
	TCDCValor      string
}{
	CDCID:          "titulo_centro_de_custo.cdc_id",
	TitID:          "titulo_centro_de_custo.tit_id",
	TCDCItem:       "titulo_centro_de_custo.tcdc_item",
	TCDCPercentual: "titulo_centro_de_custo.tcdc_percentual",
	TCDCValor:      "titulo_centro_de_custo.tcdc_valor",
}

// Generated where

var TituloCentroDeCustoWhere = struct {
	CDCID          whereHelperint
	TitID          whereHelperint
	TCDCItem       whereHelperint
	TCDCPercentual whereHelpertypes_NullDecimal
	TCDCValor      whereHelpertypes_NullDecimal
}{
	CDCID:          whereHelperint{field: "`titulo_centro_de_custo`.`cdc_id`"},
	TitID:          whereHelperint{field: "`titulo_centro_de_custo`.`tit_id`"},
	TCDCItem:       whereHelperint{field: "`titulo_centro_de_custo`.`tcdc_item`"},
	TCDCPercentual: whereHelpertypes_NullDecimal{field: "`titulo_centro_de_custo`.`tcdc_percentual`"},
	TCDCValor:      whereHelpertypes_NullDecimal{field: "`titulo_centro_de_custo`.`tcdc_valor`"},
}

// TituloCentroDeCustoRels is where relationship names are stored.
var TituloCentroDeCustoRels = struct {
	CDC string
	Tit string
}{
	CDC: "CDC",
	Tit: "Tit",
}

// tituloCentroDeCustoR is where relationships are stored.
type tituloCentroDeCustoR struct {
	CDC *CentroDeCusto `boil:"CDC" json:"CDC" toml:"CDC" yaml:"CDC"`
	Tit *Titulo        `boil:"Tit" json:"Tit" toml:"Tit" yaml:"Tit"`
}

// NewStruct creates a new relationship struct
func (*tituloCentroDeCustoR) NewStruct() *tituloCentroDeCustoR {
	return &tituloCentroDeCustoR{}
}

func (r *tituloCentroDeCustoR) GetCDC() *CentroDeCusto {
	if r == nil {
		return nil
	}
	return r.CDC
}

func (r *tituloCentroDeCustoR) GetTit() *Titulo {
	if r == nil {
		return nil
	}
	return r.Tit
}

// tituloCentroDeCustoL is where Load methods for each relationship are stored.
type tituloCentroDeCustoL struct{}

var (
	tituloCentroDeCustoAllColumns            = []string{"cdc_id", "tit_id", "tcdc_item", "tcdc_percentual", "tcdc_valor"}
	tituloCentroDeCustoColumnsWithoutDefault = []string{"cdc_id", "tit_id", "tcdc_item", "tcdc_percentual", "tcdc_valor"}
	tituloCentroDeCustoColumnsWithDefault    = []string{}
	tituloCentroDeCustoPrimaryKeyColumns     = []string{"cdc_id", "tit_id", "tcdc_item"}
	tituloCentroDeCustoGeneratedColumns      = []string{}
)

type (
	// TituloCentroDeCustoSlice is an alias for a slice of pointers to TituloCentroDeCusto.
	// This should almost always be used instead of []TituloCentroDeCusto.
	TituloCentroDeCustoSlice []*TituloCentroDeCusto
	// TituloCentroDeCustoHook is the signature for custom TituloCentroDeCusto hook methods
	TituloCentroDeCustoHook func(context.Context, boil.ContextExecutor, *TituloCentroDeCusto) error

	tituloCentroDeCustoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tituloCentroDeCustoType                 = reflect.TypeOf(&TituloCentroDeCusto{})
	tituloCentroDeCustoMapping              = queries.MakeStructMapping(tituloCentroDeCustoType)
	tituloCentroDeCustoPrimaryKeyMapping, _ = queries.BindMapping(tituloCentroDeCustoType, tituloCentroDeCustoMapping, tituloCentroDeCustoPrimaryKeyColumns)
	tituloCentroDeCustoInsertCacheMut       sync.RWMutex
	tituloCentroDeCustoInsertCache          = make(map[string]insertCache)
	tituloCentroDeCustoUpdateCacheMut       sync.RWMutex
	tituloCentroDeCustoUpdateCache          = make(map[string]updateCache)
	tituloCentroDeCustoUpsertCacheMut       sync.RWMutex
	tituloCentroDeCustoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tituloCentroDeCustoAfterSelectMu sync.Mutex
var tituloCentroDeCustoAfterSelectHooks []TituloCentroDeCustoHook

var tituloCentroDeCustoBeforeInsertMu sync.Mutex
var tituloCentroDeCustoBeforeInsertHooks []TituloCentroDeCustoHook
var tituloCentroDeCustoAfterInsertMu sync.Mutex
var tituloCentroDeCustoAfterInsertHooks []TituloCentroDeCustoHook

var tituloCentroDeCustoBeforeUpdateMu sync.Mutex
var tituloCentroDeCustoBeforeUpdateHooks []TituloCentroDeCustoHook
var tituloCentroDeCustoAfterUpdateMu sync.Mutex
var tituloCentroDeCustoAfterUpdateHooks []TituloCentroDeCustoHook

var tituloCentroDeCustoBeforeDeleteMu sync.Mutex
var tituloCentroDeCustoBeforeDeleteHooks []TituloCentroDeCustoHook
var tituloCentroDeCustoAfterDeleteMu sync.Mutex
var tituloCentroDeCustoAfterDeleteHooks []TituloCentroDeCustoHook

var tituloCentroDeCustoBeforeUpsertMu sync.Mutex
var tituloCentroDeCustoBeforeUpsertHooks []TituloCentroDeCustoHook
var tituloCentroDeCustoAfterUpsertMu sync.Mutex
var tituloCentroDeCustoAfterUpsertHooks []TituloCentroDeCustoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TituloCentroDeCusto) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TituloCentroDeCusto) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TituloCentroDeCusto) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TituloCentroDeCusto) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TituloCentroDeCusto) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TituloCentroDeCusto) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TituloCentroDeCusto) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TituloCentroDeCusto) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TituloCentroDeCusto) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloCentroDeCustoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTituloCentroDeCustoHook registers your hook function for all future operations.
func AddTituloCentroDeCustoHook(hookPoint boil.HookPoint, tituloCentroDeCustoHook TituloCentroDeCustoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tituloCentroDeCustoAfterSelectMu.Lock()
		tituloCentroDeCustoAfterSelectHooks = append(tituloCentroDeCustoAfterSelectHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tituloCentroDeCustoBeforeInsertMu.Lock()
		tituloCentroDeCustoBeforeInsertHooks = append(tituloCentroDeCustoBeforeInsertHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tituloCentroDeCustoAfterInsertMu.Lock()
		tituloCentroDeCustoAfterInsertHooks = append(tituloCentroDeCustoAfterInsertHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tituloCentroDeCustoBeforeUpdateMu.Lock()
		tituloCentroDeCustoBeforeUpdateHooks = append(tituloCentroDeCustoBeforeUpdateHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tituloCentroDeCustoAfterUpdateMu.Lock()
		tituloCentroDeCustoAfterUpdateHooks = append(tituloCentroDeCustoAfterUpdateHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tituloCentroDeCustoBeforeDeleteMu.Lock()
		tituloCentroDeCustoBeforeDeleteHooks = append(tituloCentroDeCustoBeforeDeleteHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tituloCentroDeCustoAfterDeleteMu.Lock()
		tituloCentroDeCustoAfterDeleteHooks = append(tituloCentroDeCustoAfterDeleteHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tituloCentroDeCustoBeforeUpsertMu.Lock()
		tituloCentroDeCustoBeforeUpsertHooks = append(tituloCentroDeCustoBeforeUpsertHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tituloCentroDeCustoAfterUpsertMu.Lock()
		tituloCentroDeCustoAfterUpsertHooks = append(tituloCentroDeCustoAfterUpsertHooks, tituloCentroDeCustoHook)
		tituloCentroDeCustoAfterUpsertMu.Unlock()
	}
}

// One returns a single tituloCentroDeCusto record from the query.
func (q tituloCentroDeCustoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TituloCentroDeCusto, error) {
	o := &TituloCentroDeCusto{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for titulo_centro_de_custo")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TituloCentroDeCusto records from the query.
func (q tituloCentroDeCustoQuery) All(ctx context.Context, exec boil.ContextExecutor) (TituloCentroDeCustoSlice, error) {
	var o []*TituloCentroDeCusto

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TituloCentroDeCusto slice")
	}

	if len(tituloCentroDeCustoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TituloCentroDeCusto records in the query.
func (q tituloCentroDeCustoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count titulo_centro_de_custo rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tituloCentroDeCustoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if titulo_centro_de_custo exists")
	}

	return count > 0, nil
}

// CDC pointed to by the foreign key.
func (o *TituloCentroDeCusto) CDC(mods ...qm.QueryMod) centroDeCustoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cdc_id` = ?", o.CDCID),
	}

	queryMods = append(queryMods, mods...)

	return CentroDeCustos(queryMods...)
}

// Tit pointed to by the foreign key.
func (o *TituloCentroDeCusto) Tit(mods ...qm.QueryMod) tituloQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`tit_id` = ?", o.TitID),
	}

	queryMods = append(queryMods, mods...)

	return Titulos(queryMods...)
}

// LoadCDC allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloCentroDeCustoL) LoadCDC(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloCentroDeCusto interface{}, mods queries.Applicator) error {
	var slice []*TituloCentroDeCusto
	var object *TituloCentroDeCusto

	if singular {
		var ok bool
		object, ok = maybeTituloCentroDeCusto.(*TituloCentroDeCusto)
		if !ok {
			object = new(TituloCentroDeCusto)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloCentroDeCusto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloCentroDeCusto))
			}
		}
	} else {
		s, ok := maybeTituloCentroDeCusto.(*[]*TituloCentroDeCusto)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloCentroDeCusto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloCentroDeCusto))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloCentroDeCustoR{}
		}
		args[object.CDCID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloCentroDeCustoR{}
			}

			args[obj.CDCID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`centro_de_custo`),
		qm.WhereIn(`centro_de_custo.cdc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load CentroDeCusto")
	}

	var resultSlice []*CentroDeCusto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice CentroDeCusto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for centro_de_custo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for centro_de_custo")
	}

	if len(centroDeCustoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CDC = foreign
		if foreign.R == nil {
			foreign.R = &centroDeCustoR{}
		}
		foreign.R.CDCTituloCentroDeCustos = append(foreign.R.CDCTituloCentroDeCustos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CDCID == foreign.CDCID {
				local.R.CDC = foreign
				if foreign.R == nil {
					foreign.R = &centroDeCustoR{}
				}
				foreign.R.CDCTituloCentroDeCustos = append(foreign.R.CDCTituloCentroDeCustos, local)
				break
			}
		}
	}

	return nil
}

// LoadTit allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloCentroDeCustoL) LoadTit(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloCentroDeCusto interface{}, mods queries.Applicator) error {
	var slice []*TituloCentroDeCusto
	var object *TituloCentroDeCusto

	if singular {
		var ok bool
		object, ok = maybeTituloCentroDeCusto.(*TituloCentroDeCusto)
		if !ok {
			object = new(TituloCentroDeCusto)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloCentroDeCusto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloCentroDeCusto))
			}
		}
	} else {
		s, ok := maybeTituloCentroDeCusto.(*[]*TituloCentroDeCusto)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloCentroDeCusto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloCentroDeCusto))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloCentroDeCustoR{}
		}
		args[object.TitID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloCentroDeCustoR{}
			}

			args[obj.TitID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulos`),
		qm.WhereIn(`titulos.tit_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Titulo")
	}

	var resultSlice []*Titulo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Titulo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for titulos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulos")
	}

	if len(tituloAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tit = foreign
		if foreign.R == nil {
			foreign.R = &tituloR{}
		}
		foreign.R.TitTituloCentroDeCustos = append(foreign.R.TitTituloCentroDeCustos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TitID == foreign.TitID {
				local.R.Tit = foreign
				if foreign.R == nil {
					foreign.R = &tituloR{}
				}
				foreign.R.TitTituloCentroDeCustos = append(foreign.R.TitTituloCentroDeCustos, local)
				break
			}
		}
	}

	return nil
}

// SetCDC of the tituloCentroDeCusto to the related item.
// Sets o.R.CDC to related.
// Adds o to related.R.CDCTituloCentroDeCustos.
func (o *TituloCentroDeCusto) SetCDC(ctx context.Context, exec boil.ContextExecutor, insert bool, related *CentroDeCusto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_centro_de_custo` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cdc_id"}),
		strmangle.WhereClause("`", "`", 0, tituloCentroDeCustoPrimaryKeyColumns),
	)
	values := []interface{}{related.CDCID, o.CDCID, o.TitID, o.TCDCItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CDCID = related.CDCID
	if o.R == nil {
		o.R = &tituloCentroDeCustoR{
			CDC: related,
		}
	} else {
		o.R.CDC = related
	}

	if related.R == nil {
		related.R = &centroDeCustoR{
			CDCTituloCentroDeCustos: TituloCentroDeCustoSlice{o},
		}
	} else {
		related.R.CDCTituloCentroDeCustos = append(related.R.CDCTituloCentroDeCustos, o)
	}

	return nil
}

// SetTit of the tituloCentroDeCusto to the related item.
// Sets o.R.Tit to related.
// Adds o to related.R.TitTituloCentroDeCustos.
func (o *TituloCentroDeCusto) SetTit(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Titulo) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_centro_de_custo` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"tit_id"}),
		strmangle.WhereClause("`", "`", 0, tituloCentroDeCustoPrimaryKeyColumns),
	)
	values := []interface{}{related.TitID, o.CDCID, o.TitID, o.TCDCItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TitID = related.TitID
	if o.R == nil {
		o.R = &tituloCentroDeCustoR{
			Tit: related,
		}
	} else {
		o.R.Tit = related
	}

	if related.R == nil {
		related.R = &tituloR{
			TitTituloCentroDeCustos: TituloCentroDeCustoSlice{o},
		}
	} else {
		related.R.TitTituloCentroDeCustos = append(related.R.TitTituloCentroDeCustos, o)
	}

	return nil
}

// TituloCentroDeCustos retrieves all the records using an executor.
func TituloCentroDeCustos(mods ...qm.QueryMod) tituloCentroDeCustoQuery {
	mods = append(mods, qm.From("`titulo_centro_de_custo`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`titulo_centro_de_custo`.*"})
	}

	return tituloCentroDeCustoQuery{q}
}

// FindTituloCentroDeCusto retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTituloCentroDeCusto(ctx context.Context, exec boil.ContextExecutor, cDCID int, titID int, tCDCItem int, selectCols ...string) (*TituloCentroDeCusto, error) {
	tituloCentroDeCustoObj := &TituloCentroDeCusto{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `titulo_centro_de_custo` where `cdc_id`=? AND `tit_id`=? AND `tcdc_item`=?", sel,
	)

	q := queries.Raw(query, cDCID, titID, tCDCItem)

	err := q.Bind(ctx, exec, tituloCentroDeCustoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from titulo_centro_de_custo")
	}

	if err = tituloCentroDeCustoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tituloCentroDeCustoObj, err
	}

	return tituloCentroDeCustoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TituloCentroDeCusto) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no titulo_centro_de_custo provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tituloCentroDeCustoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tituloCentroDeCustoInsertCacheMut.RLock()
	cache, cached := tituloCentroDeCustoInsertCache[key]
	tituloCentroDeCustoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tituloCentroDeCustoAllColumns,
			tituloCentroDeCustoColumnsWithDefault,
			tituloCentroDeCustoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tituloCentroDeCustoType, tituloCentroDeCustoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tituloCentroDeCustoType, tituloCentroDeCustoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `titulo_centro_de_custo` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `titulo_centro_de_custo` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `titulo_centro_de_custo` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tituloCentroDeCustoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into titulo_centro_de_custo")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CDCID,
		o.TitID,
		o.TCDCItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for titulo_centro_de_custo")
	}

CacheNoHooks:
	if !cached {
		tituloCentroDeCustoInsertCacheMut.Lock()
		tituloCentroDeCustoInsertCache[key] = cache
		tituloCentroDeCustoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TituloCentroDeCusto.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TituloCentroDeCusto) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tituloCentroDeCustoUpdateCacheMut.RLock()
	cache, cached := tituloCentroDeCustoUpdateCache[key]
	tituloCentroDeCustoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tituloCentroDeCustoAllColumns,
			tituloCentroDeCustoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update titulo_centro_de_custo, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `titulo_centro_de_custo` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tituloCentroDeCustoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tituloCentroDeCustoType, tituloCentroDeCustoMapping, append(wl, tituloCentroDeCustoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update titulo_centro_de_custo row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for titulo_centro_de_custo")
	}

	if !cached {
		tituloCentroDeCustoUpdateCacheMut.Lock()
		tituloCentroDeCustoUpdateCache[key] = cache
		tituloCentroDeCustoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tituloCentroDeCustoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for titulo_centro_de_custo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for titulo_centro_de_custo")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TituloCentroDeCustoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloCentroDeCustoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `titulo_centro_de_custo` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloCentroDeCustoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tituloCentroDeCusto slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tituloCentroDeCusto")
	}
	return rowsAff, nil
}

var mySQLTituloCentroDeCustoUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TituloCentroDeCusto) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no titulo_centro_de_custo provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tituloCentroDeCustoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTituloCentroDeCustoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tituloCentroDeCustoUpsertCacheMut.RLock()
	cache, cached := tituloCentroDeCustoUpsertCache[key]
	tituloCentroDeCustoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tituloCentroDeCustoAllColumns,
			tituloCentroDeCustoColumnsWithDefault,
			tituloCentroDeCustoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tituloCentroDeCustoAllColumns,
			tituloCentroDeCustoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert titulo_centro_de_custo, could not build update column list")
		}

		ret := strmangle.SetComplement(tituloCentroDeCustoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`titulo_centro_de_custo`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `titulo_centro_de_custo` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tituloCentroDeCustoType, tituloCentroDeCustoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tituloCentroDeCustoType, tituloCentroDeCustoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for titulo_centro_de_custo")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tituloCentroDeCustoType, tituloCentroDeCustoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for titulo_centro_de_custo")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for titulo_centro_de_custo")
	}

CacheNoHooks:
	if !cached {
		tituloCentroDeCustoUpsertCacheMut.Lock()
		tituloCentroDeCustoUpsertCache[key] = cache
		tituloCentroDeCustoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TituloCentroDeCusto record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TituloCentroDeCusto) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TituloCentroDeCusto provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tituloCentroDeCustoPrimaryKeyMapping)
	sql := "DELETE FROM `titulo_centro_de_custo` WHERE `cdc_id`=? AND `tit_id`=? AND `tcdc_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from titulo_centro_de_custo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for titulo_centro_de_custo")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tituloCentroDeCustoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tituloCentroDeCustoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from titulo_centro_de_custo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for titulo_centro_de_custo")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TituloCentroDeCustoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tituloCentroDeCustoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloCentroDeCustoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `titulo_centro_de_custo` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloCentroDeCustoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tituloCentroDeCusto slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for titulo_centro_de_custo")
	}

	if len(tituloCentroDeCustoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TituloCentroDeCusto) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTituloCentroDeCusto(ctx, exec, o.CDCID, o.TitID, o.TCDCItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TituloCentroDeCustoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TituloCentroDeCustoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloCentroDeCustoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `titulo_centro_de_custo`.* FROM `titulo_centro_de_custo` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloCentroDeCustoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TituloCentroDeCustoSlice")
	}

	*o = slice

	return nil
}

// TituloCentroDeCustoExists checks if the TituloCentroDeCusto row exists.
func TituloCentroDeCustoExists(ctx context.Context, exec boil.ContextExecutor, cDCID int, titID int, tCDCItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `titulo_centro_de_custo` where `cdc_id`=? AND `tit_id`=? AND `tcdc_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cDCID, titID, tCDCItem)
	}
	row := exec.QueryRowContext(ctx, sql, cDCID, titID, tCDCItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if titulo_centro_de_custo exists")
	}

	return exists, nil
}

// Exists checks if the TituloCentroDeCusto row exists.
func (o *TituloCentroDeCusto) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TituloCentroDeCustoExists(ctx, exec, o.CDCID, o.TitID, o.TCDCItem)
}
