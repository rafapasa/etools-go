// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PermissaoRecurso is an object representing the database table.
type PermissaoRecurso struct {
	PreID        int    `boil:"pre_id" json:"pre_id" toml:"pre_id" yaml:"pre_id"`
	PreNome      string `boil:"pre_nome" json:"pre_nome" toml:"pre_nome" yaml:"pre_nome"`
	PreDescricao string `boil:"pre_descricao" json:"pre_descricao" toml:"pre_descricao" yaml:"pre_descricao"`

	R *permissaoRecursoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L permissaoRecursoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PermissaoRecursoColumns = struct {
	PreID        string
	PreNome      string
	PreDescricao string
}{
	PreID:        "pre_id",
	PreNome:      "pre_nome",
	PreDescricao: "pre_descricao",
}

var PermissaoRecursoTableColumns = struct {
	PreID        string
	PreNome      string
	PreDescricao string
}{
	PreID:        "permissao_recurso.pre_id",
	PreNome:      "permissao_recurso.pre_nome",
	PreDescricao: "permissao_recurso.pre_descricao",
}

// Generated where

var PermissaoRecursoWhere = struct {
	PreID        whereHelperint
	PreNome      whereHelperstring
	PreDescricao whereHelperstring
}{
	PreID:        whereHelperint{field: "`permissao_recurso`.`pre_id`"},
	PreNome:      whereHelperstring{field: "`permissao_recurso`.`pre_nome`"},
	PreDescricao: whereHelperstring{field: "`permissao_recurso`.`pre_descricao`"},
}

// PermissaoRecursoRels is where relationship names are stored.
var PermissaoRecursoRels = struct {
	PrePermissaoRecursoAcaos  string
	PrePermissaoRecursoCampos string
	UfiUsuarioFilials         string
}{
	PrePermissaoRecursoAcaos:  "PrePermissaoRecursoAcaos",
	PrePermissaoRecursoCampos: "PrePermissaoRecursoCampos",
	UfiUsuarioFilials:         "UfiUsuarioFilials",
}

// permissaoRecursoR is where relationships are stored.
type permissaoRecursoR struct {
	PrePermissaoRecursoAcaos  PermissaoRecursoAcaoSlice  `boil:"PrePermissaoRecursoAcaos" json:"PrePermissaoRecursoAcaos" toml:"PrePermissaoRecursoAcaos" yaml:"PrePermissaoRecursoAcaos"`
	PrePermissaoRecursoCampos PermissaoRecursoCampoSlice `boil:"PrePermissaoRecursoCampos" json:"PrePermissaoRecursoCampos" toml:"PrePermissaoRecursoCampos" yaml:"PrePermissaoRecursoCampos"`
	UfiUsuarioFilials         UsuarioFilialSlice         `boil:"UfiUsuarioFilials" json:"UfiUsuarioFilials" toml:"UfiUsuarioFilials" yaml:"UfiUsuarioFilials"`
}

// NewStruct creates a new relationship struct
func (*permissaoRecursoR) NewStruct() *permissaoRecursoR {
	return &permissaoRecursoR{}
}

func (r *permissaoRecursoR) GetPrePermissaoRecursoAcaos() PermissaoRecursoAcaoSlice {
	if r == nil {
		return nil
	}
	return r.PrePermissaoRecursoAcaos
}

func (r *permissaoRecursoR) GetPrePermissaoRecursoCampos() PermissaoRecursoCampoSlice {
	if r == nil {
		return nil
	}
	return r.PrePermissaoRecursoCampos
}

func (r *permissaoRecursoR) GetUfiUsuarioFilials() UsuarioFilialSlice {
	if r == nil {
		return nil
	}
	return r.UfiUsuarioFilials
}

// permissaoRecursoL is where Load methods for each relationship are stored.
type permissaoRecursoL struct{}

var (
	permissaoRecursoAllColumns            = []string{"pre_id", "pre_nome", "pre_descricao"}
	permissaoRecursoColumnsWithoutDefault = []string{"pre_id", "pre_nome", "pre_descricao"}
	permissaoRecursoColumnsWithDefault    = []string{}
	permissaoRecursoPrimaryKeyColumns     = []string{"pre_id"}
	permissaoRecursoGeneratedColumns      = []string{}
)

type (
	// PermissaoRecursoSlice is an alias for a slice of pointers to PermissaoRecurso.
	// This should almost always be used instead of []PermissaoRecurso.
	PermissaoRecursoSlice []*PermissaoRecurso
	// PermissaoRecursoHook is the signature for custom PermissaoRecurso hook methods
	PermissaoRecursoHook func(context.Context, boil.ContextExecutor, *PermissaoRecurso) error

	permissaoRecursoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	permissaoRecursoType                 = reflect.TypeOf(&PermissaoRecurso{})
	permissaoRecursoMapping              = queries.MakeStructMapping(permissaoRecursoType)
	permissaoRecursoPrimaryKeyMapping, _ = queries.BindMapping(permissaoRecursoType, permissaoRecursoMapping, permissaoRecursoPrimaryKeyColumns)
	permissaoRecursoInsertCacheMut       sync.RWMutex
	permissaoRecursoInsertCache          = make(map[string]insertCache)
	permissaoRecursoUpdateCacheMut       sync.RWMutex
	permissaoRecursoUpdateCache          = make(map[string]updateCache)
	permissaoRecursoUpsertCacheMut       sync.RWMutex
	permissaoRecursoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var permissaoRecursoAfterSelectMu sync.Mutex
var permissaoRecursoAfterSelectHooks []PermissaoRecursoHook

var permissaoRecursoBeforeInsertMu sync.Mutex
var permissaoRecursoBeforeInsertHooks []PermissaoRecursoHook
var permissaoRecursoAfterInsertMu sync.Mutex
var permissaoRecursoAfterInsertHooks []PermissaoRecursoHook

var permissaoRecursoBeforeUpdateMu sync.Mutex
var permissaoRecursoBeforeUpdateHooks []PermissaoRecursoHook
var permissaoRecursoAfterUpdateMu sync.Mutex
var permissaoRecursoAfterUpdateHooks []PermissaoRecursoHook

var permissaoRecursoBeforeDeleteMu sync.Mutex
var permissaoRecursoBeforeDeleteHooks []PermissaoRecursoHook
var permissaoRecursoAfterDeleteMu sync.Mutex
var permissaoRecursoAfterDeleteHooks []PermissaoRecursoHook

var permissaoRecursoBeforeUpsertMu sync.Mutex
var permissaoRecursoBeforeUpsertHooks []PermissaoRecursoHook
var permissaoRecursoAfterUpsertMu sync.Mutex
var permissaoRecursoAfterUpsertHooks []PermissaoRecursoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PermissaoRecurso) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PermissaoRecurso) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PermissaoRecurso) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PermissaoRecurso) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PermissaoRecurso) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PermissaoRecurso) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PermissaoRecurso) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PermissaoRecurso) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PermissaoRecurso) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissaoRecursoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPermissaoRecursoHook registers your hook function for all future operations.
func AddPermissaoRecursoHook(hookPoint boil.HookPoint, permissaoRecursoHook PermissaoRecursoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		permissaoRecursoAfterSelectMu.Lock()
		permissaoRecursoAfterSelectHooks = append(permissaoRecursoAfterSelectHooks, permissaoRecursoHook)
		permissaoRecursoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		permissaoRecursoBeforeInsertMu.Lock()
		permissaoRecursoBeforeInsertHooks = append(permissaoRecursoBeforeInsertHooks, permissaoRecursoHook)
		permissaoRecursoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		permissaoRecursoAfterInsertMu.Lock()
		permissaoRecursoAfterInsertHooks = append(permissaoRecursoAfterInsertHooks, permissaoRecursoHook)
		permissaoRecursoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		permissaoRecursoBeforeUpdateMu.Lock()
		permissaoRecursoBeforeUpdateHooks = append(permissaoRecursoBeforeUpdateHooks, permissaoRecursoHook)
		permissaoRecursoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		permissaoRecursoAfterUpdateMu.Lock()
		permissaoRecursoAfterUpdateHooks = append(permissaoRecursoAfterUpdateHooks, permissaoRecursoHook)
		permissaoRecursoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		permissaoRecursoBeforeDeleteMu.Lock()
		permissaoRecursoBeforeDeleteHooks = append(permissaoRecursoBeforeDeleteHooks, permissaoRecursoHook)
		permissaoRecursoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		permissaoRecursoAfterDeleteMu.Lock()
		permissaoRecursoAfterDeleteHooks = append(permissaoRecursoAfterDeleteHooks, permissaoRecursoHook)
		permissaoRecursoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		permissaoRecursoBeforeUpsertMu.Lock()
		permissaoRecursoBeforeUpsertHooks = append(permissaoRecursoBeforeUpsertHooks, permissaoRecursoHook)
		permissaoRecursoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		permissaoRecursoAfterUpsertMu.Lock()
		permissaoRecursoAfterUpsertHooks = append(permissaoRecursoAfterUpsertHooks, permissaoRecursoHook)
		permissaoRecursoAfterUpsertMu.Unlock()
	}
}

// One returns a single permissaoRecurso record from the query.
func (q permissaoRecursoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PermissaoRecurso, error) {
	o := &PermissaoRecurso{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for permissao_recurso")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PermissaoRecurso records from the query.
func (q permissaoRecursoQuery) All(ctx context.Context, exec boil.ContextExecutor) (PermissaoRecursoSlice, error) {
	var o []*PermissaoRecurso

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PermissaoRecurso slice")
	}

	if len(permissaoRecursoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PermissaoRecurso records in the query.
func (q permissaoRecursoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count permissao_recurso rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q permissaoRecursoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if permissao_recurso exists")
	}

	return count > 0, nil
}

// PrePermissaoRecursoAcaos retrieves all the permissao_recurso_acao's PermissaoRecursoAcaos with an executor via pre_id column.
func (o *PermissaoRecurso) PrePermissaoRecursoAcaos(mods ...qm.QueryMod) permissaoRecursoAcaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`permissao_recurso_acao`.`pre_id`=?", o.PreID),
	)

	return PermissaoRecursoAcaos(queryMods...)
}

// PrePermissaoRecursoCampos retrieves all the permissao_recurso_campo's PermissaoRecursoCampos with an executor via pre_id column.
func (o *PermissaoRecurso) PrePermissaoRecursoCampos(mods ...qm.QueryMod) permissaoRecursoCampoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`permissao_recurso_campos`.`pre_id`=?", o.PreID),
	)

	return PermissaoRecursoCampos(queryMods...)
}

// UfiUsuarioFilials retrieves all the usuario_filial's UsuarioFilials with an executor via ufi_id column.
func (o *PermissaoRecurso) UfiUsuarioFilials(mods ...qm.QueryMod) usuarioFilialQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("`usuario_filial_recurso` on `usuario_filial`.`ufi_id` = `usuario_filial_recurso`.`ufi_id`"),
		qm.Where("`usuario_filial_recurso`.`pre_id`=?", o.PreID),
	)

	return UsuarioFilials(queryMods...)
}

// LoadPrePermissaoRecursoAcaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (permissaoRecursoL) LoadPrePermissaoRecursoAcaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybePermissaoRecurso interface{}, mods queries.Applicator) error {
	var slice []*PermissaoRecurso
	var object *PermissaoRecurso

	if singular {
		var ok bool
		object, ok = maybePermissaoRecurso.(*PermissaoRecurso)
		if !ok {
			object = new(PermissaoRecurso)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePermissaoRecurso)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePermissaoRecurso))
			}
		}
	} else {
		s, ok := maybePermissaoRecurso.(*[]*PermissaoRecurso)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePermissaoRecurso)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePermissaoRecurso))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &permissaoRecursoR{}
		}
		args[object.PreID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &permissaoRecursoR{}
			}
			args[obj.PreID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`permissao_recurso_acao`),
		qm.WhereIn(`permissao_recurso_acao.pre_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load permissao_recurso_acao")
	}

	var resultSlice []*PermissaoRecursoAcao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice permissao_recurso_acao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on permissao_recurso_acao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permissao_recurso_acao")
	}

	if len(permissaoRecursoAcaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PrePermissaoRecursoAcaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &permissaoRecursoAcaoR{}
			}
			foreign.R.Pre = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PreID == foreign.PreID {
				local.R.PrePermissaoRecursoAcaos = append(local.R.PrePermissaoRecursoAcaos, foreign)
				if foreign.R == nil {
					foreign.R = &permissaoRecursoAcaoR{}
				}
				foreign.R.Pre = local
				break
			}
		}
	}

	return nil
}

// LoadPrePermissaoRecursoCampos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (permissaoRecursoL) LoadPrePermissaoRecursoCampos(ctx context.Context, e boil.ContextExecutor, singular bool, maybePermissaoRecurso interface{}, mods queries.Applicator) error {
	var slice []*PermissaoRecurso
	var object *PermissaoRecurso

	if singular {
		var ok bool
		object, ok = maybePermissaoRecurso.(*PermissaoRecurso)
		if !ok {
			object = new(PermissaoRecurso)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePermissaoRecurso)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePermissaoRecurso))
			}
		}
	} else {
		s, ok := maybePermissaoRecurso.(*[]*PermissaoRecurso)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePermissaoRecurso)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePermissaoRecurso))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &permissaoRecursoR{}
		}
		args[object.PreID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &permissaoRecursoR{}
			}
			args[obj.PreID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`permissao_recurso_campos`),
		qm.WhereIn(`permissao_recurso_campos.pre_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load permissao_recurso_campos")
	}

	var resultSlice []*PermissaoRecursoCampo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice permissao_recurso_campos")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on permissao_recurso_campos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permissao_recurso_campos")
	}

	if len(permissaoRecursoCampoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PrePermissaoRecursoCampos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &permissaoRecursoCampoR{}
			}
			foreign.R.Pre = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PreID == foreign.PreID {
				local.R.PrePermissaoRecursoCampos = append(local.R.PrePermissaoRecursoCampos, foreign)
				if foreign.R == nil {
					foreign.R = &permissaoRecursoCampoR{}
				}
				foreign.R.Pre = local
				break
			}
		}
	}

	return nil
}

// LoadUfiUsuarioFilials allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (permissaoRecursoL) LoadUfiUsuarioFilials(ctx context.Context, e boil.ContextExecutor, singular bool, maybePermissaoRecurso interface{}, mods queries.Applicator) error {
	var slice []*PermissaoRecurso
	var object *PermissaoRecurso

	if singular {
		var ok bool
		object, ok = maybePermissaoRecurso.(*PermissaoRecurso)
		if !ok {
			object = new(PermissaoRecurso)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePermissaoRecurso)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePermissaoRecurso))
			}
		}
	} else {
		s, ok := maybePermissaoRecurso.(*[]*PermissaoRecurso)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePermissaoRecurso)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePermissaoRecurso))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &permissaoRecursoR{}
		}
		args[object.PreID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &permissaoRecursoR{}
			}
			args[obj.PreID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.Select("`usuario_filial`.`ufi_id`, `usuario_filial`.`usu_id`, `usuario_filial`.`emf_id`, `a`.`pre_id`"),
		qm.From("`usuario_filial`"),
		qm.InnerJoin("`usuario_filial_recurso` as `a` on `usuario_filial`.`ufi_id` = `a`.`ufi_id`"),
		qm.WhereIn("`a`.`pre_id` in ?", argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load usuario_filial")
	}

	var resultSlice []*UsuarioFilial

	var localJoinCols []int
	for results.Next() {
		one := new(UsuarioFilial)
		var localJoinCol int

		err = results.Scan(&one.UfiID, &one.UsuID, &one.EmfID, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for usuario_filial")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice usuario_filial")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on usuario_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for usuario_filial")
	}

	if len(usuarioFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UfiUsuarioFilials = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &usuarioFilialR{}
			}
			foreign.R.PrePermissaoRecursos = append(foreign.R.PrePermissaoRecursos, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.PreID == localJoinCol {
				local.R.UfiUsuarioFilials = append(local.R.UfiUsuarioFilials, foreign)
				if foreign.R == nil {
					foreign.R = &usuarioFilialR{}
				}
				foreign.R.PrePermissaoRecursos = append(foreign.R.PrePermissaoRecursos, local)
				break
			}
		}
	}

	return nil
}

// AddPrePermissaoRecursoAcaos adds the given related objects to the existing relationships
// of the permissao_recurso, optionally inserting them as new records.
// Appends related to o.R.PrePermissaoRecursoAcaos.
// Sets related.R.Pre appropriately.
func (o *PermissaoRecurso) AddPrePermissaoRecursoAcaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissaoRecursoAcao) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PreID = o.PreID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `permissao_recurso_acao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"pre_id"}),
				strmangle.WhereClause("`", "`", 0, permissaoRecursoAcaoPrimaryKeyColumns),
			)
			values := []interface{}{o.PreID, rel.PracID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PreID = o.PreID
		}
	}

	if o.R == nil {
		o.R = &permissaoRecursoR{
			PrePermissaoRecursoAcaos: related,
		}
	} else {
		o.R.PrePermissaoRecursoAcaos = append(o.R.PrePermissaoRecursoAcaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &permissaoRecursoAcaoR{
				Pre: o,
			}
		} else {
			rel.R.Pre = o
		}
	}
	return nil
}

// AddPrePermissaoRecursoCampos adds the given related objects to the existing relationships
// of the permissao_recurso, optionally inserting them as new records.
// Appends related to o.R.PrePermissaoRecursoCampos.
// Sets related.R.Pre appropriately.
func (o *PermissaoRecurso) AddPrePermissaoRecursoCampos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissaoRecursoCampo) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PreID = o.PreID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `permissao_recurso_campos` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"pre_id"}),
				strmangle.WhereClause("`", "`", 0, permissaoRecursoCampoPrimaryKeyColumns),
			)
			values := []interface{}{o.PreID, rel.PRCPID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PreID = o.PreID
		}
	}

	if o.R == nil {
		o.R = &permissaoRecursoR{
			PrePermissaoRecursoCampos: related,
		}
	} else {
		o.R.PrePermissaoRecursoCampos = append(o.R.PrePermissaoRecursoCampos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &permissaoRecursoCampoR{
				Pre: o,
			}
		} else {
			rel.R.Pre = o
		}
	}
	return nil
}

// AddUfiUsuarioFilials adds the given related objects to the existing relationships
// of the permissao_recurso, optionally inserting them as new records.
// Appends related to o.R.UfiUsuarioFilials.
// Sets related.R.PrePermissaoRecursos appropriately.
func (o *PermissaoRecurso) AddUfiUsuarioFilials(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*UsuarioFilial) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into `usuario_filial_recurso` (`pre_id`, `ufi_id`) values (?, ?)"
		values := []interface{}{o.PreID, rel.UfiID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &permissaoRecursoR{
			UfiUsuarioFilials: related,
		}
	} else {
		o.R.UfiUsuarioFilials = append(o.R.UfiUsuarioFilials, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &usuarioFilialR{
				PrePermissaoRecursos: PermissaoRecursoSlice{o},
			}
		} else {
			rel.R.PrePermissaoRecursos = append(rel.R.PrePermissaoRecursos, o)
		}
	}
	return nil
}

// SetUfiUsuarioFilials removes all previously related items of the
// permissao_recurso replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.PrePermissaoRecursos's UfiUsuarioFilials accordingly.
// Replaces o.R.UfiUsuarioFilials with related.
// Sets related.R.PrePermissaoRecursos's UfiUsuarioFilials accordingly.
func (o *PermissaoRecurso) SetUfiUsuarioFilials(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*UsuarioFilial) error {
	query := "delete from `usuario_filial_recurso` where `pre_id` = ?"
	values := []interface{}{o.PreID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeUfiUsuarioFilialsFromPrePermissaoRecursosSlice(o, related)
	if o.R != nil {
		o.R.UfiUsuarioFilials = nil
	}

	return o.AddUfiUsuarioFilials(ctx, exec, insert, related...)
}

// RemoveUfiUsuarioFilials relationships from objects passed in.
// Removes related items from R.UfiUsuarioFilials (uses pointer comparison, removal does not keep order)
// Sets related.R.PrePermissaoRecursos.
func (o *PermissaoRecurso) RemoveUfiUsuarioFilials(ctx context.Context, exec boil.ContextExecutor, related ...*UsuarioFilial) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from `usuario_filial_recurso` where `pre_id` = ? and `ufi_id` in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.PreID}
	for _, rel := range related {
		values = append(values, rel.UfiID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeUfiUsuarioFilialsFromPrePermissaoRecursosSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UfiUsuarioFilials {
			if rel != ri {
				continue
			}

			ln := len(o.R.UfiUsuarioFilials)
			if ln > 1 && i < ln-1 {
				o.R.UfiUsuarioFilials[i] = o.R.UfiUsuarioFilials[ln-1]
			}
			o.R.UfiUsuarioFilials = o.R.UfiUsuarioFilials[:ln-1]
			break
		}
	}

	return nil
}

func removeUfiUsuarioFilialsFromPrePermissaoRecursosSlice(o *PermissaoRecurso, related []*UsuarioFilial) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.PrePermissaoRecursos {
			if o.PreID != ri.PreID {
				continue
			}

			ln := len(rel.R.PrePermissaoRecursos)
			if ln > 1 && i < ln-1 {
				rel.R.PrePermissaoRecursos[i] = rel.R.PrePermissaoRecursos[ln-1]
			}
			rel.R.PrePermissaoRecursos = rel.R.PrePermissaoRecursos[:ln-1]
			break
		}
	}
}

// PermissaoRecursos retrieves all the records using an executor.
func PermissaoRecursos(mods ...qm.QueryMod) permissaoRecursoQuery {
	mods = append(mods, qm.From("`permissao_recurso`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`permissao_recurso`.*"})
	}

	return permissaoRecursoQuery{q}
}

// FindPermissaoRecurso retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPermissaoRecurso(ctx context.Context, exec boil.ContextExecutor, preID int, selectCols ...string) (*PermissaoRecurso, error) {
	permissaoRecursoObj := &PermissaoRecurso{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `permissao_recurso` where `pre_id`=?", sel,
	)

	q := queries.Raw(query, preID)

	err := q.Bind(ctx, exec, permissaoRecursoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from permissao_recurso")
	}

	if err = permissaoRecursoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return permissaoRecursoObj, err
	}

	return permissaoRecursoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PermissaoRecurso) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no permissao_recurso provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(permissaoRecursoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	permissaoRecursoInsertCacheMut.RLock()
	cache, cached := permissaoRecursoInsertCache[key]
	permissaoRecursoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			permissaoRecursoAllColumns,
			permissaoRecursoColumnsWithDefault,
			permissaoRecursoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(permissaoRecursoType, permissaoRecursoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(permissaoRecursoType, permissaoRecursoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `permissao_recurso` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `permissao_recurso` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `permissao_recurso` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, permissaoRecursoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into permissao_recurso")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.PreID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for permissao_recurso")
	}

CacheNoHooks:
	if !cached {
		permissaoRecursoInsertCacheMut.Lock()
		permissaoRecursoInsertCache[key] = cache
		permissaoRecursoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PermissaoRecurso.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PermissaoRecurso) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	permissaoRecursoUpdateCacheMut.RLock()
	cache, cached := permissaoRecursoUpdateCache[key]
	permissaoRecursoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			permissaoRecursoAllColumns,
			permissaoRecursoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update permissao_recurso, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `permissao_recurso` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, permissaoRecursoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(permissaoRecursoType, permissaoRecursoMapping, append(wl, permissaoRecursoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update permissao_recurso row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for permissao_recurso")
	}

	if !cached {
		permissaoRecursoUpdateCacheMut.Lock()
		permissaoRecursoUpdateCache[key] = cache
		permissaoRecursoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q permissaoRecursoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for permissao_recurso")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for permissao_recurso")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PermissaoRecursoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissaoRecursoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `permissao_recurso` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, permissaoRecursoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in permissaoRecurso slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all permissaoRecurso")
	}
	return rowsAff, nil
}

var mySQLPermissaoRecursoUniqueColumns = []string{
	"pre_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PermissaoRecurso) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no permissao_recurso provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(permissaoRecursoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLPermissaoRecursoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	permissaoRecursoUpsertCacheMut.RLock()
	cache, cached := permissaoRecursoUpsertCache[key]
	permissaoRecursoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			permissaoRecursoAllColumns,
			permissaoRecursoColumnsWithDefault,
			permissaoRecursoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			permissaoRecursoAllColumns,
			permissaoRecursoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert permissao_recurso, could not build update column list")
		}

		ret := strmangle.SetComplement(permissaoRecursoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`permissao_recurso`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `permissao_recurso` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(permissaoRecursoType, permissaoRecursoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(permissaoRecursoType, permissaoRecursoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for permissao_recurso")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(permissaoRecursoType, permissaoRecursoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for permissao_recurso")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for permissao_recurso")
	}

CacheNoHooks:
	if !cached {
		permissaoRecursoUpsertCacheMut.Lock()
		permissaoRecursoUpsertCache[key] = cache
		permissaoRecursoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PermissaoRecurso record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PermissaoRecurso) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PermissaoRecurso provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), permissaoRecursoPrimaryKeyMapping)
	sql := "DELETE FROM `permissao_recurso` WHERE `pre_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from permissao_recurso")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for permissao_recurso")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q permissaoRecursoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no permissaoRecursoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from permissao_recurso")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for permissao_recurso")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PermissaoRecursoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(permissaoRecursoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissaoRecursoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `permissao_recurso` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, permissaoRecursoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from permissaoRecurso slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for permissao_recurso")
	}

	if len(permissaoRecursoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PermissaoRecurso) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPermissaoRecurso(ctx, exec, o.PreID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PermissaoRecursoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PermissaoRecursoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissaoRecursoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `permissao_recurso`.* FROM `permissao_recurso` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, permissaoRecursoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PermissaoRecursoSlice")
	}

	*o = slice

	return nil
}

// PermissaoRecursoExists checks if the PermissaoRecurso row exists.
func PermissaoRecursoExists(ctx context.Context, exec boil.ContextExecutor, preID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `permissao_recurso` where `pre_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, preID)
	}
	row := exec.QueryRowContext(ctx, sql, preID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if permissao_recurso exists")
	}

	return exists, nil
}

// Exists checks if the PermissaoRecurso row exists.
func (o *PermissaoRecurso) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PermissaoRecursoExists(ctx, exec, o.PreID)
}
