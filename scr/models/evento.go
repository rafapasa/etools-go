// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// Evento is an object representing the database table.
type Evento struct {
	EveNumero    int    `boil:"eve_numero" json:"eve_numero" toml:"eve_numero" yaml:"eve_numero"`
	EmfID        int    `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	EveDescricao string `boil:"eve_descricao" json:"eve_descricao" toml:"eve_descricao" yaml:"eve_descricao"`
	// 1 - gerado pelo sistema
	//             2 - calculado dinamicamente
	//             3 - informado pelo usuário
	EveTipo      int16             `boil:"eve_tipo" json:"eve_tipo" toml:"eve_tipo" yaml:"eve_tipo"`
	EveValorfixo types.NullDecimal `boil:"eve_valorfixo" json:"eve_valorfixo,omitempty" toml:"eve_valorfixo" yaml:"eve_valorfixo,omitempty"`

	R *eventoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L eventoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EventoColumns = struct {
	EveNumero    string
	EmfID        string
	EveDescricao string
	EveTipo      string
	EveValorfixo string
}{
	EveNumero:    "eve_numero",
	EmfID:        "emf_id",
	EveDescricao: "eve_descricao",
	EveTipo:      "eve_tipo",
	EveValorfixo: "eve_valorfixo",
}

var EventoTableColumns = struct {
	EveNumero    string
	EmfID        string
	EveDescricao string
	EveTipo      string
	EveValorfixo string
}{
	EveNumero:    "evento.eve_numero",
	EmfID:        "evento.emf_id",
	EveDescricao: "evento.eve_descricao",
	EveTipo:      "evento.eve_tipo",
	EveValorfixo: "evento.eve_valorfixo",
}

// Generated where

var EventoWhere = struct {
	EveNumero    whereHelperint
	EmfID        whereHelperint
	EveDescricao whereHelperstring
	EveTipo      whereHelperint16
	EveValorfixo whereHelpertypes_NullDecimal
}{
	EveNumero:    whereHelperint{field: "`evento`.`eve_numero`"},
	EmfID:        whereHelperint{field: "`evento`.`emf_id`"},
	EveDescricao: whereHelperstring{field: "`evento`.`eve_descricao`"},
	EveTipo:      whereHelperint16{field: "`evento`.`eve_tipo`"},
	EveValorfixo: whereHelpertypes_NullDecimal{field: "`evento`.`eve_valorfixo`"},
}

// EventoRels is where relationship names are stored.
var EventoRels = struct {
	Emf                          string
	EvoEveNumeroEventoOperacaos  string
	EveNumeroEventoOperacaos     string
	EveNumeroRotinaContabilItens string
}{
	Emf:                          "Emf",
	EvoEveNumeroEventoOperacaos:  "EvoEveNumeroEventoOperacaos",
	EveNumeroEventoOperacaos:     "EveNumeroEventoOperacaos",
	EveNumeroRotinaContabilItens: "EveNumeroRotinaContabilItens",
}

// eventoR is where relationships are stored.
type eventoR struct {
	Emf                          *EmpresaFilial          `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	EvoEveNumeroEventoOperacaos  EventoOperacaoSlice     `boil:"EvoEveNumeroEventoOperacaos" json:"EvoEveNumeroEventoOperacaos" toml:"EvoEveNumeroEventoOperacaos" yaml:"EvoEveNumeroEventoOperacaos"`
	EveNumeroEventoOperacaos     EventoOperacaoSlice     `boil:"EveNumeroEventoOperacaos" json:"EveNumeroEventoOperacaos" toml:"EveNumeroEventoOperacaos" yaml:"EveNumeroEventoOperacaos"`
	EveNumeroRotinaContabilItens RotinaContabilItenSlice `boil:"EveNumeroRotinaContabilItens" json:"EveNumeroRotinaContabilItens" toml:"EveNumeroRotinaContabilItens" yaml:"EveNumeroRotinaContabilItens"`
}

// NewStruct creates a new relationship struct
func (*eventoR) NewStruct() *eventoR {
	return &eventoR{}
}

func (r *eventoR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *eventoR) GetEvoEveNumeroEventoOperacaos() EventoOperacaoSlice {
	if r == nil {
		return nil
	}
	return r.EvoEveNumeroEventoOperacaos
}

func (r *eventoR) GetEveNumeroEventoOperacaos() EventoOperacaoSlice {
	if r == nil {
		return nil
	}
	return r.EveNumeroEventoOperacaos
}

func (r *eventoR) GetEveNumeroRotinaContabilItens() RotinaContabilItenSlice {
	if r == nil {
		return nil
	}
	return r.EveNumeroRotinaContabilItens
}

// eventoL is where Load methods for each relationship are stored.
type eventoL struct{}

var (
	eventoAllColumns            = []string{"eve_numero", "emf_id", "eve_descricao", "eve_tipo", "eve_valorfixo"}
	eventoColumnsWithoutDefault = []string{"eve_numero", "emf_id", "eve_descricao", "eve_tipo", "eve_valorfixo"}
	eventoColumnsWithDefault    = []string{}
	eventoPrimaryKeyColumns     = []string{"eve_numero"}
	eventoGeneratedColumns      = []string{}
)

type (
	// EventoSlice is an alias for a slice of pointers to Evento.
	// This should almost always be used instead of []Evento.
	EventoSlice []*Evento
	// EventoHook is the signature for custom Evento hook methods
	EventoHook func(context.Context, boil.ContextExecutor, *Evento) error

	eventoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	eventoType                 = reflect.TypeOf(&Evento{})
	eventoMapping              = queries.MakeStructMapping(eventoType)
	eventoPrimaryKeyMapping, _ = queries.BindMapping(eventoType, eventoMapping, eventoPrimaryKeyColumns)
	eventoInsertCacheMut       sync.RWMutex
	eventoInsertCache          = make(map[string]insertCache)
	eventoUpdateCacheMut       sync.RWMutex
	eventoUpdateCache          = make(map[string]updateCache)
	eventoUpsertCacheMut       sync.RWMutex
	eventoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var eventoAfterSelectMu sync.Mutex
var eventoAfterSelectHooks []EventoHook

var eventoBeforeInsertMu sync.Mutex
var eventoBeforeInsertHooks []EventoHook
var eventoAfterInsertMu sync.Mutex
var eventoAfterInsertHooks []EventoHook

var eventoBeforeUpdateMu sync.Mutex
var eventoBeforeUpdateHooks []EventoHook
var eventoAfterUpdateMu sync.Mutex
var eventoAfterUpdateHooks []EventoHook

var eventoBeforeDeleteMu sync.Mutex
var eventoBeforeDeleteHooks []EventoHook
var eventoAfterDeleteMu sync.Mutex
var eventoAfterDeleteHooks []EventoHook

var eventoBeforeUpsertMu sync.Mutex
var eventoBeforeUpsertHooks []EventoHook
var eventoAfterUpsertMu sync.Mutex
var eventoAfterUpsertHooks []EventoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Evento) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Evento) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Evento) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Evento) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Evento) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Evento) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Evento) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Evento) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Evento) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range eventoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEventoHook registers your hook function for all future operations.
func AddEventoHook(hookPoint boil.HookPoint, eventoHook EventoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		eventoAfterSelectMu.Lock()
		eventoAfterSelectHooks = append(eventoAfterSelectHooks, eventoHook)
		eventoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		eventoBeforeInsertMu.Lock()
		eventoBeforeInsertHooks = append(eventoBeforeInsertHooks, eventoHook)
		eventoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		eventoAfterInsertMu.Lock()
		eventoAfterInsertHooks = append(eventoAfterInsertHooks, eventoHook)
		eventoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		eventoBeforeUpdateMu.Lock()
		eventoBeforeUpdateHooks = append(eventoBeforeUpdateHooks, eventoHook)
		eventoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		eventoAfterUpdateMu.Lock()
		eventoAfterUpdateHooks = append(eventoAfterUpdateHooks, eventoHook)
		eventoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		eventoBeforeDeleteMu.Lock()
		eventoBeforeDeleteHooks = append(eventoBeforeDeleteHooks, eventoHook)
		eventoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		eventoAfterDeleteMu.Lock()
		eventoAfterDeleteHooks = append(eventoAfterDeleteHooks, eventoHook)
		eventoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		eventoBeforeUpsertMu.Lock()
		eventoBeforeUpsertHooks = append(eventoBeforeUpsertHooks, eventoHook)
		eventoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		eventoAfterUpsertMu.Lock()
		eventoAfterUpsertHooks = append(eventoAfterUpsertHooks, eventoHook)
		eventoAfterUpsertMu.Unlock()
	}
}

// One returns a single evento record from the query.
func (q eventoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Evento, error) {
	o := &Evento{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for evento")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Evento records from the query.
func (q eventoQuery) All(ctx context.Context, exec boil.ContextExecutor) (EventoSlice, error) {
	var o []*Evento

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Evento slice")
	}

	if len(eventoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Evento records in the query.
func (q eventoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count evento rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q eventoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if evento exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *Evento) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// EvoEveNumeroEventoOperacaos retrieves all the evento_operacao's EventoOperacaos with an executor via evo_eve_numero column.
func (o *Evento) EvoEveNumeroEventoOperacaos(mods ...qm.QueryMod) eventoOperacaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`evento_operacao`.`evo_eve_numero`=?", o.EveNumero),
	)

	return EventoOperacaos(queryMods...)
}

// EveNumeroEventoOperacaos retrieves all the evento_operacao's EventoOperacaos with an executor via eve_numero column.
func (o *Evento) EveNumeroEventoOperacaos(mods ...qm.QueryMod) eventoOperacaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`evento_operacao`.`eve_numero`=?", o.EveNumero),
	)

	return EventoOperacaos(queryMods...)
}

// EveNumeroRotinaContabilItens retrieves all the rotina_contabil_iten's RotinaContabilItens with an executor via eve_numero column.
func (o *Evento) EveNumeroRotinaContabilItens(mods ...qm.QueryMod) rotinaContabilItenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`rotina_contabil_itens`.`eve_numero`=?", o.EveNumero),
	)

	return RotinaContabilItens(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (eventoL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEvento interface{}, mods queries.Applicator) error {
	var slice []*Evento
	var object *Evento

	if singular {
		var ok bool
		object, ok = maybeEvento.(*Evento)
		if !ok {
			object = new(Evento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEvento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEvento))
			}
		}
	} else {
		s, ok := maybeEvento.(*[]*Evento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEvento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEvento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &eventoR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &eventoR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfEventos = append(foreign.R.EmfEventos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfEventos = append(foreign.R.EmfEventos, local)
				break
			}
		}
	}

	return nil
}

// LoadEvoEveNumeroEventoOperacaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (eventoL) LoadEvoEveNumeroEventoOperacaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEvento interface{}, mods queries.Applicator) error {
	var slice []*Evento
	var object *Evento

	if singular {
		var ok bool
		object, ok = maybeEvento.(*Evento)
		if !ok {
			object = new(Evento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEvento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEvento))
			}
		}
	} else {
		s, ok := maybeEvento.(*[]*Evento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEvento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEvento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &eventoR{}
		}
		args[object.EveNumero] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &eventoR{}
			}
			args[obj.EveNumero] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`evento_operacao`),
		qm.WhereIn(`evento_operacao.evo_eve_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load evento_operacao")
	}

	var resultSlice []*EventoOperacao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice evento_operacao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on evento_operacao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for evento_operacao")
	}

	if len(eventoOperacaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.EvoEveNumeroEventoOperacaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &eventoOperacaoR{}
			}
			foreign.R.EvoEveNumeroEvento = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.EveNumero == foreign.EvoEveNumero {
				local.R.EvoEveNumeroEventoOperacaos = append(local.R.EvoEveNumeroEventoOperacaos, foreign)
				if foreign.R == nil {
					foreign.R = &eventoOperacaoR{}
				}
				foreign.R.EvoEveNumeroEvento = local
				break
			}
		}
	}

	return nil
}

// LoadEveNumeroEventoOperacaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (eventoL) LoadEveNumeroEventoOperacaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEvento interface{}, mods queries.Applicator) error {
	var slice []*Evento
	var object *Evento

	if singular {
		var ok bool
		object, ok = maybeEvento.(*Evento)
		if !ok {
			object = new(Evento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEvento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEvento))
			}
		}
	} else {
		s, ok := maybeEvento.(*[]*Evento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEvento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEvento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &eventoR{}
		}
		args[object.EveNumero] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &eventoR{}
			}
			args[obj.EveNumero] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`evento_operacao`),
		qm.WhereIn(`evento_operacao.eve_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load evento_operacao")
	}

	var resultSlice []*EventoOperacao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice evento_operacao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on evento_operacao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for evento_operacao")
	}

	if len(eventoOperacaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.EveNumeroEventoOperacaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &eventoOperacaoR{}
			}
			foreign.R.EveNumeroEvento = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.EveNumero == foreign.EveNumero {
				local.R.EveNumeroEventoOperacaos = append(local.R.EveNumeroEventoOperacaos, foreign)
				if foreign.R == nil {
					foreign.R = &eventoOperacaoR{}
				}
				foreign.R.EveNumeroEvento = local
				break
			}
		}
	}

	return nil
}

// LoadEveNumeroRotinaContabilItens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (eventoL) LoadEveNumeroRotinaContabilItens(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEvento interface{}, mods queries.Applicator) error {
	var slice []*Evento
	var object *Evento

	if singular {
		var ok bool
		object, ok = maybeEvento.(*Evento)
		if !ok {
			object = new(Evento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEvento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEvento))
			}
		}
	} else {
		s, ok := maybeEvento.(*[]*Evento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEvento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEvento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &eventoR{}
		}
		args[object.EveNumero] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &eventoR{}
			}
			args[obj.EveNumero] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil_itens`),
		qm.WhereIn(`rotina_contabil_itens.eve_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load rotina_contabil_itens")
	}

	var resultSlice []*RotinaContabilIten
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice rotina_contabil_itens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on rotina_contabil_itens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil_itens")
	}

	if len(rotinaContabilItenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.EveNumeroRotinaContabilItens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &rotinaContabilItenR{}
			}
			foreign.R.EveNumeroEvento = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.EveNumero == foreign.EveNumero {
				local.R.EveNumeroRotinaContabilItens = append(local.R.EveNumeroRotinaContabilItens, foreign)
				if foreign.R == nil {
					foreign.R = &rotinaContabilItenR{}
				}
				foreign.R.EveNumeroEvento = local
				break
			}
		}
	}

	return nil
}

// SetEmf of the evento to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfEventos.
func (o *Evento) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `evento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, eventoPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.EveNumero}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &eventoR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfEventos: EventoSlice{o},
		}
	} else {
		related.R.EmfEventos = append(related.R.EmfEventos, o)
	}

	return nil
}

// AddEvoEveNumeroEventoOperacaos adds the given related objects to the existing relationships
// of the evento, optionally inserting them as new records.
// Appends related to o.R.EvoEveNumeroEventoOperacaos.
// Sets related.R.EvoEveNumeroEvento appropriately.
func (o *Evento) AddEvoEveNumeroEventoOperacaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EventoOperacao) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.EvoEveNumero = o.EveNumero
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `evento_operacao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"evo_eve_numero"}),
				strmangle.WhereClause("`", "`", 0, eventoOperacaoPrimaryKeyColumns),
			)
			values := []interface{}{o.EveNumero, rel.EveNumero, rel.EvoItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.EvoEveNumero = o.EveNumero
		}
	}

	if o.R == nil {
		o.R = &eventoR{
			EvoEveNumeroEventoOperacaos: related,
		}
	} else {
		o.R.EvoEveNumeroEventoOperacaos = append(o.R.EvoEveNumeroEventoOperacaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &eventoOperacaoR{
				EvoEveNumeroEvento: o,
			}
		} else {
			rel.R.EvoEveNumeroEvento = o
		}
	}
	return nil
}

// AddEveNumeroEventoOperacaos adds the given related objects to the existing relationships
// of the evento, optionally inserting them as new records.
// Appends related to o.R.EveNumeroEventoOperacaos.
// Sets related.R.EveNumeroEvento appropriately.
func (o *Evento) AddEveNumeroEventoOperacaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EventoOperacao) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.EveNumero = o.EveNumero
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `evento_operacao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"eve_numero"}),
				strmangle.WhereClause("`", "`", 0, eventoOperacaoPrimaryKeyColumns),
			)
			values := []interface{}{o.EveNumero, rel.EveNumero, rel.EvoItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.EveNumero = o.EveNumero
		}
	}

	if o.R == nil {
		o.R = &eventoR{
			EveNumeroEventoOperacaos: related,
		}
	} else {
		o.R.EveNumeroEventoOperacaos = append(o.R.EveNumeroEventoOperacaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &eventoOperacaoR{
				EveNumeroEvento: o,
			}
		} else {
			rel.R.EveNumeroEvento = o
		}
	}
	return nil
}

// AddEveNumeroRotinaContabilItens adds the given related objects to the existing relationships
// of the evento, optionally inserting them as new records.
// Appends related to o.R.EveNumeroRotinaContabilItens.
// Sets related.R.EveNumeroEvento appropriately.
func (o *Evento) AddEveNumeroRotinaContabilItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RotinaContabilIten) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.EveNumero = o.EveNumero
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `rotina_contabil_itens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"eve_numero"}),
				strmangle.WhereClause("`", "`", 0, rotinaContabilItenPrimaryKeyColumns),
			)
			values := []interface{}{o.EveNumero, rel.RocID, rel.RociItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.EveNumero = o.EveNumero
		}
	}

	if o.R == nil {
		o.R = &eventoR{
			EveNumeroRotinaContabilItens: related,
		}
	} else {
		o.R.EveNumeroRotinaContabilItens = append(o.R.EveNumeroRotinaContabilItens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &rotinaContabilItenR{
				EveNumeroEvento: o,
			}
		} else {
			rel.R.EveNumeroEvento = o
		}
	}
	return nil
}

// Eventos retrieves all the records using an executor.
func Eventos(mods ...qm.QueryMod) eventoQuery {
	mods = append(mods, qm.From("`evento`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`evento`.*"})
	}

	return eventoQuery{q}
}

// FindEvento retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEvento(ctx context.Context, exec boil.ContextExecutor, eveNumero int, selectCols ...string) (*Evento, error) {
	eventoObj := &Evento{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `evento` where `eve_numero`=?", sel,
	)

	q := queries.Raw(query, eveNumero)

	err := q.Bind(ctx, exec, eventoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from evento")
	}

	if err = eventoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return eventoObj, err
	}

	return eventoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Evento) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no evento provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(eventoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	eventoInsertCacheMut.RLock()
	cache, cached := eventoInsertCache[key]
	eventoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			eventoAllColumns,
			eventoColumnsWithDefault,
			eventoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(eventoType, eventoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(eventoType, eventoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `evento` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `evento` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `evento` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, eventoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into evento")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.EveNumero,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for evento")
	}

CacheNoHooks:
	if !cached {
		eventoInsertCacheMut.Lock()
		eventoInsertCache[key] = cache
		eventoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Evento.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Evento) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	eventoUpdateCacheMut.RLock()
	cache, cached := eventoUpdateCache[key]
	eventoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			eventoAllColumns,
			eventoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update evento, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `evento` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, eventoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(eventoType, eventoMapping, append(wl, eventoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update evento row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for evento")
	}

	if !cached {
		eventoUpdateCacheMut.Lock()
		eventoUpdateCache[key] = cache
		eventoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q eventoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for evento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for evento")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EventoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), eventoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `evento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, eventoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in evento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all evento")
	}
	return rowsAff, nil
}

var mySQLEventoUniqueColumns = []string{
	"eve_numero",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Evento) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no evento provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(eventoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLEventoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	eventoUpsertCacheMut.RLock()
	cache, cached := eventoUpsertCache[key]
	eventoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			eventoAllColumns,
			eventoColumnsWithDefault,
			eventoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			eventoAllColumns,
			eventoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert evento, could not build update column list")
		}

		ret := strmangle.SetComplement(eventoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`evento`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `evento` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(eventoType, eventoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(eventoType, eventoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for evento")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(eventoType, eventoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for evento")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for evento")
	}

CacheNoHooks:
	if !cached {
		eventoUpsertCacheMut.Lock()
		eventoUpsertCache[key] = cache
		eventoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Evento record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Evento) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Evento provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), eventoPrimaryKeyMapping)
	sql := "DELETE FROM `evento` WHERE `eve_numero`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from evento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for evento")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q eventoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no eventoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from evento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for evento")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EventoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(eventoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), eventoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `evento` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, eventoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from evento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for evento")
	}

	if len(eventoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Evento) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEvento(ctx, exec, o.EveNumero)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EventoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EventoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), eventoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `evento`.* FROM `evento` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, eventoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in EventoSlice")
	}

	*o = slice

	return nil
}

// EventoExists checks if the Evento row exists.
func EventoExists(ctx context.Context, exec boil.ContextExecutor, eveNumero int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `evento` where `eve_numero`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, eveNumero)
	}
	row := exec.QueryRowContext(ctx, sql, eveNumero)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if evento exists")
	}

	return exists, nil
}

// Exists checks if the Evento row exists.
func (o *Evento) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return EventoExists(ctx, exec, o.EveNumero)
}
