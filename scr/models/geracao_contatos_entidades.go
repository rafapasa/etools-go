// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// GeracaoContatosEntidade is an object representing the database table.
type GeracaoContatosEntidade struct {
	GceID                int       `boil:"gce_id" json:"gce_id" toml:"gce_id" yaml:"gce_id"`
	EmfID                int       `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	GceDatageracao       time.Time `boil:"gce_datageracao" json:"gce_datageracao" toml:"gce_datageracao" yaml:"gce_datageracao"`
	GceDatareferenciaIni time.Time `boil:"gce_datareferencia_ini" json:"gce_datareferencia_ini" toml:"gce_datareferencia_ini" yaml:"gce_datareferencia_ini"`
	GceDatareferenciaFim time.Time `boil:"gce_datareferencia_fim" json:"gce_datareferencia_fim" toml:"gce_datareferencia_fim" yaml:"gce_datareferencia_fim"`
	// 0 = aberto / 1 = gerado / 2 = efetivado / 9 = cancelado
	GceSituacao int      `boil:"gce_situacao" json:"gce_situacao" toml:"gce_situacao" yaml:"gce_situacao"`
	GceTipo     null.Int `boil:"gce_tipo" json:"gce_tipo,omitempty" toml:"gce_tipo" yaml:"gce_tipo,omitempty"`

	R *geracaoContatosEntidadeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L geracaoContatosEntidadeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var GeracaoContatosEntidadeColumns = struct {
	GceID                string
	EmfID                string
	GceDatageracao       string
	GceDatareferenciaIni string
	GceDatareferenciaFim string
	GceSituacao          string
	GceTipo              string
}{
	GceID:                "gce_id",
	EmfID:                "emf_id",
	GceDatageracao:       "gce_datageracao",
	GceDatareferenciaIni: "gce_datareferencia_ini",
	GceDatareferenciaFim: "gce_datareferencia_fim",
	GceSituacao:          "gce_situacao",
	GceTipo:              "gce_tipo",
}

var GeracaoContatosEntidadeTableColumns = struct {
	GceID                string
	EmfID                string
	GceDatageracao       string
	GceDatareferenciaIni string
	GceDatareferenciaFim string
	GceSituacao          string
	GceTipo              string
}{
	GceID:                "geracao_contatos_entidades.gce_id",
	EmfID:                "geracao_contatos_entidades.emf_id",
	GceDatageracao:       "geracao_contatos_entidades.gce_datageracao",
	GceDatareferenciaIni: "geracao_contatos_entidades.gce_datareferencia_ini",
	GceDatareferenciaFim: "geracao_contatos_entidades.gce_datareferencia_fim",
	GceSituacao:          "geracao_contatos_entidades.gce_situacao",
	GceTipo:              "geracao_contatos_entidades.gce_tipo",
}

// Generated where

var GeracaoContatosEntidadeWhere = struct {
	GceID                whereHelperint
	EmfID                whereHelperint
	GceDatageracao       whereHelpertime_Time
	GceDatareferenciaIni whereHelpertime_Time
	GceDatareferenciaFim whereHelpertime_Time
	GceSituacao          whereHelperint
	GceTipo              whereHelpernull_Int
}{
	GceID:                whereHelperint{field: "`geracao_contatos_entidades`.`gce_id`"},
	EmfID:                whereHelperint{field: "`geracao_contatos_entidades`.`emf_id`"},
	GceDatageracao:       whereHelpertime_Time{field: "`geracao_contatos_entidades`.`gce_datageracao`"},
	GceDatareferenciaIni: whereHelpertime_Time{field: "`geracao_contatos_entidades`.`gce_datareferencia_ini`"},
	GceDatareferenciaFim: whereHelpertime_Time{field: "`geracao_contatos_entidades`.`gce_datareferencia_fim`"},
	GceSituacao:          whereHelperint{field: "`geracao_contatos_entidades`.`gce_situacao`"},
	GceTipo:              whereHelpernull_Int{field: "`geracao_contatos_entidades`.`gce_tipo`"},
}

// GeracaoContatosEntidadeRels is where relationship names are stored.
var GeracaoContatosEntidadeRels = struct {
	Emf                                    string
	GceGeracaoContatosEntidadesFaturas     string
	GceGeracaoContatosEntidadesNotaFiscals string
}{
	Emf:                                    "Emf",
	GceGeracaoContatosEntidadesFaturas:     "GceGeracaoContatosEntidadesFaturas",
	GceGeracaoContatosEntidadesNotaFiscals: "GceGeracaoContatosEntidadesNotaFiscals",
}

// geracaoContatosEntidadeR is where relationships are stored.
type geracaoContatosEntidadeR struct {
	Emf                                    *EmpresaFilial                          `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	GceGeracaoContatosEntidadesFaturas     GeracaoContatosEntidadesFaturasSlice    `boil:"GceGeracaoContatosEntidadesFaturas" json:"GceGeracaoContatosEntidadesFaturas" toml:"GceGeracaoContatosEntidadesFaturas" yaml:"GceGeracaoContatosEntidadesFaturas"`
	GceGeracaoContatosEntidadesNotaFiscals GeracaoContatosEntidadesNotaFiscalSlice `boil:"GceGeracaoContatosEntidadesNotaFiscals" json:"GceGeracaoContatosEntidadesNotaFiscals" toml:"GceGeracaoContatosEntidadesNotaFiscals" yaml:"GceGeracaoContatosEntidadesNotaFiscals"`
}

// NewStruct creates a new relationship struct
func (*geracaoContatosEntidadeR) NewStruct() *geracaoContatosEntidadeR {
	return &geracaoContatosEntidadeR{}
}

func (r *geracaoContatosEntidadeR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *geracaoContatosEntidadeR) GetGceGeracaoContatosEntidadesFaturas() GeracaoContatosEntidadesFaturasSlice {
	if r == nil {
		return nil
	}
	return r.GceGeracaoContatosEntidadesFaturas
}

func (r *geracaoContatosEntidadeR) GetGceGeracaoContatosEntidadesNotaFiscals() GeracaoContatosEntidadesNotaFiscalSlice {
	if r == nil {
		return nil
	}
	return r.GceGeracaoContatosEntidadesNotaFiscals
}

// geracaoContatosEntidadeL is where Load methods for each relationship are stored.
type geracaoContatosEntidadeL struct{}

var (
	geracaoContatosEntidadeAllColumns            = []string{"gce_id", "emf_id", "gce_datageracao", "gce_datareferencia_ini", "gce_datareferencia_fim", "gce_situacao", "gce_tipo"}
	geracaoContatosEntidadeColumnsWithoutDefault = []string{"gce_id", "emf_id", "gce_datageracao", "gce_datareferencia_ini", "gce_datareferencia_fim", "gce_tipo"}
	geracaoContatosEntidadeColumnsWithDefault    = []string{"gce_situacao"}
	geracaoContatosEntidadePrimaryKeyColumns     = []string{"gce_id"}
	geracaoContatosEntidadeGeneratedColumns      = []string{}
)

type (
	// GeracaoContatosEntidadeSlice is an alias for a slice of pointers to GeracaoContatosEntidade.
	// This should almost always be used instead of []GeracaoContatosEntidade.
	GeracaoContatosEntidadeSlice []*GeracaoContatosEntidade
	// GeracaoContatosEntidadeHook is the signature for custom GeracaoContatosEntidade hook methods
	GeracaoContatosEntidadeHook func(context.Context, boil.ContextExecutor, *GeracaoContatosEntidade) error

	geracaoContatosEntidadeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	geracaoContatosEntidadeType                 = reflect.TypeOf(&GeracaoContatosEntidade{})
	geracaoContatosEntidadeMapping              = queries.MakeStructMapping(geracaoContatosEntidadeType)
	geracaoContatosEntidadePrimaryKeyMapping, _ = queries.BindMapping(geracaoContatosEntidadeType, geracaoContatosEntidadeMapping, geracaoContatosEntidadePrimaryKeyColumns)
	geracaoContatosEntidadeInsertCacheMut       sync.RWMutex
	geracaoContatosEntidadeInsertCache          = make(map[string]insertCache)
	geracaoContatosEntidadeUpdateCacheMut       sync.RWMutex
	geracaoContatosEntidadeUpdateCache          = make(map[string]updateCache)
	geracaoContatosEntidadeUpsertCacheMut       sync.RWMutex
	geracaoContatosEntidadeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var geracaoContatosEntidadeAfterSelectMu sync.Mutex
var geracaoContatosEntidadeAfterSelectHooks []GeracaoContatosEntidadeHook

var geracaoContatosEntidadeBeforeInsertMu sync.Mutex
var geracaoContatosEntidadeBeforeInsertHooks []GeracaoContatosEntidadeHook
var geracaoContatosEntidadeAfterInsertMu sync.Mutex
var geracaoContatosEntidadeAfterInsertHooks []GeracaoContatosEntidadeHook

var geracaoContatosEntidadeBeforeUpdateMu sync.Mutex
var geracaoContatosEntidadeBeforeUpdateHooks []GeracaoContatosEntidadeHook
var geracaoContatosEntidadeAfterUpdateMu sync.Mutex
var geracaoContatosEntidadeAfterUpdateHooks []GeracaoContatosEntidadeHook

var geracaoContatosEntidadeBeforeDeleteMu sync.Mutex
var geracaoContatosEntidadeBeforeDeleteHooks []GeracaoContatosEntidadeHook
var geracaoContatosEntidadeAfterDeleteMu sync.Mutex
var geracaoContatosEntidadeAfterDeleteHooks []GeracaoContatosEntidadeHook

var geracaoContatosEntidadeBeforeUpsertMu sync.Mutex
var geracaoContatosEntidadeBeforeUpsertHooks []GeracaoContatosEntidadeHook
var geracaoContatosEntidadeAfterUpsertMu sync.Mutex
var geracaoContatosEntidadeAfterUpsertHooks []GeracaoContatosEntidadeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *GeracaoContatosEntidade) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *GeracaoContatosEntidade) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *GeracaoContatosEntidade) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *GeracaoContatosEntidade) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *GeracaoContatosEntidade) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *GeracaoContatosEntidade) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *GeracaoContatosEntidade) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *GeracaoContatosEntidade) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *GeracaoContatosEntidade) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range geracaoContatosEntidadeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddGeracaoContatosEntidadeHook registers your hook function for all future operations.
func AddGeracaoContatosEntidadeHook(hookPoint boil.HookPoint, geracaoContatosEntidadeHook GeracaoContatosEntidadeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		geracaoContatosEntidadeAfterSelectMu.Lock()
		geracaoContatosEntidadeAfterSelectHooks = append(geracaoContatosEntidadeAfterSelectHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		geracaoContatosEntidadeBeforeInsertMu.Lock()
		geracaoContatosEntidadeBeforeInsertHooks = append(geracaoContatosEntidadeBeforeInsertHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		geracaoContatosEntidadeAfterInsertMu.Lock()
		geracaoContatosEntidadeAfterInsertHooks = append(geracaoContatosEntidadeAfterInsertHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		geracaoContatosEntidadeBeforeUpdateMu.Lock()
		geracaoContatosEntidadeBeforeUpdateHooks = append(geracaoContatosEntidadeBeforeUpdateHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		geracaoContatosEntidadeAfterUpdateMu.Lock()
		geracaoContatosEntidadeAfterUpdateHooks = append(geracaoContatosEntidadeAfterUpdateHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		geracaoContatosEntidadeBeforeDeleteMu.Lock()
		geracaoContatosEntidadeBeforeDeleteHooks = append(geracaoContatosEntidadeBeforeDeleteHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		geracaoContatosEntidadeAfterDeleteMu.Lock()
		geracaoContatosEntidadeAfterDeleteHooks = append(geracaoContatosEntidadeAfterDeleteHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		geracaoContatosEntidadeBeforeUpsertMu.Lock()
		geracaoContatosEntidadeBeforeUpsertHooks = append(geracaoContatosEntidadeBeforeUpsertHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		geracaoContatosEntidadeAfterUpsertMu.Lock()
		geracaoContatosEntidadeAfterUpsertHooks = append(geracaoContatosEntidadeAfterUpsertHooks, geracaoContatosEntidadeHook)
		geracaoContatosEntidadeAfterUpsertMu.Unlock()
	}
}

// One returns a single geracaoContatosEntidade record from the query.
func (q geracaoContatosEntidadeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*GeracaoContatosEntidade, error) {
	o := &GeracaoContatosEntidade{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for geracao_contatos_entidades")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all GeracaoContatosEntidade records from the query.
func (q geracaoContatosEntidadeQuery) All(ctx context.Context, exec boil.ContextExecutor) (GeracaoContatosEntidadeSlice, error) {
	var o []*GeracaoContatosEntidade

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to GeracaoContatosEntidade slice")
	}

	if len(geracaoContatosEntidadeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all GeracaoContatosEntidade records in the query.
func (q geracaoContatosEntidadeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count geracao_contatos_entidades rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q geracaoContatosEntidadeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if geracao_contatos_entidades exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *GeracaoContatosEntidade) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// GceGeracaoContatosEntidadesFaturas retrieves all the geracao_contatos_entidades_faturas's GeracaoContatosEntidadesFaturas with an executor via gce_id column.
func (o *GeracaoContatosEntidade) GceGeracaoContatosEntidadesFaturas(mods ...qm.QueryMod) geracaoContatosEntidadesFaturasQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`geracao_contatos_entidades_faturas`.`gce_id`=?", o.GceID),
	)

	return GeracaoContatosEntidadesFaturas(queryMods...)
}

// GceGeracaoContatosEntidadesNotaFiscals retrieves all the geracao_contatos_entidades_nota_fiscal's GeracaoContatosEntidadesNotaFiscals with an executor via gce_id column.
func (o *GeracaoContatosEntidade) GceGeracaoContatosEntidadesNotaFiscals(mods ...qm.QueryMod) geracaoContatosEntidadesNotaFiscalQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`geracao_contatos_entidades_nota_fiscal`.`gce_id`=?", o.GceID),
	)

	return GeracaoContatosEntidadesNotaFiscals(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (geracaoContatosEntidadeL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeracaoContatosEntidade interface{}, mods queries.Applicator) error {
	var slice []*GeracaoContatosEntidade
	var object *GeracaoContatosEntidade

	if singular {
		var ok bool
		object, ok = maybeGeracaoContatosEntidade.(*GeracaoContatosEntidade)
		if !ok {
			object = new(GeracaoContatosEntidade)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeracaoContatosEntidade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeracaoContatosEntidade))
			}
		}
	} else {
		s, ok := maybeGeracaoContatosEntidade.(*[]*GeracaoContatosEntidade)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeracaoContatosEntidade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeracaoContatosEntidade))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &geracaoContatosEntidadeR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geracaoContatosEntidadeR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfGeracaoContatosEntidades = append(foreign.R.EmfGeracaoContatosEntidades, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfGeracaoContatosEntidades = append(foreign.R.EmfGeracaoContatosEntidades, local)
				break
			}
		}
	}

	return nil
}

// LoadGceGeracaoContatosEntidadesFaturas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (geracaoContatosEntidadeL) LoadGceGeracaoContatosEntidadesFaturas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeracaoContatosEntidade interface{}, mods queries.Applicator) error {
	var slice []*GeracaoContatosEntidade
	var object *GeracaoContatosEntidade

	if singular {
		var ok bool
		object, ok = maybeGeracaoContatosEntidade.(*GeracaoContatosEntidade)
		if !ok {
			object = new(GeracaoContatosEntidade)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeracaoContatosEntidade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeracaoContatosEntidade))
			}
		}
	} else {
		s, ok := maybeGeracaoContatosEntidade.(*[]*GeracaoContatosEntidade)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeracaoContatosEntidade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeracaoContatosEntidade))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &geracaoContatosEntidadeR{}
		}
		args[object.GceID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geracaoContatosEntidadeR{}
			}
			args[obj.GceID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`geracao_contatos_entidades_faturas`),
		qm.WhereIn(`geracao_contatos_entidades_faturas.gce_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load geracao_contatos_entidades_faturas")
	}

	var resultSlice []*GeracaoContatosEntidadesFaturas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice geracao_contatos_entidades_faturas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on geracao_contatos_entidades_faturas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for geracao_contatos_entidades_faturas")
	}

	if len(geracaoContatosEntidadesFaturasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.GceGeracaoContatosEntidadesFaturas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &geracaoContatosEntidadesFaturasR{}
			}
			foreign.R.Gce = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.GceID == foreign.GceID {
				local.R.GceGeracaoContatosEntidadesFaturas = append(local.R.GceGeracaoContatosEntidadesFaturas, foreign)
				if foreign.R == nil {
					foreign.R = &geracaoContatosEntidadesFaturasR{}
				}
				foreign.R.Gce = local
				break
			}
		}
	}

	return nil
}

// LoadGceGeracaoContatosEntidadesNotaFiscals allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (geracaoContatosEntidadeL) LoadGceGeracaoContatosEntidadesNotaFiscals(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGeracaoContatosEntidade interface{}, mods queries.Applicator) error {
	var slice []*GeracaoContatosEntidade
	var object *GeracaoContatosEntidade

	if singular {
		var ok bool
		object, ok = maybeGeracaoContatosEntidade.(*GeracaoContatosEntidade)
		if !ok {
			object = new(GeracaoContatosEntidade)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGeracaoContatosEntidade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGeracaoContatosEntidade))
			}
		}
	} else {
		s, ok := maybeGeracaoContatosEntidade.(*[]*GeracaoContatosEntidade)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGeracaoContatosEntidade)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGeracaoContatosEntidade))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &geracaoContatosEntidadeR{}
		}
		args[object.GceID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &geracaoContatosEntidadeR{}
			}
			args[obj.GceID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`geracao_contatos_entidades_nota_fiscal`),
		qm.WhereIn(`geracao_contatos_entidades_nota_fiscal.gce_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load geracao_contatos_entidades_nota_fiscal")
	}

	var resultSlice []*GeracaoContatosEntidadesNotaFiscal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice geracao_contatos_entidades_nota_fiscal")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on geracao_contatos_entidades_nota_fiscal")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for geracao_contatos_entidades_nota_fiscal")
	}

	if len(geracaoContatosEntidadesNotaFiscalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.GceGeracaoContatosEntidadesNotaFiscals = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &geracaoContatosEntidadesNotaFiscalR{}
			}
			foreign.R.Gce = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.GceID == foreign.GceID {
				local.R.GceGeracaoContatosEntidadesNotaFiscals = append(local.R.GceGeracaoContatosEntidadesNotaFiscals, foreign)
				if foreign.R == nil {
					foreign.R = &geracaoContatosEntidadesNotaFiscalR{}
				}
				foreign.R.Gce = local
				break
			}
		}
	}

	return nil
}

// SetEmf of the geracaoContatosEntidade to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfGeracaoContatosEntidades.
func (o *GeracaoContatosEntidade) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `geracao_contatos_entidades` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadePrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.GceID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &geracaoContatosEntidadeR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfGeracaoContatosEntidades: GeracaoContatosEntidadeSlice{o},
		}
	} else {
		related.R.EmfGeracaoContatosEntidades = append(related.R.EmfGeracaoContatosEntidades, o)
	}

	return nil
}

// AddGceGeracaoContatosEntidadesFaturas adds the given related objects to the existing relationships
// of the geracao_contatos_entidade, optionally inserting them as new records.
// Appends related to o.R.GceGeracaoContatosEntidadesFaturas.
// Sets related.R.Gce appropriately.
func (o *GeracaoContatosEntidade) AddGceGeracaoContatosEntidadesFaturas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*GeracaoContatosEntidadesFaturas) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GceID = o.GceID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `geracao_contatos_entidades_faturas` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"gce_id"}),
				strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesFaturasPrimaryKeyColumns),
			)
			values := []interface{}{o.GceID, rel.GceID, rel.GcefItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GceID = o.GceID
		}
	}

	if o.R == nil {
		o.R = &geracaoContatosEntidadeR{
			GceGeracaoContatosEntidadesFaturas: related,
		}
	} else {
		o.R.GceGeracaoContatosEntidadesFaturas = append(o.R.GceGeracaoContatosEntidadesFaturas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &geracaoContatosEntidadesFaturasR{
				Gce: o,
			}
		} else {
			rel.R.Gce = o
		}
	}
	return nil
}

// AddGceGeracaoContatosEntidadesNotaFiscals adds the given related objects to the existing relationships
// of the geracao_contatos_entidade, optionally inserting them as new records.
// Appends related to o.R.GceGeracaoContatosEntidadesNotaFiscals.
// Sets related.R.Gce appropriately.
func (o *GeracaoContatosEntidade) AddGceGeracaoContatosEntidadesNotaFiscals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*GeracaoContatosEntidadesNotaFiscal) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GceID = o.GceID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `geracao_contatos_entidades_nota_fiscal` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"gce_id"}),
				strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns),
			)
			values := []interface{}{o.GceID, rel.GceID, rel.GcenfID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GceID = o.GceID
		}
	}

	if o.R == nil {
		o.R = &geracaoContatosEntidadeR{
			GceGeracaoContatosEntidadesNotaFiscals: related,
		}
	} else {
		o.R.GceGeracaoContatosEntidadesNotaFiscals = append(o.R.GceGeracaoContatosEntidadesNotaFiscals, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &geracaoContatosEntidadesNotaFiscalR{
				Gce: o,
			}
		} else {
			rel.R.Gce = o
		}
	}
	return nil
}

// GeracaoContatosEntidades retrieves all the records using an executor.
func GeracaoContatosEntidades(mods ...qm.QueryMod) geracaoContatosEntidadeQuery {
	mods = append(mods, qm.From("`geracao_contatos_entidades`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`geracao_contatos_entidades`.*"})
	}

	return geracaoContatosEntidadeQuery{q}
}

// FindGeracaoContatosEntidade retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindGeracaoContatosEntidade(ctx context.Context, exec boil.ContextExecutor, gceID int, selectCols ...string) (*GeracaoContatosEntidade, error) {
	geracaoContatosEntidadeObj := &GeracaoContatosEntidade{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `geracao_contatos_entidades` where `gce_id`=?", sel,
	)

	q := queries.Raw(query, gceID)

	err := q.Bind(ctx, exec, geracaoContatosEntidadeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from geracao_contatos_entidades")
	}

	if err = geracaoContatosEntidadeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return geracaoContatosEntidadeObj, err
	}

	return geracaoContatosEntidadeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *GeracaoContatosEntidade) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no geracao_contatos_entidades provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(geracaoContatosEntidadeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	geracaoContatosEntidadeInsertCacheMut.RLock()
	cache, cached := geracaoContatosEntidadeInsertCache[key]
	geracaoContatosEntidadeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			geracaoContatosEntidadeAllColumns,
			geracaoContatosEntidadeColumnsWithDefault,
			geracaoContatosEntidadeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadeType, geracaoContatosEntidadeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(geracaoContatosEntidadeType, geracaoContatosEntidadeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `geracao_contatos_entidades` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `geracao_contatos_entidades` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `geracao_contatos_entidades` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into geracao_contatos_entidades")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.GceID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for geracao_contatos_entidades")
	}

CacheNoHooks:
	if !cached {
		geracaoContatosEntidadeInsertCacheMut.Lock()
		geracaoContatosEntidadeInsertCache[key] = cache
		geracaoContatosEntidadeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the GeracaoContatosEntidade.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *GeracaoContatosEntidade) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	geracaoContatosEntidadeUpdateCacheMut.RLock()
	cache, cached := geracaoContatosEntidadeUpdateCache[key]
	geracaoContatosEntidadeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			geracaoContatosEntidadeAllColumns,
			geracaoContatosEntidadePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update geracao_contatos_entidades, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `geracao_contatos_entidades` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, geracaoContatosEntidadePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadeType, geracaoContatosEntidadeMapping, append(wl, geracaoContatosEntidadePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update geracao_contatos_entidades row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for geracao_contatos_entidades")
	}

	if !cached {
		geracaoContatosEntidadeUpdateCacheMut.Lock()
		geracaoContatosEntidadeUpdateCache[key] = cache
		geracaoContatosEntidadeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q geracaoContatosEntidadeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for geracao_contatos_entidades")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for geracao_contatos_entidades")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o GeracaoContatosEntidadeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `geracao_contatos_entidades` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in geracaoContatosEntidade slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all geracaoContatosEntidade")
	}
	return rowsAff, nil
}

var mySQLGeracaoContatosEntidadeUniqueColumns = []string{
	"gce_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *GeracaoContatosEntidade) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no geracao_contatos_entidades provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(geracaoContatosEntidadeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLGeracaoContatosEntidadeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	geracaoContatosEntidadeUpsertCacheMut.RLock()
	cache, cached := geracaoContatosEntidadeUpsertCache[key]
	geracaoContatosEntidadeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			geracaoContatosEntidadeAllColumns,
			geracaoContatosEntidadeColumnsWithDefault,
			geracaoContatosEntidadeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			geracaoContatosEntidadeAllColumns,
			geracaoContatosEntidadePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert geracao_contatos_entidades, could not build update column list")
		}

		ret := strmangle.SetComplement(geracaoContatosEntidadeAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`geracao_contatos_entidades`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `geracao_contatos_entidades` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(geracaoContatosEntidadeType, geracaoContatosEntidadeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(geracaoContatosEntidadeType, geracaoContatosEntidadeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for geracao_contatos_entidades")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(geracaoContatosEntidadeType, geracaoContatosEntidadeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for geracao_contatos_entidades")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for geracao_contatos_entidades")
	}

CacheNoHooks:
	if !cached {
		geracaoContatosEntidadeUpsertCacheMut.Lock()
		geracaoContatosEntidadeUpsertCache[key] = cache
		geracaoContatosEntidadeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single GeracaoContatosEntidade record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *GeracaoContatosEntidade) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no GeracaoContatosEntidade provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), geracaoContatosEntidadePrimaryKeyMapping)
	sql := "DELETE FROM `geracao_contatos_entidades` WHERE `gce_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from geracao_contatos_entidades")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for geracao_contatos_entidades")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q geracaoContatosEntidadeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no geracaoContatosEntidadeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from geracao_contatos_entidades")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for geracao_contatos_entidades")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o GeracaoContatosEntidadeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(geracaoContatosEntidadeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `geracao_contatos_entidades` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from geracaoContatosEntidade slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for geracao_contatos_entidades")
	}

	if len(geracaoContatosEntidadeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *GeracaoContatosEntidade) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindGeracaoContatosEntidade(ctx, exec, o.GceID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GeracaoContatosEntidadeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := GeracaoContatosEntidadeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), geracaoContatosEntidadePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `geracao_contatos_entidades`.* FROM `geracao_contatos_entidades` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, geracaoContatosEntidadePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in GeracaoContatosEntidadeSlice")
	}

	*o = slice

	return nil
}

// GeracaoContatosEntidadeExists checks if the GeracaoContatosEntidade row exists.
func GeracaoContatosEntidadeExists(ctx context.Context, exec boil.ContextExecutor, gceID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `geracao_contatos_entidades` where `gce_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, gceID)
	}
	row := exec.QueryRowContext(ctx, sql, gceID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if geracao_contatos_entidades exists")
	}

	return exists, nil
}

// Exists checks if the GeracaoContatosEntidade row exists.
func (o *GeracaoContatosEntidade) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return GeracaoContatosEntidadeExists(ctx, exec, o.GceID)
}
