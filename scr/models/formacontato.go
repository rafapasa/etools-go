// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Formacontato is an object representing the database table.
type Formacontato struct {
	FRCID        int    `boil:"frc_id" json:"frc_id" toml:"frc_id" yaml:"frc_id"`
	FRCDescricao string `boil:"frc_descricao" json:"frc_descricao" toml:"frc_descricao" yaml:"frc_descricao"`
	FRCTipo      int    `boil:"frc_tipo" json:"frc_tipo" toml:"frc_tipo" yaml:"frc_tipo"`

	R *formacontatoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L formacontatoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FormacontatoColumns = struct {
	FRCID        string
	FRCDescricao string
	FRCTipo      string
}{
	FRCID:        "frc_id",
	FRCDescricao: "frc_descricao",
	FRCTipo:      "frc_tipo",
}

var FormacontatoTableColumns = struct {
	FRCID        string
	FRCDescricao string
	FRCTipo      string
}{
	FRCID:        "formacontato.frc_id",
	FRCDescricao: "formacontato.frc_descricao",
	FRCTipo:      "formacontato.frc_tipo",
}

// Generated where

var FormacontatoWhere = struct {
	FRCID        whereHelperint
	FRCDescricao whereHelperstring
	FRCTipo      whereHelperint
}{
	FRCID:        whereHelperint{field: "`formacontato`.`frc_id`"},
	FRCDescricao: whereHelperstring{field: "`formacontato`.`frc_descricao`"},
	FRCTipo:      whereHelperint{field: "`formacontato`.`frc_tipo`"},
}

// FormacontatoRels is where relationship names are stored.
var FormacontatoRels = struct {
	FRCEntidadeFormacontatos string
}{
	FRCEntidadeFormacontatos: "FRCEntidadeFormacontatos",
}

// formacontatoR is where relationships are stored.
type formacontatoR struct {
	FRCEntidadeFormacontatos EntidadeFormacontatoSlice `boil:"FRCEntidadeFormacontatos" json:"FRCEntidadeFormacontatos" toml:"FRCEntidadeFormacontatos" yaml:"FRCEntidadeFormacontatos"`
}

// NewStruct creates a new relationship struct
func (*formacontatoR) NewStruct() *formacontatoR {
	return &formacontatoR{}
}

func (r *formacontatoR) GetFRCEntidadeFormacontatos() EntidadeFormacontatoSlice {
	if r == nil {
		return nil
	}
	return r.FRCEntidadeFormacontatos
}

// formacontatoL is where Load methods for each relationship are stored.
type formacontatoL struct{}

var (
	formacontatoAllColumns            = []string{"frc_id", "frc_descricao", "frc_tipo"}
	formacontatoColumnsWithoutDefault = []string{"frc_id", "frc_descricao", "frc_tipo"}
	formacontatoColumnsWithDefault    = []string{}
	formacontatoPrimaryKeyColumns     = []string{"frc_id"}
	formacontatoGeneratedColumns      = []string{}
)

type (
	// FormacontatoSlice is an alias for a slice of pointers to Formacontato.
	// This should almost always be used instead of []Formacontato.
	FormacontatoSlice []*Formacontato
	// FormacontatoHook is the signature for custom Formacontato hook methods
	FormacontatoHook func(context.Context, boil.ContextExecutor, *Formacontato) error

	formacontatoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	formacontatoType                 = reflect.TypeOf(&Formacontato{})
	formacontatoMapping              = queries.MakeStructMapping(formacontatoType)
	formacontatoPrimaryKeyMapping, _ = queries.BindMapping(formacontatoType, formacontatoMapping, formacontatoPrimaryKeyColumns)
	formacontatoInsertCacheMut       sync.RWMutex
	formacontatoInsertCache          = make(map[string]insertCache)
	formacontatoUpdateCacheMut       sync.RWMutex
	formacontatoUpdateCache          = make(map[string]updateCache)
	formacontatoUpsertCacheMut       sync.RWMutex
	formacontatoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var formacontatoAfterSelectMu sync.Mutex
var formacontatoAfterSelectHooks []FormacontatoHook

var formacontatoBeforeInsertMu sync.Mutex
var formacontatoBeforeInsertHooks []FormacontatoHook
var formacontatoAfterInsertMu sync.Mutex
var formacontatoAfterInsertHooks []FormacontatoHook

var formacontatoBeforeUpdateMu sync.Mutex
var formacontatoBeforeUpdateHooks []FormacontatoHook
var formacontatoAfterUpdateMu sync.Mutex
var formacontatoAfterUpdateHooks []FormacontatoHook

var formacontatoBeforeDeleteMu sync.Mutex
var formacontatoBeforeDeleteHooks []FormacontatoHook
var formacontatoAfterDeleteMu sync.Mutex
var formacontatoAfterDeleteHooks []FormacontatoHook

var formacontatoBeforeUpsertMu sync.Mutex
var formacontatoBeforeUpsertHooks []FormacontatoHook
var formacontatoAfterUpsertMu sync.Mutex
var formacontatoAfterUpsertHooks []FormacontatoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Formacontato) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Formacontato) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Formacontato) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Formacontato) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Formacontato) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Formacontato) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Formacontato) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Formacontato) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Formacontato) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range formacontatoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddFormacontatoHook registers your hook function for all future operations.
func AddFormacontatoHook(hookPoint boil.HookPoint, formacontatoHook FormacontatoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		formacontatoAfterSelectMu.Lock()
		formacontatoAfterSelectHooks = append(formacontatoAfterSelectHooks, formacontatoHook)
		formacontatoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		formacontatoBeforeInsertMu.Lock()
		formacontatoBeforeInsertHooks = append(formacontatoBeforeInsertHooks, formacontatoHook)
		formacontatoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		formacontatoAfterInsertMu.Lock()
		formacontatoAfterInsertHooks = append(formacontatoAfterInsertHooks, formacontatoHook)
		formacontatoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		formacontatoBeforeUpdateMu.Lock()
		formacontatoBeforeUpdateHooks = append(formacontatoBeforeUpdateHooks, formacontatoHook)
		formacontatoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		formacontatoAfterUpdateMu.Lock()
		formacontatoAfterUpdateHooks = append(formacontatoAfterUpdateHooks, formacontatoHook)
		formacontatoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		formacontatoBeforeDeleteMu.Lock()
		formacontatoBeforeDeleteHooks = append(formacontatoBeforeDeleteHooks, formacontatoHook)
		formacontatoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		formacontatoAfterDeleteMu.Lock()
		formacontatoAfterDeleteHooks = append(formacontatoAfterDeleteHooks, formacontatoHook)
		formacontatoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		formacontatoBeforeUpsertMu.Lock()
		formacontatoBeforeUpsertHooks = append(formacontatoBeforeUpsertHooks, formacontatoHook)
		formacontatoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		formacontatoAfterUpsertMu.Lock()
		formacontatoAfterUpsertHooks = append(formacontatoAfterUpsertHooks, formacontatoHook)
		formacontatoAfterUpsertMu.Unlock()
	}
}

// One returns a single formacontato record from the query.
func (q formacontatoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Formacontato, error) {
	o := &Formacontato{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for formacontato")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Formacontato records from the query.
func (q formacontatoQuery) All(ctx context.Context, exec boil.ContextExecutor) (FormacontatoSlice, error) {
	var o []*Formacontato

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Formacontato slice")
	}

	if len(formacontatoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Formacontato records in the query.
func (q formacontatoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count formacontato rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q formacontatoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if formacontato exists")
	}

	return count > 0, nil
}

// FRCEntidadeFormacontatos retrieves all the entidade_formacontato's EntidadeFormacontatos with an executor via frc_id column.
func (o *Formacontato) FRCEntidadeFormacontatos(mods ...qm.QueryMod) entidadeFormacontatoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`entidade_formacontato`.`frc_id`=?", o.FRCID),
	)

	return EntidadeFormacontatos(queryMods...)
}

// LoadFRCEntidadeFormacontatos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (formacontatoL) LoadFRCEntidadeFormacontatos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFormacontato interface{}, mods queries.Applicator) error {
	var slice []*Formacontato
	var object *Formacontato

	if singular {
		var ok bool
		object, ok = maybeFormacontato.(*Formacontato)
		if !ok {
			object = new(Formacontato)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFormacontato)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFormacontato))
			}
		}
	} else {
		s, ok := maybeFormacontato.(*[]*Formacontato)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFormacontato)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFormacontato))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &formacontatoR{}
		}
		args[object.FRCID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &formacontatoR{}
			}
			args[obj.FRCID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade_formacontato`),
		qm.WhereIn(`entidade_formacontato.frc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load entidade_formacontato")
	}

	var resultSlice []*EntidadeFormacontato
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice entidade_formacontato")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on entidade_formacontato")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade_formacontato")
	}

	if len(entidadeFormacontatoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.FRCEntidadeFormacontatos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &entidadeFormacontatoR{}
			}
			foreign.R.FRC = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.FRCID == foreign.FRCID {
				local.R.FRCEntidadeFormacontatos = append(local.R.FRCEntidadeFormacontatos, foreign)
				if foreign.R == nil {
					foreign.R = &entidadeFormacontatoR{}
				}
				foreign.R.FRC = local
				break
			}
		}
	}

	return nil
}

// AddFRCEntidadeFormacontatos adds the given related objects to the existing relationships
// of the formacontato, optionally inserting them as new records.
// Appends related to o.R.FRCEntidadeFormacontatos.
// Sets related.R.FRC appropriately.
func (o *Formacontato) AddFRCEntidadeFormacontatos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EntidadeFormacontato) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.FRCID = o.FRCID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `entidade_formacontato` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"frc_id"}),
				strmangle.WhereClause("`", "`", 0, entidadeFormacontatoPrimaryKeyColumns),
			)
			values := []interface{}{o.FRCID, rel.EntID, rel.EfcItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.FRCID = o.FRCID
		}
	}

	if o.R == nil {
		o.R = &formacontatoR{
			FRCEntidadeFormacontatos: related,
		}
	} else {
		o.R.FRCEntidadeFormacontatos = append(o.R.FRCEntidadeFormacontatos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &entidadeFormacontatoR{
				FRC: o,
			}
		} else {
			rel.R.FRC = o
		}
	}
	return nil
}

// Formacontatos retrieves all the records using an executor.
func Formacontatos(mods ...qm.QueryMod) formacontatoQuery {
	mods = append(mods, qm.From("`formacontato`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`formacontato`.*"})
	}

	return formacontatoQuery{q}
}

// FindFormacontato retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFormacontato(ctx context.Context, exec boil.ContextExecutor, fRCID int, selectCols ...string) (*Formacontato, error) {
	formacontatoObj := &Formacontato{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `formacontato` where `frc_id`=?", sel,
	)

	q := queries.Raw(query, fRCID)

	err := q.Bind(ctx, exec, formacontatoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from formacontato")
	}

	if err = formacontatoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return formacontatoObj, err
	}

	return formacontatoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Formacontato) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no formacontato provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(formacontatoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	formacontatoInsertCacheMut.RLock()
	cache, cached := formacontatoInsertCache[key]
	formacontatoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			formacontatoAllColumns,
			formacontatoColumnsWithDefault,
			formacontatoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(formacontatoType, formacontatoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(formacontatoType, formacontatoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `formacontato` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `formacontato` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `formacontato` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, formacontatoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into formacontato")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.FRCID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for formacontato")
	}

CacheNoHooks:
	if !cached {
		formacontatoInsertCacheMut.Lock()
		formacontatoInsertCache[key] = cache
		formacontatoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Formacontato.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Formacontato) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	formacontatoUpdateCacheMut.RLock()
	cache, cached := formacontatoUpdateCache[key]
	formacontatoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			formacontatoAllColumns,
			formacontatoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update formacontato, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `formacontato` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, formacontatoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(formacontatoType, formacontatoMapping, append(wl, formacontatoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update formacontato row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for formacontato")
	}

	if !cached {
		formacontatoUpdateCacheMut.Lock()
		formacontatoUpdateCache[key] = cache
		formacontatoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q formacontatoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for formacontato")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for formacontato")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FormacontatoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), formacontatoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `formacontato` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, formacontatoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in formacontato slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all formacontato")
	}
	return rowsAff, nil
}

var mySQLFormacontatoUniqueColumns = []string{
	"frc_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Formacontato) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no formacontato provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(formacontatoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLFormacontatoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	formacontatoUpsertCacheMut.RLock()
	cache, cached := formacontatoUpsertCache[key]
	formacontatoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			formacontatoAllColumns,
			formacontatoColumnsWithDefault,
			formacontatoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			formacontatoAllColumns,
			formacontatoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert formacontato, could not build update column list")
		}

		ret := strmangle.SetComplement(formacontatoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`formacontato`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `formacontato` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(formacontatoType, formacontatoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(formacontatoType, formacontatoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for formacontato")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(formacontatoType, formacontatoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for formacontato")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for formacontato")
	}

CacheNoHooks:
	if !cached {
		formacontatoUpsertCacheMut.Lock()
		formacontatoUpsertCache[key] = cache
		formacontatoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Formacontato record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Formacontato) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Formacontato provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), formacontatoPrimaryKeyMapping)
	sql := "DELETE FROM `formacontato` WHERE `frc_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from formacontato")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for formacontato")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q formacontatoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no formacontatoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from formacontato")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for formacontato")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FormacontatoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(formacontatoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), formacontatoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `formacontato` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, formacontatoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from formacontato slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for formacontato")
	}

	if len(formacontatoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Formacontato) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindFormacontato(ctx, exec, o.FRCID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FormacontatoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FormacontatoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), formacontatoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `formacontato`.* FROM `formacontato` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, formacontatoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in FormacontatoSlice")
	}

	*o = slice

	return nil
}

// FormacontatoExists checks if the Formacontato row exists.
func FormacontatoExists(ctx context.Context, exec boil.ContextExecutor, fRCID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `formacontato` where `frc_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, fRCID)
	}
	row := exec.QueryRowContext(ctx, sql, fRCID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if formacontato exists")
	}

	return exists, nil
}

// Exists checks if the Formacontato row exists.
func (o *Formacontato) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return FormacontatoExists(ctx, exec, o.FRCID)
}
