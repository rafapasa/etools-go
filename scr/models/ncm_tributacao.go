// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// NCMTributacao is an object representing the database table.
type NCMTributacao struct {
	NCMNumero      int   `boil:"ncm_numero" json:"ncm_numero" toml:"ncm_numero" yaml:"ncm_numero"`
	NCTItem        int16 `boil:"nct_item" json:"nct_item" toml:"nct_item" yaml:"nct_item"`
	CstpiscofinsID int16 `boil:"cstpiscofins_id" json:"cstpiscofins_id" toml:"cstpiscofins_id" yaml:"cstpiscofins_id"`
	CsticmsID      int16 `boil:"csticms_id" json:"csticms_id" toml:"csticms_id" yaml:"csticms_id"`
	CstipiID       int16 `boil:"cstipi_id" json:"cstipi_id" toml:"cstipi_id" yaml:"cstipi_id"`
	EstID          int   `boil:"est_id" json:"est_id" toml:"est_id" yaml:"est_id"`
	PRCID          int   `boil:"prc_id" json:"prc_id" toml:"prc_id" yaml:"prc_id"`
	OpfID          int   `boil:"opf_id" json:"opf_id" toml:"opf_id" yaml:"opf_id"`
	// 1 - revenda
	//             2 - consumo
	NCTUsudestinatario int16 `boil:"nct_usudestinatario" json:"nct_usudestinatario" toml:"nct_usudestinatario" yaml:"nct_usudestinatario"`
	// 0 - entrada
	//             1 - saida
	NCTTipooperacao int16 `boil:"nct_tipooperacao" json:"nct_tipooperacao" toml:"nct_tipooperacao" yaml:"nct_tipooperacao"`

	R *ncmTributacaoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ncmTributacaoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var NCMTributacaoColumns = struct {
	NCMNumero          string
	NCTItem            string
	CstpiscofinsID     string
	CsticmsID          string
	CstipiID           string
	EstID              string
	PRCID              string
	OpfID              string
	NCTUsudestinatario string
	NCTTipooperacao    string
}{
	NCMNumero:          "ncm_numero",
	NCTItem:            "nct_item",
	CstpiscofinsID:     "cstpiscofins_id",
	CsticmsID:          "csticms_id",
	CstipiID:           "cstipi_id",
	EstID:              "est_id",
	PRCID:              "prc_id",
	OpfID:              "opf_id",
	NCTUsudestinatario: "nct_usudestinatario",
	NCTTipooperacao:    "nct_tipooperacao",
}

var NCMTributacaoTableColumns = struct {
	NCMNumero          string
	NCTItem            string
	CstpiscofinsID     string
	CsticmsID          string
	CstipiID           string
	EstID              string
	PRCID              string
	OpfID              string
	NCTUsudestinatario string
	NCTTipooperacao    string
}{
	NCMNumero:          "ncm_tributacao.ncm_numero",
	NCTItem:            "ncm_tributacao.nct_item",
	CstpiscofinsID:     "ncm_tributacao.cstpiscofins_id",
	CsticmsID:          "ncm_tributacao.csticms_id",
	CstipiID:           "ncm_tributacao.cstipi_id",
	EstID:              "ncm_tributacao.est_id",
	PRCID:              "ncm_tributacao.prc_id",
	OpfID:              "ncm_tributacao.opf_id",
	NCTUsudestinatario: "ncm_tributacao.nct_usudestinatario",
	NCTTipooperacao:    "ncm_tributacao.nct_tipooperacao",
}

// Generated where

var NCMTributacaoWhere = struct {
	NCMNumero          whereHelperint
	NCTItem            whereHelperint16
	CstpiscofinsID     whereHelperint16
	CsticmsID          whereHelperint16
	CstipiID           whereHelperint16
	EstID              whereHelperint
	PRCID              whereHelperint
	OpfID              whereHelperint
	NCTUsudestinatario whereHelperint16
	NCTTipooperacao    whereHelperint16
}{
	NCMNumero:          whereHelperint{field: "`ncm_tributacao`.`ncm_numero`"},
	NCTItem:            whereHelperint16{field: "`ncm_tributacao`.`nct_item`"},
	CstpiscofinsID:     whereHelperint16{field: "`ncm_tributacao`.`cstpiscofins_id`"},
	CsticmsID:          whereHelperint16{field: "`ncm_tributacao`.`csticms_id`"},
	CstipiID:           whereHelperint16{field: "`ncm_tributacao`.`cstipi_id`"},
	EstID:              whereHelperint{field: "`ncm_tributacao`.`est_id`"},
	PRCID:              whereHelperint{field: "`ncm_tributacao`.`prc_id`"},
	OpfID:              whereHelperint{field: "`ncm_tributacao`.`opf_id`"},
	NCTUsudestinatario: whereHelperint16{field: "`ncm_tributacao`.`nct_usudestinatario`"},
	NCTTipooperacao:    whereHelperint16{field: "`ncm_tributacao`.`nct_tipooperacao`"},
}

// NCMTributacaoRels is where relationship names are stored.
var NCMTributacaoRels = struct {
	Csticm       string
	Cstipi       string
	Cstpiscofin  string
	Est          string
	NCMNumeroNCM string
	PRC          string
	Opf          string
}{
	Csticm:       "Csticm",
	Cstipi:       "Cstipi",
	Cstpiscofin:  "Cstpiscofin",
	Est:          "Est",
	NCMNumeroNCM: "NCMNumeroNCM",
	PRC:          "PRC",
	Opf:          "Opf",
}

// ncmTributacaoR is where relationships are stored.
type ncmTributacaoR struct {
	Csticm       *Csticm         `boil:"Csticm" json:"Csticm" toml:"Csticm" yaml:"Csticm"`
	Cstipi       *Cstipi         `boil:"Cstipi" json:"Cstipi" toml:"Cstipi" yaml:"Cstipi"`
	Cstpiscofin  *Cstpiscofin    `boil:"Cstpiscofin" json:"Cstpiscofin" toml:"Cstpiscofin" yaml:"Cstpiscofin"`
	Est          *Estado         `boil:"Est" json:"Est" toml:"Est" yaml:"Est"`
	NCMNumeroNCM *NCM            `boil:"NCMNumeroNCM" json:"NCMNumeroNCM" toml:"NCMNumeroNCM" yaml:"NCMNumeroNCM"`
	PRC          *Processo       `boil:"PRC" json:"PRC" toml:"PRC" yaml:"PRC"`
	Opf          *Operacaofiscal `boil:"Opf" json:"Opf" toml:"Opf" yaml:"Opf"`
}

// NewStruct creates a new relationship struct
func (*ncmTributacaoR) NewStruct() *ncmTributacaoR {
	return &ncmTributacaoR{}
}

func (r *ncmTributacaoR) GetCsticm() *Csticm {
	if r == nil {
		return nil
	}
	return r.Csticm
}

func (r *ncmTributacaoR) GetCstipi() *Cstipi {
	if r == nil {
		return nil
	}
	return r.Cstipi
}

func (r *ncmTributacaoR) GetCstpiscofin() *Cstpiscofin {
	if r == nil {
		return nil
	}
	return r.Cstpiscofin
}

func (r *ncmTributacaoR) GetEst() *Estado {
	if r == nil {
		return nil
	}
	return r.Est
}

func (r *ncmTributacaoR) GetNCMNumeroNCM() *NCM {
	if r == nil {
		return nil
	}
	return r.NCMNumeroNCM
}

func (r *ncmTributacaoR) GetPRC() *Processo {
	if r == nil {
		return nil
	}
	return r.PRC
}

func (r *ncmTributacaoR) GetOpf() *Operacaofiscal {
	if r == nil {
		return nil
	}
	return r.Opf
}

// ncmTributacaoL is where Load methods for each relationship are stored.
type ncmTributacaoL struct{}

var (
	ncmTributacaoAllColumns            = []string{"ncm_numero", "nct_item", "cstpiscofins_id", "csticms_id", "cstipi_id", "est_id", "prc_id", "opf_id", "nct_usudestinatario", "nct_tipooperacao"}
	ncmTributacaoColumnsWithoutDefault = []string{"ncm_numero", "nct_item", "cstpiscofins_id", "csticms_id", "cstipi_id", "est_id", "prc_id", "opf_id", "nct_usudestinatario", "nct_tipooperacao"}
	ncmTributacaoColumnsWithDefault    = []string{}
	ncmTributacaoPrimaryKeyColumns     = []string{"ncm_numero", "nct_item"}
	ncmTributacaoGeneratedColumns      = []string{}
)

type (
	// NCMTributacaoSlice is an alias for a slice of pointers to NCMTributacao.
	// This should almost always be used instead of []NCMTributacao.
	NCMTributacaoSlice []*NCMTributacao
	// NCMTributacaoHook is the signature for custom NCMTributacao hook methods
	NCMTributacaoHook func(context.Context, boil.ContextExecutor, *NCMTributacao) error

	ncmTributacaoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ncmTributacaoType                 = reflect.TypeOf(&NCMTributacao{})
	ncmTributacaoMapping              = queries.MakeStructMapping(ncmTributacaoType)
	ncmTributacaoPrimaryKeyMapping, _ = queries.BindMapping(ncmTributacaoType, ncmTributacaoMapping, ncmTributacaoPrimaryKeyColumns)
	ncmTributacaoInsertCacheMut       sync.RWMutex
	ncmTributacaoInsertCache          = make(map[string]insertCache)
	ncmTributacaoUpdateCacheMut       sync.RWMutex
	ncmTributacaoUpdateCache          = make(map[string]updateCache)
	ncmTributacaoUpsertCacheMut       sync.RWMutex
	ncmTributacaoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ncmTributacaoAfterSelectMu sync.Mutex
var ncmTributacaoAfterSelectHooks []NCMTributacaoHook

var ncmTributacaoBeforeInsertMu sync.Mutex
var ncmTributacaoBeforeInsertHooks []NCMTributacaoHook
var ncmTributacaoAfterInsertMu sync.Mutex
var ncmTributacaoAfterInsertHooks []NCMTributacaoHook

var ncmTributacaoBeforeUpdateMu sync.Mutex
var ncmTributacaoBeforeUpdateHooks []NCMTributacaoHook
var ncmTributacaoAfterUpdateMu sync.Mutex
var ncmTributacaoAfterUpdateHooks []NCMTributacaoHook

var ncmTributacaoBeforeDeleteMu sync.Mutex
var ncmTributacaoBeforeDeleteHooks []NCMTributacaoHook
var ncmTributacaoAfterDeleteMu sync.Mutex
var ncmTributacaoAfterDeleteHooks []NCMTributacaoHook

var ncmTributacaoBeforeUpsertMu sync.Mutex
var ncmTributacaoBeforeUpsertHooks []NCMTributacaoHook
var ncmTributacaoAfterUpsertMu sync.Mutex
var ncmTributacaoAfterUpsertHooks []NCMTributacaoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *NCMTributacao) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *NCMTributacao) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *NCMTributacao) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *NCMTributacao) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *NCMTributacao) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *NCMTributacao) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *NCMTributacao) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *NCMTributacao) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *NCMTributacao) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmTributacaoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddNCMTributacaoHook registers your hook function for all future operations.
func AddNCMTributacaoHook(hookPoint boil.HookPoint, ncmTributacaoHook NCMTributacaoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ncmTributacaoAfterSelectMu.Lock()
		ncmTributacaoAfterSelectHooks = append(ncmTributacaoAfterSelectHooks, ncmTributacaoHook)
		ncmTributacaoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		ncmTributacaoBeforeInsertMu.Lock()
		ncmTributacaoBeforeInsertHooks = append(ncmTributacaoBeforeInsertHooks, ncmTributacaoHook)
		ncmTributacaoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		ncmTributacaoAfterInsertMu.Lock()
		ncmTributacaoAfterInsertHooks = append(ncmTributacaoAfterInsertHooks, ncmTributacaoHook)
		ncmTributacaoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		ncmTributacaoBeforeUpdateMu.Lock()
		ncmTributacaoBeforeUpdateHooks = append(ncmTributacaoBeforeUpdateHooks, ncmTributacaoHook)
		ncmTributacaoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		ncmTributacaoAfterUpdateMu.Lock()
		ncmTributacaoAfterUpdateHooks = append(ncmTributacaoAfterUpdateHooks, ncmTributacaoHook)
		ncmTributacaoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		ncmTributacaoBeforeDeleteMu.Lock()
		ncmTributacaoBeforeDeleteHooks = append(ncmTributacaoBeforeDeleteHooks, ncmTributacaoHook)
		ncmTributacaoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		ncmTributacaoAfterDeleteMu.Lock()
		ncmTributacaoAfterDeleteHooks = append(ncmTributacaoAfterDeleteHooks, ncmTributacaoHook)
		ncmTributacaoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		ncmTributacaoBeforeUpsertMu.Lock()
		ncmTributacaoBeforeUpsertHooks = append(ncmTributacaoBeforeUpsertHooks, ncmTributacaoHook)
		ncmTributacaoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		ncmTributacaoAfterUpsertMu.Lock()
		ncmTributacaoAfterUpsertHooks = append(ncmTributacaoAfterUpsertHooks, ncmTributacaoHook)
		ncmTributacaoAfterUpsertMu.Unlock()
	}
}

// One returns a single ncmTributacao record from the query.
func (q ncmTributacaoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*NCMTributacao, error) {
	o := &NCMTributacao{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ncm_tributacao")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all NCMTributacao records from the query.
func (q ncmTributacaoQuery) All(ctx context.Context, exec boil.ContextExecutor) (NCMTributacaoSlice, error) {
	var o []*NCMTributacao

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to NCMTributacao slice")
	}

	if len(ncmTributacaoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all NCMTributacao records in the query.
func (q ncmTributacaoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ncm_tributacao rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ncmTributacaoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ncm_tributacao exists")
	}

	return count > 0, nil
}

// Csticm pointed to by the foreign key.
func (o *NCMTributacao) Csticm(mods ...qm.QueryMod) csticmQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`csticms_id` = ?", o.CsticmsID),
	}

	queryMods = append(queryMods, mods...)

	return Csticms(queryMods...)
}

// Cstipi pointed to by the foreign key.
func (o *NCMTributacao) Cstipi(mods ...qm.QueryMod) cstipiQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cstipi_id` = ?", o.CstipiID),
	}

	queryMods = append(queryMods, mods...)

	return Cstipis(queryMods...)
}

// Cstpiscofin pointed to by the foreign key.
func (o *NCMTributacao) Cstpiscofin(mods ...qm.QueryMod) cstpiscofinQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cstpiscofins_id` = ?", o.CstpiscofinsID),
	}

	queryMods = append(queryMods, mods...)

	return Cstpiscofins(queryMods...)
}

// Est pointed to by the foreign key.
func (o *NCMTributacao) Est(mods ...qm.QueryMod) estadoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`est_id` = ?", o.EstID),
	}

	queryMods = append(queryMods, mods...)

	return Estados(queryMods...)
}

// NCMNumeroNCM pointed to by the foreign key.
func (o *NCMTributacao) NCMNumeroNCM(mods ...qm.QueryMod) ncmQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ncm_numero` = ?", o.NCMNumero),
	}

	queryMods = append(queryMods, mods...)

	return NCMS(queryMods...)
}

// PRC pointed to by the foreign key.
func (o *NCMTributacao) PRC(mods ...qm.QueryMod) processoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`prc_id` = ?", o.PRCID),
	}

	queryMods = append(queryMods, mods...)

	return Processos(queryMods...)
}

// Opf pointed to by the foreign key.
func (o *NCMTributacao) Opf(mods ...qm.QueryMod) operacaofiscalQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`opf_id` = ?", o.OpfID),
	}

	queryMods = append(queryMods, mods...)

	return Operacaofiscals(queryMods...)
}

// LoadCsticm allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmTributacaoL) LoadCsticm(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMTributacao interface{}, mods queries.Applicator) error {
	var slice []*NCMTributacao
	var object *NCMTributacao

	if singular {
		var ok bool
		object, ok = maybeNCMTributacao.(*NCMTributacao)
		if !ok {
			object = new(NCMTributacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMTributacao))
			}
		}
	} else {
		s, ok := maybeNCMTributacao.(*[]*NCMTributacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMTributacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmTributacaoR{}
		}
		args[object.CsticmsID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmTributacaoR{}
			}

			args[obj.CsticmsID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`csticms`),
		qm.WhereIn(`csticms.csticms_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Csticm")
	}

	var resultSlice []*Csticm
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Csticm")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for csticms")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for csticms")
	}

	if len(csticmAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Csticm = foreign
		if foreign.R == nil {
			foreign.R = &csticmR{}
		}
		foreign.R.NCMTributacaos = append(foreign.R.NCMTributacaos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CsticmsID == foreign.CsticmsID {
				local.R.Csticm = foreign
				if foreign.R == nil {
					foreign.R = &csticmR{}
				}
				foreign.R.NCMTributacaos = append(foreign.R.NCMTributacaos, local)
				break
			}
		}
	}

	return nil
}

// LoadCstipi allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmTributacaoL) LoadCstipi(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMTributacao interface{}, mods queries.Applicator) error {
	var slice []*NCMTributacao
	var object *NCMTributacao

	if singular {
		var ok bool
		object, ok = maybeNCMTributacao.(*NCMTributacao)
		if !ok {
			object = new(NCMTributacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMTributacao))
			}
		}
	} else {
		s, ok := maybeNCMTributacao.(*[]*NCMTributacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMTributacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmTributacaoR{}
		}
		args[object.CstipiID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmTributacaoR{}
			}

			args[obj.CstipiID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cstipi`),
		qm.WhereIn(`cstipi.cstipi_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Cstipi")
	}

	var resultSlice []*Cstipi
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Cstipi")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for cstipi")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cstipi")
	}

	if len(cstipiAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cstipi = foreign
		if foreign.R == nil {
			foreign.R = &cstipiR{}
		}
		foreign.R.NCMTributacaos = append(foreign.R.NCMTributacaos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CstipiID == foreign.CstipiID {
				local.R.Cstipi = foreign
				if foreign.R == nil {
					foreign.R = &cstipiR{}
				}
				foreign.R.NCMTributacaos = append(foreign.R.NCMTributacaos, local)
				break
			}
		}
	}

	return nil
}

// LoadCstpiscofin allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmTributacaoL) LoadCstpiscofin(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMTributacao interface{}, mods queries.Applicator) error {
	var slice []*NCMTributacao
	var object *NCMTributacao

	if singular {
		var ok bool
		object, ok = maybeNCMTributacao.(*NCMTributacao)
		if !ok {
			object = new(NCMTributacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMTributacao))
			}
		}
	} else {
		s, ok := maybeNCMTributacao.(*[]*NCMTributacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMTributacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmTributacaoR{}
		}
		args[object.CstpiscofinsID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmTributacaoR{}
			}

			args[obj.CstpiscofinsID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cstpiscofins`),
		qm.WhereIn(`cstpiscofins.cstpiscofins_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Cstpiscofin")
	}

	var resultSlice []*Cstpiscofin
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Cstpiscofin")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for cstpiscofins")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cstpiscofins")
	}

	if len(cstpiscofinAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cstpiscofin = foreign
		if foreign.R == nil {
			foreign.R = &cstpiscofinR{}
		}
		foreign.R.NCMTributacaos = append(foreign.R.NCMTributacaos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CstpiscofinsID == foreign.CstpiscofinsID {
				local.R.Cstpiscofin = foreign
				if foreign.R == nil {
					foreign.R = &cstpiscofinR{}
				}
				foreign.R.NCMTributacaos = append(foreign.R.NCMTributacaos, local)
				break
			}
		}
	}

	return nil
}

// LoadEst allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmTributacaoL) LoadEst(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMTributacao interface{}, mods queries.Applicator) error {
	var slice []*NCMTributacao
	var object *NCMTributacao

	if singular {
		var ok bool
		object, ok = maybeNCMTributacao.(*NCMTributacao)
		if !ok {
			object = new(NCMTributacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMTributacao))
			}
		}
	} else {
		s, ok := maybeNCMTributacao.(*[]*NCMTributacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMTributacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmTributacaoR{}
		}
		args[object.EstID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmTributacaoR{}
			}

			args[obj.EstID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`estado`),
		qm.WhereIn(`estado.est_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Estado")
	}

	var resultSlice []*Estado
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Estado")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for estado")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for estado")
	}

	if len(estadoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Est = foreign
		if foreign.R == nil {
			foreign.R = &estadoR{}
		}
		foreign.R.EstNCMTributacaos = append(foreign.R.EstNCMTributacaos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EstID == foreign.EstID {
				local.R.Est = foreign
				if foreign.R == nil {
					foreign.R = &estadoR{}
				}
				foreign.R.EstNCMTributacaos = append(foreign.R.EstNCMTributacaos, local)
				break
			}
		}
	}

	return nil
}

// LoadNCMNumeroNCM allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmTributacaoL) LoadNCMNumeroNCM(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMTributacao interface{}, mods queries.Applicator) error {
	var slice []*NCMTributacao
	var object *NCMTributacao

	if singular {
		var ok bool
		object, ok = maybeNCMTributacao.(*NCMTributacao)
		if !ok {
			object = new(NCMTributacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMTributacao))
			}
		}
	} else {
		s, ok := maybeNCMTributacao.(*[]*NCMTributacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMTributacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmTributacaoR{}
		}
		args[object.NCMNumero] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmTributacaoR{}
			}

			args[obj.NCMNumero] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ncm`),
		qm.WhereIn(`ncm.ncm_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load NCM")
	}

	var resultSlice []*NCM
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice NCM")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ncm")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ncm")
	}

	if len(ncmAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.NCMNumeroNCM = foreign
		if foreign.R == nil {
			foreign.R = &ncmR{}
		}
		foreign.R.NCMNumeroNCMTributacaos = append(foreign.R.NCMNumeroNCMTributacaos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.NCMNumero == foreign.NCMNumero {
				local.R.NCMNumeroNCM = foreign
				if foreign.R == nil {
					foreign.R = &ncmR{}
				}
				foreign.R.NCMNumeroNCMTributacaos = append(foreign.R.NCMNumeroNCMTributacaos, local)
				break
			}
		}
	}

	return nil
}

// LoadPRC allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmTributacaoL) LoadPRC(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMTributacao interface{}, mods queries.Applicator) error {
	var slice []*NCMTributacao
	var object *NCMTributacao

	if singular {
		var ok bool
		object, ok = maybeNCMTributacao.(*NCMTributacao)
		if !ok {
			object = new(NCMTributacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMTributacao))
			}
		}
	} else {
		s, ok := maybeNCMTributacao.(*[]*NCMTributacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMTributacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmTributacaoR{}
		}
		args[object.PRCID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmTributacaoR{}
			}

			args[obj.PRCID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`processo`),
		qm.WhereIn(`processo.prc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Processo")
	}

	var resultSlice []*Processo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Processo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for processo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for processo")
	}

	if len(processoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PRC = foreign
		if foreign.R == nil {
			foreign.R = &processoR{}
		}
		foreign.R.PRCNCMTributacaos = append(foreign.R.PRCNCMTributacaos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PRCID == foreign.PRCID {
				local.R.PRC = foreign
				if foreign.R == nil {
					foreign.R = &processoR{}
				}
				foreign.R.PRCNCMTributacaos = append(foreign.R.PRCNCMTributacaos, local)
				break
			}
		}
	}

	return nil
}

// LoadOpf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmTributacaoL) LoadOpf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCMTributacao interface{}, mods queries.Applicator) error {
	var slice []*NCMTributacao
	var object *NCMTributacao

	if singular {
		var ok bool
		object, ok = maybeNCMTributacao.(*NCMTributacao)
		if !ok {
			object = new(NCMTributacao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCMTributacao))
			}
		}
	} else {
		s, ok := maybeNCMTributacao.(*[]*NCMTributacao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCMTributacao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCMTributacao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmTributacaoR{}
		}
		args[object.OpfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmTributacaoR{}
			}

			args[obj.OpfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`operacaofiscal`),
		qm.WhereIn(`operacaofiscal.opf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Operacaofiscal")
	}

	var resultSlice []*Operacaofiscal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Operacaofiscal")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for operacaofiscal")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for operacaofiscal")
	}

	if len(operacaofiscalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Opf = foreign
		if foreign.R == nil {
			foreign.R = &operacaofiscalR{}
		}
		foreign.R.OpfNCMTributacaos = append(foreign.R.OpfNCMTributacaos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.OpfID == foreign.OpfID {
				local.R.Opf = foreign
				if foreign.R == nil {
					foreign.R = &operacaofiscalR{}
				}
				foreign.R.OpfNCMTributacaos = append(foreign.R.OpfNCMTributacaos, local)
				break
			}
		}
	}

	return nil
}

// SetCsticm of the ncmTributacao to the related item.
// Sets o.R.Csticm to related.
// Adds o to related.R.NCMTributacaos.
func (o *NCMTributacao) SetCsticm(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Csticm) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_tributacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"csticms_id"}),
		strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
	)
	values := []interface{}{related.CsticmsID, o.NCMNumero, o.NCTItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CsticmsID = related.CsticmsID
	if o.R == nil {
		o.R = &ncmTributacaoR{
			Csticm: related,
		}
	} else {
		o.R.Csticm = related
	}

	if related.R == nil {
		related.R = &csticmR{
			NCMTributacaos: NCMTributacaoSlice{o},
		}
	} else {
		related.R.NCMTributacaos = append(related.R.NCMTributacaos, o)
	}

	return nil
}

// SetCstipi of the ncmTributacao to the related item.
// Sets o.R.Cstipi to related.
// Adds o to related.R.NCMTributacaos.
func (o *NCMTributacao) SetCstipi(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Cstipi) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_tributacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cstipi_id"}),
		strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
	)
	values := []interface{}{related.CstipiID, o.NCMNumero, o.NCTItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CstipiID = related.CstipiID
	if o.R == nil {
		o.R = &ncmTributacaoR{
			Cstipi: related,
		}
	} else {
		o.R.Cstipi = related
	}

	if related.R == nil {
		related.R = &cstipiR{
			NCMTributacaos: NCMTributacaoSlice{o},
		}
	} else {
		related.R.NCMTributacaos = append(related.R.NCMTributacaos, o)
	}

	return nil
}

// SetCstpiscofin of the ncmTributacao to the related item.
// Sets o.R.Cstpiscofin to related.
// Adds o to related.R.NCMTributacaos.
func (o *NCMTributacao) SetCstpiscofin(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Cstpiscofin) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_tributacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cstpiscofins_id"}),
		strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
	)
	values := []interface{}{related.CstpiscofinsID, o.NCMNumero, o.NCTItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CstpiscofinsID = related.CstpiscofinsID
	if o.R == nil {
		o.R = &ncmTributacaoR{
			Cstpiscofin: related,
		}
	} else {
		o.R.Cstpiscofin = related
	}

	if related.R == nil {
		related.R = &cstpiscofinR{
			NCMTributacaos: NCMTributacaoSlice{o},
		}
	} else {
		related.R.NCMTributacaos = append(related.R.NCMTributacaos, o)
	}

	return nil
}

// SetEst of the ncmTributacao to the related item.
// Sets o.R.Est to related.
// Adds o to related.R.EstNCMTributacaos.
func (o *NCMTributacao) SetEst(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Estado) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_tributacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"est_id"}),
		strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
	)
	values := []interface{}{related.EstID, o.NCMNumero, o.NCTItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EstID = related.EstID
	if o.R == nil {
		o.R = &ncmTributacaoR{
			Est: related,
		}
	} else {
		o.R.Est = related
	}

	if related.R == nil {
		related.R = &estadoR{
			EstNCMTributacaos: NCMTributacaoSlice{o},
		}
	} else {
		related.R.EstNCMTributacaos = append(related.R.EstNCMTributacaos, o)
	}

	return nil
}

// SetNCMNumeroNCM of the ncmTributacao to the related item.
// Sets o.R.NCMNumeroNCM to related.
// Adds o to related.R.NCMNumeroNCMTributacaos.
func (o *NCMTributacao) SetNCMNumeroNCM(ctx context.Context, exec boil.ContextExecutor, insert bool, related *NCM) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_tributacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ncm_numero"}),
		strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
	)
	values := []interface{}{related.NCMNumero, o.NCMNumero, o.NCTItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.NCMNumero = related.NCMNumero
	if o.R == nil {
		o.R = &ncmTributacaoR{
			NCMNumeroNCM: related,
		}
	} else {
		o.R.NCMNumeroNCM = related
	}

	if related.R == nil {
		related.R = &ncmR{
			NCMNumeroNCMTributacaos: NCMTributacaoSlice{o},
		}
	} else {
		related.R.NCMNumeroNCMTributacaos = append(related.R.NCMNumeroNCMTributacaos, o)
	}

	return nil
}

// SetPRC of the ncmTributacao to the related item.
// Sets o.R.PRC to related.
// Adds o to related.R.PRCNCMTributacaos.
func (o *NCMTributacao) SetPRC(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Processo) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_tributacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"prc_id"}),
		strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
	)
	values := []interface{}{related.PRCID, o.NCMNumero, o.NCTItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PRCID = related.PRCID
	if o.R == nil {
		o.R = &ncmTributacaoR{
			PRC: related,
		}
	} else {
		o.R.PRC = related
	}

	if related.R == nil {
		related.R = &processoR{
			PRCNCMTributacaos: NCMTributacaoSlice{o},
		}
	} else {
		related.R.PRCNCMTributacaos = append(related.R.PRCNCMTributacaos, o)
	}

	return nil
}

// SetOpf of the ncmTributacao to the related item.
// Sets o.R.Opf to related.
// Adds o to related.R.OpfNCMTributacaos.
func (o *NCMTributacao) SetOpf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Operacaofiscal) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm_tributacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"opf_id"}),
		strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
	)
	values := []interface{}{related.OpfID, o.NCMNumero, o.NCTItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.OpfID = related.OpfID
	if o.R == nil {
		o.R = &ncmTributacaoR{
			Opf: related,
		}
	} else {
		o.R.Opf = related
	}

	if related.R == nil {
		related.R = &operacaofiscalR{
			OpfNCMTributacaos: NCMTributacaoSlice{o},
		}
	} else {
		related.R.OpfNCMTributacaos = append(related.R.OpfNCMTributacaos, o)
	}

	return nil
}

// NCMTributacaos retrieves all the records using an executor.
func NCMTributacaos(mods ...qm.QueryMod) ncmTributacaoQuery {
	mods = append(mods, qm.From("`ncm_tributacao`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`ncm_tributacao`.*"})
	}

	return ncmTributacaoQuery{q}
}

// FindNCMTributacao retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindNCMTributacao(ctx context.Context, exec boil.ContextExecutor, nCMNumero int, nCTItem int16, selectCols ...string) (*NCMTributacao, error) {
	ncmTributacaoObj := &NCMTributacao{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ncm_tributacao` where `ncm_numero`=? AND `nct_item`=?", sel,
	)

	q := queries.Raw(query, nCMNumero, nCTItem)

	err := q.Bind(ctx, exec, ncmTributacaoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ncm_tributacao")
	}

	if err = ncmTributacaoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ncmTributacaoObj, err
	}

	return ncmTributacaoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *NCMTributacao) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ncm_tributacao provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ncmTributacaoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ncmTributacaoInsertCacheMut.RLock()
	cache, cached := ncmTributacaoInsertCache[key]
	ncmTributacaoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ncmTributacaoAllColumns,
			ncmTributacaoColumnsWithDefault,
			ncmTributacaoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ncmTributacaoType, ncmTributacaoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ncmTributacaoType, ncmTributacaoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ncm_tributacao` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ncm_tributacao` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ncm_tributacao` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ncm_tributacao")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.NCMNumero,
		o.NCTItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ncm_tributacao")
	}

CacheNoHooks:
	if !cached {
		ncmTributacaoInsertCacheMut.Lock()
		ncmTributacaoInsertCache[key] = cache
		ncmTributacaoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the NCMTributacao.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *NCMTributacao) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ncmTributacaoUpdateCacheMut.RLock()
	cache, cached := ncmTributacaoUpdateCache[key]
	ncmTributacaoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ncmTributacaoAllColumns,
			ncmTributacaoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ncm_tributacao, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ncm_tributacao` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ncmTributacaoType, ncmTributacaoMapping, append(wl, ncmTributacaoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ncm_tributacao row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ncm_tributacao")
	}

	if !cached {
		ncmTributacaoUpdateCacheMut.Lock()
		ncmTributacaoUpdateCache[key] = cache
		ncmTributacaoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ncmTributacaoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ncm_tributacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ncm_tributacao")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o NCMTributacaoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmTributacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ncm_tributacao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmTributacaoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ncmTributacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ncmTributacao")
	}
	return rowsAff, nil
}

var mySQLNCMTributacaoUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *NCMTributacao) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ncm_tributacao provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ncmTributacaoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLNCMTributacaoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ncmTributacaoUpsertCacheMut.RLock()
	cache, cached := ncmTributacaoUpsertCache[key]
	ncmTributacaoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			ncmTributacaoAllColumns,
			ncmTributacaoColumnsWithDefault,
			ncmTributacaoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ncmTributacaoAllColumns,
			ncmTributacaoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ncm_tributacao, could not build update column list")
		}

		ret := strmangle.SetComplement(ncmTributacaoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`ncm_tributacao`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ncm_tributacao` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ncmTributacaoType, ncmTributacaoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ncmTributacaoType, ncmTributacaoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ncm_tributacao")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ncmTributacaoType, ncmTributacaoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ncm_tributacao")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ncm_tributacao")
	}

CacheNoHooks:
	if !cached {
		ncmTributacaoUpsertCacheMut.Lock()
		ncmTributacaoUpsertCache[key] = cache
		ncmTributacaoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single NCMTributacao record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *NCMTributacao) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no NCMTributacao provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ncmTributacaoPrimaryKeyMapping)
	sql := "DELETE FROM `ncm_tributacao` WHERE `ncm_numero`=? AND `nct_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ncm_tributacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ncm_tributacao")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ncmTributacaoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ncmTributacaoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ncm_tributacao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ncm_tributacao")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o NCMTributacaoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ncmTributacaoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmTributacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ncm_tributacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmTributacaoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ncmTributacao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ncm_tributacao")
	}

	if len(ncmTributacaoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *NCMTributacao) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindNCMTributacao(ctx, exec, o.NCMNumero, o.NCTItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *NCMTributacaoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := NCMTributacaoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmTributacaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ncm_tributacao`.* FROM `ncm_tributacao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmTributacaoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in NCMTributacaoSlice")
	}

	*o = slice

	return nil
}

// NCMTributacaoExists checks if the NCMTributacao row exists.
func NCMTributacaoExists(ctx context.Context, exec boil.ContextExecutor, nCMNumero int, nCTItem int16) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ncm_tributacao` where `ncm_numero`=? AND `nct_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, nCMNumero, nCTItem)
	}
	row := exec.QueryRowContext(ctx, sql, nCMNumero, nCTItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ncm_tributacao exists")
	}

	return exists, nil
}

// Exists checks if the NCMTributacao row exists.
func (o *NCMTributacao) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return NCMTributacaoExists(ctx, exec, o.NCMNumero, o.NCTItem)
}
