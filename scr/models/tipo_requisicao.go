// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TipoRequisicao is an object representing the database table.
type TipoRequisicao struct {
	TreqID        int    `boil:"treq_id" json:"treq_id" toml:"treq_id" yaml:"treq_id"`
	TreqDescricao string `boil:"treq_descricao" json:"treq_descricao" toml:"treq_descricao" yaml:"treq_descricao"`

	R *tipoRequisicaoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tipoRequisicaoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TipoRequisicaoColumns = struct {
	TreqID        string
	TreqDescricao string
}{
	TreqID:        "treq_id",
	TreqDescricao: "treq_descricao",
}

var TipoRequisicaoTableColumns = struct {
	TreqID        string
	TreqDescricao string
}{
	TreqID:        "tipo_requisicao.treq_id",
	TreqDescricao: "tipo_requisicao.treq_descricao",
}

// Generated where

var TipoRequisicaoWhere = struct {
	TreqID        whereHelperint
	TreqDescricao whereHelperstring
}{
	TreqID:        whereHelperint{field: "`tipo_requisicao`.`treq_id`"},
	TreqDescricao: whereHelperstring{field: "`tipo_requisicao`.`treq_descricao`"},
}

// TipoRequisicaoRels is where relationship names are stored.
var TipoRequisicaoRels = struct {
	TreqRequisicaos string
}{
	TreqRequisicaos: "TreqRequisicaos",
}

// tipoRequisicaoR is where relationships are stored.
type tipoRequisicaoR struct {
	TreqRequisicaos RequisicaoSlice `boil:"TreqRequisicaos" json:"TreqRequisicaos" toml:"TreqRequisicaos" yaml:"TreqRequisicaos"`
}

// NewStruct creates a new relationship struct
func (*tipoRequisicaoR) NewStruct() *tipoRequisicaoR {
	return &tipoRequisicaoR{}
}

func (r *tipoRequisicaoR) GetTreqRequisicaos() RequisicaoSlice {
	if r == nil {
		return nil
	}
	return r.TreqRequisicaos
}

// tipoRequisicaoL is where Load methods for each relationship are stored.
type tipoRequisicaoL struct{}

var (
	tipoRequisicaoAllColumns            = []string{"treq_id", "treq_descricao"}
	tipoRequisicaoColumnsWithoutDefault = []string{"treq_id", "treq_descricao"}
	tipoRequisicaoColumnsWithDefault    = []string{}
	tipoRequisicaoPrimaryKeyColumns     = []string{"treq_id"}
	tipoRequisicaoGeneratedColumns      = []string{}
)

type (
	// TipoRequisicaoSlice is an alias for a slice of pointers to TipoRequisicao.
	// This should almost always be used instead of []TipoRequisicao.
	TipoRequisicaoSlice []*TipoRequisicao
	// TipoRequisicaoHook is the signature for custom TipoRequisicao hook methods
	TipoRequisicaoHook func(context.Context, boil.ContextExecutor, *TipoRequisicao) error

	tipoRequisicaoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tipoRequisicaoType                 = reflect.TypeOf(&TipoRequisicao{})
	tipoRequisicaoMapping              = queries.MakeStructMapping(tipoRequisicaoType)
	tipoRequisicaoPrimaryKeyMapping, _ = queries.BindMapping(tipoRequisicaoType, tipoRequisicaoMapping, tipoRequisicaoPrimaryKeyColumns)
	tipoRequisicaoInsertCacheMut       sync.RWMutex
	tipoRequisicaoInsertCache          = make(map[string]insertCache)
	tipoRequisicaoUpdateCacheMut       sync.RWMutex
	tipoRequisicaoUpdateCache          = make(map[string]updateCache)
	tipoRequisicaoUpsertCacheMut       sync.RWMutex
	tipoRequisicaoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tipoRequisicaoAfterSelectMu sync.Mutex
var tipoRequisicaoAfterSelectHooks []TipoRequisicaoHook

var tipoRequisicaoBeforeInsertMu sync.Mutex
var tipoRequisicaoBeforeInsertHooks []TipoRequisicaoHook
var tipoRequisicaoAfterInsertMu sync.Mutex
var tipoRequisicaoAfterInsertHooks []TipoRequisicaoHook

var tipoRequisicaoBeforeUpdateMu sync.Mutex
var tipoRequisicaoBeforeUpdateHooks []TipoRequisicaoHook
var tipoRequisicaoAfterUpdateMu sync.Mutex
var tipoRequisicaoAfterUpdateHooks []TipoRequisicaoHook

var tipoRequisicaoBeforeDeleteMu sync.Mutex
var tipoRequisicaoBeforeDeleteHooks []TipoRequisicaoHook
var tipoRequisicaoAfterDeleteMu sync.Mutex
var tipoRequisicaoAfterDeleteHooks []TipoRequisicaoHook

var tipoRequisicaoBeforeUpsertMu sync.Mutex
var tipoRequisicaoBeforeUpsertHooks []TipoRequisicaoHook
var tipoRequisicaoAfterUpsertMu sync.Mutex
var tipoRequisicaoAfterUpsertHooks []TipoRequisicaoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TipoRequisicao) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TipoRequisicao) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TipoRequisicao) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TipoRequisicao) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TipoRequisicao) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TipoRequisicao) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TipoRequisicao) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TipoRequisicao) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TipoRequisicao) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoRequisicaoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTipoRequisicaoHook registers your hook function for all future operations.
func AddTipoRequisicaoHook(hookPoint boil.HookPoint, tipoRequisicaoHook TipoRequisicaoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tipoRequisicaoAfterSelectMu.Lock()
		tipoRequisicaoAfterSelectHooks = append(tipoRequisicaoAfterSelectHooks, tipoRequisicaoHook)
		tipoRequisicaoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tipoRequisicaoBeforeInsertMu.Lock()
		tipoRequisicaoBeforeInsertHooks = append(tipoRequisicaoBeforeInsertHooks, tipoRequisicaoHook)
		tipoRequisicaoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tipoRequisicaoAfterInsertMu.Lock()
		tipoRequisicaoAfterInsertHooks = append(tipoRequisicaoAfterInsertHooks, tipoRequisicaoHook)
		tipoRequisicaoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tipoRequisicaoBeforeUpdateMu.Lock()
		tipoRequisicaoBeforeUpdateHooks = append(tipoRequisicaoBeforeUpdateHooks, tipoRequisicaoHook)
		tipoRequisicaoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tipoRequisicaoAfterUpdateMu.Lock()
		tipoRequisicaoAfterUpdateHooks = append(tipoRequisicaoAfterUpdateHooks, tipoRequisicaoHook)
		tipoRequisicaoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tipoRequisicaoBeforeDeleteMu.Lock()
		tipoRequisicaoBeforeDeleteHooks = append(tipoRequisicaoBeforeDeleteHooks, tipoRequisicaoHook)
		tipoRequisicaoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tipoRequisicaoAfterDeleteMu.Lock()
		tipoRequisicaoAfterDeleteHooks = append(tipoRequisicaoAfterDeleteHooks, tipoRequisicaoHook)
		tipoRequisicaoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tipoRequisicaoBeforeUpsertMu.Lock()
		tipoRequisicaoBeforeUpsertHooks = append(tipoRequisicaoBeforeUpsertHooks, tipoRequisicaoHook)
		tipoRequisicaoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tipoRequisicaoAfterUpsertMu.Lock()
		tipoRequisicaoAfterUpsertHooks = append(tipoRequisicaoAfterUpsertHooks, tipoRequisicaoHook)
		tipoRequisicaoAfterUpsertMu.Unlock()
	}
}

// One returns a single tipoRequisicao record from the query.
func (q tipoRequisicaoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TipoRequisicao, error) {
	o := &TipoRequisicao{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for tipo_requisicao")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TipoRequisicao records from the query.
func (q tipoRequisicaoQuery) All(ctx context.Context, exec boil.ContextExecutor) (TipoRequisicaoSlice, error) {
	var o []*TipoRequisicao

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TipoRequisicao slice")
	}

	if len(tipoRequisicaoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TipoRequisicao records in the query.
func (q tipoRequisicaoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count tipo_requisicao rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tipoRequisicaoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if tipo_requisicao exists")
	}

	return count > 0, nil
}

// TreqRequisicaos retrieves all the requisicao's Requisicaos with an executor via treq_id column.
func (o *TipoRequisicao) TreqRequisicaos(mods ...qm.QueryMod) requisicaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`requisicao`.`treq_id`=?", o.TreqID),
	)

	return Requisicaos(queryMods...)
}

// LoadTreqRequisicaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (tipoRequisicaoL) LoadTreqRequisicaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTipoRequisicao interface{}, mods queries.Applicator) error {
	var slice []*TipoRequisicao
	var object *TipoRequisicao

	if singular {
		var ok bool
		object, ok = maybeTipoRequisicao.(*TipoRequisicao)
		if !ok {
			object = new(TipoRequisicao)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTipoRequisicao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTipoRequisicao))
			}
		}
	} else {
		s, ok := maybeTipoRequisicao.(*[]*TipoRequisicao)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTipoRequisicao)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTipoRequisicao))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tipoRequisicaoR{}
		}
		args[object.TreqID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tipoRequisicaoR{}
			}
			args[obj.TreqID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`requisicao`),
		qm.WhereIn(`requisicao.treq_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load requisicao")
	}

	var resultSlice []*Requisicao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice requisicao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on requisicao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for requisicao")
	}

	if len(requisicaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TreqRequisicaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &requisicaoR{}
			}
			foreign.R.Treq = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.TreqID, foreign.TreqID) {
				local.R.TreqRequisicaos = append(local.R.TreqRequisicaos, foreign)
				if foreign.R == nil {
					foreign.R = &requisicaoR{}
				}
				foreign.R.Treq = local
				break
			}
		}
	}

	return nil
}

// AddTreqRequisicaos adds the given related objects to the existing relationships
// of the tipo_requisicao, optionally inserting them as new records.
// Appends related to o.R.TreqRequisicaos.
// Sets related.R.Treq appropriately.
func (o *TipoRequisicao) AddTreqRequisicaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Requisicao) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.TreqID, o.TreqID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `requisicao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"treq_id"}),
				strmangle.WhereClause("`", "`", 0, requisicaoPrimaryKeyColumns),
			)
			values := []interface{}{o.TreqID, rel.ReqID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.TreqID, o.TreqID)
		}
	}

	if o.R == nil {
		o.R = &tipoRequisicaoR{
			TreqRequisicaos: related,
		}
	} else {
		o.R.TreqRequisicaos = append(o.R.TreqRequisicaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &requisicaoR{
				Treq: o,
			}
		} else {
			rel.R.Treq = o
		}
	}
	return nil
}

// SetTreqRequisicaos removes all previously related items of the
// tipo_requisicao replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Treq's TreqRequisicaos accordingly.
// Replaces o.R.TreqRequisicaos with related.
// Sets related.R.Treq's TreqRequisicaos accordingly.
func (o *TipoRequisicao) SetTreqRequisicaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Requisicao) error {
	query := "update `requisicao` set `treq_id` = null where `treq_id` = ?"
	values := []interface{}{o.TreqID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.TreqRequisicaos {
			queries.SetScanner(&rel.TreqID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Treq = nil
		}
		o.R.TreqRequisicaos = nil
	}

	return o.AddTreqRequisicaos(ctx, exec, insert, related...)
}

// RemoveTreqRequisicaos relationships from objects passed in.
// Removes related items from R.TreqRequisicaos (uses pointer comparison, removal does not keep order)
// Sets related.R.Treq.
func (o *TipoRequisicao) RemoveTreqRequisicaos(ctx context.Context, exec boil.ContextExecutor, related ...*Requisicao) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.TreqID, nil)
		if rel.R != nil {
			rel.R.Treq = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("treq_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.TreqRequisicaos {
			if rel != ri {
				continue
			}

			ln := len(o.R.TreqRequisicaos)
			if ln > 1 && i < ln-1 {
				o.R.TreqRequisicaos[i] = o.R.TreqRequisicaos[ln-1]
			}
			o.R.TreqRequisicaos = o.R.TreqRequisicaos[:ln-1]
			break
		}
	}

	return nil
}

// TipoRequisicaos retrieves all the records using an executor.
func TipoRequisicaos(mods ...qm.QueryMod) tipoRequisicaoQuery {
	mods = append(mods, qm.From("`tipo_requisicao`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`tipo_requisicao`.*"})
	}

	return tipoRequisicaoQuery{q}
}

// FindTipoRequisicao retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTipoRequisicao(ctx context.Context, exec boil.ContextExecutor, treqID int, selectCols ...string) (*TipoRequisicao, error) {
	tipoRequisicaoObj := &TipoRequisicao{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `tipo_requisicao` where `treq_id`=?", sel,
	)

	q := queries.Raw(query, treqID)

	err := q.Bind(ctx, exec, tipoRequisicaoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from tipo_requisicao")
	}

	if err = tipoRequisicaoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tipoRequisicaoObj, err
	}

	return tipoRequisicaoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TipoRequisicao) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tipo_requisicao provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tipoRequisicaoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tipoRequisicaoInsertCacheMut.RLock()
	cache, cached := tipoRequisicaoInsertCache[key]
	tipoRequisicaoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tipoRequisicaoAllColumns,
			tipoRequisicaoColumnsWithDefault,
			tipoRequisicaoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tipoRequisicaoType, tipoRequisicaoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tipoRequisicaoType, tipoRequisicaoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `tipo_requisicao` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `tipo_requisicao` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `tipo_requisicao` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tipoRequisicaoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into tipo_requisicao")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TreqID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tipo_requisicao")
	}

CacheNoHooks:
	if !cached {
		tipoRequisicaoInsertCacheMut.Lock()
		tipoRequisicaoInsertCache[key] = cache
		tipoRequisicaoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TipoRequisicao.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TipoRequisicao) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tipoRequisicaoUpdateCacheMut.RLock()
	cache, cached := tipoRequisicaoUpdateCache[key]
	tipoRequisicaoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tipoRequisicaoAllColumns,
			tipoRequisicaoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update tipo_requisicao, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `tipo_requisicao` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tipoRequisicaoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tipoRequisicaoType, tipoRequisicaoMapping, append(wl, tipoRequisicaoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update tipo_requisicao row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for tipo_requisicao")
	}

	if !cached {
		tipoRequisicaoUpdateCacheMut.Lock()
		tipoRequisicaoUpdateCache[key] = cache
		tipoRequisicaoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tipoRequisicaoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for tipo_requisicao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for tipo_requisicao")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TipoRequisicaoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tipoRequisicaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `tipo_requisicao` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tipoRequisicaoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tipoRequisicao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tipoRequisicao")
	}
	return rowsAff, nil
}

var mySQLTipoRequisicaoUniqueColumns = []string{
	"treq_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TipoRequisicao) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tipo_requisicao provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tipoRequisicaoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTipoRequisicaoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tipoRequisicaoUpsertCacheMut.RLock()
	cache, cached := tipoRequisicaoUpsertCache[key]
	tipoRequisicaoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tipoRequisicaoAllColumns,
			tipoRequisicaoColumnsWithDefault,
			tipoRequisicaoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tipoRequisicaoAllColumns,
			tipoRequisicaoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert tipo_requisicao, could not build update column list")
		}

		ret := strmangle.SetComplement(tipoRequisicaoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`tipo_requisicao`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `tipo_requisicao` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tipoRequisicaoType, tipoRequisicaoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tipoRequisicaoType, tipoRequisicaoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for tipo_requisicao")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tipoRequisicaoType, tipoRequisicaoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for tipo_requisicao")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tipo_requisicao")
	}

CacheNoHooks:
	if !cached {
		tipoRequisicaoUpsertCacheMut.Lock()
		tipoRequisicaoUpsertCache[key] = cache
		tipoRequisicaoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TipoRequisicao record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TipoRequisicao) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TipoRequisicao provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tipoRequisicaoPrimaryKeyMapping)
	sql := "DELETE FROM `tipo_requisicao` WHERE `treq_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from tipo_requisicao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for tipo_requisicao")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tipoRequisicaoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tipoRequisicaoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tipo_requisicao")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tipo_requisicao")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TipoRequisicaoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tipoRequisicaoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tipoRequisicaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `tipo_requisicao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tipoRequisicaoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tipoRequisicao slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tipo_requisicao")
	}

	if len(tipoRequisicaoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TipoRequisicao) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTipoRequisicao(ctx, exec, o.TreqID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TipoRequisicaoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TipoRequisicaoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tipoRequisicaoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `tipo_requisicao`.* FROM `tipo_requisicao` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tipoRequisicaoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TipoRequisicaoSlice")
	}

	*o = slice

	return nil
}

// TipoRequisicaoExists checks if the TipoRequisicao row exists.
func TipoRequisicaoExists(ctx context.Context, exec boil.ContextExecutor, treqID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `tipo_requisicao` where `treq_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, treqID)
	}
	row := exec.QueryRowContext(ctx, sql, treqID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if tipo_requisicao exists")
	}

	return exists, nil
}

// Exists checks if the TipoRequisicao row exists.
func (o *TipoRequisicao) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TipoRequisicaoExists(ctx, exec, o.TreqID)
}
