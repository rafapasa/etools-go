// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DocumentoVendaItemDetalhe is an object representing the database table.
type DocumentoVendaItemDetalhe struct {
	DDVID            int         `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	DviItem          int         `boil:"dvi_item" json:"dvi_item" toml:"dvi_item" yaml:"dvi_item"`
	DvidItem         int         `boil:"dvid_item" json:"dvid_item" toml:"dvid_item" yaml:"dvid_item"`
	DvidNumeropedido null.String `boil:"dvid_numeropedido" json:"dvid_numeropedido,omitempty" toml:"dvid_numeropedido" yaml:"dvid_numeropedido,omitempty"`
	DvidItempedido   null.Int    `boil:"dvid_itempedido" json:"dvid_itempedido,omitempty" toml:"dvid_itempedido" yaml:"dvid_itempedido,omitempty"`

	R *documentoVendaItemDetalheR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L documentoVendaItemDetalheL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DocumentoVendaItemDetalheColumns = struct {
	DDVID            string
	DviItem          string
	DvidItem         string
	DvidNumeropedido string
	DvidItempedido   string
}{
	DDVID:            "ddv_id",
	DviItem:          "dvi_item",
	DvidItem:         "dvid_item",
	DvidNumeropedido: "dvid_numeropedido",
	DvidItempedido:   "dvid_itempedido",
}

var DocumentoVendaItemDetalheTableColumns = struct {
	DDVID            string
	DviItem          string
	DvidItem         string
	DvidNumeropedido string
	DvidItempedido   string
}{
	DDVID:            "documento_venda_item_detalhes.ddv_id",
	DviItem:          "documento_venda_item_detalhes.dvi_item",
	DvidItem:         "documento_venda_item_detalhes.dvid_item",
	DvidNumeropedido: "documento_venda_item_detalhes.dvid_numeropedido",
	DvidItempedido:   "documento_venda_item_detalhes.dvid_itempedido",
}

// Generated where

var DocumentoVendaItemDetalheWhere = struct {
	DDVID            whereHelperint
	DviItem          whereHelperint
	DvidItem         whereHelperint
	DvidNumeropedido whereHelpernull_String
	DvidItempedido   whereHelpernull_Int
}{
	DDVID:            whereHelperint{field: "`documento_venda_item_detalhes`.`ddv_id`"},
	DviItem:          whereHelperint{field: "`documento_venda_item_detalhes`.`dvi_item`"},
	DvidItem:         whereHelperint{field: "`documento_venda_item_detalhes`.`dvid_item`"},
	DvidNumeropedido: whereHelpernull_String{field: "`documento_venda_item_detalhes`.`dvid_numeropedido`"},
	DvidItempedido:   whereHelpernull_Int{field: "`documento_venda_item_detalhes`.`dvid_itempedido`"},
}

// DocumentoVendaItemDetalheRels is where relationship names are stored.
var DocumentoVendaItemDetalheRels = struct {
}{}

// documentoVendaItemDetalheR is where relationships are stored.
type documentoVendaItemDetalheR struct {
}

// NewStruct creates a new relationship struct
func (*documentoVendaItemDetalheR) NewStruct() *documentoVendaItemDetalheR {
	return &documentoVendaItemDetalheR{}
}

// documentoVendaItemDetalheL is where Load methods for each relationship are stored.
type documentoVendaItemDetalheL struct{}

var (
	documentoVendaItemDetalheAllColumns            = []string{"ddv_id", "dvi_item", "dvid_item", "dvid_numeropedido", "dvid_itempedido"}
	documentoVendaItemDetalheColumnsWithoutDefault = []string{"ddv_id", "dvi_item", "dvid_item", "dvid_numeropedido", "dvid_itempedido"}
	documentoVendaItemDetalheColumnsWithDefault    = []string{}
	documentoVendaItemDetalhePrimaryKeyColumns     = []string{"ddv_id", "dvi_item", "dvid_item"}
	documentoVendaItemDetalheGeneratedColumns      = []string{}
)

type (
	// DocumentoVendaItemDetalheSlice is an alias for a slice of pointers to DocumentoVendaItemDetalhe.
	// This should almost always be used instead of []DocumentoVendaItemDetalhe.
	DocumentoVendaItemDetalheSlice []*DocumentoVendaItemDetalhe
	// DocumentoVendaItemDetalheHook is the signature for custom DocumentoVendaItemDetalhe hook methods
	DocumentoVendaItemDetalheHook func(context.Context, boil.ContextExecutor, *DocumentoVendaItemDetalhe) error

	documentoVendaItemDetalheQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	documentoVendaItemDetalheType                 = reflect.TypeOf(&DocumentoVendaItemDetalhe{})
	documentoVendaItemDetalheMapping              = queries.MakeStructMapping(documentoVendaItemDetalheType)
	documentoVendaItemDetalhePrimaryKeyMapping, _ = queries.BindMapping(documentoVendaItemDetalheType, documentoVendaItemDetalheMapping, documentoVendaItemDetalhePrimaryKeyColumns)
	documentoVendaItemDetalheInsertCacheMut       sync.RWMutex
	documentoVendaItemDetalheInsertCache          = make(map[string]insertCache)
	documentoVendaItemDetalheUpdateCacheMut       sync.RWMutex
	documentoVendaItemDetalheUpdateCache          = make(map[string]updateCache)
	documentoVendaItemDetalheUpsertCacheMut       sync.RWMutex
	documentoVendaItemDetalheUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var documentoVendaItemDetalheAfterSelectMu sync.Mutex
var documentoVendaItemDetalheAfterSelectHooks []DocumentoVendaItemDetalheHook

var documentoVendaItemDetalheBeforeInsertMu sync.Mutex
var documentoVendaItemDetalheBeforeInsertHooks []DocumentoVendaItemDetalheHook
var documentoVendaItemDetalheAfterInsertMu sync.Mutex
var documentoVendaItemDetalheAfterInsertHooks []DocumentoVendaItemDetalheHook

var documentoVendaItemDetalheBeforeUpdateMu sync.Mutex
var documentoVendaItemDetalheBeforeUpdateHooks []DocumentoVendaItemDetalheHook
var documentoVendaItemDetalheAfterUpdateMu sync.Mutex
var documentoVendaItemDetalheAfterUpdateHooks []DocumentoVendaItemDetalheHook

var documentoVendaItemDetalheBeforeDeleteMu sync.Mutex
var documentoVendaItemDetalheBeforeDeleteHooks []DocumentoVendaItemDetalheHook
var documentoVendaItemDetalheAfterDeleteMu sync.Mutex
var documentoVendaItemDetalheAfterDeleteHooks []DocumentoVendaItemDetalheHook

var documentoVendaItemDetalheBeforeUpsertMu sync.Mutex
var documentoVendaItemDetalheBeforeUpsertHooks []DocumentoVendaItemDetalheHook
var documentoVendaItemDetalheAfterUpsertMu sync.Mutex
var documentoVendaItemDetalheAfterUpsertHooks []DocumentoVendaItemDetalheHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DocumentoVendaItemDetalhe) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DocumentoVendaItemDetalhe) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DocumentoVendaItemDetalhe) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DocumentoVendaItemDetalhe) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DocumentoVendaItemDetalhe) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DocumentoVendaItemDetalhe) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DocumentoVendaItemDetalhe) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DocumentoVendaItemDetalhe) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DocumentoVendaItemDetalhe) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemDetalheAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDocumentoVendaItemDetalheHook registers your hook function for all future operations.
func AddDocumentoVendaItemDetalheHook(hookPoint boil.HookPoint, documentoVendaItemDetalheHook DocumentoVendaItemDetalheHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		documentoVendaItemDetalheAfterSelectMu.Lock()
		documentoVendaItemDetalheAfterSelectHooks = append(documentoVendaItemDetalheAfterSelectHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		documentoVendaItemDetalheBeforeInsertMu.Lock()
		documentoVendaItemDetalheBeforeInsertHooks = append(documentoVendaItemDetalheBeforeInsertHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		documentoVendaItemDetalheAfterInsertMu.Lock()
		documentoVendaItemDetalheAfterInsertHooks = append(documentoVendaItemDetalheAfterInsertHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		documentoVendaItemDetalheBeforeUpdateMu.Lock()
		documentoVendaItemDetalheBeforeUpdateHooks = append(documentoVendaItemDetalheBeforeUpdateHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		documentoVendaItemDetalheAfterUpdateMu.Lock()
		documentoVendaItemDetalheAfterUpdateHooks = append(documentoVendaItemDetalheAfterUpdateHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		documentoVendaItemDetalheBeforeDeleteMu.Lock()
		documentoVendaItemDetalheBeforeDeleteHooks = append(documentoVendaItemDetalheBeforeDeleteHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		documentoVendaItemDetalheAfterDeleteMu.Lock()
		documentoVendaItemDetalheAfterDeleteHooks = append(documentoVendaItemDetalheAfterDeleteHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		documentoVendaItemDetalheBeforeUpsertMu.Lock()
		documentoVendaItemDetalheBeforeUpsertHooks = append(documentoVendaItemDetalheBeforeUpsertHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		documentoVendaItemDetalheAfterUpsertMu.Lock()
		documentoVendaItemDetalheAfterUpsertHooks = append(documentoVendaItemDetalheAfterUpsertHooks, documentoVendaItemDetalheHook)
		documentoVendaItemDetalheAfterUpsertMu.Unlock()
	}
}

// One returns a single documentoVendaItemDetalhe record from the query.
func (q documentoVendaItemDetalheQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DocumentoVendaItemDetalhe, error) {
	o := &DocumentoVendaItemDetalhe{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for documento_venda_item_detalhes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DocumentoVendaItemDetalhe records from the query.
func (q documentoVendaItemDetalheQuery) All(ctx context.Context, exec boil.ContextExecutor) (DocumentoVendaItemDetalheSlice, error) {
	var o []*DocumentoVendaItemDetalhe

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DocumentoVendaItemDetalhe slice")
	}

	if len(documentoVendaItemDetalheAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DocumentoVendaItemDetalhe records in the query.
func (q documentoVendaItemDetalheQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count documento_venda_item_detalhes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q documentoVendaItemDetalheQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if documento_venda_item_detalhes exists")
	}

	return count > 0, nil
}

// DocumentoVendaItemDetalhes retrieves all the records using an executor.
func DocumentoVendaItemDetalhes(mods ...qm.QueryMod) documentoVendaItemDetalheQuery {
	mods = append(mods, qm.From("`documento_venda_item_detalhes`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`documento_venda_item_detalhes`.*"})
	}

	return documentoVendaItemDetalheQuery{q}
}

// FindDocumentoVendaItemDetalhe retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDocumentoVendaItemDetalhe(ctx context.Context, exec boil.ContextExecutor, dDVID int, dviItem int, dvidItem int, selectCols ...string) (*DocumentoVendaItemDetalhe, error) {
	documentoVendaItemDetalheObj := &DocumentoVendaItemDetalhe{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `documento_venda_item_detalhes` where `ddv_id`=? AND `dvi_item`=? AND `dvid_item`=?", sel,
	)

	q := queries.Raw(query, dDVID, dviItem, dvidItem)

	err := q.Bind(ctx, exec, documentoVendaItemDetalheObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from documento_venda_item_detalhes")
	}

	if err = documentoVendaItemDetalheObj.doAfterSelectHooks(ctx, exec); err != nil {
		return documentoVendaItemDetalheObj, err
	}

	return documentoVendaItemDetalheObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DocumentoVendaItemDetalhe) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_item_detalhes provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaItemDetalheColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	documentoVendaItemDetalheInsertCacheMut.RLock()
	cache, cached := documentoVendaItemDetalheInsertCache[key]
	documentoVendaItemDetalheInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			documentoVendaItemDetalheAllColumns,
			documentoVendaItemDetalheColumnsWithDefault,
			documentoVendaItemDetalheColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaItemDetalheType, documentoVendaItemDetalheMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(documentoVendaItemDetalheType, documentoVendaItemDetalheMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `documento_venda_item_detalhes` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `documento_venda_item_detalhes` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `documento_venda_item_detalhes` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, documentoVendaItemDetalhePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into documento_venda_item_detalhes")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DDVID,
		o.DviItem,
		o.DvidItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_item_detalhes")
	}

CacheNoHooks:
	if !cached {
		documentoVendaItemDetalheInsertCacheMut.Lock()
		documentoVendaItemDetalheInsertCache[key] = cache
		documentoVendaItemDetalheInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DocumentoVendaItemDetalhe.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DocumentoVendaItemDetalhe) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	documentoVendaItemDetalheUpdateCacheMut.RLock()
	cache, cached := documentoVendaItemDetalheUpdateCache[key]
	documentoVendaItemDetalheUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			documentoVendaItemDetalheAllColumns,
			documentoVendaItemDetalhePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update documento_venda_item_detalhes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `documento_venda_item_detalhes` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, documentoVendaItemDetalhePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(documentoVendaItemDetalheType, documentoVendaItemDetalheMapping, append(wl, documentoVendaItemDetalhePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update documento_venda_item_detalhes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for documento_venda_item_detalhes")
	}

	if !cached {
		documentoVendaItemDetalheUpdateCacheMut.Lock()
		documentoVendaItemDetalheUpdateCache[key] = cache
		documentoVendaItemDetalheUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q documentoVendaItemDetalheQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for documento_venda_item_detalhes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for documento_venda_item_detalhes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DocumentoVendaItemDetalheSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemDetalhePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `documento_venda_item_detalhes` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemDetalhePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in documentoVendaItemDetalhe slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all documentoVendaItemDetalhe")
	}
	return rowsAff, nil
}

var mySQLDocumentoVendaItemDetalheUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DocumentoVendaItemDetalhe) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_item_detalhes provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaItemDetalheColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDocumentoVendaItemDetalheUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	documentoVendaItemDetalheUpsertCacheMut.RLock()
	cache, cached := documentoVendaItemDetalheUpsertCache[key]
	documentoVendaItemDetalheUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			documentoVendaItemDetalheAllColumns,
			documentoVendaItemDetalheColumnsWithDefault,
			documentoVendaItemDetalheColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			documentoVendaItemDetalheAllColumns,
			documentoVendaItemDetalhePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert documento_venda_item_detalhes, could not build update column list")
		}

		ret := strmangle.SetComplement(documentoVendaItemDetalheAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`documento_venda_item_detalhes`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `documento_venda_item_detalhes` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaItemDetalheType, documentoVendaItemDetalheMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(documentoVendaItemDetalheType, documentoVendaItemDetalheMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for documento_venda_item_detalhes")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(documentoVendaItemDetalheType, documentoVendaItemDetalheMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for documento_venda_item_detalhes")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_item_detalhes")
	}

CacheNoHooks:
	if !cached {
		documentoVendaItemDetalheUpsertCacheMut.Lock()
		documentoVendaItemDetalheUpsertCache[key] = cache
		documentoVendaItemDetalheUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DocumentoVendaItemDetalhe record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DocumentoVendaItemDetalhe) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DocumentoVendaItemDetalhe provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), documentoVendaItemDetalhePrimaryKeyMapping)
	sql := "DELETE FROM `documento_venda_item_detalhes` WHERE `ddv_id`=? AND `dvi_item`=? AND `dvid_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from documento_venda_item_detalhes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for documento_venda_item_detalhes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q documentoVendaItemDetalheQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no documentoVendaItemDetalheQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documento_venda_item_detalhes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_item_detalhes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DocumentoVendaItemDetalheSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(documentoVendaItemDetalheBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemDetalhePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `documento_venda_item_detalhes` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemDetalhePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documentoVendaItemDetalhe slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_item_detalhes")
	}

	if len(documentoVendaItemDetalheAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DocumentoVendaItemDetalhe) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDocumentoVendaItemDetalhe(ctx, exec, o.DDVID, o.DviItem, o.DvidItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DocumentoVendaItemDetalheSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DocumentoVendaItemDetalheSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemDetalhePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `documento_venda_item_detalhes`.* FROM `documento_venda_item_detalhes` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemDetalhePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DocumentoVendaItemDetalheSlice")
	}

	*o = slice

	return nil
}

// DocumentoVendaItemDetalheExists checks if the DocumentoVendaItemDetalhe row exists.
func DocumentoVendaItemDetalheExists(ctx context.Context, exec boil.ContextExecutor, dDVID int, dviItem int, dvidItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `documento_venda_item_detalhes` where `ddv_id`=? AND `dvi_item`=? AND `dvid_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, dDVID, dviItem, dvidItem)
	}
	row := exec.QueryRowContext(ctx, sql, dDVID, dviItem, dvidItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if documento_venda_item_detalhes exists")
	}

	return exists, nil
}

// Exists checks if the DocumentoVendaItemDetalhe row exists.
func (o *DocumentoVendaItemDetalhe) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DocumentoVendaItemDetalheExists(ctx, exec, o.DDVID, o.DviItem, o.DvidItem)
}
