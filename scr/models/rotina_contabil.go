// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// RotinaContabil is an object representing the database table.
type RotinaContabil struct {
	RocID        int      `boil:"roc_id" json:"roc_id" toml:"roc_id" yaml:"roc_id"`
	EmfID        int      `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	SetID        null.Int `boil:"set_id" json:"set_id,omitempty" toml:"set_id" yaml:"set_id,omitempty"`
	RocDescricao string   `boil:"roc_descricao" json:"roc_descricao" toml:"roc_descricao" yaml:"roc_descricao"`
	RocSituacao  int16    `boil:"roc_situacao" json:"roc_situacao" toml:"roc_situacao" yaml:"roc_situacao"`

	R *rotinaContabilR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L rotinaContabilL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RotinaContabilColumns = struct {
	RocID        string
	EmfID        string
	SetID        string
	RocDescricao string
	RocSituacao  string
}{
	RocID:        "roc_id",
	EmfID:        "emf_id",
	SetID:        "set_id",
	RocDescricao: "roc_descricao",
	RocSituacao:  "roc_situacao",
}

var RotinaContabilTableColumns = struct {
	RocID        string
	EmfID        string
	SetID        string
	RocDescricao string
	RocSituacao  string
}{
	RocID:        "rotina_contabil.roc_id",
	EmfID:        "rotina_contabil.emf_id",
	SetID:        "rotina_contabil.set_id",
	RocDescricao: "rotina_contabil.roc_descricao",
	RocSituacao:  "rotina_contabil.roc_situacao",
}

// Generated where

var RotinaContabilWhere = struct {
	RocID        whereHelperint
	EmfID        whereHelperint
	SetID        whereHelpernull_Int
	RocDescricao whereHelperstring
	RocSituacao  whereHelperint16
}{
	RocID:        whereHelperint{field: "`rotina_contabil`.`roc_id`"},
	EmfID:        whereHelperint{field: "`rotina_contabil`.`emf_id`"},
	SetID:        whereHelpernull_Int{field: "`rotina_contabil`.`set_id`"},
	RocDescricao: whereHelperstring{field: "`rotina_contabil`.`roc_descricao`"},
	RocSituacao:  whereHelperint16{field: "`rotina_contabil`.`roc_situacao`"},
}

// RotinaContabilRels is where relationship names are stored.
var RotinaContabilRels = struct {
	Emf                         string
	Set                         string
	RocAdiantamentoEntidades    string
	RocChequeOcorenciaCheques   string
	RocChequesRecebidos         string
	RocCondicaoPagamentos       string
	RocDocumentoVendaItems      string
	RocDocumentoVendaPagamentos string
	RocEquipamentos             string
	RocFormaPagamentos          string
	RocLancamentoContabils      string
	RocNotaFiscalItems          string
	RocNotaFiscalPagamentos     string
	RocProcessos                string
	RocRenegociacaos            string
	RocRenegociacaoDocumentos   string
	RocRequisicaos              string
	RocRotinaContabilItens      string
	RocTituloBaixas             string
	RocTitulos                  string
}{
	Emf:                         "Emf",
	Set:                         "Set",
	RocAdiantamentoEntidades:    "RocAdiantamentoEntidades",
	RocChequeOcorenciaCheques:   "RocChequeOcorenciaCheques",
	RocChequesRecebidos:         "RocChequesRecebidos",
	RocCondicaoPagamentos:       "RocCondicaoPagamentos",
	RocDocumentoVendaItems:      "RocDocumentoVendaItems",
	RocDocumentoVendaPagamentos: "RocDocumentoVendaPagamentos",
	RocEquipamentos:             "RocEquipamentos",
	RocFormaPagamentos:          "RocFormaPagamentos",
	RocLancamentoContabils:      "RocLancamentoContabils",
	RocNotaFiscalItems:          "RocNotaFiscalItems",
	RocNotaFiscalPagamentos:     "RocNotaFiscalPagamentos",
	RocProcessos:                "RocProcessos",
	RocRenegociacaos:            "RocRenegociacaos",
	RocRenegociacaoDocumentos:   "RocRenegociacaoDocumentos",
	RocRequisicaos:              "RocRequisicaos",
	RocRotinaContabilItens:      "RocRotinaContabilItens",
	RocTituloBaixas:             "RocTituloBaixas",
	RocTitulos:                  "RocTitulos",
}

// rotinaContabilR is where relationships are stored.
type rotinaContabilR struct {
	Emf                         *EmpresaFilial               `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	Set                         *Setor                       `boil:"Set" json:"Set" toml:"Set" yaml:"Set"`
	RocAdiantamentoEntidades    AdiantamentoEntidadeSlice    `boil:"RocAdiantamentoEntidades" json:"RocAdiantamentoEntidades" toml:"RocAdiantamentoEntidades" yaml:"RocAdiantamentoEntidades"`
	RocChequeOcorenciaCheques   ChequeOcorenciaChequeSlice   `boil:"RocChequeOcorenciaCheques" json:"RocChequeOcorenciaCheques" toml:"RocChequeOcorenciaCheques" yaml:"RocChequeOcorenciaCheques"`
	RocChequesRecebidos         ChequesRecebidoSlice         `boil:"RocChequesRecebidos" json:"RocChequesRecebidos" toml:"RocChequesRecebidos" yaml:"RocChequesRecebidos"`
	RocCondicaoPagamentos       CondicaoPagamentoSlice       `boil:"RocCondicaoPagamentos" json:"RocCondicaoPagamentos" toml:"RocCondicaoPagamentos" yaml:"RocCondicaoPagamentos"`
	RocDocumentoVendaItems      DocumentoVendaItemSlice      `boil:"RocDocumentoVendaItems" json:"RocDocumentoVendaItems" toml:"RocDocumentoVendaItems" yaml:"RocDocumentoVendaItems"`
	RocDocumentoVendaPagamentos DocumentoVendaPagamentoSlice `boil:"RocDocumentoVendaPagamentos" json:"RocDocumentoVendaPagamentos" toml:"RocDocumentoVendaPagamentos" yaml:"RocDocumentoVendaPagamentos"`
	RocEquipamentos             EquipamentoSlice             `boil:"RocEquipamentos" json:"RocEquipamentos" toml:"RocEquipamentos" yaml:"RocEquipamentos"`
	RocFormaPagamentos          FormaPagamentoSlice          `boil:"RocFormaPagamentos" json:"RocFormaPagamentos" toml:"RocFormaPagamentos" yaml:"RocFormaPagamentos"`
	RocLancamentoContabils      LancamentoContabilSlice      `boil:"RocLancamentoContabils" json:"RocLancamentoContabils" toml:"RocLancamentoContabils" yaml:"RocLancamentoContabils"`
	RocNotaFiscalItems          NotaFiscalItemSlice          `boil:"RocNotaFiscalItems" json:"RocNotaFiscalItems" toml:"RocNotaFiscalItems" yaml:"RocNotaFiscalItems"`
	RocNotaFiscalPagamentos     NotaFiscalPagamentoSlice     `boil:"RocNotaFiscalPagamentos" json:"RocNotaFiscalPagamentos" toml:"RocNotaFiscalPagamentos" yaml:"RocNotaFiscalPagamentos"`
	RocProcessos                ProcessoSlice                `boil:"RocProcessos" json:"RocProcessos" toml:"RocProcessos" yaml:"RocProcessos"`
	RocRenegociacaos            RenegociacaoSlice            `boil:"RocRenegociacaos" json:"RocRenegociacaos" toml:"RocRenegociacaos" yaml:"RocRenegociacaos"`
	RocRenegociacaoDocumentos   RenegociacaoDocumentoSlice   `boil:"RocRenegociacaoDocumentos" json:"RocRenegociacaoDocumentos" toml:"RocRenegociacaoDocumentos" yaml:"RocRenegociacaoDocumentos"`
	RocRequisicaos              RequisicaoSlice              `boil:"RocRequisicaos" json:"RocRequisicaos" toml:"RocRequisicaos" yaml:"RocRequisicaos"`
	RocRotinaContabilItens      RotinaContabilItenSlice      `boil:"RocRotinaContabilItens" json:"RocRotinaContabilItens" toml:"RocRotinaContabilItens" yaml:"RocRotinaContabilItens"`
	RocTituloBaixas             TituloBaixasSlice            `boil:"RocTituloBaixas" json:"RocTituloBaixas" toml:"RocTituloBaixas" yaml:"RocTituloBaixas"`
	RocTitulos                  TituloSlice                  `boil:"RocTitulos" json:"RocTitulos" toml:"RocTitulos" yaml:"RocTitulos"`
}

// NewStruct creates a new relationship struct
func (*rotinaContabilR) NewStruct() *rotinaContabilR {
	return &rotinaContabilR{}
}

func (r *rotinaContabilR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *rotinaContabilR) GetSet() *Setor {
	if r == nil {
		return nil
	}
	return r.Set
}

func (r *rotinaContabilR) GetRocAdiantamentoEntidades() AdiantamentoEntidadeSlice {
	if r == nil {
		return nil
	}
	return r.RocAdiantamentoEntidades
}

func (r *rotinaContabilR) GetRocChequeOcorenciaCheques() ChequeOcorenciaChequeSlice {
	if r == nil {
		return nil
	}
	return r.RocChequeOcorenciaCheques
}

func (r *rotinaContabilR) GetRocChequesRecebidos() ChequesRecebidoSlice {
	if r == nil {
		return nil
	}
	return r.RocChequesRecebidos
}

func (r *rotinaContabilR) GetRocCondicaoPagamentos() CondicaoPagamentoSlice {
	if r == nil {
		return nil
	}
	return r.RocCondicaoPagamentos
}

func (r *rotinaContabilR) GetRocDocumentoVendaItems() DocumentoVendaItemSlice {
	if r == nil {
		return nil
	}
	return r.RocDocumentoVendaItems
}

func (r *rotinaContabilR) GetRocDocumentoVendaPagamentos() DocumentoVendaPagamentoSlice {
	if r == nil {
		return nil
	}
	return r.RocDocumentoVendaPagamentos
}

func (r *rotinaContabilR) GetRocEquipamentos() EquipamentoSlice {
	if r == nil {
		return nil
	}
	return r.RocEquipamentos
}

func (r *rotinaContabilR) GetRocFormaPagamentos() FormaPagamentoSlice {
	if r == nil {
		return nil
	}
	return r.RocFormaPagamentos
}

func (r *rotinaContabilR) GetRocLancamentoContabils() LancamentoContabilSlice {
	if r == nil {
		return nil
	}
	return r.RocLancamentoContabils
}

func (r *rotinaContabilR) GetRocNotaFiscalItems() NotaFiscalItemSlice {
	if r == nil {
		return nil
	}
	return r.RocNotaFiscalItems
}

func (r *rotinaContabilR) GetRocNotaFiscalPagamentos() NotaFiscalPagamentoSlice {
	if r == nil {
		return nil
	}
	return r.RocNotaFiscalPagamentos
}

func (r *rotinaContabilR) GetRocProcessos() ProcessoSlice {
	if r == nil {
		return nil
	}
	return r.RocProcessos
}

func (r *rotinaContabilR) GetRocRenegociacaos() RenegociacaoSlice {
	if r == nil {
		return nil
	}
	return r.RocRenegociacaos
}

func (r *rotinaContabilR) GetRocRenegociacaoDocumentos() RenegociacaoDocumentoSlice {
	if r == nil {
		return nil
	}
	return r.RocRenegociacaoDocumentos
}

func (r *rotinaContabilR) GetRocRequisicaos() RequisicaoSlice {
	if r == nil {
		return nil
	}
	return r.RocRequisicaos
}

func (r *rotinaContabilR) GetRocRotinaContabilItens() RotinaContabilItenSlice {
	if r == nil {
		return nil
	}
	return r.RocRotinaContabilItens
}

func (r *rotinaContabilR) GetRocTituloBaixas() TituloBaixasSlice {
	if r == nil {
		return nil
	}
	return r.RocTituloBaixas
}

func (r *rotinaContabilR) GetRocTitulos() TituloSlice {
	if r == nil {
		return nil
	}
	return r.RocTitulos
}

// rotinaContabilL is where Load methods for each relationship are stored.
type rotinaContabilL struct{}

var (
	rotinaContabilAllColumns            = []string{"roc_id", "emf_id", "set_id", "roc_descricao", "roc_situacao"}
	rotinaContabilColumnsWithoutDefault = []string{"roc_id", "emf_id", "set_id", "roc_descricao", "roc_situacao"}
	rotinaContabilColumnsWithDefault    = []string{}
	rotinaContabilPrimaryKeyColumns     = []string{"roc_id"}
	rotinaContabilGeneratedColumns      = []string{}
)

type (
	// RotinaContabilSlice is an alias for a slice of pointers to RotinaContabil.
	// This should almost always be used instead of []RotinaContabil.
	RotinaContabilSlice []*RotinaContabil
	// RotinaContabilHook is the signature for custom RotinaContabil hook methods
	RotinaContabilHook func(context.Context, boil.ContextExecutor, *RotinaContabil) error

	rotinaContabilQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	rotinaContabilType                 = reflect.TypeOf(&RotinaContabil{})
	rotinaContabilMapping              = queries.MakeStructMapping(rotinaContabilType)
	rotinaContabilPrimaryKeyMapping, _ = queries.BindMapping(rotinaContabilType, rotinaContabilMapping, rotinaContabilPrimaryKeyColumns)
	rotinaContabilInsertCacheMut       sync.RWMutex
	rotinaContabilInsertCache          = make(map[string]insertCache)
	rotinaContabilUpdateCacheMut       sync.RWMutex
	rotinaContabilUpdateCache          = make(map[string]updateCache)
	rotinaContabilUpsertCacheMut       sync.RWMutex
	rotinaContabilUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var rotinaContabilAfterSelectMu sync.Mutex
var rotinaContabilAfterSelectHooks []RotinaContabilHook

var rotinaContabilBeforeInsertMu sync.Mutex
var rotinaContabilBeforeInsertHooks []RotinaContabilHook
var rotinaContabilAfterInsertMu sync.Mutex
var rotinaContabilAfterInsertHooks []RotinaContabilHook

var rotinaContabilBeforeUpdateMu sync.Mutex
var rotinaContabilBeforeUpdateHooks []RotinaContabilHook
var rotinaContabilAfterUpdateMu sync.Mutex
var rotinaContabilAfterUpdateHooks []RotinaContabilHook

var rotinaContabilBeforeDeleteMu sync.Mutex
var rotinaContabilBeforeDeleteHooks []RotinaContabilHook
var rotinaContabilAfterDeleteMu sync.Mutex
var rotinaContabilAfterDeleteHooks []RotinaContabilHook

var rotinaContabilBeforeUpsertMu sync.Mutex
var rotinaContabilBeforeUpsertHooks []RotinaContabilHook
var rotinaContabilAfterUpsertMu sync.Mutex
var rotinaContabilAfterUpsertHooks []RotinaContabilHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RotinaContabil) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RotinaContabil) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RotinaContabil) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RotinaContabil) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RotinaContabil) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RotinaContabil) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RotinaContabil) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RotinaContabil) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RotinaContabil) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range rotinaContabilAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRotinaContabilHook registers your hook function for all future operations.
func AddRotinaContabilHook(hookPoint boil.HookPoint, rotinaContabilHook RotinaContabilHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		rotinaContabilAfterSelectMu.Lock()
		rotinaContabilAfterSelectHooks = append(rotinaContabilAfterSelectHooks, rotinaContabilHook)
		rotinaContabilAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		rotinaContabilBeforeInsertMu.Lock()
		rotinaContabilBeforeInsertHooks = append(rotinaContabilBeforeInsertHooks, rotinaContabilHook)
		rotinaContabilBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		rotinaContabilAfterInsertMu.Lock()
		rotinaContabilAfterInsertHooks = append(rotinaContabilAfterInsertHooks, rotinaContabilHook)
		rotinaContabilAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		rotinaContabilBeforeUpdateMu.Lock()
		rotinaContabilBeforeUpdateHooks = append(rotinaContabilBeforeUpdateHooks, rotinaContabilHook)
		rotinaContabilBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		rotinaContabilAfterUpdateMu.Lock()
		rotinaContabilAfterUpdateHooks = append(rotinaContabilAfterUpdateHooks, rotinaContabilHook)
		rotinaContabilAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		rotinaContabilBeforeDeleteMu.Lock()
		rotinaContabilBeforeDeleteHooks = append(rotinaContabilBeforeDeleteHooks, rotinaContabilHook)
		rotinaContabilBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		rotinaContabilAfterDeleteMu.Lock()
		rotinaContabilAfterDeleteHooks = append(rotinaContabilAfterDeleteHooks, rotinaContabilHook)
		rotinaContabilAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		rotinaContabilBeforeUpsertMu.Lock()
		rotinaContabilBeforeUpsertHooks = append(rotinaContabilBeforeUpsertHooks, rotinaContabilHook)
		rotinaContabilBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		rotinaContabilAfterUpsertMu.Lock()
		rotinaContabilAfterUpsertHooks = append(rotinaContabilAfterUpsertHooks, rotinaContabilHook)
		rotinaContabilAfterUpsertMu.Unlock()
	}
}

// One returns a single rotinaContabil record from the query.
func (q rotinaContabilQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RotinaContabil, error) {
	o := &RotinaContabil{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for rotina_contabil")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RotinaContabil records from the query.
func (q rotinaContabilQuery) All(ctx context.Context, exec boil.ContextExecutor) (RotinaContabilSlice, error) {
	var o []*RotinaContabil

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RotinaContabil slice")
	}

	if len(rotinaContabilAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RotinaContabil records in the query.
func (q rotinaContabilQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count rotina_contabil rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q rotinaContabilQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if rotina_contabil exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *RotinaContabil) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// Set pointed to by the foreign key.
func (o *RotinaContabil) Set(mods ...qm.QueryMod) setorQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`set_id` = ?", o.SetID),
	}

	queryMods = append(queryMods, mods...)

	return Setors(queryMods...)
}

// RocAdiantamentoEntidades retrieves all the adiantamento_entidade's AdiantamentoEntidades with an executor via roc_id column.
func (o *RotinaContabil) RocAdiantamentoEntidades(mods ...qm.QueryMod) adiantamentoEntidadeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`adiantamento_entidade`.`roc_id`=?", o.RocID),
	)

	return AdiantamentoEntidades(queryMods...)
}

// RocChequeOcorenciaCheques retrieves all the cheque_ocorencia_cheque's ChequeOcorenciaCheques with an executor via roc_id column.
func (o *RotinaContabil) RocChequeOcorenciaCheques(mods ...qm.QueryMod) chequeOcorenciaChequeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`cheque_ocorencia_cheque`.`roc_id`=?", o.RocID),
	)

	return ChequeOcorenciaCheques(queryMods...)
}

// RocChequesRecebidos retrieves all the cheques_recebido's ChequesRecebidos with an executor via roc_id column.
func (o *RotinaContabil) RocChequesRecebidos(mods ...qm.QueryMod) chequesRecebidoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`cheques_recebidos`.`roc_id`=?", o.RocID),
	)

	return ChequesRecebidos(queryMods...)
}

// RocCondicaoPagamentos retrieves all the condicao_pagamento's CondicaoPagamentos with an executor via roc_id column.
func (o *RotinaContabil) RocCondicaoPagamentos(mods ...qm.QueryMod) condicaoPagamentoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`condicao_pagamento`.`roc_id`=?", o.RocID),
	)

	return CondicaoPagamentos(queryMods...)
}

// RocDocumentoVendaItems retrieves all the documento_venda_item's DocumentoVendaItems with an executor via roc_id column.
func (o *RotinaContabil) RocDocumentoVendaItems(mods ...qm.QueryMod) documentoVendaItemQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`documento_venda_item`.`roc_id`=?", o.RocID),
	)

	return DocumentoVendaItems(queryMods...)
}

// RocDocumentoVendaPagamentos retrieves all the documento_venda_pagamento's DocumentoVendaPagamentos with an executor via roc_id column.
func (o *RotinaContabil) RocDocumentoVendaPagamentos(mods ...qm.QueryMod) documentoVendaPagamentoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`documento_venda_pagamento`.`roc_id`=?", o.RocID),
	)

	return DocumentoVendaPagamentos(queryMods...)
}

// RocEquipamentos retrieves all the equipamento's Equipamentos with an executor via roc_id column.
func (o *RotinaContabil) RocEquipamentos(mods ...qm.QueryMod) equipamentoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`equipamento`.`roc_id`=?", o.RocID),
	)

	return Equipamentos(queryMods...)
}

// RocFormaPagamentos retrieves all the forma_pagamento's FormaPagamentos with an executor via roc_id column.
func (o *RotinaContabil) RocFormaPagamentos(mods ...qm.QueryMod) formaPagamentoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`forma_pagamento`.`roc_id`=?", o.RocID),
	)

	return FormaPagamentos(queryMods...)
}

// RocLancamentoContabils retrieves all the lancamento_contabil's LancamentoContabils with an executor via roc_id column.
func (o *RotinaContabil) RocLancamentoContabils(mods ...qm.QueryMod) lancamentoContabilQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`lancamento_contabil`.`roc_id`=?", o.RocID),
	)

	return LancamentoContabils(queryMods...)
}

// RocNotaFiscalItems retrieves all the nota_fiscal_item's NotaFiscalItems with an executor via roc_id column.
func (o *RotinaContabil) RocNotaFiscalItems(mods ...qm.QueryMod) notaFiscalItemQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal_item`.`roc_id`=?", o.RocID),
	)

	return NotaFiscalItems(queryMods...)
}

// RocNotaFiscalPagamentos retrieves all the nota_fiscal_pagamento's NotaFiscalPagamentos with an executor via roc_id column.
func (o *RotinaContabil) RocNotaFiscalPagamentos(mods ...qm.QueryMod) notaFiscalPagamentoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal_pagamento`.`roc_id`=?", o.RocID),
	)

	return NotaFiscalPagamentos(queryMods...)
}

// RocProcessos retrieves all the processo's Processos with an executor via roc_id column.
func (o *RotinaContabil) RocProcessos(mods ...qm.QueryMod) processoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`processo`.`roc_id`=?", o.RocID),
	)

	return Processos(queryMods...)
}

// RocRenegociacaos retrieves all the renegociacao's Renegociacaos with an executor via roc_id column.
func (o *RotinaContabil) RocRenegociacaos(mods ...qm.QueryMod) renegociacaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`renegociacao`.`roc_id`=?", o.RocID),
	)

	return Renegociacaos(queryMods...)
}

// RocRenegociacaoDocumentos retrieves all the renegociacao_documento's RenegociacaoDocumentos with an executor via roc_id column.
func (o *RotinaContabil) RocRenegociacaoDocumentos(mods ...qm.QueryMod) renegociacaoDocumentoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`renegociacao_documentos`.`roc_id`=?", o.RocID),
	)

	return RenegociacaoDocumentos(queryMods...)
}

// RocRequisicaos retrieves all the requisicao's Requisicaos with an executor via roc_id column.
func (o *RotinaContabil) RocRequisicaos(mods ...qm.QueryMod) requisicaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`requisicao`.`roc_id`=?", o.RocID),
	)

	return Requisicaos(queryMods...)
}

// RocRotinaContabilItens retrieves all the rotina_contabil_iten's RotinaContabilItens with an executor via roc_id column.
func (o *RotinaContabil) RocRotinaContabilItens(mods ...qm.QueryMod) rotinaContabilItenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`rotina_contabil_itens`.`roc_id`=?", o.RocID),
	)

	return RotinaContabilItens(queryMods...)
}

// RocTituloBaixas retrieves all the titulo_baixas's TituloBaixas with an executor via roc_id column.
func (o *RotinaContabil) RocTituloBaixas(mods ...qm.QueryMod) tituloBaixasQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`titulo_baixas`.`roc_id`=?", o.RocID),
	)

	return TituloBaixas(queryMods...)
}

// RocTitulos retrieves all the titulo's Titulos with an executor via roc_id column.
func (o *RotinaContabil) RocTitulos(mods ...qm.QueryMod) tituloQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`titulos`.`roc_id`=?", o.RocID),
	)

	return Titulos(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (rotinaContabilL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfRotinaContabils = append(foreign.R.EmfRotinaContabils, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfRotinaContabils = append(foreign.R.EmfRotinaContabils, local)
				break
			}
		}
	}

	return nil
}

// LoadSet allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (rotinaContabilL) LoadSet(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		if !queries.IsNil(object.SetID) {
			args[object.SetID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}

			if !queries.IsNil(obj.SetID) {
				args[obj.SetID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`setor`),
		qm.WhereIn(`setor.set_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Setor")
	}

	var resultSlice []*Setor
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Setor")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for setor")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for setor")
	}

	if len(setorAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Set = foreign
		if foreign.R == nil {
			foreign.R = &setorR{}
		}
		foreign.R.SetRotinaContabils = append(foreign.R.SetRotinaContabils, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.SetID, foreign.SetID) {
				local.R.Set = foreign
				if foreign.R == nil {
					foreign.R = &setorR{}
				}
				foreign.R.SetRotinaContabils = append(foreign.R.SetRotinaContabils, local)
				break
			}
		}
	}

	return nil
}

// LoadRocAdiantamentoEntidades allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocAdiantamentoEntidades(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`adiantamento_entidade`),
		qm.WhereIn(`adiantamento_entidade.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load adiantamento_entidade")
	}

	var resultSlice []*AdiantamentoEntidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice adiantamento_entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on adiantamento_entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for adiantamento_entidade")
	}

	if len(adiantamentoEntidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocAdiantamentoEntidades = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &adiantamentoEntidadeR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocAdiantamentoEntidades = append(local.R.RocAdiantamentoEntidades, foreign)
				if foreign.R == nil {
					foreign.R = &adiantamentoEntidadeR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocChequeOcorenciaCheques allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocChequeOcorenciaCheques(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cheque_ocorencia_cheque`),
		qm.WhereIn(`cheque_ocorencia_cheque.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load cheque_ocorencia_cheque")
	}

	var resultSlice []*ChequeOcorenciaCheque
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice cheque_ocorencia_cheque")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on cheque_ocorencia_cheque")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cheque_ocorencia_cheque")
	}

	if len(chequeOcorenciaChequeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocChequeOcorenciaCheques = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &chequeOcorenciaChequeR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocChequeOcorenciaCheques = append(local.R.RocChequeOcorenciaCheques, foreign)
				if foreign.R == nil {
					foreign.R = &chequeOcorenciaChequeR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocChequesRecebidos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocChequesRecebidos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cheques_recebidos`),
		qm.WhereIn(`cheques_recebidos.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load cheques_recebidos")
	}

	var resultSlice []*ChequesRecebido
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice cheques_recebidos")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on cheques_recebidos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cheques_recebidos")
	}

	if len(chequesRecebidoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocChequesRecebidos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &chequesRecebidoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocChequesRecebidos = append(local.R.RocChequesRecebidos, foreign)
				if foreign.R == nil {
					foreign.R = &chequesRecebidoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocCondicaoPagamentos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocCondicaoPagamentos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`condicao_pagamento`),
		qm.WhereIn(`condicao_pagamento.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load condicao_pagamento")
	}

	var resultSlice []*CondicaoPagamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice condicao_pagamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on condicao_pagamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for condicao_pagamento")
	}

	if len(condicaoPagamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocCondicaoPagamentos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &condicaoPagamentoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocCondicaoPagamentos = append(local.R.RocCondicaoPagamentos, foreign)
				if foreign.R == nil {
					foreign.R = &condicaoPagamentoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocDocumentoVendaItems allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocDocumentoVendaItems(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documento_venda_item`),
		qm.WhereIn(`documento_venda_item.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load documento_venda_item")
	}

	var resultSlice []*DocumentoVendaItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice documento_venda_item")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on documento_venda_item")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documento_venda_item")
	}

	if len(documentoVendaItemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocDocumentoVendaItems = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentoVendaItemR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocDocumentoVendaItems = append(local.R.RocDocumentoVendaItems, foreign)
				if foreign.R == nil {
					foreign.R = &documentoVendaItemR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocDocumentoVendaPagamentos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocDocumentoVendaPagamentos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documento_venda_pagamento`),
		qm.WhereIn(`documento_venda_pagamento.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load documento_venda_pagamento")
	}

	var resultSlice []*DocumentoVendaPagamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice documento_venda_pagamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on documento_venda_pagamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documento_venda_pagamento")
	}

	if len(documentoVendaPagamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocDocumentoVendaPagamentos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentoVendaPagamentoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocDocumentoVendaPagamentos = append(local.R.RocDocumentoVendaPagamentos, foreign)
				if foreign.R == nil {
					foreign.R = &documentoVendaPagamentoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocEquipamentos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocEquipamentos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`equipamento`),
		qm.WhereIn(`equipamento.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load equipamento")
	}

	var resultSlice []*Equipamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice equipamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on equipamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for equipamento")
	}

	if len(equipamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocEquipamentos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &equipamentoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocEquipamentos = append(local.R.RocEquipamentos, foreign)
				if foreign.R == nil {
					foreign.R = &equipamentoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocFormaPagamentos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocFormaPagamentos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`forma_pagamento`),
		qm.WhereIn(`forma_pagamento.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load forma_pagamento")
	}

	var resultSlice []*FormaPagamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice forma_pagamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on forma_pagamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for forma_pagamento")
	}

	if len(formaPagamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocFormaPagamentos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &formaPagamentoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocFormaPagamentos = append(local.R.RocFormaPagamentos, foreign)
				if foreign.R == nil {
					foreign.R = &formaPagamentoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocLancamentoContabils allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocLancamentoContabils(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`lancamento_contabil`),
		qm.WhereIn(`lancamento_contabil.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load lancamento_contabil")
	}

	var resultSlice []*LancamentoContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice lancamento_contabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on lancamento_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for lancamento_contabil")
	}

	if len(lancamentoContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocLancamentoContabils = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &lancamentoContabilR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.RocID == foreign.RocID {
				local.R.RocLancamentoContabils = append(local.R.RocLancamentoContabils, foreign)
				if foreign.R == nil {
					foreign.R = &lancamentoContabilR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocNotaFiscalItems allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocNotaFiscalItems(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal_item`),
		qm.WhereIn(`nota_fiscal_item.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal_item")
	}

	var resultSlice []*NotaFiscalItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal_item")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal_item")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal_item")
	}

	if len(notaFiscalItemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocNotaFiscalItems = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalItemR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocNotaFiscalItems = append(local.R.RocNotaFiscalItems, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalItemR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocNotaFiscalPagamentos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocNotaFiscalPagamentos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal_pagamento`),
		qm.WhereIn(`nota_fiscal_pagamento.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal_pagamento")
	}

	var resultSlice []*NotaFiscalPagamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal_pagamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal_pagamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal_pagamento")
	}

	if len(notaFiscalPagamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocNotaFiscalPagamentos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalPagamentoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocNotaFiscalPagamentos = append(local.R.RocNotaFiscalPagamentos, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalPagamentoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocProcessos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocProcessos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`processo`),
		qm.WhereIn(`processo.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load processo")
	}

	var resultSlice []*Processo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice processo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on processo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for processo")
	}

	if len(processoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocProcessos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &processoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocProcessos = append(local.R.RocProcessos, foreign)
				if foreign.R == nil {
					foreign.R = &processoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocRenegociacaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocRenegociacaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`renegociacao`),
		qm.WhereIn(`renegociacao.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load renegociacao")
	}

	var resultSlice []*Renegociacao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice renegociacao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on renegociacao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for renegociacao")
	}

	if len(renegociacaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocRenegociacaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &renegociacaoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocRenegociacaos = append(local.R.RocRenegociacaos, foreign)
				if foreign.R == nil {
					foreign.R = &renegociacaoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocRenegociacaoDocumentos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocRenegociacaoDocumentos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`renegociacao_documentos`),
		qm.WhereIn(`renegociacao_documentos.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load renegociacao_documentos")
	}

	var resultSlice []*RenegociacaoDocumento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice renegociacao_documentos")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on renegociacao_documentos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for renegociacao_documentos")
	}

	if len(renegociacaoDocumentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocRenegociacaoDocumentos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &renegociacaoDocumentoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocRenegociacaoDocumentos = append(local.R.RocRenegociacaoDocumentos, foreign)
				if foreign.R == nil {
					foreign.R = &renegociacaoDocumentoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocRequisicaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocRequisicaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`requisicao`),
		qm.WhereIn(`requisicao.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load requisicao")
	}

	var resultSlice []*Requisicao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice requisicao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on requisicao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for requisicao")
	}

	if len(requisicaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocRequisicaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &requisicaoR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocRequisicaos = append(local.R.RocRequisicaos, foreign)
				if foreign.R == nil {
					foreign.R = &requisicaoR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocRotinaContabilItens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocRotinaContabilItens(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil_itens`),
		qm.WhereIn(`rotina_contabil_itens.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load rotina_contabil_itens")
	}

	var resultSlice []*RotinaContabilIten
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice rotina_contabil_itens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on rotina_contabil_itens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil_itens")
	}

	if len(rotinaContabilItenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocRotinaContabilItens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &rotinaContabilItenR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.RocID == foreign.RocID {
				local.R.RocRotinaContabilItens = append(local.R.RocRotinaContabilItens, foreign)
				if foreign.R == nil {
					foreign.R = &rotinaContabilItenR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocTituloBaixas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocTituloBaixas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulo_baixas`),
		qm.WhereIn(`titulo_baixas.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load titulo_baixas")
	}

	var resultSlice []*TituloBaixas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice titulo_baixas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on titulo_baixas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulo_baixas")
	}

	if len(tituloBaixasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocTituloBaixas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &tituloBaixasR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocTituloBaixas = append(local.R.RocTituloBaixas, foreign)
				if foreign.R == nil {
					foreign.R = &tituloBaixasR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// LoadRocTitulos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (rotinaContabilL) LoadRocTitulos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRotinaContabil interface{}, mods queries.Applicator) error {
	var slice []*RotinaContabil
	var object *RotinaContabil

	if singular {
		var ok bool
		object, ok = maybeRotinaContabil.(*RotinaContabil)
		if !ok {
			object = new(RotinaContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeRotinaContabil))
			}
		}
	} else {
		s, ok := maybeRotinaContabil.(*[]*RotinaContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeRotinaContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeRotinaContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &rotinaContabilR{}
		}
		args[object.RocID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &rotinaContabilR{}
			}
			args[obj.RocID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulos`),
		qm.WhereIn(`titulos.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load titulos")
	}

	var resultSlice []*Titulo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice titulos")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on titulos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulos")
	}

	if len(tituloAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RocTitulos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &tituloR{}
			}
			foreign.R.Roc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.RocTitulos = append(local.R.RocTitulos, foreign)
				if foreign.R == nil {
					foreign.R = &tituloR{}
				}
				foreign.R.Roc = local
				break
			}
		}
	}

	return nil
}

// SetEmf of the rotinaContabil to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfRotinaContabils.
func (o *RotinaContabil) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `rotina_contabil` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, rotinaContabilPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.RocID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &rotinaContabilR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfRotinaContabils: RotinaContabilSlice{o},
		}
	} else {
		related.R.EmfRotinaContabils = append(related.R.EmfRotinaContabils, o)
	}

	return nil
}

// SetSet of the rotinaContabil to the related item.
// Sets o.R.Set to related.
// Adds o to related.R.SetRotinaContabils.
func (o *RotinaContabil) SetSet(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Setor) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `rotina_contabil` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"set_id"}),
		strmangle.WhereClause("`", "`", 0, rotinaContabilPrimaryKeyColumns),
	)
	values := []interface{}{related.SetID, o.RocID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.SetID, related.SetID)
	if o.R == nil {
		o.R = &rotinaContabilR{
			Set: related,
		}
	} else {
		o.R.Set = related
	}

	if related.R == nil {
		related.R = &setorR{
			SetRotinaContabils: RotinaContabilSlice{o},
		}
	} else {
		related.R.SetRotinaContabils = append(related.R.SetRotinaContabils, o)
	}

	return nil
}

// RemoveSet relationship.
// Sets o.R.Set to nil.
// Removes o from all passed in related items' relationships struct.
func (o *RotinaContabil) RemoveSet(ctx context.Context, exec boil.ContextExecutor, related *Setor) error {
	var err error

	queries.SetScanner(&o.SetID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("set_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Set = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.SetRotinaContabils {
		if queries.Equal(o.SetID, ri.SetID) {
			continue
		}

		ln := len(related.R.SetRotinaContabils)
		if ln > 1 && i < ln-1 {
			related.R.SetRotinaContabils[i] = related.R.SetRotinaContabils[ln-1]
		}
		related.R.SetRotinaContabils = related.R.SetRotinaContabils[:ln-1]
		break
	}
	return nil
}

// AddRocAdiantamentoEntidades adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocAdiantamentoEntidades.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocAdiantamentoEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AdiantamentoEntidade) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `adiantamento_entidade` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, adiantamentoEntidadePrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.AdtentID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocAdiantamentoEntidades: related,
		}
	} else {
		o.R.RocAdiantamentoEntidades = append(o.R.RocAdiantamentoEntidades, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &adiantamentoEntidadeR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocAdiantamentoEntidades removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocAdiantamentoEntidades accordingly.
// Replaces o.R.RocAdiantamentoEntidades with related.
// Sets related.R.Roc's RocAdiantamentoEntidades accordingly.
func (o *RotinaContabil) SetRocAdiantamentoEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AdiantamentoEntidade) error {
	query := "update `adiantamento_entidade` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocAdiantamentoEntidades {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocAdiantamentoEntidades = nil
	}

	return o.AddRocAdiantamentoEntidades(ctx, exec, insert, related...)
}

// RemoveRocAdiantamentoEntidades relationships from objects passed in.
// Removes related items from R.RocAdiantamentoEntidades (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocAdiantamentoEntidades(ctx context.Context, exec boil.ContextExecutor, related ...*AdiantamentoEntidade) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocAdiantamentoEntidades {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocAdiantamentoEntidades)
			if ln > 1 && i < ln-1 {
				o.R.RocAdiantamentoEntidades[i] = o.R.RocAdiantamentoEntidades[ln-1]
			}
			o.R.RocAdiantamentoEntidades = o.R.RocAdiantamentoEntidades[:ln-1]
			break
		}
	}

	return nil
}

// AddRocChequeOcorenciaCheques adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocChequeOcorenciaCheques.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocChequeOcorenciaCheques(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ChequeOcorenciaCheque) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `cheque_ocorencia_cheque` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, chequeOcorenciaChequePrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.ChqrecID, rel.ChqochItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocChequeOcorenciaCheques: related,
		}
	} else {
		o.R.RocChequeOcorenciaCheques = append(o.R.RocChequeOcorenciaCheques, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &chequeOcorenciaChequeR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocChequeOcorenciaCheques removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocChequeOcorenciaCheques accordingly.
// Replaces o.R.RocChequeOcorenciaCheques with related.
// Sets related.R.Roc's RocChequeOcorenciaCheques accordingly.
func (o *RotinaContabil) SetRocChequeOcorenciaCheques(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ChequeOcorenciaCheque) error {
	query := "update `cheque_ocorencia_cheque` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocChequeOcorenciaCheques {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocChequeOcorenciaCheques = nil
	}

	return o.AddRocChequeOcorenciaCheques(ctx, exec, insert, related...)
}

// RemoveRocChequeOcorenciaCheques relationships from objects passed in.
// Removes related items from R.RocChequeOcorenciaCheques (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocChequeOcorenciaCheques(ctx context.Context, exec boil.ContextExecutor, related ...*ChequeOcorenciaCheque) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocChequeOcorenciaCheques {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocChequeOcorenciaCheques)
			if ln > 1 && i < ln-1 {
				o.R.RocChequeOcorenciaCheques[i] = o.R.RocChequeOcorenciaCheques[ln-1]
			}
			o.R.RocChequeOcorenciaCheques = o.R.RocChequeOcorenciaCheques[:ln-1]
			break
		}
	}

	return nil
}

// AddRocChequesRecebidos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocChequesRecebidos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocChequesRecebidos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ChequesRecebido) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `cheques_recebidos` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, chequesRecebidoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.ChqrecID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocChequesRecebidos: related,
		}
	} else {
		o.R.RocChequesRecebidos = append(o.R.RocChequesRecebidos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &chequesRecebidoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocChequesRecebidos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocChequesRecebidos accordingly.
// Replaces o.R.RocChequesRecebidos with related.
// Sets related.R.Roc's RocChequesRecebidos accordingly.
func (o *RotinaContabil) SetRocChequesRecebidos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ChequesRecebido) error {
	query := "update `cheques_recebidos` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocChequesRecebidos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocChequesRecebidos = nil
	}

	return o.AddRocChequesRecebidos(ctx, exec, insert, related...)
}

// RemoveRocChequesRecebidos relationships from objects passed in.
// Removes related items from R.RocChequesRecebidos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocChequesRecebidos(ctx context.Context, exec boil.ContextExecutor, related ...*ChequesRecebido) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocChequesRecebidos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocChequesRecebidos)
			if ln > 1 && i < ln-1 {
				o.R.RocChequesRecebidos[i] = o.R.RocChequesRecebidos[ln-1]
			}
			o.R.RocChequesRecebidos = o.R.RocChequesRecebidos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocCondicaoPagamentos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocCondicaoPagamentos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocCondicaoPagamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CondicaoPagamento) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `condicao_pagamento` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, condicaoPagamentoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.CodpgtID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocCondicaoPagamentos: related,
		}
	} else {
		o.R.RocCondicaoPagamentos = append(o.R.RocCondicaoPagamentos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &condicaoPagamentoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocCondicaoPagamentos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocCondicaoPagamentos accordingly.
// Replaces o.R.RocCondicaoPagamentos with related.
// Sets related.R.Roc's RocCondicaoPagamentos accordingly.
func (o *RotinaContabil) SetRocCondicaoPagamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CondicaoPagamento) error {
	query := "update `condicao_pagamento` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocCondicaoPagamentos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocCondicaoPagamentos = nil
	}

	return o.AddRocCondicaoPagamentos(ctx, exec, insert, related...)
}

// RemoveRocCondicaoPagamentos relationships from objects passed in.
// Removes related items from R.RocCondicaoPagamentos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocCondicaoPagamentos(ctx context.Context, exec boil.ContextExecutor, related ...*CondicaoPagamento) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocCondicaoPagamentos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocCondicaoPagamentos)
			if ln > 1 && i < ln-1 {
				o.R.RocCondicaoPagamentos[i] = o.R.RocCondicaoPagamentos[ln-1]
			}
			o.R.RocCondicaoPagamentos = o.R.RocCondicaoPagamentos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocDocumentoVendaItems adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocDocumentoVendaItems.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocDocumentoVendaItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVendaItem) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `documento_venda_item` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, documentoVendaItemPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.DDVID, rel.DviItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocDocumentoVendaItems: related,
		}
	} else {
		o.R.RocDocumentoVendaItems = append(o.R.RocDocumentoVendaItems, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentoVendaItemR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocDocumentoVendaItems removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocDocumentoVendaItems accordingly.
// Replaces o.R.RocDocumentoVendaItems with related.
// Sets related.R.Roc's RocDocumentoVendaItems accordingly.
func (o *RotinaContabil) SetRocDocumentoVendaItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVendaItem) error {
	query := "update `documento_venda_item` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocDocumentoVendaItems {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocDocumentoVendaItems = nil
	}

	return o.AddRocDocumentoVendaItems(ctx, exec, insert, related...)
}

// RemoveRocDocumentoVendaItems relationships from objects passed in.
// Removes related items from R.RocDocumentoVendaItems (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocDocumentoVendaItems(ctx context.Context, exec boil.ContextExecutor, related ...*DocumentoVendaItem) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocDocumentoVendaItems {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocDocumentoVendaItems)
			if ln > 1 && i < ln-1 {
				o.R.RocDocumentoVendaItems[i] = o.R.RocDocumentoVendaItems[ln-1]
			}
			o.R.RocDocumentoVendaItems = o.R.RocDocumentoVendaItems[:ln-1]
			break
		}
	}

	return nil
}

// AddRocDocumentoVendaPagamentos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocDocumentoVendaPagamentos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocDocumentoVendaPagamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVendaPagamento) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `documento_venda_pagamento` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, documentoVendaPagamentoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.DDVID, rel.DVPItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocDocumentoVendaPagamentos: related,
		}
	} else {
		o.R.RocDocumentoVendaPagamentos = append(o.R.RocDocumentoVendaPagamentos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentoVendaPagamentoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocDocumentoVendaPagamentos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocDocumentoVendaPagamentos accordingly.
// Replaces o.R.RocDocumentoVendaPagamentos with related.
// Sets related.R.Roc's RocDocumentoVendaPagamentos accordingly.
func (o *RotinaContabil) SetRocDocumentoVendaPagamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVendaPagamento) error {
	query := "update `documento_venda_pagamento` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocDocumentoVendaPagamentos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocDocumentoVendaPagamentos = nil
	}

	return o.AddRocDocumentoVendaPagamentos(ctx, exec, insert, related...)
}

// RemoveRocDocumentoVendaPagamentos relationships from objects passed in.
// Removes related items from R.RocDocumentoVendaPagamentos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocDocumentoVendaPagamentos(ctx context.Context, exec boil.ContextExecutor, related ...*DocumentoVendaPagamento) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocDocumentoVendaPagamentos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocDocumentoVendaPagamentos)
			if ln > 1 && i < ln-1 {
				o.R.RocDocumentoVendaPagamentos[i] = o.R.RocDocumentoVendaPagamentos[ln-1]
			}
			o.R.RocDocumentoVendaPagamentos = o.R.RocDocumentoVendaPagamentos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocEquipamentos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocEquipamentos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocEquipamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Equipamento) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `equipamento` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, equipamentoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.EquID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocEquipamentos: related,
		}
	} else {
		o.R.RocEquipamentos = append(o.R.RocEquipamentos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &equipamentoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocEquipamentos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocEquipamentos accordingly.
// Replaces o.R.RocEquipamentos with related.
// Sets related.R.Roc's RocEquipamentos accordingly.
func (o *RotinaContabil) SetRocEquipamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Equipamento) error {
	query := "update `equipamento` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocEquipamentos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocEquipamentos = nil
	}

	return o.AddRocEquipamentos(ctx, exec, insert, related...)
}

// RemoveRocEquipamentos relationships from objects passed in.
// Removes related items from R.RocEquipamentos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocEquipamentos(ctx context.Context, exec boil.ContextExecutor, related ...*Equipamento) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocEquipamentos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocEquipamentos)
			if ln > 1 && i < ln-1 {
				o.R.RocEquipamentos[i] = o.R.RocEquipamentos[ln-1]
			}
			o.R.RocEquipamentos = o.R.RocEquipamentos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocFormaPagamentos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocFormaPagamentos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocFormaPagamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FormaPagamento) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `forma_pagamento` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, formaPagamentoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.FrmpgtoID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocFormaPagamentos: related,
		}
	} else {
		o.R.RocFormaPagamentos = append(o.R.RocFormaPagamentos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &formaPagamentoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocFormaPagamentos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocFormaPagamentos accordingly.
// Replaces o.R.RocFormaPagamentos with related.
// Sets related.R.Roc's RocFormaPagamentos accordingly.
func (o *RotinaContabil) SetRocFormaPagamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FormaPagamento) error {
	query := "update `forma_pagamento` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocFormaPagamentos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocFormaPagamentos = nil
	}

	return o.AddRocFormaPagamentos(ctx, exec, insert, related...)
}

// RemoveRocFormaPagamentos relationships from objects passed in.
// Removes related items from R.RocFormaPagamentos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocFormaPagamentos(ctx context.Context, exec boil.ContextExecutor, related ...*FormaPagamento) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocFormaPagamentos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocFormaPagamentos)
			if ln > 1 && i < ln-1 {
				o.R.RocFormaPagamentos[i] = o.R.RocFormaPagamentos[ln-1]
			}
			o.R.RocFormaPagamentos = o.R.RocFormaPagamentos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocLancamentoContabils adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocLancamentoContabils.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocLancamentoContabils(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoContabil) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.RocID = o.RocID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `lancamento_contabil` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, lancamentoContabilPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.LCTBID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.RocID = o.RocID
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocLancamentoContabils: related,
		}
	} else {
		o.R.RocLancamentoContabils = append(o.R.RocLancamentoContabils, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &lancamentoContabilR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// AddRocNotaFiscalItems adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocNotaFiscalItems.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocNotaFiscalItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalItem) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal_item` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalItemPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.NTFID, rel.NfiItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocNotaFiscalItems: related,
		}
	} else {
		o.R.RocNotaFiscalItems = append(o.R.RocNotaFiscalItems, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalItemR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocNotaFiscalItems removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocNotaFiscalItems accordingly.
// Replaces o.R.RocNotaFiscalItems with related.
// Sets related.R.Roc's RocNotaFiscalItems accordingly.
func (o *RotinaContabil) SetRocNotaFiscalItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalItem) error {
	query := "update `nota_fiscal_item` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocNotaFiscalItems {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocNotaFiscalItems = nil
	}

	return o.AddRocNotaFiscalItems(ctx, exec, insert, related...)
}

// RemoveRocNotaFiscalItems relationships from objects passed in.
// Removes related items from R.RocNotaFiscalItems (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocNotaFiscalItems(ctx context.Context, exec boil.ContextExecutor, related ...*NotaFiscalItem) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocNotaFiscalItems {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocNotaFiscalItems)
			if ln > 1 && i < ln-1 {
				o.R.RocNotaFiscalItems[i] = o.R.RocNotaFiscalItems[ln-1]
			}
			o.R.RocNotaFiscalItems = o.R.RocNotaFiscalItems[:ln-1]
			break
		}
	}

	return nil
}

// AddRocNotaFiscalPagamentos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocNotaFiscalPagamentos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocNotaFiscalPagamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalPagamento) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal_pagamento` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalPagamentoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.NTFID, rel.NFPItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocNotaFiscalPagamentos: related,
		}
	} else {
		o.R.RocNotaFiscalPagamentos = append(o.R.RocNotaFiscalPagamentos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalPagamentoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocNotaFiscalPagamentos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocNotaFiscalPagamentos accordingly.
// Replaces o.R.RocNotaFiscalPagamentos with related.
// Sets related.R.Roc's RocNotaFiscalPagamentos accordingly.
func (o *RotinaContabil) SetRocNotaFiscalPagamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalPagamento) error {
	query := "update `nota_fiscal_pagamento` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocNotaFiscalPagamentos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocNotaFiscalPagamentos = nil
	}

	return o.AddRocNotaFiscalPagamentos(ctx, exec, insert, related...)
}

// RemoveRocNotaFiscalPagamentos relationships from objects passed in.
// Removes related items from R.RocNotaFiscalPagamentos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocNotaFiscalPagamentos(ctx context.Context, exec boil.ContextExecutor, related ...*NotaFiscalPagamento) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocNotaFiscalPagamentos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocNotaFiscalPagamentos)
			if ln > 1 && i < ln-1 {
				o.R.RocNotaFiscalPagamentos[i] = o.R.RocNotaFiscalPagamentos[ln-1]
			}
			o.R.RocNotaFiscalPagamentos = o.R.RocNotaFiscalPagamentos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocProcessos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocProcessos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocProcessos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Processo) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `processo` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, processoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.PRCID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocProcessos: related,
		}
	} else {
		o.R.RocProcessos = append(o.R.RocProcessos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &processoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocProcessos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocProcessos accordingly.
// Replaces o.R.RocProcessos with related.
// Sets related.R.Roc's RocProcessos accordingly.
func (o *RotinaContabil) SetRocProcessos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Processo) error {
	query := "update `processo` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocProcessos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocProcessos = nil
	}

	return o.AddRocProcessos(ctx, exec, insert, related...)
}

// RemoveRocProcessos relationships from objects passed in.
// Removes related items from R.RocProcessos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocProcessos(ctx context.Context, exec boil.ContextExecutor, related ...*Processo) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocProcessos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocProcessos)
			if ln > 1 && i < ln-1 {
				o.R.RocProcessos[i] = o.R.RocProcessos[ln-1]
			}
			o.R.RocProcessos = o.R.RocProcessos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocRenegociacaos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocRenegociacaos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocRenegociacaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Renegociacao) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `renegociacao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, renegociacaoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.RengID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocRenegociacaos: related,
		}
	} else {
		o.R.RocRenegociacaos = append(o.R.RocRenegociacaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &renegociacaoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocRenegociacaos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocRenegociacaos accordingly.
// Replaces o.R.RocRenegociacaos with related.
// Sets related.R.Roc's RocRenegociacaos accordingly.
func (o *RotinaContabil) SetRocRenegociacaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Renegociacao) error {
	query := "update `renegociacao` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocRenegociacaos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocRenegociacaos = nil
	}

	return o.AddRocRenegociacaos(ctx, exec, insert, related...)
}

// RemoveRocRenegociacaos relationships from objects passed in.
// Removes related items from R.RocRenegociacaos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocRenegociacaos(ctx context.Context, exec boil.ContextExecutor, related ...*Renegociacao) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocRenegociacaos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocRenegociacaos)
			if ln > 1 && i < ln-1 {
				o.R.RocRenegociacaos[i] = o.R.RocRenegociacaos[ln-1]
			}
			o.R.RocRenegociacaos = o.R.RocRenegociacaos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocRenegociacaoDocumentos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocRenegociacaoDocumentos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocRenegociacaoDocumentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RenegociacaoDocumento) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `renegociacao_documentos` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, renegociacaoDocumentoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.RengID, rel.RemgdocItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocRenegociacaoDocumentos: related,
		}
	} else {
		o.R.RocRenegociacaoDocumentos = append(o.R.RocRenegociacaoDocumentos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &renegociacaoDocumentoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocRenegociacaoDocumentos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocRenegociacaoDocumentos accordingly.
// Replaces o.R.RocRenegociacaoDocumentos with related.
// Sets related.R.Roc's RocRenegociacaoDocumentos accordingly.
func (o *RotinaContabil) SetRocRenegociacaoDocumentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RenegociacaoDocumento) error {
	query := "update `renegociacao_documentos` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocRenegociacaoDocumentos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocRenegociacaoDocumentos = nil
	}

	return o.AddRocRenegociacaoDocumentos(ctx, exec, insert, related...)
}

// RemoveRocRenegociacaoDocumentos relationships from objects passed in.
// Removes related items from R.RocRenegociacaoDocumentos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocRenegociacaoDocumentos(ctx context.Context, exec boil.ContextExecutor, related ...*RenegociacaoDocumento) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocRenegociacaoDocumentos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocRenegociacaoDocumentos)
			if ln > 1 && i < ln-1 {
				o.R.RocRenegociacaoDocumentos[i] = o.R.RocRenegociacaoDocumentos[ln-1]
			}
			o.R.RocRenegociacaoDocumentos = o.R.RocRenegociacaoDocumentos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocRequisicaos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocRequisicaos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocRequisicaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Requisicao) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `requisicao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, requisicaoPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.ReqID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocRequisicaos: related,
		}
	} else {
		o.R.RocRequisicaos = append(o.R.RocRequisicaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &requisicaoR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocRequisicaos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocRequisicaos accordingly.
// Replaces o.R.RocRequisicaos with related.
// Sets related.R.Roc's RocRequisicaos accordingly.
func (o *RotinaContabil) SetRocRequisicaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Requisicao) error {
	query := "update `requisicao` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocRequisicaos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocRequisicaos = nil
	}

	return o.AddRocRequisicaos(ctx, exec, insert, related...)
}

// RemoveRocRequisicaos relationships from objects passed in.
// Removes related items from R.RocRequisicaos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocRequisicaos(ctx context.Context, exec boil.ContextExecutor, related ...*Requisicao) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocRequisicaos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocRequisicaos)
			if ln > 1 && i < ln-1 {
				o.R.RocRequisicaos[i] = o.R.RocRequisicaos[ln-1]
			}
			o.R.RocRequisicaos = o.R.RocRequisicaos[:ln-1]
			break
		}
	}

	return nil
}

// AddRocRotinaContabilItens adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocRotinaContabilItens.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocRotinaContabilItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RotinaContabilIten) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.RocID = o.RocID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `rotina_contabil_itens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, rotinaContabilItenPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.RocID, rel.RociItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.RocID = o.RocID
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocRotinaContabilItens: related,
		}
	} else {
		o.R.RocRotinaContabilItens = append(o.R.RocRotinaContabilItens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &rotinaContabilItenR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// AddRocTituloBaixas adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocTituloBaixas.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocTituloBaixas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TituloBaixas) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `titulo_baixas` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.TitID, rel.TBXItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocTituloBaixas: related,
		}
	} else {
		o.R.RocTituloBaixas = append(o.R.RocTituloBaixas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &tituloBaixasR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocTituloBaixas removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocTituloBaixas accordingly.
// Replaces o.R.RocTituloBaixas with related.
// Sets related.R.Roc's RocTituloBaixas accordingly.
func (o *RotinaContabil) SetRocTituloBaixas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TituloBaixas) error {
	query := "update `titulo_baixas` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocTituloBaixas {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocTituloBaixas = nil
	}

	return o.AddRocTituloBaixas(ctx, exec, insert, related...)
}

// RemoveRocTituloBaixas relationships from objects passed in.
// Removes related items from R.RocTituloBaixas (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocTituloBaixas(ctx context.Context, exec boil.ContextExecutor, related ...*TituloBaixas) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocTituloBaixas {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocTituloBaixas)
			if ln > 1 && i < ln-1 {
				o.R.RocTituloBaixas[i] = o.R.RocTituloBaixas[ln-1]
			}
			o.R.RocTituloBaixas = o.R.RocTituloBaixas[:ln-1]
			break
		}
	}

	return nil
}

// AddRocTitulos adds the given related objects to the existing relationships
// of the rotina_contabil, optionally inserting them as new records.
// Appends related to o.R.RocTitulos.
// Sets related.R.Roc appropriately.
func (o *RotinaContabil) AddRocTitulos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Titulo) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RocID, o.RocID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `titulos` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
				strmangle.WhereClause("`", "`", 0, tituloPrimaryKeyColumns),
			)
			values := []interface{}{o.RocID, rel.TitID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RocID, o.RocID)
		}
	}

	if o.R == nil {
		o.R = &rotinaContabilR{
			RocTitulos: related,
		}
	} else {
		o.R.RocTitulos = append(o.R.RocTitulos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &tituloR{
				Roc: o,
			}
		} else {
			rel.R.Roc = o
		}
	}
	return nil
}

// SetRocTitulos removes all previously related items of the
// rotina_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Roc's RocTitulos accordingly.
// Replaces o.R.RocTitulos with related.
// Sets related.R.Roc's RocTitulos accordingly.
func (o *RotinaContabil) SetRocTitulos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Titulo) error {
	query := "update `titulos` set `roc_id` = null where `roc_id` = ?"
	values := []interface{}{o.RocID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RocTitulos {
			queries.SetScanner(&rel.RocID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Roc = nil
		}
		o.R.RocTitulos = nil
	}

	return o.AddRocTitulos(ctx, exec, insert, related...)
}

// RemoveRocTitulos relationships from objects passed in.
// Removes related items from R.RocTitulos (uses pointer comparison, removal does not keep order)
// Sets related.R.Roc.
func (o *RotinaContabil) RemoveRocTitulos(ctx context.Context, exec boil.ContextExecutor, related ...*Titulo) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RocID, nil)
		if rel.R != nil {
			rel.R.Roc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RocTitulos {
			if rel != ri {
				continue
			}

			ln := len(o.R.RocTitulos)
			if ln > 1 && i < ln-1 {
				o.R.RocTitulos[i] = o.R.RocTitulos[ln-1]
			}
			o.R.RocTitulos = o.R.RocTitulos[:ln-1]
			break
		}
	}

	return nil
}

// RotinaContabils retrieves all the records using an executor.
func RotinaContabils(mods ...qm.QueryMod) rotinaContabilQuery {
	mods = append(mods, qm.From("`rotina_contabil`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`rotina_contabil`.*"})
	}

	return rotinaContabilQuery{q}
}

// FindRotinaContabil retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRotinaContabil(ctx context.Context, exec boil.ContextExecutor, rocID int, selectCols ...string) (*RotinaContabil, error) {
	rotinaContabilObj := &RotinaContabil{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `rotina_contabil` where `roc_id`=?", sel,
	)

	q := queries.Raw(query, rocID)

	err := q.Bind(ctx, exec, rotinaContabilObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from rotina_contabil")
	}

	if err = rotinaContabilObj.doAfterSelectHooks(ctx, exec); err != nil {
		return rotinaContabilObj, err
	}

	return rotinaContabilObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RotinaContabil) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no rotina_contabil provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(rotinaContabilColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	rotinaContabilInsertCacheMut.RLock()
	cache, cached := rotinaContabilInsertCache[key]
	rotinaContabilInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			rotinaContabilAllColumns,
			rotinaContabilColumnsWithDefault,
			rotinaContabilColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(rotinaContabilType, rotinaContabilMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(rotinaContabilType, rotinaContabilMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `rotina_contabil` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `rotina_contabil` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `rotina_contabil` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, rotinaContabilPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into rotina_contabil")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.RocID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for rotina_contabil")
	}

CacheNoHooks:
	if !cached {
		rotinaContabilInsertCacheMut.Lock()
		rotinaContabilInsertCache[key] = cache
		rotinaContabilInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RotinaContabil.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RotinaContabil) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	rotinaContabilUpdateCacheMut.RLock()
	cache, cached := rotinaContabilUpdateCache[key]
	rotinaContabilUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			rotinaContabilAllColumns,
			rotinaContabilPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update rotina_contabil, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `rotina_contabil` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, rotinaContabilPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(rotinaContabilType, rotinaContabilMapping, append(wl, rotinaContabilPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update rotina_contabil row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for rotina_contabil")
	}

	if !cached {
		rotinaContabilUpdateCacheMut.Lock()
		rotinaContabilUpdateCache[key] = cache
		rotinaContabilUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q rotinaContabilQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for rotina_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for rotina_contabil")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RotinaContabilSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), rotinaContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `rotina_contabil` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, rotinaContabilPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in rotinaContabil slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all rotinaContabil")
	}
	return rowsAff, nil
}

var mySQLRotinaContabilUniqueColumns = []string{
	"roc_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RotinaContabil) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no rotina_contabil provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(rotinaContabilColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRotinaContabilUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	rotinaContabilUpsertCacheMut.RLock()
	cache, cached := rotinaContabilUpsertCache[key]
	rotinaContabilUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			rotinaContabilAllColumns,
			rotinaContabilColumnsWithDefault,
			rotinaContabilColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			rotinaContabilAllColumns,
			rotinaContabilPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert rotina_contabil, could not build update column list")
		}

		ret := strmangle.SetComplement(rotinaContabilAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`rotina_contabil`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `rotina_contabil` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(rotinaContabilType, rotinaContabilMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(rotinaContabilType, rotinaContabilMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for rotina_contabil")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(rotinaContabilType, rotinaContabilMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for rotina_contabil")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for rotina_contabil")
	}

CacheNoHooks:
	if !cached {
		rotinaContabilUpsertCacheMut.Lock()
		rotinaContabilUpsertCache[key] = cache
		rotinaContabilUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RotinaContabil record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RotinaContabil) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RotinaContabil provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), rotinaContabilPrimaryKeyMapping)
	sql := "DELETE FROM `rotina_contabil` WHERE `roc_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from rotina_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for rotina_contabil")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q rotinaContabilQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no rotinaContabilQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from rotina_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for rotina_contabil")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RotinaContabilSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(rotinaContabilBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), rotinaContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `rotina_contabil` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, rotinaContabilPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from rotinaContabil slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for rotina_contabil")
	}

	if len(rotinaContabilAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RotinaContabil) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRotinaContabil(ctx, exec, o.RocID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RotinaContabilSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RotinaContabilSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), rotinaContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `rotina_contabil`.* FROM `rotina_contabil` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, rotinaContabilPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RotinaContabilSlice")
	}

	*o = slice

	return nil
}

// RotinaContabilExists checks if the RotinaContabil row exists.
func RotinaContabilExists(ctx context.Context, exec boil.ContextExecutor, rocID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `rotina_contabil` where `roc_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, rocID)
	}
	row := exec.QueryRowContext(ctx, sql, rocID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if rotina_contabil exists")
	}

	return exists, nil
}

// Exists checks if the RotinaContabil row exists.
func (o *RotinaContabil) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RotinaContabilExists(ctx, exec, o.RocID)
}
