// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// FrenteCaixa is an object representing the database table.
type FrenteCaixa struct {
	FRTCXID        int      `boil:"frtcx_id" json:"frtcx_id" toml:"frtcx_id" yaml:"frtcx_id"`
	FRTCXDescricao string   `boil:"frtcx_descricao" json:"frtcx_descricao" toml:"frtcx_descricao" yaml:"frtcx_descricao"`
	FRTCXSituacao  null.Int `boil:"frtcx_situacao" json:"frtcx_situacao,omitempty" toml:"frtcx_situacao" yaml:"frtcx_situacao,omitempty"`

	R *frenteCaixaR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L frenteCaixaL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FrenteCaixaColumns = struct {
	FRTCXID        string
	FRTCXDescricao string
	FRTCXSituacao  string
}{
	FRTCXID:        "frtcx_id",
	FRTCXDescricao: "frtcx_descricao",
	FRTCXSituacao:  "frtcx_situacao",
}

var FrenteCaixaTableColumns = struct {
	FRTCXID        string
	FRTCXDescricao string
	FRTCXSituacao  string
}{
	FRTCXID:        "frente_caixa.frtcx_id",
	FRTCXDescricao: "frente_caixa.frtcx_descricao",
	FRTCXSituacao:  "frente_caixa.frtcx_situacao",
}

// Generated where

var FrenteCaixaWhere = struct {
	FRTCXID        whereHelperint
	FRTCXDescricao whereHelperstring
	FRTCXSituacao  whereHelpernull_Int
}{
	FRTCXID:        whereHelperint{field: "`frente_caixa`.`frtcx_id`"},
	FRTCXDescricao: whereHelperstring{field: "`frente_caixa`.`frtcx_descricao`"},
	FRTCXSituacao:  whereHelpernull_Int{field: "`frente_caixa`.`frtcx_situacao`"},
}

// FrenteCaixaRels is where relationship names are stored.
var FrenteCaixaRels = struct {
	FRTCXCaixaFinanceiroTotais string
	FRTCXLancamentoFinanceiros string
}{
	FRTCXCaixaFinanceiroTotais: "FRTCXCaixaFinanceiroTotais",
	FRTCXLancamentoFinanceiros: "FRTCXLancamentoFinanceiros",
}

// frenteCaixaR is where relationships are stored.
type frenteCaixaR struct {
	FRTCXCaixaFinanceiroTotais CaixaFinanceiroTotaisSlice `boil:"FRTCXCaixaFinanceiroTotais" json:"FRTCXCaixaFinanceiroTotais" toml:"FRTCXCaixaFinanceiroTotais" yaml:"FRTCXCaixaFinanceiroTotais"`
	FRTCXLancamentoFinanceiros LancamentoFinanceiroSlice  `boil:"FRTCXLancamentoFinanceiros" json:"FRTCXLancamentoFinanceiros" toml:"FRTCXLancamentoFinanceiros" yaml:"FRTCXLancamentoFinanceiros"`
}

// NewStruct creates a new relationship struct
func (*frenteCaixaR) NewStruct() *frenteCaixaR {
	return &frenteCaixaR{}
}

func (r *frenteCaixaR) GetFRTCXCaixaFinanceiroTotais() CaixaFinanceiroTotaisSlice {
	if r == nil {
		return nil
	}
	return r.FRTCXCaixaFinanceiroTotais
}

func (r *frenteCaixaR) GetFRTCXLancamentoFinanceiros() LancamentoFinanceiroSlice {
	if r == nil {
		return nil
	}
	return r.FRTCXLancamentoFinanceiros
}

// frenteCaixaL is where Load methods for each relationship are stored.
type frenteCaixaL struct{}

var (
	frenteCaixaAllColumns            = []string{"frtcx_id", "frtcx_descricao", "frtcx_situacao"}
	frenteCaixaColumnsWithoutDefault = []string{"frtcx_id", "frtcx_descricao", "frtcx_situacao"}
	frenteCaixaColumnsWithDefault    = []string{}
	frenteCaixaPrimaryKeyColumns     = []string{"frtcx_id"}
	frenteCaixaGeneratedColumns      = []string{}
)

type (
	// FrenteCaixaSlice is an alias for a slice of pointers to FrenteCaixa.
	// This should almost always be used instead of []FrenteCaixa.
	FrenteCaixaSlice []*FrenteCaixa
	// FrenteCaixaHook is the signature for custom FrenteCaixa hook methods
	FrenteCaixaHook func(context.Context, boil.ContextExecutor, *FrenteCaixa) error

	frenteCaixaQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	frenteCaixaType                 = reflect.TypeOf(&FrenteCaixa{})
	frenteCaixaMapping              = queries.MakeStructMapping(frenteCaixaType)
	frenteCaixaPrimaryKeyMapping, _ = queries.BindMapping(frenteCaixaType, frenteCaixaMapping, frenteCaixaPrimaryKeyColumns)
	frenteCaixaInsertCacheMut       sync.RWMutex
	frenteCaixaInsertCache          = make(map[string]insertCache)
	frenteCaixaUpdateCacheMut       sync.RWMutex
	frenteCaixaUpdateCache          = make(map[string]updateCache)
	frenteCaixaUpsertCacheMut       sync.RWMutex
	frenteCaixaUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var frenteCaixaAfterSelectMu sync.Mutex
var frenteCaixaAfterSelectHooks []FrenteCaixaHook

var frenteCaixaBeforeInsertMu sync.Mutex
var frenteCaixaBeforeInsertHooks []FrenteCaixaHook
var frenteCaixaAfterInsertMu sync.Mutex
var frenteCaixaAfterInsertHooks []FrenteCaixaHook

var frenteCaixaBeforeUpdateMu sync.Mutex
var frenteCaixaBeforeUpdateHooks []FrenteCaixaHook
var frenteCaixaAfterUpdateMu sync.Mutex
var frenteCaixaAfterUpdateHooks []FrenteCaixaHook

var frenteCaixaBeforeDeleteMu sync.Mutex
var frenteCaixaBeforeDeleteHooks []FrenteCaixaHook
var frenteCaixaAfterDeleteMu sync.Mutex
var frenteCaixaAfterDeleteHooks []FrenteCaixaHook

var frenteCaixaBeforeUpsertMu sync.Mutex
var frenteCaixaBeforeUpsertHooks []FrenteCaixaHook
var frenteCaixaAfterUpsertMu sync.Mutex
var frenteCaixaAfterUpsertHooks []FrenteCaixaHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *FrenteCaixa) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *FrenteCaixa) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *FrenteCaixa) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *FrenteCaixa) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *FrenteCaixa) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *FrenteCaixa) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *FrenteCaixa) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *FrenteCaixa) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *FrenteCaixa) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range frenteCaixaAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddFrenteCaixaHook registers your hook function for all future operations.
func AddFrenteCaixaHook(hookPoint boil.HookPoint, frenteCaixaHook FrenteCaixaHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		frenteCaixaAfterSelectMu.Lock()
		frenteCaixaAfterSelectHooks = append(frenteCaixaAfterSelectHooks, frenteCaixaHook)
		frenteCaixaAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		frenteCaixaBeforeInsertMu.Lock()
		frenteCaixaBeforeInsertHooks = append(frenteCaixaBeforeInsertHooks, frenteCaixaHook)
		frenteCaixaBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		frenteCaixaAfterInsertMu.Lock()
		frenteCaixaAfterInsertHooks = append(frenteCaixaAfterInsertHooks, frenteCaixaHook)
		frenteCaixaAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		frenteCaixaBeforeUpdateMu.Lock()
		frenteCaixaBeforeUpdateHooks = append(frenteCaixaBeforeUpdateHooks, frenteCaixaHook)
		frenteCaixaBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		frenteCaixaAfterUpdateMu.Lock()
		frenteCaixaAfterUpdateHooks = append(frenteCaixaAfterUpdateHooks, frenteCaixaHook)
		frenteCaixaAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		frenteCaixaBeforeDeleteMu.Lock()
		frenteCaixaBeforeDeleteHooks = append(frenteCaixaBeforeDeleteHooks, frenteCaixaHook)
		frenteCaixaBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		frenteCaixaAfterDeleteMu.Lock()
		frenteCaixaAfterDeleteHooks = append(frenteCaixaAfterDeleteHooks, frenteCaixaHook)
		frenteCaixaAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		frenteCaixaBeforeUpsertMu.Lock()
		frenteCaixaBeforeUpsertHooks = append(frenteCaixaBeforeUpsertHooks, frenteCaixaHook)
		frenteCaixaBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		frenteCaixaAfterUpsertMu.Lock()
		frenteCaixaAfterUpsertHooks = append(frenteCaixaAfterUpsertHooks, frenteCaixaHook)
		frenteCaixaAfterUpsertMu.Unlock()
	}
}

// One returns a single frenteCaixa record from the query.
func (q frenteCaixaQuery) One(ctx context.Context, exec boil.ContextExecutor) (*FrenteCaixa, error) {
	o := &FrenteCaixa{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for frente_caixa")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all FrenteCaixa records from the query.
func (q frenteCaixaQuery) All(ctx context.Context, exec boil.ContextExecutor) (FrenteCaixaSlice, error) {
	var o []*FrenteCaixa

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to FrenteCaixa slice")
	}

	if len(frenteCaixaAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all FrenteCaixa records in the query.
func (q frenteCaixaQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count frente_caixa rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q frenteCaixaQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if frente_caixa exists")
	}

	return count > 0, nil
}

// FRTCXCaixaFinanceiroTotais retrieves all the caixa_financeiro_totais's CaixaFinanceiroTotais with an executor via frtcx_id column.
func (o *FrenteCaixa) FRTCXCaixaFinanceiroTotais(mods ...qm.QueryMod) caixaFinanceiroTotaisQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`caixa_financeiro_totais`.`frtcx_id`=?", o.FRTCXID),
	)

	return CaixaFinanceiroTotais(queryMods...)
}

// FRTCXLancamentoFinanceiros retrieves all the lancamento_financeiro's LancamentoFinanceiros with an executor via frtcx_id column.
func (o *FrenteCaixa) FRTCXLancamentoFinanceiros(mods ...qm.QueryMod) lancamentoFinanceiroQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`lancamento_financeiro`.`frtcx_id`=?", o.FRTCXID),
	)

	return LancamentoFinanceiros(queryMods...)
}

// LoadFRTCXCaixaFinanceiroTotais allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (frenteCaixaL) LoadFRTCXCaixaFinanceiroTotais(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFrenteCaixa interface{}, mods queries.Applicator) error {
	var slice []*FrenteCaixa
	var object *FrenteCaixa

	if singular {
		var ok bool
		object, ok = maybeFrenteCaixa.(*FrenteCaixa)
		if !ok {
			object = new(FrenteCaixa)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFrenteCaixa)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFrenteCaixa))
			}
		}
	} else {
		s, ok := maybeFrenteCaixa.(*[]*FrenteCaixa)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFrenteCaixa)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFrenteCaixa))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &frenteCaixaR{}
		}
		args[object.FRTCXID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &frenteCaixaR{}
			}
			args[obj.FRTCXID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`caixa_financeiro_totais`),
		qm.WhereIn(`caixa_financeiro_totais.frtcx_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load caixa_financeiro_totais")
	}

	var resultSlice []*CaixaFinanceiroTotais
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice caixa_financeiro_totais")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on caixa_financeiro_totais")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for caixa_financeiro_totais")
	}

	if len(caixaFinanceiroTotaisAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.FRTCXCaixaFinanceiroTotais = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &caixaFinanceiroTotaisR{}
			}
			foreign.R.FRTCX = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.FRTCXID == foreign.FRTCXID {
				local.R.FRTCXCaixaFinanceiroTotais = append(local.R.FRTCXCaixaFinanceiroTotais, foreign)
				if foreign.R == nil {
					foreign.R = &caixaFinanceiroTotaisR{}
				}
				foreign.R.FRTCX = local
				break
			}
		}
	}

	return nil
}

// LoadFRTCXLancamentoFinanceiros allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (frenteCaixaL) LoadFRTCXLancamentoFinanceiros(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFrenteCaixa interface{}, mods queries.Applicator) error {
	var slice []*FrenteCaixa
	var object *FrenteCaixa

	if singular {
		var ok bool
		object, ok = maybeFrenteCaixa.(*FrenteCaixa)
		if !ok {
			object = new(FrenteCaixa)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFrenteCaixa)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFrenteCaixa))
			}
		}
	} else {
		s, ok := maybeFrenteCaixa.(*[]*FrenteCaixa)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFrenteCaixa)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFrenteCaixa))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &frenteCaixaR{}
		}
		args[object.FRTCXID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &frenteCaixaR{}
			}
			args[obj.FRTCXID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`lancamento_financeiro`),
		qm.WhereIn(`lancamento_financeiro.frtcx_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load lancamento_financeiro")
	}

	var resultSlice []*LancamentoFinanceiro
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice lancamento_financeiro")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on lancamento_financeiro")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for lancamento_financeiro")
	}

	if len(lancamentoFinanceiroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.FRTCXLancamentoFinanceiros = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &lancamentoFinanceiroR{}
			}
			foreign.R.FRTCX = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.FRTCXID, foreign.FRTCXID) {
				local.R.FRTCXLancamentoFinanceiros = append(local.R.FRTCXLancamentoFinanceiros, foreign)
				if foreign.R == nil {
					foreign.R = &lancamentoFinanceiroR{}
				}
				foreign.R.FRTCX = local
				break
			}
		}
	}

	return nil
}

// AddFRTCXCaixaFinanceiroTotais adds the given related objects to the existing relationships
// of the frente_caixa, optionally inserting them as new records.
// Appends related to o.R.FRTCXCaixaFinanceiroTotais.
// Sets related.R.FRTCX appropriately.
func (o *FrenteCaixa) AddFRTCXCaixaFinanceiroTotais(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CaixaFinanceiroTotais) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.FRTCXID = o.FRTCXID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `caixa_financeiro_totais` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"frtcx_id"}),
				strmangle.WhereClause("`", "`", 0, caixaFinanceiroTotaisPrimaryKeyColumns),
			)
			values := []interface{}{o.FRTCXID, rel.CxfinID, rel.FRTCXID, rel.CxfintItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.FRTCXID = o.FRTCXID
		}
	}

	if o.R == nil {
		o.R = &frenteCaixaR{
			FRTCXCaixaFinanceiroTotais: related,
		}
	} else {
		o.R.FRTCXCaixaFinanceiroTotais = append(o.R.FRTCXCaixaFinanceiroTotais, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &caixaFinanceiroTotaisR{
				FRTCX: o,
			}
		} else {
			rel.R.FRTCX = o
		}
	}
	return nil
}

// AddFRTCXLancamentoFinanceiros adds the given related objects to the existing relationships
// of the frente_caixa, optionally inserting them as new records.
// Appends related to o.R.FRTCXLancamentoFinanceiros.
// Sets related.R.FRTCX appropriately.
func (o *FrenteCaixa) AddFRTCXLancamentoFinanceiros(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoFinanceiro) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.FRTCXID, o.FRTCXID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `lancamento_financeiro` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"frtcx_id"}),
				strmangle.WhereClause("`", "`", 0, lancamentoFinanceiroPrimaryKeyColumns),
			)
			values := []interface{}{o.FRTCXID, rel.LfinID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.FRTCXID, o.FRTCXID)
		}
	}

	if o.R == nil {
		o.R = &frenteCaixaR{
			FRTCXLancamentoFinanceiros: related,
		}
	} else {
		o.R.FRTCXLancamentoFinanceiros = append(o.R.FRTCXLancamentoFinanceiros, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &lancamentoFinanceiroR{
				FRTCX: o,
			}
		} else {
			rel.R.FRTCX = o
		}
	}
	return nil
}

// SetFRTCXLancamentoFinanceiros removes all previously related items of the
// frente_caixa replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.FRTCX's FRTCXLancamentoFinanceiros accordingly.
// Replaces o.R.FRTCXLancamentoFinanceiros with related.
// Sets related.R.FRTCX's FRTCXLancamentoFinanceiros accordingly.
func (o *FrenteCaixa) SetFRTCXLancamentoFinanceiros(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoFinanceiro) error {
	query := "update `lancamento_financeiro` set `frtcx_id` = null where `frtcx_id` = ?"
	values := []interface{}{o.FRTCXID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.FRTCXLancamentoFinanceiros {
			queries.SetScanner(&rel.FRTCXID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.FRTCX = nil
		}
		o.R.FRTCXLancamentoFinanceiros = nil
	}

	return o.AddFRTCXLancamentoFinanceiros(ctx, exec, insert, related...)
}

// RemoveFRTCXLancamentoFinanceiros relationships from objects passed in.
// Removes related items from R.FRTCXLancamentoFinanceiros (uses pointer comparison, removal does not keep order)
// Sets related.R.FRTCX.
func (o *FrenteCaixa) RemoveFRTCXLancamentoFinanceiros(ctx context.Context, exec boil.ContextExecutor, related ...*LancamentoFinanceiro) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.FRTCXID, nil)
		if rel.R != nil {
			rel.R.FRTCX = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("frtcx_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.FRTCXLancamentoFinanceiros {
			if rel != ri {
				continue
			}

			ln := len(o.R.FRTCXLancamentoFinanceiros)
			if ln > 1 && i < ln-1 {
				o.R.FRTCXLancamentoFinanceiros[i] = o.R.FRTCXLancamentoFinanceiros[ln-1]
			}
			o.R.FRTCXLancamentoFinanceiros = o.R.FRTCXLancamentoFinanceiros[:ln-1]
			break
		}
	}

	return nil
}

// FrenteCaixas retrieves all the records using an executor.
func FrenteCaixas(mods ...qm.QueryMod) frenteCaixaQuery {
	mods = append(mods, qm.From("`frente_caixa`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`frente_caixa`.*"})
	}

	return frenteCaixaQuery{q}
}

// FindFrenteCaixa retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFrenteCaixa(ctx context.Context, exec boil.ContextExecutor, fRTCXID int, selectCols ...string) (*FrenteCaixa, error) {
	frenteCaixaObj := &FrenteCaixa{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `frente_caixa` where `frtcx_id`=?", sel,
	)

	q := queries.Raw(query, fRTCXID)

	err := q.Bind(ctx, exec, frenteCaixaObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from frente_caixa")
	}

	if err = frenteCaixaObj.doAfterSelectHooks(ctx, exec); err != nil {
		return frenteCaixaObj, err
	}

	return frenteCaixaObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *FrenteCaixa) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no frente_caixa provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(frenteCaixaColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	frenteCaixaInsertCacheMut.RLock()
	cache, cached := frenteCaixaInsertCache[key]
	frenteCaixaInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			frenteCaixaAllColumns,
			frenteCaixaColumnsWithDefault,
			frenteCaixaColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(frenteCaixaType, frenteCaixaMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(frenteCaixaType, frenteCaixaMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `frente_caixa` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `frente_caixa` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `frente_caixa` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, frenteCaixaPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into frente_caixa")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.FRTCXID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for frente_caixa")
	}

CacheNoHooks:
	if !cached {
		frenteCaixaInsertCacheMut.Lock()
		frenteCaixaInsertCache[key] = cache
		frenteCaixaInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the FrenteCaixa.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *FrenteCaixa) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	frenteCaixaUpdateCacheMut.RLock()
	cache, cached := frenteCaixaUpdateCache[key]
	frenteCaixaUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			frenteCaixaAllColumns,
			frenteCaixaPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update frente_caixa, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `frente_caixa` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, frenteCaixaPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(frenteCaixaType, frenteCaixaMapping, append(wl, frenteCaixaPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update frente_caixa row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for frente_caixa")
	}

	if !cached {
		frenteCaixaUpdateCacheMut.Lock()
		frenteCaixaUpdateCache[key] = cache
		frenteCaixaUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q frenteCaixaQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for frente_caixa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for frente_caixa")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FrenteCaixaSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), frenteCaixaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `frente_caixa` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, frenteCaixaPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in frenteCaixa slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all frenteCaixa")
	}
	return rowsAff, nil
}

var mySQLFrenteCaixaUniqueColumns = []string{
	"frtcx_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *FrenteCaixa) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no frente_caixa provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(frenteCaixaColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLFrenteCaixaUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	frenteCaixaUpsertCacheMut.RLock()
	cache, cached := frenteCaixaUpsertCache[key]
	frenteCaixaUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			frenteCaixaAllColumns,
			frenteCaixaColumnsWithDefault,
			frenteCaixaColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			frenteCaixaAllColumns,
			frenteCaixaPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert frente_caixa, could not build update column list")
		}

		ret := strmangle.SetComplement(frenteCaixaAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`frente_caixa`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `frente_caixa` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(frenteCaixaType, frenteCaixaMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(frenteCaixaType, frenteCaixaMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for frente_caixa")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(frenteCaixaType, frenteCaixaMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for frente_caixa")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for frente_caixa")
	}

CacheNoHooks:
	if !cached {
		frenteCaixaUpsertCacheMut.Lock()
		frenteCaixaUpsertCache[key] = cache
		frenteCaixaUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single FrenteCaixa record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *FrenteCaixa) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no FrenteCaixa provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), frenteCaixaPrimaryKeyMapping)
	sql := "DELETE FROM `frente_caixa` WHERE `frtcx_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from frente_caixa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for frente_caixa")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q frenteCaixaQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no frenteCaixaQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from frente_caixa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for frente_caixa")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FrenteCaixaSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(frenteCaixaBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), frenteCaixaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `frente_caixa` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, frenteCaixaPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from frenteCaixa slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for frente_caixa")
	}

	if len(frenteCaixaAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *FrenteCaixa) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindFrenteCaixa(ctx, exec, o.FRTCXID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FrenteCaixaSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FrenteCaixaSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), frenteCaixaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `frente_caixa`.* FROM `frente_caixa` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, frenteCaixaPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in FrenteCaixaSlice")
	}

	*o = slice

	return nil
}

// FrenteCaixaExists checks if the FrenteCaixa row exists.
func FrenteCaixaExists(ctx context.Context, exec boil.ContextExecutor, fRTCXID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `frente_caixa` where `frtcx_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, fRTCXID)
	}
	row := exec.QueryRowContext(ctx, sql, fRTCXID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if frente_caixa exists")
	}

	return exists, nil
}

// Exists checks if the FrenteCaixa row exists.
func (o *FrenteCaixa) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return FrenteCaixaExists(ctx, exec, o.FRTCXID)
}
