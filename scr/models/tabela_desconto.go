// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// TabelaDesconto is an object representing the database table.
type TabelaDesconto struct {
	TdescID          int               `boil:"tdesc_id" json:"tdesc_id" toml:"tdesc_id" yaml:"tdesc_id"`
	TdescDescricao   string            `boil:"tdesc_descricao" json:"tdesc_descricao" toml:"tdesc_descricao" yaml:"tdesc_descricao"`
	TdescPercproduto types.Decimal     `boil:"tdesc_percproduto" json:"tdesc_percproduto" toml:"tdesc_percproduto" yaml:"tdesc_percproduto"`
	TdescPercservico types.NullDecimal `boil:"tdesc_percservico" json:"tdesc_percservico,omitempty" toml:"tdesc_percservico" yaml:"tdesc_percservico,omitempty"`

	R *tabelaDescontoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tabelaDescontoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TabelaDescontoColumns = struct {
	TdescID          string
	TdescDescricao   string
	TdescPercproduto string
	TdescPercservico string
}{
	TdescID:          "tdesc_id",
	TdescDescricao:   "tdesc_descricao",
	TdescPercproduto: "tdesc_percproduto",
	TdescPercservico: "tdesc_percservico",
}

var TabelaDescontoTableColumns = struct {
	TdescID          string
	TdescDescricao   string
	TdescPercproduto string
	TdescPercservico string
}{
	TdescID:          "tabela_desconto.tdesc_id",
	TdescDescricao:   "tabela_desconto.tdesc_descricao",
	TdescPercproduto: "tabela_desconto.tdesc_percproduto",
	TdescPercservico: "tabela_desconto.tdesc_percservico",
}

// Generated where

var TabelaDescontoWhere = struct {
	TdescID          whereHelperint
	TdescDescricao   whereHelperstring
	TdescPercproduto whereHelpertypes_Decimal
	TdescPercservico whereHelpertypes_NullDecimal
}{
	TdescID:          whereHelperint{field: "`tabela_desconto`.`tdesc_id`"},
	TdescDescricao:   whereHelperstring{field: "`tabela_desconto`.`tdesc_descricao`"},
	TdescPercproduto: whereHelpertypes_Decimal{field: "`tabela_desconto`.`tdesc_percproduto`"},
	TdescPercservico: whereHelpertypes_NullDecimal{field: "`tabela_desconto`.`tdesc_percservico`"},
}

// TabelaDescontoRels is where relationship names are stored.
var TabelaDescontoRels = struct {
	TdescEntidades string
}{
	TdescEntidades: "TdescEntidades",
}

// tabelaDescontoR is where relationships are stored.
type tabelaDescontoR struct {
	TdescEntidades EntidadeSlice `boil:"TdescEntidades" json:"TdescEntidades" toml:"TdescEntidades" yaml:"TdescEntidades"`
}

// NewStruct creates a new relationship struct
func (*tabelaDescontoR) NewStruct() *tabelaDescontoR {
	return &tabelaDescontoR{}
}

func (r *tabelaDescontoR) GetTdescEntidades() EntidadeSlice {
	if r == nil {
		return nil
	}
	return r.TdescEntidades
}

// tabelaDescontoL is where Load methods for each relationship are stored.
type tabelaDescontoL struct{}

var (
	tabelaDescontoAllColumns            = []string{"tdesc_id", "tdesc_descricao", "tdesc_percproduto", "tdesc_percservico"}
	tabelaDescontoColumnsWithoutDefault = []string{"tdesc_id", "tdesc_descricao"}
	tabelaDescontoColumnsWithDefault    = []string{"tdesc_percproduto", "tdesc_percservico"}
	tabelaDescontoPrimaryKeyColumns     = []string{"tdesc_id"}
	tabelaDescontoGeneratedColumns      = []string{}
)

type (
	// TabelaDescontoSlice is an alias for a slice of pointers to TabelaDesconto.
	// This should almost always be used instead of []TabelaDesconto.
	TabelaDescontoSlice []*TabelaDesconto
	// TabelaDescontoHook is the signature for custom TabelaDesconto hook methods
	TabelaDescontoHook func(context.Context, boil.ContextExecutor, *TabelaDesconto) error

	tabelaDescontoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tabelaDescontoType                 = reflect.TypeOf(&TabelaDesconto{})
	tabelaDescontoMapping              = queries.MakeStructMapping(tabelaDescontoType)
	tabelaDescontoPrimaryKeyMapping, _ = queries.BindMapping(tabelaDescontoType, tabelaDescontoMapping, tabelaDescontoPrimaryKeyColumns)
	tabelaDescontoInsertCacheMut       sync.RWMutex
	tabelaDescontoInsertCache          = make(map[string]insertCache)
	tabelaDescontoUpdateCacheMut       sync.RWMutex
	tabelaDescontoUpdateCache          = make(map[string]updateCache)
	tabelaDescontoUpsertCacheMut       sync.RWMutex
	tabelaDescontoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tabelaDescontoAfterSelectMu sync.Mutex
var tabelaDescontoAfterSelectHooks []TabelaDescontoHook

var tabelaDescontoBeforeInsertMu sync.Mutex
var tabelaDescontoBeforeInsertHooks []TabelaDescontoHook
var tabelaDescontoAfterInsertMu sync.Mutex
var tabelaDescontoAfterInsertHooks []TabelaDescontoHook

var tabelaDescontoBeforeUpdateMu sync.Mutex
var tabelaDescontoBeforeUpdateHooks []TabelaDescontoHook
var tabelaDescontoAfterUpdateMu sync.Mutex
var tabelaDescontoAfterUpdateHooks []TabelaDescontoHook

var tabelaDescontoBeforeDeleteMu sync.Mutex
var tabelaDescontoBeforeDeleteHooks []TabelaDescontoHook
var tabelaDescontoAfterDeleteMu sync.Mutex
var tabelaDescontoAfterDeleteHooks []TabelaDescontoHook

var tabelaDescontoBeforeUpsertMu sync.Mutex
var tabelaDescontoBeforeUpsertHooks []TabelaDescontoHook
var tabelaDescontoAfterUpsertMu sync.Mutex
var tabelaDescontoAfterUpsertHooks []TabelaDescontoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TabelaDesconto) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TabelaDesconto) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TabelaDesconto) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TabelaDesconto) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TabelaDesconto) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TabelaDesconto) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TabelaDesconto) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TabelaDesconto) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TabelaDesconto) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaDescontoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTabelaDescontoHook registers your hook function for all future operations.
func AddTabelaDescontoHook(hookPoint boil.HookPoint, tabelaDescontoHook TabelaDescontoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tabelaDescontoAfterSelectMu.Lock()
		tabelaDescontoAfterSelectHooks = append(tabelaDescontoAfterSelectHooks, tabelaDescontoHook)
		tabelaDescontoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tabelaDescontoBeforeInsertMu.Lock()
		tabelaDescontoBeforeInsertHooks = append(tabelaDescontoBeforeInsertHooks, tabelaDescontoHook)
		tabelaDescontoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tabelaDescontoAfterInsertMu.Lock()
		tabelaDescontoAfterInsertHooks = append(tabelaDescontoAfterInsertHooks, tabelaDescontoHook)
		tabelaDescontoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tabelaDescontoBeforeUpdateMu.Lock()
		tabelaDescontoBeforeUpdateHooks = append(tabelaDescontoBeforeUpdateHooks, tabelaDescontoHook)
		tabelaDescontoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tabelaDescontoAfterUpdateMu.Lock()
		tabelaDescontoAfterUpdateHooks = append(tabelaDescontoAfterUpdateHooks, tabelaDescontoHook)
		tabelaDescontoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tabelaDescontoBeforeDeleteMu.Lock()
		tabelaDescontoBeforeDeleteHooks = append(tabelaDescontoBeforeDeleteHooks, tabelaDescontoHook)
		tabelaDescontoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tabelaDescontoAfterDeleteMu.Lock()
		tabelaDescontoAfterDeleteHooks = append(tabelaDescontoAfterDeleteHooks, tabelaDescontoHook)
		tabelaDescontoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tabelaDescontoBeforeUpsertMu.Lock()
		tabelaDescontoBeforeUpsertHooks = append(tabelaDescontoBeforeUpsertHooks, tabelaDescontoHook)
		tabelaDescontoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tabelaDescontoAfterUpsertMu.Lock()
		tabelaDescontoAfterUpsertHooks = append(tabelaDescontoAfterUpsertHooks, tabelaDescontoHook)
		tabelaDescontoAfterUpsertMu.Unlock()
	}
}

// One returns a single tabelaDesconto record from the query.
func (q tabelaDescontoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TabelaDesconto, error) {
	o := &TabelaDesconto{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for tabela_desconto")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TabelaDesconto records from the query.
func (q tabelaDescontoQuery) All(ctx context.Context, exec boil.ContextExecutor) (TabelaDescontoSlice, error) {
	var o []*TabelaDesconto

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TabelaDesconto slice")
	}

	if len(tabelaDescontoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TabelaDesconto records in the query.
func (q tabelaDescontoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count tabela_desconto rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tabelaDescontoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if tabela_desconto exists")
	}

	return count > 0, nil
}

// TdescEntidades retrieves all the entidade's Entidades with an executor via tdesc_id column.
func (o *TabelaDesconto) TdescEntidades(mods ...qm.QueryMod) entidadeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`entidade`.`tdesc_id`=?", o.TdescID),
	)

	return Entidades(queryMods...)
}

// LoadTdescEntidades allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (tabelaDescontoL) LoadTdescEntidades(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTabelaDesconto interface{}, mods queries.Applicator) error {
	var slice []*TabelaDesconto
	var object *TabelaDesconto

	if singular {
		var ok bool
		object, ok = maybeTabelaDesconto.(*TabelaDesconto)
		if !ok {
			object = new(TabelaDesconto)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTabelaDesconto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTabelaDesconto))
			}
		}
	} else {
		s, ok := maybeTabelaDesconto.(*[]*TabelaDesconto)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTabelaDesconto)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTabelaDesconto))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tabelaDescontoR{}
		}
		args[object.TdescID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tabelaDescontoR{}
			}
			args[obj.TdescID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`entidade`),
		qm.WhereIn(`entidade.tdesc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load entidade")
	}

	var resultSlice []*Entidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for entidade")
	}

	if len(entidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TdescEntidades = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &entidadeR{}
			}
			foreign.R.Tdesc = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.TdescID, foreign.TdescID) {
				local.R.TdescEntidades = append(local.R.TdescEntidades, foreign)
				if foreign.R == nil {
					foreign.R = &entidadeR{}
				}
				foreign.R.Tdesc = local
				break
			}
		}
	}

	return nil
}

// AddTdescEntidades adds the given related objects to the existing relationships
// of the tabela_desconto, optionally inserting them as new records.
// Appends related to o.R.TdescEntidades.
// Sets related.R.Tdesc appropriately.
func (o *TabelaDesconto) AddTdescEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Entidade) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.TdescID, o.TdescID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `entidade` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"tdesc_id"}),
				strmangle.WhereClause("`", "`", 0, entidadePrimaryKeyColumns),
			)
			values := []interface{}{o.TdescID, rel.EntID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.TdescID, o.TdescID)
		}
	}

	if o.R == nil {
		o.R = &tabelaDescontoR{
			TdescEntidades: related,
		}
	} else {
		o.R.TdescEntidades = append(o.R.TdescEntidades, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &entidadeR{
				Tdesc: o,
			}
		} else {
			rel.R.Tdesc = o
		}
	}
	return nil
}

// SetTdescEntidades removes all previously related items of the
// tabela_desconto replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Tdesc's TdescEntidades accordingly.
// Replaces o.R.TdescEntidades with related.
// Sets related.R.Tdesc's TdescEntidades accordingly.
func (o *TabelaDesconto) SetTdescEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Entidade) error {
	query := "update `entidade` set `tdesc_id` = null where `tdesc_id` = ?"
	values := []interface{}{o.TdescID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.TdescEntidades {
			queries.SetScanner(&rel.TdescID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Tdesc = nil
		}
		o.R.TdescEntidades = nil
	}

	return o.AddTdescEntidades(ctx, exec, insert, related...)
}

// RemoveTdescEntidades relationships from objects passed in.
// Removes related items from R.TdescEntidades (uses pointer comparison, removal does not keep order)
// Sets related.R.Tdesc.
func (o *TabelaDesconto) RemoveTdescEntidades(ctx context.Context, exec boil.ContextExecutor, related ...*Entidade) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.TdescID, nil)
		if rel.R != nil {
			rel.R.Tdesc = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("tdesc_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.TdescEntidades {
			if rel != ri {
				continue
			}

			ln := len(o.R.TdescEntidades)
			if ln > 1 && i < ln-1 {
				o.R.TdescEntidades[i] = o.R.TdescEntidades[ln-1]
			}
			o.R.TdescEntidades = o.R.TdescEntidades[:ln-1]
			break
		}
	}

	return nil
}

// TabelaDescontos retrieves all the records using an executor.
func TabelaDescontos(mods ...qm.QueryMod) tabelaDescontoQuery {
	mods = append(mods, qm.From("`tabela_desconto`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`tabela_desconto`.*"})
	}

	return tabelaDescontoQuery{q}
}

// FindTabelaDesconto retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTabelaDesconto(ctx context.Context, exec boil.ContextExecutor, tdescID int, selectCols ...string) (*TabelaDesconto, error) {
	tabelaDescontoObj := &TabelaDesconto{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `tabela_desconto` where `tdesc_id`=?", sel,
	)

	q := queries.Raw(query, tdescID)

	err := q.Bind(ctx, exec, tabelaDescontoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from tabela_desconto")
	}

	if err = tabelaDescontoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tabelaDescontoObj, err
	}

	return tabelaDescontoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TabelaDesconto) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tabela_desconto provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tabelaDescontoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tabelaDescontoInsertCacheMut.RLock()
	cache, cached := tabelaDescontoInsertCache[key]
	tabelaDescontoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tabelaDescontoAllColumns,
			tabelaDescontoColumnsWithDefault,
			tabelaDescontoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tabelaDescontoType, tabelaDescontoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tabelaDescontoType, tabelaDescontoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `tabela_desconto` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `tabela_desconto` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `tabela_desconto` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tabelaDescontoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into tabela_desconto")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TdescID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tabela_desconto")
	}

CacheNoHooks:
	if !cached {
		tabelaDescontoInsertCacheMut.Lock()
		tabelaDescontoInsertCache[key] = cache
		tabelaDescontoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TabelaDesconto.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TabelaDesconto) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tabelaDescontoUpdateCacheMut.RLock()
	cache, cached := tabelaDescontoUpdateCache[key]
	tabelaDescontoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tabelaDescontoAllColumns,
			tabelaDescontoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update tabela_desconto, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `tabela_desconto` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tabelaDescontoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tabelaDescontoType, tabelaDescontoMapping, append(wl, tabelaDescontoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update tabela_desconto row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for tabela_desconto")
	}

	if !cached {
		tabelaDescontoUpdateCacheMut.Lock()
		tabelaDescontoUpdateCache[key] = cache
		tabelaDescontoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tabelaDescontoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for tabela_desconto")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for tabela_desconto")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TabelaDescontoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaDescontoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `tabela_desconto` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaDescontoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tabelaDesconto slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tabelaDesconto")
	}
	return rowsAff, nil
}

var mySQLTabelaDescontoUniqueColumns = []string{
	"tdesc_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TabelaDesconto) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tabela_desconto provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tabelaDescontoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTabelaDescontoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tabelaDescontoUpsertCacheMut.RLock()
	cache, cached := tabelaDescontoUpsertCache[key]
	tabelaDescontoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tabelaDescontoAllColumns,
			tabelaDescontoColumnsWithDefault,
			tabelaDescontoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tabelaDescontoAllColumns,
			tabelaDescontoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert tabela_desconto, could not build update column list")
		}

		ret := strmangle.SetComplement(tabelaDescontoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`tabela_desconto`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `tabela_desconto` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tabelaDescontoType, tabelaDescontoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tabelaDescontoType, tabelaDescontoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for tabela_desconto")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tabelaDescontoType, tabelaDescontoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for tabela_desconto")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tabela_desconto")
	}

CacheNoHooks:
	if !cached {
		tabelaDescontoUpsertCacheMut.Lock()
		tabelaDescontoUpsertCache[key] = cache
		tabelaDescontoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TabelaDesconto record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TabelaDesconto) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TabelaDesconto provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tabelaDescontoPrimaryKeyMapping)
	sql := "DELETE FROM `tabela_desconto` WHERE `tdesc_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from tabela_desconto")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for tabela_desconto")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tabelaDescontoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tabelaDescontoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tabela_desconto")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tabela_desconto")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TabelaDescontoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tabelaDescontoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaDescontoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `tabela_desconto` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaDescontoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tabelaDesconto slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tabela_desconto")
	}

	if len(tabelaDescontoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TabelaDesconto) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTabelaDesconto(ctx, exec, o.TdescID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TabelaDescontoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TabelaDescontoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaDescontoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `tabela_desconto`.* FROM `tabela_desconto` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaDescontoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TabelaDescontoSlice")
	}

	*o = slice

	return nil
}

// TabelaDescontoExists checks if the TabelaDesconto row exists.
func TabelaDescontoExists(ctx context.Context, exec boil.ContextExecutor, tdescID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `tabela_desconto` where `tdesc_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, tdescID)
	}
	row := exec.QueryRowContext(ctx, sql, tdescID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if tabela_desconto exists")
	}

	return exists, nil
}

// Exists checks if the TabelaDesconto row exists.
func (o *TabelaDesconto) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TabelaDescontoExists(ctx, exec, o.TdescID)
}
