// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Tabelaanp is an object representing the database table.
type Tabelaanp struct {
	AnpID        int         `boil:"anp_id" json:"anp_id" toml:"anp_id" yaml:"anp_id"`
	AnpCodigo    null.String `boil:"anp_codigo" json:"anp_codigo,omitempty" toml:"anp_codigo" yaml:"anp_codigo,omitempty"`
	AnpDescricao null.String `boil:"anp_descricao" json:"anp_descricao,omitempty" toml:"anp_descricao" yaml:"anp_descricao,omitempty"`

	R *tabelaanpR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tabelaanpL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TabelaanpColumns = struct {
	AnpID        string
	AnpCodigo    string
	AnpDescricao string
}{
	AnpID:        "anp_id",
	AnpCodigo:    "anp_codigo",
	AnpDescricao: "anp_descricao",
}

var TabelaanpTableColumns = struct {
	AnpID        string
	AnpCodigo    string
	AnpDescricao string
}{
	AnpID:        "tabelaanp.anp_id",
	AnpCodigo:    "tabelaanp.anp_codigo",
	AnpDescricao: "tabelaanp.anp_descricao",
}

// Generated where

var TabelaanpWhere = struct {
	AnpID        whereHelperint
	AnpCodigo    whereHelpernull_String
	AnpDescricao whereHelpernull_String
}{
	AnpID:        whereHelperint{field: "`tabelaanp`.`anp_id`"},
	AnpCodigo:    whereHelpernull_String{field: "`tabelaanp`.`anp_codigo`"},
	AnpDescricao: whereHelpernull_String{field: "`tabelaanp`.`anp_descricao`"},
}

// TabelaanpRels is where relationship names are stored.
var TabelaanpRels = struct {
}{}

// tabelaanpR is where relationships are stored.
type tabelaanpR struct {
}

// NewStruct creates a new relationship struct
func (*tabelaanpR) NewStruct() *tabelaanpR {
	return &tabelaanpR{}
}

// tabelaanpL is where Load methods for each relationship are stored.
type tabelaanpL struct{}

var (
	tabelaanpAllColumns            = []string{"anp_id", "anp_codigo", "anp_descricao"}
	tabelaanpColumnsWithoutDefault = []string{"anp_id", "anp_codigo", "anp_descricao"}
	tabelaanpColumnsWithDefault    = []string{}
	tabelaanpPrimaryKeyColumns     = []string{"anp_id"}
	tabelaanpGeneratedColumns      = []string{}
)

type (
	// TabelaanpSlice is an alias for a slice of pointers to Tabelaanp.
	// This should almost always be used instead of []Tabelaanp.
	TabelaanpSlice []*Tabelaanp
	// TabelaanpHook is the signature for custom Tabelaanp hook methods
	TabelaanpHook func(context.Context, boil.ContextExecutor, *Tabelaanp) error

	tabelaanpQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tabelaanpType                 = reflect.TypeOf(&Tabelaanp{})
	tabelaanpMapping              = queries.MakeStructMapping(tabelaanpType)
	tabelaanpPrimaryKeyMapping, _ = queries.BindMapping(tabelaanpType, tabelaanpMapping, tabelaanpPrimaryKeyColumns)
	tabelaanpInsertCacheMut       sync.RWMutex
	tabelaanpInsertCache          = make(map[string]insertCache)
	tabelaanpUpdateCacheMut       sync.RWMutex
	tabelaanpUpdateCache          = make(map[string]updateCache)
	tabelaanpUpsertCacheMut       sync.RWMutex
	tabelaanpUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tabelaanpAfterSelectMu sync.Mutex
var tabelaanpAfterSelectHooks []TabelaanpHook

var tabelaanpBeforeInsertMu sync.Mutex
var tabelaanpBeforeInsertHooks []TabelaanpHook
var tabelaanpAfterInsertMu sync.Mutex
var tabelaanpAfterInsertHooks []TabelaanpHook

var tabelaanpBeforeUpdateMu sync.Mutex
var tabelaanpBeforeUpdateHooks []TabelaanpHook
var tabelaanpAfterUpdateMu sync.Mutex
var tabelaanpAfterUpdateHooks []TabelaanpHook

var tabelaanpBeforeDeleteMu sync.Mutex
var tabelaanpBeforeDeleteHooks []TabelaanpHook
var tabelaanpAfterDeleteMu sync.Mutex
var tabelaanpAfterDeleteHooks []TabelaanpHook

var tabelaanpBeforeUpsertMu sync.Mutex
var tabelaanpBeforeUpsertHooks []TabelaanpHook
var tabelaanpAfterUpsertMu sync.Mutex
var tabelaanpAfterUpsertHooks []TabelaanpHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Tabelaanp) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Tabelaanp) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Tabelaanp) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Tabelaanp) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Tabelaanp) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Tabelaanp) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Tabelaanp) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Tabelaanp) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Tabelaanp) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tabelaanpAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTabelaanpHook registers your hook function for all future operations.
func AddTabelaanpHook(hookPoint boil.HookPoint, tabelaanpHook TabelaanpHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tabelaanpAfterSelectMu.Lock()
		tabelaanpAfterSelectHooks = append(tabelaanpAfterSelectHooks, tabelaanpHook)
		tabelaanpAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tabelaanpBeforeInsertMu.Lock()
		tabelaanpBeforeInsertHooks = append(tabelaanpBeforeInsertHooks, tabelaanpHook)
		tabelaanpBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tabelaanpAfterInsertMu.Lock()
		tabelaanpAfterInsertHooks = append(tabelaanpAfterInsertHooks, tabelaanpHook)
		tabelaanpAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tabelaanpBeforeUpdateMu.Lock()
		tabelaanpBeforeUpdateHooks = append(tabelaanpBeforeUpdateHooks, tabelaanpHook)
		tabelaanpBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tabelaanpAfterUpdateMu.Lock()
		tabelaanpAfterUpdateHooks = append(tabelaanpAfterUpdateHooks, tabelaanpHook)
		tabelaanpAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tabelaanpBeforeDeleteMu.Lock()
		tabelaanpBeforeDeleteHooks = append(tabelaanpBeforeDeleteHooks, tabelaanpHook)
		tabelaanpBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tabelaanpAfterDeleteMu.Lock()
		tabelaanpAfterDeleteHooks = append(tabelaanpAfterDeleteHooks, tabelaanpHook)
		tabelaanpAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tabelaanpBeforeUpsertMu.Lock()
		tabelaanpBeforeUpsertHooks = append(tabelaanpBeforeUpsertHooks, tabelaanpHook)
		tabelaanpBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tabelaanpAfterUpsertMu.Lock()
		tabelaanpAfterUpsertHooks = append(tabelaanpAfterUpsertHooks, tabelaanpHook)
		tabelaanpAfterUpsertMu.Unlock()
	}
}

// One returns a single tabelaanp record from the query.
func (q tabelaanpQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Tabelaanp, error) {
	o := &Tabelaanp{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for tabelaanp")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Tabelaanp records from the query.
func (q tabelaanpQuery) All(ctx context.Context, exec boil.ContextExecutor) (TabelaanpSlice, error) {
	var o []*Tabelaanp

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Tabelaanp slice")
	}

	if len(tabelaanpAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Tabelaanp records in the query.
func (q tabelaanpQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count tabelaanp rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tabelaanpQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if tabelaanp exists")
	}

	return count > 0, nil
}

// Tabelaanps retrieves all the records using an executor.
func Tabelaanps(mods ...qm.QueryMod) tabelaanpQuery {
	mods = append(mods, qm.From("`tabelaanp`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`tabelaanp`.*"})
	}

	return tabelaanpQuery{q}
}

// FindTabelaanp retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTabelaanp(ctx context.Context, exec boil.ContextExecutor, anpID int, selectCols ...string) (*Tabelaanp, error) {
	tabelaanpObj := &Tabelaanp{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `tabelaanp` where `anp_id`=?", sel,
	)

	q := queries.Raw(query, anpID)

	err := q.Bind(ctx, exec, tabelaanpObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from tabelaanp")
	}

	if err = tabelaanpObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tabelaanpObj, err
	}

	return tabelaanpObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Tabelaanp) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tabelaanp provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tabelaanpColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tabelaanpInsertCacheMut.RLock()
	cache, cached := tabelaanpInsertCache[key]
	tabelaanpInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tabelaanpAllColumns,
			tabelaanpColumnsWithDefault,
			tabelaanpColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tabelaanpType, tabelaanpMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tabelaanpType, tabelaanpMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `tabelaanp` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `tabelaanp` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `tabelaanp` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tabelaanpPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into tabelaanp")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.AnpID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tabelaanp")
	}

CacheNoHooks:
	if !cached {
		tabelaanpInsertCacheMut.Lock()
		tabelaanpInsertCache[key] = cache
		tabelaanpInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Tabelaanp.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Tabelaanp) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tabelaanpUpdateCacheMut.RLock()
	cache, cached := tabelaanpUpdateCache[key]
	tabelaanpUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tabelaanpAllColumns,
			tabelaanpPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update tabelaanp, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `tabelaanp` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tabelaanpPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tabelaanpType, tabelaanpMapping, append(wl, tabelaanpPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update tabelaanp row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for tabelaanp")
	}

	if !cached {
		tabelaanpUpdateCacheMut.Lock()
		tabelaanpUpdateCache[key] = cache
		tabelaanpUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tabelaanpQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for tabelaanp")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for tabelaanp")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TabelaanpSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaanpPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `tabelaanp` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaanpPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tabelaanp slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tabelaanp")
	}
	return rowsAff, nil
}

var mySQLTabelaanpUniqueColumns = []string{
	"anp_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Tabelaanp) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tabelaanp provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tabelaanpColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTabelaanpUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tabelaanpUpsertCacheMut.RLock()
	cache, cached := tabelaanpUpsertCache[key]
	tabelaanpUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tabelaanpAllColumns,
			tabelaanpColumnsWithDefault,
			tabelaanpColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tabelaanpAllColumns,
			tabelaanpPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert tabelaanp, could not build update column list")
		}

		ret := strmangle.SetComplement(tabelaanpAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`tabelaanp`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `tabelaanp` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tabelaanpType, tabelaanpMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tabelaanpType, tabelaanpMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for tabelaanp")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tabelaanpType, tabelaanpMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for tabelaanp")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tabelaanp")
	}

CacheNoHooks:
	if !cached {
		tabelaanpUpsertCacheMut.Lock()
		tabelaanpUpsertCache[key] = cache
		tabelaanpUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Tabelaanp record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Tabelaanp) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Tabelaanp provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tabelaanpPrimaryKeyMapping)
	sql := "DELETE FROM `tabelaanp` WHERE `anp_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from tabelaanp")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for tabelaanp")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tabelaanpQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tabelaanpQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tabelaanp")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tabelaanp")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TabelaanpSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tabelaanpBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaanpPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `tabelaanp` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaanpPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tabelaanp slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tabelaanp")
	}

	if len(tabelaanpAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Tabelaanp) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTabelaanp(ctx, exec, o.AnpID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TabelaanpSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TabelaanpSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tabelaanpPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `tabelaanp`.* FROM `tabelaanp` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tabelaanpPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TabelaanpSlice")
	}

	*o = slice

	return nil
}

// TabelaanpExists checks if the Tabelaanp row exists.
func TabelaanpExists(ctx context.Context, exec boil.ContextExecutor, anpID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `tabelaanp` where `anp_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, anpID)
	}
	row := exec.QueryRowContext(ctx, sql, anpID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if tabelaanp exists")
	}

	return exists, nil
}

// Exists checks if the Tabelaanp row exists.
func (o *Tabelaanp) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TabelaanpExists(ctx, exec, o.AnpID)
}
