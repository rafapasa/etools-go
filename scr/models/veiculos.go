// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Veiculo is an object representing the database table.
type Veiculo struct {
	VeiID      int         `boil:"vei_id" json:"vei_id" toml:"vei_id" yaml:"vei_id"`
	EmfID      int         `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	VeiPlaca   string      `boil:"vei_placa" json:"vei_placa" toml:"vei_placa" yaml:"vei_placa"`
	VeiPlacaUf null.String `boil:"vei_placa_uf" json:"vei_placa_uf,omitempty" toml:"vei_placa_uf" yaml:"vei_placa_uf,omitempty"`
	VeiMarca   null.String `boil:"vei_marca" json:"vei_marca,omitempty" toml:"vei_marca" yaml:"vei_marca,omitempty"`
	VeiModelo  null.String `boil:"vei_modelo" json:"vei_modelo,omitempty" toml:"vei_modelo" yaml:"vei_modelo,omitempty"`
	VeiRenava  null.String `boil:"vei_renava" json:"vei_renava,omitempty" toml:"vei_renava" yaml:"vei_renava,omitempty"`
	VeiRNTC    null.String `boil:"vei_rntc" json:"vei_rntc,omitempty" toml:"vei_rntc" yaml:"vei_rntc,omitempty"`
	VeiGuincho null.Int    `boil:"vei_guincho" json:"vei_guincho,omitempty" toml:"vei_guincho" yaml:"vei_guincho,omitempty"`

	R *veiculoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L veiculoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VeiculoColumns = struct {
	VeiID      string
	EmfID      string
	VeiPlaca   string
	VeiPlacaUf string
	VeiMarca   string
	VeiModelo  string
	VeiRenava  string
	VeiRNTC    string
	VeiGuincho string
}{
	VeiID:      "vei_id",
	EmfID:      "emf_id",
	VeiPlaca:   "vei_placa",
	VeiPlacaUf: "vei_placa_uf",
	VeiMarca:   "vei_marca",
	VeiModelo:  "vei_modelo",
	VeiRenava:  "vei_renava",
	VeiRNTC:    "vei_rntc",
	VeiGuincho: "vei_guincho",
}

var VeiculoTableColumns = struct {
	VeiID      string
	EmfID      string
	VeiPlaca   string
	VeiPlacaUf string
	VeiMarca   string
	VeiModelo  string
	VeiRenava  string
	VeiRNTC    string
	VeiGuincho string
}{
	VeiID:      "veiculos.vei_id",
	EmfID:      "veiculos.emf_id",
	VeiPlaca:   "veiculos.vei_placa",
	VeiPlacaUf: "veiculos.vei_placa_uf",
	VeiMarca:   "veiculos.vei_marca",
	VeiModelo:  "veiculos.vei_modelo",
	VeiRenava:  "veiculos.vei_renava",
	VeiRNTC:    "veiculos.vei_rntc",
	VeiGuincho: "veiculos.vei_guincho",
}

// Generated where

var VeiculoWhere = struct {
	VeiID      whereHelperint
	EmfID      whereHelperint
	VeiPlaca   whereHelperstring
	VeiPlacaUf whereHelpernull_String
	VeiMarca   whereHelpernull_String
	VeiModelo  whereHelpernull_String
	VeiRenava  whereHelpernull_String
	VeiRNTC    whereHelpernull_String
	VeiGuincho whereHelpernull_Int
}{
	VeiID:      whereHelperint{field: "`veiculos`.`vei_id`"},
	EmfID:      whereHelperint{field: "`veiculos`.`emf_id`"},
	VeiPlaca:   whereHelperstring{field: "`veiculos`.`vei_placa`"},
	VeiPlacaUf: whereHelpernull_String{field: "`veiculos`.`vei_placa_uf`"},
	VeiMarca:   whereHelpernull_String{field: "`veiculos`.`vei_marca`"},
	VeiModelo:  whereHelpernull_String{field: "`veiculos`.`vei_modelo`"},
	VeiRenava:  whereHelpernull_String{field: "`veiculos`.`vei_renava`"},
	VeiRNTC:    whereHelpernull_String{field: "`veiculos`.`vei_rntc`"},
	VeiGuincho: whereHelpernull_Int{field: "`veiculos`.`vei_guincho`"},
}

// VeiculoRels is where relationship names are stored.
var VeiculoRels = struct {
	Emf                               string
	VeiCarregamentos                  string
	VeiDocumentoVendaVeiculoMotorista string
	VeiIDTRSNPNotaFiscals             string
	VeiNotaFiscalDestinadaItens       string
	VeiOrdemServicos                  string
	VeiPatrimonioBens                 string
	VeiVeiculoEntidades               string
	VeiVeiculoManutencaos             string
}{
	Emf:                               "Emf",
	VeiCarregamentos:                  "VeiCarregamentos",
	VeiDocumentoVendaVeiculoMotorista: "VeiDocumentoVendaVeiculoMotorista",
	VeiIDTRSNPNotaFiscals:             "VeiIDTRSNPNotaFiscals",
	VeiNotaFiscalDestinadaItens:       "VeiNotaFiscalDestinadaItens",
	VeiOrdemServicos:                  "VeiOrdemServicos",
	VeiPatrimonioBens:                 "VeiPatrimonioBens",
	VeiVeiculoEntidades:               "VeiVeiculoEntidades",
	VeiVeiculoManutencaos:             "VeiVeiculoManutencaos",
}

// veiculoR is where relationships are stored.
type veiculoR struct {
	Emf                               *EmpresaFilial                       `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	VeiCarregamentos                  CarregamentoSlice                    `boil:"VeiCarregamentos" json:"VeiCarregamentos" toml:"VeiCarregamentos" yaml:"VeiCarregamentos"`
	VeiDocumentoVendaVeiculoMotorista DocumentoVendaVeiculoMotoristumSlice `boil:"VeiDocumentoVendaVeiculoMotorista" json:"VeiDocumentoVendaVeiculoMotorista" toml:"VeiDocumentoVendaVeiculoMotorista" yaml:"VeiDocumentoVendaVeiculoMotorista"`
	VeiIDTRSNPNotaFiscals             NotaFiscalSlice                      `boil:"VeiIDTRSNPNotaFiscals" json:"VeiIDTRSNPNotaFiscals" toml:"VeiIDTRSNPNotaFiscals" yaml:"VeiIDTRSNPNotaFiscals"`
	VeiNotaFiscalDestinadaItens       NotaFiscalDestinadaItenSlice         `boil:"VeiNotaFiscalDestinadaItens" json:"VeiNotaFiscalDestinadaItens" toml:"VeiNotaFiscalDestinadaItens" yaml:"VeiNotaFiscalDestinadaItens"`
	VeiOrdemServicos                  OrdemServicoSlice                    `boil:"VeiOrdemServicos" json:"VeiOrdemServicos" toml:"VeiOrdemServicos" yaml:"VeiOrdemServicos"`
	VeiPatrimonioBens                 PatrimonioBenSlice                   `boil:"VeiPatrimonioBens" json:"VeiPatrimonioBens" toml:"VeiPatrimonioBens" yaml:"VeiPatrimonioBens"`
	VeiVeiculoEntidades               VeiculoEntidadeSlice                 `boil:"VeiVeiculoEntidades" json:"VeiVeiculoEntidades" toml:"VeiVeiculoEntidades" yaml:"VeiVeiculoEntidades"`
	VeiVeiculoManutencaos             VeiculoManutencaoSlice               `boil:"VeiVeiculoManutencaos" json:"VeiVeiculoManutencaos" toml:"VeiVeiculoManutencaos" yaml:"VeiVeiculoManutencaos"`
}

// NewStruct creates a new relationship struct
func (*veiculoR) NewStruct() *veiculoR {
	return &veiculoR{}
}

func (r *veiculoR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *veiculoR) GetVeiCarregamentos() CarregamentoSlice {
	if r == nil {
		return nil
	}
	return r.VeiCarregamentos
}

func (r *veiculoR) GetVeiDocumentoVendaVeiculoMotorista() DocumentoVendaVeiculoMotoristumSlice {
	if r == nil {
		return nil
	}
	return r.VeiDocumentoVendaVeiculoMotorista
}

func (r *veiculoR) GetVeiIDTRSNPNotaFiscals() NotaFiscalSlice {
	if r == nil {
		return nil
	}
	return r.VeiIDTRSNPNotaFiscals
}

func (r *veiculoR) GetVeiNotaFiscalDestinadaItens() NotaFiscalDestinadaItenSlice {
	if r == nil {
		return nil
	}
	return r.VeiNotaFiscalDestinadaItens
}

func (r *veiculoR) GetVeiOrdemServicos() OrdemServicoSlice {
	if r == nil {
		return nil
	}
	return r.VeiOrdemServicos
}

func (r *veiculoR) GetVeiPatrimonioBens() PatrimonioBenSlice {
	if r == nil {
		return nil
	}
	return r.VeiPatrimonioBens
}

func (r *veiculoR) GetVeiVeiculoEntidades() VeiculoEntidadeSlice {
	if r == nil {
		return nil
	}
	return r.VeiVeiculoEntidades
}

func (r *veiculoR) GetVeiVeiculoManutencaos() VeiculoManutencaoSlice {
	if r == nil {
		return nil
	}
	return r.VeiVeiculoManutencaos
}

// veiculoL is where Load methods for each relationship are stored.
type veiculoL struct{}

var (
	veiculoAllColumns            = []string{"vei_id", "emf_id", "vei_placa", "vei_placa_uf", "vei_marca", "vei_modelo", "vei_renava", "vei_rntc", "vei_guincho"}
	veiculoColumnsWithoutDefault = []string{"vei_id", "emf_id", "vei_placa", "vei_placa_uf", "vei_marca", "vei_modelo", "vei_renava", "vei_rntc", "vei_guincho"}
	veiculoColumnsWithDefault    = []string{}
	veiculoPrimaryKeyColumns     = []string{"vei_id"}
	veiculoGeneratedColumns      = []string{}
)

type (
	// VeiculoSlice is an alias for a slice of pointers to Veiculo.
	// This should almost always be used instead of []Veiculo.
	VeiculoSlice []*Veiculo
	// VeiculoHook is the signature for custom Veiculo hook methods
	VeiculoHook func(context.Context, boil.ContextExecutor, *Veiculo) error

	veiculoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	veiculoType                 = reflect.TypeOf(&Veiculo{})
	veiculoMapping              = queries.MakeStructMapping(veiculoType)
	veiculoPrimaryKeyMapping, _ = queries.BindMapping(veiculoType, veiculoMapping, veiculoPrimaryKeyColumns)
	veiculoInsertCacheMut       sync.RWMutex
	veiculoInsertCache          = make(map[string]insertCache)
	veiculoUpdateCacheMut       sync.RWMutex
	veiculoUpdateCache          = make(map[string]updateCache)
	veiculoUpsertCacheMut       sync.RWMutex
	veiculoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var veiculoAfterSelectMu sync.Mutex
var veiculoAfterSelectHooks []VeiculoHook

var veiculoBeforeInsertMu sync.Mutex
var veiculoBeforeInsertHooks []VeiculoHook
var veiculoAfterInsertMu sync.Mutex
var veiculoAfterInsertHooks []VeiculoHook

var veiculoBeforeUpdateMu sync.Mutex
var veiculoBeforeUpdateHooks []VeiculoHook
var veiculoAfterUpdateMu sync.Mutex
var veiculoAfterUpdateHooks []VeiculoHook

var veiculoBeforeDeleteMu sync.Mutex
var veiculoBeforeDeleteHooks []VeiculoHook
var veiculoAfterDeleteMu sync.Mutex
var veiculoAfterDeleteHooks []VeiculoHook

var veiculoBeforeUpsertMu sync.Mutex
var veiculoBeforeUpsertHooks []VeiculoHook
var veiculoAfterUpsertMu sync.Mutex
var veiculoAfterUpsertHooks []VeiculoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Veiculo) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Veiculo) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Veiculo) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Veiculo) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Veiculo) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Veiculo) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Veiculo) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Veiculo) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Veiculo) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range veiculoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddVeiculoHook registers your hook function for all future operations.
func AddVeiculoHook(hookPoint boil.HookPoint, veiculoHook VeiculoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		veiculoAfterSelectMu.Lock()
		veiculoAfterSelectHooks = append(veiculoAfterSelectHooks, veiculoHook)
		veiculoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		veiculoBeforeInsertMu.Lock()
		veiculoBeforeInsertHooks = append(veiculoBeforeInsertHooks, veiculoHook)
		veiculoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		veiculoAfterInsertMu.Lock()
		veiculoAfterInsertHooks = append(veiculoAfterInsertHooks, veiculoHook)
		veiculoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		veiculoBeforeUpdateMu.Lock()
		veiculoBeforeUpdateHooks = append(veiculoBeforeUpdateHooks, veiculoHook)
		veiculoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		veiculoAfterUpdateMu.Lock()
		veiculoAfterUpdateHooks = append(veiculoAfterUpdateHooks, veiculoHook)
		veiculoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		veiculoBeforeDeleteMu.Lock()
		veiculoBeforeDeleteHooks = append(veiculoBeforeDeleteHooks, veiculoHook)
		veiculoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		veiculoAfterDeleteMu.Lock()
		veiculoAfterDeleteHooks = append(veiculoAfterDeleteHooks, veiculoHook)
		veiculoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		veiculoBeforeUpsertMu.Lock()
		veiculoBeforeUpsertHooks = append(veiculoBeforeUpsertHooks, veiculoHook)
		veiculoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		veiculoAfterUpsertMu.Lock()
		veiculoAfterUpsertHooks = append(veiculoAfterUpsertHooks, veiculoHook)
		veiculoAfterUpsertMu.Unlock()
	}
}

// One returns a single veiculo record from the query.
func (q veiculoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Veiculo, error) {
	o := &Veiculo{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for veiculos")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Veiculo records from the query.
func (q veiculoQuery) All(ctx context.Context, exec boil.ContextExecutor) (VeiculoSlice, error) {
	var o []*Veiculo

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Veiculo slice")
	}

	if len(veiculoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Veiculo records in the query.
func (q veiculoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count veiculos rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q veiculoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if veiculos exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *Veiculo) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// VeiCarregamentos retrieves all the carregamento's Carregamentos with an executor via vei_id column.
func (o *Veiculo) VeiCarregamentos(mods ...qm.QueryMod) carregamentoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`carregamento`.`vei_id`=?", o.VeiID),
	)

	return Carregamentos(queryMods...)
}

// VeiDocumentoVendaVeiculoMotorista retrieves all the documento_venda_veiculo_motoristum's DocumentoVendaVeiculoMotorista with an executor via vei_id column.
func (o *Veiculo) VeiDocumentoVendaVeiculoMotorista(mods ...qm.QueryMod) documentoVendaVeiculoMotoristumQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`documento_venda_veiculo_motorista`.`vei_id`=?", o.VeiID),
	)

	return DocumentoVendaVeiculoMotorista(queryMods...)
}

// VeiIDTRSNPNotaFiscals retrieves all the nota_fiscal's NotaFiscals with an executor via vei_id_trsnp column.
func (o *Veiculo) VeiIDTRSNPNotaFiscals(mods ...qm.QueryMod) notaFiscalQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal`.`vei_id_trsnp`=?", o.VeiID),
	)

	return NotaFiscals(queryMods...)
}

// VeiNotaFiscalDestinadaItens retrieves all the nota_fiscal_destinada_iten's NotaFiscalDestinadaItens with an executor via vei_id column.
func (o *Veiculo) VeiNotaFiscalDestinadaItens(mods ...qm.QueryMod) notaFiscalDestinadaItenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal_destinada_itens`.`vei_id`=?", o.VeiID),
	)

	return NotaFiscalDestinadaItens(queryMods...)
}

// VeiOrdemServicos retrieves all the ordem_servico's OrdemServicos with an executor via vei_id column.
func (o *Veiculo) VeiOrdemServicos(mods ...qm.QueryMod) ordemServicoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`ordem_servico`.`vei_id`=?", o.VeiID),
	)

	return OrdemServicos(queryMods...)
}

// VeiPatrimonioBens retrieves all the patrimonio_ben's PatrimonioBens with an executor via vei_id column.
func (o *Veiculo) VeiPatrimonioBens(mods ...qm.QueryMod) patrimonioBenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`patrimonio_bens`.`vei_id`=?", o.VeiID),
	)

	return PatrimonioBens(queryMods...)
}

// VeiVeiculoEntidades retrieves all the veiculo_entidade's VeiculoEntidades with an executor via vei_id column.
func (o *Veiculo) VeiVeiculoEntidades(mods ...qm.QueryMod) veiculoEntidadeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`veiculo_entidade`.`vei_id`=?", o.VeiID),
	)

	return VeiculoEntidades(queryMods...)
}

// VeiVeiculoManutencaos retrieves all the veiculo_manutencao's VeiculoManutencaos with an executor via vei_id column.
func (o *Veiculo) VeiVeiculoManutencaos(mods ...qm.QueryMod) veiculoManutencaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`veiculo_manutencao`.`vei_id`=?", o.VeiID),
	)

	return VeiculoManutencaos(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (veiculoL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfVeiculos = append(foreign.R.EmfVeiculos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfVeiculos = append(foreign.R.EmfVeiculos, local)
				break
			}
		}
	}

	return nil
}

// LoadVeiCarregamentos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (veiculoL) LoadVeiCarregamentos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.VeiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}
			args[obj.VeiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`carregamento`),
		qm.WhereIn(`carregamento.vei_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load carregamento")
	}

	var resultSlice []*Carregamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice carregamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on carregamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for carregamento")
	}

	if len(carregamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VeiCarregamentos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &carregamentoR{}
			}
			foreign.R.Vei = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.VeiID, foreign.VeiID) {
				local.R.VeiCarregamentos = append(local.R.VeiCarregamentos, foreign)
				if foreign.R == nil {
					foreign.R = &carregamentoR{}
				}
				foreign.R.Vei = local
				break
			}
		}
	}

	return nil
}

// LoadVeiDocumentoVendaVeiculoMotorista allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (veiculoL) LoadVeiDocumentoVendaVeiculoMotorista(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.VeiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}
			args[obj.VeiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documento_venda_veiculo_motorista`),
		qm.WhereIn(`documento_venda_veiculo_motorista.vei_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load documento_venda_veiculo_motorista")
	}

	var resultSlice []*DocumentoVendaVeiculoMotoristum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice documento_venda_veiculo_motorista")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on documento_venda_veiculo_motorista")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documento_venda_veiculo_motorista")
	}

	if len(documentoVendaVeiculoMotoristumAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VeiDocumentoVendaVeiculoMotorista = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentoVendaVeiculoMotoristumR{}
			}
			foreign.R.Vei = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.VeiID, foreign.VeiID) {
				local.R.VeiDocumentoVendaVeiculoMotorista = append(local.R.VeiDocumentoVendaVeiculoMotorista, foreign)
				if foreign.R == nil {
					foreign.R = &documentoVendaVeiculoMotoristumR{}
				}
				foreign.R.Vei = local
				break
			}
		}
	}

	return nil
}

// LoadVeiIDTRSNPNotaFiscals allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (veiculoL) LoadVeiIDTRSNPNotaFiscals(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.VeiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}
			args[obj.VeiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal`),
		qm.WhereIn(`nota_fiscal.vei_id_trsnp in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal")
	}

	var resultSlice []*NotaFiscal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal")
	}

	if len(notaFiscalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VeiIDTRSNPNotaFiscals = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalR{}
			}
			foreign.R.VeiIDTRSNPVeiculo = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.VeiID, foreign.VeiIDTRSNP) {
				local.R.VeiIDTRSNPNotaFiscals = append(local.R.VeiIDTRSNPNotaFiscals, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalR{}
				}
				foreign.R.VeiIDTRSNPVeiculo = local
				break
			}
		}
	}

	return nil
}

// LoadVeiNotaFiscalDestinadaItens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (veiculoL) LoadVeiNotaFiscalDestinadaItens(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.VeiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}
			args[obj.VeiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal_destinada_itens`),
		qm.WhereIn(`nota_fiscal_destinada_itens.vei_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal_destinada_itens")
	}

	var resultSlice []*NotaFiscalDestinadaIten
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal_destinada_itens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal_destinada_itens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal_destinada_itens")
	}

	if len(notaFiscalDestinadaItenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VeiNotaFiscalDestinadaItens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalDestinadaItenR{}
			}
			foreign.R.Vei = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.VeiID, foreign.VeiID) {
				local.R.VeiNotaFiscalDestinadaItens = append(local.R.VeiNotaFiscalDestinadaItens, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalDestinadaItenR{}
				}
				foreign.R.Vei = local
				break
			}
		}
	}

	return nil
}

// LoadVeiOrdemServicos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (veiculoL) LoadVeiOrdemServicos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.VeiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}
			args[obj.VeiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ordem_servico`),
		qm.WhereIn(`ordem_servico.vei_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ordem_servico")
	}

	var resultSlice []*OrdemServico
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ordem_servico")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on ordem_servico")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ordem_servico")
	}

	if len(ordemServicoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VeiOrdemServicos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &ordemServicoR{}
			}
			foreign.R.Vei = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.VeiID, foreign.VeiID) {
				local.R.VeiOrdemServicos = append(local.R.VeiOrdemServicos, foreign)
				if foreign.R == nil {
					foreign.R = &ordemServicoR{}
				}
				foreign.R.Vei = local
				break
			}
		}
	}

	return nil
}

// LoadVeiPatrimonioBens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (veiculoL) LoadVeiPatrimonioBens(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.VeiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}
			args[obj.VeiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`patrimonio_bens`),
		qm.WhereIn(`patrimonio_bens.vei_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load patrimonio_bens")
	}

	var resultSlice []*PatrimonioBen
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice patrimonio_bens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on patrimonio_bens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for patrimonio_bens")
	}

	if len(patrimonioBenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VeiPatrimonioBens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &patrimonioBenR{}
			}
			foreign.R.Vei = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.VeiID, foreign.VeiID) {
				local.R.VeiPatrimonioBens = append(local.R.VeiPatrimonioBens, foreign)
				if foreign.R == nil {
					foreign.R = &patrimonioBenR{}
				}
				foreign.R.Vei = local
				break
			}
		}
	}

	return nil
}

// LoadVeiVeiculoEntidades allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (veiculoL) LoadVeiVeiculoEntidades(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.VeiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}
			args[obj.VeiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`veiculo_entidade`),
		qm.WhereIn(`veiculo_entidade.vei_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load veiculo_entidade")
	}

	var resultSlice []*VeiculoEntidade
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice veiculo_entidade")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on veiculo_entidade")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for veiculo_entidade")
	}

	if len(veiculoEntidadeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VeiVeiculoEntidades = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &veiculoEntidadeR{}
			}
			foreign.R.Vei = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.VeiID == foreign.VeiID {
				local.R.VeiVeiculoEntidades = append(local.R.VeiVeiculoEntidades, foreign)
				if foreign.R == nil {
					foreign.R = &veiculoEntidadeR{}
				}
				foreign.R.Vei = local
				break
			}
		}
	}

	return nil
}

// LoadVeiVeiculoManutencaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (veiculoL) LoadVeiVeiculoManutencaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVeiculo interface{}, mods queries.Applicator) error {
	var slice []*Veiculo
	var object *Veiculo

	if singular {
		var ok bool
		object, ok = maybeVeiculo.(*Veiculo)
		if !ok {
			object = new(Veiculo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVeiculo))
			}
		}
	} else {
		s, ok := maybeVeiculo.(*[]*Veiculo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVeiculo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVeiculo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &veiculoR{}
		}
		args[object.VeiID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &veiculoR{}
			}
			args[obj.VeiID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`veiculo_manutencao`),
		qm.WhereIn(`veiculo_manutencao.vei_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load veiculo_manutencao")
	}

	var resultSlice []*VeiculoManutencao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice veiculo_manutencao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on veiculo_manutencao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for veiculo_manutencao")
	}

	if len(veiculoManutencaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.VeiVeiculoManutencaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &veiculoManutencaoR{}
			}
			foreign.R.Vei = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.VeiID == foreign.VeiID {
				local.R.VeiVeiculoManutencaos = append(local.R.VeiVeiculoManutencaos, foreign)
				if foreign.R == nil {
					foreign.R = &veiculoManutencaoR{}
				}
				foreign.R.Vei = local
				break
			}
		}
	}

	return nil
}

// SetEmf of the veiculo to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfVeiculos.
func (o *Veiculo) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `veiculos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, veiculoPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.VeiID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &veiculoR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfVeiculos: VeiculoSlice{o},
		}
	} else {
		related.R.EmfVeiculos = append(related.R.EmfVeiculos, o)
	}

	return nil
}

// AddVeiCarregamentos adds the given related objects to the existing relationships
// of the veiculo, optionally inserting them as new records.
// Appends related to o.R.VeiCarregamentos.
// Sets related.R.Vei appropriately.
func (o *Veiculo) AddVeiCarregamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Carregamento) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.VeiID, o.VeiID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `carregamento` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"vei_id"}),
				strmangle.WhereClause("`", "`", 0, carregamentoPrimaryKeyColumns),
			)
			values := []interface{}{o.VeiID, rel.CarID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.VeiID, o.VeiID)
		}
	}

	if o.R == nil {
		o.R = &veiculoR{
			VeiCarregamentos: related,
		}
	} else {
		o.R.VeiCarregamentos = append(o.R.VeiCarregamentos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &carregamentoR{
				Vei: o,
			}
		} else {
			rel.R.Vei = o
		}
	}
	return nil
}

// SetVeiCarregamentos removes all previously related items of the
// veiculo replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Vei's VeiCarregamentos accordingly.
// Replaces o.R.VeiCarregamentos with related.
// Sets related.R.Vei's VeiCarregamentos accordingly.
func (o *Veiculo) SetVeiCarregamentos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Carregamento) error {
	query := "update `carregamento` set `vei_id` = null where `vei_id` = ?"
	values := []interface{}{o.VeiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.VeiCarregamentos {
			queries.SetScanner(&rel.VeiID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Vei = nil
		}
		o.R.VeiCarregamentos = nil
	}

	return o.AddVeiCarregamentos(ctx, exec, insert, related...)
}

// RemoveVeiCarregamentos relationships from objects passed in.
// Removes related items from R.VeiCarregamentos (uses pointer comparison, removal does not keep order)
// Sets related.R.Vei.
func (o *Veiculo) RemoveVeiCarregamentos(ctx context.Context, exec boil.ContextExecutor, related ...*Carregamento) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.VeiID, nil)
		if rel.R != nil {
			rel.R.Vei = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("vei_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.VeiCarregamentos {
			if rel != ri {
				continue
			}

			ln := len(o.R.VeiCarregamentos)
			if ln > 1 && i < ln-1 {
				o.R.VeiCarregamentos[i] = o.R.VeiCarregamentos[ln-1]
			}
			o.R.VeiCarregamentos = o.R.VeiCarregamentos[:ln-1]
			break
		}
	}

	return nil
}

// AddVeiDocumentoVendaVeiculoMotorista adds the given related objects to the existing relationships
// of the veiculo, optionally inserting them as new records.
// Appends related to o.R.VeiDocumentoVendaVeiculoMotorista.
// Sets related.R.Vei appropriately.
func (o *Veiculo) AddVeiDocumentoVendaVeiculoMotorista(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVendaVeiculoMotoristum) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.VeiID, o.VeiID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `documento_venda_veiculo_motorista` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"vei_id"}),
				strmangle.WhereClause("`", "`", 0, documentoVendaVeiculoMotoristumPrimaryKeyColumns),
			)
			values := []interface{}{o.VeiID, rel.DDVID, rel.DviItem, rel.DDDVVMItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.VeiID, o.VeiID)
		}
	}

	if o.R == nil {
		o.R = &veiculoR{
			VeiDocumentoVendaVeiculoMotorista: related,
		}
	} else {
		o.R.VeiDocumentoVendaVeiculoMotorista = append(o.R.VeiDocumentoVendaVeiculoMotorista, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentoVendaVeiculoMotoristumR{
				Vei: o,
			}
		} else {
			rel.R.Vei = o
		}
	}
	return nil
}

// SetVeiDocumentoVendaVeiculoMotorista removes all previously related items of the
// veiculo replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Vei's VeiDocumentoVendaVeiculoMotorista accordingly.
// Replaces o.R.VeiDocumentoVendaVeiculoMotorista with related.
// Sets related.R.Vei's VeiDocumentoVendaVeiculoMotorista accordingly.
func (o *Veiculo) SetVeiDocumentoVendaVeiculoMotorista(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVendaVeiculoMotoristum) error {
	query := "update `documento_venda_veiculo_motorista` set `vei_id` = null where `vei_id` = ?"
	values := []interface{}{o.VeiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.VeiDocumentoVendaVeiculoMotorista {
			queries.SetScanner(&rel.VeiID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Vei = nil
		}
		o.R.VeiDocumentoVendaVeiculoMotorista = nil
	}

	return o.AddVeiDocumentoVendaVeiculoMotorista(ctx, exec, insert, related...)
}

// RemoveVeiDocumentoVendaVeiculoMotorista relationships from objects passed in.
// Removes related items from R.VeiDocumentoVendaVeiculoMotorista (uses pointer comparison, removal does not keep order)
// Sets related.R.Vei.
func (o *Veiculo) RemoveVeiDocumentoVendaVeiculoMotorista(ctx context.Context, exec boil.ContextExecutor, related ...*DocumentoVendaVeiculoMotoristum) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.VeiID, nil)
		if rel.R != nil {
			rel.R.Vei = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("vei_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.VeiDocumentoVendaVeiculoMotorista {
			if rel != ri {
				continue
			}

			ln := len(o.R.VeiDocumentoVendaVeiculoMotorista)
			if ln > 1 && i < ln-1 {
				o.R.VeiDocumentoVendaVeiculoMotorista[i] = o.R.VeiDocumentoVendaVeiculoMotorista[ln-1]
			}
			o.R.VeiDocumentoVendaVeiculoMotorista = o.R.VeiDocumentoVendaVeiculoMotorista[:ln-1]
			break
		}
	}

	return nil
}

// AddVeiIDTRSNPNotaFiscals adds the given related objects to the existing relationships
// of the veiculo, optionally inserting them as new records.
// Appends related to o.R.VeiIDTRSNPNotaFiscals.
// Sets related.R.VeiIDTRSNPVeiculo appropriately.
func (o *Veiculo) AddVeiIDTRSNPNotaFiscals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscal) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.VeiIDTRSNP, o.VeiID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"vei_id_trsnp"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalPrimaryKeyColumns),
			)
			values := []interface{}{o.VeiID, rel.NTFID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.VeiIDTRSNP, o.VeiID)
		}
	}

	if o.R == nil {
		o.R = &veiculoR{
			VeiIDTRSNPNotaFiscals: related,
		}
	} else {
		o.R.VeiIDTRSNPNotaFiscals = append(o.R.VeiIDTRSNPNotaFiscals, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalR{
				VeiIDTRSNPVeiculo: o,
			}
		} else {
			rel.R.VeiIDTRSNPVeiculo = o
		}
	}
	return nil
}

// SetVeiIDTRSNPNotaFiscals removes all previously related items of the
// veiculo replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.VeiIDTRSNPVeiculo's VeiIDTRSNPNotaFiscals accordingly.
// Replaces o.R.VeiIDTRSNPNotaFiscals with related.
// Sets related.R.VeiIDTRSNPVeiculo's VeiIDTRSNPNotaFiscals accordingly.
func (o *Veiculo) SetVeiIDTRSNPNotaFiscals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscal) error {
	query := "update `nota_fiscal` set `vei_id_trsnp` = null where `vei_id_trsnp` = ?"
	values := []interface{}{o.VeiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.VeiIDTRSNPNotaFiscals {
			queries.SetScanner(&rel.VeiIDTRSNP, nil)
			if rel.R == nil {
				continue
			}

			rel.R.VeiIDTRSNPVeiculo = nil
		}
		o.R.VeiIDTRSNPNotaFiscals = nil
	}

	return o.AddVeiIDTRSNPNotaFiscals(ctx, exec, insert, related...)
}

// RemoveVeiIDTRSNPNotaFiscals relationships from objects passed in.
// Removes related items from R.VeiIDTRSNPNotaFiscals (uses pointer comparison, removal does not keep order)
// Sets related.R.VeiIDTRSNPVeiculo.
func (o *Veiculo) RemoveVeiIDTRSNPNotaFiscals(ctx context.Context, exec boil.ContextExecutor, related ...*NotaFiscal) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.VeiIDTRSNP, nil)
		if rel.R != nil {
			rel.R.VeiIDTRSNPVeiculo = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("vei_id_trsnp")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.VeiIDTRSNPNotaFiscals {
			if rel != ri {
				continue
			}

			ln := len(o.R.VeiIDTRSNPNotaFiscals)
			if ln > 1 && i < ln-1 {
				o.R.VeiIDTRSNPNotaFiscals[i] = o.R.VeiIDTRSNPNotaFiscals[ln-1]
			}
			o.R.VeiIDTRSNPNotaFiscals = o.R.VeiIDTRSNPNotaFiscals[:ln-1]
			break
		}
	}

	return nil
}

// AddVeiNotaFiscalDestinadaItens adds the given related objects to the existing relationships
// of the veiculo, optionally inserting them as new records.
// Appends related to o.R.VeiNotaFiscalDestinadaItens.
// Sets related.R.Vei appropriately.
func (o *Veiculo) AddVeiNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalDestinadaIten) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.VeiID, o.VeiID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal_destinada_itens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"vei_id"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalDestinadaItenPrimaryKeyColumns),
			)
			values := []interface{}{o.VeiID, rel.NFDID, rel.NfdiItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.VeiID, o.VeiID)
		}
	}

	if o.R == nil {
		o.R = &veiculoR{
			VeiNotaFiscalDestinadaItens: related,
		}
	} else {
		o.R.VeiNotaFiscalDestinadaItens = append(o.R.VeiNotaFiscalDestinadaItens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalDestinadaItenR{
				Vei: o,
			}
		} else {
			rel.R.Vei = o
		}
	}
	return nil
}

// SetVeiNotaFiscalDestinadaItens removes all previously related items of the
// veiculo replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Vei's VeiNotaFiscalDestinadaItens accordingly.
// Replaces o.R.VeiNotaFiscalDestinadaItens with related.
// Sets related.R.Vei's VeiNotaFiscalDestinadaItens accordingly.
func (o *Veiculo) SetVeiNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalDestinadaIten) error {
	query := "update `nota_fiscal_destinada_itens` set `vei_id` = null where `vei_id` = ?"
	values := []interface{}{o.VeiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.VeiNotaFiscalDestinadaItens {
			queries.SetScanner(&rel.VeiID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Vei = nil
		}
		o.R.VeiNotaFiscalDestinadaItens = nil
	}

	return o.AddVeiNotaFiscalDestinadaItens(ctx, exec, insert, related...)
}

// RemoveVeiNotaFiscalDestinadaItens relationships from objects passed in.
// Removes related items from R.VeiNotaFiscalDestinadaItens (uses pointer comparison, removal does not keep order)
// Sets related.R.Vei.
func (o *Veiculo) RemoveVeiNotaFiscalDestinadaItens(ctx context.Context, exec boil.ContextExecutor, related ...*NotaFiscalDestinadaIten) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.VeiID, nil)
		if rel.R != nil {
			rel.R.Vei = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("vei_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.VeiNotaFiscalDestinadaItens {
			if rel != ri {
				continue
			}

			ln := len(o.R.VeiNotaFiscalDestinadaItens)
			if ln > 1 && i < ln-1 {
				o.R.VeiNotaFiscalDestinadaItens[i] = o.R.VeiNotaFiscalDestinadaItens[ln-1]
			}
			o.R.VeiNotaFiscalDestinadaItens = o.R.VeiNotaFiscalDestinadaItens[:ln-1]
			break
		}
	}

	return nil
}

// AddVeiOrdemServicos adds the given related objects to the existing relationships
// of the veiculo, optionally inserting them as new records.
// Appends related to o.R.VeiOrdemServicos.
// Sets related.R.Vei appropriately.
func (o *Veiculo) AddVeiOrdemServicos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*OrdemServico) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.VeiID, o.VeiID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `ordem_servico` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"vei_id"}),
				strmangle.WhereClause("`", "`", 0, ordemServicoPrimaryKeyColumns),
			)
			values := []interface{}{o.VeiID, rel.OsID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.VeiID, o.VeiID)
		}
	}

	if o.R == nil {
		o.R = &veiculoR{
			VeiOrdemServicos: related,
		}
	} else {
		o.R.VeiOrdemServicos = append(o.R.VeiOrdemServicos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &ordemServicoR{
				Vei: o,
			}
		} else {
			rel.R.Vei = o
		}
	}
	return nil
}

// SetVeiOrdemServicos removes all previously related items of the
// veiculo replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Vei's VeiOrdemServicos accordingly.
// Replaces o.R.VeiOrdemServicos with related.
// Sets related.R.Vei's VeiOrdemServicos accordingly.
func (o *Veiculo) SetVeiOrdemServicos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*OrdemServico) error {
	query := "update `ordem_servico` set `vei_id` = null where `vei_id` = ?"
	values := []interface{}{o.VeiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.VeiOrdemServicos {
			queries.SetScanner(&rel.VeiID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Vei = nil
		}
		o.R.VeiOrdemServicos = nil
	}

	return o.AddVeiOrdemServicos(ctx, exec, insert, related...)
}

// RemoveVeiOrdemServicos relationships from objects passed in.
// Removes related items from R.VeiOrdemServicos (uses pointer comparison, removal does not keep order)
// Sets related.R.Vei.
func (o *Veiculo) RemoveVeiOrdemServicos(ctx context.Context, exec boil.ContextExecutor, related ...*OrdemServico) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.VeiID, nil)
		if rel.R != nil {
			rel.R.Vei = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("vei_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.VeiOrdemServicos {
			if rel != ri {
				continue
			}

			ln := len(o.R.VeiOrdemServicos)
			if ln > 1 && i < ln-1 {
				o.R.VeiOrdemServicos[i] = o.R.VeiOrdemServicos[ln-1]
			}
			o.R.VeiOrdemServicos = o.R.VeiOrdemServicos[:ln-1]
			break
		}
	}

	return nil
}

// AddVeiPatrimonioBens adds the given related objects to the existing relationships
// of the veiculo, optionally inserting them as new records.
// Appends related to o.R.VeiPatrimonioBens.
// Sets related.R.Vei appropriately.
func (o *Veiculo) AddVeiPatrimonioBens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PatrimonioBen) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.VeiID, o.VeiID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `patrimonio_bens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"vei_id"}),
				strmangle.WhereClause("`", "`", 0, patrimonioBenPrimaryKeyColumns),
			)
			values := []interface{}{o.VeiID, rel.PbenID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.VeiID, o.VeiID)
		}
	}

	if o.R == nil {
		o.R = &veiculoR{
			VeiPatrimonioBens: related,
		}
	} else {
		o.R.VeiPatrimonioBens = append(o.R.VeiPatrimonioBens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &patrimonioBenR{
				Vei: o,
			}
		} else {
			rel.R.Vei = o
		}
	}
	return nil
}

// SetVeiPatrimonioBens removes all previously related items of the
// veiculo replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Vei's VeiPatrimonioBens accordingly.
// Replaces o.R.VeiPatrimonioBens with related.
// Sets related.R.Vei's VeiPatrimonioBens accordingly.
func (o *Veiculo) SetVeiPatrimonioBens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PatrimonioBen) error {
	query := "update `patrimonio_bens` set `vei_id` = null where `vei_id` = ?"
	values := []interface{}{o.VeiID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.VeiPatrimonioBens {
			queries.SetScanner(&rel.VeiID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Vei = nil
		}
		o.R.VeiPatrimonioBens = nil
	}

	return o.AddVeiPatrimonioBens(ctx, exec, insert, related...)
}

// RemoveVeiPatrimonioBens relationships from objects passed in.
// Removes related items from R.VeiPatrimonioBens (uses pointer comparison, removal does not keep order)
// Sets related.R.Vei.
func (o *Veiculo) RemoveVeiPatrimonioBens(ctx context.Context, exec boil.ContextExecutor, related ...*PatrimonioBen) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.VeiID, nil)
		if rel.R != nil {
			rel.R.Vei = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("vei_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.VeiPatrimonioBens {
			if rel != ri {
				continue
			}

			ln := len(o.R.VeiPatrimonioBens)
			if ln > 1 && i < ln-1 {
				o.R.VeiPatrimonioBens[i] = o.R.VeiPatrimonioBens[ln-1]
			}
			o.R.VeiPatrimonioBens = o.R.VeiPatrimonioBens[:ln-1]
			break
		}
	}

	return nil
}

// AddVeiVeiculoEntidades adds the given related objects to the existing relationships
// of the veiculo, optionally inserting them as new records.
// Appends related to o.R.VeiVeiculoEntidades.
// Sets related.R.Vei appropriately.
func (o *Veiculo) AddVeiVeiculoEntidades(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*VeiculoEntidade) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.VeiID = o.VeiID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `veiculo_entidade` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"vei_id"}),
				strmangle.WhereClause("`", "`", 0, veiculoEntidadePrimaryKeyColumns),
			)
			values := []interface{}{o.VeiID, rel.EntID, rel.VeiID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.VeiID = o.VeiID
		}
	}

	if o.R == nil {
		o.R = &veiculoR{
			VeiVeiculoEntidades: related,
		}
	} else {
		o.R.VeiVeiculoEntidades = append(o.R.VeiVeiculoEntidades, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &veiculoEntidadeR{
				Vei: o,
			}
		} else {
			rel.R.Vei = o
		}
	}
	return nil
}

// AddVeiVeiculoManutencaos adds the given related objects to the existing relationships
// of the veiculo, optionally inserting them as new records.
// Appends related to o.R.VeiVeiculoManutencaos.
// Sets related.R.Vei appropriately.
func (o *Veiculo) AddVeiVeiculoManutencaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*VeiculoManutencao) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.VeiID = o.VeiID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `veiculo_manutencao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"vei_id"}),
				strmangle.WhereClause("`", "`", 0, veiculoManutencaoPrimaryKeyColumns),
			)
			values := []interface{}{o.VeiID, rel.VeiID, rel.ManID, rel.VeimanItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.VeiID = o.VeiID
		}
	}

	if o.R == nil {
		o.R = &veiculoR{
			VeiVeiculoManutencaos: related,
		}
	} else {
		o.R.VeiVeiculoManutencaos = append(o.R.VeiVeiculoManutencaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &veiculoManutencaoR{
				Vei: o,
			}
		} else {
			rel.R.Vei = o
		}
	}
	return nil
}

// Veiculos retrieves all the records using an executor.
func Veiculos(mods ...qm.QueryMod) veiculoQuery {
	mods = append(mods, qm.From("`veiculos`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`veiculos`.*"})
	}

	return veiculoQuery{q}
}

// FindVeiculo retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVeiculo(ctx context.Context, exec boil.ContextExecutor, veiID int, selectCols ...string) (*Veiculo, error) {
	veiculoObj := &Veiculo{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `veiculos` where `vei_id`=?", sel,
	)

	q := queries.Raw(query, veiID)

	err := q.Bind(ctx, exec, veiculoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from veiculos")
	}

	if err = veiculoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return veiculoObj, err
	}

	return veiculoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Veiculo) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no veiculos provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(veiculoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	veiculoInsertCacheMut.RLock()
	cache, cached := veiculoInsertCache[key]
	veiculoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			veiculoAllColumns,
			veiculoColumnsWithDefault,
			veiculoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(veiculoType, veiculoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(veiculoType, veiculoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `veiculos` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `veiculos` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `veiculos` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, veiculoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into veiculos")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.VeiID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for veiculos")
	}

CacheNoHooks:
	if !cached {
		veiculoInsertCacheMut.Lock()
		veiculoInsertCache[key] = cache
		veiculoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Veiculo.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Veiculo) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	veiculoUpdateCacheMut.RLock()
	cache, cached := veiculoUpdateCache[key]
	veiculoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			veiculoAllColumns,
			veiculoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update veiculos, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `veiculos` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, veiculoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(veiculoType, veiculoMapping, append(wl, veiculoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update veiculos row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for veiculos")
	}

	if !cached {
		veiculoUpdateCacheMut.Lock()
		veiculoUpdateCache[key] = cache
		veiculoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q veiculoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for veiculos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for veiculos")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VeiculoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), veiculoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `veiculos` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, veiculoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in veiculo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all veiculo")
	}
	return rowsAff, nil
}

var mySQLVeiculoUniqueColumns = []string{
	"vei_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Veiculo) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no veiculos provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(veiculoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLVeiculoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	veiculoUpsertCacheMut.RLock()
	cache, cached := veiculoUpsertCache[key]
	veiculoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			veiculoAllColumns,
			veiculoColumnsWithDefault,
			veiculoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			veiculoAllColumns,
			veiculoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert veiculos, could not build update column list")
		}

		ret := strmangle.SetComplement(veiculoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`veiculos`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `veiculos` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(veiculoType, veiculoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(veiculoType, veiculoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for veiculos")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(veiculoType, veiculoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for veiculos")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for veiculos")
	}

CacheNoHooks:
	if !cached {
		veiculoUpsertCacheMut.Lock()
		veiculoUpsertCache[key] = cache
		veiculoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Veiculo record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Veiculo) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Veiculo provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), veiculoPrimaryKeyMapping)
	sql := "DELETE FROM `veiculos` WHERE `vei_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from veiculos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for veiculos")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q veiculoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no veiculoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from veiculos")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for veiculos")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VeiculoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(veiculoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), veiculoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `veiculos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, veiculoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from veiculo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for veiculos")
	}

	if len(veiculoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Veiculo) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindVeiculo(ctx, exec, o.VeiID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VeiculoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VeiculoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), veiculoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `veiculos`.* FROM `veiculos` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, veiculoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in VeiculoSlice")
	}

	*o = slice

	return nil
}

// VeiculoExists checks if the Veiculo row exists.
func VeiculoExists(ctx context.Context, exec boil.ContextExecutor, veiID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `veiculos` where `vei_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, veiID)
	}
	row := exec.QueryRowContext(ctx, sql, veiID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if veiculos exists")
	}

	return exists, nil
}

// Exists checks if the Veiculo row exists.
func (o *Veiculo) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return VeiculoExists(ctx, exec, o.VeiID)
}
