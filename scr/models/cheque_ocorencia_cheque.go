// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// ChequeOcorenciaCheque is an object representing the database table.
type ChequeOcorenciaCheque struct {
	ChqrecID             int               `boil:"chqrec_id" json:"chqrec_id" toml:"chqrec_id" yaml:"chqrec_id"`
	ChqochItem           int               `boil:"chqoch_item" json:"chqoch_item" toml:"chqoch_item" yaml:"chqoch_item"`
	OchID                int               `boil:"och_id" json:"och_id" toml:"och_id" yaml:"och_id"`
	RocID                null.Int          `boil:"roc_id" json:"roc_id,omitempty" toml:"roc_id" yaml:"roc_id,omitempty"`
	ChqochDataocorencia  time.Time         `boil:"chqoch_dataocorencia" json:"chqoch_dataocorencia" toml:"chqoch_dataocorencia" yaml:"chqoch_dataocorencia"`
	ChqochObservacao     null.String       `boil:"chqoch_observacao" json:"chqoch_observacao,omitempty" toml:"chqoch_observacao" yaml:"chqoch_observacao,omitempty"`
	ChqochValorocorencia types.NullDecimal `boil:"chqoch_valorocorencia" json:"chqoch_valorocorencia,omitempty" toml:"chqoch_valorocorencia" yaml:"chqoch_valorocorencia,omitempty"`
	ChqochValorjuros     types.NullDecimal `boil:"chqoch_valorjuros" json:"chqoch_valorjuros,omitempty" toml:"chqoch_valorjuros" yaml:"chqoch_valorjuros,omitempty"`
	FrmpgtoID            null.Int          `boil:"frmpgto_id" json:"frmpgto_id,omitempty" toml:"frmpgto_id" yaml:"frmpgto_id,omitempty"`

	R *chequeOcorenciaChequeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L chequeOcorenciaChequeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ChequeOcorenciaChequeColumns = struct {
	ChqrecID             string
	ChqochItem           string
	OchID                string
	RocID                string
	ChqochDataocorencia  string
	ChqochObservacao     string
	ChqochValorocorencia string
	ChqochValorjuros     string
	FrmpgtoID            string
}{
	ChqrecID:             "chqrec_id",
	ChqochItem:           "chqoch_item",
	OchID:                "och_id",
	RocID:                "roc_id",
	ChqochDataocorencia:  "chqoch_dataocorencia",
	ChqochObservacao:     "chqoch_observacao",
	ChqochValorocorencia: "chqoch_valorocorencia",
	ChqochValorjuros:     "chqoch_valorjuros",
	FrmpgtoID:            "frmpgto_id",
}

var ChequeOcorenciaChequeTableColumns = struct {
	ChqrecID             string
	ChqochItem           string
	OchID                string
	RocID                string
	ChqochDataocorencia  string
	ChqochObservacao     string
	ChqochValorocorencia string
	ChqochValorjuros     string
	FrmpgtoID            string
}{
	ChqrecID:             "cheque_ocorencia_cheque.chqrec_id",
	ChqochItem:           "cheque_ocorencia_cheque.chqoch_item",
	OchID:                "cheque_ocorencia_cheque.och_id",
	RocID:                "cheque_ocorencia_cheque.roc_id",
	ChqochDataocorencia:  "cheque_ocorencia_cheque.chqoch_dataocorencia",
	ChqochObservacao:     "cheque_ocorencia_cheque.chqoch_observacao",
	ChqochValorocorencia: "cheque_ocorencia_cheque.chqoch_valorocorencia",
	ChqochValorjuros:     "cheque_ocorencia_cheque.chqoch_valorjuros",
	FrmpgtoID:            "cheque_ocorencia_cheque.frmpgto_id",
}

// Generated where

var ChequeOcorenciaChequeWhere = struct {
	ChqrecID             whereHelperint
	ChqochItem           whereHelperint
	OchID                whereHelperint
	RocID                whereHelpernull_Int
	ChqochDataocorencia  whereHelpertime_Time
	ChqochObservacao     whereHelpernull_String
	ChqochValorocorencia whereHelpertypes_NullDecimal
	ChqochValorjuros     whereHelpertypes_NullDecimal
	FrmpgtoID            whereHelpernull_Int
}{
	ChqrecID:             whereHelperint{field: "`cheque_ocorencia_cheque`.`chqrec_id`"},
	ChqochItem:           whereHelperint{field: "`cheque_ocorencia_cheque`.`chqoch_item`"},
	OchID:                whereHelperint{field: "`cheque_ocorencia_cheque`.`och_id`"},
	RocID:                whereHelpernull_Int{field: "`cheque_ocorencia_cheque`.`roc_id`"},
	ChqochDataocorencia:  whereHelpertime_Time{field: "`cheque_ocorencia_cheque`.`chqoch_dataocorencia`"},
	ChqochObservacao:     whereHelpernull_String{field: "`cheque_ocorencia_cheque`.`chqoch_observacao`"},
	ChqochValorocorencia: whereHelpertypes_NullDecimal{field: "`cheque_ocorencia_cheque`.`chqoch_valorocorencia`"},
	ChqochValorjuros:     whereHelpertypes_NullDecimal{field: "`cheque_ocorencia_cheque`.`chqoch_valorjuros`"},
	FrmpgtoID:            whereHelpernull_Int{field: "`cheque_ocorencia_cheque`.`frmpgto_id`"},
}

// ChequeOcorenciaChequeRels is where relationship names are stored.
var ChequeOcorenciaChequeRels = struct {
	Chqrec  string
	Frmpgto string
	Och     string
	Roc     string
}{
	Chqrec:  "Chqrec",
	Frmpgto: "Frmpgto",
	Och:     "Och",
	Roc:     "Roc",
}

// chequeOcorenciaChequeR is where relationships are stored.
type chequeOcorenciaChequeR struct {
	Chqrec  *ChequesRecebido  `boil:"Chqrec" json:"Chqrec" toml:"Chqrec" yaml:"Chqrec"`
	Frmpgto *FormaPagamento   `boil:"Frmpgto" json:"Frmpgto" toml:"Frmpgto" yaml:"Frmpgto"`
	Och     *OcorenciasCheque `boil:"Och" json:"Och" toml:"Och" yaml:"Och"`
	Roc     *RotinaContabil   `boil:"Roc" json:"Roc" toml:"Roc" yaml:"Roc"`
}

// NewStruct creates a new relationship struct
func (*chequeOcorenciaChequeR) NewStruct() *chequeOcorenciaChequeR {
	return &chequeOcorenciaChequeR{}
}

func (r *chequeOcorenciaChequeR) GetChqrec() *ChequesRecebido {
	if r == nil {
		return nil
	}
	return r.Chqrec
}

func (r *chequeOcorenciaChequeR) GetFrmpgto() *FormaPagamento {
	if r == nil {
		return nil
	}
	return r.Frmpgto
}

func (r *chequeOcorenciaChequeR) GetOch() *OcorenciasCheque {
	if r == nil {
		return nil
	}
	return r.Och
}

func (r *chequeOcorenciaChequeR) GetRoc() *RotinaContabil {
	if r == nil {
		return nil
	}
	return r.Roc
}

// chequeOcorenciaChequeL is where Load methods for each relationship are stored.
type chequeOcorenciaChequeL struct{}

var (
	chequeOcorenciaChequeAllColumns            = []string{"chqrec_id", "chqoch_item", "och_id", "roc_id", "chqoch_dataocorencia", "chqoch_observacao", "chqoch_valorocorencia", "chqoch_valorjuros", "frmpgto_id"}
	chequeOcorenciaChequeColumnsWithoutDefault = []string{"chqrec_id", "chqoch_item", "och_id", "roc_id", "chqoch_dataocorencia", "chqoch_observacao", "chqoch_valorocorencia", "chqoch_valorjuros", "frmpgto_id"}
	chequeOcorenciaChequeColumnsWithDefault    = []string{}
	chequeOcorenciaChequePrimaryKeyColumns     = []string{"chqrec_id", "chqoch_item"}
	chequeOcorenciaChequeGeneratedColumns      = []string{}
)

type (
	// ChequeOcorenciaChequeSlice is an alias for a slice of pointers to ChequeOcorenciaCheque.
	// This should almost always be used instead of []ChequeOcorenciaCheque.
	ChequeOcorenciaChequeSlice []*ChequeOcorenciaCheque
	// ChequeOcorenciaChequeHook is the signature for custom ChequeOcorenciaCheque hook methods
	ChequeOcorenciaChequeHook func(context.Context, boil.ContextExecutor, *ChequeOcorenciaCheque) error

	chequeOcorenciaChequeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	chequeOcorenciaChequeType                 = reflect.TypeOf(&ChequeOcorenciaCheque{})
	chequeOcorenciaChequeMapping              = queries.MakeStructMapping(chequeOcorenciaChequeType)
	chequeOcorenciaChequePrimaryKeyMapping, _ = queries.BindMapping(chequeOcorenciaChequeType, chequeOcorenciaChequeMapping, chequeOcorenciaChequePrimaryKeyColumns)
	chequeOcorenciaChequeInsertCacheMut       sync.RWMutex
	chequeOcorenciaChequeInsertCache          = make(map[string]insertCache)
	chequeOcorenciaChequeUpdateCacheMut       sync.RWMutex
	chequeOcorenciaChequeUpdateCache          = make(map[string]updateCache)
	chequeOcorenciaChequeUpsertCacheMut       sync.RWMutex
	chequeOcorenciaChequeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var chequeOcorenciaChequeAfterSelectMu sync.Mutex
var chequeOcorenciaChequeAfterSelectHooks []ChequeOcorenciaChequeHook

var chequeOcorenciaChequeBeforeInsertMu sync.Mutex
var chequeOcorenciaChequeBeforeInsertHooks []ChequeOcorenciaChequeHook
var chequeOcorenciaChequeAfterInsertMu sync.Mutex
var chequeOcorenciaChequeAfterInsertHooks []ChequeOcorenciaChequeHook

var chequeOcorenciaChequeBeforeUpdateMu sync.Mutex
var chequeOcorenciaChequeBeforeUpdateHooks []ChequeOcorenciaChequeHook
var chequeOcorenciaChequeAfterUpdateMu sync.Mutex
var chequeOcorenciaChequeAfterUpdateHooks []ChequeOcorenciaChequeHook

var chequeOcorenciaChequeBeforeDeleteMu sync.Mutex
var chequeOcorenciaChequeBeforeDeleteHooks []ChequeOcorenciaChequeHook
var chequeOcorenciaChequeAfterDeleteMu sync.Mutex
var chequeOcorenciaChequeAfterDeleteHooks []ChequeOcorenciaChequeHook

var chequeOcorenciaChequeBeforeUpsertMu sync.Mutex
var chequeOcorenciaChequeBeforeUpsertHooks []ChequeOcorenciaChequeHook
var chequeOcorenciaChequeAfterUpsertMu sync.Mutex
var chequeOcorenciaChequeAfterUpsertHooks []ChequeOcorenciaChequeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ChequeOcorenciaCheque) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ChequeOcorenciaCheque) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ChequeOcorenciaCheque) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ChequeOcorenciaCheque) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ChequeOcorenciaCheque) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ChequeOcorenciaCheque) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ChequeOcorenciaCheque) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ChequeOcorenciaCheque) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ChequeOcorenciaCheque) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range chequeOcorenciaChequeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddChequeOcorenciaChequeHook registers your hook function for all future operations.
func AddChequeOcorenciaChequeHook(hookPoint boil.HookPoint, chequeOcorenciaChequeHook ChequeOcorenciaChequeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		chequeOcorenciaChequeAfterSelectMu.Lock()
		chequeOcorenciaChequeAfterSelectHooks = append(chequeOcorenciaChequeAfterSelectHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		chequeOcorenciaChequeBeforeInsertMu.Lock()
		chequeOcorenciaChequeBeforeInsertHooks = append(chequeOcorenciaChequeBeforeInsertHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		chequeOcorenciaChequeAfterInsertMu.Lock()
		chequeOcorenciaChequeAfterInsertHooks = append(chequeOcorenciaChequeAfterInsertHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		chequeOcorenciaChequeBeforeUpdateMu.Lock()
		chequeOcorenciaChequeBeforeUpdateHooks = append(chequeOcorenciaChequeBeforeUpdateHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		chequeOcorenciaChequeAfterUpdateMu.Lock()
		chequeOcorenciaChequeAfterUpdateHooks = append(chequeOcorenciaChequeAfterUpdateHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		chequeOcorenciaChequeBeforeDeleteMu.Lock()
		chequeOcorenciaChequeBeforeDeleteHooks = append(chequeOcorenciaChequeBeforeDeleteHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		chequeOcorenciaChequeAfterDeleteMu.Lock()
		chequeOcorenciaChequeAfterDeleteHooks = append(chequeOcorenciaChequeAfterDeleteHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		chequeOcorenciaChequeBeforeUpsertMu.Lock()
		chequeOcorenciaChequeBeforeUpsertHooks = append(chequeOcorenciaChequeBeforeUpsertHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		chequeOcorenciaChequeAfterUpsertMu.Lock()
		chequeOcorenciaChequeAfterUpsertHooks = append(chequeOcorenciaChequeAfterUpsertHooks, chequeOcorenciaChequeHook)
		chequeOcorenciaChequeAfterUpsertMu.Unlock()
	}
}

// One returns a single chequeOcorenciaCheque record from the query.
func (q chequeOcorenciaChequeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ChequeOcorenciaCheque, error) {
	o := &ChequeOcorenciaCheque{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cheque_ocorencia_cheque")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ChequeOcorenciaCheque records from the query.
func (q chequeOcorenciaChequeQuery) All(ctx context.Context, exec boil.ContextExecutor) (ChequeOcorenciaChequeSlice, error) {
	var o []*ChequeOcorenciaCheque

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ChequeOcorenciaCheque slice")
	}

	if len(chequeOcorenciaChequeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ChequeOcorenciaCheque records in the query.
func (q chequeOcorenciaChequeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cheque_ocorencia_cheque rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q chequeOcorenciaChequeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cheque_ocorencia_cheque exists")
	}

	return count > 0, nil
}

// Chqrec pointed to by the foreign key.
func (o *ChequeOcorenciaCheque) Chqrec(mods ...qm.QueryMod) chequesRecebidoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`chqrec_id` = ?", o.ChqrecID),
	}

	queryMods = append(queryMods, mods...)

	return ChequesRecebidos(queryMods...)
}

// Frmpgto pointed to by the foreign key.
func (o *ChequeOcorenciaCheque) Frmpgto(mods ...qm.QueryMod) formaPagamentoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`frmpgto_id` = ?", o.FrmpgtoID),
	}

	queryMods = append(queryMods, mods...)

	return FormaPagamentos(queryMods...)
}

// Och pointed to by the foreign key.
func (o *ChequeOcorenciaCheque) Och(mods ...qm.QueryMod) ocorenciasChequeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`och_id` = ?", o.OchID),
	}

	queryMods = append(queryMods, mods...)

	return OcorenciasCheques(queryMods...)
}

// Roc pointed to by the foreign key.
func (o *ChequeOcorenciaCheque) Roc(mods ...qm.QueryMod) rotinaContabilQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`roc_id` = ?", o.RocID),
	}

	queryMods = append(queryMods, mods...)

	return RotinaContabils(queryMods...)
}

// LoadChqrec allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequeOcorenciaChequeL) LoadChqrec(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequeOcorenciaCheque interface{}, mods queries.Applicator) error {
	var slice []*ChequeOcorenciaCheque
	var object *ChequeOcorenciaCheque

	if singular {
		var ok bool
		object, ok = maybeChequeOcorenciaCheque.(*ChequeOcorenciaCheque)
		if !ok {
			object = new(ChequeOcorenciaCheque)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequeOcorenciaCheque)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequeOcorenciaCheque))
			}
		}
	} else {
		s, ok := maybeChequeOcorenciaCheque.(*[]*ChequeOcorenciaCheque)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequeOcorenciaCheque)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequeOcorenciaCheque))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequeOcorenciaChequeR{}
		}
		args[object.ChqrecID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequeOcorenciaChequeR{}
			}

			args[obj.ChqrecID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cheques_recebidos`),
		qm.WhereIn(`cheques_recebidos.chqrec_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ChequesRecebido")
	}

	var resultSlice []*ChequesRecebido
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ChequesRecebido")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for cheques_recebidos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cheques_recebidos")
	}

	if len(chequesRecebidoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Chqrec = foreign
		if foreign.R == nil {
			foreign.R = &chequesRecebidoR{}
		}
		foreign.R.ChqrecChequeOcorenciaCheques = append(foreign.R.ChqrecChequeOcorenciaCheques, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ChqrecID == foreign.ChqrecID {
				local.R.Chqrec = foreign
				if foreign.R == nil {
					foreign.R = &chequesRecebidoR{}
				}
				foreign.R.ChqrecChequeOcorenciaCheques = append(foreign.R.ChqrecChequeOcorenciaCheques, local)
				break
			}
		}
	}

	return nil
}

// LoadFrmpgto allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequeOcorenciaChequeL) LoadFrmpgto(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequeOcorenciaCheque interface{}, mods queries.Applicator) error {
	var slice []*ChequeOcorenciaCheque
	var object *ChequeOcorenciaCheque

	if singular {
		var ok bool
		object, ok = maybeChequeOcorenciaCheque.(*ChequeOcorenciaCheque)
		if !ok {
			object = new(ChequeOcorenciaCheque)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequeOcorenciaCheque)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequeOcorenciaCheque))
			}
		}
	} else {
		s, ok := maybeChequeOcorenciaCheque.(*[]*ChequeOcorenciaCheque)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequeOcorenciaCheque)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequeOcorenciaCheque))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequeOcorenciaChequeR{}
		}
		if !queries.IsNil(object.FrmpgtoID) {
			args[object.FrmpgtoID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequeOcorenciaChequeR{}
			}

			if !queries.IsNil(obj.FrmpgtoID) {
				args[obj.FrmpgtoID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`forma_pagamento`),
		qm.WhereIn(`forma_pagamento.frmpgto_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load FormaPagamento")
	}

	var resultSlice []*FormaPagamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice FormaPagamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for forma_pagamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for forma_pagamento")
	}

	if len(formaPagamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Frmpgto = foreign
		if foreign.R == nil {
			foreign.R = &formaPagamentoR{}
		}
		foreign.R.FrmpgtoChequeOcorenciaCheques = append(foreign.R.FrmpgtoChequeOcorenciaCheques, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.FrmpgtoID, foreign.FrmpgtoID) {
				local.R.Frmpgto = foreign
				if foreign.R == nil {
					foreign.R = &formaPagamentoR{}
				}
				foreign.R.FrmpgtoChequeOcorenciaCheques = append(foreign.R.FrmpgtoChequeOcorenciaCheques, local)
				break
			}
		}
	}

	return nil
}

// LoadOch allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequeOcorenciaChequeL) LoadOch(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequeOcorenciaCheque interface{}, mods queries.Applicator) error {
	var slice []*ChequeOcorenciaCheque
	var object *ChequeOcorenciaCheque

	if singular {
		var ok bool
		object, ok = maybeChequeOcorenciaCheque.(*ChequeOcorenciaCheque)
		if !ok {
			object = new(ChequeOcorenciaCheque)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequeOcorenciaCheque)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequeOcorenciaCheque))
			}
		}
	} else {
		s, ok := maybeChequeOcorenciaCheque.(*[]*ChequeOcorenciaCheque)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequeOcorenciaCheque)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequeOcorenciaCheque))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequeOcorenciaChequeR{}
		}
		args[object.OchID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequeOcorenciaChequeR{}
			}

			args[obj.OchID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ocorencias_cheques`),
		qm.WhereIn(`ocorencias_cheques.och_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load OcorenciasCheque")
	}

	var resultSlice []*OcorenciasCheque
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice OcorenciasCheque")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ocorencias_cheques")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ocorencias_cheques")
	}

	if len(ocorenciasChequeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Och = foreign
		if foreign.R == nil {
			foreign.R = &ocorenciasChequeR{}
		}
		foreign.R.OchChequeOcorenciaCheques = append(foreign.R.OchChequeOcorenciaCheques, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.OchID == foreign.OchID {
				local.R.Och = foreign
				if foreign.R == nil {
					foreign.R = &ocorenciasChequeR{}
				}
				foreign.R.OchChequeOcorenciaCheques = append(foreign.R.OchChequeOcorenciaCheques, local)
				break
			}
		}
	}

	return nil
}

// LoadRoc allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (chequeOcorenciaChequeL) LoadRoc(ctx context.Context, e boil.ContextExecutor, singular bool, maybeChequeOcorenciaCheque interface{}, mods queries.Applicator) error {
	var slice []*ChequeOcorenciaCheque
	var object *ChequeOcorenciaCheque

	if singular {
		var ok bool
		object, ok = maybeChequeOcorenciaCheque.(*ChequeOcorenciaCheque)
		if !ok {
			object = new(ChequeOcorenciaCheque)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeChequeOcorenciaCheque)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeChequeOcorenciaCheque))
			}
		}
	} else {
		s, ok := maybeChequeOcorenciaCheque.(*[]*ChequeOcorenciaCheque)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeChequeOcorenciaCheque)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeChequeOcorenciaCheque))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &chequeOcorenciaChequeR{}
		}
		if !queries.IsNil(object.RocID) {
			args[object.RocID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &chequeOcorenciaChequeR{}
			}

			if !queries.IsNil(obj.RocID) {
				args[obj.RocID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil`),
		qm.WhereIn(`rotina_contabil.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RotinaContabil")
	}

	var resultSlice []*RotinaContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RotinaContabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for rotina_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil")
	}

	if len(rotinaContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Roc = foreign
		if foreign.R == nil {
			foreign.R = &rotinaContabilR{}
		}
		foreign.R.RocChequeOcorenciaCheques = append(foreign.R.RocChequeOcorenciaCheques, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.Roc = foreign
				if foreign.R == nil {
					foreign.R = &rotinaContabilR{}
				}
				foreign.R.RocChequeOcorenciaCheques = append(foreign.R.RocChequeOcorenciaCheques, local)
				break
			}
		}
	}

	return nil
}

// SetChqrec of the chequeOcorenciaCheque to the related item.
// Sets o.R.Chqrec to related.
// Adds o to related.R.ChqrecChequeOcorenciaCheques.
func (o *ChequeOcorenciaCheque) SetChqrec(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ChequesRecebido) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheque_ocorencia_cheque` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"chqrec_id"}),
		strmangle.WhereClause("`", "`", 0, chequeOcorenciaChequePrimaryKeyColumns),
	)
	values := []interface{}{related.ChqrecID, o.ChqrecID, o.ChqochItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ChqrecID = related.ChqrecID
	if o.R == nil {
		o.R = &chequeOcorenciaChequeR{
			Chqrec: related,
		}
	} else {
		o.R.Chqrec = related
	}

	if related.R == nil {
		related.R = &chequesRecebidoR{
			ChqrecChequeOcorenciaCheques: ChequeOcorenciaChequeSlice{o},
		}
	} else {
		related.R.ChqrecChequeOcorenciaCheques = append(related.R.ChqrecChequeOcorenciaCheques, o)
	}

	return nil
}

// SetFrmpgto of the chequeOcorenciaCheque to the related item.
// Sets o.R.Frmpgto to related.
// Adds o to related.R.FrmpgtoChequeOcorenciaCheques.
func (o *ChequeOcorenciaCheque) SetFrmpgto(ctx context.Context, exec boil.ContextExecutor, insert bool, related *FormaPagamento) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheque_ocorencia_cheque` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"frmpgto_id"}),
		strmangle.WhereClause("`", "`", 0, chequeOcorenciaChequePrimaryKeyColumns),
	)
	values := []interface{}{related.FrmpgtoID, o.ChqrecID, o.ChqochItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.FrmpgtoID, related.FrmpgtoID)
	if o.R == nil {
		o.R = &chequeOcorenciaChequeR{
			Frmpgto: related,
		}
	} else {
		o.R.Frmpgto = related
	}

	if related.R == nil {
		related.R = &formaPagamentoR{
			FrmpgtoChequeOcorenciaCheques: ChequeOcorenciaChequeSlice{o},
		}
	} else {
		related.R.FrmpgtoChequeOcorenciaCheques = append(related.R.FrmpgtoChequeOcorenciaCheques, o)
	}

	return nil
}

// RemoveFrmpgto relationship.
// Sets o.R.Frmpgto to nil.
// Removes o from all passed in related items' relationships struct.
func (o *ChequeOcorenciaCheque) RemoveFrmpgto(ctx context.Context, exec boil.ContextExecutor, related *FormaPagamento) error {
	var err error

	queries.SetScanner(&o.FrmpgtoID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("frmpgto_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Frmpgto = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.FrmpgtoChequeOcorenciaCheques {
		if queries.Equal(o.FrmpgtoID, ri.FrmpgtoID) {
			continue
		}

		ln := len(related.R.FrmpgtoChequeOcorenciaCheques)
		if ln > 1 && i < ln-1 {
			related.R.FrmpgtoChequeOcorenciaCheques[i] = related.R.FrmpgtoChequeOcorenciaCheques[ln-1]
		}
		related.R.FrmpgtoChequeOcorenciaCheques = related.R.FrmpgtoChequeOcorenciaCheques[:ln-1]
		break
	}
	return nil
}

// SetOch of the chequeOcorenciaCheque to the related item.
// Sets o.R.Och to related.
// Adds o to related.R.OchChequeOcorenciaCheques.
func (o *ChequeOcorenciaCheque) SetOch(ctx context.Context, exec boil.ContextExecutor, insert bool, related *OcorenciasCheque) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheque_ocorencia_cheque` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"och_id"}),
		strmangle.WhereClause("`", "`", 0, chequeOcorenciaChequePrimaryKeyColumns),
	)
	values := []interface{}{related.OchID, o.ChqrecID, o.ChqochItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.OchID = related.OchID
	if o.R == nil {
		o.R = &chequeOcorenciaChequeR{
			Och: related,
		}
	} else {
		o.R.Och = related
	}

	if related.R == nil {
		related.R = &ocorenciasChequeR{
			OchChequeOcorenciaCheques: ChequeOcorenciaChequeSlice{o},
		}
	} else {
		related.R.OchChequeOcorenciaCheques = append(related.R.OchChequeOcorenciaCheques, o)
	}

	return nil
}

// SetRoc of the chequeOcorenciaCheque to the related item.
// Sets o.R.Roc to related.
// Adds o to related.R.RocChequeOcorenciaCheques.
func (o *ChequeOcorenciaCheque) SetRoc(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RotinaContabil) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `cheque_ocorencia_cheque` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
		strmangle.WhereClause("`", "`", 0, chequeOcorenciaChequePrimaryKeyColumns),
	)
	values := []interface{}{related.RocID, o.ChqrecID, o.ChqochItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RocID, related.RocID)
	if o.R == nil {
		o.R = &chequeOcorenciaChequeR{
			Roc: related,
		}
	} else {
		o.R.Roc = related
	}

	if related.R == nil {
		related.R = &rotinaContabilR{
			RocChequeOcorenciaCheques: ChequeOcorenciaChequeSlice{o},
		}
	} else {
		related.R.RocChequeOcorenciaCheques = append(related.R.RocChequeOcorenciaCheques, o)
	}

	return nil
}

// RemoveRoc relationship.
// Sets o.R.Roc to nil.
// Removes o from all passed in related items' relationships struct.
func (o *ChequeOcorenciaCheque) RemoveRoc(ctx context.Context, exec boil.ContextExecutor, related *RotinaContabil) error {
	var err error

	queries.SetScanner(&o.RocID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Roc = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RocChequeOcorenciaCheques {
		if queries.Equal(o.RocID, ri.RocID) {
			continue
		}

		ln := len(related.R.RocChequeOcorenciaCheques)
		if ln > 1 && i < ln-1 {
			related.R.RocChequeOcorenciaCheques[i] = related.R.RocChequeOcorenciaCheques[ln-1]
		}
		related.R.RocChequeOcorenciaCheques = related.R.RocChequeOcorenciaCheques[:ln-1]
		break
	}
	return nil
}

// ChequeOcorenciaCheques retrieves all the records using an executor.
func ChequeOcorenciaCheques(mods ...qm.QueryMod) chequeOcorenciaChequeQuery {
	mods = append(mods, qm.From("`cheque_ocorencia_cheque`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`cheque_ocorencia_cheque`.*"})
	}

	return chequeOcorenciaChequeQuery{q}
}

// FindChequeOcorenciaCheque retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindChequeOcorenciaCheque(ctx context.Context, exec boil.ContextExecutor, chqrecID int, chqochItem int, selectCols ...string) (*ChequeOcorenciaCheque, error) {
	chequeOcorenciaChequeObj := &ChequeOcorenciaCheque{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cheque_ocorencia_cheque` where `chqrec_id`=? AND `chqoch_item`=?", sel,
	)

	q := queries.Raw(query, chqrecID, chqochItem)

	err := q.Bind(ctx, exec, chequeOcorenciaChequeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cheque_ocorencia_cheque")
	}

	if err = chequeOcorenciaChequeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return chequeOcorenciaChequeObj, err
	}

	return chequeOcorenciaChequeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ChequeOcorenciaCheque) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cheque_ocorencia_cheque provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(chequeOcorenciaChequeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	chequeOcorenciaChequeInsertCacheMut.RLock()
	cache, cached := chequeOcorenciaChequeInsertCache[key]
	chequeOcorenciaChequeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			chequeOcorenciaChequeAllColumns,
			chequeOcorenciaChequeColumnsWithDefault,
			chequeOcorenciaChequeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(chequeOcorenciaChequeType, chequeOcorenciaChequeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(chequeOcorenciaChequeType, chequeOcorenciaChequeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cheque_ocorencia_cheque` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cheque_ocorencia_cheque` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cheque_ocorencia_cheque` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, chequeOcorenciaChequePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cheque_ocorencia_cheque")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ChqrecID,
		o.ChqochItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cheque_ocorencia_cheque")
	}

CacheNoHooks:
	if !cached {
		chequeOcorenciaChequeInsertCacheMut.Lock()
		chequeOcorenciaChequeInsertCache[key] = cache
		chequeOcorenciaChequeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ChequeOcorenciaCheque.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ChequeOcorenciaCheque) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	chequeOcorenciaChequeUpdateCacheMut.RLock()
	cache, cached := chequeOcorenciaChequeUpdateCache[key]
	chequeOcorenciaChequeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			chequeOcorenciaChequeAllColumns,
			chequeOcorenciaChequePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cheque_ocorencia_cheque, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cheque_ocorencia_cheque` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, chequeOcorenciaChequePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(chequeOcorenciaChequeType, chequeOcorenciaChequeMapping, append(wl, chequeOcorenciaChequePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cheque_ocorencia_cheque row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cheque_ocorencia_cheque")
	}

	if !cached {
		chequeOcorenciaChequeUpdateCacheMut.Lock()
		chequeOcorenciaChequeUpdateCache[key] = cache
		chequeOcorenciaChequeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q chequeOcorenciaChequeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cheque_ocorencia_cheque")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cheque_ocorencia_cheque")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ChequeOcorenciaChequeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequeOcorenciaChequePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cheque_ocorencia_cheque` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequeOcorenciaChequePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in chequeOcorenciaCheque slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all chequeOcorenciaCheque")
	}
	return rowsAff, nil
}

var mySQLChequeOcorenciaChequeUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ChequeOcorenciaCheque) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cheque_ocorencia_cheque provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(chequeOcorenciaChequeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLChequeOcorenciaChequeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	chequeOcorenciaChequeUpsertCacheMut.RLock()
	cache, cached := chequeOcorenciaChequeUpsertCache[key]
	chequeOcorenciaChequeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			chequeOcorenciaChequeAllColumns,
			chequeOcorenciaChequeColumnsWithDefault,
			chequeOcorenciaChequeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			chequeOcorenciaChequeAllColumns,
			chequeOcorenciaChequePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cheque_ocorencia_cheque, could not build update column list")
		}

		ret := strmangle.SetComplement(chequeOcorenciaChequeAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`cheque_ocorencia_cheque`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cheque_ocorencia_cheque` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(chequeOcorenciaChequeType, chequeOcorenciaChequeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(chequeOcorenciaChequeType, chequeOcorenciaChequeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cheque_ocorencia_cheque")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(chequeOcorenciaChequeType, chequeOcorenciaChequeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cheque_ocorencia_cheque")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cheque_ocorencia_cheque")
	}

CacheNoHooks:
	if !cached {
		chequeOcorenciaChequeUpsertCacheMut.Lock()
		chequeOcorenciaChequeUpsertCache[key] = cache
		chequeOcorenciaChequeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ChequeOcorenciaCheque record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ChequeOcorenciaCheque) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ChequeOcorenciaCheque provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), chequeOcorenciaChequePrimaryKeyMapping)
	sql := "DELETE FROM `cheque_ocorencia_cheque` WHERE `chqrec_id`=? AND `chqoch_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cheque_ocorencia_cheque")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cheque_ocorencia_cheque")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q chequeOcorenciaChequeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no chequeOcorenciaChequeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cheque_ocorencia_cheque")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cheque_ocorencia_cheque")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ChequeOcorenciaChequeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(chequeOcorenciaChequeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequeOcorenciaChequePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cheque_ocorencia_cheque` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequeOcorenciaChequePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from chequeOcorenciaCheque slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cheque_ocorencia_cheque")
	}

	if len(chequeOcorenciaChequeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ChequeOcorenciaCheque) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindChequeOcorenciaCheque(ctx, exec, o.ChqrecID, o.ChqochItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ChequeOcorenciaChequeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ChequeOcorenciaChequeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), chequeOcorenciaChequePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cheque_ocorencia_cheque`.* FROM `cheque_ocorencia_cheque` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, chequeOcorenciaChequePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ChequeOcorenciaChequeSlice")
	}

	*o = slice

	return nil
}

// ChequeOcorenciaChequeExists checks if the ChequeOcorenciaCheque row exists.
func ChequeOcorenciaChequeExists(ctx context.Context, exec boil.ContextExecutor, chqrecID int, chqochItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cheque_ocorencia_cheque` where `chqrec_id`=? AND `chqoch_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, chqrecID, chqochItem)
	}
	row := exec.QueryRowContext(ctx, sql, chqrecID, chqochItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cheque_ocorencia_cheque exists")
	}

	return exists, nil
}

// Exists checks if the ChequeOcorenciaCheque row exists.
func (o *ChequeOcorenciaCheque) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ChequeOcorenciaChequeExists(ctx, exec, o.ChqrecID, o.ChqochItem)
}
