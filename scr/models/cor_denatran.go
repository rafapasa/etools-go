// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CorDenatran is an object representing the database table.
type CorDenatran struct {
	CordCodigo    string `boil:"cord_codigo" json:"cord_codigo" toml:"cord_codigo" yaml:"cord_codigo"`
	CordDescricao string `boil:"cord_descricao" json:"cord_descricao" toml:"cord_descricao" yaml:"cord_descricao"`

	R *corDenatranR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L corDenatranL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CorDenatranColumns = struct {
	CordCodigo    string
	CordDescricao string
}{
	CordCodigo:    "cord_codigo",
	CordDescricao: "cord_descricao",
}

var CorDenatranTableColumns = struct {
	CordCodigo    string
	CordDescricao string
}{
	CordCodigo:    "cor_denatran.cord_codigo",
	CordDescricao: "cor_denatran.cord_descricao",
}

// Generated where

var CorDenatranWhere = struct {
	CordCodigo    whereHelperstring
	CordDescricao whereHelperstring
}{
	CordCodigo:    whereHelperstring{field: "`cor_denatran`.`cord_codigo`"},
	CordDescricao: whereHelperstring{field: "`cor_denatran`.`cord_descricao`"},
}

// CorDenatranRels is where relationship names are stored.
var CorDenatranRels = struct {
	CordCodigoNotaFiscalItemVeiculos string
}{
	CordCodigoNotaFiscalItemVeiculos: "CordCodigoNotaFiscalItemVeiculos",
}

// corDenatranR is where relationships are stored.
type corDenatranR struct {
	CordCodigoNotaFiscalItemVeiculos NotaFiscalItemVeiculoSlice `boil:"CordCodigoNotaFiscalItemVeiculos" json:"CordCodigoNotaFiscalItemVeiculos" toml:"CordCodigoNotaFiscalItemVeiculos" yaml:"CordCodigoNotaFiscalItemVeiculos"`
}

// NewStruct creates a new relationship struct
func (*corDenatranR) NewStruct() *corDenatranR {
	return &corDenatranR{}
}

func (r *corDenatranR) GetCordCodigoNotaFiscalItemVeiculos() NotaFiscalItemVeiculoSlice {
	if r == nil {
		return nil
	}
	return r.CordCodigoNotaFiscalItemVeiculos
}

// corDenatranL is where Load methods for each relationship are stored.
type corDenatranL struct{}

var (
	corDenatranAllColumns            = []string{"cord_codigo", "cord_descricao"}
	corDenatranColumnsWithoutDefault = []string{"cord_codigo", "cord_descricao"}
	corDenatranColumnsWithDefault    = []string{}
	corDenatranPrimaryKeyColumns     = []string{"cord_codigo"}
	corDenatranGeneratedColumns      = []string{}
)

type (
	// CorDenatranSlice is an alias for a slice of pointers to CorDenatran.
	// This should almost always be used instead of []CorDenatran.
	CorDenatranSlice []*CorDenatran
	// CorDenatranHook is the signature for custom CorDenatran hook methods
	CorDenatranHook func(context.Context, boil.ContextExecutor, *CorDenatran) error

	corDenatranQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	corDenatranType                 = reflect.TypeOf(&CorDenatran{})
	corDenatranMapping              = queries.MakeStructMapping(corDenatranType)
	corDenatranPrimaryKeyMapping, _ = queries.BindMapping(corDenatranType, corDenatranMapping, corDenatranPrimaryKeyColumns)
	corDenatranInsertCacheMut       sync.RWMutex
	corDenatranInsertCache          = make(map[string]insertCache)
	corDenatranUpdateCacheMut       sync.RWMutex
	corDenatranUpdateCache          = make(map[string]updateCache)
	corDenatranUpsertCacheMut       sync.RWMutex
	corDenatranUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var corDenatranAfterSelectMu sync.Mutex
var corDenatranAfterSelectHooks []CorDenatranHook

var corDenatranBeforeInsertMu sync.Mutex
var corDenatranBeforeInsertHooks []CorDenatranHook
var corDenatranAfterInsertMu sync.Mutex
var corDenatranAfterInsertHooks []CorDenatranHook

var corDenatranBeforeUpdateMu sync.Mutex
var corDenatranBeforeUpdateHooks []CorDenatranHook
var corDenatranAfterUpdateMu sync.Mutex
var corDenatranAfterUpdateHooks []CorDenatranHook

var corDenatranBeforeDeleteMu sync.Mutex
var corDenatranBeforeDeleteHooks []CorDenatranHook
var corDenatranAfterDeleteMu sync.Mutex
var corDenatranAfterDeleteHooks []CorDenatranHook

var corDenatranBeforeUpsertMu sync.Mutex
var corDenatranBeforeUpsertHooks []CorDenatranHook
var corDenatranAfterUpsertMu sync.Mutex
var corDenatranAfterUpsertHooks []CorDenatranHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CorDenatran) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CorDenatran) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CorDenatran) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CorDenatran) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CorDenatran) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CorDenatran) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CorDenatran) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CorDenatran) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CorDenatran) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range corDenatranAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCorDenatranHook registers your hook function for all future operations.
func AddCorDenatranHook(hookPoint boil.HookPoint, corDenatranHook CorDenatranHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		corDenatranAfterSelectMu.Lock()
		corDenatranAfterSelectHooks = append(corDenatranAfterSelectHooks, corDenatranHook)
		corDenatranAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		corDenatranBeforeInsertMu.Lock()
		corDenatranBeforeInsertHooks = append(corDenatranBeforeInsertHooks, corDenatranHook)
		corDenatranBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		corDenatranAfterInsertMu.Lock()
		corDenatranAfterInsertHooks = append(corDenatranAfterInsertHooks, corDenatranHook)
		corDenatranAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		corDenatranBeforeUpdateMu.Lock()
		corDenatranBeforeUpdateHooks = append(corDenatranBeforeUpdateHooks, corDenatranHook)
		corDenatranBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		corDenatranAfterUpdateMu.Lock()
		corDenatranAfterUpdateHooks = append(corDenatranAfterUpdateHooks, corDenatranHook)
		corDenatranAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		corDenatranBeforeDeleteMu.Lock()
		corDenatranBeforeDeleteHooks = append(corDenatranBeforeDeleteHooks, corDenatranHook)
		corDenatranBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		corDenatranAfterDeleteMu.Lock()
		corDenatranAfterDeleteHooks = append(corDenatranAfterDeleteHooks, corDenatranHook)
		corDenatranAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		corDenatranBeforeUpsertMu.Lock()
		corDenatranBeforeUpsertHooks = append(corDenatranBeforeUpsertHooks, corDenatranHook)
		corDenatranBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		corDenatranAfterUpsertMu.Lock()
		corDenatranAfterUpsertHooks = append(corDenatranAfterUpsertHooks, corDenatranHook)
		corDenatranAfterUpsertMu.Unlock()
	}
}

// One returns a single corDenatran record from the query.
func (q corDenatranQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CorDenatran, error) {
	o := &CorDenatran{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cor_denatran")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CorDenatran records from the query.
func (q corDenatranQuery) All(ctx context.Context, exec boil.ContextExecutor) (CorDenatranSlice, error) {
	var o []*CorDenatran

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CorDenatran slice")
	}

	if len(corDenatranAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CorDenatran records in the query.
func (q corDenatranQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cor_denatran rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q corDenatranQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cor_denatran exists")
	}

	return count > 0, nil
}

// CordCodigoNotaFiscalItemVeiculos retrieves all the nota_fiscal_item_veiculo's NotaFiscalItemVeiculos with an executor via cord_codigo column.
func (o *CorDenatran) CordCodigoNotaFiscalItemVeiculos(mods ...qm.QueryMod) notaFiscalItemVeiculoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal_item_veiculo`.`cord_codigo`=?", o.CordCodigo),
	)

	return NotaFiscalItemVeiculos(queryMods...)
}

// LoadCordCodigoNotaFiscalItemVeiculos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (corDenatranL) LoadCordCodigoNotaFiscalItemVeiculos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCorDenatran interface{}, mods queries.Applicator) error {
	var slice []*CorDenatran
	var object *CorDenatran

	if singular {
		var ok bool
		object, ok = maybeCorDenatran.(*CorDenatran)
		if !ok {
			object = new(CorDenatran)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCorDenatran)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCorDenatran))
			}
		}
	} else {
		s, ok := maybeCorDenatran.(*[]*CorDenatran)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCorDenatran)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCorDenatran))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &corDenatranR{}
		}
		args[object.CordCodigo] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &corDenatranR{}
			}
			args[obj.CordCodigo] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal_item_veiculo`),
		qm.WhereIn(`nota_fiscal_item_veiculo.cord_codigo in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal_item_veiculo")
	}

	var resultSlice []*NotaFiscalItemVeiculo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal_item_veiculo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal_item_veiculo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal_item_veiculo")
	}

	if len(notaFiscalItemVeiculoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CordCodigoNotaFiscalItemVeiculos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalItemVeiculoR{}
			}
			foreign.R.CordCodigoCorDenatran = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.CordCodigo == foreign.CordCodigo {
				local.R.CordCodigoNotaFiscalItemVeiculos = append(local.R.CordCodigoNotaFiscalItemVeiculos, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalItemVeiculoR{}
				}
				foreign.R.CordCodigoCorDenatran = local
				break
			}
		}
	}

	return nil
}

// AddCordCodigoNotaFiscalItemVeiculos adds the given related objects to the existing relationships
// of the cor_denatran, optionally inserting them as new records.
// Appends related to o.R.CordCodigoNotaFiscalItemVeiculos.
// Sets related.R.CordCodigoCorDenatran appropriately.
func (o *CorDenatran) AddCordCodigoNotaFiscalItemVeiculos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalItemVeiculo) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CordCodigo = o.CordCodigo
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal_item_veiculo` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"cord_codigo"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalItemVeiculoPrimaryKeyColumns),
			)
			values := []interface{}{o.CordCodigo, rel.NTFID, rel.NfiItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CordCodigo = o.CordCodigo
		}
	}

	if o.R == nil {
		o.R = &corDenatranR{
			CordCodigoNotaFiscalItemVeiculos: related,
		}
	} else {
		o.R.CordCodigoNotaFiscalItemVeiculos = append(o.R.CordCodigoNotaFiscalItemVeiculos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalItemVeiculoR{
				CordCodigoCorDenatran: o,
			}
		} else {
			rel.R.CordCodigoCorDenatran = o
		}
	}
	return nil
}

// CorDenatrans retrieves all the records using an executor.
func CorDenatrans(mods ...qm.QueryMod) corDenatranQuery {
	mods = append(mods, qm.From("`cor_denatran`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`cor_denatran`.*"})
	}

	return corDenatranQuery{q}
}

// FindCorDenatran retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCorDenatran(ctx context.Context, exec boil.ContextExecutor, cordCodigo string, selectCols ...string) (*CorDenatran, error) {
	corDenatranObj := &CorDenatran{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cor_denatran` where `cord_codigo`=?", sel,
	)

	q := queries.Raw(query, cordCodigo)

	err := q.Bind(ctx, exec, corDenatranObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cor_denatran")
	}

	if err = corDenatranObj.doAfterSelectHooks(ctx, exec); err != nil {
		return corDenatranObj, err
	}

	return corDenatranObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CorDenatran) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cor_denatran provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(corDenatranColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	corDenatranInsertCacheMut.RLock()
	cache, cached := corDenatranInsertCache[key]
	corDenatranInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			corDenatranAllColumns,
			corDenatranColumnsWithDefault,
			corDenatranColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(corDenatranType, corDenatranMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(corDenatranType, corDenatranMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cor_denatran` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cor_denatran` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cor_denatran` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, corDenatranPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cor_denatran")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CordCodigo,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cor_denatran")
	}

CacheNoHooks:
	if !cached {
		corDenatranInsertCacheMut.Lock()
		corDenatranInsertCache[key] = cache
		corDenatranInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CorDenatran.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CorDenatran) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	corDenatranUpdateCacheMut.RLock()
	cache, cached := corDenatranUpdateCache[key]
	corDenatranUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			corDenatranAllColumns,
			corDenatranPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cor_denatran, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cor_denatran` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, corDenatranPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(corDenatranType, corDenatranMapping, append(wl, corDenatranPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cor_denatran row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cor_denatran")
	}

	if !cached {
		corDenatranUpdateCacheMut.Lock()
		corDenatranUpdateCache[key] = cache
		corDenatranUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q corDenatranQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cor_denatran")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cor_denatran")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CorDenatranSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), corDenatranPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cor_denatran` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, corDenatranPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in corDenatran slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all corDenatran")
	}
	return rowsAff, nil
}

var mySQLCorDenatranUniqueColumns = []string{
	"cord_codigo",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CorDenatran) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cor_denatran provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(corDenatranColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCorDenatranUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	corDenatranUpsertCacheMut.RLock()
	cache, cached := corDenatranUpsertCache[key]
	corDenatranUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			corDenatranAllColumns,
			corDenatranColumnsWithDefault,
			corDenatranColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			corDenatranAllColumns,
			corDenatranPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cor_denatran, could not build update column list")
		}

		ret := strmangle.SetComplement(corDenatranAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`cor_denatran`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cor_denatran` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(corDenatranType, corDenatranMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(corDenatranType, corDenatranMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cor_denatran")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(corDenatranType, corDenatranMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cor_denatran")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cor_denatran")
	}

CacheNoHooks:
	if !cached {
		corDenatranUpsertCacheMut.Lock()
		corDenatranUpsertCache[key] = cache
		corDenatranUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CorDenatran record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CorDenatran) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CorDenatran provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), corDenatranPrimaryKeyMapping)
	sql := "DELETE FROM `cor_denatran` WHERE `cord_codigo`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cor_denatran")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cor_denatran")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q corDenatranQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no corDenatranQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cor_denatran")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cor_denatran")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CorDenatranSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(corDenatranBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), corDenatranPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cor_denatran` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, corDenatranPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from corDenatran slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cor_denatran")
	}

	if len(corDenatranAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CorDenatran) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCorDenatran(ctx, exec, o.CordCodigo)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CorDenatranSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CorDenatranSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), corDenatranPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cor_denatran`.* FROM `cor_denatran` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, corDenatranPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CorDenatranSlice")
	}

	*o = slice

	return nil
}

// CorDenatranExists checks if the CorDenatran row exists.
func CorDenatranExists(ctx context.Context, exec boil.ContextExecutor, cordCodigo string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cor_denatran` where `cord_codigo`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cordCodigo)
	}
	row := exec.QueryRowContext(ctx, sql, cordCodigo)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cor_denatran exists")
	}

	return exists, nil
}

// Exists checks if the CorDenatran row exists.
func (o *CorDenatran) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CorDenatranExists(ctx, exec, o.CordCodigo)
}
