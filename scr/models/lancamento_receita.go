// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// LancamentoReceitum is an object representing the database table.
type LancamentoReceitum struct {
	LrecID        int           `boil:"lrec_id" json:"lrec_id" toml:"lrec_id" yaml:"lrec_id"`
	RecID         null.Int      `boil:"rec_id" json:"rec_id,omitempty" toml:"rec_id" yaml:"rec_id,omitempty"`
	LrecHistorico string        `boil:"lrec_historico" json:"lrec_historico" toml:"lrec_historico" yaml:"lrec_historico"`
	LrecData      time.Time     `boil:"lrec_data" json:"lrec_data" toml:"lrec_data" yaml:"lrec_data"`
	LrecValor     types.Decimal `boil:"lrec_valor" json:"lrec_valor" toml:"lrec_valor" yaml:"lrec_valor"`
	TitID         null.Int      `boil:"tit_id" json:"tit_id,omitempty" toml:"tit_id" yaml:"tit_id,omitempty"`
	TBXItem       null.Int      `boil:"tbx_item" json:"tbx_item,omitempty" toml:"tbx_item" yaml:"tbx_item,omitempty"`
	// 0-prevista  1-realizada
	LrecSituacao int `boil:"lrec_situacao" json:"lrec_situacao" toml:"lrec_situacao" yaml:"lrec_situacao"`

	R *lancamentoReceitumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lancamentoReceitumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var LancamentoReceitumColumns = struct {
	LrecID        string
	RecID         string
	LrecHistorico string
	LrecData      string
	LrecValor     string
	TitID         string
	TBXItem       string
	LrecSituacao  string
}{
	LrecID:        "lrec_id",
	RecID:         "rec_id",
	LrecHistorico: "lrec_historico",
	LrecData:      "lrec_data",
	LrecValor:     "lrec_valor",
	TitID:         "tit_id",
	TBXItem:       "tbx_item",
	LrecSituacao:  "lrec_situacao",
}

var LancamentoReceitumTableColumns = struct {
	LrecID        string
	RecID         string
	LrecHistorico string
	LrecData      string
	LrecValor     string
	TitID         string
	TBXItem       string
	LrecSituacao  string
}{
	LrecID:        "lancamento_receita.lrec_id",
	RecID:         "lancamento_receita.rec_id",
	LrecHistorico: "lancamento_receita.lrec_historico",
	LrecData:      "lancamento_receita.lrec_data",
	LrecValor:     "lancamento_receita.lrec_valor",
	TitID:         "lancamento_receita.tit_id",
	TBXItem:       "lancamento_receita.tbx_item",
	LrecSituacao:  "lancamento_receita.lrec_situacao",
}

// Generated where

var LancamentoReceitumWhere = struct {
	LrecID        whereHelperint
	RecID         whereHelpernull_Int
	LrecHistorico whereHelperstring
	LrecData      whereHelpertime_Time
	LrecValor     whereHelpertypes_Decimal
	TitID         whereHelpernull_Int
	TBXItem       whereHelpernull_Int
	LrecSituacao  whereHelperint
}{
	LrecID:        whereHelperint{field: "`lancamento_receita`.`lrec_id`"},
	RecID:         whereHelpernull_Int{field: "`lancamento_receita`.`rec_id`"},
	LrecHistorico: whereHelperstring{field: "`lancamento_receita`.`lrec_historico`"},
	LrecData:      whereHelpertime_Time{field: "`lancamento_receita`.`lrec_data`"},
	LrecValor:     whereHelpertypes_Decimal{field: "`lancamento_receita`.`lrec_valor`"},
	TitID:         whereHelpernull_Int{field: "`lancamento_receita`.`tit_id`"},
	TBXItem:       whereHelpernull_Int{field: "`lancamento_receita`.`tbx_item`"},
	LrecSituacao:  whereHelperint{field: "`lancamento_receita`.`lrec_situacao`"},
}

// LancamentoReceitumRels is where relationship names are stored.
var LancamentoReceitumRels = struct {
	Rec string
}{
	Rec: "Rec",
}

// lancamentoReceitumR is where relationships are stored.
type lancamentoReceitumR struct {
	Rec *Receitas `boil:"Rec" json:"Rec" toml:"Rec" yaml:"Rec"`
}

// NewStruct creates a new relationship struct
func (*lancamentoReceitumR) NewStruct() *lancamentoReceitumR {
	return &lancamentoReceitumR{}
}

func (r *lancamentoReceitumR) GetRec() *Receitas {
	if r == nil {
		return nil
	}
	return r.Rec
}

// lancamentoReceitumL is where Load methods for each relationship are stored.
type lancamentoReceitumL struct{}

var (
	lancamentoReceitumAllColumns            = []string{"lrec_id", "rec_id", "lrec_historico", "lrec_data", "lrec_valor", "tit_id", "tbx_item", "lrec_situacao"}
	lancamentoReceitumColumnsWithoutDefault = []string{"lrec_id", "rec_id", "lrec_historico", "lrec_data", "lrec_valor", "tit_id", "tbx_item"}
	lancamentoReceitumColumnsWithDefault    = []string{"lrec_situacao"}
	lancamentoReceitumPrimaryKeyColumns     = []string{"lrec_id"}
	lancamentoReceitumGeneratedColumns      = []string{}
)

type (
	// LancamentoReceitumSlice is an alias for a slice of pointers to LancamentoReceitum.
	// This should almost always be used instead of []LancamentoReceitum.
	LancamentoReceitumSlice []*LancamentoReceitum
	// LancamentoReceitumHook is the signature for custom LancamentoReceitum hook methods
	LancamentoReceitumHook func(context.Context, boil.ContextExecutor, *LancamentoReceitum) error

	lancamentoReceitumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lancamentoReceitumType                 = reflect.TypeOf(&LancamentoReceitum{})
	lancamentoReceitumMapping              = queries.MakeStructMapping(lancamentoReceitumType)
	lancamentoReceitumPrimaryKeyMapping, _ = queries.BindMapping(lancamentoReceitumType, lancamentoReceitumMapping, lancamentoReceitumPrimaryKeyColumns)
	lancamentoReceitumInsertCacheMut       sync.RWMutex
	lancamentoReceitumInsertCache          = make(map[string]insertCache)
	lancamentoReceitumUpdateCacheMut       sync.RWMutex
	lancamentoReceitumUpdateCache          = make(map[string]updateCache)
	lancamentoReceitumUpsertCacheMut       sync.RWMutex
	lancamentoReceitumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var lancamentoReceitumAfterSelectMu sync.Mutex
var lancamentoReceitumAfterSelectHooks []LancamentoReceitumHook

var lancamentoReceitumBeforeInsertMu sync.Mutex
var lancamentoReceitumBeforeInsertHooks []LancamentoReceitumHook
var lancamentoReceitumAfterInsertMu sync.Mutex
var lancamentoReceitumAfterInsertHooks []LancamentoReceitumHook

var lancamentoReceitumBeforeUpdateMu sync.Mutex
var lancamentoReceitumBeforeUpdateHooks []LancamentoReceitumHook
var lancamentoReceitumAfterUpdateMu sync.Mutex
var lancamentoReceitumAfterUpdateHooks []LancamentoReceitumHook

var lancamentoReceitumBeforeDeleteMu sync.Mutex
var lancamentoReceitumBeforeDeleteHooks []LancamentoReceitumHook
var lancamentoReceitumAfterDeleteMu sync.Mutex
var lancamentoReceitumAfterDeleteHooks []LancamentoReceitumHook

var lancamentoReceitumBeforeUpsertMu sync.Mutex
var lancamentoReceitumBeforeUpsertHooks []LancamentoReceitumHook
var lancamentoReceitumAfterUpsertMu sync.Mutex
var lancamentoReceitumAfterUpsertHooks []LancamentoReceitumHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *LancamentoReceitum) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *LancamentoReceitum) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *LancamentoReceitum) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *LancamentoReceitum) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *LancamentoReceitum) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *LancamentoReceitum) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *LancamentoReceitum) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *LancamentoReceitum) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *LancamentoReceitum) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lancamentoReceitumAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddLancamentoReceitumHook registers your hook function for all future operations.
func AddLancamentoReceitumHook(hookPoint boil.HookPoint, lancamentoReceitumHook LancamentoReceitumHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		lancamentoReceitumAfterSelectMu.Lock()
		lancamentoReceitumAfterSelectHooks = append(lancamentoReceitumAfterSelectHooks, lancamentoReceitumHook)
		lancamentoReceitumAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		lancamentoReceitumBeforeInsertMu.Lock()
		lancamentoReceitumBeforeInsertHooks = append(lancamentoReceitumBeforeInsertHooks, lancamentoReceitumHook)
		lancamentoReceitumBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		lancamentoReceitumAfterInsertMu.Lock()
		lancamentoReceitumAfterInsertHooks = append(lancamentoReceitumAfterInsertHooks, lancamentoReceitumHook)
		lancamentoReceitumAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		lancamentoReceitumBeforeUpdateMu.Lock()
		lancamentoReceitumBeforeUpdateHooks = append(lancamentoReceitumBeforeUpdateHooks, lancamentoReceitumHook)
		lancamentoReceitumBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		lancamentoReceitumAfterUpdateMu.Lock()
		lancamentoReceitumAfterUpdateHooks = append(lancamentoReceitumAfterUpdateHooks, lancamentoReceitumHook)
		lancamentoReceitumAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		lancamentoReceitumBeforeDeleteMu.Lock()
		lancamentoReceitumBeforeDeleteHooks = append(lancamentoReceitumBeforeDeleteHooks, lancamentoReceitumHook)
		lancamentoReceitumBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		lancamentoReceitumAfterDeleteMu.Lock()
		lancamentoReceitumAfterDeleteHooks = append(lancamentoReceitumAfterDeleteHooks, lancamentoReceitumHook)
		lancamentoReceitumAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		lancamentoReceitumBeforeUpsertMu.Lock()
		lancamentoReceitumBeforeUpsertHooks = append(lancamentoReceitumBeforeUpsertHooks, lancamentoReceitumHook)
		lancamentoReceitumBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		lancamentoReceitumAfterUpsertMu.Lock()
		lancamentoReceitumAfterUpsertHooks = append(lancamentoReceitumAfterUpsertHooks, lancamentoReceitumHook)
		lancamentoReceitumAfterUpsertMu.Unlock()
	}
}

// One returns a single lancamentoReceitum record from the query.
func (q lancamentoReceitumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*LancamentoReceitum, error) {
	o := &LancamentoReceitum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for lancamento_receita")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all LancamentoReceitum records from the query.
func (q lancamentoReceitumQuery) All(ctx context.Context, exec boil.ContextExecutor) (LancamentoReceitumSlice, error) {
	var o []*LancamentoReceitum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to LancamentoReceitum slice")
	}

	if len(lancamentoReceitumAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all LancamentoReceitum records in the query.
func (q lancamentoReceitumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count lancamento_receita rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q lancamentoReceitumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if lancamento_receita exists")
	}

	return count > 0, nil
}

// Rec pointed to by the foreign key.
func (o *LancamentoReceitum) Rec(mods ...qm.QueryMod) receitasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`rec_id` = ?", o.RecID),
	}

	queryMods = append(queryMods, mods...)

	return Receitas(queryMods...)
}

// LoadRec allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (lancamentoReceitumL) LoadRec(ctx context.Context, e boil.ContextExecutor, singular bool, maybeLancamentoReceitum interface{}, mods queries.Applicator) error {
	var slice []*LancamentoReceitum
	var object *LancamentoReceitum

	if singular {
		var ok bool
		object, ok = maybeLancamentoReceitum.(*LancamentoReceitum)
		if !ok {
			object = new(LancamentoReceitum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeLancamentoReceitum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeLancamentoReceitum))
			}
		}
	} else {
		s, ok := maybeLancamentoReceitum.(*[]*LancamentoReceitum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeLancamentoReceitum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeLancamentoReceitum))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &lancamentoReceitumR{}
		}
		if !queries.IsNil(object.RecID) {
			args[object.RecID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &lancamentoReceitumR{}
			}

			if !queries.IsNil(obj.RecID) {
				args[obj.RecID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`receitas`),
		qm.WhereIn(`receitas.rec_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Receitas")
	}

	var resultSlice []*Receitas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Receitas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for receitas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for receitas")
	}

	if len(receitasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Rec = foreign
		if foreign.R == nil {
			foreign.R = &receitasR{}
		}
		foreign.R.RecLancamentoReceita = append(foreign.R.RecLancamentoReceita, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RecID, foreign.RecID) {
				local.R.Rec = foreign
				if foreign.R == nil {
					foreign.R = &receitasR{}
				}
				foreign.R.RecLancamentoReceita = append(foreign.R.RecLancamentoReceita, local)
				break
			}
		}
	}

	return nil
}

// SetRec of the lancamentoReceitum to the related item.
// Sets o.R.Rec to related.
// Adds o to related.R.RecLancamentoReceita.
func (o *LancamentoReceitum) SetRec(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Receitas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `lancamento_receita` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"rec_id"}),
		strmangle.WhereClause("`", "`", 0, lancamentoReceitumPrimaryKeyColumns),
	)
	values := []interface{}{related.RecID, o.LrecID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RecID, related.RecID)
	if o.R == nil {
		o.R = &lancamentoReceitumR{
			Rec: related,
		}
	} else {
		o.R.Rec = related
	}

	if related.R == nil {
		related.R = &receitasR{
			RecLancamentoReceita: LancamentoReceitumSlice{o},
		}
	} else {
		related.R.RecLancamentoReceita = append(related.R.RecLancamentoReceita, o)
	}

	return nil
}

// RemoveRec relationship.
// Sets o.R.Rec to nil.
// Removes o from all passed in related items' relationships struct.
func (o *LancamentoReceitum) RemoveRec(ctx context.Context, exec boil.ContextExecutor, related *Receitas) error {
	var err error

	queries.SetScanner(&o.RecID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("rec_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Rec = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RecLancamentoReceita {
		if queries.Equal(o.RecID, ri.RecID) {
			continue
		}

		ln := len(related.R.RecLancamentoReceita)
		if ln > 1 && i < ln-1 {
			related.R.RecLancamentoReceita[i] = related.R.RecLancamentoReceita[ln-1]
		}
		related.R.RecLancamentoReceita = related.R.RecLancamentoReceita[:ln-1]
		break
	}
	return nil
}

// LancamentoReceita retrieves all the records using an executor.
func LancamentoReceita(mods ...qm.QueryMod) lancamentoReceitumQuery {
	mods = append(mods, qm.From("`lancamento_receita`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`lancamento_receita`.*"})
	}

	return lancamentoReceitumQuery{q}
}

// FindLancamentoReceitum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLancamentoReceitum(ctx context.Context, exec boil.ContextExecutor, lrecID int, selectCols ...string) (*LancamentoReceitum, error) {
	lancamentoReceitumObj := &LancamentoReceitum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `lancamento_receita` where `lrec_id`=?", sel,
	)

	q := queries.Raw(query, lrecID)

	err := q.Bind(ctx, exec, lancamentoReceitumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from lancamento_receita")
	}

	if err = lancamentoReceitumObj.doAfterSelectHooks(ctx, exec); err != nil {
		return lancamentoReceitumObj, err
	}

	return lancamentoReceitumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *LancamentoReceitum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no lancamento_receita provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lancamentoReceitumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	lancamentoReceitumInsertCacheMut.RLock()
	cache, cached := lancamentoReceitumInsertCache[key]
	lancamentoReceitumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			lancamentoReceitumAllColumns,
			lancamentoReceitumColumnsWithDefault,
			lancamentoReceitumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(lancamentoReceitumType, lancamentoReceitumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lancamentoReceitumType, lancamentoReceitumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `lancamento_receita` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `lancamento_receita` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `lancamento_receita` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, lancamentoReceitumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into lancamento_receita")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.LrecID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for lancamento_receita")
	}

CacheNoHooks:
	if !cached {
		lancamentoReceitumInsertCacheMut.Lock()
		lancamentoReceitumInsertCache[key] = cache
		lancamentoReceitumInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the LancamentoReceitum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *LancamentoReceitum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	lancamentoReceitumUpdateCacheMut.RLock()
	cache, cached := lancamentoReceitumUpdateCache[key]
	lancamentoReceitumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			lancamentoReceitumAllColumns,
			lancamentoReceitumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update lancamento_receita, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `lancamento_receita` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, lancamentoReceitumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lancamentoReceitumType, lancamentoReceitumMapping, append(wl, lancamentoReceitumPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update lancamento_receita row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for lancamento_receita")
	}

	if !cached {
		lancamentoReceitumUpdateCacheMut.Lock()
		lancamentoReceitumUpdateCache[key] = cache
		lancamentoReceitumUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q lancamentoReceitumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for lancamento_receita")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for lancamento_receita")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LancamentoReceitumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lancamentoReceitumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `lancamento_receita` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lancamentoReceitumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in lancamentoReceitum slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all lancamentoReceitum")
	}
	return rowsAff, nil
}

var mySQLLancamentoReceitumUniqueColumns = []string{
	"lrec_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *LancamentoReceitum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no lancamento_receita provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lancamentoReceitumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLLancamentoReceitumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lancamentoReceitumUpsertCacheMut.RLock()
	cache, cached := lancamentoReceitumUpsertCache[key]
	lancamentoReceitumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			lancamentoReceitumAllColumns,
			lancamentoReceitumColumnsWithDefault,
			lancamentoReceitumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			lancamentoReceitumAllColumns,
			lancamentoReceitumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert lancamento_receita, could not build update column list")
		}

		ret := strmangle.SetComplement(lancamentoReceitumAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`lancamento_receita`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `lancamento_receita` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(lancamentoReceitumType, lancamentoReceitumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lancamentoReceitumType, lancamentoReceitumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for lancamento_receita")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(lancamentoReceitumType, lancamentoReceitumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for lancamento_receita")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for lancamento_receita")
	}

CacheNoHooks:
	if !cached {
		lancamentoReceitumUpsertCacheMut.Lock()
		lancamentoReceitumUpsertCache[key] = cache
		lancamentoReceitumUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single LancamentoReceitum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LancamentoReceitum) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no LancamentoReceitum provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lancamentoReceitumPrimaryKeyMapping)
	sql := "DELETE FROM `lancamento_receita` WHERE `lrec_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from lancamento_receita")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for lancamento_receita")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q lancamentoReceitumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no lancamentoReceitumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from lancamento_receita")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for lancamento_receita")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LancamentoReceitumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(lancamentoReceitumBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lancamentoReceitumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `lancamento_receita` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lancamentoReceitumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from lancamentoReceitum slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for lancamento_receita")
	}

	if len(lancamentoReceitumAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LancamentoReceitum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindLancamentoReceitum(ctx, exec, o.LrecID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LancamentoReceitumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := LancamentoReceitumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lancamentoReceitumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `lancamento_receita`.* FROM `lancamento_receita` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lancamentoReceitumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in LancamentoReceitumSlice")
	}

	*o = slice

	return nil
}

// LancamentoReceitumExists checks if the LancamentoReceitum row exists.
func LancamentoReceitumExists(ctx context.Context, exec boil.ContextExecutor, lrecID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `lancamento_receita` where `lrec_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, lrecID)
	}
	row := exec.QueryRowContext(ctx, sql, lrecID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if lancamento_receita exists")
	}

	return exists, nil
}

// Exists checks if the LancamentoReceitum row exists.
func (o *LancamentoReceitum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return LancamentoReceitumExists(ctx, exec, o.LrecID)
}
