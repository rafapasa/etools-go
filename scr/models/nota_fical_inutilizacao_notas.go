// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// NotaFicalInutilizacaoNotas is an object representing the database table.
type NotaFicalInutilizacaoNotas struct {
	NfinutID             int         `boil:"nfinut_id" json:"nfinut_id" toml:"nfinut_id" yaml:"nfinut_id"`
	NfinutitemItem       int         `boil:"nfinutitem_item" json:"nfinutitem_item" toml:"nfinutitem_item" yaml:"nfinutitem_item"`
	NfinutitemNumeronota int         `boil:"nfinutitem_numeronota" json:"nfinutitem_numeronota" toml:"nfinutitem_numeronota" yaml:"nfinutitem_numeronota"`
	NfinutitemResultado  null.String `boil:"nfinutitem_resultado" json:"nfinutitem_resultado,omitempty" toml:"nfinutitem_resultado" yaml:"nfinutitem_resultado,omitempty"`
	NfinutitemSerienota  null.String `boil:"nfinutitem_serienota" json:"nfinutitem_serienota,omitempty" toml:"nfinutitem_serienota" yaml:"nfinutitem_serienota,omitempty"`

	R *notaFicalInutilizacaoNotasR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L notaFicalInutilizacaoNotasL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var NotaFicalInutilizacaoNotasColumns = struct {
	NfinutID             string
	NfinutitemItem       string
	NfinutitemNumeronota string
	NfinutitemResultado  string
	NfinutitemSerienota  string
}{
	NfinutID:             "nfinut_id",
	NfinutitemItem:       "nfinutitem_item",
	NfinutitemNumeronota: "nfinutitem_numeronota",
	NfinutitemResultado:  "nfinutitem_resultado",
	NfinutitemSerienota:  "nfinutitem_serienota",
}

var NotaFicalInutilizacaoNotasTableColumns = struct {
	NfinutID             string
	NfinutitemItem       string
	NfinutitemNumeronota string
	NfinutitemResultado  string
	NfinutitemSerienota  string
}{
	NfinutID:             "nota_fical_inutilizacao_notas.nfinut_id",
	NfinutitemItem:       "nota_fical_inutilizacao_notas.nfinutitem_item",
	NfinutitemNumeronota: "nota_fical_inutilizacao_notas.nfinutitem_numeronota",
	NfinutitemResultado:  "nota_fical_inutilizacao_notas.nfinutitem_resultado",
	NfinutitemSerienota:  "nota_fical_inutilizacao_notas.nfinutitem_serienota",
}

// Generated where

var NotaFicalInutilizacaoNotasWhere = struct {
	NfinutID             whereHelperint
	NfinutitemItem       whereHelperint
	NfinutitemNumeronota whereHelperint
	NfinutitemResultado  whereHelpernull_String
	NfinutitemSerienota  whereHelpernull_String
}{
	NfinutID:             whereHelperint{field: "`nota_fical_inutilizacao_notas`.`nfinut_id`"},
	NfinutitemItem:       whereHelperint{field: "`nota_fical_inutilizacao_notas`.`nfinutitem_item`"},
	NfinutitemNumeronota: whereHelperint{field: "`nota_fical_inutilizacao_notas`.`nfinutitem_numeronota`"},
	NfinutitemResultado:  whereHelpernull_String{field: "`nota_fical_inutilizacao_notas`.`nfinutitem_resultado`"},
	NfinutitemSerienota:  whereHelpernull_String{field: "`nota_fical_inutilizacao_notas`.`nfinutitem_serienota`"},
}

// NotaFicalInutilizacaoNotasRels is where relationship names are stored.
var NotaFicalInutilizacaoNotasRels = struct {
	Nfinut string
}{
	Nfinut: "Nfinut",
}

// notaFicalInutilizacaoNotasR is where relationships are stored.
type notaFicalInutilizacaoNotasR struct {
	Nfinut *NotaFicalInutilizacao `boil:"Nfinut" json:"Nfinut" toml:"Nfinut" yaml:"Nfinut"`
}

// NewStruct creates a new relationship struct
func (*notaFicalInutilizacaoNotasR) NewStruct() *notaFicalInutilizacaoNotasR {
	return &notaFicalInutilizacaoNotasR{}
}

func (r *notaFicalInutilizacaoNotasR) GetNfinut() *NotaFicalInutilizacao {
	if r == nil {
		return nil
	}
	return r.Nfinut
}

// notaFicalInutilizacaoNotasL is where Load methods for each relationship are stored.
type notaFicalInutilizacaoNotasL struct{}

var (
	notaFicalInutilizacaoNotasAllColumns            = []string{"nfinut_id", "nfinutitem_item", "nfinutitem_numeronota", "nfinutitem_resultado", "nfinutitem_serienota"}
	notaFicalInutilizacaoNotasColumnsWithoutDefault = []string{"nfinut_id", "nfinutitem_item", "nfinutitem_numeronota", "nfinutitem_resultado", "nfinutitem_serienota"}
	notaFicalInutilizacaoNotasColumnsWithDefault    = []string{}
	notaFicalInutilizacaoNotasPrimaryKeyColumns     = []string{"nfinut_id", "nfinutitem_item"}
	notaFicalInutilizacaoNotasGeneratedColumns      = []string{}
)

type (
	// NotaFicalInutilizacaoNotasSlice is an alias for a slice of pointers to NotaFicalInutilizacaoNotas.
	// This should almost always be used instead of []NotaFicalInutilizacaoNotas.
	NotaFicalInutilizacaoNotasSlice []*NotaFicalInutilizacaoNotas
	// NotaFicalInutilizacaoNotasHook is the signature for custom NotaFicalInutilizacaoNotas hook methods
	NotaFicalInutilizacaoNotasHook func(context.Context, boil.ContextExecutor, *NotaFicalInutilizacaoNotas) error

	notaFicalInutilizacaoNotasQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	notaFicalInutilizacaoNotasType                 = reflect.TypeOf(&NotaFicalInutilizacaoNotas{})
	notaFicalInutilizacaoNotasMapping              = queries.MakeStructMapping(notaFicalInutilizacaoNotasType)
	notaFicalInutilizacaoNotasPrimaryKeyMapping, _ = queries.BindMapping(notaFicalInutilizacaoNotasType, notaFicalInutilizacaoNotasMapping, notaFicalInutilizacaoNotasPrimaryKeyColumns)
	notaFicalInutilizacaoNotasInsertCacheMut       sync.RWMutex
	notaFicalInutilizacaoNotasInsertCache          = make(map[string]insertCache)
	notaFicalInutilizacaoNotasUpdateCacheMut       sync.RWMutex
	notaFicalInutilizacaoNotasUpdateCache          = make(map[string]updateCache)
	notaFicalInutilizacaoNotasUpsertCacheMut       sync.RWMutex
	notaFicalInutilizacaoNotasUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var notaFicalInutilizacaoNotasAfterSelectMu sync.Mutex
var notaFicalInutilizacaoNotasAfterSelectHooks []NotaFicalInutilizacaoNotasHook

var notaFicalInutilizacaoNotasBeforeInsertMu sync.Mutex
var notaFicalInutilizacaoNotasBeforeInsertHooks []NotaFicalInutilizacaoNotasHook
var notaFicalInutilizacaoNotasAfterInsertMu sync.Mutex
var notaFicalInutilizacaoNotasAfterInsertHooks []NotaFicalInutilizacaoNotasHook

var notaFicalInutilizacaoNotasBeforeUpdateMu sync.Mutex
var notaFicalInutilizacaoNotasBeforeUpdateHooks []NotaFicalInutilizacaoNotasHook
var notaFicalInutilizacaoNotasAfterUpdateMu sync.Mutex
var notaFicalInutilizacaoNotasAfterUpdateHooks []NotaFicalInutilizacaoNotasHook

var notaFicalInutilizacaoNotasBeforeDeleteMu sync.Mutex
var notaFicalInutilizacaoNotasBeforeDeleteHooks []NotaFicalInutilizacaoNotasHook
var notaFicalInutilizacaoNotasAfterDeleteMu sync.Mutex
var notaFicalInutilizacaoNotasAfterDeleteHooks []NotaFicalInutilizacaoNotasHook

var notaFicalInutilizacaoNotasBeforeUpsertMu sync.Mutex
var notaFicalInutilizacaoNotasBeforeUpsertHooks []NotaFicalInutilizacaoNotasHook
var notaFicalInutilizacaoNotasAfterUpsertMu sync.Mutex
var notaFicalInutilizacaoNotasAfterUpsertHooks []NotaFicalInutilizacaoNotasHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *NotaFicalInutilizacaoNotas) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *NotaFicalInutilizacaoNotas) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *NotaFicalInutilizacaoNotas) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *NotaFicalInutilizacaoNotas) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *NotaFicalInutilizacaoNotas) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *NotaFicalInutilizacaoNotas) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *NotaFicalInutilizacaoNotas) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *NotaFicalInutilizacaoNotas) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *NotaFicalInutilizacaoNotas) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range notaFicalInutilizacaoNotasAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddNotaFicalInutilizacaoNotasHook registers your hook function for all future operations.
func AddNotaFicalInutilizacaoNotasHook(hookPoint boil.HookPoint, notaFicalInutilizacaoNotasHook NotaFicalInutilizacaoNotasHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		notaFicalInutilizacaoNotasAfterSelectMu.Lock()
		notaFicalInutilizacaoNotasAfterSelectHooks = append(notaFicalInutilizacaoNotasAfterSelectHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		notaFicalInutilizacaoNotasBeforeInsertMu.Lock()
		notaFicalInutilizacaoNotasBeforeInsertHooks = append(notaFicalInutilizacaoNotasBeforeInsertHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		notaFicalInutilizacaoNotasAfterInsertMu.Lock()
		notaFicalInutilizacaoNotasAfterInsertHooks = append(notaFicalInutilizacaoNotasAfterInsertHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		notaFicalInutilizacaoNotasBeforeUpdateMu.Lock()
		notaFicalInutilizacaoNotasBeforeUpdateHooks = append(notaFicalInutilizacaoNotasBeforeUpdateHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		notaFicalInutilizacaoNotasAfterUpdateMu.Lock()
		notaFicalInutilizacaoNotasAfterUpdateHooks = append(notaFicalInutilizacaoNotasAfterUpdateHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		notaFicalInutilizacaoNotasBeforeDeleteMu.Lock()
		notaFicalInutilizacaoNotasBeforeDeleteHooks = append(notaFicalInutilizacaoNotasBeforeDeleteHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		notaFicalInutilizacaoNotasAfterDeleteMu.Lock()
		notaFicalInutilizacaoNotasAfterDeleteHooks = append(notaFicalInutilizacaoNotasAfterDeleteHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		notaFicalInutilizacaoNotasBeforeUpsertMu.Lock()
		notaFicalInutilizacaoNotasBeforeUpsertHooks = append(notaFicalInutilizacaoNotasBeforeUpsertHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		notaFicalInutilizacaoNotasAfterUpsertMu.Lock()
		notaFicalInutilizacaoNotasAfterUpsertHooks = append(notaFicalInutilizacaoNotasAfterUpsertHooks, notaFicalInutilizacaoNotasHook)
		notaFicalInutilizacaoNotasAfterUpsertMu.Unlock()
	}
}

// One returns a single notaFicalInutilizacaoNotas record from the query.
func (q notaFicalInutilizacaoNotasQuery) One(ctx context.Context, exec boil.ContextExecutor) (*NotaFicalInutilizacaoNotas, error) {
	o := &NotaFicalInutilizacaoNotas{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for nota_fical_inutilizacao_notas")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all NotaFicalInutilizacaoNotas records from the query.
func (q notaFicalInutilizacaoNotasQuery) All(ctx context.Context, exec boil.ContextExecutor) (NotaFicalInutilizacaoNotasSlice, error) {
	var o []*NotaFicalInutilizacaoNotas

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to NotaFicalInutilizacaoNotas slice")
	}

	if len(notaFicalInutilizacaoNotasAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all NotaFicalInutilizacaoNotas records in the query.
func (q notaFicalInutilizacaoNotasQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count nota_fical_inutilizacao_notas rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q notaFicalInutilizacaoNotasQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if nota_fical_inutilizacao_notas exists")
	}

	return count > 0, nil
}

// Nfinut pointed to by the foreign key.
func (o *NotaFicalInutilizacaoNotas) Nfinut(mods ...qm.QueryMod) notaFicalInutilizacaoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`nfinut_id` = ?", o.NfinutID),
	}

	queryMods = append(queryMods, mods...)

	return NotaFicalInutilizacaos(queryMods...)
}

// LoadNfinut allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (notaFicalInutilizacaoNotasL) LoadNfinut(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNotaFicalInutilizacaoNotas interface{}, mods queries.Applicator) error {
	var slice []*NotaFicalInutilizacaoNotas
	var object *NotaFicalInutilizacaoNotas

	if singular {
		var ok bool
		object, ok = maybeNotaFicalInutilizacaoNotas.(*NotaFicalInutilizacaoNotas)
		if !ok {
			object = new(NotaFicalInutilizacaoNotas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNotaFicalInutilizacaoNotas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNotaFicalInutilizacaoNotas))
			}
		}
	} else {
		s, ok := maybeNotaFicalInutilizacaoNotas.(*[]*NotaFicalInutilizacaoNotas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNotaFicalInutilizacaoNotas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNotaFicalInutilizacaoNotas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &notaFicalInutilizacaoNotasR{}
		}
		args[object.NfinutID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &notaFicalInutilizacaoNotasR{}
			}

			args[obj.NfinutID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fical_inutilizacao`),
		qm.WhereIn(`nota_fical_inutilizacao.nfinut_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load NotaFicalInutilizacao")
	}

	var resultSlice []*NotaFicalInutilizacao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice NotaFicalInutilizacao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for nota_fical_inutilizacao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fical_inutilizacao")
	}

	if len(notaFicalInutilizacaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Nfinut = foreign
		if foreign.R == nil {
			foreign.R = &notaFicalInutilizacaoR{}
		}
		foreign.R.NfinutNotaFicalInutilizacaoNotas = append(foreign.R.NfinutNotaFicalInutilizacaoNotas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.NfinutID == foreign.NfinutID {
				local.R.Nfinut = foreign
				if foreign.R == nil {
					foreign.R = &notaFicalInutilizacaoR{}
				}
				foreign.R.NfinutNotaFicalInutilizacaoNotas = append(foreign.R.NfinutNotaFicalInutilizacaoNotas, local)
				break
			}
		}
	}

	return nil
}

// SetNfinut of the notaFicalInutilizacaoNotas to the related item.
// Sets o.R.Nfinut to related.
// Adds o to related.R.NfinutNotaFicalInutilizacaoNotas.
func (o *NotaFicalInutilizacaoNotas) SetNfinut(ctx context.Context, exec boil.ContextExecutor, insert bool, related *NotaFicalInutilizacao) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `nota_fical_inutilizacao_notas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"nfinut_id"}),
		strmangle.WhereClause("`", "`", 0, notaFicalInutilizacaoNotasPrimaryKeyColumns),
	)
	values := []interface{}{related.NfinutID, o.NfinutID, o.NfinutitemItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.NfinutID = related.NfinutID
	if o.R == nil {
		o.R = &notaFicalInutilizacaoNotasR{
			Nfinut: related,
		}
	} else {
		o.R.Nfinut = related
	}

	if related.R == nil {
		related.R = &notaFicalInutilizacaoR{
			NfinutNotaFicalInutilizacaoNotas: NotaFicalInutilizacaoNotasSlice{o},
		}
	} else {
		related.R.NfinutNotaFicalInutilizacaoNotas = append(related.R.NfinutNotaFicalInutilizacaoNotas, o)
	}

	return nil
}

// NotaFicalInutilizacaoNotas retrieves all the records using an executor.
func NotaFicalInutilizacaoNotas(mods ...qm.QueryMod) notaFicalInutilizacaoNotasQuery {
	mods = append(mods, qm.From("`nota_fical_inutilizacao_notas`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`nota_fical_inutilizacao_notas`.*"})
	}

	return notaFicalInutilizacaoNotasQuery{q}
}

// FindNotaFicalInutilizacaoNotas retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindNotaFicalInutilizacaoNotas(ctx context.Context, exec boil.ContextExecutor, nfinutID int, nfinutitemItem int, selectCols ...string) (*NotaFicalInutilizacaoNotas, error) {
	notaFicalInutilizacaoNotasObj := &NotaFicalInutilizacaoNotas{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `nota_fical_inutilizacao_notas` where `nfinut_id`=? AND `nfinutitem_item`=?", sel,
	)

	q := queries.Raw(query, nfinutID, nfinutitemItem)

	err := q.Bind(ctx, exec, notaFicalInutilizacaoNotasObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from nota_fical_inutilizacao_notas")
	}

	if err = notaFicalInutilizacaoNotasObj.doAfterSelectHooks(ctx, exec); err != nil {
		return notaFicalInutilizacaoNotasObj, err
	}

	return notaFicalInutilizacaoNotasObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *NotaFicalInutilizacaoNotas) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no nota_fical_inutilizacao_notas provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(notaFicalInutilizacaoNotasColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	notaFicalInutilizacaoNotasInsertCacheMut.RLock()
	cache, cached := notaFicalInutilizacaoNotasInsertCache[key]
	notaFicalInutilizacaoNotasInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			notaFicalInutilizacaoNotasAllColumns,
			notaFicalInutilizacaoNotasColumnsWithDefault,
			notaFicalInutilizacaoNotasColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(notaFicalInutilizacaoNotasType, notaFicalInutilizacaoNotasMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(notaFicalInutilizacaoNotasType, notaFicalInutilizacaoNotasMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `nota_fical_inutilizacao_notas` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `nota_fical_inutilizacao_notas` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `nota_fical_inutilizacao_notas` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, notaFicalInutilizacaoNotasPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into nota_fical_inutilizacao_notas")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.NfinutID,
		o.NfinutitemItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for nota_fical_inutilizacao_notas")
	}

CacheNoHooks:
	if !cached {
		notaFicalInutilizacaoNotasInsertCacheMut.Lock()
		notaFicalInutilizacaoNotasInsertCache[key] = cache
		notaFicalInutilizacaoNotasInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the NotaFicalInutilizacaoNotas.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *NotaFicalInutilizacaoNotas) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	notaFicalInutilizacaoNotasUpdateCacheMut.RLock()
	cache, cached := notaFicalInutilizacaoNotasUpdateCache[key]
	notaFicalInutilizacaoNotasUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			notaFicalInutilizacaoNotasAllColumns,
			notaFicalInutilizacaoNotasPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update nota_fical_inutilizacao_notas, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `nota_fical_inutilizacao_notas` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, notaFicalInutilizacaoNotasPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(notaFicalInutilizacaoNotasType, notaFicalInutilizacaoNotasMapping, append(wl, notaFicalInutilizacaoNotasPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update nota_fical_inutilizacao_notas row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for nota_fical_inutilizacao_notas")
	}

	if !cached {
		notaFicalInutilizacaoNotasUpdateCacheMut.Lock()
		notaFicalInutilizacaoNotasUpdateCache[key] = cache
		notaFicalInutilizacaoNotasUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q notaFicalInutilizacaoNotasQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for nota_fical_inutilizacao_notas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for nota_fical_inutilizacao_notas")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o NotaFicalInutilizacaoNotasSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFicalInutilizacaoNotasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `nota_fical_inutilizacao_notas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFicalInutilizacaoNotasPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in notaFicalInutilizacaoNotas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all notaFicalInutilizacaoNotas")
	}
	return rowsAff, nil
}

var mySQLNotaFicalInutilizacaoNotasUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *NotaFicalInutilizacaoNotas) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no nota_fical_inutilizacao_notas provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(notaFicalInutilizacaoNotasColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLNotaFicalInutilizacaoNotasUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	notaFicalInutilizacaoNotasUpsertCacheMut.RLock()
	cache, cached := notaFicalInutilizacaoNotasUpsertCache[key]
	notaFicalInutilizacaoNotasUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			notaFicalInutilizacaoNotasAllColumns,
			notaFicalInutilizacaoNotasColumnsWithDefault,
			notaFicalInutilizacaoNotasColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			notaFicalInutilizacaoNotasAllColumns,
			notaFicalInutilizacaoNotasPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert nota_fical_inutilizacao_notas, could not build update column list")
		}

		ret := strmangle.SetComplement(notaFicalInutilizacaoNotasAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`nota_fical_inutilizacao_notas`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `nota_fical_inutilizacao_notas` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(notaFicalInutilizacaoNotasType, notaFicalInutilizacaoNotasMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(notaFicalInutilizacaoNotasType, notaFicalInutilizacaoNotasMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for nota_fical_inutilizacao_notas")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(notaFicalInutilizacaoNotasType, notaFicalInutilizacaoNotasMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for nota_fical_inutilizacao_notas")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for nota_fical_inutilizacao_notas")
	}

CacheNoHooks:
	if !cached {
		notaFicalInutilizacaoNotasUpsertCacheMut.Lock()
		notaFicalInutilizacaoNotasUpsertCache[key] = cache
		notaFicalInutilizacaoNotasUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single NotaFicalInutilizacaoNotas record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *NotaFicalInutilizacaoNotas) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no NotaFicalInutilizacaoNotas provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), notaFicalInutilizacaoNotasPrimaryKeyMapping)
	sql := "DELETE FROM `nota_fical_inutilizacao_notas` WHERE `nfinut_id`=? AND `nfinutitem_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from nota_fical_inutilizacao_notas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for nota_fical_inutilizacao_notas")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q notaFicalInutilizacaoNotasQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no notaFicalInutilizacaoNotasQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from nota_fical_inutilizacao_notas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for nota_fical_inutilizacao_notas")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o NotaFicalInutilizacaoNotasSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(notaFicalInutilizacaoNotasBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFicalInutilizacaoNotasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `nota_fical_inutilizacao_notas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFicalInutilizacaoNotasPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from notaFicalInutilizacaoNotas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for nota_fical_inutilizacao_notas")
	}

	if len(notaFicalInutilizacaoNotasAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *NotaFicalInutilizacaoNotas) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindNotaFicalInutilizacaoNotas(ctx, exec, o.NfinutID, o.NfinutitemItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *NotaFicalInutilizacaoNotasSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := NotaFicalInutilizacaoNotasSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), notaFicalInutilizacaoNotasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `nota_fical_inutilizacao_notas`.* FROM `nota_fical_inutilizacao_notas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, notaFicalInutilizacaoNotasPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in NotaFicalInutilizacaoNotasSlice")
	}

	*o = slice

	return nil
}

// NotaFicalInutilizacaoNotasExists checks if the NotaFicalInutilizacaoNotas row exists.
func NotaFicalInutilizacaoNotasExists(ctx context.Context, exec boil.ContextExecutor, nfinutID int, nfinutitemItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `nota_fical_inutilizacao_notas` where `nfinut_id`=? AND `nfinutitem_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, nfinutID, nfinutitemItem)
	}
	row := exec.QueryRowContext(ctx, sql, nfinutID, nfinutitemItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if nota_fical_inutilizacao_notas exists")
	}

	return exists, nil
}

// Exists checks if the NotaFicalInutilizacaoNotas row exists.
func (o *NotaFicalInutilizacaoNotas) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return NotaFicalInutilizacaoNotasExists(ctx, exec, o.NfinutID, o.NfinutitemItem)
}
