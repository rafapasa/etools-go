// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TituloOcorrenciaTipo is an object representing the database table.
type TituloOcorrenciaTipo struct {
	TotID        int    `boil:"tot_id" json:"tot_id" toml:"tot_id" yaml:"tot_id"`
	TotDescricao string `boil:"tot_descricao" json:"tot_descricao" toml:"tot_descricao" yaml:"tot_descricao"`

	R *tituloOcorrenciaTipoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tituloOcorrenciaTipoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TituloOcorrenciaTipoColumns = struct {
	TotID        string
	TotDescricao string
}{
	TotID:        "tot_id",
	TotDescricao: "tot_descricao",
}

var TituloOcorrenciaTipoTableColumns = struct {
	TotID        string
	TotDescricao string
}{
	TotID:        "titulo_ocorrencia_tipo.tot_id",
	TotDescricao: "titulo_ocorrencia_tipo.tot_descricao",
}

// Generated where

var TituloOcorrenciaTipoWhere = struct {
	TotID        whereHelperint
	TotDescricao whereHelperstring
}{
	TotID:        whereHelperint{field: "`titulo_ocorrencia_tipo`.`tot_id`"},
	TotDescricao: whereHelperstring{field: "`titulo_ocorrencia_tipo`.`tot_descricao`"},
}

// TituloOcorrenciaTipoRels is where relationship names are stored.
var TituloOcorrenciaTipoRels = struct {
	TotTituloOcorrencia string
}{
	TotTituloOcorrencia: "TotTituloOcorrencia",
}

// tituloOcorrenciaTipoR is where relationships are stored.
type tituloOcorrenciaTipoR struct {
	TotTituloOcorrencia TituloOcorrenciumSlice `boil:"TotTituloOcorrencia" json:"TotTituloOcorrencia" toml:"TotTituloOcorrencia" yaml:"TotTituloOcorrencia"`
}

// NewStruct creates a new relationship struct
func (*tituloOcorrenciaTipoR) NewStruct() *tituloOcorrenciaTipoR {
	return &tituloOcorrenciaTipoR{}
}

func (r *tituloOcorrenciaTipoR) GetTotTituloOcorrencia() TituloOcorrenciumSlice {
	if r == nil {
		return nil
	}
	return r.TotTituloOcorrencia
}

// tituloOcorrenciaTipoL is where Load methods for each relationship are stored.
type tituloOcorrenciaTipoL struct{}

var (
	tituloOcorrenciaTipoAllColumns            = []string{"tot_id", "tot_descricao"}
	tituloOcorrenciaTipoColumnsWithoutDefault = []string{"tot_id", "tot_descricao"}
	tituloOcorrenciaTipoColumnsWithDefault    = []string{}
	tituloOcorrenciaTipoPrimaryKeyColumns     = []string{"tot_id"}
	tituloOcorrenciaTipoGeneratedColumns      = []string{}
)

type (
	// TituloOcorrenciaTipoSlice is an alias for a slice of pointers to TituloOcorrenciaTipo.
	// This should almost always be used instead of []TituloOcorrenciaTipo.
	TituloOcorrenciaTipoSlice []*TituloOcorrenciaTipo
	// TituloOcorrenciaTipoHook is the signature for custom TituloOcorrenciaTipo hook methods
	TituloOcorrenciaTipoHook func(context.Context, boil.ContextExecutor, *TituloOcorrenciaTipo) error

	tituloOcorrenciaTipoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tituloOcorrenciaTipoType                 = reflect.TypeOf(&TituloOcorrenciaTipo{})
	tituloOcorrenciaTipoMapping              = queries.MakeStructMapping(tituloOcorrenciaTipoType)
	tituloOcorrenciaTipoPrimaryKeyMapping, _ = queries.BindMapping(tituloOcorrenciaTipoType, tituloOcorrenciaTipoMapping, tituloOcorrenciaTipoPrimaryKeyColumns)
	tituloOcorrenciaTipoInsertCacheMut       sync.RWMutex
	tituloOcorrenciaTipoInsertCache          = make(map[string]insertCache)
	tituloOcorrenciaTipoUpdateCacheMut       sync.RWMutex
	tituloOcorrenciaTipoUpdateCache          = make(map[string]updateCache)
	tituloOcorrenciaTipoUpsertCacheMut       sync.RWMutex
	tituloOcorrenciaTipoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tituloOcorrenciaTipoAfterSelectMu sync.Mutex
var tituloOcorrenciaTipoAfterSelectHooks []TituloOcorrenciaTipoHook

var tituloOcorrenciaTipoBeforeInsertMu sync.Mutex
var tituloOcorrenciaTipoBeforeInsertHooks []TituloOcorrenciaTipoHook
var tituloOcorrenciaTipoAfterInsertMu sync.Mutex
var tituloOcorrenciaTipoAfterInsertHooks []TituloOcorrenciaTipoHook

var tituloOcorrenciaTipoBeforeUpdateMu sync.Mutex
var tituloOcorrenciaTipoBeforeUpdateHooks []TituloOcorrenciaTipoHook
var tituloOcorrenciaTipoAfterUpdateMu sync.Mutex
var tituloOcorrenciaTipoAfterUpdateHooks []TituloOcorrenciaTipoHook

var tituloOcorrenciaTipoBeforeDeleteMu sync.Mutex
var tituloOcorrenciaTipoBeforeDeleteHooks []TituloOcorrenciaTipoHook
var tituloOcorrenciaTipoAfterDeleteMu sync.Mutex
var tituloOcorrenciaTipoAfterDeleteHooks []TituloOcorrenciaTipoHook

var tituloOcorrenciaTipoBeforeUpsertMu sync.Mutex
var tituloOcorrenciaTipoBeforeUpsertHooks []TituloOcorrenciaTipoHook
var tituloOcorrenciaTipoAfterUpsertMu sync.Mutex
var tituloOcorrenciaTipoAfterUpsertHooks []TituloOcorrenciaTipoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TituloOcorrenciaTipo) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TituloOcorrenciaTipo) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TituloOcorrenciaTipo) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TituloOcorrenciaTipo) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TituloOcorrenciaTipo) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TituloOcorrenciaTipo) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TituloOcorrenciaTipo) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TituloOcorrenciaTipo) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TituloOcorrenciaTipo) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciaTipoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTituloOcorrenciaTipoHook registers your hook function for all future operations.
func AddTituloOcorrenciaTipoHook(hookPoint boil.HookPoint, tituloOcorrenciaTipoHook TituloOcorrenciaTipoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tituloOcorrenciaTipoAfterSelectMu.Lock()
		tituloOcorrenciaTipoAfterSelectHooks = append(tituloOcorrenciaTipoAfterSelectHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tituloOcorrenciaTipoBeforeInsertMu.Lock()
		tituloOcorrenciaTipoBeforeInsertHooks = append(tituloOcorrenciaTipoBeforeInsertHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tituloOcorrenciaTipoAfterInsertMu.Lock()
		tituloOcorrenciaTipoAfterInsertHooks = append(tituloOcorrenciaTipoAfterInsertHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tituloOcorrenciaTipoBeforeUpdateMu.Lock()
		tituloOcorrenciaTipoBeforeUpdateHooks = append(tituloOcorrenciaTipoBeforeUpdateHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tituloOcorrenciaTipoAfterUpdateMu.Lock()
		tituloOcorrenciaTipoAfterUpdateHooks = append(tituloOcorrenciaTipoAfterUpdateHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tituloOcorrenciaTipoBeforeDeleteMu.Lock()
		tituloOcorrenciaTipoBeforeDeleteHooks = append(tituloOcorrenciaTipoBeforeDeleteHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tituloOcorrenciaTipoAfterDeleteMu.Lock()
		tituloOcorrenciaTipoAfterDeleteHooks = append(tituloOcorrenciaTipoAfterDeleteHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tituloOcorrenciaTipoBeforeUpsertMu.Lock()
		tituloOcorrenciaTipoBeforeUpsertHooks = append(tituloOcorrenciaTipoBeforeUpsertHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tituloOcorrenciaTipoAfterUpsertMu.Lock()
		tituloOcorrenciaTipoAfterUpsertHooks = append(tituloOcorrenciaTipoAfterUpsertHooks, tituloOcorrenciaTipoHook)
		tituloOcorrenciaTipoAfterUpsertMu.Unlock()
	}
}

// One returns a single tituloOcorrenciaTipo record from the query.
func (q tituloOcorrenciaTipoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TituloOcorrenciaTipo, error) {
	o := &TituloOcorrenciaTipo{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for titulo_ocorrencia_tipo")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TituloOcorrenciaTipo records from the query.
func (q tituloOcorrenciaTipoQuery) All(ctx context.Context, exec boil.ContextExecutor) (TituloOcorrenciaTipoSlice, error) {
	var o []*TituloOcorrenciaTipo

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TituloOcorrenciaTipo slice")
	}

	if len(tituloOcorrenciaTipoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TituloOcorrenciaTipo records in the query.
func (q tituloOcorrenciaTipoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count titulo_ocorrencia_tipo rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tituloOcorrenciaTipoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if titulo_ocorrencia_tipo exists")
	}

	return count > 0, nil
}

// TotTituloOcorrencia retrieves all the titulo_ocorrencium's TituloOcorrencia with an executor via tot_id column.
func (o *TituloOcorrenciaTipo) TotTituloOcorrencia(mods ...qm.QueryMod) tituloOcorrenciumQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`titulo_ocorrencia`.`tot_id`=?", o.TotID),
	)

	return TituloOcorrencia(queryMods...)
}

// LoadTotTituloOcorrencia allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (tituloOcorrenciaTipoL) LoadTotTituloOcorrencia(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloOcorrenciaTipo interface{}, mods queries.Applicator) error {
	var slice []*TituloOcorrenciaTipo
	var object *TituloOcorrenciaTipo

	if singular {
		var ok bool
		object, ok = maybeTituloOcorrenciaTipo.(*TituloOcorrenciaTipo)
		if !ok {
			object = new(TituloOcorrenciaTipo)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloOcorrenciaTipo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloOcorrenciaTipo))
			}
		}
	} else {
		s, ok := maybeTituloOcorrenciaTipo.(*[]*TituloOcorrenciaTipo)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloOcorrenciaTipo)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloOcorrenciaTipo))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloOcorrenciaTipoR{}
		}
		args[object.TotID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloOcorrenciaTipoR{}
			}
			args[obj.TotID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulo_ocorrencia`),
		qm.WhereIn(`titulo_ocorrencia.tot_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load titulo_ocorrencia")
	}

	var resultSlice []*TituloOcorrencium
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice titulo_ocorrencia")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on titulo_ocorrencia")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulo_ocorrencia")
	}

	if len(tituloOcorrenciumAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TotTituloOcorrencia = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &tituloOcorrenciumR{}
			}
			foreign.R.Tot = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.TotID == foreign.TotID {
				local.R.TotTituloOcorrencia = append(local.R.TotTituloOcorrencia, foreign)
				if foreign.R == nil {
					foreign.R = &tituloOcorrenciumR{}
				}
				foreign.R.Tot = local
				break
			}
		}
	}

	return nil
}

// AddTotTituloOcorrencia adds the given related objects to the existing relationships
// of the titulo_ocorrencia_tipo, optionally inserting them as new records.
// Appends related to o.R.TotTituloOcorrencia.
// Sets related.R.Tot appropriately.
func (o *TituloOcorrenciaTipo) AddTotTituloOcorrencia(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TituloOcorrencium) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.TotID = o.TotID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `titulo_ocorrencia` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"tot_id"}),
				strmangle.WhereClause("`", "`", 0, tituloOcorrenciumPrimaryKeyColumns),
			)
			values := []interface{}{o.TotID, rel.TitID, rel.TitoItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.TotID = o.TotID
		}
	}

	if o.R == nil {
		o.R = &tituloOcorrenciaTipoR{
			TotTituloOcorrencia: related,
		}
	} else {
		o.R.TotTituloOcorrencia = append(o.R.TotTituloOcorrencia, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &tituloOcorrenciumR{
				Tot: o,
			}
		} else {
			rel.R.Tot = o
		}
	}
	return nil
}

// TituloOcorrenciaTipos retrieves all the records using an executor.
func TituloOcorrenciaTipos(mods ...qm.QueryMod) tituloOcorrenciaTipoQuery {
	mods = append(mods, qm.From("`titulo_ocorrencia_tipo`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`titulo_ocorrencia_tipo`.*"})
	}

	return tituloOcorrenciaTipoQuery{q}
}

// FindTituloOcorrenciaTipo retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTituloOcorrenciaTipo(ctx context.Context, exec boil.ContextExecutor, totID int, selectCols ...string) (*TituloOcorrenciaTipo, error) {
	tituloOcorrenciaTipoObj := &TituloOcorrenciaTipo{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `titulo_ocorrencia_tipo` where `tot_id`=?", sel,
	)

	q := queries.Raw(query, totID)

	err := q.Bind(ctx, exec, tituloOcorrenciaTipoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from titulo_ocorrencia_tipo")
	}

	if err = tituloOcorrenciaTipoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tituloOcorrenciaTipoObj, err
	}

	return tituloOcorrenciaTipoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TituloOcorrenciaTipo) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no titulo_ocorrencia_tipo provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tituloOcorrenciaTipoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tituloOcorrenciaTipoInsertCacheMut.RLock()
	cache, cached := tituloOcorrenciaTipoInsertCache[key]
	tituloOcorrenciaTipoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tituloOcorrenciaTipoAllColumns,
			tituloOcorrenciaTipoColumnsWithDefault,
			tituloOcorrenciaTipoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tituloOcorrenciaTipoType, tituloOcorrenciaTipoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tituloOcorrenciaTipoType, tituloOcorrenciaTipoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `titulo_ocorrencia_tipo` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `titulo_ocorrencia_tipo` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `titulo_ocorrencia_tipo` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tituloOcorrenciaTipoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into titulo_ocorrencia_tipo")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TotID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for titulo_ocorrencia_tipo")
	}

CacheNoHooks:
	if !cached {
		tituloOcorrenciaTipoInsertCacheMut.Lock()
		tituloOcorrenciaTipoInsertCache[key] = cache
		tituloOcorrenciaTipoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TituloOcorrenciaTipo.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TituloOcorrenciaTipo) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tituloOcorrenciaTipoUpdateCacheMut.RLock()
	cache, cached := tituloOcorrenciaTipoUpdateCache[key]
	tituloOcorrenciaTipoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tituloOcorrenciaTipoAllColumns,
			tituloOcorrenciaTipoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update titulo_ocorrencia_tipo, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `titulo_ocorrencia_tipo` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tituloOcorrenciaTipoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tituloOcorrenciaTipoType, tituloOcorrenciaTipoMapping, append(wl, tituloOcorrenciaTipoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update titulo_ocorrencia_tipo row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for titulo_ocorrencia_tipo")
	}

	if !cached {
		tituloOcorrenciaTipoUpdateCacheMut.Lock()
		tituloOcorrenciaTipoUpdateCache[key] = cache
		tituloOcorrenciaTipoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tituloOcorrenciaTipoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for titulo_ocorrencia_tipo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for titulo_ocorrencia_tipo")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TituloOcorrenciaTipoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloOcorrenciaTipoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `titulo_ocorrencia_tipo` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloOcorrenciaTipoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tituloOcorrenciaTipo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tituloOcorrenciaTipo")
	}
	return rowsAff, nil
}

var mySQLTituloOcorrenciaTipoUniqueColumns = []string{
	"tot_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TituloOcorrenciaTipo) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no titulo_ocorrencia_tipo provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tituloOcorrenciaTipoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTituloOcorrenciaTipoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tituloOcorrenciaTipoUpsertCacheMut.RLock()
	cache, cached := tituloOcorrenciaTipoUpsertCache[key]
	tituloOcorrenciaTipoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tituloOcorrenciaTipoAllColumns,
			tituloOcorrenciaTipoColumnsWithDefault,
			tituloOcorrenciaTipoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tituloOcorrenciaTipoAllColumns,
			tituloOcorrenciaTipoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert titulo_ocorrencia_tipo, could not build update column list")
		}

		ret := strmangle.SetComplement(tituloOcorrenciaTipoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`titulo_ocorrencia_tipo`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `titulo_ocorrencia_tipo` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tituloOcorrenciaTipoType, tituloOcorrenciaTipoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tituloOcorrenciaTipoType, tituloOcorrenciaTipoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for titulo_ocorrencia_tipo")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tituloOcorrenciaTipoType, tituloOcorrenciaTipoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for titulo_ocorrencia_tipo")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for titulo_ocorrencia_tipo")
	}

CacheNoHooks:
	if !cached {
		tituloOcorrenciaTipoUpsertCacheMut.Lock()
		tituloOcorrenciaTipoUpsertCache[key] = cache
		tituloOcorrenciaTipoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TituloOcorrenciaTipo record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TituloOcorrenciaTipo) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TituloOcorrenciaTipo provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tituloOcorrenciaTipoPrimaryKeyMapping)
	sql := "DELETE FROM `titulo_ocorrencia_tipo` WHERE `tot_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from titulo_ocorrencia_tipo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for titulo_ocorrencia_tipo")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tituloOcorrenciaTipoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tituloOcorrenciaTipoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from titulo_ocorrencia_tipo")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for titulo_ocorrencia_tipo")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TituloOcorrenciaTipoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tituloOcorrenciaTipoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloOcorrenciaTipoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `titulo_ocorrencia_tipo` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloOcorrenciaTipoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tituloOcorrenciaTipo slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for titulo_ocorrencia_tipo")
	}

	if len(tituloOcorrenciaTipoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TituloOcorrenciaTipo) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTituloOcorrenciaTipo(ctx, exec, o.TotID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TituloOcorrenciaTipoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TituloOcorrenciaTipoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloOcorrenciaTipoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `titulo_ocorrencia_tipo`.* FROM `titulo_ocorrencia_tipo` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloOcorrenciaTipoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TituloOcorrenciaTipoSlice")
	}

	*o = slice

	return nil
}

// TituloOcorrenciaTipoExists checks if the TituloOcorrenciaTipo row exists.
func TituloOcorrenciaTipoExists(ctx context.Context, exec boil.ContextExecutor, totID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `titulo_ocorrencia_tipo` where `tot_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, totID)
	}
	row := exec.QueryRowContext(ctx, sql, totID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if titulo_ocorrencia_tipo exists")
	}

	return exists, nil
}

// Exists checks if the TituloOcorrenciaTipo row exists.
func (o *TituloOcorrenciaTipo) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TituloOcorrenciaTipoExists(ctx, exec, o.TotID)
}
