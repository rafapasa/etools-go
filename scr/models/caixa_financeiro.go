// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CaixaFinanceiro is an object representing the database table.
type CaixaFinanceiro struct {
	CxfinID        int    `boil:"cxfin_id" json:"cxfin_id" toml:"cxfin_id" yaml:"cxfin_id"`
	PCFID          int    `boil:"pcf_id" json:"pcf_id" toml:"pcf_id" yaml:"pcf_id"`
	CxfinDescricao string `boil:"cxfin_descricao" json:"cxfin_descricao" toml:"cxfin_descricao" yaml:"cxfin_descricao"`
	CxfinSituacao  int    `boil:"cxfin_situacao" json:"cxfin_situacao" toml:"cxfin_situacao" yaml:"cxfin_situacao"`

	R *caixaFinanceiroR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L caixaFinanceiroL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CaixaFinanceiroColumns = struct {
	CxfinID        string
	PCFID          string
	CxfinDescricao string
	CxfinSituacao  string
}{
	CxfinID:        "cxfin_id",
	PCFID:          "pcf_id",
	CxfinDescricao: "cxfin_descricao",
	CxfinSituacao:  "cxfin_situacao",
}

var CaixaFinanceiroTableColumns = struct {
	CxfinID        string
	PCFID          string
	CxfinDescricao string
	CxfinSituacao  string
}{
	CxfinID:        "caixa_financeiro.cxfin_id",
	PCFID:          "caixa_financeiro.pcf_id",
	CxfinDescricao: "caixa_financeiro.cxfin_descricao",
	CxfinSituacao:  "caixa_financeiro.cxfin_situacao",
}

// Generated where

var CaixaFinanceiroWhere = struct {
	CxfinID        whereHelperint
	PCFID          whereHelperint
	CxfinDescricao whereHelperstring
	CxfinSituacao  whereHelperint
}{
	CxfinID:        whereHelperint{field: "`caixa_financeiro`.`cxfin_id`"},
	PCFID:          whereHelperint{field: "`caixa_financeiro`.`pcf_id`"},
	CxfinDescricao: whereHelperstring{field: "`caixa_financeiro`.`cxfin_descricao`"},
	CxfinSituacao:  whereHelperint{field: "`caixa_financeiro`.`cxfin_situacao`"},
}

// CaixaFinanceiroRels is where relationship names are stored.
var CaixaFinanceiroRels = struct {
	PCF                        string
	CxfinCaixaFinanceiroTotais string
}{
	PCF:                        "PCF",
	CxfinCaixaFinanceiroTotais: "CxfinCaixaFinanceiroTotais",
}

// caixaFinanceiroR is where relationships are stored.
type caixaFinanceiroR struct {
	PCF                        *PlanoContasFinanceiro     `boil:"PCF" json:"PCF" toml:"PCF" yaml:"PCF"`
	CxfinCaixaFinanceiroTotais CaixaFinanceiroTotaisSlice `boil:"CxfinCaixaFinanceiroTotais" json:"CxfinCaixaFinanceiroTotais" toml:"CxfinCaixaFinanceiroTotais" yaml:"CxfinCaixaFinanceiroTotais"`
}

// NewStruct creates a new relationship struct
func (*caixaFinanceiroR) NewStruct() *caixaFinanceiroR {
	return &caixaFinanceiroR{}
}

func (r *caixaFinanceiroR) GetPCF() *PlanoContasFinanceiro {
	if r == nil {
		return nil
	}
	return r.PCF
}

func (r *caixaFinanceiroR) GetCxfinCaixaFinanceiroTotais() CaixaFinanceiroTotaisSlice {
	if r == nil {
		return nil
	}
	return r.CxfinCaixaFinanceiroTotais
}

// caixaFinanceiroL is where Load methods for each relationship are stored.
type caixaFinanceiroL struct{}

var (
	caixaFinanceiroAllColumns            = []string{"cxfin_id", "pcf_id", "cxfin_descricao", "cxfin_situacao"}
	caixaFinanceiroColumnsWithoutDefault = []string{"cxfin_id", "pcf_id", "cxfin_descricao", "cxfin_situacao"}
	caixaFinanceiroColumnsWithDefault    = []string{}
	caixaFinanceiroPrimaryKeyColumns     = []string{"cxfin_id"}
	caixaFinanceiroGeneratedColumns      = []string{}
)

type (
	// CaixaFinanceiroSlice is an alias for a slice of pointers to CaixaFinanceiro.
	// This should almost always be used instead of []CaixaFinanceiro.
	CaixaFinanceiroSlice []*CaixaFinanceiro
	// CaixaFinanceiroHook is the signature for custom CaixaFinanceiro hook methods
	CaixaFinanceiroHook func(context.Context, boil.ContextExecutor, *CaixaFinanceiro) error

	caixaFinanceiroQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	caixaFinanceiroType                 = reflect.TypeOf(&CaixaFinanceiro{})
	caixaFinanceiroMapping              = queries.MakeStructMapping(caixaFinanceiroType)
	caixaFinanceiroPrimaryKeyMapping, _ = queries.BindMapping(caixaFinanceiroType, caixaFinanceiroMapping, caixaFinanceiroPrimaryKeyColumns)
	caixaFinanceiroInsertCacheMut       sync.RWMutex
	caixaFinanceiroInsertCache          = make(map[string]insertCache)
	caixaFinanceiroUpdateCacheMut       sync.RWMutex
	caixaFinanceiroUpdateCache          = make(map[string]updateCache)
	caixaFinanceiroUpsertCacheMut       sync.RWMutex
	caixaFinanceiroUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var caixaFinanceiroAfterSelectMu sync.Mutex
var caixaFinanceiroAfterSelectHooks []CaixaFinanceiroHook

var caixaFinanceiroBeforeInsertMu sync.Mutex
var caixaFinanceiroBeforeInsertHooks []CaixaFinanceiroHook
var caixaFinanceiroAfterInsertMu sync.Mutex
var caixaFinanceiroAfterInsertHooks []CaixaFinanceiroHook

var caixaFinanceiroBeforeUpdateMu sync.Mutex
var caixaFinanceiroBeforeUpdateHooks []CaixaFinanceiroHook
var caixaFinanceiroAfterUpdateMu sync.Mutex
var caixaFinanceiroAfterUpdateHooks []CaixaFinanceiroHook

var caixaFinanceiroBeforeDeleteMu sync.Mutex
var caixaFinanceiroBeforeDeleteHooks []CaixaFinanceiroHook
var caixaFinanceiroAfterDeleteMu sync.Mutex
var caixaFinanceiroAfterDeleteHooks []CaixaFinanceiroHook

var caixaFinanceiroBeforeUpsertMu sync.Mutex
var caixaFinanceiroBeforeUpsertHooks []CaixaFinanceiroHook
var caixaFinanceiroAfterUpsertMu sync.Mutex
var caixaFinanceiroAfterUpsertHooks []CaixaFinanceiroHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CaixaFinanceiro) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CaixaFinanceiro) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CaixaFinanceiro) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CaixaFinanceiro) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CaixaFinanceiro) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CaixaFinanceiro) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CaixaFinanceiro) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CaixaFinanceiro) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CaixaFinanceiro) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCaixaFinanceiroHook registers your hook function for all future operations.
func AddCaixaFinanceiroHook(hookPoint boil.HookPoint, caixaFinanceiroHook CaixaFinanceiroHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		caixaFinanceiroAfterSelectMu.Lock()
		caixaFinanceiroAfterSelectHooks = append(caixaFinanceiroAfterSelectHooks, caixaFinanceiroHook)
		caixaFinanceiroAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		caixaFinanceiroBeforeInsertMu.Lock()
		caixaFinanceiroBeforeInsertHooks = append(caixaFinanceiroBeforeInsertHooks, caixaFinanceiroHook)
		caixaFinanceiroBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		caixaFinanceiroAfterInsertMu.Lock()
		caixaFinanceiroAfterInsertHooks = append(caixaFinanceiroAfterInsertHooks, caixaFinanceiroHook)
		caixaFinanceiroAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		caixaFinanceiroBeforeUpdateMu.Lock()
		caixaFinanceiroBeforeUpdateHooks = append(caixaFinanceiroBeforeUpdateHooks, caixaFinanceiroHook)
		caixaFinanceiroBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		caixaFinanceiroAfterUpdateMu.Lock()
		caixaFinanceiroAfterUpdateHooks = append(caixaFinanceiroAfterUpdateHooks, caixaFinanceiroHook)
		caixaFinanceiroAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		caixaFinanceiroBeforeDeleteMu.Lock()
		caixaFinanceiroBeforeDeleteHooks = append(caixaFinanceiroBeforeDeleteHooks, caixaFinanceiroHook)
		caixaFinanceiroBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		caixaFinanceiroAfterDeleteMu.Lock()
		caixaFinanceiroAfterDeleteHooks = append(caixaFinanceiroAfterDeleteHooks, caixaFinanceiroHook)
		caixaFinanceiroAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		caixaFinanceiroBeforeUpsertMu.Lock()
		caixaFinanceiroBeforeUpsertHooks = append(caixaFinanceiroBeforeUpsertHooks, caixaFinanceiroHook)
		caixaFinanceiroBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		caixaFinanceiroAfterUpsertMu.Lock()
		caixaFinanceiroAfterUpsertHooks = append(caixaFinanceiroAfterUpsertHooks, caixaFinanceiroHook)
		caixaFinanceiroAfterUpsertMu.Unlock()
	}
}

// One returns a single caixaFinanceiro record from the query.
func (q caixaFinanceiroQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CaixaFinanceiro, error) {
	o := &CaixaFinanceiro{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for caixa_financeiro")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CaixaFinanceiro records from the query.
func (q caixaFinanceiroQuery) All(ctx context.Context, exec boil.ContextExecutor) (CaixaFinanceiroSlice, error) {
	var o []*CaixaFinanceiro

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CaixaFinanceiro slice")
	}

	if len(caixaFinanceiroAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CaixaFinanceiro records in the query.
func (q caixaFinanceiroQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count caixa_financeiro rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q caixaFinanceiroQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if caixa_financeiro exists")
	}

	return count > 0, nil
}

// PCF pointed to by the foreign key.
func (o *CaixaFinanceiro) PCF(mods ...qm.QueryMod) planoContasFinanceiroQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pcf_id` = ?", o.PCFID),
	}

	queryMods = append(queryMods, mods...)

	return PlanoContasFinanceiros(queryMods...)
}

// CxfinCaixaFinanceiroTotais retrieves all the caixa_financeiro_totais's CaixaFinanceiroTotais with an executor via cxfin_id column.
func (o *CaixaFinanceiro) CxfinCaixaFinanceiroTotais(mods ...qm.QueryMod) caixaFinanceiroTotaisQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`caixa_financeiro_totais`.`cxfin_id`=?", o.CxfinID),
	)

	return CaixaFinanceiroTotais(queryMods...)
}

// LoadPCF allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (caixaFinanceiroL) LoadPCF(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCaixaFinanceiro interface{}, mods queries.Applicator) error {
	var slice []*CaixaFinanceiro
	var object *CaixaFinanceiro

	if singular {
		var ok bool
		object, ok = maybeCaixaFinanceiro.(*CaixaFinanceiro)
		if !ok {
			object = new(CaixaFinanceiro)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCaixaFinanceiro)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCaixaFinanceiro))
			}
		}
	} else {
		s, ok := maybeCaixaFinanceiro.(*[]*CaixaFinanceiro)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCaixaFinanceiro)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCaixaFinanceiro))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &caixaFinanceiroR{}
		}
		args[object.PCFID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &caixaFinanceiroR{}
			}

			args[obj.PCFID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`plano_contas_financeiro`),
		qm.WhereIn(`plano_contas_financeiro.pcf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PlanoContasFinanceiro")
	}

	var resultSlice []*PlanoContasFinanceiro
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PlanoContasFinanceiro")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for plano_contas_financeiro")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for plano_contas_financeiro")
	}

	if len(planoContasFinanceiroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PCF = foreign
		if foreign.R == nil {
			foreign.R = &planoContasFinanceiroR{}
		}
		foreign.R.PCFCaixaFinanceiros = append(foreign.R.PCFCaixaFinanceiros, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PCFID == foreign.PCFID {
				local.R.PCF = foreign
				if foreign.R == nil {
					foreign.R = &planoContasFinanceiroR{}
				}
				foreign.R.PCFCaixaFinanceiros = append(foreign.R.PCFCaixaFinanceiros, local)
				break
			}
		}
	}

	return nil
}

// LoadCxfinCaixaFinanceiroTotais allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (caixaFinanceiroL) LoadCxfinCaixaFinanceiroTotais(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCaixaFinanceiro interface{}, mods queries.Applicator) error {
	var slice []*CaixaFinanceiro
	var object *CaixaFinanceiro

	if singular {
		var ok bool
		object, ok = maybeCaixaFinanceiro.(*CaixaFinanceiro)
		if !ok {
			object = new(CaixaFinanceiro)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCaixaFinanceiro)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCaixaFinanceiro))
			}
		}
	} else {
		s, ok := maybeCaixaFinanceiro.(*[]*CaixaFinanceiro)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCaixaFinanceiro)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCaixaFinanceiro))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &caixaFinanceiroR{}
		}
		args[object.CxfinID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &caixaFinanceiroR{}
			}
			args[obj.CxfinID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`caixa_financeiro_totais`),
		qm.WhereIn(`caixa_financeiro_totais.cxfin_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load caixa_financeiro_totais")
	}

	var resultSlice []*CaixaFinanceiroTotais
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice caixa_financeiro_totais")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on caixa_financeiro_totais")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for caixa_financeiro_totais")
	}

	if len(caixaFinanceiroTotaisAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CxfinCaixaFinanceiroTotais = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &caixaFinanceiroTotaisR{}
			}
			foreign.R.Cxfin = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.CxfinID == foreign.CxfinID {
				local.R.CxfinCaixaFinanceiroTotais = append(local.R.CxfinCaixaFinanceiroTotais, foreign)
				if foreign.R == nil {
					foreign.R = &caixaFinanceiroTotaisR{}
				}
				foreign.R.Cxfin = local
				break
			}
		}
	}

	return nil
}

// SetPCF of the caixaFinanceiro to the related item.
// Sets o.R.PCF to related.
// Adds o to related.R.PCFCaixaFinanceiros.
func (o *CaixaFinanceiro) SetPCF(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PlanoContasFinanceiro) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `caixa_financeiro` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pcf_id"}),
		strmangle.WhereClause("`", "`", 0, caixaFinanceiroPrimaryKeyColumns),
	)
	values := []interface{}{related.PCFID, o.CxfinID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PCFID = related.PCFID
	if o.R == nil {
		o.R = &caixaFinanceiroR{
			PCF: related,
		}
	} else {
		o.R.PCF = related
	}

	if related.R == nil {
		related.R = &planoContasFinanceiroR{
			PCFCaixaFinanceiros: CaixaFinanceiroSlice{o},
		}
	} else {
		related.R.PCFCaixaFinanceiros = append(related.R.PCFCaixaFinanceiros, o)
	}

	return nil
}

// AddCxfinCaixaFinanceiroTotais adds the given related objects to the existing relationships
// of the caixa_financeiro, optionally inserting them as new records.
// Appends related to o.R.CxfinCaixaFinanceiroTotais.
// Sets related.R.Cxfin appropriately.
func (o *CaixaFinanceiro) AddCxfinCaixaFinanceiroTotais(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*CaixaFinanceiroTotais) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CxfinID = o.CxfinID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `caixa_financeiro_totais` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"cxfin_id"}),
				strmangle.WhereClause("`", "`", 0, caixaFinanceiroTotaisPrimaryKeyColumns),
			)
			values := []interface{}{o.CxfinID, rel.CxfinID, rel.FRTCXID, rel.CxfintItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CxfinID = o.CxfinID
		}
	}

	if o.R == nil {
		o.R = &caixaFinanceiroR{
			CxfinCaixaFinanceiroTotais: related,
		}
	} else {
		o.R.CxfinCaixaFinanceiroTotais = append(o.R.CxfinCaixaFinanceiroTotais, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &caixaFinanceiroTotaisR{
				Cxfin: o,
			}
		} else {
			rel.R.Cxfin = o
		}
	}
	return nil
}

// CaixaFinanceiros retrieves all the records using an executor.
func CaixaFinanceiros(mods ...qm.QueryMod) caixaFinanceiroQuery {
	mods = append(mods, qm.From("`caixa_financeiro`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`caixa_financeiro`.*"})
	}

	return caixaFinanceiroQuery{q}
}

// FindCaixaFinanceiro retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCaixaFinanceiro(ctx context.Context, exec boil.ContextExecutor, cxfinID int, selectCols ...string) (*CaixaFinanceiro, error) {
	caixaFinanceiroObj := &CaixaFinanceiro{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `caixa_financeiro` where `cxfin_id`=?", sel,
	)

	q := queries.Raw(query, cxfinID)

	err := q.Bind(ctx, exec, caixaFinanceiroObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from caixa_financeiro")
	}

	if err = caixaFinanceiroObj.doAfterSelectHooks(ctx, exec); err != nil {
		return caixaFinanceiroObj, err
	}

	return caixaFinanceiroObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CaixaFinanceiro) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no caixa_financeiro provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(caixaFinanceiroColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	caixaFinanceiroInsertCacheMut.RLock()
	cache, cached := caixaFinanceiroInsertCache[key]
	caixaFinanceiroInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			caixaFinanceiroAllColumns,
			caixaFinanceiroColumnsWithDefault,
			caixaFinanceiroColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(caixaFinanceiroType, caixaFinanceiroMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(caixaFinanceiroType, caixaFinanceiroMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `caixa_financeiro` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `caixa_financeiro` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `caixa_financeiro` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, caixaFinanceiroPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into caixa_financeiro")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CxfinID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for caixa_financeiro")
	}

CacheNoHooks:
	if !cached {
		caixaFinanceiroInsertCacheMut.Lock()
		caixaFinanceiroInsertCache[key] = cache
		caixaFinanceiroInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CaixaFinanceiro.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CaixaFinanceiro) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	caixaFinanceiroUpdateCacheMut.RLock()
	cache, cached := caixaFinanceiroUpdateCache[key]
	caixaFinanceiroUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			caixaFinanceiroAllColumns,
			caixaFinanceiroPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update caixa_financeiro, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `caixa_financeiro` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, caixaFinanceiroPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(caixaFinanceiroType, caixaFinanceiroMapping, append(wl, caixaFinanceiroPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update caixa_financeiro row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for caixa_financeiro")
	}

	if !cached {
		caixaFinanceiroUpdateCacheMut.Lock()
		caixaFinanceiroUpdateCache[key] = cache
		caixaFinanceiroUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q caixaFinanceiroQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for caixa_financeiro")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for caixa_financeiro")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CaixaFinanceiroSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), caixaFinanceiroPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `caixa_financeiro` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, caixaFinanceiroPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in caixaFinanceiro slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all caixaFinanceiro")
	}
	return rowsAff, nil
}

var mySQLCaixaFinanceiroUniqueColumns = []string{
	"cxfin_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CaixaFinanceiro) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no caixa_financeiro provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(caixaFinanceiroColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCaixaFinanceiroUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	caixaFinanceiroUpsertCacheMut.RLock()
	cache, cached := caixaFinanceiroUpsertCache[key]
	caixaFinanceiroUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			caixaFinanceiroAllColumns,
			caixaFinanceiroColumnsWithDefault,
			caixaFinanceiroColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			caixaFinanceiroAllColumns,
			caixaFinanceiroPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert caixa_financeiro, could not build update column list")
		}

		ret := strmangle.SetComplement(caixaFinanceiroAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`caixa_financeiro`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `caixa_financeiro` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(caixaFinanceiroType, caixaFinanceiroMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(caixaFinanceiroType, caixaFinanceiroMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for caixa_financeiro")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(caixaFinanceiroType, caixaFinanceiroMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for caixa_financeiro")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for caixa_financeiro")
	}

CacheNoHooks:
	if !cached {
		caixaFinanceiroUpsertCacheMut.Lock()
		caixaFinanceiroUpsertCache[key] = cache
		caixaFinanceiroUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CaixaFinanceiro record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CaixaFinanceiro) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CaixaFinanceiro provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), caixaFinanceiroPrimaryKeyMapping)
	sql := "DELETE FROM `caixa_financeiro` WHERE `cxfin_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from caixa_financeiro")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for caixa_financeiro")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q caixaFinanceiroQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no caixaFinanceiroQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from caixa_financeiro")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for caixa_financeiro")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CaixaFinanceiroSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(caixaFinanceiroBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), caixaFinanceiroPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `caixa_financeiro` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, caixaFinanceiroPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from caixaFinanceiro slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for caixa_financeiro")
	}

	if len(caixaFinanceiroAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CaixaFinanceiro) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCaixaFinanceiro(ctx, exec, o.CxfinID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CaixaFinanceiroSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CaixaFinanceiroSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), caixaFinanceiroPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `caixa_financeiro`.* FROM `caixa_financeiro` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, caixaFinanceiroPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CaixaFinanceiroSlice")
	}

	*o = slice

	return nil
}

// CaixaFinanceiroExists checks if the CaixaFinanceiro row exists.
func CaixaFinanceiroExists(ctx context.Context, exec boil.ContextExecutor, cxfinID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `caixa_financeiro` where `cxfin_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cxfinID)
	}
	row := exec.QueryRowContext(ctx, sql, cxfinID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if caixa_financeiro exists")
	}

	return exists, nil
}

// Exists checks if the CaixaFinanceiro row exists.
func (o *CaixaFinanceiro) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CaixaFinanceiroExists(ctx, exec, o.CxfinID)
}
