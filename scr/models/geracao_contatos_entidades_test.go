// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testGeracaoContatosEntidades(t *testing.T) {
	t.Parallel()

	query := GeracaoContatosEntidades()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testGeracaoContatosEntidadesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGeracaoContatosEntidadesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := GeracaoContatosEntidades().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGeracaoContatosEntidadesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := GeracaoContatosEntidadeSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGeracaoContatosEntidadesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := GeracaoContatosEntidadeExists(ctx, tx, o.GceID)
	if err != nil {
		t.Errorf("Unable to check if GeracaoContatosEntidade exists: %s", err)
	}
	if !e {
		t.Errorf("Expected GeracaoContatosEntidadeExists to return true, but got false.")
	}
}

func testGeracaoContatosEntidadesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	geracaoContatosEntidadeFound, err := FindGeracaoContatosEntidade(ctx, tx, o.GceID)
	if err != nil {
		t.Error(err)
	}

	if geracaoContatosEntidadeFound == nil {
		t.Error("want a record, got nil")
	}
}

func testGeracaoContatosEntidadesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = GeracaoContatosEntidades().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testGeracaoContatosEntidadesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := GeracaoContatosEntidades().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testGeracaoContatosEntidadesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	geracaoContatosEntidadeOne := &GeracaoContatosEntidade{}
	geracaoContatosEntidadeTwo := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, geracaoContatosEntidadeOne, geracaoContatosEntidadeDBTypes, false, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}
	if err = randomize.Struct(seed, geracaoContatosEntidadeTwo, geracaoContatosEntidadeDBTypes, false, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = geracaoContatosEntidadeOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = geracaoContatosEntidadeTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := GeracaoContatosEntidades().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testGeracaoContatosEntidadesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	geracaoContatosEntidadeOne := &GeracaoContatosEntidade{}
	geracaoContatosEntidadeTwo := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, geracaoContatosEntidadeOne, geracaoContatosEntidadeDBTypes, false, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}
	if err = randomize.Struct(seed, geracaoContatosEntidadeTwo, geracaoContatosEntidadeDBTypes, false, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = geracaoContatosEntidadeOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = geracaoContatosEntidadeTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func geracaoContatosEntidadeBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func geracaoContatosEntidadeAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func geracaoContatosEntidadeAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func geracaoContatosEntidadeBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func geracaoContatosEntidadeAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func geracaoContatosEntidadeBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func geracaoContatosEntidadeAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func geracaoContatosEntidadeBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func geracaoContatosEntidadeAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *GeracaoContatosEntidade) error {
	*o = GeracaoContatosEntidade{}
	return nil
}

func testGeracaoContatosEntidadesHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &GeracaoContatosEntidade{}
	o := &GeracaoContatosEntidade{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, false); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade object: %s", err)
	}

	AddGeracaoContatosEntidadeHook(boil.BeforeInsertHook, geracaoContatosEntidadeBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeBeforeInsertHooks = []GeracaoContatosEntidadeHook{}

	AddGeracaoContatosEntidadeHook(boil.AfterInsertHook, geracaoContatosEntidadeAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeAfterInsertHooks = []GeracaoContatosEntidadeHook{}

	AddGeracaoContatosEntidadeHook(boil.AfterSelectHook, geracaoContatosEntidadeAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeAfterSelectHooks = []GeracaoContatosEntidadeHook{}

	AddGeracaoContatosEntidadeHook(boil.BeforeUpdateHook, geracaoContatosEntidadeBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeBeforeUpdateHooks = []GeracaoContatosEntidadeHook{}

	AddGeracaoContatosEntidadeHook(boil.AfterUpdateHook, geracaoContatosEntidadeAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeAfterUpdateHooks = []GeracaoContatosEntidadeHook{}

	AddGeracaoContatosEntidadeHook(boil.BeforeDeleteHook, geracaoContatosEntidadeBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeBeforeDeleteHooks = []GeracaoContatosEntidadeHook{}

	AddGeracaoContatosEntidadeHook(boil.AfterDeleteHook, geracaoContatosEntidadeAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeAfterDeleteHooks = []GeracaoContatosEntidadeHook{}

	AddGeracaoContatosEntidadeHook(boil.BeforeUpsertHook, geracaoContatosEntidadeBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeBeforeUpsertHooks = []GeracaoContatosEntidadeHook{}

	AddGeracaoContatosEntidadeHook(boil.AfterUpsertHook, geracaoContatosEntidadeAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	geracaoContatosEntidadeAfterUpsertHooks = []GeracaoContatosEntidadeHook{}
}

func testGeracaoContatosEntidadesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testGeracaoContatosEntidadesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(geracaoContatosEntidadeColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testGeracaoContatosEntidadeToManyGceGeracaoContatosEntidadesFaturas(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidade
	var b, c GeracaoContatosEntidadesFaturas

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, geracaoContatosEntidadesFaturasDBTypes, false, geracaoContatosEntidadesFaturasColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, geracaoContatosEntidadesFaturasDBTypes, false, geracaoContatosEntidadesFaturasColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.GceID = a.GceID
	c.GceID = a.GceID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.GceGeracaoContatosEntidadesFaturas().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.GceID == b.GceID {
			bFound = true
		}
		if v.GceID == c.GceID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := GeracaoContatosEntidadeSlice{&a}
	if err = a.L.LoadGceGeracaoContatosEntidadesFaturas(ctx, tx, false, (*[]*GeracaoContatosEntidade)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.GceGeracaoContatosEntidadesFaturas); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.GceGeracaoContatosEntidadesFaturas = nil
	if err = a.L.LoadGceGeracaoContatosEntidadesFaturas(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.GceGeracaoContatosEntidadesFaturas); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testGeracaoContatosEntidadeToManyGceGeracaoContatosEntidadesNotaFiscals(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidade
	var b, c GeracaoContatosEntidadesNotaFiscal

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, geracaoContatosEntidadesNotaFiscalDBTypes, false, geracaoContatosEntidadesNotaFiscalColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.GceID = a.GceID
	c.GceID = a.GceID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.GceGeracaoContatosEntidadesNotaFiscals().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.GceID == b.GceID {
			bFound = true
		}
		if v.GceID == c.GceID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := GeracaoContatosEntidadeSlice{&a}
	if err = a.L.LoadGceGeracaoContatosEntidadesNotaFiscals(ctx, tx, false, (*[]*GeracaoContatosEntidade)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.GceGeracaoContatosEntidadesNotaFiscals); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.GceGeracaoContatosEntidadesNotaFiscals = nil
	if err = a.L.LoadGceGeracaoContatosEntidadesNotaFiscals(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.GceGeracaoContatosEntidadesNotaFiscals); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testGeracaoContatosEntidadeToManyAddOpGceGeracaoContatosEntidadesFaturas(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidade
	var b, c, d, e GeracaoContatosEntidadesFaturas

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadeDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadePrimaryKeyColumns, geracaoContatosEntidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*GeracaoContatosEntidadesFaturas{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, geracaoContatosEntidadesFaturasDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadesFaturasPrimaryKeyColumns, geracaoContatosEntidadesFaturasColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*GeracaoContatosEntidadesFaturas{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddGceGeracaoContatosEntidadesFaturas(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.GceID != first.GceID {
			t.Error("foreign key was wrong value", a.GceID, first.GceID)
		}
		if a.GceID != second.GceID {
			t.Error("foreign key was wrong value", a.GceID, second.GceID)
		}

		if first.R.Gce != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Gce != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.GceGeracaoContatosEntidadesFaturas[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.GceGeracaoContatosEntidadesFaturas[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.GceGeracaoContatosEntidadesFaturas().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testGeracaoContatosEntidadeToManyAddOpGceGeracaoContatosEntidadesNotaFiscals(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidade
	var b, c, d, e GeracaoContatosEntidadesNotaFiscal

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadeDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadePrimaryKeyColumns, geracaoContatosEntidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*GeracaoContatosEntidadesNotaFiscal{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, geracaoContatosEntidadesNotaFiscalDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadesNotaFiscalPrimaryKeyColumns, geracaoContatosEntidadesNotaFiscalColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*GeracaoContatosEntidadesNotaFiscal{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddGceGeracaoContatosEntidadesNotaFiscals(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.GceID != first.GceID {
			t.Error("foreign key was wrong value", a.GceID, first.GceID)
		}
		if a.GceID != second.GceID {
			t.Error("foreign key was wrong value", a.GceID, second.GceID)
		}

		if first.R.Gce != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Gce != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.GceGeracaoContatosEntidadesNotaFiscals[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.GceGeracaoContatosEntidadesNotaFiscals[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.GceGeracaoContatosEntidadesNotaFiscals().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testGeracaoContatosEntidadeToOneEmpresaFilialUsingEmf(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local GeracaoContatosEntidade
	var foreign EmpresaFilial

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, geracaoContatosEntidadeDBTypes, false, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, empresaFilialDBTypes, false, empresaFilialColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize EmpresaFilial struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.EmfID = foreign.EmfID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Emf().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.EmfID != foreign.EmfID {
		t.Errorf("want: %v, got %v", foreign.EmfID, check.EmfID)
	}

	ranAfterSelectHook := false
	AddEmpresaFilialHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *EmpresaFilial) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := GeracaoContatosEntidadeSlice{&local}
	if err = local.L.LoadEmf(ctx, tx, false, (*[]*GeracaoContatosEntidade)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Emf == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Emf = nil
	if err = local.L.LoadEmf(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Emf == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testGeracaoContatosEntidadeToOneSetOpEmpresaFilialUsingEmf(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a GeracaoContatosEntidade
	var b, c EmpresaFilial

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, geracaoContatosEntidadeDBTypes, false, strmangle.SetComplement(geracaoContatosEntidadePrimaryKeyColumns, geracaoContatosEntidadeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, empresaFilialDBTypes, false, strmangle.SetComplement(empresaFilialPrimaryKeyColumns, empresaFilialColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, empresaFilialDBTypes, false, strmangle.SetComplement(empresaFilialPrimaryKeyColumns, empresaFilialColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*EmpresaFilial{&b, &c} {
		err = a.SetEmf(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Emf != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.EmfGeracaoContatosEntidades[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.EmfID != x.EmfID {
			t.Error("foreign key was wrong value", a.EmfID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.EmfID))
		reflect.Indirect(reflect.ValueOf(&a.EmfID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.EmfID != x.EmfID {
			t.Error("foreign key was wrong value", a.EmfID, x.EmfID)
		}
	}
}

func testGeracaoContatosEntidadesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testGeracaoContatosEntidadesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := GeracaoContatosEntidadeSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testGeracaoContatosEntidadesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := GeracaoContatosEntidades().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	geracaoContatosEntidadeDBTypes = map[string]string{`GceID`: `int`, `EmfID`: `int`, `GceDatageracao`: `date`, `GceDatareferenciaIni`: `date`, `GceDatareferenciaFim`: `date`, `GceSituacao`: `int`, `GceTipo`: `int`}
	_                              = bytes.MinRead
)

func testGeracaoContatosEntidadesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(geracaoContatosEntidadePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(geracaoContatosEntidadeAllColumns) == len(geracaoContatosEntidadePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testGeracaoContatosEntidadesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(geracaoContatosEntidadeAllColumns) == len(geracaoContatosEntidadePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, geracaoContatosEntidadeDBTypes, true, geracaoContatosEntidadePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(geracaoContatosEntidadeAllColumns, geracaoContatosEntidadePrimaryKeyColumns) {
		fields = geracaoContatosEntidadeAllColumns
	} else {
		fields = strmangle.SetComplement(
			geracaoContatosEntidadeAllColumns,
			geracaoContatosEntidadePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := GeracaoContatosEntidadeSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testGeracaoContatosEntidadesUpsert(t *testing.T) {
	t.Parallel()

	if len(geracaoContatosEntidadeAllColumns) == len(geracaoContatosEntidadePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLGeracaoContatosEntidadeUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := GeracaoContatosEntidade{}
	if err = randomize.Struct(seed, &o, geracaoContatosEntidadeDBTypes, false); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert GeracaoContatosEntidade: %s", err)
	}

	count, err := GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, geracaoContatosEntidadeDBTypes, false, geracaoContatosEntidadePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GeracaoContatosEntidade struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert GeracaoContatosEntidade: %s", err)
	}

	count, err = GeracaoContatosEntidades().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
