// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CaixaFinanceiroTotais is an object representing the database table.
type CaixaFinanceiroTotais struct {
	CxfinID              int               `boil:"cxfin_id" json:"cxfin_id" toml:"cxfin_id" yaml:"cxfin_id"`
	FRTCXID              int               `boil:"frtcx_id" json:"frtcx_id" toml:"frtcx_id" yaml:"frtcx_id"`
	CxfintItem           int               `boil:"cxfint_item" json:"cxfint_item" toml:"cxfint_item" yaml:"cxfint_item"`
	CxfintDataabertura   time.Time         `boil:"cxfint_dataabertura" json:"cxfint_dataabertura" toml:"cxfint_dataabertura" yaml:"cxfint_dataabertura"`
	CxfintDatafechamento null.Time         `boil:"cxfint_datafechamento" json:"cxfint_datafechamento,omitempty" toml:"cxfint_datafechamento" yaml:"cxfint_datafechamento,omitempty"`
	CxfintSituacao       int               `boil:"cxfint_situacao" json:"cxfint_situacao" toml:"cxfint_situacao" yaml:"cxfint_situacao"`
	CxfintValorcalculado types.NullDecimal `boil:"cxfint_valorcalculado" json:"cxfint_valorcalculado,omitempty" toml:"cxfint_valorcalculado" yaml:"cxfint_valorcalculado,omitempty"`
	CxfintValorinformado types.NullDecimal `boil:"cxfint_valorinformado" json:"cxfint_valorinformado,omitempty" toml:"cxfint_valorinformado" yaml:"cxfint_valorinformado,omitempty"`
	CxfintValorabertura  types.NullDecimal `boil:"cxfint_valorabertura" json:"cxfint_valorabertura,omitempty" toml:"cxfint_valorabertura" yaml:"cxfint_valorabertura,omitempty"`
	CxfintIndice         null.Int          `boil:"cxfint_indice" json:"cxfint_indice,omitempty" toml:"cxfint_indice" yaml:"cxfint_indice,omitempty"`

	R *caixaFinanceiroTotaisR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L caixaFinanceiroTotaisL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CaixaFinanceiroTotaisColumns = struct {
	CxfinID              string
	FRTCXID              string
	CxfintItem           string
	CxfintDataabertura   string
	CxfintDatafechamento string
	CxfintSituacao       string
	CxfintValorcalculado string
	CxfintValorinformado string
	CxfintValorabertura  string
	CxfintIndice         string
}{
	CxfinID:              "cxfin_id",
	FRTCXID:              "frtcx_id",
	CxfintItem:           "cxfint_item",
	CxfintDataabertura:   "cxfint_dataabertura",
	CxfintDatafechamento: "cxfint_datafechamento",
	CxfintSituacao:       "cxfint_situacao",
	CxfintValorcalculado: "cxfint_valorcalculado",
	CxfintValorinformado: "cxfint_valorinformado",
	CxfintValorabertura:  "cxfint_valorabertura",
	CxfintIndice:         "cxfint_indice",
}

var CaixaFinanceiroTotaisTableColumns = struct {
	CxfinID              string
	FRTCXID              string
	CxfintItem           string
	CxfintDataabertura   string
	CxfintDatafechamento string
	CxfintSituacao       string
	CxfintValorcalculado string
	CxfintValorinformado string
	CxfintValorabertura  string
	CxfintIndice         string
}{
	CxfinID:              "caixa_financeiro_totais.cxfin_id",
	FRTCXID:              "caixa_financeiro_totais.frtcx_id",
	CxfintItem:           "caixa_financeiro_totais.cxfint_item",
	CxfintDataabertura:   "caixa_financeiro_totais.cxfint_dataabertura",
	CxfintDatafechamento: "caixa_financeiro_totais.cxfint_datafechamento",
	CxfintSituacao:       "caixa_financeiro_totais.cxfint_situacao",
	CxfintValorcalculado: "caixa_financeiro_totais.cxfint_valorcalculado",
	CxfintValorinformado: "caixa_financeiro_totais.cxfint_valorinformado",
	CxfintValorabertura:  "caixa_financeiro_totais.cxfint_valorabertura",
	CxfintIndice:         "caixa_financeiro_totais.cxfint_indice",
}

// Generated where

var CaixaFinanceiroTotaisWhere = struct {
	CxfinID              whereHelperint
	FRTCXID              whereHelperint
	CxfintItem           whereHelperint
	CxfintDataabertura   whereHelpertime_Time
	CxfintDatafechamento whereHelpernull_Time
	CxfintSituacao       whereHelperint
	CxfintValorcalculado whereHelpertypes_NullDecimal
	CxfintValorinformado whereHelpertypes_NullDecimal
	CxfintValorabertura  whereHelpertypes_NullDecimal
	CxfintIndice         whereHelpernull_Int
}{
	CxfinID:              whereHelperint{field: "`caixa_financeiro_totais`.`cxfin_id`"},
	FRTCXID:              whereHelperint{field: "`caixa_financeiro_totais`.`frtcx_id`"},
	CxfintItem:           whereHelperint{field: "`caixa_financeiro_totais`.`cxfint_item`"},
	CxfintDataabertura:   whereHelpertime_Time{field: "`caixa_financeiro_totais`.`cxfint_dataabertura`"},
	CxfintDatafechamento: whereHelpernull_Time{field: "`caixa_financeiro_totais`.`cxfint_datafechamento`"},
	CxfintSituacao:       whereHelperint{field: "`caixa_financeiro_totais`.`cxfint_situacao`"},
	CxfintValorcalculado: whereHelpertypes_NullDecimal{field: "`caixa_financeiro_totais`.`cxfint_valorcalculado`"},
	CxfintValorinformado: whereHelpertypes_NullDecimal{field: "`caixa_financeiro_totais`.`cxfint_valorinformado`"},
	CxfintValorabertura:  whereHelpertypes_NullDecimal{field: "`caixa_financeiro_totais`.`cxfint_valorabertura`"},
	CxfintIndice:         whereHelpernull_Int{field: "`caixa_financeiro_totais`.`cxfint_indice`"},
}

// CaixaFinanceiroTotaisRels is where relationship names are stored.
var CaixaFinanceiroTotaisRels = struct {
	Cxfin string
	FRTCX string
}{
	Cxfin: "Cxfin",
	FRTCX: "FRTCX",
}

// caixaFinanceiroTotaisR is where relationships are stored.
type caixaFinanceiroTotaisR struct {
	Cxfin *CaixaFinanceiro `boil:"Cxfin" json:"Cxfin" toml:"Cxfin" yaml:"Cxfin"`
	FRTCX *FrenteCaixa     `boil:"FRTCX" json:"FRTCX" toml:"FRTCX" yaml:"FRTCX"`
}

// NewStruct creates a new relationship struct
func (*caixaFinanceiroTotaisR) NewStruct() *caixaFinanceiroTotaisR {
	return &caixaFinanceiroTotaisR{}
}

func (r *caixaFinanceiroTotaisR) GetCxfin() *CaixaFinanceiro {
	if r == nil {
		return nil
	}
	return r.Cxfin
}

func (r *caixaFinanceiroTotaisR) GetFRTCX() *FrenteCaixa {
	if r == nil {
		return nil
	}
	return r.FRTCX
}

// caixaFinanceiroTotaisL is where Load methods for each relationship are stored.
type caixaFinanceiroTotaisL struct{}

var (
	caixaFinanceiroTotaisAllColumns            = []string{"cxfin_id", "frtcx_id", "cxfint_item", "cxfint_dataabertura", "cxfint_datafechamento", "cxfint_situacao", "cxfint_valorcalculado", "cxfint_valorinformado", "cxfint_valorabertura", "cxfint_indice"}
	caixaFinanceiroTotaisColumnsWithoutDefault = []string{"cxfin_id", "frtcx_id", "cxfint_item", "cxfint_dataabertura", "cxfint_datafechamento", "cxfint_situacao", "cxfint_valorcalculado", "cxfint_valorinformado", "cxfint_valorabertura"}
	caixaFinanceiroTotaisColumnsWithDefault    = []string{"cxfint_indice"}
	caixaFinanceiroTotaisPrimaryKeyColumns     = []string{"cxfin_id", "frtcx_id", "cxfint_item"}
	caixaFinanceiroTotaisGeneratedColumns      = []string{}
)

type (
	// CaixaFinanceiroTotaisSlice is an alias for a slice of pointers to CaixaFinanceiroTotais.
	// This should almost always be used instead of []CaixaFinanceiroTotais.
	CaixaFinanceiroTotaisSlice []*CaixaFinanceiroTotais
	// CaixaFinanceiroTotaisHook is the signature for custom CaixaFinanceiroTotais hook methods
	CaixaFinanceiroTotaisHook func(context.Context, boil.ContextExecutor, *CaixaFinanceiroTotais) error

	caixaFinanceiroTotaisQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	caixaFinanceiroTotaisType                 = reflect.TypeOf(&CaixaFinanceiroTotais{})
	caixaFinanceiroTotaisMapping              = queries.MakeStructMapping(caixaFinanceiroTotaisType)
	caixaFinanceiroTotaisPrimaryKeyMapping, _ = queries.BindMapping(caixaFinanceiroTotaisType, caixaFinanceiroTotaisMapping, caixaFinanceiroTotaisPrimaryKeyColumns)
	caixaFinanceiroTotaisInsertCacheMut       sync.RWMutex
	caixaFinanceiroTotaisInsertCache          = make(map[string]insertCache)
	caixaFinanceiroTotaisUpdateCacheMut       sync.RWMutex
	caixaFinanceiroTotaisUpdateCache          = make(map[string]updateCache)
	caixaFinanceiroTotaisUpsertCacheMut       sync.RWMutex
	caixaFinanceiroTotaisUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var caixaFinanceiroTotaisAfterSelectMu sync.Mutex
var caixaFinanceiroTotaisAfterSelectHooks []CaixaFinanceiroTotaisHook

var caixaFinanceiroTotaisBeforeInsertMu sync.Mutex
var caixaFinanceiroTotaisBeforeInsertHooks []CaixaFinanceiroTotaisHook
var caixaFinanceiroTotaisAfterInsertMu sync.Mutex
var caixaFinanceiroTotaisAfterInsertHooks []CaixaFinanceiroTotaisHook

var caixaFinanceiroTotaisBeforeUpdateMu sync.Mutex
var caixaFinanceiroTotaisBeforeUpdateHooks []CaixaFinanceiroTotaisHook
var caixaFinanceiroTotaisAfterUpdateMu sync.Mutex
var caixaFinanceiroTotaisAfterUpdateHooks []CaixaFinanceiroTotaisHook

var caixaFinanceiroTotaisBeforeDeleteMu sync.Mutex
var caixaFinanceiroTotaisBeforeDeleteHooks []CaixaFinanceiroTotaisHook
var caixaFinanceiroTotaisAfterDeleteMu sync.Mutex
var caixaFinanceiroTotaisAfterDeleteHooks []CaixaFinanceiroTotaisHook

var caixaFinanceiroTotaisBeforeUpsertMu sync.Mutex
var caixaFinanceiroTotaisBeforeUpsertHooks []CaixaFinanceiroTotaisHook
var caixaFinanceiroTotaisAfterUpsertMu sync.Mutex
var caixaFinanceiroTotaisAfterUpsertHooks []CaixaFinanceiroTotaisHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CaixaFinanceiroTotais) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CaixaFinanceiroTotais) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CaixaFinanceiroTotais) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CaixaFinanceiroTotais) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CaixaFinanceiroTotais) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CaixaFinanceiroTotais) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CaixaFinanceiroTotais) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CaixaFinanceiroTotais) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CaixaFinanceiroTotais) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range caixaFinanceiroTotaisAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCaixaFinanceiroTotaisHook registers your hook function for all future operations.
func AddCaixaFinanceiroTotaisHook(hookPoint boil.HookPoint, caixaFinanceiroTotaisHook CaixaFinanceiroTotaisHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		caixaFinanceiroTotaisAfterSelectMu.Lock()
		caixaFinanceiroTotaisAfterSelectHooks = append(caixaFinanceiroTotaisAfterSelectHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		caixaFinanceiroTotaisBeforeInsertMu.Lock()
		caixaFinanceiroTotaisBeforeInsertHooks = append(caixaFinanceiroTotaisBeforeInsertHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		caixaFinanceiroTotaisAfterInsertMu.Lock()
		caixaFinanceiroTotaisAfterInsertHooks = append(caixaFinanceiroTotaisAfterInsertHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		caixaFinanceiroTotaisBeforeUpdateMu.Lock()
		caixaFinanceiroTotaisBeforeUpdateHooks = append(caixaFinanceiroTotaisBeforeUpdateHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		caixaFinanceiroTotaisAfterUpdateMu.Lock()
		caixaFinanceiroTotaisAfterUpdateHooks = append(caixaFinanceiroTotaisAfterUpdateHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		caixaFinanceiroTotaisBeforeDeleteMu.Lock()
		caixaFinanceiroTotaisBeforeDeleteHooks = append(caixaFinanceiroTotaisBeforeDeleteHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		caixaFinanceiroTotaisAfterDeleteMu.Lock()
		caixaFinanceiroTotaisAfterDeleteHooks = append(caixaFinanceiroTotaisAfterDeleteHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		caixaFinanceiroTotaisBeforeUpsertMu.Lock()
		caixaFinanceiroTotaisBeforeUpsertHooks = append(caixaFinanceiroTotaisBeforeUpsertHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		caixaFinanceiroTotaisAfterUpsertMu.Lock()
		caixaFinanceiroTotaisAfterUpsertHooks = append(caixaFinanceiroTotaisAfterUpsertHooks, caixaFinanceiroTotaisHook)
		caixaFinanceiroTotaisAfterUpsertMu.Unlock()
	}
}

// One returns a single caixaFinanceiroTotais record from the query.
func (q caixaFinanceiroTotaisQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CaixaFinanceiroTotais, error) {
	o := &CaixaFinanceiroTotais{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for caixa_financeiro_totais")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CaixaFinanceiroTotais records from the query.
func (q caixaFinanceiroTotaisQuery) All(ctx context.Context, exec boil.ContextExecutor) (CaixaFinanceiroTotaisSlice, error) {
	var o []*CaixaFinanceiroTotais

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CaixaFinanceiroTotais slice")
	}

	if len(caixaFinanceiroTotaisAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CaixaFinanceiroTotais records in the query.
func (q caixaFinanceiroTotaisQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count caixa_financeiro_totais rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q caixaFinanceiroTotaisQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if caixa_financeiro_totais exists")
	}

	return count > 0, nil
}

// Cxfin pointed to by the foreign key.
func (o *CaixaFinanceiroTotais) Cxfin(mods ...qm.QueryMod) caixaFinanceiroQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cxfin_id` = ?", o.CxfinID),
	}

	queryMods = append(queryMods, mods...)

	return CaixaFinanceiros(queryMods...)
}

// FRTCX pointed to by the foreign key.
func (o *CaixaFinanceiroTotais) FRTCX(mods ...qm.QueryMod) frenteCaixaQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`frtcx_id` = ?", o.FRTCXID),
	}

	queryMods = append(queryMods, mods...)

	return FrenteCaixas(queryMods...)
}

// LoadCxfin allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (caixaFinanceiroTotaisL) LoadCxfin(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCaixaFinanceiroTotais interface{}, mods queries.Applicator) error {
	var slice []*CaixaFinanceiroTotais
	var object *CaixaFinanceiroTotais

	if singular {
		var ok bool
		object, ok = maybeCaixaFinanceiroTotais.(*CaixaFinanceiroTotais)
		if !ok {
			object = new(CaixaFinanceiroTotais)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCaixaFinanceiroTotais)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCaixaFinanceiroTotais))
			}
		}
	} else {
		s, ok := maybeCaixaFinanceiroTotais.(*[]*CaixaFinanceiroTotais)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCaixaFinanceiroTotais)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCaixaFinanceiroTotais))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &caixaFinanceiroTotaisR{}
		}
		args[object.CxfinID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &caixaFinanceiroTotaisR{}
			}

			args[obj.CxfinID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`caixa_financeiro`),
		qm.WhereIn(`caixa_financeiro.cxfin_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load CaixaFinanceiro")
	}

	var resultSlice []*CaixaFinanceiro
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice CaixaFinanceiro")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for caixa_financeiro")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for caixa_financeiro")
	}

	if len(caixaFinanceiroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cxfin = foreign
		if foreign.R == nil {
			foreign.R = &caixaFinanceiroR{}
		}
		foreign.R.CxfinCaixaFinanceiroTotais = append(foreign.R.CxfinCaixaFinanceiroTotais, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CxfinID == foreign.CxfinID {
				local.R.Cxfin = foreign
				if foreign.R == nil {
					foreign.R = &caixaFinanceiroR{}
				}
				foreign.R.CxfinCaixaFinanceiroTotais = append(foreign.R.CxfinCaixaFinanceiroTotais, local)
				break
			}
		}
	}

	return nil
}

// LoadFRTCX allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (caixaFinanceiroTotaisL) LoadFRTCX(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCaixaFinanceiroTotais interface{}, mods queries.Applicator) error {
	var slice []*CaixaFinanceiroTotais
	var object *CaixaFinanceiroTotais

	if singular {
		var ok bool
		object, ok = maybeCaixaFinanceiroTotais.(*CaixaFinanceiroTotais)
		if !ok {
			object = new(CaixaFinanceiroTotais)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCaixaFinanceiroTotais)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCaixaFinanceiroTotais))
			}
		}
	} else {
		s, ok := maybeCaixaFinanceiroTotais.(*[]*CaixaFinanceiroTotais)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCaixaFinanceiroTotais)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCaixaFinanceiroTotais))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &caixaFinanceiroTotaisR{}
		}
		args[object.FRTCXID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &caixaFinanceiroTotaisR{}
			}

			args[obj.FRTCXID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`frente_caixa`),
		qm.WhereIn(`frente_caixa.frtcx_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load FrenteCaixa")
	}

	var resultSlice []*FrenteCaixa
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice FrenteCaixa")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for frente_caixa")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for frente_caixa")
	}

	if len(frenteCaixaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.FRTCX = foreign
		if foreign.R == nil {
			foreign.R = &frenteCaixaR{}
		}
		foreign.R.FRTCXCaixaFinanceiroTotais = append(foreign.R.FRTCXCaixaFinanceiroTotais, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.FRTCXID == foreign.FRTCXID {
				local.R.FRTCX = foreign
				if foreign.R == nil {
					foreign.R = &frenteCaixaR{}
				}
				foreign.R.FRTCXCaixaFinanceiroTotais = append(foreign.R.FRTCXCaixaFinanceiroTotais, local)
				break
			}
		}
	}

	return nil
}

// SetCxfin of the caixaFinanceiroTotais to the related item.
// Sets o.R.Cxfin to related.
// Adds o to related.R.CxfinCaixaFinanceiroTotais.
func (o *CaixaFinanceiroTotais) SetCxfin(ctx context.Context, exec boil.ContextExecutor, insert bool, related *CaixaFinanceiro) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `caixa_financeiro_totais` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cxfin_id"}),
		strmangle.WhereClause("`", "`", 0, caixaFinanceiroTotaisPrimaryKeyColumns),
	)
	values := []interface{}{related.CxfinID, o.CxfinID, o.FRTCXID, o.CxfintItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CxfinID = related.CxfinID
	if o.R == nil {
		o.R = &caixaFinanceiroTotaisR{
			Cxfin: related,
		}
	} else {
		o.R.Cxfin = related
	}

	if related.R == nil {
		related.R = &caixaFinanceiroR{
			CxfinCaixaFinanceiroTotais: CaixaFinanceiroTotaisSlice{o},
		}
	} else {
		related.R.CxfinCaixaFinanceiroTotais = append(related.R.CxfinCaixaFinanceiroTotais, o)
	}

	return nil
}

// SetFRTCX of the caixaFinanceiroTotais to the related item.
// Sets o.R.FRTCX to related.
// Adds o to related.R.FRTCXCaixaFinanceiroTotais.
func (o *CaixaFinanceiroTotais) SetFRTCX(ctx context.Context, exec boil.ContextExecutor, insert bool, related *FrenteCaixa) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `caixa_financeiro_totais` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"frtcx_id"}),
		strmangle.WhereClause("`", "`", 0, caixaFinanceiroTotaisPrimaryKeyColumns),
	)
	values := []interface{}{related.FRTCXID, o.CxfinID, o.FRTCXID, o.CxfintItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.FRTCXID = related.FRTCXID
	if o.R == nil {
		o.R = &caixaFinanceiroTotaisR{
			FRTCX: related,
		}
	} else {
		o.R.FRTCX = related
	}

	if related.R == nil {
		related.R = &frenteCaixaR{
			FRTCXCaixaFinanceiroTotais: CaixaFinanceiroTotaisSlice{o},
		}
	} else {
		related.R.FRTCXCaixaFinanceiroTotais = append(related.R.FRTCXCaixaFinanceiroTotais, o)
	}

	return nil
}

// CaixaFinanceiroTotais retrieves all the records using an executor.
func CaixaFinanceiroTotais(mods ...qm.QueryMod) caixaFinanceiroTotaisQuery {
	mods = append(mods, qm.From("`caixa_financeiro_totais`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`caixa_financeiro_totais`.*"})
	}

	return caixaFinanceiroTotaisQuery{q}
}

// FindCaixaFinanceiroTotais retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCaixaFinanceiroTotais(ctx context.Context, exec boil.ContextExecutor, cxfinID int, fRTCXID int, cxfintItem int, selectCols ...string) (*CaixaFinanceiroTotais, error) {
	caixaFinanceiroTotaisObj := &CaixaFinanceiroTotais{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `caixa_financeiro_totais` where `cxfin_id`=? AND `frtcx_id`=? AND `cxfint_item`=?", sel,
	)

	q := queries.Raw(query, cxfinID, fRTCXID, cxfintItem)

	err := q.Bind(ctx, exec, caixaFinanceiroTotaisObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from caixa_financeiro_totais")
	}

	if err = caixaFinanceiroTotaisObj.doAfterSelectHooks(ctx, exec); err != nil {
		return caixaFinanceiroTotaisObj, err
	}

	return caixaFinanceiroTotaisObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CaixaFinanceiroTotais) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no caixa_financeiro_totais provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(caixaFinanceiroTotaisColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	caixaFinanceiroTotaisInsertCacheMut.RLock()
	cache, cached := caixaFinanceiroTotaisInsertCache[key]
	caixaFinanceiroTotaisInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			caixaFinanceiroTotaisAllColumns,
			caixaFinanceiroTotaisColumnsWithDefault,
			caixaFinanceiroTotaisColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(caixaFinanceiroTotaisType, caixaFinanceiroTotaisMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(caixaFinanceiroTotaisType, caixaFinanceiroTotaisMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `caixa_financeiro_totais` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `caixa_financeiro_totais` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `caixa_financeiro_totais` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, caixaFinanceiroTotaisPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into caixa_financeiro_totais")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CxfinID,
		o.FRTCXID,
		o.CxfintItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for caixa_financeiro_totais")
	}

CacheNoHooks:
	if !cached {
		caixaFinanceiroTotaisInsertCacheMut.Lock()
		caixaFinanceiroTotaisInsertCache[key] = cache
		caixaFinanceiroTotaisInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CaixaFinanceiroTotais.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CaixaFinanceiroTotais) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	caixaFinanceiroTotaisUpdateCacheMut.RLock()
	cache, cached := caixaFinanceiroTotaisUpdateCache[key]
	caixaFinanceiroTotaisUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			caixaFinanceiroTotaisAllColumns,
			caixaFinanceiroTotaisPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update caixa_financeiro_totais, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `caixa_financeiro_totais` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, caixaFinanceiroTotaisPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(caixaFinanceiroTotaisType, caixaFinanceiroTotaisMapping, append(wl, caixaFinanceiroTotaisPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update caixa_financeiro_totais row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for caixa_financeiro_totais")
	}

	if !cached {
		caixaFinanceiroTotaisUpdateCacheMut.Lock()
		caixaFinanceiroTotaisUpdateCache[key] = cache
		caixaFinanceiroTotaisUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q caixaFinanceiroTotaisQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for caixa_financeiro_totais")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for caixa_financeiro_totais")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CaixaFinanceiroTotaisSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), caixaFinanceiroTotaisPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `caixa_financeiro_totais` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, caixaFinanceiroTotaisPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in caixaFinanceiroTotais slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all caixaFinanceiroTotais")
	}
	return rowsAff, nil
}

var mySQLCaixaFinanceiroTotaisUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CaixaFinanceiroTotais) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no caixa_financeiro_totais provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(caixaFinanceiroTotaisColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCaixaFinanceiroTotaisUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	caixaFinanceiroTotaisUpsertCacheMut.RLock()
	cache, cached := caixaFinanceiroTotaisUpsertCache[key]
	caixaFinanceiroTotaisUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			caixaFinanceiroTotaisAllColumns,
			caixaFinanceiroTotaisColumnsWithDefault,
			caixaFinanceiroTotaisColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			caixaFinanceiroTotaisAllColumns,
			caixaFinanceiroTotaisPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert caixa_financeiro_totais, could not build update column list")
		}

		ret := strmangle.SetComplement(caixaFinanceiroTotaisAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`caixa_financeiro_totais`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `caixa_financeiro_totais` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(caixaFinanceiroTotaisType, caixaFinanceiroTotaisMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(caixaFinanceiroTotaisType, caixaFinanceiroTotaisMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for caixa_financeiro_totais")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(caixaFinanceiroTotaisType, caixaFinanceiroTotaisMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for caixa_financeiro_totais")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for caixa_financeiro_totais")
	}

CacheNoHooks:
	if !cached {
		caixaFinanceiroTotaisUpsertCacheMut.Lock()
		caixaFinanceiroTotaisUpsertCache[key] = cache
		caixaFinanceiroTotaisUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CaixaFinanceiroTotais record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CaixaFinanceiroTotais) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CaixaFinanceiroTotais provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), caixaFinanceiroTotaisPrimaryKeyMapping)
	sql := "DELETE FROM `caixa_financeiro_totais` WHERE `cxfin_id`=? AND `frtcx_id`=? AND `cxfint_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from caixa_financeiro_totais")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for caixa_financeiro_totais")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q caixaFinanceiroTotaisQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no caixaFinanceiroTotaisQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from caixa_financeiro_totais")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for caixa_financeiro_totais")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CaixaFinanceiroTotaisSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(caixaFinanceiroTotaisBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), caixaFinanceiroTotaisPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `caixa_financeiro_totais` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, caixaFinanceiroTotaisPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from caixaFinanceiroTotais slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for caixa_financeiro_totais")
	}

	if len(caixaFinanceiroTotaisAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CaixaFinanceiroTotais) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCaixaFinanceiroTotais(ctx, exec, o.CxfinID, o.FRTCXID, o.CxfintItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CaixaFinanceiroTotaisSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CaixaFinanceiroTotaisSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), caixaFinanceiroTotaisPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `caixa_financeiro_totais`.* FROM `caixa_financeiro_totais` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, caixaFinanceiroTotaisPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CaixaFinanceiroTotaisSlice")
	}

	*o = slice

	return nil
}

// CaixaFinanceiroTotaisExists checks if the CaixaFinanceiroTotais row exists.
func CaixaFinanceiroTotaisExists(ctx context.Context, exec boil.ContextExecutor, cxfinID int, fRTCXID int, cxfintItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `caixa_financeiro_totais` where `cxfin_id`=? AND `frtcx_id`=? AND `cxfint_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cxfinID, fRTCXID, cxfintItem)
	}
	row := exec.QueryRowContext(ctx, sql, cxfinID, fRTCXID, cxfintItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if caixa_financeiro_totais exists")
	}

	return exists, nil
}

// Exists checks if the CaixaFinanceiroTotais row exists.
func (o *CaixaFinanceiroTotais) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CaixaFinanceiroTotaisExists(ctx, exec, o.CxfinID, o.FRTCXID, o.CxfintItem)
}
