// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TipoCombustivel is an object representing the database table.
type TipoCombustivel struct {
	TCBID        int    `boil:"tcb_id" json:"tcb_id" toml:"tcb_id" yaml:"tcb_id"`
	TCBDescricao string `boil:"tcb_descricao" json:"tcb_descricao" toml:"tcb_descricao" yaml:"tcb_descricao"`

	R *tipoCombustivelR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tipoCombustivelL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TipoCombustivelColumns = struct {
	TCBID        string
	TCBDescricao string
}{
	TCBID:        "tcb_id",
	TCBDescricao: "tcb_descricao",
}

var TipoCombustivelTableColumns = struct {
	TCBID        string
	TCBDescricao string
}{
	TCBID:        "tipo_combustivel.tcb_id",
	TCBDescricao: "tipo_combustivel.tcb_descricao",
}

// Generated where

var TipoCombustivelWhere = struct {
	TCBID        whereHelperint
	TCBDescricao whereHelperstring
}{
	TCBID:        whereHelperint{field: "`tipo_combustivel`.`tcb_id`"},
	TCBDescricao: whereHelperstring{field: "`tipo_combustivel`.`tcb_descricao`"},
}

// TipoCombustivelRels is where relationship names are stored.
var TipoCombustivelRels = struct {
	TCBNotaFiscalItemVeiculos string
}{
	TCBNotaFiscalItemVeiculos: "TCBNotaFiscalItemVeiculos",
}

// tipoCombustivelR is where relationships are stored.
type tipoCombustivelR struct {
	TCBNotaFiscalItemVeiculos NotaFiscalItemVeiculoSlice `boil:"TCBNotaFiscalItemVeiculos" json:"TCBNotaFiscalItemVeiculos" toml:"TCBNotaFiscalItemVeiculos" yaml:"TCBNotaFiscalItemVeiculos"`
}

// NewStruct creates a new relationship struct
func (*tipoCombustivelR) NewStruct() *tipoCombustivelR {
	return &tipoCombustivelR{}
}

func (r *tipoCombustivelR) GetTCBNotaFiscalItemVeiculos() NotaFiscalItemVeiculoSlice {
	if r == nil {
		return nil
	}
	return r.TCBNotaFiscalItemVeiculos
}

// tipoCombustivelL is where Load methods for each relationship are stored.
type tipoCombustivelL struct{}

var (
	tipoCombustivelAllColumns            = []string{"tcb_id", "tcb_descricao"}
	tipoCombustivelColumnsWithoutDefault = []string{"tcb_id", "tcb_descricao"}
	tipoCombustivelColumnsWithDefault    = []string{}
	tipoCombustivelPrimaryKeyColumns     = []string{"tcb_id"}
	tipoCombustivelGeneratedColumns      = []string{}
)

type (
	// TipoCombustivelSlice is an alias for a slice of pointers to TipoCombustivel.
	// This should almost always be used instead of []TipoCombustivel.
	TipoCombustivelSlice []*TipoCombustivel
	// TipoCombustivelHook is the signature for custom TipoCombustivel hook methods
	TipoCombustivelHook func(context.Context, boil.ContextExecutor, *TipoCombustivel) error

	tipoCombustivelQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tipoCombustivelType                 = reflect.TypeOf(&TipoCombustivel{})
	tipoCombustivelMapping              = queries.MakeStructMapping(tipoCombustivelType)
	tipoCombustivelPrimaryKeyMapping, _ = queries.BindMapping(tipoCombustivelType, tipoCombustivelMapping, tipoCombustivelPrimaryKeyColumns)
	tipoCombustivelInsertCacheMut       sync.RWMutex
	tipoCombustivelInsertCache          = make(map[string]insertCache)
	tipoCombustivelUpdateCacheMut       sync.RWMutex
	tipoCombustivelUpdateCache          = make(map[string]updateCache)
	tipoCombustivelUpsertCacheMut       sync.RWMutex
	tipoCombustivelUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tipoCombustivelAfterSelectMu sync.Mutex
var tipoCombustivelAfterSelectHooks []TipoCombustivelHook

var tipoCombustivelBeforeInsertMu sync.Mutex
var tipoCombustivelBeforeInsertHooks []TipoCombustivelHook
var tipoCombustivelAfterInsertMu sync.Mutex
var tipoCombustivelAfterInsertHooks []TipoCombustivelHook

var tipoCombustivelBeforeUpdateMu sync.Mutex
var tipoCombustivelBeforeUpdateHooks []TipoCombustivelHook
var tipoCombustivelAfterUpdateMu sync.Mutex
var tipoCombustivelAfterUpdateHooks []TipoCombustivelHook

var tipoCombustivelBeforeDeleteMu sync.Mutex
var tipoCombustivelBeforeDeleteHooks []TipoCombustivelHook
var tipoCombustivelAfterDeleteMu sync.Mutex
var tipoCombustivelAfterDeleteHooks []TipoCombustivelHook

var tipoCombustivelBeforeUpsertMu sync.Mutex
var tipoCombustivelBeforeUpsertHooks []TipoCombustivelHook
var tipoCombustivelAfterUpsertMu sync.Mutex
var tipoCombustivelAfterUpsertHooks []TipoCombustivelHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TipoCombustivel) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TipoCombustivel) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TipoCombustivel) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TipoCombustivel) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TipoCombustivel) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TipoCombustivel) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TipoCombustivel) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TipoCombustivel) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TipoCombustivel) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tipoCombustivelAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTipoCombustivelHook registers your hook function for all future operations.
func AddTipoCombustivelHook(hookPoint boil.HookPoint, tipoCombustivelHook TipoCombustivelHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tipoCombustivelAfterSelectMu.Lock()
		tipoCombustivelAfterSelectHooks = append(tipoCombustivelAfterSelectHooks, tipoCombustivelHook)
		tipoCombustivelAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tipoCombustivelBeforeInsertMu.Lock()
		tipoCombustivelBeforeInsertHooks = append(tipoCombustivelBeforeInsertHooks, tipoCombustivelHook)
		tipoCombustivelBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tipoCombustivelAfterInsertMu.Lock()
		tipoCombustivelAfterInsertHooks = append(tipoCombustivelAfterInsertHooks, tipoCombustivelHook)
		tipoCombustivelAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tipoCombustivelBeforeUpdateMu.Lock()
		tipoCombustivelBeforeUpdateHooks = append(tipoCombustivelBeforeUpdateHooks, tipoCombustivelHook)
		tipoCombustivelBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tipoCombustivelAfterUpdateMu.Lock()
		tipoCombustivelAfterUpdateHooks = append(tipoCombustivelAfterUpdateHooks, tipoCombustivelHook)
		tipoCombustivelAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tipoCombustivelBeforeDeleteMu.Lock()
		tipoCombustivelBeforeDeleteHooks = append(tipoCombustivelBeforeDeleteHooks, tipoCombustivelHook)
		tipoCombustivelBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tipoCombustivelAfterDeleteMu.Lock()
		tipoCombustivelAfterDeleteHooks = append(tipoCombustivelAfterDeleteHooks, tipoCombustivelHook)
		tipoCombustivelAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tipoCombustivelBeforeUpsertMu.Lock()
		tipoCombustivelBeforeUpsertHooks = append(tipoCombustivelBeforeUpsertHooks, tipoCombustivelHook)
		tipoCombustivelBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tipoCombustivelAfterUpsertMu.Lock()
		tipoCombustivelAfterUpsertHooks = append(tipoCombustivelAfterUpsertHooks, tipoCombustivelHook)
		tipoCombustivelAfterUpsertMu.Unlock()
	}
}

// One returns a single tipoCombustivel record from the query.
func (q tipoCombustivelQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TipoCombustivel, error) {
	o := &TipoCombustivel{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for tipo_combustivel")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TipoCombustivel records from the query.
func (q tipoCombustivelQuery) All(ctx context.Context, exec boil.ContextExecutor) (TipoCombustivelSlice, error) {
	var o []*TipoCombustivel

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TipoCombustivel slice")
	}

	if len(tipoCombustivelAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TipoCombustivel records in the query.
func (q tipoCombustivelQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count tipo_combustivel rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tipoCombustivelQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if tipo_combustivel exists")
	}

	return count > 0, nil
}

// TCBNotaFiscalItemVeiculos retrieves all the nota_fiscal_item_veiculo's NotaFiscalItemVeiculos with an executor via tcb_id column.
func (o *TipoCombustivel) TCBNotaFiscalItemVeiculos(mods ...qm.QueryMod) notaFiscalItemVeiculoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`nota_fiscal_item_veiculo`.`tcb_id`=?", o.TCBID),
	)

	return NotaFiscalItemVeiculos(queryMods...)
}

// LoadTCBNotaFiscalItemVeiculos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (tipoCombustivelL) LoadTCBNotaFiscalItemVeiculos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTipoCombustivel interface{}, mods queries.Applicator) error {
	var slice []*TipoCombustivel
	var object *TipoCombustivel

	if singular {
		var ok bool
		object, ok = maybeTipoCombustivel.(*TipoCombustivel)
		if !ok {
			object = new(TipoCombustivel)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTipoCombustivel)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTipoCombustivel))
			}
		}
	} else {
		s, ok := maybeTipoCombustivel.(*[]*TipoCombustivel)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTipoCombustivel)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTipoCombustivel))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tipoCombustivelR{}
		}
		args[object.TCBID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tipoCombustivelR{}
			}
			args[obj.TCBID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`nota_fiscal_item_veiculo`),
		qm.WhereIn(`nota_fiscal_item_veiculo.tcb_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load nota_fiscal_item_veiculo")
	}

	var resultSlice []*NotaFiscalItemVeiculo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice nota_fiscal_item_veiculo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on nota_fiscal_item_veiculo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for nota_fiscal_item_veiculo")
	}

	if len(notaFiscalItemVeiculoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TCBNotaFiscalItemVeiculos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &notaFiscalItemVeiculoR{}
			}
			foreign.R.TCB = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.TCBID == foreign.TCBID {
				local.R.TCBNotaFiscalItemVeiculos = append(local.R.TCBNotaFiscalItemVeiculos, foreign)
				if foreign.R == nil {
					foreign.R = &notaFiscalItemVeiculoR{}
				}
				foreign.R.TCB = local
				break
			}
		}
	}

	return nil
}

// AddTCBNotaFiscalItemVeiculos adds the given related objects to the existing relationships
// of the tipo_combustivel, optionally inserting them as new records.
// Appends related to o.R.TCBNotaFiscalItemVeiculos.
// Sets related.R.TCB appropriately.
func (o *TipoCombustivel) AddTCBNotaFiscalItemVeiculos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NotaFiscalItemVeiculo) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.TCBID = o.TCBID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `nota_fiscal_item_veiculo` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"tcb_id"}),
				strmangle.WhereClause("`", "`", 0, notaFiscalItemVeiculoPrimaryKeyColumns),
			)
			values := []interface{}{o.TCBID, rel.NTFID, rel.NfiItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.TCBID = o.TCBID
		}
	}

	if o.R == nil {
		o.R = &tipoCombustivelR{
			TCBNotaFiscalItemVeiculos: related,
		}
	} else {
		o.R.TCBNotaFiscalItemVeiculos = append(o.R.TCBNotaFiscalItemVeiculos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &notaFiscalItemVeiculoR{
				TCB: o,
			}
		} else {
			rel.R.TCB = o
		}
	}
	return nil
}

// TipoCombustivels retrieves all the records using an executor.
func TipoCombustivels(mods ...qm.QueryMod) tipoCombustivelQuery {
	mods = append(mods, qm.From("`tipo_combustivel`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`tipo_combustivel`.*"})
	}

	return tipoCombustivelQuery{q}
}

// FindTipoCombustivel retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTipoCombustivel(ctx context.Context, exec boil.ContextExecutor, tCBID int, selectCols ...string) (*TipoCombustivel, error) {
	tipoCombustivelObj := &TipoCombustivel{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `tipo_combustivel` where `tcb_id`=?", sel,
	)

	q := queries.Raw(query, tCBID)

	err := q.Bind(ctx, exec, tipoCombustivelObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from tipo_combustivel")
	}

	if err = tipoCombustivelObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tipoCombustivelObj, err
	}

	return tipoCombustivelObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TipoCombustivel) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tipo_combustivel provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tipoCombustivelColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tipoCombustivelInsertCacheMut.RLock()
	cache, cached := tipoCombustivelInsertCache[key]
	tipoCombustivelInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tipoCombustivelAllColumns,
			tipoCombustivelColumnsWithDefault,
			tipoCombustivelColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tipoCombustivelType, tipoCombustivelMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tipoCombustivelType, tipoCombustivelMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `tipo_combustivel` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `tipo_combustivel` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `tipo_combustivel` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tipoCombustivelPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into tipo_combustivel")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TCBID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tipo_combustivel")
	}

CacheNoHooks:
	if !cached {
		tipoCombustivelInsertCacheMut.Lock()
		tipoCombustivelInsertCache[key] = cache
		tipoCombustivelInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TipoCombustivel.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TipoCombustivel) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tipoCombustivelUpdateCacheMut.RLock()
	cache, cached := tipoCombustivelUpdateCache[key]
	tipoCombustivelUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tipoCombustivelAllColumns,
			tipoCombustivelPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update tipo_combustivel, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `tipo_combustivel` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tipoCombustivelPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tipoCombustivelType, tipoCombustivelMapping, append(wl, tipoCombustivelPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update tipo_combustivel row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for tipo_combustivel")
	}

	if !cached {
		tipoCombustivelUpdateCacheMut.Lock()
		tipoCombustivelUpdateCache[key] = cache
		tipoCombustivelUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tipoCombustivelQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for tipo_combustivel")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for tipo_combustivel")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TipoCombustivelSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tipoCombustivelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `tipo_combustivel` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tipoCombustivelPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tipoCombustivel slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tipoCombustivel")
	}
	return rowsAff, nil
}

var mySQLTipoCombustivelUniqueColumns = []string{
	"tcb_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TipoCombustivel) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tipo_combustivel provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tipoCombustivelColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTipoCombustivelUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tipoCombustivelUpsertCacheMut.RLock()
	cache, cached := tipoCombustivelUpsertCache[key]
	tipoCombustivelUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tipoCombustivelAllColumns,
			tipoCombustivelColumnsWithDefault,
			tipoCombustivelColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tipoCombustivelAllColumns,
			tipoCombustivelPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert tipo_combustivel, could not build update column list")
		}

		ret := strmangle.SetComplement(tipoCombustivelAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`tipo_combustivel`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `tipo_combustivel` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tipoCombustivelType, tipoCombustivelMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tipoCombustivelType, tipoCombustivelMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for tipo_combustivel")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tipoCombustivelType, tipoCombustivelMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for tipo_combustivel")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tipo_combustivel")
	}

CacheNoHooks:
	if !cached {
		tipoCombustivelUpsertCacheMut.Lock()
		tipoCombustivelUpsertCache[key] = cache
		tipoCombustivelUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TipoCombustivel record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TipoCombustivel) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TipoCombustivel provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tipoCombustivelPrimaryKeyMapping)
	sql := "DELETE FROM `tipo_combustivel` WHERE `tcb_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from tipo_combustivel")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for tipo_combustivel")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tipoCombustivelQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tipoCombustivelQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tipo_combustivel")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tipo_combustivel")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TipoCombustivelSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tipoCombustivelBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tipoCombustivelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `tipo_combustivel` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tipoCombustivelPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tipoCombustivel slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tipo_combustivel")
	}

	if len(tipoCombustivelAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TipoCombustivel) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTipoCombustivel(ctx, exec, o.TCBID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TipoCombustivelSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TipoCombustivelSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tipoCombustivelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `tipo_combustivel`.* FROM `tipo_combustivel` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tipoCombustivelPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TipoCombustivelSlice")
	}

	*o = slice

	return nil
}

// TipoCombustivelExists checks if the TipoCombustivel row exists.
func TipoCombustivelExists(ctx context.Context, exec boil.ContextExecutor, tCBID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `tipo_combustivel` where `tcb_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, tCBID)
	}
	row := exec.QueryRowContext(ctx, sql, tCBID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if tipo_combustivel exists")
	}

	return exists, nil
}

// Exists checks if the TipoCombustivel row exists.
func (o *TipoCombustivel) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TipoCombustivelExists(ctx, exec, o.TCBID)
}
