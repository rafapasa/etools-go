// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// NCM is an object representing the database table.
type NCM struct {
	NCMNumero          int           `boil:"ncm_numero" json:"ncm_numero" toml:"ncm_numero" yaml:"ncm_numero"`
	UndID              int           `boil:"und_id" json:"und_id" toml:"und_id" yaml:"und_id"`
	NCMDescricao       string        `boil:"ncm_descricao" json:"ncm_descricao" toml:"ncm_descricao" yaml:"ncm_descricao"`
	NCMReducaoBCIpi    types.Decimal `boil:"ncm_reducao_bc_ipi" json:"ncm_reducao_bc_ipi" toml:"ncm_reducao_bc_ipi" yaml:"ncm_reducao_bc_ipi"`
	NCMReducaoBCPis    types.Decimal `boil:"ncm_reducao_bc_pis" json:"ncm_reducao_bc_pis" toml:"ncm_reducao_bc_pis" yaml:"ncm_reducao_bc_pis"`
	NCMReducaoBCCofins types.Decimal `boil:"ncm_reducao_bc_cofins" json:"ncm_reducao_bc_cofins" toml:"ncm_reducao_bc_cofins" yaml:"ncm_reducao_bc_cofins"`
	NCMAliquotaipi     types.Decimal `boil:"ncm_aliquotaipi" json:"ncm_aliquotaipi" toml:"ncm_aliquotaipi" yaml:"ncm_aliquotaipi"`
	UndIDTrib          null.Int      `boil:"und_id_trib" json:"und_id_trib,omitempty" toml:"und_id_trib" yaml:"und_id_trib,omitempty"`
	CestNumero         null.Int      `boil:"cest_numero" json:"cest_numero,omitempty" toml:"cest_numero" yaml:"cest_numero,omitempty"`
	NCMMonofasico      null.Int      `boil:"ncm_monofasico" json:"ncm_monofasico,omitempty" toml:"ncm_monofasico" yaml:"ncm_monofasico,omitempty"`
	CstpiscofinsID     null.Int16    `boil:"cstpiscofins_id" json:"cstpiscofins_id,omitempty" toml:"cstpiscofins_id" yaml:"cstpiscofins_id,omitempty"`

	R *ncmR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ncmL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var NCMColumns = struct {
	NCMNumero          string
	UndID              string
	NCMDescricao       string
	NCMReducaoBCIpi    string
	NCMReducaoBCPis    string
	NCMReducaoBCCofins string
	NCMAliquotaipi     string
	UndIDTrib          string
	CestNumero         string
	NCMMonofasico      string
	CstpiscofinsID     string
}{
	NCMNumero:          "ncm_numero",
	UndID:              "und_id",
	NCMDescricao:       "ncm_descricao",
	NCMReducaoBCIpi:    "ncm_reducao_bc_ipi",
	NCMReducaoBCPis:    "ncm_reducao_bc_pis",
	NCMReducaoBCCofins: "ncm_reducao_bc_cofins",
	NCMAliquotaipi:     "ncm_aliquotaipi",
	UndIDTrib:          "und_id_trib",
	CestNumero:         "cest_numero",
	NCMMonofasico:      "ncm_monofasico",
	CstpiscofinsID:     "cstpiscofins_id",
}

var NCMTableColumns = struct {
	NCMNumero          string
	UndID              string
	NCMDescricao       string
	NCMReducaoBCIpi    string
	NCMReducaoBCPis    string
	NCMReducaoBCCofins string
	NCMAliquotaipi     string
	UndIDTrib          string
	CestNumero         string
	NCMMonofasico      string
	CstpiscofinsID     string
}{
	NCMNumero:          "ncm.ncm_numero",
	UndID:              "ncm.und_id",
	NCMDescricao:       "ncm.ncm_descricao",
	NCMReducaoBCIpi:    "ncm.ncm_reducao_bc_ipi",
	NCMReducaoBCPis:    "ncm.ncm_reducao_bc_pis",
	NCMReducaoBCCofins: "ncm.ncm_reducao_bc_cofins",
	NCMAliquotaipi:     "ncm.ncm_aliquotaipi",
	UndIDTrib:          "ncm.und_id_trib",
	CestNumero:         "ncm.cest_numero",
	NCMMonofasico:      "ncm.ncm_monofasico",
	CstpiscofinsID:     "ncm.cstpiscofins_id",
}

// Generated where

var NCMWhere = struct {
	NCMNumero          whereHelperint
	UndID              whereHelperint
	NCMDescricao       whereHelperstring
	NCMReducaoBCIpi    whereHelpertypes_Decimal
	NCMReducaoBCPis    whereHelpertypes_Decimal
	NCMReducaoBCCofins whereHelpertypes_Decimal
	NCMAliquotaipi     whereHelpertypes_Decimal
	UndIDTrib          whereHelpernull_Int
	CestNumero         whereHelpernull_Int
	NCMMonofasico      whereHelpernull_Int
	CstpiscofinsID     whereHelpernull_Int16
}{
	NCMNumero:          whereHelperint{field: "`ncm`.`ncm_numero`"},
	UndID:              whereHelperint{field: "`ncm`.`und_id`"},
	NCMDescricao:       whereHelperstring{field: "`ncm`.`ncm_descricao`"},
	NCMReducaoBCIpi:    whereHelpertypes_Decimal{field: "`ncm`.`ncm_reducao_bc_ipi`"},
	NCMReducaoBCPis:    whereHelpertypes_Decimal{field: "`ncm`.`ncm_reducao_bc_pis`"},
	NCMReducaoBCCofins: whereHelpertypes_Decimal{field: "`ncm`.`ncm_reducao_bc_cofins`"},
	NCMAliquotaipi:     whereHelpertypes_Decimal{field: "`ncm`.`ncm_aliquotaipi`"},
	UndIDTrib:          whereHelpernull_Int{field: "`ncm`.`und_id_trib`"},
	CestNumero:         whereHelpernull_Int{field: "`ncm`.`cest_numero`"},
	NCMMonofasico:      whereHelpernull_Int{field: "`ncm`.`ncm_monofasico`"},
	CstpiscofinsID:     whereHelpernull_Int16{field: "`ncm`.`cstpiscofins_id`"},
}

// NCMRels is where relationship names are stored.
var NCMRels = struct {
	Cstpiscofin             string
	CestNumeroCest          string
	UndIDTribUnidademedidas string
	Und                     string
	NCMNumeroEcfAliquota    string
	NCMNumeroNCMEstados     string
	NCMNumeroNCMTributacaos string
	NCMNumeroProdutos       string
}{
	Cstpiscofin:             "Cstpiscofin",
	CestNumeroCest:          "CestNumeroCest",
	UndIDTribUnidademedidas: "UndIDTribUnidademedidas",
	Und:                     "Und",
	NCMNumeroEcfAliquota:    "NCMNumeroEcfAliquota",
	NCMNumeroNCMEstados:     "NCMNumeroNCMEstados",
	NCMNumeroNCMTributacaos: "NCMNumeroNCMTributacaos",
	NCMNumeroProdutos:       "NCMNumeroProdutos",
}

// ncmR is where relationships are stored.
type ncmR struct {
	Cstpiscofin             *Cstpiscofin       `boil:"Cstpiscofin" json:"Cstpiscofin" toml:"Cstpiscofin" yaml:"Cstpiscofin"`
	CestNumeroCest          *Cest              `boil:"CestNumeroCest" json:"CestNumeroCest" toml:"CestNumeroCest" yaml:"CestNumeroCest"`
	UndIDTribUnidademedidas *Unidademedidas    `boil:"UndIDTribUnidademedidas" json:"UndIDTribUnidademedidas" toml:"UndIDTribUnidademedidas" yaml:"UndIDTribUnidademedidas"`
	Und                     *Unidademedidas    `boil:"Und" json:"Und" toml:"Und" yaml:"Und"`
	NCMNumeroEcfAliquota    EcfAliquotumSlice  `boil:"NCMNumeroEcfAliquota" json:"NCMNumeroEcfAliquota" toml:"NCMNumeroEcfAliquota" yaml:"NCMNumeroEcfAliquota"`
	NCMNumeroNCMEstados     NCMEstadoSlice     `boil:"NCMNumeroNCMEstados" json:"NCMNumeroNCMEstados" toml:"NCMNumeroNCMEstados" yaml:"NCMNumeroNCMEstados"`
	NCMNumeroNCMTributacaos NCMTributacaoSlice `boil:"NCMNumeroNCMTributacaos" json:"NCMNumeroNCMTributacaos" toml:"NCMNumeroNCMTributacaos" yaml:"NCMNumeroNCMTributacaos"`
	NCMNumeroProdutos       ProdutoSlice       `boil:"NCMNumeroProdutos" json:"NCMNumeroProdutos" toml:"NCMNumeroProdutos" yaml:"NCMNumeroProdutos"`
}

// NewStruct creates a new relationship struct
func (*ncmR) NewStruct() *ncmR {
	return &ncmR{}
}

func (r *ncmR) GetCstpiscofin() *Cstpiscofin {
	if r == nil {
		return nil
	}
	return r.Cstpiscofin
}

func (r *ncmR) GetCestNumeroCest() *Cest {
	if r == nil {
		return nil
	}
	return r.CestNumeroCest
}

func (r *ncmR) GetUndIDTribUnidademedidas() *Unidademedidas {
	if r == nil {
		return nil
	}
	return r.UndIDTribUnidademedidas
}

func (r *ncmR) GetUnd() *Unidademedidas {
	if r == nil {
		return nil
	}
	return r.Und
}

func (r *ncmR) GetNCMNumeroEcfAliquota() EcfAliquotumSlice {
	if r == nil {
		return nil
	}
	return r.NCMNumeroEcfAliquota
}

func (r *ncmR) GetNCMNumeroNCMEstados() NCMEstadoSlice {
	if r == nil {
		return nil
	}
	return r.NCMNumeroNCMEstados
}

func (r *ncmR) GetNCMNumeroNCMTributacaos() NCMTributacaoSlice {
	if r == nil {
		return nil
	}
	return r.NCMNumeroNCMTributacaos
}

func (r *ncmR) GetNCMNumeroProdutos() ProdutoSlice {
	if r == nil {
		return nil
	}
	return r.NCMNumeroProdutos
}

// ncmL is where Load methods for each relationship are stored.
type ncmL struct{}

var (
	ncmAllColumns            = []string{"ncm_numero", "und_id", "ncm_descricao", "ncm_reducao_bc_ipi", "ncm_reducao_bc_pis", "ncm_reducao_bc_cofins", "ncm_aliquotaipi", "und_id_trib", "cest_numero", "ncm_monofasico", "cstpiscofins_id"}
	ncmColumnsWithoutDefault = []string{"ncm_numero", "und_id", "ncm_descricao", "ncm_aliquotaipi", "und_id_trib", "cest_numero", "ncm_monofasico", "cstpiscofins_id"}
	ncmColumnsWithDefault    = []string{"ncm_reducao_bc_ipi", "ncm_reducao_bc_pis", "ncm_reducao_bc_cofins"}
	ncmPrimaryKeyColumns     = []string{"ncm_numero"}
	ncmGeneratedColumns      = []string{}
)

type (
	// NCMSlice is an alias for a slice of pointers to NCM.
	// This should almost always be used instead of []NCM.
	NCMSlice []*NCM
	// NCMHook is the signature for custom NCM hook methods
	NCMHook func(context.Context, boil.ContextExecutor, *NCM) error

	ncmQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ncmType                 = reflect.TypeOf(&NCM{})
	ncmMapping              = queries.MakeStructMapping(ncmType)
	ncmPrimaryKeyMapping, _ = queries.BindMapping(ncmType, ncmMapping, ncmPrimaryKeyColumns)
	ncmInsertCacheMut       sync.RWMutex
	ncmInsertCache          = make(map[string]insertCache)
	ncmUpdateCacheMut       sync.RWMutex
	ncmUpdateCache          = make(map[string]updateCache)
	ncmUpsertCacheMut       sync.RWMutex
	ncmUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ncmAfterSelectMu sync.Mutex
var ncmAfterSelectHooks []NCMHook

var ncmBeforeInsertMu sync.Mutex
var ncmBeforeInsertHooks []NCMHook
var ncmAfterInsertMu sync.Mutex
var ncmAfterInsertHooks []NCMHook

var ncmBeforeUpdateMu sync.Mutex
var ncmBeforeUpdateHooks []NCMHook
var ncmAfterUpdateMu sync.Mutex
var ncmAfterUpdateHooks []NCMHook

var ncmBeforeDeleteMu sync.Mutex
var ncmBeforeDeleteHooks []NCMHook
var ncmAfterDeleteMu sync.Mutex
var ncmAfterDeleteHooks []NCMHook

var ncmBeforeUpsertMu sync.Mutex
var ncmBeforeUpsertHooks []NCMHook
var ncmAfterUpsertMu sync.Mutex
var ncmAfterUpsertHooks []NCMHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *NCM) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *NCM) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *NCM) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *NCM) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *NCM) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *NCM) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *NCM) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *NCM) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *NCM) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ncmAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddNCMHook registers your hook function for all future operations.
func AddNCMHook(hookPoint boil.HookPoint, ncmHook NCMHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ncmAfterSelectMu.Lock()
		ncmAfterSelectHooks = append(ncmAfterSelectHooks, ncmHook)
		ncmAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		ncmBeforeInsertMu.Lock()
		ncmBeforeInsertHooks = append(ncmBeforeInsertHooks, ncmHook)
		ncmBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		ncmAfterInsertMu.Lock()
		ncmAfterInsertHooks = append(ncmAfterInsertHooks, ncmHook)
		ncmAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		ncmBeforeUpdateMu.Lock()
		ncmBeforeUpdateHooks = append(ncmBeforeUpdateHooks, ncmHook)
		ncmBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		ncmAfterUpdateMu.Lock()
		ncmAfterUpdateHooks = append(ncmAfterUpdateHooks, ncmHook)
		ncmAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		ncmBeforeDeleteMu.Lock()
		ncmBeforeDeleteHooks = append(ncmBeforeDeleteHooks, ncmHook)
		ncmBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		ncmAfterDeleteMu.Lock()
		ncmAfterDeleteHooks = append(ncmAfterDeleteHooks, ncmHook)
		ncmAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		ncmBeforeUpsertMu.Lock()
		ncmBeforeUpsertHooks = append(ncmBeforeUpsertHooks, ncmHook)
		ncmBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		ncmAfterUpsertMu.Lock()
		ncmAfterUpsertHooks = append(ncmAfterUpsertHooks, ncmHook)
		ncmAfterUpsertMu.Unlock()
	}
}

// One returns a single ncm record from the query.
func (q ncmQuery) One(ctx context.Context, exec boil.ContextExecutor) (*NCM, error) {
	o := &NCM{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ncm")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all NCM records from the query.
func (q ncmQuery) All(ctx context.Context, exec boil.ContextExecutor) (NCMSlice, error) {
	var o []*NCM

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to NCM slice")
	}

	if len(ncmAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all NCM records in the query.
func (q ncmQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ncm rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ncmQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ncm exists")
	}

	return count > 0, nil
}

// Cstpiscofin pointed to by the foreign key.
func (o *NCM) Cstpiscofin(mods ...qm.QueryMod) cstpiscofinQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cstpiscofins_id` = ?", o.CstpiscofinsID),
	}

	queryMods = append(queryMods, mods...)

	return Cstpiscofins(queryMods...)
}

// CestNumeroCest pointed to by the foreign key.
func (o *NCM) CestNumeroCest(mods ...qm.QueryMod) cestQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`cest_numero` = ?", o.CestNumero),
	}

	queryMods = append(queryMods, mods...)

	return Cests(queryMods...)
}

// UndIDTribUnidademedidas pointed to by the foreign key.
func (o *NCM) UndIDTribUnidademedidas(mods ...qm.QueryMod) unidademedidasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`und_id` = ?", o.UndIDTrib),
	}

	queryMods = append(queryMods, mods...)

	return Unidademedidas(queryMods...)
}

// Und pointed to by the foreign key.
func (o *NCM) Und(mods ...qm.QueryMod) unidademedidasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`und_id` = ?", o.UndID),
	}

	queryMods = append(queryMods, mods...)

	return Unidademedidas(queryMods...)
}

// NCMNumeroEcfAliquota retrieves all the ecf_aliquotum's EcfAliquota with an executor via ncm_numero column.
func (o *NCM) NCMNumeroEcfAliquota(mods ...qm.QueryMod) ecfAliquotumQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`ecf_aliquota`.`ncm_numero`=?", o.NCMNumero),
	)

	return EcfAliquota(queryMods...)
}

// NCMNumeroNCMEstados retrieves all the ncm_estado's NCMEstados with an executor via ncm_numero column.
func (o *NCM) NCMNumeroNCMEstados(mods ...qm.QueryMod) ncmEstadoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`ncm_estado`.`ncm_numero`=?", o.NCMNumero),
	)

	return NCMEstados(queryMods...)
}

// NCMNumeroNCMTributacaos retrieves all the ncm_tributacao's NCMTributacaos with an executor via ncm_numero column.
func (o *NCM) NCMNumeroNCMTributacaos(mods ...qm.QueryMod) ncmTributacaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`ncm_tributacao`.`ncm_numero`=?", o.NCMNumero),
	)

	return NCMTributacaos(queryMods...)
}

// NCMNumeroProdutos retrieves all the produto's Produtos with an executor via ncm_numero column.
func (o *NCM) NCMNumeroProdutos(mods ...qm.QueryMod) produtoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto`.`ncm_numero`=?", o.NCMNumero),
	)

	return Produtos(queryMods...)
}

// LoadCstpiscofin allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmL) LoadCstpiscofin(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCM interface{}, mods queries.Applicator) error {
	var slice []*NCM
	var object *NCM

	if singular {
		var ok bool
		object, ok = maybeNCM.(*NCM)
		if !ok {
			object = new(NCM)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCM))
			}
		}
	} else {
		s, ok := maybeNCM.(*[]*NCM)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCM))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmR{}
		}
		if !queries.IsNil(object.CstpiscofinsID) {
			args[object.CstpiscofinsID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmR{}
			}

			if !queries.IsNil(obj.CstpiscofinsID) {
				args[obj.CstpiscofinsID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cstpiscofins`),
		qm.WhereIn(`cstpiscofins.cstpiscofins_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Cstpiscofin")
	}

	var resultSlice []*Cstpiscofin
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Cstpiscofin")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for cstpiscofins")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cstpiscofins")
	}

	if len(cstpiscofinAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cstpiscofin = foreign
		if foreign.R == nil {
			foreign.R = &cstpiscofinR{}
		}
		foreign.R.NCMS = append(foreign.R.NCMS, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.CstpiscofinsID, foreign.CstpiscofinsID) {
				local.R.Cstpiscofin = foreign
				if foreign.R == nil {
					foreign.R = &cstpiscofinR{}
				}
				foreign.R.NCMS = append(foreign.R.NCMS, local)
				break
			}
		}
	}

	return nil
}

// LoadCestNumeroCest allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmL) LoadCestNumeroCest(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCM interface{}, mods queries.Applicator) error {
	var slice []*NCM
	var object *NCM

	if singular {
		var ok bool
		object, ok = maybeNCM.(*NCM)
		if !ok {
			object = new(NCM)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCM))
			}
		}
	} else {
		s, ok := maybeNCM.(*[]*NCM)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCM))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmR{}
		}
		if !queries.IsNil(object.CestNumero) {
			args[object.CestNumero] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmR{}
			}

			if !queries.IsNil(obj.CestNumero) {
				args[obj.CestNumero] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cest`),
		qm.WhereIn(`cest.cest_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Cest")
	}

	var resultSlice []*Cest
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Cest")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for cest")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cest")
	}

	if len(cestAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CestNumeroCest = foreign
		if foreign.R == nil {
			foreign.R = &cestR{}
		}
		foreign.R.CestNumeroNCMS = append(foreign.R.CestNumeroNCMS, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.CestNumero, foreign.CestNumero) {
				local.R.CestNumeroCest = foreign
				if foreign.R == nil {
					foreign.R = &cestR{}
				}
				foreign.R.CestNumeroNCMS = append(foreign.R.CestNumeroNCMS, local)
				break
			}
		}
	}

	return nil
}

// LoadUndIDTribUnidademedidas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmL) LoadUndIDTribUnidademedidas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCM interface{}, mods queries.Applicator) error {
	var slice []*NCM
	var object *NCM

	if singular {
		var ok bool
		object, ok = maybeNCM.(*NCM)
		if !ok {
			object = new(NCM)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCM))
			}
		}
	} else {
		s, ok := maybeNCM.(*[]*NCM)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCM))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmR{}
		}
		if !queries.IsNil(object.UndIDTrib) {
			args[object.UndIDTrib] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmR{}
			}

			if !queries.IsNil(obj.UndIDTrib) {
				args[obj.UndIDTrib] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`unidademedidas`),
		qm.WhereIn(`unidademedidas.und_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Unidademedidas")
	}

	var resultSlice []*Unidademedidas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Unidademedidas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for unidademedidas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for unidademedidas")
	}

	if len(unidademedidasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.UndIDTribUnidademedidas = foreign
		if foreign.R == nil {
			foreign.R = &unidademedidasR{}
		}
		foreign.R.UndIDTribNCMS = append(foreign.R.UndIDTribNCMS, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.UndIDTrib, foreign.UndID) {
				local.R.UndIDTribUnidademedidas = foreign
				if foreign.R == nil {
					foreign.R = &unidademedidasR{}
				}
				foreign.R.UndIDTribNCMS = append(foreign.R.UndIDTribNCMS, local)
				break
			}
		}
	}

	return nil
}

// LoadUnd allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ncmL) LoadUnd(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCM interface{}, mods queries.Applicator) error {
	var slice []*NCM
	var object *NCM

	if singular {
		var ok bool
		object, ok = maybeNCM.(*NCM)
		if !ok {
			object = new(NCM)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCM))
			}
		}
	} else {
		s, ok := maybeNCM.(*[]*NCM)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCM))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmR{}
		}
		args[object.UndID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmR{}
			}

			args[obj.UndID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`unidademedidas`),
		qm.WhereIn(`unidademedidas.und_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Unidademedidas")
	}

	var resultSlice []*Unidademedidas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Unidademedidas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for unidademedidas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for unidademedidas")
	}

	if len(unidademedidasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Und = foreign
		if foreign.R == nil {
			foreign.R = &unidademedidasR{}
		}
		foreign.R.UndNCMS = append(foreign.R.UndNCMS, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UndID == foreign.UndID {
				local.R.Und = foreign
				if foreign.R == nil {
					foreign.R = &unidademedidasR{}
				}
				foreign.R.UndNCMS = append(foreign.R.UndNCMS, local)
				break
			}
		}
	}

	return nil
}

// LoadNCMNumeroEcfAliquota allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (ncmL) LoadNCMNumeroEcfAliquota(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCM interface{}, mods queries.Applicator) error {
	var slice []*NCM
	var object *NCM

	if singular {
		var ok bool
		object, ok = maybeNCM.(*NCM)
		if !ok {
			object = new(NCM)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCM))
			}
		}
	} else {
		s, ok := maybeNCM.(*[]*NCM)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCM))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmR{}
		}
		args[object.NCMNumero] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmR{}
			}
			args[obj.NCMNumero] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ecf_aliquota`),
		qm.WhereIn(`ecf_aliquota.ncm_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ecf_aliquota")
	}

	var resultSlice []*EcfAliquotum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ecf_aliquota")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on ecf_aliquota")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ecf_aliquota")
	}

	if len(ecfAliquotumAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.NCMNumeroEcfAliquota = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &ecfAliquotumR{}
			}
			foreign.R.NCMNumeroNCM = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.NCMNumero, foreign.NCMNumero) {
				local.R.NCMNumeroEcfAliquota = append(local.R.NCMNumeroEcfAliquota, foreign)
				if foreign.R == nil {
					foreign.R = &ecfAliquotumR{}
				}
				foreign.R.NCMNumeroNCM = local
				break
			}
		}
	}

	return nil
}

// LoadNCMNumeroNCMEstados allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (ncmL) LoadNCMNumeroNCMEstados(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCM interface{}, mods queries.Applicator) error {
	var slice []*NCM
	var object *NCM

	if singular {
		var ok bool
		object, ok = maybeNCM.(*NCM)
		if !ok {
			object = new(NCM)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCM))
			}
		}
	} else {
		s, ok := maybeNCM.(*[]*NCM)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCM))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmR{}
		}
		args[object.NCMNumero] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmR{}
			}
			args[obj.NCMNumero] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ncm_estado`),
		qm.WhereIn(`ncm_estado.ncm_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ncm_estado")
	}

	var resultSlice []*NCMEstado
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ncm_estado")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on ncm_estado")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ncm_estado")
	}

	if len(ncmEstadoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.NCMNumeroNCMEstados = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &ncmEstadoR{}
			}
			foreign.R.NCMNumeroNCM = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.NCMNumero == foreign.NCMNumero {
				local.R.NCMNumeroNCMEstados = append(local.R.NCMNumeroNCMEstados, foreign)
				if foreign.R == nil {
					foreign.R = &ncmEstadoR{}
				}
				foreign.R.NCMNumeroNCM = local
				break
			}
		}
	}

	return nil
}

// LoadNCMNumeroNCMTributacaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (ncmL) LoadNCMNumeroNCMTributacaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCM interface{}, mods queries.Applicator) error {
	var slice []*NCM
	var object *NCM

	if singular {
		var ok bool
		object, ok = maybeNCM.(*NCM)
		if !ok {
			object = new(NCM)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCM))
			}
		}
	} else {
		s, ok := maybeNCM.(*[]*NCM)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCM))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmR{}
		}
		args[object.NCMNumero] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmR{}
			}
			args[obj.NCMNumero] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ncm_tributacao`),
		qm.WhereIn(`ncm_tributacao.ncm_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ncm_tributacao")
	}

	var resultSlice []*NCMTributacao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ncm_tributacao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on ncm_tributacao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ncm_tributacao")
	}

	if len(ncmTributacaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.NCMNumeroNCMTributacaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &ncmTributacaoR{}
			}
			foreign.R.NCMNumeroNCM = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.NCMNumero == foreign.NCMNumero {
				local.R.NCMNumeroNCMTributacaos = append(local.R.NCMNumeroNCMTributacaos, foreign)
				if foreign.R == nil {
					foreign.R = &ncmTributacaoR{}
				}
				foreign.R.NCMNumeroNCM = local
				break
			}
		}
	}

	return nil
}

// LoadNCMNumeroProdutos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (ncmL) LoadNCMNumeroProdutos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeNCM interface{}, mods queries.Applicator) error {
	var slice []*NCM
	var object *NCM

	if singular {
		var ok bool
		object, ok = maybeNCM.(*NCM)
		if !ok {
			object = new(NCM)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeNCM))
			}
		}
	} else {
		s, ok := maybeNCM.(*[]*NCM)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeNCM)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeNCM))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ncmR{}
		}
		args[object.NCMNumero] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ncmR{}
			}
			args[obj.NCMNumero] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.ncm_numero in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.NCMNumeroProdutos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoR{}
			}
			foreign.R.NCMNumeroNCM = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.NCMNumero == foreign.NCMNumero {
				local.R.NCMNumeroProdutos = append(local.R.NCMNumeroProdutos, foreign)
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.NCMNumeroNCM = local
				break
			}
		}
	}

	return nil
}

// SetCstpiscofin of the ncm to the related item.
// Sets o.R.Cstpiscofin to related.
// Adds o to related.R.NCMS.
func (o *NCM) SetCstpiscofin(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Cstpiscofin) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cstpiscofins_id"}),
		strmangle.WhereClause("`", "`", 0, ncmPrimaryKeyColumns),
	)
	values := []interface{}{related.CstpiscofinsID, o.NCMNumero}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.CstpiscofinsID, related.CstpiscofinsID)
	if o.R == nil {
		o.R = &ncmR{
			Cstpiscofin: related,
		}
	} else {
		o.R.Cstpiscofin = related
	}

	if related.R == nil {
		related.R = &cstpiscofinR{
			NCMS: NCMSlice{o},
		}
	} else {
		related.R.NCMS = append(related.R.NCMS, o)
	}

	return nil
}

// RemoveCstpiscofin relationship.
// Sets o.R.Cstpiscofin to nil.
// Removes o from all passed in related items' relationships struct.
func (o *NCM) RemoveCstpiscofin(ctx context.Context, exec boil.ContextExecutor, related *Cstpiscofin) error {
	var err error

	queries.SetScanner(&o.CstpiscofinsID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("cstpiscofins_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Cstpiscofin = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.NCMS {
		if queries.Equal(o.CstpiscofinsID, ri.CstpiscofinsID) {
			continue
		}

		ln := len(related.R.NCMS)
		if ln > 1 && i < ln-1 {
			related.R.NCMS[i] = related.R.NCMS[ln-1]
		}
		related.R.NCMS = related.R.NCMS[:ln-1]
		break
	}
	return nil
}

// SetCestNumeroCest of the ncm to the related item.
// Sets o.R.CestNumeroCest to related.
// Adds o to related.R.CestNumeroNCMS.
func (o *NCM) SetCestNumeroCest(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Cest) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"cest_numero"}),
		strmangle.WhereClause("`", "`", 0, ncmPrimaryKeyColumns),
	)
	values := []interface{}{related.CestNumero, o.NCMNumero}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.CestNumero, related.CestNumero)
	if o.R == nil {
		o.R = &ncmR{
			CestNumeroCest: related,
		}
	} else {
		o.R.CestNumeroCest = related
	}

	if related.R == nil {
		related.R = &cestR{
			CestNumeroNCMS: NCMSlice{o},
		}
	} else {
		related.R.CestNumeroNCMS = append(related.R.CestNumeroNCMS, o)
	}

	return nil
}

// RemoveCestNumeroCest relationship.
// Sets o.R.CestNumeroCest to nil.
// Removes o from all passed in related items' relationships struct.
func (o *NCM) RemoveCestNumeroCest(ctx context.Context, exec boil.ContextExecutor, related *Cest) error {
	var err error

	queries.SetScanner(&o.CestNumero, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("cest_numero")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.CestNumeroCest = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.CestNumeroNCMS {
		if queries.Equal(o.CestNumero, ri.CestNumero) {
			continue
		}

		ln := len(related.R.CestNumeroNCMS)
		if ln > 1 && i < ln-1 {
			related.R.CestNumeroNCMS[i] = related.R.CestNumeroNCMS[ln-1]
		}
		related.R.CestNumeroNCMS = related.R.CestNumeroNCMS[:ln-1]
		break
	}
	return nil
}

// SetUndIDTribUnidademedidas of the ncm to the related item.
// Sets o.R.UndIDTribUnidademedidas to related.
// Adds o to related.R.UndIDTribNCMS.
func (o *NCM) SetUndIDTribUnidademedidas(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Unidademedidas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"und_id_trib"}),
		strmangle.WhereClause("`", "`", 0, ncmPrimaryKeyColumns),
	)
	values := []interface{}{related.UndID, o.NCMNumero}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.UndIDTrib, related.UndID)
	if o.R == nil {
		o.R = &ncmR{
			UndIDTribUnidademedidas: related,
		}
	} else {
		o.R.UndIDTribUnidademedidas = related
	}

	if related.R == nil {
		related.R = &unidademedidasR{
			UndIDTribNCMS: NCMSlice{o},
		}
	} else {
		related.R.UndIDTribNCMS = append(related.R.UndIDTribNCMS, o)
	}

	return nil
}

// RemoveUndIDTribUnidademedidas relationship.
// Sets o.R.UndIDTribUnidademedidas to nil.
// Removes o from all passed in related items' relationships struct.
func (o *NCM) RemoveUndIDTribUnidademedidas(ctx context.Context, exec boil.ContextExecutor, related *Unidademedidas) error {
	var err error

	queries.SetScanner(&o.UndIDTrib, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("und_id_trib")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.UndIDTribUnidademedidas = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.UndIDTribNCMS {
		if queries.Equal(o.UndIDTrib, ri.UndIDTrib) {
			continue
		}

		ln := len(related.R.UndIDTribNCMS)
		if ln > 1 && i < ln-1 {
			related.R.UndIDTribNCMS[i] = related.R.UndIDTribNCMS[ln-1]
		}
		related.R.UndIDTribNCMS = related.R.UndIDTribNCMS[:ln-1]
		break
	}
	return nil
}

// SetUnd of the ncm to the related item.
// Sets o.R.Und to related.
// Adds o to related.R.UndNCMS.
func (o *NCM) SetUnd(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Unidademedidas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ncm` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"und_id"}),
		strmangle.WhereClause("`", "`", 0, ncmPrimaryKeyColumns),
	)
	values := []interface{}{related.UndID, o.NCMNumero}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UndID = related.UndID
	if o.R == nil {
		o.R = &ncmR{
			Und: related,
		}
	} else {
		o.R.Und = related
	}

	if related.R == nil {
		related.R = &unidademedidasR{
			UndNCMS: NCMSlice{o},
		}
	} else {
		related.R.UndNCMS = append(related.R.UndNCMS, o)
	}

	return nil
}

// AddNCMNumeroEcfAliquota adds the given related objects to the existing relationships
// of the ncm, optionally inserting them as new records.
// Appends related to o.R.NCMNumeroEcfAliquota.
// Sets related.R.NCMNumeroNCM appropriately.
func (o *NCM) AddNCMNumeroEcfAliquota(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EcfAliquotum) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.NCMNumero, o.NCMNumero)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `ecf_aliquota` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"ncm_numero"}),
				strmangle.WhereClause("`", "`", 0, ecfAliquotumPrimaryKeyColumns),
			)
			values := []interface{}{o.NCMNumero, rel.EcaID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.NCMNumero, o.NCMNumero)
		}
	}

	if o.R == nil {
		o.R = &ncmR{
			NCMNumeroEcfAliquota: related,
		}
	} else {
		o.R.NCMNumeroEcfAliquota = append(o.R.NCMNumeroEcfAliquota, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &ecfAliquotumR{
				NCMNumeroNCM: o,
			}
		} else {
			rel.R.NCMNumeroNCM = o
		}
	}
	return nil
}

// SetNCMNumeroEcfAliquota removes all previously related items of the
// ncm replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.NCMNumeroNCM's NCMNumeroEcfAliquota accordingly.
// Replaces o.R.NCMNumeroEcfAliquota with related.
// Sets related.R.NCMNumeroNCM's NCMNumeroEcfAliquota accordingly.
func (o *NCM) SetNCMNumeroEcfAliquota(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EcfAliquotum) error {
	query := "update `ecf_aliquota` set `ncm_numero` = null where `ncm_numero` = ?"
	values := []interface{}{o.NCMNumero}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.NCMNumeroEcfAliquota {
			queries.SetScanner(&rel.NCMNumero, nil)
			if rel.R == nil {
				continue
			}

			rel.R.NCMNumeroNCM = nil
		}
		o.R.NCMNumeroEcfAliquota = nil
	}

	return o.AddNCMNumeroEcfAliquota(ctx, exec, insert, related...)
}

// RemoveNCMNumeroEcfAliquota relationships from objects passed in.
// Removes related items from R.NCMNumeroEcfAliquota (uses pointer comparison, removal does not keep order)
// Sets related.R.NCMNumeroNCM.
func (o *NCM) RemoveNCMNumeroEcfAliquota(ctx context.Context, exec boil.ContextExecutor, related ...*EcfAliquotum) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.NCMNumero, nil)
		if rel.R != nil {
			rel.R.NCMNumeroNCM = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("ncm_numero")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.NCMNumeroEcfAliquota {
			if rel != ri {
				continue
			}

			ln := len(o.R.NCMNumeroEcfAliquota)
			if ln > 1 && i < ln-1 {
				o.R.NCMNumeroEcfAliquota[i] = o.R.NCMNumeroEcfAliquota[ln-1]
			}
			o.R.NCMNumeroEcfAliquota = o.R.NCMNumeroEcfAliquota[:ln-1]
			break
		}
	}

	return nil
}

// AddNCMNumeroNCMEstados adds the given related objects to the existing relationships
// of the ncm, optionally inserting them as new records.
// Appends related to o.R.NCMNumeroNCMEstados.
// Sets related.R.NCMNumeroNCM appropriately.
func (o *NCM) AddNCMNumeroNCMEstados(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NCMEstado) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.NCMNumero = o.NCMNumero
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `ncm_estado` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"ncm_numero"}),
				strmangle.WhereClause("`", "`", 0, ncmEstadoPrimaryKeyColumns),
			)
			values := []interface{}{o.NCMNumero, rel.NCMNumero, rel.EstID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.NCMNumero = o.NCMNumero
		}
	}

	if o.R == nil {
		o.R = &ncmR{
			NCMNumeroNCMEstados: related,
		}
	} else {
		o.R.NCMNumeroNCMEstados = append(o.R.NCMNumeroNCMEstados, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &ncmEstadoR{
				NCMNumeroNCM: o,
			}
		} else {
			rel.R.NCMNumeroNCM = o
		}
	}
	return nil
}

// AddNCMNumeroNCMTributacaos adds the given related objects to the existing relationships
// of the ncm, optionally inserting them as new records.
// Appends related to o.R.NCMNumeroNCMTributacaos.
// Sets related.R.NCMNumeroNCM appropriately.
func (o *NCM) AddNCMNumeroNCMTributacaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*NCMTributacao) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.NCMNumero = o.NCMNumero
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `ncm_tributacao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"ncm_numero"}),
				strmangle.WhereClause("`", "`", 0, ncmTributacaoPrimaryKeyColumns),
			)
			values := []interface{}{o.NCMNumero, rel.NCMNumero, rel.NCTItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.NCMNumero = o.NCMNumero
		}
	}

	if o.R == nil {
		o.R = &ncmR{
			NCMNumeroNCMTributacaos: related,
		}
	} else {
		o.R.NCMNumeroNCMTributacaos = append(o.R.NCMNumeroNCMTributacaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &ncmTributacaoR{
				NCMNumeroNCM: o,
			}
		} else {
			rel.R.NCMNumeroNCM = o
		}
	}
	return nil
}

// AddNCMNumeroProdutos adds the given related objects to the existing relationships
// of the ncm, optionally inserting them as new records.
// Appends related to o.R.NCMNumeroProdutos.
// Sets related.R.NCMNumeroNCM appropriately.
func (o *NCM) AddNCMNumeroProdutos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Produto) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.NCMNumero = o.NCMNumero
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"ncm_numero"}),
				strmangle.WhereClause("`", "`", 0, produtoPrimaryKeyColumns),
			)
			values := []interface{}{o.NCMNumero, rel.ProID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.NCMNumero = o.NCMNumero
		}
	}

	if o.R == nil {
		o.R = &ncmR{
			NCMNumeroProdutos: related,
		}
	} else {
		o.R.NCMNumeroProdutos = append(o.R.NCMNumeroProdutos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoR{
				NCMNumeroNCM: o,
			}
		} else {
			rel.R.NCMNumeroNCM = o
		}
	}
	return nil
}

// NCMS retrieves all the records using an executor.
func NCMS(mods ...qm.QueryMod) ncmQuery {
	mods = append(mods, qm.From("`ncm`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`ncm`.*"})
	}

	return ncmQuery{q}
}

// FindNCM retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindNCM(ctx context.Context, exec boil.ContextExecutor, nCMNumero int, selectCols ...string) (*NCM, error) {
	ncmObj := &NCM{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ncm` where `ncm_numero`=?", sel,
	)

	q := queries.Raw(query, nCMNumero)

	err := q.Bind(ctx, exec, ncmObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ncm")
	}

	if err = ncmObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ncmObj, err
	}

	return ncmObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *NCM) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ncm provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ncmColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ncmInsertCacheMut.RLock()
	cache, cached := ncmInsertCache[key]
	ncmInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ncmAllColumns,
			ncmColumnsWithDefault,
			ncmColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ncmType, ncmMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ncmType, ncmMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ncm` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ncm` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ncm` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ncmPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ncm")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.NCMNumero,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ncm")
	}

CacheNoHooks:
	if !cached {
		ncmInsertCacheMut.Lock()
		ncmInsertCache[key] = cache
		ncmInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the NCM.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *NCM) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ncmUpdateCacheMut.RLock()
	cache, cached := ncmUpdateCache[key]
	ncmUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ncmAllColumns,
			ncmPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ncm, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ncm` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ncmPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ncmType, ncmMapping, append(wl, ncmPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ncm row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ncm")
	}

	if !cached {
		ncmUpdateCacheMut.Lock()
		ncmUpdateCache[key] = cache
		ncmUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ncmQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ncm")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ncm")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o NCMSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ncm` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ncm slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ncm")
	}
	return rowsAff, nil
}

var mySQLNCMUniqueColumns = []string{
	"ncm_numero",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *NCM) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ncm provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ncmColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLNCMUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ncmUpsertCacheMut.RLock()
	cache, cached := ncmUpsertCache[key]
	ncmUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			ncmAllColumns,
			ncmColumnsWithDefault,
			ncmColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ncmAllColumns,
			ncmPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ncm, could not build update column list")
		}

		ret := strmangle.SetComplement(ncmAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`ncm`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ncm` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ncmType, ncmMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ncmType, ncmMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ncm")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ncmType, ncmMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ncm")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ncm")
	}

CacheNoHooks:
	if !cached {
		ncmUpsertCacheMut.Lock()
		ncmUpsertCache[key] = cache
		ncmUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single NCM record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *NCM) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no NCM provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ncmPrimaryKeyMapping)
	sql := "DELETE FROM `ncm` WHERE `ncm_numero`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ncm")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ncm")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ncmQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ncmQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ncm")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ncm")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o NCMSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ncmBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ncm` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ncm slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ncm")
	}

	if len(ncmAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *NCM) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindNCM(ctx, exec, o.NCMNumero)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *NCMSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := NCMSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ncmPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ncm`.* FROM `ncm` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ncmPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in NCMSlice")
	}

	*o = slice

	return nil
}

// NCMExists checks if the NCM row exists.
func NCMExists(ctx context.Context, exec boil.ContextExecutor, nCMNumero int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ncm` where `ncm_numero`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, nCMNumero)
	}
	row := exec.QueryRowContext(ctx, sql, nCMNumero)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ncm exists")
	}

	return exists, nil
}

// Exists checks if the NCM row exists.
func (o *NCM) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return NCMExists(ctx, exec, o.NCMNumero)
}
