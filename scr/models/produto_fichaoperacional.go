// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// ProdutoFichaoperacional is an object representing the database table.
type ProdutoFichaoperacional struct {
	ProID         int               `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	DespID        int               `boil:"desp_id" json:"desp_id" toml:"desp_id" yaml:"desp_id"`
	PfoQuantidade types.Decimal     `boil:"pfo_quantidade" json:"pfo_quantidade" toml:"pfo_quantidade" yaml:"pfo_quantidade"`
	PfoCusto      types.NullDecimal `boil:"pfo_custo" json:"pfo_custo,omitempty" toml:"pfo_custo" yaml:"pfo_custo,omitempty"`

	R *produtoFichaoperacionalR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L produtoFichaoperacionalL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ProdutoFichaoperacionalColumns = struct {
	ProID         string
	DespID        string
	PfoQuantidade string
	PfoCusto      string
}{
	ProID:         "pro_id",
	DespID:        "desp_id",
	PfoQuantidade: "pfo_quantidade",
	PfoCusto:      "pfo_custo",
}

var ProdutoFichaoperacionalTableColumns = struct {
	ProID         string
	DespID        string
	PfoQuantidade string
	PfoCusto      string
}{
	ProID:         "produto_fichaoperacional.pro_id",
	DespID:        "produto_fichaoperacional.desp_id",
	PfoQuantidade: "produto_fichaoperacional.pfo_quantidade",
	PfoCusto:      "produto_fichaoperacional.pfo_custo",
}

// Generated where

var ProdutoFichaoperacionalWhere = struct {
	ProID         whereHelperint
	DespID        whereHelperint
	PfoQuantidade whereHelpertypes_Decimal
	PfoCusto      whereHelpertypes_NullDecimal
}{
	ProID:         whereHelperint{field: "`produto_fichaoperacional`.`pro_id`"},
	DespID:        whereHelperint{field: "`produto_fichaoperacional`.`desp_id`"},
	PfoQuantidade: whereHelpertypes_Decimal{field: "`produto_fichaoperacional`.`pfo_quantidade`"},
	PfoCusto:      whereHelpertypes_NullDecimal{field: "`produto_fichaoperacional`.`pfo_custo`"},
}

// ProdutoFichaoperacionalRels is where relationship names are stored.
var ProdutoFichaoperacionalRels = struct {
	Desp string
	Pro  string
}{
	Desp: "Desp",
	Pro:  "Pro",
}

// produtoFichaoperacionalR is where relationships are stored.
type produtoFichaoperacionalR struct {
	Desp *Despesas `boil:"Desp" json:"Desp" toml:"Desp" yaml:"Desp"`
	Pro  *Produto  `boil:"Pro" json:"Pro" toml:"Pro" yaml:"Pro"`
}

// NewStruct creates a new relationship struct
func (*produtoFichaoperacionalR) NewStruct() *produtoFichaoperacionalR {
	return &produtoFichaoperacionalR{}
}

func (r *produtoFichaoperacionalR) GetDesp() *Despesas {
	if r == nil {
		return nil
	}
	return r.Desp
}

func (r *produtoFichaoperacionalR) GetPro() *Produto {
	if r == nil {
		return nil
	}
	return r.Pro
}

// produtoFichaoperacionalL is where Load methods for each relationship are stored.
type produtoFichaoperacionalL struct{}

var (
	produtoFichaoperacionalAllColumns            = []string{"pro_id", "desp_id", "pfo_quantidade", "pfo_custo"}
	produtoFichaoperacionalColumnsWithoutDefault = []string{"pro_id", "desp_id", "pfo_quantidade", "pfo_custo"}
	produtoFichaoperacionalColumnsWithDefault    = []string{}
	produtoFichaoperacionalPrimaryKeyColumns     = []string{"pro_id", "desp_id"}
	produtoFichaoperacionalGeneratedColumns      = []string{}
)

type (
	// ProdutoFichaoperacionalSlice is an alias for a slice of pointers to ProdutoFichaoperacional.
	// This should almost always be used instead of []ProdutoFichaoperacional.
	ProdutoFichaoperacionalSlice []*ProdutoFichaoperacional
	// ProdutoFichaoperacionalHook is the signature for custom ProdutoFichaoperacional hook methods
	ProdutoFichaoperacionalHook func(context.Context, boil.ContextExecutor, *ProdutoFichaoperacional) error

	produtoFichaoperacionalQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	produtoFichaoperacionalType                 = reflect.TypeOf(&ProdutoFichaoperacional{})
	produtoFichaoperacionalMapping              = queries.MakeStructMapping(produtoFichaoperacionalType)
	produtoFichaoperacionalPrimaryKeyMapping, _ = queries.BindMapping(produtoFichaoperacionalType, produtoFichaoperacionalMapping, produtoFichaoperacionalPrimaryKeyColumns)
	produtoFichaoperacionalInsertCacheMut       sync.RWMutex
	produtoFichaoperacionalInsertCache          = make(map[string]insertCache)
	produtoFichaoperacionalUpdateCacheMut       sync.RWMutex
	produtoFichaoperacionalUpdateCache          = make(map[string]updateCache)
	produtoFichaoperacionalUpsertCacheMut       sync.RWMutex
	produtoFichaoperacionalUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var produtoFichaoperacionalAfterSelectMu sync.Mutex
var produtoFichaoperacionalAfterSelectHooks []ProdutoFichaoperacionalHook

var produtoFichaoperacionalBeforeInsertMu sync.Mutex
var produtoFichaoperacionalBeforeInsertHooks []ProdutoFichaoperacionalHook
var produtoFichaoperacionalAfterInsertMu sync.Mutex
var produtoFichaoperacionalAfterInsertHooks []ProdutoFichaoperacionalHook

var produtoFichaoperacionalBeforeUpdateMu sync.Mutex
var produtoFichaoperacionalBeforeUpdateHooks []ProdutoFichaoperacionalHook
var produtoFichaoperacionalAfterUpdateMu sync.Mutex
var produtoFichaoperacionalAfterUpdateHooks []ProdutoFichaoperacionalHook

var produtoFichaoperacionalBeforeDeleteMu sync.Mutex
var produtoFichaoperacionalBeforeDeleteHooks []ProdutoFichaoperacionalHook
var produtoFichaoperacionalAfterDeleteMu sync.Mutex
var produtoFichaoperacionalAfterDeleteHooks []ProdutoFichaoperacionalHook

var produtoFichaoperacionalBeforeUpsertMu sync.Mutex
var produtoFichaoperacionalBeforeUpsertHooks []ProdutoFichaoperacionalHook
var produtoFichaoperacionalAfterUpsertMu sync.Mutex
var produtoFichaoperacionalAfterUpsertHooks []ProdutoFichaoperacionalHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ProdutoFichaoperacional) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ProdutoFichaoperacional) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ProdutoFichaoperacional) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ProdutoFichaoperacional) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ProdutoFichaoperacional) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ProdutoFichaoperacional) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ProdutoFichaoperacional) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ProdutoFichaoperacional) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ProdutoFichaoperacional) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range produtoFichaoperacionalAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddProdutoFichaoperacionalHook registers your hook function for all future operations.
func AddProdutoFichaoperacionalHook(hookPoint boil.HookPoint, produtoFichaoperacionalHook ProdutoFichaoperacionalHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		produtoFichaoperacionalAfterSelectMu.Lock()
		produtoFichaoperacionalAfterSelectHooks = append(produtoFichaoperacionalAfterSelectHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		produtoFichaoperacionalBeforeInsertMu.Lock()
		produtoFichaoperacionalBeforeInsertHooks = append(produtoFichaoperacionalBeforeInsertHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		produtoFichaoperacionalAfterInsertMu.Lock()
		produtoFichaoperacionalAfterInsertHooks = append(produtoFichaoperacionalAfterInsertHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		produtoFichaoperacionalBeforeUpdateMu.Lock()
		produtoFichaoperacionalBeforeUpdateHooks = append(produtoFichaoperacionalBeforeUpdateHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		produtoFichaoperacionalAfterUpdateMu.Lock()
		produtoFichaoperacionalAfterUpdateHooks = append(produtoFichaoperacionalAfterUpdateHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		produtoFichaoperacionalBeforeDeleteMu.Lock()
		produtoFichaoperacionalBeforeDeleteHooks = append(produtoFichaoperacionalBeforeDeleteHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		produtoFichaoperacionalAfterDeleteMu.Lock()
		produtoFichaoperacionalAfterDeleteHooks = append(produtoFichaoperacionalAfterDeleteHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		produtoFichaoperacionalBeforeUpsertMu.Lock()
		produtoFichaoperacionalBeforeUpsertHooks = append(produtoFichaoperacionalBeforeUpsertHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		produtoFichaoperacionalAfterUpsertMu.Lock()
		produtoFichaoperacionalAfterUpsertHooks = append(produtoFichaoperacionalAfterUpsertHooks, produtoFichaoperacionalHook)
		produtoFichaoperacionalAfterUpsertMu.Unlock()
	}
}

// One returns a single produtoFichaoperacional record from the query.
func (q produtoFichaoperacionalQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ProdutoFichaoperacional, error) {
	o := &ProdutoFichaoperacional{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for produto_fichaoperacional")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ProdutoFichaoperacional records from the query.
func (q produtoFichaoperacionalQuery) All(ctx context.Context, exec boil.ContextExecutor) (ProdutoFichaoperacionalSlice, error) {
	var o []*ProdutoFichaoperacional

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ProdutoFichaoperacional slice")
	}

	if len(produtoFichaoperacionalAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ProdutoFichaoperacional records in the query.
func (q produtoFichaoperacionalQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count produto_fichaoperacional rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q produtoFichaoperacionalQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if produto_fichaoperacional exists")
	}

	return count > 0, nil
}

// Desp pointed to by the foreign key.
func (o *ProdutoFichaoperacional) Desp(mods ...qm.QueryMod) despesasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`desp_id` = ?", o.DespID),
	}

	queryMods = append(queryMods, mods...)

	return Despesas(queryMods...)
}

// Pro pointed to by the foreign key.
func (o *ProdutoFichaoperacional) Pro(mods ...qm.QueryMod) produtoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pro_id` = ?", o.ProID),
	}

	queryMods = append(queryMods, mods...)

	return Produtos(queryMods...)
}

// LoadDesp allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (produtoFichaoperacionalL) LoadDesp(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProdutoFichaoperacional interface{}, mods queries.Applicator) error {
	var slice []*ProdutoFichaoperacional
	var object *ProdutoFichaoperacional

	if singular {
		var ok bool
		object, ok = maybeProdutoFichaoperacional.(*ProdutoFichaoperacional)
		if !ok {
			object = new(ProdutoFichaoperacional)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeProdutoFichaoperacional)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeProdutoFichaoperacional))
			}
		}
	} else {
		s, ok := maybeProdutoFichaoperacional.(*[]*ProdutoFichaoperacional)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeProdutoFichaoperacional)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeProdutoFichaoperacional))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &produtoFichaoperacionalR{}
		}
		args[object.DespID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &produtoFichaoperacionalR{}
			}

			args[obj.DespID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`despesas`),
		qm.WhereIn(`despesas.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Despesas")
	}

	var resultSlice []*Despesas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Despesas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for despesas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for despesas")
	}

	if len(despesasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Desp = foreign
		if foreign.R == nil {
			foreign.R = &despesasR{}
		}
		foreign.R.DespProdutoFichaoperacionals = append(foreign.R.DespProdutoFichaoperacionals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.DespID == foreign.DespID {
				local.R.Desp = foreign
				if foreign.R == nil {
					foreign.R = &despesasR{}
				}
				foreign.R.DespProdutoFichaoperacionals = append(foreign.R.DespProdutoFichaoperacionals, local)
				break
			}
		}
	}

	return nil
}

// LoadPro allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (produtoFichaoperacionalL) LoadPro(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProdutoFichaoperacional interface{}, mods queries.Applicator) error {
	var slice []*ProdutoFichaoperacional
	var object *ProdutoFichaoperacional

	if singular {
		var ok bool
		object, ok = maybeProdutoFichaoperacional.(*ProdutoFichaoperacional)
		if !ok {
			object = new(ProdutoFichaoperacional)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeProdutoFichaoperacional)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeProdutoFichaoperacional))
			}
		}
	} else {
		s, ok := maybeProdutoFichaoperacional.(*[]*ProdutoFichaoperacional)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeProdutoFichaoperacional)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeProdutoFichaoperacional))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &produtoFichaoperacionalR{}
		}
		args[object.ProID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &produtoFichaoperacionalR{}
			}

			args[obj.ProID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.pro_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pro = foreign
		if foreign.R == nil {
			foreign.R = &produtoR{}
		}
		foreign.R.ProProdutoFichaoperacionals = append(foreign.R.ProProdutoFichaoperacionals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProID == foreign.ProID {
				local.R.Pro = foreign
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.ProProdutoFichaoperacionals = append(foreign.R.ProProdutoFichaoperacionals, local)
				break
			}
		}
	}

	return nil
}

// SetDesp of the produtoFichaoperacional to the related item.
// Sets o.R.Desp to related.
// Adds o to related.R.DespProdutoFichaoperacionals.
func (o *ProdutoFichaoperacional) SetDesp(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Despesas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `produto_fichaoperacional` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
		strmangle.WhereClause("`", "`", 0, produtoFichaoperacionalPrimaryKeyColumns),
	)
	values := []interface{}{related.DespID, o.ProID, o.DespID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.DespID = related.DespID
	if o.R == nil {
		o.R = &produtoFichaoperacionalR{
			Desp: related,
		}
	} else {
		o.R.Desp = related
	}

	if related.R == nil {
		related.R = &despesasR{
			DespProdutoFichaoperacionals: ProdutoFichaoperacionalSlice{o},
		}
	} else {
		related.R.DespProdutoFichaoperacionals = append(related.R.DespProdutoFichaoperacionals, o)
	}

	return nil
}

// SetPro of the produtoFichaoperacional to the related item.
// Sets o.R.Pro to related.
// Adds o to related.R.ProProdutoFichaoperacionals.
func (o *ProdutoFichaoperacional) SetPro(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Produto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `produto_fichaoperacional` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pro_id"}),
		strmangle.WhereClause("`", "`", 0, produtoFichaoperacionalPrimaryKeyColumns),
	)
	values := []interface{}{related.ProID, o.ProID, o.DespID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProID = related.ProID
	if o.R == nil {
		o.R = &produtoFichaoperacionalR{
			Pro: related,
		}
	} else {
		o.R.Pro = related
	}

	if related.R == nil {
		related.R = &produtoR{
			ProProdutoFichaoperacionals: ProdutoFichaoperacionalSlice{o},
		}
	} else {
		related.R.ProProdutoFichaoperacionals = append(related.R.ProProdutoFichaoperacionals, o)
	}

	return nil
}

// ProdutoFichaoperacionals retrieves all the records using an executor.
func ProdutoFichaoperacionals(mods ...qm.QueryMod) produtoFichaoperacionalQuery {
	mods = append(mods, qm.From("`produto_fichaoperacional`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`produto_fichaoperacional`.*"})
	}

	return produtoFichaoperacionalQuery{q}
}

// FindProdutoFichaoperacional retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindProdutoFichaoperacional(ctx context.Context, exec boil.ContextExecutor, proID int, despID int, selectCols ...string) (*ProdutoFichaoperacional, error) {
	produtoFichaoperacionalObj := &ProdutoFichaoperacional{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `produto_fichaoperacional` where `pro_id`=? AND `desp_id`=?", sel,
	)

	q := queries.Raw(query, proID, despID)

	err := q.Bind(ctx, exec, produtoFichaoperacionalObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from produto_fichaoperacional")
	}

	if err = produtoFichaoperacionalObj.doAfterSelectHooks(ctx, exec); err != nil {
		return produtoFichaoperacionalObj, err
	}

	return produtoFichaoperacionalObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ProdutoFichaoperacional) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no produto_fichaoperacional provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(produtoFichaoperacionalColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	produtoFichaoperacionalInsertCacheMut.RLock()
	cache, cached := produtoFichaoperacionalInsertCache[key]
	produtoFichaoperacionalInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			produtoFichaoperacionalAllColumns,
			produtoFichaoperacionalColumnsWithDefault,
			produtoFichaoperacionalColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(produtoFichaoperacionalType, produtoFichaoperacionalMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(produtoFichaoperacionalType, produtoFichaoperacionalMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `produto_fichaoperacional` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `produto_fichaoperacional` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `produto_fichaoperacional` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, produtoFichaoperacionalPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into produto_fichaoperacional")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ProID,
		o.DespID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for produto_fichaoperacional")
	}

CacheNoHooks:
	if !cached {
		produtoFichaoperacionalInsertCacheMut.Lock()
		produtoFichaoperacionalInsertCache[key] = cache
		produtoFichaoperacionalInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ProdutoFichaoperacional.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ProdutoFichaoperacional) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	produtoFichaoperacionalUpdateCacheMut.RLock()
	cache, cached := produtoFichaoperacionalUpdateCache[key]
	produtoFichaoperacionalUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			produtoFichaoperacionalAllColumns,
			produtoFichaoperacionalPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update produto_fichaoperacional, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `produto_fichaoperacional` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, produtoFichaoperacionalPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(produtoFichaoperacionalType, produtoFichaoperacionalMapping, append(wl, produtoFichaoperacionalPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update produto_fichaoperacional row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for produto_fichaoperacional")
	}

	if !cached {
		produtoFichaoperacionalUpdateCacheMut.Lock()
		produtoFichaoperacionalUpdateCache[key] = cache
		produtoFichaoperacionalUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q produtoFichaoperacionalQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for produto_fichaoperacional")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for produto_fichaoperacional")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ProdutoFichaoperacionalSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), produtoFichaoperacionalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `produto_fichaoperacional` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, produtoFichaoperacionalPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in produtoFichaoperacional slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all produtoFichaoperacional")
	}
	return rowsAff, nil
}

var mySQLProdutoFichaoperacionalUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ProdutoFichaoperacional) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no produto_fichaoperacional provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(produtoFichaoperacionalColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLProdutoFichaoperacionalUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	produtoFichaoperacionalUpsertCacheMut.RLock()
	cache, cached := produtoFichaoperacionalUpsertCache[key]
	produtoFichaoperacionalUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			produtoFichaoperacionalAllColumns,
			produtoFichaoperacionalColumnsWithDefault,
			produtoFichaoperacionalColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			produtoFichaoperacionalAllColumns,
			produtoFichaoperacionalPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert produto_fichaoperacional, could not build update column list")
		}

		ret := strmangle.SetComplement(produtoFichaoperacionalAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`produto_fichaoperacional`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `produto_fichaoperacional` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(produtoFichaoperacionalType, produtoFichaoperacionalMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(produtoFichaoperacionalType, produtoFichaoperacionalMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for produto_fichaoperacional")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(produtoFichaoperacionalType, produtoFichaoperacionalMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for produto_fichaoperacional")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for produto_fichaoperacional")
	}

CacheNoHooks:
	if !cached {
		produtoFichaoperacionalUpsertCacheMut.Lock()
		produtoFichaoperacionalUpsertCache[key] = cache
		produtoFichaoperacionalUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ProdutoFichaoperacional record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ProdutoFichaoperacional) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ProdutoFichaoperacional provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), produtoFichaoperacionalPrimaryKeyMapping)
	sql := "DELETE FROM `produto_fichaoperacional` WHERE `pro_id`=? AND `desp_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from produto_fichaoperacional")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for produto_fichaoperacional")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q produtoFichaoperacionalQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no produtoFichaoperacionalQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from produto_fichaoperacional")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for produto_fichaoperacional")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ProdutoFichaoperacionalSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(produtoFichaoperacionalBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), produtoFichaoperacionalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `produto_fichaoperacional` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, produtoFichaoperacionalPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from produtoFichaoperacional slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for produto_fichaoperacional")
	}

	if len(produtoFichaoperacionalAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ProdutoFichaoperacional) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindProdutoFichaoperacional(ctx, exec, o.ProID, o.DespID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ProdutoFichaoperacionalSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ProdutoFichaoperacionalSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), produtoFichaoperacionalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `produto_fichaoperacional`.* FROM `produto_fichaoperacional` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, produtoFichaoperacionalPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ProdutoFichaoperacionalSlice")
	}

	*o = slice

	return nil
}

// ProdutoFichaoperacionalExists checks if the ProdutoFichaoperacional row exists.
func ProdutoFichaoperacionalExists(ctx context.Context, exec boil.ContextExecutor, proID int, despID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `produto_fichaoperacional` where `pro_id`=? AND `desp_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, proID, despID)
	}
	row := exec.QueryRowContext(ctx, sql, proID, despID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if produto_fichaoperacional exists")
	}

	return exists, nil
}

// Exists checks if the ProdutoFichaoperacional row exists.
func (o *ProdutoFichaoperacional) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ProdutoFichaoperacionalExists(ctx, exec, o.ProID, o.DespID)
}
