// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// BancoTipoDocumento is an object representing the database table.
type BancoTipoDocumento struct {
	BanID            int    `boil:"ban_id" json:"ban_id" toml:"ban_id" yaml:"ban_id"`
	BantdocCodigo    string `boil:"bantdoc_codigo" json:"bantdoc_codigo" toml:"bantdoc_codigo" yaml:"bantdoc_codigo"`
	BantdocDescricao string `boil:"bantdoc_descricao" json:"bantdoc_descricao" toml:"bantdoc_descricao" yaml:"bantdoc_descricao"`

	R *bancoTipoDocumentoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L bancoTipoDocumentoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BancoTipoDocumentoColumns = struct {
	BanID            string
	BantdocCodigo    string
	BantdocDescricao string
}{
	BanID:            "ban_id",
	BantdocCodigo:    "bantdoc_codigo",
	BantdocDescricao: "bantdoc_descricao",
}

var BancoTipoDocumentoTableColumns = struct {
	BanID            string
	BantdocCodigo    string
	BantdocDescricao string
}{
	BanID:            "banco_tipo_documento.ban_id",
	BantdocCodigo:    "banco_tipo_documento.bantdoc_codigo",
	BantdocDescricao: "banco_tipo_documento.bantdoc_descricao",
}

// Generated where

var BancoTipoDocumentoWhere = struct {
	BanID            whereHelperint
	BantdocCodigo    whereHelperstring
	BantdocDescricao whereHelperstring
}{
	BanID:            whereHelperint{field: "`banco_tipo_documento`.`ban_id`"},
	BantdocCodigo:    whereHelperstring{field: "`banco_tipo_documento`.`bantdoc_codigo`"},
	BantdocDescricao: whereHelperstring{field: "`banco_tipo_documento`.`bantdoc_descricao`"},
}

// BancoTipoDocumentoRels is where relationship names are stored.
var BancoTipoDocumentoRels = struct {
	Ban string
}{
	Ban: "Ban",
}

// bancoTipoDocumentoR is where relationships are stored.
type bancoTipoDocumentoR struct {
	Ban *Banco `boil:"Ban" json:"Ban" toml:"Ban" yaml:"Ban"`
}

// NewStruct creates a new relationship struct
func (*bancoTipoDocumentoR) NewStruct() *bancoTipoDocumentoR {
	return &bancoTipoDocumentoR{}
}

func (r *bancoTipoDocumentoR) GetBan() *Banco {
	if r == nil {
		return nil
	}
	return r.Ban
}

// bancoTipoDocumentoL is where Load methods for each relationship are stored.
type bancoTipoDocumentoL struct{}

var (
	bancoTipoDocumentoAllColumns            = []string{"ban_id", "bantdoc_codigo", "bantdoc_descricao"}
	bancoTipoDocumentoColumnsWithoutDefault = []string{"ban_id", "bantdoc_codigo", "bantdoc_descricao"}
	bancoTipoDocumentoColumnsWithDefault    = []string{}
	bancoTipoDocumentoPrimaryKeyColumns     = []string{"ban_id", "bantdoc_codigo"}
	bancoTipoDocumentoGeneratedColumns      = []string{}
)

type (
	// BancoTipoDocumentoSlice is an alias for a slice of pointers to BancoTipoDocumento.
	// This should almost always be used instead of []BancoTipoDocumento.
	BancoTipoDocumentoSlice []*BancoTipoDocumento
	// BancoTipoDocumentoHook is the signature for custom BancoTipoDocumento hook methods
	BancoTipoDocumentoHook func(context.Context, boil.ContextExecutor, *BancoTipoDocumento) error

	bancoTipoDocumentoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	bancoTipoDocumentoType                 = reflect.TypeOf(&BancoTipoDocumento{})
	bancoTipoDocumentoMapping              = queries.MakeStructMapping(bancoTipoDocumentoType)
	bancoTipoDocumentoPrimaryKeyMapping, _ = queries.BindMapping(bancoTipoDocumentoType, bancoTipoDocumentoMapping, bancoTipoDocumentoPrimaryKeyColumns)
	bancoTipoDocumentoInsertCacheMut       sync.RWMutex
	bancoTipoDocumentoInsertCache          = make(map[string]insertCache)
	bancoTipoDocumentoUpdateCacheMut       sync.RWMutex
	bancoTipoDocumentoUpdateCache          = make(map[string]updateCache)
	bancoTipoDocumentoUpsertCacheMut       sync.RWMutex
	bancoTipoDocumentoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var bancoTipoDocumentoAfterSelectMu sync.Mutex
var bancoTipoDocumentoAfterSelectHooks []BancoTipoDocumentoHook

var bancoTipoDocumentoBeforeInsertMu sync.Mutex
var bancoTipoDocumentoBeforeInsertHooks []BancoTipoDocumentoHook
var bancoTipoDocumentoAfterInsertMu sync.Mutex
var bancoTipoDocumentoAfterInsertHooks []BancoTipoDocumentoHook

var bancoTipoDocumentoBeforeUpdateMu sync.Mutex
var bancoTipoDocumentoBeforeUpdateHooks []BancoTipoDocumentoHook
var bancoTipoDocumentoAfterUpdateMu sync.Mutex
var bancoTipoDocumentoAfterUpdateHooks []BancoTipoDocumentoHook

var bancoTipoDocumentoBeforeDeleteMu sync.Mutex
var bancoTipoDocumentoBeforeDeleteHooks []BancoTipoDocumentoHook
var bancoTipoDocumentoAfterDeleteMu sync.Mutex
var bancoTipoDocumentoAfterDeleteHooks []BancoTipoDocumentoHook

var bancoTipoDocumentoBeforeUpsertMu sync.Mutex
var bancoTipoDocumentoBeforeUpsertHooks []BancoTipoDocumentoHook
var bancoTipoDocumentoAfterUpsertMu sync.Mutex
var bancoTipoDocumentoAfterUpsertHooks []BancoTipoDocumentoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BancoTipoDocumento) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BancoTipoDocumento) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BancoTipoDocumento) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BancoTipoDocumento) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BancoTipoDocumento) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BancoTipoDocumento) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BancoTipoDocumento) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BancoTipoDocumento) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BancoTipoDocumento) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bancoTipoDocumentoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBancoTipoDocumentoHook registers your hook function for all future operations.
func AddBancoTipoDocumentoHook(hookPoint boil.HookPoint, bancoTipoDocumentoHook BancoTipoDocumentoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		bancoTipoDocumentoAfterSelectMu.Lock()
		bancoTipoDocumentoAfterSelectHooks = append(bancoTipoDocumentoAfterSelectHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		bancoTipoDocumentoBeforeInsertMu.Lock()
		bancoTipoDocumentoBeforeInsertHooks = append(bancoTipoDocumentoBeforeInsertHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		bancoTipoDocumentoAfterInsertMu.Lock()
		bancoTipoDocumentoAfterInsertHooks = append(bancoTipoDocumentoAfterInsertHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		bancoTipoDocumentoBeforeUpdateMu.Lock()
		bancoTipoDocumentoBeforeUpdateHooks = append(bancoTipoDocumentoBeforeUpdateHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		bancoTipoDocumentoAfterUpdateMu.Lock()
		bancoTipoDocumentoAfterUpdateHooks = append(bancoTipoDocumentoAfterUpdateHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		bancoTipoDocumentoBeforeDeleteMu.Lock()
		bancoTipoDocumentoBeforeDeleteHooks = append(bancoTipoDocumentoBeforeDeleteHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		bancoTipoDocumentoAfterDeleteMu.Lock()
		bancoTipoDocumentoAfterDeleteHooks = append(bancoTipoDocumentoAfterDeleteHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		bancoTipoDocumentoBeforeUpsertMu.Lock()
		bancoTipoDocumentoBeforeUpsertHooks = append(bancoTipoDocumentoBeforeUpsertHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		bancoTipoDocumentoAfterUpsertMu.Lock()
		bancoTipoDocumentoAfterUpsertHooks = append(bancoTipoDocumentoAfterUpsertHooks, bancoTipoDocumentoHook)
		bancoTipoDocumentoAfterUpsertMu.Unlock()
	}
}

// One returns a single bancoTipoDocumento record from the query.
func (q bancoTipoDocumentoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BancoTipoDocumento, error) {
	o := &BancoTipoDocumento{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for banco_tipo_documento")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all BancoTipoDocumento records from the query.
func (q bancoTipoDocumentoQuery) All(ctx context.Context, exec boil.ContextExecutor) (BancoTipoDocumentoSlice, error) {
	var o []*BancoTipoDocumento

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BancoTipoDocumento slice")
	}

	if len(bancoTipoDocumentoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all BancoTipoDocumento records in the query.
func (q bancoTipoDocumentoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count banco_tipo_documento rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q bancoTipoDocumentoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if banco_tipo_documento exists")
	}

	return count > 0, nil
}

// Ban pointed to by the foreign key.
func (o *BancoTipoDocumento) Ban(mods ...qm.QueryMod) bancoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ban_id` = ?", o.BanID),
	}

	queryMods = append(queryMods, mods...)

	return Bancos(queryMods...)
}

// LoadBan allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (bancoTipoDocumentoL) LoadBan(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBancoTipoDocumento interface{}, mods queries.Applicator) error {
	var slice []*BancoTipoDocumento
	var object *BancoTipoDocumento

	if singular {
		var ok bool
		object, ok = maybeBancoTipoDocumento.(*BancoTipoDocumento)
		if !ok {
			object = new(BancoTipoDocumento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeBancoTipoDocumento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeBancoTipoDocumento))
			}
		}
	} else {
		s, ok := maybeBancoTipoDocumento.(*[]*BancoTipoDocumento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeBancoTipoDocumento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeBancoTipoDocumento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &bancoTipoDocumentoR{}
		}
		args[object.BanID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &bancoTipoDocumentoR{}
			}

			args[obj.BanID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`bancos`),
		qm.WhereIn(`bancos.ban_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Banco")
	}

	var resultSlice []*Banco
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Banco")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for bancos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for bancos")
	}

	if len(bancoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ban = foreign
		if foreign.R == nil {
			foreign.R = &bancoR{}
		}
		foreign.R.BanBancoTipoDocumentos = append(foreign.R.BanBancoTipoDocumentos, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BanID == foreign.BanID {
				local.R.Ban = foreign
				if foreign.R == nil {
					foreign.R = &bancoR{}
				}
				foreign.R.BanBancoTipoDocumentos = append(foreign.R.BanBancoTipoDocumentos, local)
				break
			}
		}
	}

	return nil
}

// SetBan of the bancoTipoDocumento to the related item.
// Sets o.R.Ban to related.
// Adds o to related.R.BanBancoTipoDocumentos.
func (o *BancoTipoDocumento) SetBan(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Banco) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `banco_tipo_documento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ban_id"}),
		strmangle.WhereClause("`", "`", 0, bancoTipoDocumentoPrimaryKeyColumns),
	)
	values := []interface{}{related.BanID, o.BanID, o.BantdocCodigo}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BanID = related.BanID
	if o.R == nil {
		o.R = &bancoTipoDocumentoR{
			Ban: related,
		}
	} else {
		o.R.Ban = related
	}

	if related.R == nil {
		related.R = &bancoR{
			BanBancoTipoDocumentos: BancoTipoDocumentoSlice{o},
		}
	} else {
		related.R.BanBancoTipoDocumentos = append(related.R.BanBancoTipoDocumentos, o)
	}

	return nil
}

// BancoTipoDocumentos retrieves all the records using an executor.
func BancoTipoDocumentos(mods ...qm.QueryMod) bancoTipoDocumentoQuery {
	mods = append(mods, qm.From("`banco_tipo_documento`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`banco_tipo_documento`.*"})
	}

	return bancoTipoDocumentoQuery{q}
}

// FindBancoTipoDocumento retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBancoTipoDocumento(ctx context.Context, exec boil.ContextExecutor, banID int, bantdocCodigo string, selectCols ...string) (*BancoTipoDocumento, error) {
	bancoTipoDocumentoObj := &BancoTipoDocumento{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `banco_tipo_documento` where `ban_id`=? AND `bantdoc_codigo`=?", sel,
	)

	q := queries.Raw(query, banID, bantdocCodigo)

	err := q.Bind(ctx, exec, bancoTipoDocumentoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from banco_tipo_documento")
	}

	if err = bancoTipoDocumentoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return bancoTipoDocumentoObj, err
	}

	return bancoTipoDocumentoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BancoTipoDocumento) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no banco_tipo_documento provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(bancoTipoDocumentoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	bancoTipoDocumentoInsertCacheMut.RLock()
	cache, cached := bancoTipoDocumentoInsertCache[key]
	bancoTipoDocumentoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			bancoTipoDocumentoAllColumns,
			bancoTipoDocumentoColumnsWithDefault,
			bancoTipoDocumentoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(bancoTipoDocumentoType, bancoTipoDocumentoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(bancoTipoDocumentoType, bancoTipoDocumentoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `banco_tipo_documento` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `banco_tipo_documento` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `banco_tipo_documento` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, bancoTipoDocumentoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into banco_tipo_documento")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.BanID,
		o.BantdocCodigo,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for banco_tipo_documento")
	}

CacheNoHooks:
	if !cached {
		bancoTipoDocumentoInsertCacheMut.Lock()
		bancoTipoDocumentoInsertCache[key] = cache
		bancoTipoDocumentoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the BancoTipoDocumento.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BancoTipoDocumento) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	bancoTipoDocumentoUpdateCacheMut.RLock()
	cache, cached := bancoTipoDocumentoUpdateCache[key]
	bancoTipoDocumentoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			bancoTipoDocumentoAllColumns,
			bancoTipoDocumentoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update banco_tipo_documento, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `banco_tipo_documento` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, bancoTipoDocumentoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(bancoTipoDocumentoType, bancoTipoDocumentoMapping, append(wl, bancoTipoDocumentoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update banco_tipo_documento row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for banco_tipo_documento")
	}

	if !cached {
		bancoTipoDocumentoUpdateCacheMut.Lock()
		bancoTipoDocumentoUpdateCache[key] = cache
		bancoTipoDocumentoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q bancoTipoDocumentoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for banco_tipo_documento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for banco_tipo_documento")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BancoTipoDocumentoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bancoTipoDocumentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `banco_tipo_documento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bancoTipoDocumentoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in bancoTipoDocumento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all bancoTipoDocumento")
	}
	return rowsAff, nil
}

var mySQLBancoTipoDocumentoUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BancoTipoDocumento) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no banco_tipo_documento provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(bancoTipoDocumentoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLBancoTipoDocumentoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	bancoTipoDocumentoUpsertCacheMut.RLock()
	cache, cached := bancoTipoDocumentoUpsertCache[key]
	bancoTipoDocumentoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			bancoTipoDocumentoAllColumns,
			bancoTipoDocumentoColumnsWithDefault,
			bancoTipoDocumentoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			bancoTipoDocumentoAllColumns,
			bancoTipoDocumentoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert banco_tipo_documento, could not build update column list")
		}

		ret := strmangle.SetComplement(bancoTipoDocumentoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`banco_tipo_documento`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `banco_tipo_documento` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(bancoTipoDocumentoType, bancoTipoDocumentoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(bancoTipoDocumentoType, bancoTipoDocumentoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for banco_tipo_documento")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(bancoTipoDocumentoType, bancoTipoDocumentoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for banco_tipo_documento")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for banco_tipo_documento")
	}

CacheNoHooks:
	if !cached {
		bancoTipoDocumentoUpsertCacheMut.Lock()
		bancoTipoDocumentoUpsertCache[key] = cache
		bancoTipoDocumentoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single BancoTipoDocumento record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BancoTipoDocumento) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no BancoTipoDocumento provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), bancoTipoDocumentoPrimaryKeyMapping)
	sql := "DELETE FROM `banco_tipo_documento` WHERE `ban_id`=? AND `bantdoc_codigo`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from banco_tipo_documento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for banco_tipo_documento")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q bancoTipoDocumentoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no bancoTipoDocumentoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from banco_tipo_documento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for banco_tipo_documento")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BancoTipoDocumentoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(bancoTipoDocumentoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bancoTipoDocumentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `banco_tipo_documento` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bancoTipoDocumentoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from bancoTipoDocumento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for banco_tipo_documento")
	}

	if len(bancoTipoDocumentoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BancoTipoDocumento) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBancoTipoDocumento(ctx, exec, o.BanID, o.BantdocCodigo)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BancoTipoDocumentoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BancoTipoDocumentoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bancoTipoDocumentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `banco_tipo_documento`.* FROM `banco_tipo_documento` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bancoTipoDocumentoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BancoTipoDocumentoSlice")
	}

	*o = slice

	return nil
}

// BancoTipoDocumentoExists checks if the BancoTipoDocumento row exists.
func BancoTipoDocumentoExists(ctx context.Context, exec boil.ContextExecutor, banID int, bantdocCodigo string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `banco_tipo_documento` where `ban_id`=? AND `bantdoc_codigo`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, banID, bantdocCodigo)
	}
	row := exec.QueryRowContext(ctx, sql, banID, bantdocCodigo)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if banco_tipo_documento exists")
	}

	return exists, nil
}

// Exists checks if the BancoTipoDocumento row exists.
func (o *BancoTipoDocumento) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return BancoTipoDocumentoExists(ctx, exec, o.BanID, o.BantdocCodigo)
}
