// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TituloOcorrencium is an object representing the database table.
type TituloOcorrencium struct {
	TitID          int         `boil:"tit_id" json:"tit_id" toml:"tit_id" yaml:"tit_id"`
	TitoItem       int         `boil:"tito_item" json:"tito_item" toml:"tito_item" yaml:"tito_item"`
	TotID          int         `boil:"tot_id" json:"tot_id" toml:"tot_id" yaml:"tot_id"`
	TitoData       time.Time   `boil:"tito_data" json:"tito_data" toml:"tito_data" yaml:"tito_data"`
	TitoObservacao null.String `boil:"tito_observacao" json:"tito_observacao,omitempty" toml:"tito_observacao" yaml:"tito_observacao,omitempty"`

	R *tituloOcorrenciumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tituloOcorrenciumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TituloOcorrenciumColumns = struct {
	TitID          string
	TitoItem       string
	TotID          string
	TitoData       string
	TitoObservacao string
}{
	TitID:          "tit_id",
	TitoItem:       "tito_item",
	TotID:          "tot_id",
	TitoData:       "tito_data",
	TitoObservacao: "tito_observacao",
}

var TituloOcorrenciumTableColumns = struct {
	TitID          string
	TitoItem       string
	TotID          string
	TitoData       string
	TitoObservacao string
}{
	TitID:          "titulo_ocorrencia.tit_id",
	TitoItem:       "titulo_ocorrencia.tito_item",
	TotID:          "titulo_ocorrencia.tot_id",
	TitoData:       "titulo_ocorrencia.tito_data",
	TitoObservacao: "titulo_ocorrencia.tito_observacao",
}

// Generated where

var TituloOcorrenciumWhere = struct {
	TitID          whereHelperint
	TitoItem       whereHelperint
	TotID          whereHelperint
	TitoData       whereHelpertime_Time
	TitoObservacao whereHelpernull_String
}{
	TitID:          whereHelperint{field: "`titulo_ocorrencia`.`tit_id`"},
	TitoItem:       whereHelperint{field: "`titulo_ocorrencia`.`tito_item`"},
	TotID:          whereHelperint{field: "`titulo_ocorrencia`.`tot_id`"},
	TitoData:       whereHelpertime_Time{field: "`titulo_ocorrencia`.`tito_data`"},
	TitoObservacao: whereHelpernull_String{field: "`titulo_ocorrencia`.`tito_observacao`"},
}

// TituloOcorrenciumRels is where relationship names are stored.
var TituloOcorrenciumRels = struct {
	Tit string
	Tot string
}{
	Tit: "Tit",
	Tot: "Tot",
}

// tituloOcorrenciumR is where relationships are stored.
type tituloOcorrenciumR struct {
	Tit *Titulo               `boil:"Tit" json:"Tit" toml:"Tit" yaml:"Tit"`
	Tot *TituloOcorrenciaTipo `boil:"Tot" json:"Tot" toml:"Tot" yaml:"Tot"`
}

// NewStruct creates a new relationship struct
func (*tituloOcorrenciumR) NewStruct() *tituloOcorrenciumR {
	return &tituloOcorrenciumR{}
}

func (r *tituloOcorrenciumR) GetTit() *Titulo {
	if r == nil {
		return nil
	}
	return r.Tit
}

func (r *tituloOcorrenciumR) GetTot() *TituloOcorrenciaTipo {
	if r == nil {
		return nil
	}
	return r.Tot
}

// tituloOcorrenciumL is where Load methods for each relationship are stored.
type tituloOcorrenciumL struct{}

var (
	tituloOcorrenciumAllColumns            = []string{"tit_id", "tito_item", "tot_id", "tito_data", "tito_observacao"}
	tituloOcorrenciumColumnsWithoutDefault = []string{"tit_id", "tito_item", "tot_id", "tito_data", "tito_observacao"}
	tituloOcorrenciumColumnsWithDefault    = []string{}
	tituloOcorrenciumPrimaryKeyColumns     = []string{"tit_id", "tito_item"}
	tituloOcorrenciumGeneratedColumns      = []string{}
)

type (
	// TituloOcorrenciumSlice is an alias for a slice of pointers to TituloOcorrencium.
	// This should almost always be used instead of []TituloOcorrencium.
	TituloOcorrenciumSlice []*TituloOcorrencium
	// TituloOcorrenciumHook is the signature for custom TituloOcorrencium hook methods
	TituloOcorrenciumHook func(context.Context, boil.ContextExecutor, *TituloOcorrencium) error

	tituloOcorrenciumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tituloOcorrenciumType                 = reflect.TypeOf(&TituloOcorrencium{})
	tituloOcorrenciumMapping              = queries.MakeStructMapping(tituloOcorrenciumType)
	tituloOcorrenciumPrimaryKeyMapping, _ = queries.BindMapping(tituloOcorrenciumType, tituloOcorrenciumMapping, tituloOcorrenciumPrimaryKeyColumns)
	tituloOcorrenciumInsertCacheMut       sync.RWMutex
	tituloOcorrenciumInsertCache          = make(map[string]insertCache)
	tituloOcorrenciumUpdateCacheMut       sync.RWMutex
	tituloOcorrenciumUpdateCache          = make(map[string]updateCache)
	tituloOcorrenciumUpsertCacheMut       sync.RWMutex
	tituloOcorrenciumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tituloOcorrenciumAfterSelectMu sync.Mutex
var tituloOcorrenciumAfterSelectHooks []TituloOcorrenciumHook

var tituloOcorrenciumBeforeInsertMu sync.Mutex
var tituloOcorrenciumBeforeInsertHooks []TituloOcorrenciumHook
var tituloOcorrenciumAfterInsertMu sync.Mutex
var tituloOcorrenciumAfterInsertHooks []TituloOcorrenciumHook

var tituloOcorrenciumBeforeUpdateMu sync.Mutex
var tituloOcorrenciumBeforeUpdateHooks []TituloOcorrenciumHook
var tituloOcorrenciumAfterUpdateMu sync.Mutex
var tituloOcorrenciumAfterUpdateHooks []TituloOcorrenciumHook

var tituloOcorrenciumBeforeDeleteMu sync.Mutex
var tituloOcorrenciumBeforeDeleteHooks []TituloOcorrenciumHook
var tituloOcorrenciumAfterDeleteMu sync.Mutex
var tituloOcorrenciumAfterDeleteHooks []TituloOcorrenciumHook

var tituloOcorrenciumBeforeUpsertMu sync.Mutex
var tituloOcorrenciumBeforeUpsertHooks []TituloOcorrenciumHook
var tituloOcorrenciumAfterUpsertMu sync.Mutex
var tituloOcorrenciumAfterUpsertHooks []TituloOcorrenciumHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TituloOcorrencium) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TituloOcorrencium) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TituloOcorrencium) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TituloOcorrencium) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TituloOcorrencium) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TituloOcorrencium) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TituloOcorrencium) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TituloOcorrencium) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TituloOcorrencium) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloOcorrenciumAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTituloOcorrenciumHook registers your hook function for all future operations.
func AddTituloOcorrenciumHook(hookPoint boil.HookPoint, tituloOcorrenciumHook TituloOcorrenciumHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tituloOcorrenciumAfterSelectMu.Lock()
		tituloOcorrenciumAfterSelectHooks = append(tituloOcorrenciumAfterSelectHooks, tituloOcorrenciumHook)
		tituloOcorrenciumAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tituloOcorrenciumBeforeInsertMu.Lock()
		tituloOcorrenciumBeforeInsertHooks = append(tituloOcorrenciumBeforeInsertHooks, tituloOcorrenciumHook)
		tituloOcorrenciumBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tituloOcorrenciumAfterInsertMu.Lock()
		tituloOcorrenciumAfterInsertHooks = append(tituloOcorrenciumAfterInsertHooks, tituloOcorrenciumHook)
		tituloOcorrenciumAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tituloOcorrenciumBeforeUpdateMu.Lock()
		tituloOcorrenciumBeforeUpdateHooks = append(tituloOcorrenciumBeforeUpdateHooks, tituloOcorrenciumHook)
		tituloOcorrenciumBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tituloOcorrenciumAfterUpdateMu.Lock()
		tituloOcorrenciumAfterUpdateHooks = append(tituloOcorrenciumAfterUpdateHooks, tituloOcorrenciumHook)
		tituloOcorrenciumAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tituloOcorrenciumBeforeDeleteMu.Lock()
		tituloOcorrenciumBeforeDeleteHooks = append(tituloOcorrenciumBeforeDeleteHooks, tituloOcorrenciumHook)
		tituloOcorrenciumBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tituloOcorrenciumAfterDeleteMu.Lock()
		tituloOcorrenciumAfterDeleteHooks = append(tituloOcorrenciumAfterDeleteHooks, tituloOcorrenciumHook)
		tituloOcorrenciumAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tituloOcorrenciumBeforeUpsertMu.Lock()
		tituloOcorrenciumBeforeUpsertHooks = append(tituloOcorrenciumBeforeUpsertHooks, tituloOcorrenciumHook)
		tituloOcorrenciumBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tituloOcorrenciumAfterUpsertMu.Lock()
		tituloOcorrenciumAfterUpsertHooks = append(tituloOcorrenciumAfterUpsertHooks, tituloOcorrenciumHook)
		tituloOcorrenciumAfterUpsertMu.Unlock()
	}
}

// One returns a single tituloOcorrencium record from the query.
func (q tituloOcorrenciumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TituloOcorrencium, error) {
	o := &TituloOcorrencium{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for titulo_ocorrencia")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TituloOcorrencium records from the query.
func (q tituloOcorrenciumQuery) All(ctx context.Context, exec boil.ContextExecutor) (TituloOcorrenciumSlice, error) {
	var o []*TituloOcorrencium

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TituloOcorrencium slice")
	}

	if len(tituloOcorrenciumAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TituloOcorrencium records in the query.
func (q tituloOcorrenciumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count titulo_ocorrencia rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tituloOcorrenciumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if titulo_ocorrencia exists")
	}

	return count > 0, nil
}

// Tit pointed to by the foreign key.
func (o *TituloOcorrencium) Tit(mods ...qm.QueryMod) tituloQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`tit_id` = ?", o.TitID),
	}

	queryMods = append(queryMods, mods...)

	return Titulos(queryMods...)
}

// Tot pointed to by the foreign key.
func (o *TituloOcorrencium) Tot(mods ...qm.QueryMod) tituloOcorrenciaTipoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`tot_id` = ?", o.TotID),
	}

	queryMods = append(queryMods, mods...)

	return TituloOcorrenciaTipos(queryMods...)
}

// LoadTit allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloOcorrenciumL) LoadTit(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloOcorrencium interface{}, mods queries.Applicator) error {
	var slice []*TituloOcorrencium
	var object *TituloOcorrencium

	if singular {
		var ok bool
		object, ok = maybeTituloOcorrencium.(*TituloOcorrencium)
		if !ok {
			object = new(TituloOcorrencium)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloOcorrencium)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloOcorrencium))
			}
		}
	} else {
		s, ok := maybeTituloOcorrencium.(*[]*TituloOcorrencium)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloOcorrencium)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloOcorrencium))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloOcorrenciumR{}
		}
		args[object.TitID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloOcorrenciumR{}
			}

			args[obj.TitID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulos`),
		qm.WhereIn(`titulos.tit_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Titulo")
	}

	var resultSlice []*Titulo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Titulo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for titulos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulos")
	}

	if len(tituloAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tit = foreign
		if foreign.R == nil {
			foreign.R = &tituloR{}
		}
		foreign.R.TitTituloOcorrencia = append(foreign.R.TitTituloOcorrencia, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TitID == foreign.TitID {
				local.R.Tit = foreign
				if foreign.R == nil {
					foreign.R = &tituloR{}
				}
				foreign.R.TitTituloOcorrencia = append(foreign.R.TitTituloOcorrencia, local)
				break
			}
		}
	}

	return nil
}

// LoadTot allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloOcorrenciumL) LoadTot(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloOcorrencium interface{}, mods queries.Applicator) error {
	var slice []*TituloOcorrencium
	var object *TituloOcorrencium

	if singular {
		var ok bool
		object, ok = maybeTituloOcorrencium.(*TituloOcorrencium)
		if !ok {
			object = new(TituloOcorrencium)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloOcorrencium)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloOcorrencium))
			}
		}
	} else {
		s, ok := maybeTituloOcorrencium.(*[]*TituloOcorrencium)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloOcorrencium)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloOcorrencium))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloOcorrenciumR{}
		}
		args[object.TotID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloOcorrenciumR{}
			}

			args[obj.TotID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulo_ocorrencia_tipo`),
		qm.WhereIn(`titulo_ocorrencia_tipo.tot_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TituloOcorrenciaTipo")
	}

	var resultSlice []*TituloOcorrenciaTipo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TituloOcorrenciaTipo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for titulo_ocorrencia_tipo")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulo_ocorrencia_tipo")
	}

	if len(tituloOcorrenciaTipoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tot = foreign
		if foreign.R == nil {
			foreign.R = &tituloOcorrenciaTipoR{}
		}
		foreign.R.TotTituloOcorrencia = append(foreign.R.TotTituloOcorrencia, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TotID == foreign.TotID {
				local.R.Tot = foreign
				if foreign.R == nil {
					foreign.R = &tituloOcorrenciaTipoR{}
				}
				foreign.R.TotTituloOcorrencia = append(foreign.R.TotTituloOcorrencia, local)
				break
			}
		}
	}

	return nil
}

// SetTit of the tituloOcorrencium to the related item.
// Sets o.R.Tit to related.
// Adds o to related.R.TitTituloOcorrencia.
func (o *TituloOcorrencium) SetTit(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Titulo) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_ocorrencia` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"tit_id"}),
		strmangle.WhereClause("`", "`", 0, tituloOcorrenciumPrimaryKeyColumns),
	)
	values := []interface{}{related.TitID, o.TitID, o.TitoItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TitID = related.TitID
	if o.R == nil {
		o.R = &tituloOcorrenciumR{
			Tit: related,
		}
	} else {
		o.R.Tit = related
	}

	if related.R == nil {
		related.R = &tituloR{
			TitTituloOcorrencia: TituloOcorrenciumSlice{o},
		}
	} else {
		related.R.TitTituloOcorrencia = append(related.R.TitTituloOcorrencia, o)
	}

	return nil
}

// SetTot of the tituloOcorrencium to the related item.
// Sets o.R.Tot to related.
// Adds o to related.R.TotTituloOcorrencia.
func (o *TituloOcorrencium) SetTot(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TituloOcorrenciaTipo) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_ocorrencia` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"tot_id"}),
		strmangle.WhereClause("`", "`", 0, tituloOcorrenciumPrimaryKeyColumns),
	)
	values := []interface{}{related.TotID, o.TitID, o.TitoItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TotID = related.TotID
	if o.R == nil {
		o.R = &tituloOcorrenciumR{
			Tot: related,
		}
	} else {
		o.R.Tot = related
	}

	if related.R == nil {
		related.R = &tituloOcorrenciaTipoR{
			TotTituloOcorrencia: TituloOcorrenciumSlice{o},
		}
	} else {
		related.R.TotTituloOcorrencia = append(related.R.TotTituloOcorrencia, o)
	}

	return nil
}

// TituloOcorrencia retrieves all the records using an executor.
func TituloOcorrencia(mods ...qm.QueryMod) tituloOcorrenciumQuery {
	mods = append(mods, qm.From("`titulo_ocorrencia`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`titulo_ocorrencia`.*"})
	}

	return tituloOcorrenciumQuery{q}
}

// FindTituloOcorrencium retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTituloOcorrencium(ctx context.Context, exec boil.ContextExecutor, titID int, titoItem int, selectCols ...string) (*TituloOcorrencium, error) {
	tituloOcorrenciumObj := &TituloOcorrencium{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `titulo_ocorrencia` where `tit_id`=? AND `tito_item`=?", sel,
	)

	q := queries.Raw(query, titID, titoItem)

	err := q.Bind(ctx, exec, tituloOcorrenciumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from titulo_ocorrencia")
	}

	if err = tituloOcorrenciumObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tituloOcorrenciumObj, err
	}

	return tituloOcorrenciumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TituloOcorrencium) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no titulo_ocorrencia provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tituloOcorrenciumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tituloOcorrenciumInsertCacheMut.RLock()
	cache, cached := tituloOcorrenciumInsertCache[key]
	tituloOcorrenciumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tituloOcorrenciumAllColumns,
			tituloOcorrenciumColumnsWithDefault,
			tituloOcorrenciumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tituloOcorrenciumType, tituloOcorrenciumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tituloOcorrenciumType, tituloOcorrenciumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `titulo_ocorrencia` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `titulo_ocorrencia` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `titulo_ocorrencia` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tituloOcorrenciumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into titulo_ocorrencia")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TitID,
		o.TitoItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for titulo_ocorrencia")
	}

CacheNoHooks:
	if !cached {
		tituloOcorrenciumInsertCacheMut.Lock()
		tituloOcorrenciumInsertCache[key] = cache
		tituloOcorrenciumInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TituloOcorrencium.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TituloOcorrencium) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tituloOcorrenciumUpdateCacheMut.RLock()
	cache, cached := tituloOcorrenciumUpdateCache[key]
	tituloOcorrenciumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tituloOcorrenciumAllColumns,
			tituloOcorrenciumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update titulo_ocorrencia, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `titulo_ocorrencia` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tituloOcorrenciumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tituloOcorrenciumType, tituloOcorrenciumMapping, append(wl, tituloOcorrenciumPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update titulo_ocorrencia row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for titulo_ocorrencia")
	}

	if !cached {
		tituloOcorrenciumUpdateCacheMut.Lock()
		tituloOcorrenciumUpdateCache[key] = cache
		tituloOcorrenciumUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tituloOcorrenciumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for titulo_ocorrencia")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for titulo_ocorrencia")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TituloOcorrenciumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloOcorrenciumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `titulo_ocorrencia` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloOcorrenciumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tituloOcorrencium slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tituloOcorrencium")
	}
	return rowsAff, nil
}

var mySQLTituloOcorrenciumUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TituloOcorrencium) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no titulo_ocorrencia provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tituloOcorrenciumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTituloOcorrenciumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tituloOcorrenciumUpsertCacheMut.RLock()
	cache, cached := tituloOcorrenciumUpsertCache[key]
	tituloOcorrenciumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tituloOcorrenciumAllColumns,
			tituloOcorrenciumColumnsWithDefault,
			tituloOcorrenciumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tituloOcorrenciumAllColumns,
			tituloOcorrenciumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert titulo_ocorrencia, could not build update column list")
		}

		ret := strmangle.SetComplement(tituloOcorrenciumAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`titulo_ocorrencia`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `titulo_ocorrencia` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tituloOcorrenciumType, tituloOcorrenciumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tituloOcorrenciumType, tituloOcorrenciumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for titulo_ocorrencia")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tituloOcorrenciumType, tituloOcorrenciumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for titulo_ocorrencia")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for titulo_ocorrencia")
	}

CacheNoHooks:
	if !cached {
		tituloOcorrenciumUpsertCacheMut.Lock()
		tituloOcorrenciumUpsertCache[key] = cache
		tituloOcorrenciumUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TituloOcorrencium record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TituloOcorrencium) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TituloOcorrencium provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tituloOcorrenciumPrimaryKeyMapping)
	sql := "DELETE FROM `titulo_ocorrencia` WHERE `tit_id`=? AND `tito_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from titulo_ocorrencia")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for titulo_ocorrencia")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tituloOcorrenciumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tituloOcorrenciumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from titulo_ocorrencia")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for titulo_ocorrencia")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TituloOcorrenciumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tituloOcorrenciumBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloOcorrenciumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `titulo_ocorrencia` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloOcorrenciumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tituloOcorrencium slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for titulo_ocorrencia")
	}

	if len(tituloOcorrenciumAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TituloOcorrencium) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTituloOcorrencium(ctx, exec, o.TitID, o.TitoItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TituloOcorrenciumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TituloOcorrenciumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloOcorrenciumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `titulo_ocorrencia`.* FROM `titulo_ocorrencia` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloOcorrenciumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TituloOcorrenciumSlice")
	}

	*o = slice

	return nil
}

// TituloOcorrenciumExists checks if the TituloOcorrencium row exists.
func TituloOcorrenciumExists(ctx context.Context, exec boil.ContextExecutor, titID int, titoItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `titulo_ocorrencia` where `tit_id`=? AND `tito_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, titID, titoItem)
	}
	row := exec.QueryRowContext(ctx, sql, titID, titoItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if titulo_ocorrencia exists")
	}

	return exists, nil
}

// Exists checks if the TituloOcorrencium row exists.
func (o *TituloOcorrencium) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TituloOcorrenciumExists(ctx, exec, o.TitID, o.TitoItem)
}
