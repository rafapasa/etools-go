// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// TituloBaixas is an object representing the database table.
type TituloBaixas struct {
	TitID            int           `boil:"tit_id" json:"tit_id" toml:"tit_id" yaml:"tit_id"`
	TBXItem          int           `boil:"tbx_item" json:"tbx_item" toml:"tbx_item" yaml:"tbx_item"`
	RocID            null.Int      `boil:"roc_id" json:"roc_id,omitempty" toml:"roc_id" yaml:"roc_id,omitempty"`
	TBXDatapagto     time.Time     `boil:"tbx_datapagto" json:"tbx_datapagto" toml:"tbx_datapagto" yaml:"tbx_datapagto"`
	TBXDataliberacao null.Time     `boil:"tbx_dataliberacao" json:"tbx_dataliberacao,omitempty" toml:"tbx_dataliberacao" yaml:"tbx_dataliberacao,omitempty"`
	TBXValorjuros    types.Decimal `boil:"tbx_valorjuros" json:"tbx_valorjuros" toml:"tbx_valorjuros" yaml:"tbx_valorjuros"`
	TBXValordesconto types.Decimal `boil:"tbx_valordesconto" json:"tbx_valordesconto" toml:"tbx_valordesconto" yaml:"tbx_valordesconto"`
	TBXValorpago     types.Decimal `boil:"tbx_valorpago" json:"tbx_valorpago" toml:"tbx_valorpago" yaml:"tbx_valorpago"`
	TBXObservacao    null.String   `boil:"tbx_observacao" json:"tbx_observacao,omitempty" toml:"tbx_observacao" yaml:"tbx_observacao,omitempty"`
	FrmpgtoID        int           `boil:"frmpgto_id" json:"frmpgto_id" toml:"frmpgto_id" yaml:"frmpgto_id"`
	DespID           null.Int      `boil:"desp_id" json:"desp_id,omitempty" toml:"desp_id" yaml:"desp_id,omitempty"`
	RecID            null.Int      `boil:"rec_id" json:"rec_id,omitempty" toml:"rec_id" yaml:"rec_id,omitempty"`

	R *tituloBaixasR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tituloBaixasL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TituloBaixasColumns = struct {
	TitID            string
	TBXItem          string
	RocID            string
	TBXDatapagto     string
	TBXDataliberacao string
	TBXValorjuros    string
	TBXValordesconto string
	TBXValorpago     string
	TBXObservacao    string
	FrmpgtoID        string
	DespID           string
	RecID            string
}{
	TitID:            "tit_id",
	TBXItem:          "tbx_item",
	RocID:            "roc_id",
	TBXDatapagto:     "tbx_datapagto",
	TBXDataliberacao: "tbx_dataliberacao",
	TBXValorjuros:    "tbx_valorjuros",
	TBXValordesconto: "tbx_valordesconto",
	TBXValorpago:     "tbx_valorpago",
	TBXObservacao:    "tbx_observacao",
	FrmpgtoID:        "frmpgto_id",
	DespID:           "desp_id",
	RecID:            "rec_id",
}

var TituloBaixasTableColumns = struct {
	TitID            string
	TBXItem          string
	RocID            string
	TBXDatapagto     string
	TBXDataliberacao string
	TBXValorjuros    string
	TBXValordesconto string
	TBXValorpago     string
	TBXObservacao    string
	FrmpgtoID        string
	DespID           string
	RecID            string
}{
	TitID:            "titulo_baixas.tit_id",
	TBXItem:          "titulo_baixas.tbx_item",
	RocID:            "titulo_baixas.roc_id",
	TBXDatapagto:     "titulo_baixas.tbx_datapagto",
	TBXDataliberacao: "titulo_baixas.tbx_dataliberacao",
	TBXValorjuros:    "titulo_baixas.tbx_valorjuros",
	TBXValordesconto: "titulo_baixas.tbx_valordesconto",
	TBXValorpago:     "titulo_baixas.tbx_valorpago",
	TBXObservacao:    "titulo_baixas.tbx_observacao",
	FrmpgtoID:        "titulo_baixas.frmpgto_id",
	DespID:           "titulo_baixas.desp_id",
	RecID:            "titulo_baixas.rec_id",
}

// Generated where

var TituloBaixasWhere = struct {
	TitID            whereHelperint
	TBXItem          whereHelperint
	RocID            whereHelpernull_Int
	TBXDatapagto     whereHelpertime_Time
	TBXDataliberacao whereHelpernull_Time
	TBXValorjuros    whereHelpertypes_Decimal
	TBXValordesconto whereHelpertypes_Decimal
	TBXValorpago     whereHelpertypes_Decimal
	TBXObservacao    whereHelpernull_String
	FrmpgtoID        whereHelperint
	DespID           whereHelpernull_Int
	RecID            whereHelpernull_Int
}{
	TitID:            whereHelperint{field: "`titulo_baixas`.`tit_id`"},
	TBXItem:          whereHelperint{field: "`titulo_baixas`.`tbx_item`"},
	RocID:            whereHelpernull_Int{field: "`titulo_baixas`.`roc_id`"},
	TBXDatapagto:     whereHelpertime_Time{field: "`titulo_baixas`.`tbx_datapagto`"},
	TBXDataliberacao: whereHelpernull_Time{field: "`titulo_baixas`.`tbx_dataliberacao`"},
	TBXValorjuros:    whereHelpertypes_Decimal{field: "`titulo_baixas`.`tbx_valorjuros`"},
	TBXValordesconto: whereHelpertypes_Decimal{field: "`titulo_baixas`.`tbx_valordesconto`"},
	TBXValorpago:     whereHelpertypes_Decimal{field: "`titulo_baixas`.`tbx_valorpago`"},
	TBXObservacao:    whereHelpernull_String{field: "`titulo_baixas`.`tbx_observacao`"},
	FrmpgtoID:        whereHelperint{field: "`titulo_baixas`.`frmpgto_id`"},
	DespID:           whereHelpernull_Int{field: "`titulo_baixas`.`desp_id`"},
	RecID:            whereHelpernull_Int{field: "`titulo_baixas`.`rec_id`"},
}

// TituloBaixasRels is where relationship names are stored.
var TituloBaixasRels = struct {
	Desp    string
	Rec     string
	Frmpgto string
	Roc     string
	Tit     string
}{
	Desp:    "Desp",
	Rec:     "Rec",
	Frmpgto: "Frmpgto",
	Roc:     "Roc",
	Tit:     "Tit",
}

// tituloBaixasR is where relationships are stored.
type tituloBaixasR struct {
	Desp    *Despesas       `boil:"Desp" json:"Desp" toml:"Desp" yaml:"Desp"`
	Rec     *Receitas       `boil:"Rec" json:"Rec" toml:"Rec" yaml:"Rec"`
	Frmpgto *FormaPagamento `boil:"Frmpgto" json:"Frmpgto" toml:"Frmpgto" yaml:"Frmpgto"`
	Roc     *RotinaContabil `boil:"Roc" json:"Roc" toml:"Roc" yaml:"Roc"`
	Tit     *Titulo         `boil:"Tit" json:"Tit" toml:"Tit" yaml:"Tit"`
}

// NewStruct creates a new relationship struct
func (*tituloBaixasR) NewStruct() *tituloBaixasR {
	return &tituloBaixasR{}
}

func (r *tituloBaixasR) GetDesp() *Despesas {
	if r == nil {
		return nil
	}
	return r.Desp
}

func (r *tituloBaixasR) GetRec() *Receitas {
	if r == nil {
		return nil
	}
	return r.Rec
}

func (r *tituloBaixasR) GetFrmpgto() *FormaPagamento {
	if r == nil {
		return nil
	}
	return r.Frmpgto
}

func (r *tituloBaixasR) GetRoc() *RotinaContabil {
	if r == nil {
		return nil
	}
	return r.Roc
}

func (r *tituloBaixasR) GetTit() *Titulo {
	if r == nil {
		return nil
	}
	return r.Tit
}

// tituloBaixasL is where Load methods for each relationship are stored.
type tituloBaixasL struct{}

var (
	tituloBaixasAllColumns            = []string{"tit_id", "tbx_item", "roc_id", "tbx_datapagto", "tbx_dataliberacao", "tbx_valorjuros", "tbx_valordesconto", "tbx_valorpago", "tbx_observacao", "frmpgto_id", "desp_id", "rec_id"}
	tituloBaixasColumnsWithoutDefault = []string{"tit_id", "tbx_item", "roc_id", "tbx_datapagto", "tbx_dataliberacao", "tbx_valorjuros", "tbx_valordesconto", "tbx_valorpago", "tbx_observacao", "desp_id", "rec_id"}
	tituloBaixasColumnsWithDefault    = []string{"frmpgto_id"}
	tituloBaixasPrimaryKeyColumns     = []string{"tit_id", "tbx_item"}
	tituloBaixasGeneratedColumns      = []string{}
)

type (
	// TituloBaixasSlice is an alias for a slice of pointers to TituloBaixas.
	// This should almost always be used instead of []TituloBaixas.
	TituloBaixasSlice []*TituloBaixas
	// TituloBaixasHook is the signature for custom TituloBaixas hook methods
	TituloBaixasHook func(context.Context, boil.ContextExecutor, *TituloBaixas) error

	tituloBaixasQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tituloBaixasType                 = reflect.TypeOf(&TituloBaixas{})
	tituloBaixasMapping              = queries.MakeStructMapping(tituloBaixasType)
	tituloBaixasPrimaryKeyMapping, _ = queries.BindMapping(tituloBaixasType, tituloBaixasMapping, tituloBaixasPrimaryKeyColumns)
	tituloBaixasInsertCacheMut       sync.RWMutex
	tituloBaixasInsertCache          = make(map[string]insertCache)
	tituloBaixasUpdateCacheMut       sync.RWMutex
	tituloBaixasUpdateCache          = make(map[string]updateCache)
	tituloBaixasUpsertCacheMut       sync.RWMutex
	tituloBaixasUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tituloBaixasAfterSelectMu sync.Mutex
var tituloBaixasAfterSelectHooks []TituloBaixasHook

var tituloBaixasBeforeInsertMu sync.Mutex
var tituloBaixasBeforeInsertHooks []TituloBaixasHook
var tituloBaixasAfterInsertMu sync.Mutex
var tituloBaixasAfterInsertHooks []TituloBaixasHook

var tituloBaixasBeforeUpdateMu sync.Mutex
var tituloBaixasBeforeUpdateHooks []TituloBaixasHook
var tituloBaixasAfterUpdateMu sync.Mutex
var tituloBaixasAfterUpdateHooks []TituloBaixasHook

var tituloBaixasBeforeDeleteMu sync.Mutex
var tituloBaixasBeforeDeleteHooks []TituloBaixasHook
var tituloBaixasAfterDeleteMu sync.Mutex
var tituloBaixasAfterDeleteHooks []TituloBaixasHook

var tituloBaixasBeforeUpsertMu sync.Mutex
var tituloBaixasBeforeUpsertHooks []TituloBaixasHook
var tituloBaixasAfterUpsertMu sync.Mutex
var tituloBaixasAfterUpsertHooks []TituloBaixasHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TituloBaixas) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TituloBaixas) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TituloBaixas) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TituloBaixas) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TituloBaixas) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TituloBaixas) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TituloBaixas) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TituloBaixas) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TituloBaixas) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tituloBaixasAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTituloBaixasHook registers your hook function for all future operations.
func AddTituloBaixasHook(hookPoint boil.HookPoint, tituloBaixasHook TituloBaixasHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tituloBaixasAfterSelectMu.Lock()
		tituloBaixasAfterSelectHooks = append(tituloBaixasAfterSelectHooks, tituloBaixasHook)
		tituloBaixasAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tituloBaixasBeforeInsertMu.Lock()
		tituloBaixasBeforeInsertHooks = append(tituloBaixasBeforeInsertHooks, tituloBaixasHook)
		tituloBaixasBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tituloBaixasAfterInsertMu.Lock()
		tituloBaixasAfterInsertHooks = append(tituloBaixasAfterInsertHooks, tituloBaixasHook)
		tituloBaixasAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tituloBaixasBeforeUpdateMu.Lock()
		tituloBaixasBeforeUpdateHooks = append(tituloBaixasBeforeUpdateHooks, tituloBaixasHook)
		tituloBaixasBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tituloBaixasAfterUpdateMu.Lock()
		tituloBaixasAfterUpdateHooks = append(tituloBaixasAfterUpdateHooks, tituloBaixasHook)
		tituloBaixasAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tituloBaixasBeforeDeleteMu.Lock()
		tituloBaixasBeforeDeleteHooks = append(tituloBaixasBeforeDeleteHooks, tituloBaixasHook)
		tituloBaixasBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tituloBaixasAfterDeleteMu.Lock()
		tituloBaixasAfterDeleteHooks = append(tituloBaixasAfterDeleteHooks, tituloBaixasHook)
		tituloBaixasAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tituloBaixasBeforeUpsertMu.Lock()
		tituloBaixasBeforeUpsertHooks = append(tituloBaixasBeforeUpsertHooks, tituloBaixasHook)
		tituloBaixasBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tituloBaixasAfterUpsertMu.Lock()
		tituloBaixasAfterUpsertHooks = append(tituloBaixasAfterUpsertHooks, tituloBaixasHook)
		tituloBaixasAfterUpsertMu.Unlock()
	}
}

// One returns a single tituloBaixas record from the query.
func (q tituloBaixasQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TituloBaixas, error) {
	o := &TituloBaixas{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for titulo_baixas")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TituloBaixas records from the query.
func (q tituloBaixasQuery) All(ctx context.Context, exec boil.ContextExecutor) (TituloBaixasSlice, error) {
	var o []*TituloBaixas

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TituloBaixas slice")
	}

	if len(tituloBaixasAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TituloBaixas records in the query.
func (q tituloBaixasQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count titulo_baixas rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tituloBaixasQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if titulo_baixas exists")
	}

	return count > 0, nil
}

// Desp pointed to by the foreign key.
func (o *TituloBaixas) Desp(mods ...qm.QueryMod) despesasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`desp_id` = ?", o.DespID),
	}

	queryMods = append(queryMods, mods...)

	return Despesas(queryMods...)
}

// Rec pointed to by the foreign key.
func (o *TituloBaixas) Rec(mods ...qm.QueryMod) receitasQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`rec_id` = ?", o.RecID),
	}

	queryMods = append(queryMods, mods...)

	return Receitas(queryMods...)
}

// Frmpgto pointed to by the foreign key.
func (o *TituloBaixas) Frmpgto(mods ...qm.QueryMod) formaPagamentoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`frmpgto_id` = ?", o.FrmpgtoID),
	}

	queryMods = append(queryMods, mods...)

	return FormaPagamentos(queryMods...)
}

// Roc pointed to by the foreign key.
func (o *TituloBaixas) Roc(mods ...qm.QueryMod) rotinaContabilQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`roc_id` = ?", o.RocID),
	}

	queryMods = append(queryMods, mods...)

	return RotinaContabils(queryMods...)
}

// Tit pointed to by the foreign key.
func (o *TituloBaixas) Tit(mods ...qm.QueryMod) tituloQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`tit_id` = ?", o.TitID),
	}

	queryMods = append(queryMods, mods...)

	return Titulos(queryMods...)
}

// LoadDesp allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloBaixasL) LoadDesp(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloBaixas interface{}, mods queries.Applicator) error {
	var slice []*TituloBaixas
	var object *TituloBaixas

	if singular {
		var ok bool
		object, ok = maybeTituloBaixas.(*TituloBaixas)
		if !ok {
			object = new(TituloBaixas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloBaixas))
			}
		}
	} else {
		s, ok := maybeTituloBaixas.(*[]*TituloBaixas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloBaixas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloBaixasR{}
		}
		if !queries.IsNil(object.DespID) {
			args[object.DespID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloBaixasR{}
			}

			if !queries.IsNil(obj.DespID) {
				args[obj.DespID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`despesas`),
		qm.WhereIn(`despesas.desp_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Despesas")
	}

	var resultSlice []*Despesas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Despesas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for despesas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for despesas")
	}

	if len(despesasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Desp = foreign
		if foreign.R == nil {
			foreign.R = &despesasR{}
		}
		foreign.R.DespTituloBaixas = append(foreign.R.DespTituloBaixas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.DespID, foreign.DespID) {
				local.R.Desp = foreign
				if foreign.R == nil {
					foreign.R = &despesasR{}
				}
				foreign.R.DespTituloBaixas = append(foreign.R.DespTituloBaixas, local)
				break
			}
		}
	}

	return nil
}

// LoadRec allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloBaixasL) LoadRec(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloBaixas interface{}, mods queries.Applicator) error {
	var slice []*TituloBaixas
	var object *TituloBaixas

	if singular {
		var ok bool
		object, ok = maybeTituloBaixas.(*TituloBaixas)
		if !ok {
			object = new(TituloBaixas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloBaixas))
			}
		}
	} else {
		s, ok := maybeTituloBaixas.(*[]*TituloBaixas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloBaixas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloBaixasR{}
		}
		if !queries.IsNil(object.RecID) {
			args[object.RecID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloBaixasR{}
			}

			if !queries.IsNil(obj.RecID) {
				args[obj.RecID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`receitas`),
		qm.WhereIn(`receitas.rec_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Receitas")
	}

	var resultSlice []*Receitas
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Receitas")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for receitas")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for receitas")
	}

	if len(receitasAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Rec = foreign
		if foreign.R == nil {
			foreign.R = &receitasR{}
		}
		foreign.R.RecTituloBaixas = append(foreign.R.RecTituloBaixas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RecID, foreign.RecID) {
				local.R.Rec = foreign
				if foreign.R == nil {
					foreign.R = &receitasR{}
				}
				foreign.R.RecTituloBaixas = append(foreign.R.RecTituloBaixas, local)
				break
			}
		}
	}

	return nil
}

// LoadFrmpgto allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloBaixasL) LoadFrmpgto(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloBaixas interface{}, mods queries.Applicator) error {
	var slice []*TituloBaixas
	var object *TituloBaixas

	if singular {
		var ok bool
		object, ok = maybeTituloBaixas.(*TituloBaixas)
		if !ok {
			object = new(TituloBaixas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloBaixas))
			}
		}
	} else {
		s, ok := maybeTituloBaixas.(*[]*TituloBaixas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloBaixas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloBaixasR{}
		}
		args[object.FrmpgtoID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloBaixasR{}
			}

			args[obj.FrmpgtoID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`forma_pagamento`),
		qm.WhereIn(`forma_pagamento.frmpgto_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load FormaPagamento")
	}

	var resultSlice []*FormaPagamento
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice FormaPagamento")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for forma_pagamento")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for forma_pagamento")
	}

	if len(formaPagamentoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Frmpgto = foreign
		if foreign.R == nil {
			foreign.R = &formaPagamentoR{}
		}
		foreign.R.FrmpgtoTituloBaixas = append(foreign.R.FrmpgtoTituloBaixas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.FrmpgtoID == foreign.FrmpgtoID {
				local.R.Frmpgto = foreign
				if foreign.R == nil {
					foreign.R = &formaPagamentoR{}
				}
				foreign.R.FrmpgtoTituloBaixas = append(foreign.R.FrmpgtoTituloBaixas, local)
				break
			}
		}
	}

	return nil
}

// LoadRoc allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloBaixasL) LoadRoc(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloBaixas interface{}, mods queries.Applicator) error {
	var slice []*TituloBaixas
	var object *TituloBaixas

	if singular {
		var ok bool
		object, ok = maybeTituloBaixas.(*TituloBaixas)
		if !ok {
			object = new(TituloBaixas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloBaixas))
			}
		}
	} else {
		s, ok := maybeTituloBaixas.(*[]*TituloBaixas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloBaixas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloBaixasR{}
		}
		if !queries.IsNil(object.RocID) {
			args[object.RocID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloBaixasR{}
			}

			if !queries.IsNil(obj.RocID) {
				args[obj.RocID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil`),
		qm.WhereIn(`rotina_contabil.roc_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RotinaContabil")
	}

	var resultSlice []*RotinaContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RotinaContabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for rotina_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil")
	}

	if len(rotinaContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Roc = foreign
		if foreign.R == nil {
			foreign.R = &rotinaContabilR{}
		}
		foreign.R.RocTituloBaixas = append(foreign.R.RocTituloBaixas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RocID, foreign.RocID) {
				local.R.Roc = foreign
				if foreign.R == nil {
					foreign.R = &rotinaContabilR{}
				}
				foreign.R.RocTituloBaixas = append(foreign.R.RocTituloBaixas, local)
				break
			}
		}
	}

	return nil
}

// LoadTit allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tituloBaixasL) LoadTit(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTituloBaixas interface{}, mods queries.Applicator) error {
	var slice []*TituloBaixas
	var object *TituloBaixas

	if singular {
		var ok bool
		object, ok = maybeTituloBaixas.(*TituloBaixas)
		if !ok {
			object = new(TituloBaixas)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTituloBaixas))
			}
		}
	} else {
		s, ok := maybeTituloBaixas.(*[]*TituloBaixas)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTituloBaixas)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTituloBaixas))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tituloBaixasR{}
		}
		args[object.TitID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tituloBaixasR{}
			}

			args[obj.TitID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`titulos`),
		qm.WhereIn(`titulos.tit_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Titulo")
	}

	var resultSlice []*Titulo
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Titulo")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for titulos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for titulos")
	}

	if len(tituloAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tit = foreign
		if foreign.R == nil {
			foreign.R = &tituloR{}
		}
		foreign.R.TitTituloBaixas = append(foreign.R.TitTituloBaixas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TitID == foreign.TitID {
				local.R.Tit = foreign
				if foreign.R == nil {
					foreign.R = &tituloR{}
				}
				foreign.R.TitTituloBaixas = append(foreign.R.TitTituloBaixas, local)
				break
			}
		}
	}

	return nil
}

// SetDesp of the tituloBaixas to the related item.
// Sets o.R.Desp to related.
// Adds o to related.R.DespTituloBaixas.
func (o *TituloBaixas) SetDesp(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Despesas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_baixas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"desp_id"}),
		strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns),
	)
	values := []interface{}{related.DespID, o.TitID, o.TBXItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.DespID, related.DespID)
	if o.R == nil {
		o.R = &tituloBaixasR{
			Desp: related,
		}
	} else {
		o.R.Desp = related
	}

	if related.R == nil {
		related.R = &despesasR{
			DespTituloBaixas: TituloBaixasSlice{o},
		}
	} else {
		related.R.DespTituloBaixas = append(related.R.DespTituloBaixas, o)
	}

	return nil
}

// RemoveDesp relationship.
// Sets o.R.Desp to nil.
// Removes o from all passed in related items' relationships struct.
func (o *TituloBaixas) RemoveDesp(ctx context.Context, exec boil.ContextExecutor, related *Despesas) error {
	var err error

	queries.SetScanner(&o.DespID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("desp_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Desp = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.DespTituloBaixas {
		if queries.Equal(o.DespID, ri.DespID) {
			continue
		}

		ln := len(related.R.DespTituloBaixas)
		if ln > 1 && i < ln-1 {
			related.R.DespTituloBaixas[i] = related.R.DespTituloBaixas[ln-1]
		}
		related.R.DespTituloBaixas = related.R.DespTituloBaixas[:ln-1]
		break
	}
	return nil
}

// SetRec of the tituloBaixas to the related item.
// Sets o.R.Rec to related.
// Adds o to related.R.RecTituloBaixas.
func (o *TituloBaixas) SetRec(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Receitas) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_baixas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"rec_id"}),
		strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns),
	)
	values := []interface{}{related.RecID, o.TitID, o.TBXItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RecID, related.RecID)
	if o.R == nil {
		o.R = &tituloBaixasR{
			Rec: related,
		}
	} else {
		o.R.Rec = related
	}

	if related.R == nil {
		related.R = &receitasR{
			RecTituloBaixas: TituloBaixasSlice{o},
		}
	} else {
		related.R.RecTituloBaixas = append(related.R.RecTituloBaixas, o)
	}

	return nil
}

// RemoveRec relationship.
// Sets o.R.Rec to nil.
// Removes o from all passed in related items' relationships struct.
func (o *TituloBaixas) RemoveRec(ctx context.Context, exec boil.ContextExecutor, related *Receitas) error {
	var err error

	queries.SetScanner(&o.RecID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("rec_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Rec = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RecTituloBaixas {
		if queries.Equal(o.RecID, ri.RecID) {
			continue
		}

		ln := len(related.R.RecTituloBaixas)
		if ln > 1 && i < ln-1 {
			related.R.RecTituloBaixas[i] = related.R.RecTituloBaixas[ln-1]
		}
		related.R.RecTituloBaixas = related.R.RecTituloBaixas[:ln-1]
		break
	}
	return nil
}

// SetFrmpgto of the tituloBaixas to the related item.
// Sets o.R.Frmpgto to related.
// Adds o to related.R.FrmpgtoTituloBaixas.
func (o *TituloBaixas) SetFrmpgto(ctx context.Context, exec boil.ContextExecutor, insert bool, related *FormaPagamento) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_baixas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"frmpgto_id"}),
		strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns),
	)
	values := []interface{}{related.FrmpgtoID, o.TitID, o.TBXItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.FrmpgtoID = related.FrmpgtoID
	if o.R == nil {
		o.R = &tituloBaixasR{
			Frmpgto: related,
		}
	} else {
		o.R.Frmpgto = related
	}

	if related.R == nil {
		related.R = &formaPagamentoR{
			FrmpgtoTituloBaixas: TituloBaixasSlice{o},
		}
	} else {
		related.R.FrmpgtoTituloBaixas = append(related.R.FrmpgtoTituloBaixas, o)
	}

	return nil
}

// SetRoc of the tituloBaixas to the related item.
// Sets o.R.Roc to related.
// Adds o to related.R.RocTituloBaixas.
func (o *TituloBaixas) SetRoc(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RotinaContabil) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_baixas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"roc_id"}),
		strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns),
	)
	values := []interface{}{related.RocID, o.TitID, o.TBXItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RocID, related.RocID)
	if o.R == nil {
		o.R = &tituloBaixasR{
			Roc: related,
		}
	} else {
		o.R.Roc = related
	}

	if related.R == nil {
		related.R = &rotinaContabilR{
			RocTituloBaixas: TituloBaixasSlice{o},
		}
	} else {
		related.R.RocTituloBaixas = append(related.R.RocTituloBaixas, o)
	}

	return nil
}

// RemoveRoc relationship.
// Sets o.R.Roc to nil.
// Removes o from all passed in related items' relationships struct.
func (o *TituloBaixas) RemoveRoc(ctx context.Context, exec boil.ContextExecutor, related *RotinaContabil) error {
	var err error

	queries.SetScanner(&o.RocID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("roc_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Roc = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RocTituloBaixas {
		if queries.Equal(o.RocID, ri.RocID) {
			continue
		}

		ln := len(related.R.RocTituloBaixas)
		if ln > 1 && i < ln-1 {
			related.R.RocTituloBaixas[i] = related.R.RocTituloBaixas[ln-1]
		}
		related.R.RocTituloBaixas = related.R.RocTituloBaixas[:ln-1]
		break
	}
	return nil
}

// SetTit of the tituloBaixas to the related item.
// Sets o.R.Tit to related.
// Adds o to related.R.TitTituloBaixas.
func (o *TituloBaixas) SetTit(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Titulo) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `titulo_baixas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"tit_id"}),
		strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns),
	)
	values := []interface{}{related.TitID, o.TitID, o.TBXItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TitID = related.TitID
	if o.R == nil {
		o.R = &tituloBaixasR{
			Tit: related,
		}
	} else {
		o.R.Tit = related
	}

	if related.R == nil {
		related.R = &tituloR{
			TitTituloBaixas: TituloBaixasSlice{o},
		}
	} else {
		related.R.TitTituloBaixas = append(related.R.TitTituloBaixas, o)
	}

	return nil
}

// TituloBaixas retrieves all the records using an executor.
func TituloBaixas(mods ...qm.QueryMod) tituloBaixasQuery {
	mods = append(mods, qm.From("`titulo_baixas`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`titulo_baixas`.*"})
	}

	return tituloBaixasQuery{q}
}

// FindTituloBaixas retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTituloBaixas(ctx context.Context, exec boil.ContextExecutor, titID int, tBXItem int, selectCols ...string) (*TituloBaixas, error) {
	tituloBaixasObj := &TituloBaixas{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `titulo_baixas` where `tit_id`=? AND `tbx_item`=?", sel,
	)

	q := queries.Raw(query, titID, tBXItem)

	err := q.Bind(ctx, exec, tituloBaixasObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from titulo_baixas")
	}

	if err = tituloBaixasObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tituloBaixasObj, err
	}

	return tituloBaixasObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TituloBaixas) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no titulo_baixas provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tituloBaixasColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tituloBaixasInsertCacheMut.RLock()
	cache, cached := tituloBaixasInsertCache[key]
	tituloBaixasInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tituloBaixasAllColumns,
			tituloBaixasColumnsWithDefault,
			tituloBaixasColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tituloBaixasType, tituloBaixasMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tituloBaixasType, tituloBaixasMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `titulo_baixas` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `titulo_baixas` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `titulo_baixas` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into titulo_baixas")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TitID,
		o.TBXItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for titulo_baixas")
	}

CacheNoHooks:
	if !cached {
		tituloBaixasInsertCacheMut.Lock()
		tituloBaixasInsertCache[key] = cache
		tituloBaixasInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TituloBaixas.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TituloBaixas) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tituloBaixasUpdateCacheMut.RLock()
	cache, cached := tituloBaixasUpdateCache[key]
	tituloBaixasUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tituloBaixasAllColumns,
			tituloBaixasPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update titulo_baixas, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `titulo_baixas` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, tituloBaixasPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tituloBaixasType, tituloBaixasMapping, append(wl, tituloBaixasPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update titulo_baixas row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for titulo_baixas")
	}

	if !cached {
		tituloBaixasUpdateCacheMut.Lock()
		tituloBaixasUpdateCache[key] = cache
		tituloBaixasUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tituloBaixasQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for titulo_baixas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for titulo_baixas")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TituloBaixasSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloBaixasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `titulo_baixas` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloBaixasPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tituloBaixas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tituloBaixas")
	}
	return rowsAff, nil
}

var mySQLTituloBaixasUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TituloBaixas) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no titulo_baixas provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tituloBaixasColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTituloBaixasUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tituloBaixasUpsertCacheMut.RLock()
	cache, cached := tituloBaixasUpsertCache[key]
	tituloBaixasUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tituloBaixasAllColumns,
			tituloBaixasColumnsWithDefault,
			tituloBaixasColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tituloBaixasAllColumns,
			tituloBaixasPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert titulo_baixas, could not build update column list")
		}

		ret := strmangle.SetComplement(tituloBaixasAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`titulo_baixas`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `titulo_baixas` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(tituloBaixasType, tituloBaixasMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tituloBaixasType, tituloBaixasMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for titulo_baixas")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(tituloBaixasType, tituloBaixasMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for titulo_baixas")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for titulo_baixas")
	}

CacheNoHooks:
	if !cached {
		tituloBaixasUpsertCacheMut.Lock()
		tituloBaixasUpsertCache[key] = cache
		tituloBaixasUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TituloBaixas record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TituloBaixas) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TituloBaixas provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tituloBaixasPrimaryKeyMapping)
	sql := "DELETE FROM `titulo_baixas` WHERE `tit_id`=? AND `tbx_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from titulo_baixas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for titulo_baixas")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tituloBaixasQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tituloBaixasQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from titulo_baixas")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for titulo_baixas")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TituloBaixasSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tituloBaixasBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloBaixasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `titulo_baixas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloBaixasPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tituloBaixas slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for titulo_baixas")
	}

	if len(tituloBaixasAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TituloBaixas) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTituloBaixas(ctx, exec, o.TitID, o.TBXItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TituloBaixasSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TituloBaixasSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tituloBaixasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `titulo_baixas`.* FROM `titulo_baixas` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, tituloBaixasPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TituloBaixasSlice")
	}

	*o = slice

	return nil
}

// TituloBaixasExists checks if the TituloBaixas row exists.
func TituloBaixasExists(ctx context.Context, exec boil.ContextExecutor, titID int, tBXItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `titulo_baixas` where `tit_id`=? AND `tbx_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, titID, tBXItem)
	}
	row := exec.QueryRowContext(ctx, sql, titID, tBXItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if titulo_baixas exists")
	}

	return exists, nil
}

// Exists checks if the TituloBaixas row exists.
func (o *TituloBaixas) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TituloBaixasExists(ctx, exec, o.TitID, o.TBXItem)
}
