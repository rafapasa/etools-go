// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// DocumentoVendaItemFicha is an object representing the database table.
type DocumentoVendaItemFicha struct {
	DDVID               int           `boil:"ddv_id" json:"ddv_id" toml:"ddv_id" yaml:"ddv_id"`
	DviItem             int           `boil:"dvi_item" json:"dvi_item" toml:"dvi_item" yaml:"dvi_item"`
	DvifItem            int           `boil:"dvif_item" json:"dvif_item" toml:"dvif_item" yaml:"dvif_item"`
	ProID               int           `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	DvifQuantsolicitada types.Decimal `boil:"dvif_quantsolicitada" json:"dvif_quantsolicitada" toml:"dvif_quantsolicitada" yaml:"dvif_quantsolicitada"`
	DvifQuantenviada    types.Decimal `boil:"dvif_quantenviada" json:"dvif_quantenviada" toml:"dvif_quantenviada" yaml:"dvif_quantenviada"`
	DvifValorcusto      types.Decimal `boil:"dvif_valorcusto" json:"dvif_valorcusto" toml:"dvif_valorcusto" yaml:"dvif_valorcusto"`

	R *documentoVendaItemFichaR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L documentoVendaItemFichaL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DocumentoVendaItemFichaColumns = struct {
	DDVID               string
	DviItem             string
	DvifItem            string
	ProID               string
	DvifQuantsolicitada string
	DvifQuantenviada    string
	DvifValorcusto      string
}{
	DDVID:               "ddv_id",
	DviItem:             "dvi_item",
	DvifItem:            "dvif_item",
	ProID:               "pro_id",
	DvifQuantsolicitada: "dvif_quantsolicitada",
	DvifQuantenviada:    "dvif_quantenviada",
	DvifValorcusto:      "dvif_valorcusto",
}

var DocumentoVendaItemFichaTableColumns = struct {
	DDVID               string
	DviItem             string
	DvifItem            string
	ProID               string
	DvifQuantsolicitada string
	DvifQuantenviada    string
	DvifValorcusto      string
}{
	DDVID:               "documento_venda_item_ficha.ddv_id",
	DviItem:             "documento_venda_item_ficha.dvi_item",
	DvifItem:            "documento_venda_item_ficha.dvif_item",
	ProID:               "documento_venda_item_ficha.pro_id",
	DvifQuantsolicitada: "documento_venda_item_ficha.dvif_quantsolicitada",
	DvifQuantenviada:    "documento_venda_item_ficha.dvif_quantenviada",
	DvifValorcusto:      "documento_venda_item_ficha.dvif_valorcusto",
}

// Generated where

var DocumentoVendaItemFichaWhere = struct {
	DDVID               whereHelperint
	DviItem             whereHelperint
	DvifItem            whereHelperint
	ProID               whereHelperint
	DvifQuantsolicitada whereHelpertypes_Decimal
	DvifQuantenviada    whereHelpertypes_Decimal
	DvifValorcusto      whereHelpertypes_Decimal
}{
	DDVID:               whereHelperint{field: "`documento_venda_item_ficha`.`ddv_id`"},
	DviItem:             whereHelperint{field: "`documento_venda_item_ficha`.`dvi_item`"},
	DvifItem:            whereHelperint{field: "`documento_venda_item_ficha`.`dvif_item`"},
	ProID:               whereHelperint{field: "`documento_venda_item_ficha`.`pro_id`"},
	DvifQuantsolicitada: whereHelpertypes_Decimal{field: "`documento_venda_item_ficha`.`dvif_quantsolicitada`"},
	DvifQuantenviada:    whereHelpertypes_Decimal{field: "`documento_venda_item_ficha`.`dvif_quantenviada`"},
	DvifValorcusto:      whereHelpertypes_Decimal{field: "`documento_venda_item_ficha`.`dvif_valorcusto`"},
}

// DocumentoVendaItemFichaRels is where relationship names are stored.
var DocumentoVendaItemFichaRels = struct {
	Pro string
}{
	Pro: "Pro",
}

// documentoVendaItemFichaR is where relationships are stored.
type documentoVendaItemFichaR struct {
	Pro *Produto `boil:"Pro" json:"Pro" toml:"Pro" yaml:"Pro"`
}

// NewStruct creates a new relationship struct
func (*documentoVendaItemFichaR) NewStruct() *documentoVendaItemFichaR {
	return &documentoVendaItemFichaR{}
}

func (r *documentoVendaItemFichaR) GetPro() *Produto {
	if r == nil {
		return nil
	}
	return r.Pro
}

// documentoVendaItemFichaL is where Load methods for each relationship are stored.
type documentoVendaItemFichaL struct{}

var (
	documentoVendaItemFichaAllColumns            = []string{"ddv_id", "dvi_item", "dvif_item", "pro_id", "dvif_quantsolicitada", "dvif_quantenviada", "dvif_valorcusto"}
	documentoVendaItemFichaColumnsWithoutDefault = []string{"ddv_id", "dvi_item", "dvif_item", "pro_id", "dvif_quantsolicitada", "dvif_quantenviada"}
	documentoVendaItemFichaColumnsWithDefault    = []string{"dvif_valorcusto"}
	documentoVendaItemFichaPrimaryKeyColumns     = []string{"ddv_id", "dvi_item", "dvif_item"}
	documentoVendaItemFichaGeneratedColumns      = []string{}
)

type (
	// DocumentoVendaItemFichaSlice is an alias for a slice of pointers to DocumentoVendaItemFicha.
	// This should almost always be used instead of []DocumentoVendaItemFicha.
	DocumentoVendaItemFichaSlice []*DocumentoVendaItemFicha
	// DocumentoVendaItemFichaHook is the signature for custom DocumentoVendaItemFicha hook methods
	DocumentoVendaItemFichaHook func(context.Context, boil.ContextExecutor, *DocumentoVendaItemFicha) error

	documentoVendaItemFichaQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	documentoVendaItemFichaType                 = reflect.TypeOf(&DocumentoVendaItemFicha{})
	documentoVendaItemFichaMapping              = queries.MakeStructMapping(documentoVendaItemFichaType)
	documentoVendaItemFichaPrimaryKeyMapping, _ = queries.BindMapping(documentoVendaItemFichaType, documentoVendaItemFichaMapping, documentoVendaItemFichaPrimaryKeyColumns)
	documentoVendaItemFichaInsertCacheMut       sync.RWMutex
	documentoVendaItemFichaInsertCache          = make(map[string]insertCache)
	documentoVendaItemFichaUpdateCacheMut       sync.RWMutex
	documentoVendaItemFichaUpdateCache          = make(map[string]updateCache)
	documentoVendaItemFichaUpsertCacheMut       sync.RWMutex
	documentoVendaItemFichaUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var documentoVendaItemFichaAfterSelectMu sync.Mutex
var documentoVendaItemFichaAfterSelectHooks []DocumentoVendaItemFichaHook

var documentoVendaItemFichaBeforeInsertMu sync.Mutex
var documentoVendaItemFichaBeforeInsertHooks []DocumentoVendaItemFichaHook
var documentoVendaItemFichaAfterInsertMu sync.Mutex
var documentoVendaItemFichaAfterInsertHooks []DocumentoVendaItemFichaHook

var documentoVendaItemFichaBeforeUpdateMu sync.Mutex
var documentoVendaItemFichaBeforeUpdateHooks []DocumentoVendaItemFichaHook
var documentoVendaItemFichaAfterUpdateMu sync.Mutex
var documentoVendaItemFichaAfterUpdateHooks []DocumentoVendaItemFichaHook

var documentoVendaItemFichaBeforeDeleteMu sync.Mutex
var documentoVendaItemFichaBeforeDeleteHooks []DocumentoVendaItemFichaHook
var documentoVendaItemFichaAfterDeleteMu sync.Mutex
var documentoVendaItemFichaAfterDeleteHooks []DocumentoVendaItemFichaHook

var documentoVendaItemFichaBeforeUpsertMu sync.Mutex
var documentoVendaItemFichaBeforeUpsertHooks []DocumentoVendaItemFichaHook
var documentoVendaItemFichaAfterUpsertMu sync.Mutex
var documentoVendaItemFichaAfterUpsertHooks []DocumentoVendaItemFichaHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DocumentoVendaItemFicha) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DocumentoVendaItemFicha) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DocumentoVendaItemFicha) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DocumentoVendaItemFicha) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DocumentoVendaItemFicha) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DocumentoVendaItemFicha) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DocumentoVendaItemFicha) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DocumentoVendaItemFicha) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DocumentoVendaItemFicha) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range documentoVendaItemFichaAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDocumentoVendaItemFichaHook registers your hook function for all future operations.
func AddDocumentoVendaItemFichaHook(hookPoint boil.HookPoint, documentoVendaItemFichaHook DocumentoVendaItemFichaHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		documentoVendaItemFichaAfterSelectMu.Lock()
		documentoVendaItemFichaAfterSelectHooks = append(documentoVendaItemFichaAfterSelectHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		documentoVendaItemFichaBeforeInsertMu.Lock()
		documentoVendaItemFichaBeforeInsertHooks = append(documentoVendaItemFichaBeforeInsertHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		documentoVendaItemFichaAfterInsertMu.Lock()
		documentoVendaItemFichaAfterInsertHooks = append(documentoVendaItemFichaAfterInsertHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		documentoVendaItemFichaBeforeUpdateMu.Lock()
		documentoVendaItemFichaBeforeUpdateHooks = append(documentoVendaItemFichaBeforeUpdateHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		documentoVendaItemFichaAfterUpdateMu.Lock()
		documentoVendaItemFichaAfterUpdateHooks = append(documentoVendaItemFichaAfterUpdateHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		documentoVendaItemFichaBeforeDeleteMu.Lock()
		documentoVendaItemFichaBeforeDeleteHooks = append(documentoVendaItemFichaBeforeDeleteHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		documentoVendaItemFichaAfterDeleteMu.Lock()
		documentoVendaItemFichaAfterDeleteHooks = append(documentoVendaItemFichaAfterDeleteHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		documentoVendaItemFichaBeforeUpsertMu.Lock()
		documentoVendaItemFichaBeforeUpsertHooks = append(documentoVendaItemFichaBeforeUpsertHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		documentoVendaItemFichaAfterUpsertMu.Lock()
		documentoVendaItemFichaAfterUpsertHooks = append(documentoVendaItemFichaAfterUpsertHooks, documentoVendaItemFichaHook)
		documentoVendaItemFichaAfterUpsertMu.Unlock()
	}
}

// One returns a single documentoVendaItemFicha record from the query.
func (q documentoVendaItemFichaQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DocumentoVendaItemFicha, error) {
	o := &DocumentoVendaItemFicha{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for documento_venda_item_ficha")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DocumentoVendaItemFicha records from the query.
func (q documentoVendaItemFichaQuery) All(ctx context.Context, exec boil.ContextExecutor) (DocumentoVendaItemFichaSlice, error) {
	var o []*DocumentoVendaItemFicha

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DocumentoVendaItemFicha slice")
	}

	if len(documentoVendaItemFichaAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DocumentoVendaItemFicha records in the query.
func (q documentoVendaItemFichaQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count documento_venda_item_ficha rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q documentoVendaItemFichaQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if documento_venda_item_ficha exists")
	}

	return count > 0, nil
}

// Pro pointed to by the foreign key.
func (o *DocumentoVendaItemFicha) Pro(mods ...qm.QueryMod) produtoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`pro_id` = ?", o.ProID),
	}

	queryMods = append(queryMods, mods...)

	return Produtos(queryMods...)
}

// LoadPro allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (documentoVendaItemFichaL) LoadPro(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDocumentoVendaItemFicha interface{}, mods queries.Applicator) error {
	var slice []*DocumentoVendaItemFicha
	var object *DocumentoVendaItemFicha

	if singular {
		var ok bool
		object, ok = maybeDocumentoVendaItemFicha.(*DocumentoVendaItemFicha)
		if !ok {
			object = new(DocumentoVendaItemFicha)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDocumentoVendaItemFicha)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDocumentoVendaItemFicha))
			}
		}
	} else {
		s, ok := maybeDocumentoVendaItemFicha.(*[]*DocumentoVendaItemFicha)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDocumentoVendaItemFicha)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDocumentoVendaItemFicha))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &documentoVendaItemFichaR{}
		}
		args[object.ProID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &documentoVendaItemFichaR{}
			}

			args[obj.ProID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto`),
		qm.WhereIn(`produto.pro_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Produto")
	}

	var resultSlice []*Produto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Produto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for produto")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto")
	}

	if len(produtoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pro = foreign
		if foreign.R == nil {
			foreign.R = &produtoR{}
		}
		foreign.R.ProDocumentoVendaItemFichas = append(foreign.R.ProDocumentoVendaItemFichas, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProID == foreign.ProID {
				local.R.Pro = foreign
				if foreign.R == nil {
					foreign.R = &produtoR{}
				}
				foreign.R.ProDocumentoVendaItemFichas = append(foreign.R.ProDocumentoVendaItemFichas, local)
				break
			}
		}
	}

	return nil
}

// SetPro of the documentoVendaItemFicha to the related item.
// Sets o.R.Pro to related.
// Adds o to related.R.ProDocumentoVendaItemFichas.
func (o *DocumentoVendaItemFicha) SetPro(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Produto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `documento_venda_item_ficha` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pro_id"}),
		strmangle.WhereClause("`", "`", 0, documentoVendaItemFichaPrimaryKeyColumns),
	)
	values := []interface{}{related.ProID, o.DDVID, o.DviItem, o.DvifItem}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProID = related.ProID
	if o.R == nil {
		o.R = &documentoVendaItemFichaR{
			Pro: related,
		}
	} else {
		o.R.Pro = related
	}

	if related.R == nil {
		related.R = &produtoR{
			ProDocumentoVendaItemFichas: DocumentoVendaItemFichaSlice{o},
		}
	} else {
		related.R.ProDocumentoVendaItemFichas = append(related.R.ProDocumentoVendaItemFichas, o)
	}

	return nil
}

// DocumentoVendaItemFichas retrieves all the records using an executor.
func DocumentoVendaItemFichas(mods ...qm.QueryMod) documentoVendaItemFichaQuery {
	mods = append(mods, qm.From("`documento_venda_item_ficha`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`documento_venda_item_ficha`.*"})
	}

	return documentoVendaItemFichaQuery{q}
}

// FindDocumentoVendaItemFicha retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDocumentoVendaItemFicha(ctx context.Context, exec boil.ContextExecutor, dDVID int, dviItem int, dvifItem int, selectCols ...string) (*DocumentoVendaItemFicha, error) {
	documentoVendaItemFichaObj := &DocumentoVendaItemFicha{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `documento_venda_item_ficha` where `ddv_id`=? AND `dvi_item`=? AND `dvif_item`=?", sel,
	)

	q := queries.Raw(query, dDVID, dviItem, dvifItem)

	err := q.Bind(ctx, exec, documentoVendaItemFichaObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from documento_venda_item_ficha")
	}

	if err = documentoVendaItemFichaObj.doAfterSelectHooks(ctx, exec); err != nil {
		return documentoVendaItemFichaObj, err
	}

	return documentoVendaItemFichaObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DocumentoVendaItemFicha) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_item_ficha provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaItemFichaColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	documentoVendaItemFichaInsertCacheMut.RLock()
	cache, cached := documentoVendaItemFichaInsertCache[key]
	documentoVendaItemFichaInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			documentoVendaItemFichaAllColumns,
			documentoVendaItemFichaColumnsWithDefault,
			documentoVendaItemFichaColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaItemFichaType, documentoVendaItemFichaMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(documentoVendaItemFichaType, documentoVendaItemFichaMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `documento_venda_item_ficha` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `documento_venda_item_ficha` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `documento_venda_item_ficha` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, documentoVendaItemFichaPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into documento_venda_item_ficha")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DDVID,
		o.DviItem,
		o.DvifItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_item_ficha")
	}

CacheNoHooks:
	if !cached {
		documentoVendaItemFichaInsertCacheMut.Lock()
		documentoVendaItemFichaInsertCache[key] = cache
		documentoVendaItemFichaInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DocumentoVendaItemFicha.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DocumentoVendaItemFicha) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	documentoVendaItemFichaUpdateCacheMut.RLock()
	cache, cached := documentoVendaItemFichaUpdateCache[key]
	documentoVendaItemFichaUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			documentoVendaItemFichaAllColumns,
			documentoVendaItemFichaPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update documento_venda_item_ficha, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `documento_venda_item_ficha` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, documentoVendaItemFichaPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(documentoVendaItemFichaType, documentoVendaItemFichaMapping, append(wl, documentoVendaItemFichaPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update documento_venda_item_ficha row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for documento_venda_item_ficha")
	}

	if !cached {
		documentoVendaItemFichaUpdateCacheMut.Lock()
		documentoVendaItemFichaUpdateCache[key] = cache
		documentoVendaItemFichaUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q documentoVendaItemFichaQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for documento_venda_item_ficha")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for documento_venda_item_ficha")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DocumentoVendaItemFichaSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemFichaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `documento_venda_item_ficha` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemFichaPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in documentoVendaItemFicha slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all documentoVendaItemFicha")
	}
	return rowsAff, nil
}

var mySQLDocumentoVendaItemFichaUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DocumentoVendaItemFicha) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no documento_venda_item_ficha provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(documentoVendaItemFichaColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDocumentoVendaItemFichaUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	documentoVendaItemFichaUpsertCacheMut.RLock()
	cache, cached := documentoVendaItemFichaUpsertCache[key]
	documentoVendaItemFichaUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			documentoVendaItemFichaAllColumns,
			documentoVendaItemFichaColumnsWithDefault,
			documentoVendaItemFichaColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			documentoVendaItemFichaAllColumns,
			documentoVendaItemFichaPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert documento_venda_item_ficha, could not build update column list")
		}

		ret := strmangle.SetComplement(documentoVendaItemFichaAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`documento_venda_item_ficha`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `documento_venda_item_ficha` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(documentoVendaItemFichaType, documentoVendaItemFichaMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(documentoVendaItemFichaType, documentoVendaItemFichaMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for documento_venda_item_ficha")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(documentoVendaItemFichaType, documentoVendaItemFichaMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for documento_venda_item_ficha")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for documento_venda_item_ficha")
	}

CacheNoHooks:
	if !cached {
		documentoVendaItemFichaUpsertCacheMut.Lock()
		documentoVendaItemFichaUpsertCache[key] = cache
		documentoVendaItemFichaUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DocumentoVendaItemFicha record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DocumentoVendaItemFicha) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DocumentoVendaItemFicha provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), documentoVendaItemFichaPrimaryKeyMapping)
	sql := "DELETE FROM `documento_venda_item_ficha` WHERE `ddv_id`=? AND `dvi_item`=? AND `dvif_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from documento_venda_item_ficha")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for documento_venda_item_ficha")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q documentoVendaItemFichaQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no documentoVendaItemFichaQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documento_venda_item_ficha")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_item_ficha")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DocumentoVendaItemFichaSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(documentoVendaItemFichaBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemFichaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `documento_venda_item_ficha` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemFichaPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from documentoVendaItemFicha slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for documento_venda_item_ficha")
	}

	if len(documentoVendaItemFichaAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DocumentoVendaItemFicha) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDocumentoVendaItemFicha(ctx, exec, o.DDVID, o.DviItem, o.DvifItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DocumentoVendaItemFichaSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DocumentoVendaItemFichaSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), documentoVendaItemFichaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `documento_venda_item_ficha`.* FROM `documento_venda_item_ficha` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, documentoVendaItemFichaPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DocumentoVendaItemFichaSlice")
	}

	*o = slice

	return nil
}

// DocumentoVendaItemFichaExists checks if the DocumentoVendaItemFicha row exists.
func DocumentoVendaItemFichaExists(ctx context.Context, exec boil.ContextExecutor, dDVID int, dviItem int, dvifItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `documento_venda_item_ficha` where `ddv_id`=? AND `dvi_item`=? AND `dvif_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, dDVID, dviItem, dvifItem)
	}
	row := exec.QueryRowContext(ctx, sql, dDVID, dviItem, dvifItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if documento_venda_item_ficha exists")
	}

	return exists, nil
}

// Exists checks if the DocumentoVendaItemFicha row exists.
func (o *DocumentoVendaItemFicha) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DocumentoVendaItemFichaExists(ctx, exec, o.DDVID, o.DviItem, o.DvifItem)
}
