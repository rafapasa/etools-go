// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// RequisicaoItemLote is an object representing the database table.
type RequisicaoItemLote struct {
	ReqID              int           `boil:"req_id" json:"req_id" toml:"req_id" yaml:"req_id"`
	ReiItem            int           `boil:"rei_item" json:"rei_item" toml:"rei_item" yaml:"rei_item"`
	ReilItem           int           `boil:"reil_item" json:"reil_item" toml:"reil_item" yaml:"reil_item"`
	ProID              int           `boil:"pro_id" json:"pro_id" toml:"pro_id" yaml:"pro_id"`
	ProlItem           int           `boil:"prol_item" json:"prol_item" toml:"prol_item" yaml:"prol_item"`
	ReilQuantidade     types.Decimal `boil:"reil_quantidade" json:"reil_quantidade" toml:"reil_quantidade" yaml:"reil_quantidade"`
	ReilLote           string        `boil:"reil_lote" json:"reil_lote" toml:"reil_lote" yaml:"reil_lote"`
	ReilDatafabricacao time.Time     `boil:"reil_datafabricacao" json:"reil_datafabricacao" toml:"reil_datafabricacao" yaml:"reil_datafabricacao"`
	ReilDatavalidade   time.Time     `boil:"reil_datavalidade" json:"reil_datavalidade" toml:"reil_datavalidade" yaml:"reil_datavalidade"`

	R *requisicaoItemLoteR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L requisicaoItemLoteL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RequisicaoItemLoteColumns = struct {
	ReqID              string
	ReiItem            string
	ReilItem           string
	ProID              string
	ProlItem           string
	ReilQuantidade     string
	ReilLote           string
	ReilDatafabricacao string
	ReilDatavalidade   string
}{
	ReqID:              "req_id",
	ReiItem:            "rei_item",
	ReilItem:           "reil_item",
	ProID:              "pro_id",
	ProlItem:           "prol_item",
	ReilQuantidade:     "reil_quantidade",
	ReilLote:           "reil_lote",
	ReilDatafabricacao: "reil_datafabricacao",
	ReilDatavalidade:   "reil_datavalidade",
}

var RequisicaoItemLoteTableColumns = struct {
	ReqID              string
	ReiItem            string
	ReilItem           string
	ProID              string
	ProlItem           string
	ReilQuantidade     string
	ReilLote           string
	ReilDatafabricacao string
	ReilDatavalidade   string
}{
	ReqID:              "requisicao_item_lote.req_id",
	ReiItem:            "requisicao_item_lote.rei_item",
	ReilItem:           "requisicao_item_lote.reil_item",
	ProID:              "requisicao_item_lote.pro_id",
	ProlItem:           "requisicao_item_lote.prol_item",
	ReilQuantidade:     "requisicao_item_lote.reil_quantidade",
	ReilLote:           "requisicao_item_lote.reil_lote",
	ReilDatafabricacao: "requisicao_item_lote.reil_datafabricacao",
	ReilDatavalidade:   "requisicao_item_lote.reil_datavalidade",
}

// Generated where

var RequisicaoItemLoteWhere = struct {
	ReqID              whereHelperint
	ReiItem            whereHelperint
	ReilItem           whereHelperint
	ProID              whereHelperint
	ProlItem           whereHelperint
	ReilQuantidade     whereHelpertypes_Decimal
	ReilLote           whereHelperstring
	ReilDatafabricacao whereHelpertime_Time
	ReilDatavalidade   whereHelpertime_Time
}{
	ReqID:              whereHelperint{field: "`requisicao_item_lote`.`req_id`"},
	ReiItem:            whereHelperint{field: "`requisicao_item_lote`.`rei_item`"},
	ReilItem:           whereHelperint{field: "`requisicao_item_lote`.`reil_item`"},
	ProID:              whereHelperint{field: "`requisicao_item_lote`.`pro_id`"},
	ProlItem:           whereHelperint{field: "`requisicao_item_lote`.`prol_item`"},
	ReilQuantidade:     whereHelpertypes_Decimal{field: "`requisicao_item_lote`.`reil_quantidade`"},
	ReilLote:           whereHelperstring{field: "`requisicao_item_lote`.`reil_lote`"},
	ReilDatafabricacao: whereHelpertime_Time{field: "`requisicao_item_lote`.`reil_datafabricacao`"},
	ReilDatavalidade:   whereHelpertime_Time{field: "`requisicao_item_lote`.`reil_datavalidade`"},
}

// RequisicaoItemLoteRels is where relationship names are stored.
var RequisicaoItemLoteRels = struct {
}{}

// requisicaoItemLoteR is where relationships are stored.
type requisicaoItemLoteR struct {
}

// NewStruct creates a new relationship struct
func (*requisicaoItemLoteR) NewStruct() *requisicaoItemLoteR {
	return &requisicaoItemLoteR{}
}

// requisicaoItemLoteL is where Load methods for each relationship are stored.
type requisicaoItemLoteL struct{}

var (
	requisicaoItemLoteAllColumns            = []string{"req_id", "rei_item", "reil_item", "pro_id", "prol_item", "reil_quantidade", "reil_lote", "reil_datafabricacao", "reil_datavalidade"}
	requisicaoItemLoteColumnsWithoutDefault = []string{"req_id", "rei_item", "reil_item", "pro_id", "prol_item", "reil_quantidade", "reil_lote", "reil_datafabricacao", "reil_datavalidade"}
	requisicaoItemLoteColumnsWithDefault    = []string{}
	requisicaoItemLotePrimaryKeyColumns     = []string{"req_id", "rei_item", "reil_item"}
	requisicaoItemLoteGeneratedColumns      = []string{}
)

type (
	// RequisicaoItemLoteSlice is an alias for a slice of pointers to RequisicaoItemLote.
	// This should almost always be used instead of []RequisicaoItemLote.
	RequisicaoItemLoteSlice []*RequisicaoItemLote
	// RequisicaoItemLoteHook is the signature for custom RequisicaoItemLote hook methods
	RequisicaoItemLoteHook func(context.Context, boil.ContextExecutor, *RequisicaoItemLote) error

	requisicaoItemLoteQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	requisicaoItemLoteType                 = reflect.TypeOf(&RequisicaoItemLote{})
	requisicaoItemLoteMapping              = queries.MakeStructMapping(requisicaoItemLoteType)
	requisicaoItemLotePrimaryKeyMapping, _ = queries.BindMapping(requisicaoItemLoteType, requisicaoItemLoteMapping, requisicaoItemLotePrimaryKeyColumns)
	requisicaoItemLoteInsertCacheMut       sync.RWMutex
	requisicaoItemLoteInsertCache          = make(map[string]insertCache)
	requisicaoItemLoteUpdateCacheMut       sync.RWMutex
	requisicaoItemLoteUpdateCache          = make(map[string]updateCache)
	requisicaoItemLoteUpsertCacheMut       sync.RWMutex
	requisicaoItemLoteUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var requisicaoItemLoteAfterSelectMu sync.Mutex
var requisicaoItemLoteAfterSelectHooks []RequisicaoItemLoteHook

var requisicaoItemLoteBeforeInsertMu sync.Mutex
var requisicaoItemLoteBeforeInsertHooks []RequisicaoItemLoteHook
var requisicaoItemLoteAfterInsertMu sync.Mutex
var requisicaoItemLoteAfterInsertHooks []RequisicaoItemLoteHook

var requisicaoItemLoteBeforeUpdateMu sync.Mutex
var requisicaoItemLoteBeforeUpdateHooks []RequisicaoItemLoteHook
var requisicaoItemLoteAfterUpdateMu sync.Mutex
var requisicaoItemLoteAfterUpdateHooks []RequisicaoItemLoteHook

var requisicaoItemLoteBeforeDeleteMu sync.Mutex
var requisicaoItemLoteBeforeDeleteHooks []RequisicaoItemLoteHook
var requisicaoItemLoteAfterDeleteMu sync.Mutex
var requisicaoItemLoteAfterDeleteHooks []RequisicaoItemLoteHook

var requisicaoItemLoteBeforeUpsertMu sync.Mutex
var requisicaoItemLoteBeforeUpsertHooks []RequisicaoItemLoteHook
var requisicaoItemLoteAfterUpsertMu sync.Mutex
var requisicaoItemLoteAfterUpsertHooks []RequisicaoItemLoteHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RequisicaoItemLote) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RequisicaoItemLote) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RequisicaoItemLote) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RequisicaoItemLote) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RequisicaoItemLote) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RequisicaoItemLote) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RequisicaoItemLote) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RequisicaoItemLote) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RequisicaoItemLote) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range requisicaoItemLoteAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRequisicaoItemLoteHook registers your hook function for all future operations.
func AddRequisicaoItemLoteHook(hookPoint boil.HookPoint, requisicaoItemLoteHook RequisicaoItemLoteHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		requisicaoItemLoteAfterSelectMu.Lock()
		requisicaoItemLoteAfterSelectHooks = append(requisicaoItemLoteAfterSelectHooks, requisicaoItemLoteHook)
		requisicaoItemLoteAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		requisicaoItemLoteBeforeInsertMu.Lock()
		requisicaoItemLoteBeforeInsertHooks = append(requisicaoItemLoteBeforeInsertHooks, requisicaoItemLoteHook)
		requisicaoItemLoteBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		requisicaoItemLoteAfterInsertMu.Lock()
		requisicaoItemLoteAfterInsertHooks = append(requisicaoItemLoteAfterInsertHooks, requisicaoItemLoteHook)
		requisicaoItemLoteAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		requisicaoItemLoteBeforeUpdateMu.Lock()
		requisicaoItemLoteBeforeUpdateHooks = append(requisicaoItemLoteBeforeUpdateHooks, requisicaoItemLoteHook)
		requisicaoItemLoteBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		requisicaoItemLoteAfterUpdateMu.Lock()
		requisicaoItemLoteAfterUpdateHooks = append(requisicaoItemLoteAfterUpdateHooks, requisicaoItemLoteHook)
		requisicaoItemLoteAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		requisicaoItemLoteBeforeDeleteMu.Lock()
		requisicaoItemLoteBeforeDeleteHooks = append(requisicaoItemLoteBeforeDeleteHooks, requisicaoItemLoteHook)
		requisicaoItemLoteBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		requisicaoItemLoteAfterDeleteMu.Lock()
		requisicaoItemLoteAfterDeleteHooks = append(requisicaoItemLoteAfterDeleteHooks, requisicaoItemLoteHook)
		requisicaoItemLoteAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		requisicaoItemLoteBeforeUpsertMu.Lock()
		requisicaoItemLoteBeforeUpsertHooks = append(requisicaoItemLoteBeforeUpsertHooks, requisicaoItemLoteHook)
		requisicaoItemLoteBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		requisicaoItemLoteAfterUpsertMu.Lock()
		requisicaoItemLoteAfterUpsertHooks = append(requisicaoItemLoteAfterUpsertHooks, requisicaoItemLoteHook)
		requisicaoItemLoteAfterUpsertMu.Unlock()
	}
}

// One returns a single requisicaoItemLote record from the query.
func (q requisicaoItemLoteQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RequisicaoItemLote, error) {
	o := &RequisicaoItemLote{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for requisicao_item_lote")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RequisicaoItemLote records from the query.
func (q requisicaoItemLoteQuery) All(ctx context.Context, exec boil.ContextExecutor) (RequisicaoItemLoteSlice, error) {
	var o []*RequisicaoItemLote

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RequisicaoItemLote slice")
	}

	if len(requisicaoItemLoteAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RequisicaoItemLote records in the query.
func (q requisicaoItemLoteQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count requisicao_item_lote rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q requisicaoItemLoteQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if requisicao_item_lote exists")
	}

	return count > 0, nil
}

// RequisicaoItemLotes retrieves all the records using an executor.
func RequisicaoItemLotes(mods ...qm.QueryMod) requisicaoItemLoteQuery {
	mods = append(mods, qm.From("`requisicao_item_lote`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`requisicao_item_lote`.*"})
	}

	return requisicaoItemLoteQuery{q}
}

// FindRequisicaoItemLote retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRequisicaoItemLote(ctx context.Context, exec boil.ContextExecutor, reqID int, reiItem int, reilItem int, selectCols ...string) (*RequisicaoItemLote, error) {
	requisicaoItemLoteObj := &RequisicaoItemLote{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `requisicao_item_lote` where `req_id`=? AND `rei_item`=? AND `reil_item`=?", sel,
	)

	q := queries.Raw(query, reqID, reiItem, reilItem)

	err := q.Bind(ctx, exec, requisicaoItemLoteObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from requisicao_item_lote")
	}

	if err = requisicaoItemLoteObj.doAfterSelectHooks(ctx, exec); err != nil {
		return requisicaoItemLoteObj, err
	}

	return requisicaoItemLoteObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RequisicaoItemLote) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no requisicao_item_lote provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(requisicaoItemLoteColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	requisicaoItemLoteInsertCacheMut.RLock()
	cache, cached := requisicaoItemLoteInsertCache[key]
	requisicaoItemLoteInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			requisicaoItemLoteAllColumns,
			requisicaoItemLoteColumnsWithDefault,
			requisicaoItemLoteColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(requisicaoItemLoteType, requisicaoItemLoteMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(requisicaoItemLoteType, requisicaoItemLoteMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `requisicao_item_lote` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `requisicao_item_lote` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `requisicao_item_lote` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, requisicaoItemLotePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into requisicao_item_lote")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ReqID,
		o.ReiItem,
		o.ReilItem,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for requisicao_item_lote")
	}

CacheNoHooks:
	if !cached {
		requisicaoItemLoteInsertCacheMut.Lock()
		requisicaoItemLoteInsertCache[key] = cache
		requisicaoItemLoteInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RequisicaoItemLote.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RequisicaoItemLote) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	requisicaoItemLoteUpdateCacheMut.RLock()
	cache, cached := requisicaoItemLoteUpdateCache[key]
	requisicaoItemLoteUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			requisicaoItemLoteAllColumns,
			requisicaoItemLotePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update requisicao_item_lote, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `requisicao_item_lote` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, requisicaoItemLotePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(requisicaoItemLoteType, requisicaoItemLoteMapping, append(wl, requisicaoItemLotePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update requisicao_item_lote row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for requisicao_item_lote")
	}

	if !cached {
		requisicaoItemLoteUpdateCacheMut.Lock()
		requisicaoItemLoteUpdateCache[key] = cache
		requisicaoItemLoteUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q requisicaoItemLoteQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for requisicao_item_lote")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for requisicao_item_lote")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RequisicaoItemLoteSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemLotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `requisicao_item_lote` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemLotePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in requisicaoItemLote slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all requisicaoItemLote")
	}
	return rowsAff, nil
}

var mySQLRequisicaoItemLoteUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RequisicaoItemLote) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no requisicao_item_lote provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(requisicaoItemLoteColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRequisicaoItemLoteUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	requisicaoItemLoteUpsertCacheMut.RLock()
	cache, cached := requisicaoItemLoteUpsertCache[key]
	requisicaoItemLoteUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			requisicaoItemLoteAllColumns,
			requisicaoItemLoteColumnsWithDefault,
			requisicaoItemLoteColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			requisicaoItemLoteAllColumns,
			requisicaoItemLotePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert requisicao_item_lote, could not build update column list")
		}

		ret := strmangle.SetComplement(requisicaoItemLoteAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`requisicao_item_lote`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `requisicao_item_lote` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(requisicaoItemLoteType, requisicaoItemLoteMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(requisicaoItemLoteType, requisicaoItemLoteMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for requisicao_item_lote")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(requisicaoItemLoteType, requisicaoItemLoteMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for requisicao_item_lote")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for requisicao_item_lote")
	}

CacheNoHooks:
	if !cached {
		requisicaoItemLoteUpsertCacheMut.Lock()
		requisicaoItemLoteUpsertCache[key] = cache
		requisicaoItemLoteUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RequisicaoItemLote record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RequisicaoItemLote) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RequisicaoItemLote provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), requisicaoItemLotePrimaryKeyMapping)
	sql := "DELETE FROM `requisicao_item_lote` WHERE `req_id`=? AND `rei_item`=? AND `reil_item`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from requisicao_item_lote")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for requisicao_item_lote")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q requisicaoItemLoteQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no requisicaoItemLoteQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from requisicao_item_lote")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for requisicao_item_lote")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RequisicaoItemLoteSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(requisicaoItemLoteBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemLotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `requisicao_item_lote` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemLotePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from requisicaoItemLote slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for requisicao_item_lote")
	}

	if len(requisicaoItemLoteAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RequisicaoItemLote) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRequisicaoItemLote(ctx, exec, o.ReqID, o.ReiItem, o.ReilItem)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RequisicaoItemLoteSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RequisicaoItemLoteSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), requisicaoItemLotePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `requisicao_item_lote`.* FROM `requisicao_item_lote` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, requisicaoItemLotePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RequisicaoItemLoteSlice")
	}

	*o = slice

	return nil
}

// RequisicaoItemLoteExists checks if the RequisicaoItemLote row exists.
func RequisicaoItemLoteExists(ctx context.Context, exec boil.ContextExecutor, reqID int, reiItem int, reilItem int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `requisicao_item_lote` where `req_id`=? AND `rei_item`=? AND `reil_item`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, reqID, reiItem, reilItem)
	}
	row := exec.QueryRowContext(ctx, sql, reqID, reiItem, reilItem)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if requisicao_item_lote exists")
	}

	return exists, nil
}

// Exists checks if the RequisicaoItemLote row exists.
func (o *RequisicaoItemLote) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RequisicaoItemLoteExists(ctx, exec, o.ReqID, o.ReiItem, o.ReilItem)
}
