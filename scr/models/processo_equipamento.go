// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ProcessoEquipamento is an object representing the database table.
type ProcessoEquipamento struct {
	PrceID        int    `boil:"prce_id" json:"prce_id" toml:"prce_id" yaml:"prce_id"`
	PrceDescricao string `boil:"prce_descricao" json:"prce_descricao" toml:"prce_descricao" yaml:"prce_descricao"`

	R *processoEquipamentoR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L processoEquipamentoL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ProcessoEquipamentoColumns = struct {
	PrceID        string
	PrceDescricao string
}{
	PrceID:        "prce_id",
	PrceDescricao: "prce_descricao",
}

var ProcessoEquipamentoTableColumns = struct {
	PrceID        string
	PrceDescricao string
}{
	PrceID:        "processo_equipamento.prce_id",
	PrceDescricao: "processo_equipamento.prce_descricao",
}

// Generated where

var ProcessoEquipamentoWhere = struct {
	PrceID        whereHelperint
	PrceDescricao whereHelperstring
}{
	PrceID:        whereHelperint{field: "`processo_equipamento`.`prce_id`"},
	PrceDescricao: whereHelperstring{field: "`processo_equipamento`.`prce_descricao`"},
}

// ProcessoEquipamentoRels is where relationship names are stored.
var ProcessoEquipamentoRels = struct {
	PrceEquipamentoProcessoEqus string
}{
	PrceEquipamentoProcessoEqus: "PrceEquipamentoProcessoEqus",
}

// processoEquipamentoR is where relationships are stored.
type processoEquipamentoR struct {
	PrceEquipamentoProcessoEqus EquipamentoProcessoEquSlice `boil:"PrceEquipamentoProcessoEqus" json:"PrceEquipamentoProcessoEqus" toml:"PrceEquipamentoProcessoEqus" yaml:"PrceEquipamentoProcessoEqus"`
}

// NewStruct creates a new relationship struct
func (*processoEquipamentoR) NewStruct() *processoEquipamentoR {
	return &processoEquipamentoR{}
}

func (r *processoEquipamentoR) GetPrceEquipamentoProcessoEqus() EquipamentoProcessoEquSlice {
	if r == nil {
		return nil
	}
	return r.PrceEquipamentoProcessoEqus
}

// processoEquipamentoL is where Load methods for each relationship are stored.
type processoEquipamentoL struct{}

var (
	processoEquipamentoAllColumns            = []string{"prce_id", "prce_descricao"}
	processoEquipamentoColumnsWithoutDefault = []string{"prce_id", "prce_descricao"}
	processoEquipamentoColumnsWithDefault    = []string{}
	processoEquipamentoPrimaryKeyColumns     = []string{"prce_id"}
	processoEquipamentoGeneratedColumns      = []string{}
)

type (
	// ProcessoEquipamentoSlice is an alias for a slice of pointers to ProcessoEquipamento.
	// This should almost always be used instead of []ProcessoEquipamento.
	ProcessoEquipamentoSlice []*ProcessoEquipamento
	// ProcessoEquipamentoHook is the signature for custom ProcessoEquipamento hook methods
	ProcessoEquipamentoHook func(context.Context, boil.ContextExecutor, *ProcessoEquipamento) error

	processoEquipamentoQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	processoEquipamentoType                 = reflect.TypeOf(&ProcessoEquipamento{})
	processoEquipamentoMapping              = queries.MakeStructMapping(processoEquipamentoType)
	processoEquipamentoPrimaryKeyMapping, _ = queries.BindMapping(processoEquipamentoType, processoEquipamentoMapping, processoEquipamentoPrimaryKeyColumns)
	processoEquipamentoInsertCacheMut       sync.RWMutex
	processoEquipamentoInsertCache          = make(map[string]insertCache)
	processoEquipamentoUpdateCacheMut       sync.RWMutex
	processoEquipamentoUpdateCache          = make(map[string]updateCache)
	processoEquipamentoUpsertCacheMut       sync.RWMutex
	processoEquipamentoUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var processoEquipamentoAfterSelectMu sync.Mutex
var processoEquipamentoAfterSelectHooks []ProcessoEquipamentoHook

var processoEquipamentoBeforeInsertMu sync.Mutex
var processoEquipamentoBeforeInsertHooks []ProcessoEquipamentoHook
var processoEquipamentoAfterInsertMu sync.Mutex
var processoEquipamentoAfterInsertHooks []ProcessoEquipamentoHook

var processoEquipamentoBeforeUpdateMu sync.Mutex
var processoEquipamentoBeforeUpdateHooks []ProcessoEquipamentoHook
var processoEquipamentoAfterUpdateMu sync.Mutex
var processoEquipamentoAfterUpdateHooks []ProcessoEquipamentoHook

var processoEquipamentoBeforeDeleteMu sync.Mutex
var processoEquipamentoBeforeDeleteHooks []ProcessoEquipamentoHook
var processoEquipamentoAfterDeleteMu sync.Mutex
var processoEquipamentoAfterDeleteHooks []ProcessoEquipamentoHook

var processoEquipamentoBeforeUpsertMu sync.Mutex
var processoEquipamentoBeforeUpsertHooks []ProcessoEquipamentoHook
var processoEquipamentoAfterUpsertMu sync.Mutex
var processoEquipamentoAfterUpsertHooks []ProcessoEquipamentoHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ProcessoEquipamento) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ProcessoEquipamento) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ProcessoEquipamento) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ProcessoEquipamento) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ProcessoEquipamento) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ProcessoEquipamento) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ProcessoEquipamento) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ProcessoEquipamento) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ProcessoEquipamento) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range processoEquipamentoAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddProcessoEquipamentoHook registers your hook function for all future operations.
func AddProcessoEquipamentoHook(hookPoint boil.HookPoint, processoEquipamentoHook ProcessoEquipamentoHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		processoEquipamentoAfterSelectMu.Lock()
		processoEquipamentoAfterSelectHooks = append(processoEquipamentoAfterSelectHooks, processoEquipamentoHook)
		processoEquipamentoAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		processoEquipamentoBeforeInsertMu.Lock()
		processoEquipamentoBeforeInsertHooks = append(processoEquipamentoBeforeInsertHooks, processoEquipamentoHook)
		processoEquipamentoBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		processoEquipamentoAfterInsertMu.Lock()
		processoEquipamentoAfterInsertHooks = append(processoEquipamentoAfterInsertHooks, processoEquipamentoHook)
		processoEquipamentoAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		processoEquipamentoBeforeUpdateMu.Lock()
		processoEquipamentoBeforeUpdateHooks = append(processoEquipamentoBeforeUpdateHooks, processoEquipamentoHook)
		processoEquipamentoBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		processoEquipamentoAfterUpdateMu.Lock()
		processoEquipamentoAfterUpdateHooks = append(processoEquipamentoAfterUpdateHooks, processoEquipamentoHook)
		processoEquipamentoAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		processoEquipamentoBeforeDeleteMu.Lock()
		processoEquipamentoBeforeDeleteHooks = append(processoEquipamentoBeforeDeleteHooks, processoEquipamentoHook)
		processoEquipamentoBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		processoEquipamentoAfterDeleteMu.Lock()
		processoEquipamentoAfterDeleteHooks = append(processoEquipamentoAfterDeleteHooks, processoEquipamentoHook)
		processoEquipamentoAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		processoEquipamentoBeforeUpsertMu.Lock()
		processoEquipamentoBeforeUpsertHooks = append(processoEquipamentoBeforeUpsertHooks, processoEquipamentoHook)
		processoEquipamentoBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		processoEquipamentoAfterUpsertMu.Lock()
		processoEquipamentoAfterUpsertHooks = append(processoEquipamentoAfterUpsertHooks, processoEquipamentoHook)
		processoEquipamentoAfterUpsertMu.Unlock()
	}
}

// One returns a single processoEquipamento record from the query.
func (q processoEquipamentoQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ProcessoEquipamento, error) {
	o := &ProcessoEquipamento{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for processo_equipamento")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ProcessoEquipamento records from the query.
func (q processoEquipamentoQuery) All(ctx context.Context, exec boil.ContextExecutor) (ProcessoEquipamentoSlice, error) {
	var o []*ProcessoEquipamento

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ProcessoEquipamento slice")
	}

	if len(processoEquipamentoAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ProcessoEquipamento records in the query.
func (q processoEquipamentoQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count processo_equipamento rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q processoEquipamentoQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if processo_equipamento exists")
	}

	return count > 0, nil
}

// PrceEquipamentoProcessoEqus retrieves all the equipamento_processo_equ's EquipamentoProcessoEqus with an executor via prce_id column.
func (o *ProcessoEquipamento) PrceEquipamentoProcessoEqus(mods ...qm.QueryMod) equipamentoProcessoEquQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`equipamento_processo_equ`.`prce_id`=?", o.PrceID),
	)

	return EquipamentoProcessoEqus(queryMods...)
}

// LoadPrceEquipamentoProcessoEqus allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (processoEquipamentoL) LoadPrceEquipamentoProcessoEqus(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProcessoEquipamento interface{}, mods queries.Applicator) error {
	var slice []*ProcessoEquipamento
	var object *ProcessoEquipamento

	if singular {
		var ok bool
		object, ok = maybeProcessoEquipamento.(*ProcessoEquipamento)
		if !ok {
			object = new(ProcessoEquipamento)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeProcessoEquipamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeProcessoEquipamento))
			}
		}
	} else {
		s, ok := maybeProcessoEquipamento.(*[]*ProcessoEquipamento)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeProcessoEquipamento)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeProcessoEquipamento))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &processoEquipamentoR{}
		}
		args[object.PrceID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &processoEquipamentoR{}
			}
			args[obj.PrceID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`equipamento_processo_equ`),
		qm.WhereIn(`equipamento_processo_equ.prce_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load equipamento_processo_equ")
	}

	var resultSlice []*EquipamentoProcessoEqu
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice equipamento_processo_equ")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on equipamento_processo_equ")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for equipamento_processo_equ")
	}

	if len(equipamentoProcessoEquAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PrceEquipamentoProcessoEqus = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &equipamentoProcessoEquR{}
			}
			foreign.R.Prce = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PrceID == foreign.PrceID {
				local.R.PrceEquipamentoProcessoEqus = append(local.R.PrceEquipamentoProcessoEqus, foreign)
				if foreign.R == nil {
					foreign.R = &equipamentoProcessoEquR{}
				}
				foreign.R.Prce = local
				break
			}
		}
	}

	return nil
}

// AddPrceEquipamentoProcessoEqus adds the given related objects to the existing relationships
// of the processo_equipamento, optionally inserting them as new records.
// Appends related to o.R.PrceEquipamentoProcessoEqus.
// Sets related.R.Prce appropriately.
func (o *ProcessoEquipamento) AddPrceEquipamentoProcessoEqus(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*EquipamentoProcessoEqu) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PrceID = o.PrceID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `equipamento_processo_equ` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"prce_id"}),
				strmangle.WhereClause("`", "`", 0, equipamentoProcessoEquPrimaryKeyColumns),
			)
			values := []interface{}{o.PrceID, rel.EpeID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PrceID = o.PrceID
		}
	}

	if o.R == nil {
		o.R = &processoEquipamentoR{
			PrceEquipamentoProcessoEqus: related,
		}
	} else {
		o.R.PrceEquipamentoProcessoEqus = append(o.R.PrceEquipamentoProcessoEqus, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &equipamentoProcessoEquR{
				Prce: o,
			}
		} else {
			rel.R.Prce = o
		}
	}
	return nil
}

// ProcessoEquipamentos retrieves all the records using an executor.
func ProcessoEquipamentos(mods ...qm.QueryMod) processoEquipamentoQuery {
	mods = append(mods, qm.From("`processo_equipamento`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`processo_equipamento`.*"})
	}

	return processoEquipamentoQuery{q}
}

// FindProcessoEquipamento retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindProcessoEquipamento(ctx context.Context, exec boil.ContextExecutor, prceID int, selectCols ...string) (*ProcessoEquipamento, error) {
	processoEquipamentoObj := &ProcessoEquipamento{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `processo_equipamento` where `prce_id`=?", sel,
	)

	q := queries.Raw(query, prceID)

	err := q.Bind(ctx, exec, processoEquipamentoObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from processo_equipamento")
	}

	if err = processoEquipamentoObj.doAfterSelectHooks(ctx, exec); err != nil {
		return processoEquipamentoObj, err
	}

	return processoEquipamentoObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ProcessoEquipamento) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no processo_equipamento provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(processoEquipamentoColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	processoEquipamentoInsertCacheMut.RLock()
	cache, cached := processoEquipamentoInsertCache[key]
	processoEquipamentoInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			processoEquipamentoAllColumns,
			processoEquipamentoColumnsWithDefault,
			processoEquipamentoColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(processoEquipamentoType, processoEquipamentoMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(processoEquipamentoType, processoEquipamentoMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `processo_equipamento` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `processo_equipamento` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `processo_equipamento` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, processoEquipamentoPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into processo_equipamento")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.PrceID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for processo_equipamento")
	}

CacheNoHooks:
	if !cached {
		processoEquipamentoInsertCacheMut.Lock()
		processoEquipamentoInsertCache[key] = cache
		processoEquipamentoInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ProcessoEquipamento.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ProcessoEquipamento) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	processoEquipamentoUpdateCacheMut.RLock()
	cache, cached := processoEquipamentoUpdateCache[key]
	processoEquipamentoUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			processoEquipamentoAllColumns,
			processoEquipamentoPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update processo_equipamento, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `processo_equipamento` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, processoEquipamentoPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(processoEquipamentoType, processoEquipamentoMapping, append(wl, processoEquipamentoPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update processo_equipamento row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for processo_equipamento")
	}

	if !cached {
		processoEquipamentoUpdateCacheMut.Lock()
		processoEquipamentoUpdateCache[key] = cache
		processoEquipamentoUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q processoEquipamentoQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for processo_equipamento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for processo_equipamento")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ProcessoEquipamentoSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), processoEquipamentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `processo_equipamento` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, processoEquipamentoPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in processoEquipamento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all processoEquipamento")
	}
	return rowsAff, nil
}

var mySQLProcessoEquipamentoUniqueColumns = []string{
	"prce_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ProcessoEquipamento) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no processo_equipamento provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(processoEquipamentoColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLProcessoEquipamentoUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	processoEquipamentoUpsertCacheMut.RLock()
	cache, cached := processoEquipamentoUpsertCache[key]
	processoEquipamentoUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			processoEquipamentoAllColumns,
			processoEquipamentoColumnsWithDefault,
			processoEquipamentoColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			processoEquipamentoAllColumns,
			processoEquipamentoPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert processo_equipamento, could not build update column list")
		}

		ret := strmangle.SetComplement(processoEquipamentoAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`processo_equipamento`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `processo_equipamento` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(processoEquipamentoType, processoEquipamentoMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(processoEquipamentoType, processoEquipamentoMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for processo_equipamento")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(processoEquipamentoType, processoEquipamentoMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for processo_equipamento")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for processo_equipamento")
	}

CacheNoHooks:
	if !cached {
		processoEquipamentoUpsertCacheMut.Lock()
		processoEquipamentoUpsertCache[key] = cache
		processoEquipamentoUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ProcessoEquipamento record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ProcessoEquipamento) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ProcessoEquipamento provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), processoEquipamentoPrimaryKeyMapping)
	sql := "DELETE FROM `processo_equipamento` WHERE `prce_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from processo_equipamento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for processo_equipamento")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q processoEquipamentoQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no processoEquipamentoQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from processo_equipamento")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for processo_equipamento")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ProcessoEquipamentoSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(processoEquipamentoBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), processoEquipamentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `processo_equipamento` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, processoEquipamentoPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from processoEquipamento slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for processo_equipamento")
	}

	if len(processoEquipamentoAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ProcessoEquipamento) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindProcessoEquipamento(ctx, exec, o.PrceID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ProcessoEquipamentoSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ProcessoEquipamentoSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), processoEquipamentoPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `processo_equipamento`.* FROM `processo_equipamento` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, processoEquipamentoPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ProcessoEquipamentoSlice")
	}

	*o = slice

	return nil
}

// ProcessoEquipamentoExists checks if the ProcessoEquipamento row exists.
func ProcessoEquipamentoExists(ctx context.Context, exec boil.ContextExecutor, prceID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `processo_equipamento` where `prce_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, prceID)
	}
	row := exec.QueryRowContext(ctx, sql, prceID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if processo_equipamento exists")
	}

	return exists, nil
}

// Exists checks if the ProcessoEquipamento row exists.
func (o *ProcessoEquipamento) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ProcessoEquipamentoExists(ctx, exec, o.PrceID)
}
