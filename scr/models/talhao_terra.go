// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// TalhaoTerra is an object representing the database table.
type TalhaoTerra struct {
	TatID          int               `boil:"tat_id" json:"tat_id" toml:"tat_id" yaml:"tat_id"`
	TatDescricao   string            `boil:"tat_descricao" json:"tat_descricao" toml:"tat_descricao" yaml:"tat_descricao"`
	TatSituacao    int               `boil:"tat_situacao" json:"tat_situacao" toml:"tat_situacao" yaml:"tat_situacao"`
	TatQuanthectar types.NullDecimal `boil:"tat_quanthectar" json:"tat_quanthectar,omitempty" toml:"tat_quanthectar" yaml:"tat_quanthectar,omitempty"`

	R *talhaoTerraR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L talhaoTerraL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TalhaoTerraColumns = struct {
	TatID          string
	TatDescricao   string
	TatSituacao    string
	TatQuanthectar string
}{
	TatID:          "tat_id",
	TatDescricao:   "tat_descricao",
	TatSituacao:    "tat_situacao",
	TatQuanthectar: "tat_quanthectar",
}

var TalhaoTerraTableColumns = struct {
	TatID          string
	TatDescricao   string
	TatSituacao    string
	TatQuanthectar string
}{
	TatID:          "talhao_terra.tat_id",
	TatDescricao:   "talhao_terra.tat_descricao",
	TatSituacao:    "talhao_terra.tat_situacao",
	TatQuanthectar: "talhao_terra.tat_quanthectar",
}

// Generated where

var TalhaoTerraWhere = struct {
	TatID          whereHelperint
	TatDescricao   whereHelperstring
	TatSituacao    whereHelperint
	TatQuanthectar whereHelpertypes_NullDecimal
}{
	TatID:          whereHelperint{field: "`talhao_terra`.`tat_id`"},
	TatDescricao:   whereHelperstring{field: "`talhao_terra`.`tat_descricao`"},
	TatSituacao:    whereHelperint{field: "`talhao_terra`.`tat_situacao`"},
	TatQuanthectar: whereHelpertypes_NullDecimal{field: "`talhao_terra`.`tat_quanthectar`"},
}

// TalhaoTerraRels is where relationship names are stored.
var TalhaoTerraRels = struct {
	TatDocumentoVendas string
	TatProdutoEstoques string
	TatRequisicaos     string
}{
	TatDocumentoVendas: "TatDocumentoVendas",
	TatProdutoEstoques: "TatProdutoEstoques",
	TatRequisicaos:     "TatRequisicaos",
}

// talhaoTerraR is where relationships are stored.
type talhaoTerraR struct {
	TatDocumentoVendas DocumentoVendaSlice `boil:"TatDocumentoVendas" json:"TatDocumentoVendas" toml:"TatDocumentoVendas" yaml:"TatDocumentoVendas"`
	TatProdutoEstoques ProdutoEstoqueSlice `boil:"TatProdutoEstoques" json:"TatProdutoEstoques" toml:"TatProdutoEstoques" yaml:"TatProdutoEstoques"`
	TatRequisicaos     RequisicaoSlice     `boil:"TatRequisicaos" json:"TatRequisicaos" toml:"TatRequisicaos" yaml:"TatRequisicaos"`
}

// NewStruct creates a new relationship struct
func (*talhaoTerraR) NewStruct() *talhaoTerraR {
	return &talhaoTerraR{}
}

func (r *talhaoTerraR) GetTatDocumentoVendas() DocumentoVendaSlice {
	if r == nil {
		return nil
	}
	return r.TatDocumentoVendas
}

func (r *talhaoTerraR) GetTatProdutoEstoques() ProdutoEstoqueSlice {
	if r == nil {
		return nil
	}
	return r.TatProdutoEstoques
}

func (r *talhaoTerraR) GetTatRequisicaos() RequisicaoSlice {
	if r == nil {
		return nil
	}
	return r.TatRequisicaos
}

// talhaoTerraL is where Load methods for each relationship are stored.
type talhaoTerraL struct{}

var (
	talhaoTerraAllColumns            = []string{"tat_id", "tat_descricao", "tat_situacao", "tat_quanthectar"}
	talhaoTerraColumnsWithoutDefault = []string{"tat_id", "tat_descricao", "tat_situacao", "tat_quanthectar"}
	talhaoTerraColumnsWithDefault    = []string{}
	talhaoTerraPrimaryKeyColumns     = []string{"tat_id"}
	talhaoTerraGeneratedColumns      = []string{}
)

type (
	// TalhaoTerraSlice is an alias for a slice of pointers to TalhaoTerra.
	// This should almost always be used instead of []TalhaoTerra.
	TalhaoTerraSlice []*TalhaoTerra
	// TalhaoTerraHook is the signature for custom TalhaoTerra hook methods
	TalhaoTerraHook func(context.Context, boil.ContextExecutor, *TalhaoTerra) error

	talhaoTerraQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	talhaoTerraType                 = reflect.TypeOf(&TalhaoTerra{})
	talhaoTerraMapping              = queries.MakeStructMapping(talhaoTerraType)
	talhaoTerraPrimaryKeyMapping, _ = queries.BindMapping(talhaoTerraType, talhaoTerraMapping, talhaoTerraPrimaryKeyColumns)
	talhaoTerraInsertCacheMut       sync.RWMutex
	talhaoTerraInsertCache          = make(map[string]insertCache)
	talhaoTerraUpdateCacheMut       sync.RWMutex
	talhaoTerraUpdateCache          = make(map[string]updateCache)
	talhaoTerraUpsertCacheMut       sync.RWMutex
	talhaoTerraUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var talhaoTerraAfterSelectMu sync.Mutex
var talhaoTerraAfterSelectHooks []TalhaoTerraHook

var talhaoTerraBeforeInsertMu sync.Mutex
var talhaoTerraBeforeInsertHooks []TalhaoTerraHook
var talhaoTerraAfterInsertMu sync.Mutex
var talhaoTerraAfterInsertHooks []TalhaoTerraHook

var talhaoTerraBeforeUpdateMu sync.Mutex
var talhaoTerraBeforeUpdateHooks []TalhaoTerraHook
var talhaoTerraAfterUpdateMu sync.Mutex
var talhaoTerraAfterUpdateHooks []TalhaoTerraHook

var talhaoTerraBeforeDeleteMu sync.Mutex
var talhaoTerraBeforeDeleteHooks []TalhaoTerraHook
var talhaoTerraAfterDeleteMu sync.Mutex
var talhaoTerraAfterDeleteHooks []TalhaoTerraHook

var talhaoTerraBeforeUpsertMu sync.Mutex
var talhaoTerraBeforeUpsertHooks []TalhaoTerraHook
var talhaoTerraAfterUpsertMu sync.Mutex
var talhaoTerraAfterUpsertHooks []TalhaoTerraHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TalhaoTerra) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TalhaoTerra) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TalhaoTerra) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TalhaoTerra) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TalhaoTerra) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TalhaoTerra) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TalhaoTerra) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TalhaoTerra) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TalhaoTerra) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range talhaoTerraAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTalhaoTerraHook registers your hook function for all future operations.
func AddTalhaoTerraHook(hookPoint boil.HookPoint, talhaoTerraHook TalhaoTerraHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		talhaoTerraAfterSelectMu.Lock()
		talhaoTerraAfterSelectHooks = append(talhaoTerraAfterSelectHooks, talhaoTerraHook)
		talhaoTerraAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		talhaoTerraBeforeInsertMu.Lock()
		talhaoTerraBeforeInsertHooks = append(talhaoTerraBeforeInsertHooks, talhaoTerraHook)
		talhaoTerraBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		talhaoTerraAfterInsertMu.Lock()
		talhaoTerraAfterInsertHooks = append(talhaoTerraAfterInsertHooks, talhaoTerraHook)
		talhaoTerraAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		talhaoTerraBeforeUpdateMu.Lock()
		talhaoTerraBeforeUpdateHooks = append(talhaoTerraBeforeUpdateHooks, talhaoTerraHook)
		talhaoTerraBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		talhaoTerraAfterUpdateMu.Lock()
		talhaoTerraAfterUpdateHooks = append(talhaoTerraAfterUpdateHooks, talhaoTerraHook)
		talhaoTerraAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		talhaoTerraBeforeDeleteMu.Lock()
		talhaoTerraBeforeDeleteHooks = append(talhaoTerraBeforeDeleteHooks, talhaoTerraHook)
		talhaoTerraBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		talhaoTerraAfterDeleteMu.Lock()
		talhaoTerraAfterDeleteHooks = append(talhaoTerraAfterDeleteHooks, talhaoTerraHook)
		talhaoTerraAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		talhaoTerraBeforeUpsertMu.Lock()
		talhaoTerraBeforeUpsertHooks = append(talhaoTerraBeforeUpsertHooks, talhaoTerraHook)
		talhaoTerraBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		talhaoTerraAfterUpsertMu.Lock()
		talhaoTerraAfterUpsertHooks = append(talhaoTerraAfterUpsertHooks, talhaoTerraHook)
		talhaoTerraAfterUpsertMu.Unlock()
	}
}

// One returns a single talhaoTerra record from the query.
func (q talhaoTerraQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TalhaoTerra, error) {
	o := &TalhaoTerra{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for talhao_terra")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TalhaoTerra records from the query.
func (q talhaoTerraQuery) All(ctx context.Context, exec boil.ContextExecutor) (TalhaoTerraSlice, error) {
	var o []*TalhaoTerra

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TalhaoTerra slice")
	}

	if len(talhaoTerraAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TalhaoTerra records in the query.
func (q talhaoTerraQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count talhao_terra rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q talhaoTerraQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if talhao_terra exists")
	}

	return count > 0, nil
}

// TatDocumentoVendas retrieves all the documento_venda's DocumentoVendas with an executor via tat_id column.
func (o *TalhaoTerra) TatDocumentoVendas(mods ...qm.QueryMod) documentoVendaQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`documento_venda`.`tat_id`=?", o.TatID),
	)

	return DocumentoVendas(queryMods...)
}

// TatProdutoEstoques retrieves all the produto_estoque's ProdutoEstoques with an executor via tat_id column.
func (o *TalhaoTerra) TatProdutoEstoques(mods ...qm.QueryMod) produtoEstoqueQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`produto_estoque`.`tat_id`=?", o.TatID),
	)

	return ProdutoEstoques(queryMods...)
}

// TatRequisicaos retrieves all the requisicao's Requisicaos with an executor via tat_id column.
func (o *TalhaoTerra) TatRequisicaos(mods ...qm.QueryMod) requisicaoQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`requisicao`.`tat_id`=?", o.TatID),
	)

	return Requisicaos(queryMods...)
}

// LoadTatDocumentoVendas allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (talhaoTerraL) LoadTatDocumentoVendas(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTalhaoTerra interface{}, mods queries.Applicator) error {
	var slice []*TalhaoTerra
	var object *TalhaoTerra

	if singular {
		var ok bool
		object, ok = maybeTalhaoTerra.(*TalhaoTerra)
		if !ok {
			object = new(TalhaoTerra)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTalhaoTerra)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTalhaoTerra))
			}
		}
	} else {
		s, ok := maybeTalhaoTerra.(*[]*TalhaoTerra)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTalhaoTerra)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTalhaoTerra))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &talhaoTerraR{}
		}
		args[object.TatID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &talhaoTerraR{}
			}
			args[obj.TatID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documento_venda`),
		qm.WhereIn(`documento_venda.tat_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load documento_venda")
	}

	var resultSlice []*DocumentoVenda
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice documento_venda")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on documento_venda")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documento_venda")
	}

	if len(documentoVendaAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TatDocumentoVendas = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentoVendaR{}
			}
			foreign.R.Tat = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.TatID, foreign.TatID) {
				local.R.TatDocumentoVendas = append(local.R.TatDocumentoVendas, foreign)
				if foreign.R == nil {
					foreign.R = &documentoVendaR{}
				}
				foreign.R.Tat = local
				break
			}
		}
	}

	return nil
}

// LoadTatProdutoEstoques allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (talhaoTerraL) LoadTatProdutoEstoques(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTalhaoTerra interface{}, mods queries.Applicator) error {
	var slice []*TalhaoTerra
	var object *TalhaoTerra

	if singular {
		var ok bool
		object, ok = maybeTalhaoTerra.(*TalhaoTerra)
		if !ok {
			object = new(TalhaoTerra)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTalhaoTerra)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTalhaoTerra))
			}
		}
	} else {
		s, ok := maybeTalhaoTerra.(*[]*TalhaoTerra)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTalhaoTerra)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTalhaoTerra))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &talhaoTerraR{}
		}
		args[object.TatID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &talhaoTerraR{}
			}
			args[obj.TatID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`produto_estoque`),
		qm.WhereIn(`produto_estoque.tat_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load produto_estoque")
	}

	var resultSlice []*ProdutoEstoque
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice produto_estoque")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on produto_estoque")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for produto_estoque")
	}

	if len(produtoEstoqueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TatProdutoEstoques = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &produtoEstoqueR{}
			}
			foreign.R.Tat = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.TatID, foreign.TatID) {
				local.R.TatProdutoEstoques = append(local.R.TatProdutoEstoques, foreign)
				if foreign.R == nil {
					foreign.R = &produtoEstoqueR{}
				}
				foreign.R.Tat = local
				break
			}
		}
	}

	return nil
}

// LoadTatRequisicaos allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (talhaoTerraL) LoadTatRequisicaos(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTalhaoTerra interface{}, mods queries.Applicator) error {
	var slice []*TalhaoTerra
	var object *TalhaoTerra

	if singular {
		var ok bool
		object, ok = maybeTalhaoTerra.(*TalhaoTerra)
		if !ok {
			object = new(TalhaoTerra)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTalhaoTerra)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTalhaoTerra))
			}
		}
	} else {
		s, ok := maybeTalhaoTerra.(*[]*TalhaoTerra)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTalhaoTerra)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTalhaoTerra))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &talhaoTerraR{}
		}
		args[object.TatID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &talhaoTerraR{}
			}
			args[obj.TatID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`requisicao`),
		qm.WhereIn(`requisicao.tat_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load requisicao")
	}

	var resultSlice []*Requisicao
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice requisicao")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on requisicao")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for requisicao")
	}

	if len(requisicaoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TatRequisicaos = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &requisicaoR{}
			}
			foreign.R.Tat = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.TatID, foreign.TatID) {
				local.R.TatRequisicaos = append(local.R.TatRequisicaos, foreign)
				if foreign.R == nil {
					foreign.R = &requisicaoR{}
				}
				foreign.R.Tat = local
				break
			}
		}
	}

	return nil
}

// AddTatDocumentoVendas adds the given related objects to the existing relationships
// of the talhao_terra, optionally inserting them as new records.
// Appends related to o.R.TatDocumentoVendas.
// Sets related.R.Tat appropriately.
func (o *TalhaoTerra) AddTatDocumentoVendas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVenda) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.TatID, o.TatID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `documento_venda` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"tat_id"}),
				strmangle.WhereClause("`", "`", 0, documentoVendaPrimaryKeyColumns),
			)
			values := []interface{}{o.TatID, rel.DDVID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.TatID, o.TatID)
		}
	}

	if o.R == nil {
		o.R = &talhaoTerraR{
			TatDocumentoVendas: related,
		}
	} else {
		o.R.TatDocumentoVendas = append(o.R.TatDocumentoVendas, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentoVendaR{
				Tat: o,
			}
		} else {
			rel.R.Tat = o
		}
	}
	return nil
}

// SetTatDocumentoVendas removes all previously related items of the
// talhao_terra replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Tat's TatDocumentoVendas accordingly.
// Replaces o.R.TatDocumentoVendas with related.
// Sets related.R.Tat's TatDocumentoVendas accordingly.
func (o *TalhaoTerra) SetTatDocumentoVendas(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentoVenda) error {
	query := "update `documento_venda` set `tat_id` = null where `tat_id` = ?"
	values := []interface{}{o.TatID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.TatDocumentoVendas {
			queries.SetScanner(&rel.TatID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Tat = nil
		}
		o.R.TatDocumentoVendas = nil
	}

	return o.AddTatDocumentoVendas(ctx, exec, insert, related...)
}

// RemoveTatDocumentoVendas relationships from objects passed in.
// Removes related items from R.TatDocumentoVendas (uses pointer comparison, removal does not keep order)
// Sets related.R.Tat.
func (o *TalhaoTerra) RemoveTatDocumentoVendas(ctx context.Context, exec boil.ContextExecutor, related ...*DocumentoVenda) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.TatID, nil)
		if rel.R != nil {
			rel.R.Tat = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("tat_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.TatDocumentoVendas {
			if rel != ri {
				continue
			}

			ln := len(o.R.TatDocumentoVendas)
			if ln > 1 && i < ln-1 {
				o.R.TatDocumentoVendas[i] = o.R.TatDocumentoVendas[ln-1]
			}
			o.R.TatDocumentoVendas = o.R.TatDocumentoVendas[:ln-1]
			break
		}
	}

	return nil
}

// AddTatProdutoEstoques adds the given related objects to the existing relationships
// of the talhao_terra, optionally inserting them as new records.
// Appends related to o.R.TatProdutoEstoques.
// Sets related.R.Tat appropriately.
func (o *TalhaoTerra) AddTatProdutoEstoques(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoEstoque) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.TatID, o.TatID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `produto_estoque` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"tat_id"}),
				strmangle.WhereClause("`", "`", 0, produtoEstoquePrimaryKeyColumns),
			)
			values := []interface{}{o.TatID, rel.ProestID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.TatID, o.TatID)
		}
	}

	if o.R == nil {
		o.R = &talhaoTerraR{
			TatProdutoEstoques: related,
		}
	} else {
		o.R.TatProdutoEstoques = append(o.R.TatProdutoEstoques, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &produtoEstoqueR{
				Tat: o,
			}
		} else {
			rel.R.Tat = o
		}
	}
	return nil
}

// SetTatProdutoEstoques removes all previously related items of the
// talhao_terra replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Tat's TatProdutoEstoques accordingly.
// Replaces o.R.TatProdutoEstoques with related.
// Sets related.R.Tat's TatProdutoEstoques accordingly.
func (o *TalhaoTerra) SetTatProdutoEstoques(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*ProdutoEstoque) error {
	query := "update `produto_estoque` set `tat_id` = null where `tat_id` = ?"
	values := []interface{}{o.TatID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.TatProdutoEstoques {
			queries.SetScanner(&rel.TatID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Tat = nil
		}
		o.R.TatProdutoEstoques = nil
	}

	return o.AddTatProdutoEstoques(ctx, exec, insert, related...)
}

// RemoveTatProdutoEstoques relationships from objects passed in.
// Removes related items from R.TatProdutoEstoques (uses pointer comparison, removal does not keep order)
// Sets related.R.Tat.
func (o *TalhaoTerra) RemoveTatProdutoEstoques(ctx context.Context, exec boil.ContextExecutor, related ...*ProdutoEstoque) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.TatID, nil)
		if rel.R != nil {
			rel.R.Tat = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("tat_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.TatProdutoEstoques {
			if rel != ri {
				continue
			}

			ln := len(o.R.TatProdutoEstoques)
			if ln > 1 && i < ln-1 {
				o.R.TatProdutoEstoques[i] = o.R.TatProdutoEstoques[ln-1]
			}
			o.R.TatProdutoEstoques = o.R.TatProdutoEstoques[:ln-1]
			break
		}
	}

	return nil
}

// AddTatRequisicaos adds the given related objects to the existing relationships
// of the talhao_terra, optionally inserting them as new records.
// Appends related to o.R.TatRequisicaos.
// Sets related.R.Tat appropriately.
func (o *TalhaoTerra) AddTatRequisicaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Requisicao) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.TatID, o.TatID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `requisicao` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"tat_id"}),
				strmangle.WhereClause("`", "`", 0, requisicaoPrimaryKeyColumns),
			)
			values := []interface{}{o.TatID, rel.ReqID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.TatID, o.TatID)
		}
	}

	if o.R == nil {
		o.R = &talhaoTerraR{
			TatRequisicaos: related,
		}
	} else {
		o.R.TatRequisicaos = append(o.R.TatRequisicaos, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &requisicaoR{
				Tat: o,
			}
		} else {
			rel.R.Tat = o
		}
	}
	return nil
}

// SetTatRequisicaos removes all previously related items of the
// talhao_terra replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Tat's TatRequisicaos accordingly.
// Replaces o.R.TatRequisicaos with related.
// Sets related.R.Tat's TatRequisicaos accordingly.
func (o *TalhaoTerra) SetTatRequisicaos(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Requisicao) error {
	query := "update `requisicao` set `tat_id` = null where `tat_id` = ?"
	values := []interface{}{o.TatID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.TatRequisicaos {
			queries.SetScanner(&rel.TatID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Tat = nil
		}
		o.R.TatRequisicaos = nil
	}

	return o.AddTatRequisicaos(ctx, exec, insert, related...)
}

// RemoveTatRequisicaos relationships from objects passed in.
// Removes related items from R.TatRequisicaos (uses pointer comparison, removal does not keep order)
// Sets related.R.Tat.
func (o *TalhaoTerra) RemoveTatRequisicaos(ctx context.Context, exec boil.ContextExecutor, related ...*Requisicao) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.TatID, nil)
		if rel.R != nil {
			rel.R.Tat = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("tat_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.TatRequisicaos {
			if rel != ri {
				continue
			}

			ln := len(o.R.TatRequisicaos)
			if ln > 1 && i < ln-1 {
				o.R.TatRequisicaos[i] = o.R.TatRequisicaos[ln-1]
			}
			o.R.TatRequisicaos = o.R.TatRequisicaos[:ln-1]
			break
		}
	}

	return nil
}

// TalhaoTerras retrieves all the records using an executor.
func TalhaoTerras(mods ...qm.QueryMod) talhaoTerraQuery {
	mods = append(mods, qm.From("`talhao_terra`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`talhao_terra`.*"})
	}

	return talhaoTerraQuery{q}
}

// FindTalhaoTerra retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTalhaoTerra(ctx context.Context, exec boil.ContextExecutor, tatID int, selectCols ...string) (*TalhaoTerra, error) {
	talhaoTerraObj := &TalhaoTerra{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `talhao_terra` where `tat_id`=?", sel,
	)

	q := queries.Raw(query, tatID)

	err := q.Bind(ctx, exec, talhaoTerraObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from talhao_terra")
	}

	if err = talhaoTerraObj.doAfterSelectHooks(ctx, exec); err != nil {
		return talhaoTerraObj, err
	}

	return talhaoTerraObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TalhaoTerra) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no talhao_terra provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(talhaoTerraColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	talhaoTerraInsertCacheMut.RLock()
	cache, cached := talhaoTerraInsertCache[key]
	talhaoTerraInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			talhaoTerraAllColumns,
			talhaoTerraColumnsWithDefault,
			talhaoTerraColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(talhaoTerraType, talhaoTerraMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(talhaoTerraType, talhaoTerraMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `talhao_terra` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `talhao_terra` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `talhao_terra` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, talhaoTerraPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into talhao_terra")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TatID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for talhao_terra")
	}

CacheNoHooks:
	if !cached {
		talhaoTerraInsertCacheMut.Lock()
		talhaoTerraInsertCache[key] = cache
		talhaoTerraInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TalhaoTerra.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TalhaoTerra) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	talhaoTerraUpdateCacheMut.RLock()
	cache, cached := talhaoTerraUpdateCache[key]
	talhaoTerraUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			talhaoTerraAllColumns,
			talhaoTerraPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update talhao_terra, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `talhao_terra` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, talhaoTerraPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(talhaoTerraType, talhaoTerraMapping, append(wl, talhaoTerraPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update talhao_terra row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for talhao_terra")
	}

	if !cached {
		talhaoTerraUpdateCacheMut.Lock()
		talhaoTerraUpdateCache[key] = cache
		talhaoTerraUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q talhaoTerraQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for talhao_terra")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for talhao_terra")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TalhaoTerraSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), talhaoTerraPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `talhao_terra` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, talhaoTerraPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in talhaoTerra slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all talhaoTerra")
	}
	return rowsAff, nil
}

var mySQLTalhaoTerraUniqueColumns = []string{
	"tat_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TalhaoTerra) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no talhao_terra provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(talhaoTerraColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTalhaoTerraUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	talhaoTerraUpsertCacheMut.RLock()
	cache, cached := talhaoTerraUpsertCache[key]
	talhaoTerraUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			talhaoTerraAllColumns,
			talhaoTerraColumnsWithDefault,
			talhaoTerraColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			talhaoTerraAllColumns,
			talhaoTerraPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert talhao_terra, could not build update column list")
		}

		ret := strmangle.SetComplement(talhaoTerraAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`talhao_terra`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `talhao_terra` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(talhaoTerraType, talhaoTerraMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(talhaoTerraType, talhaoTerraMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for talhao_terra")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(talhaoTerraType, talhaoTerraMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for talhao_terra")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for talhao_terra")
	}

CacheNoHooks:
	if !cached {
		talhaoTerraUpsertCacheMut.Lock()
		talhaoTerraUpsertCache[key] = cache
		talhaoTerraUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TalhaoTerra record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TalhaoTerra) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TalhaoTerra provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), talhaoTerraPrimaryKeyMapping)
	sql := "DELETE FROM `talhao_terra` WHERE `tat_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from talhao_terra")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for talhao_terra")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q talhaoTerraQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no talhaoTerraQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from talhao_terra")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for talhao_terra")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TalhaoTerraSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(talhaoTerraBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), talhaoTerraPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `talhao_terra` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, talhaoTerraPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from talhaoTerra slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for talhao_terra")
	}

	if len(talhaoTerraAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TalhaoTerra) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTalhaoTerra(ctx, exec, o.TatID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TalhaoTerraSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TalhaoTerraSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), talhaoTerraPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `talhao_terra`.* FROM `talhao_terra` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, talhaoTerraPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TalhaoTerraSlice")
	}

	*o = slice

	return nil
}

// TalhaoTerraExists checks if the TalhaoTerra row exists.
func TalhaoTerraExists(ctx context.Context, exec boil.ContextExecutor, tatID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `talhao_terra` where `tat_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, tatID)
	}
	row := exec.QueryRowContext(ctx, sql, tatID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if talhao_terra exists")
	}

	return exists, nil
}

// Exists checks if the TalhaoTerra row exists.
func (o *TalhaoTerra) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TalhaoTerraExists(ctx, exec, o.TatID)
}
