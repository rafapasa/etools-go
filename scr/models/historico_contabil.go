// Code generated by SQLBoiler 4.18.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// HistoricoContabil is an object representing the database table.
type HistoricoContabil struct {
	HisctbID    int    `boil:"hisctb_id" json:"hisctb_id" toml:"hisctb_id" yaml:"hisctb_id"`
	EmfID       int    `boil:"emf_id" json:"emf_id" toml:"emf_id" yaml:"emf_id"`
	HisctbTexto string `boil:"hisctb_texto" json:"hisctb_texto" toml:"hisctb_texto" yaml:"hisctb_texto"`
	// 1 - ativo
	//             2 - inativo
	HisctbSituacao int16 `boil:"hisctb_situacao" json:"hisctb_situacao" toml:"hisctb_situacao" yaml:"hisctb_situacao"`

	R *historicoContabilR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L historicoContabilL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var HistoricoContabilColumns = struct {
	HisctbID       string
	EmfID          string
	HisctbTexto    string
	HisctbSituacao string
}{
	HisctbID:       "hisctb_id",
	EmfID:          "emf_id",
	HisctbTexto:    "hisctb_texto",
	HisctbSituacao: "hisctb_situacao",
}

var HistoricoContabilTableColumns = struct {
	HisctbID       string
	EmfID          string
	HisctbTexto    string
	HisctbSituacao string
}{
	HisctbID:       "historico_contabil.hisctb_id",
	EmfID:          "historico_contabil.emf_id",
	HisctbTexto:    "historico_contabil.hisctb_texto",
	HisctbSituacao: "historico_contabil.hisctb_situacao",
}

// Generated where

var HistoricoContabilWhere = struct {
	HisctbID       whereHelperint
	EmfID          whereHelperint
	HisctbTexto    whereHelperstring
	HisctbSituacao whereHelperint16
}{
	HisctbID:       whereHelperint{field: "`historico_contabil`.`hisctb_id`"},
	EmfID:          whereHelperint{field: "`historico_contabil`.`emf_id`"},
	HisctbTexto:    whereHelperstring{field: "`historico_contabil`.`hisctb_texto`"},
	HisctbSituacao: whereHelperint16{field: "`historico_contabil`.`hisctb_situacao`"},
}

// HistoricoContabilRels is where relationship names are stored.
var HistoricoContabilRels = struct {
	Emf                       string
	HisctbLancamentoContabils string
	HisctbRotinaContabilItens string
}{
	Emf:                       "Emf",
	HisctbLancamentoContabils: "HisctbLancamentoContabils",
	HisctbRotinaContabilItens: "HisctbRotinaContabilItens",
}

// historicoContabilR is where relationships are stored.
type historicoContabilR struct {
	Emf                       *EmpresaFilial          `boil:"Emf" json:"Emf" toml:"Emf" yaml:"Emf"`
	HisctbLancamentoContabils LancamentoContabilSlice `boil:"HisctbLancamentoContabils" json:"HisctbLancamentoContabils" toml:"HisctbLancamentoContabils" yaml:"HisctbLancamentoContabils"`
	HisctbRotinaContabilItens RotinaContabilItenSlice `boil:"HisctbRotinaContabilItens" json:"HisctbRotinaContabilItens" toml:"HisctbRotinaContabilItens" yaml:"HisctbRotinaContabilItens"`
}

// NewStruct creates a new relationship struct
func (*historicoContabilR) NewStruct() *historicoContabilR {
	return &historicoContabilR{}
}

func (r *historicoContabilR) GetEmf() *EmpresaFilial {
	if r == nil {
		return nil
	}
	return r.Emf
}

func (r *historicoContabilR) GetHisctbLancamentoContabils() LancamentoContabilSlice {
	if r == nil {
		return nil
	}
	return r.HisctbLancamentoContabils
}

func (r *historicoContabilR) GetHisctbRotinaContabilItens() RotinaContabilItenSlice {
	if r == nil {
		return nil
	}
	return r.HisctbRotinaContabilItens
}

// historicoContabilL is where Load methods for each relationship are stored.
type historicoContabilL struct{}

var (
	historicoContabilAllColumns            = []string{"hisctb_id", "emf_id", "hisctb_texto", "hisctb_situacao"}
	historicoContabilColumnsWithoutDefault = []string{"hisctb_id", "emf_id", "hisctb_texto", "hisctb_situacao"}
	historicoContabilColumnsWithDefault    = []string{}
	historicoContabilPrimaryKeyColumns     = []string{"hisctb_id"}
	historicoContabilGeneratedColumns      = []string{}
)

type (
	// HistoricoContabilSlice is an alias for a slice of pointers to HistoricoContabil.
	// This should almost always be used instead of []HistoricoContabil.
	HistoricoContabilSlice []*HistoricoContabil
	// HistoricoContabilHook is the signature for custom HistoricoContabil hook methods
	HistoricoContabilHook func(context.Context, boil.ContextExecutor, *HistoricoContabil) error

	historicoContabilQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	historicoContabilType                 = reflect.TypeOf(&HistoricoContabil{})
	historicoContabilMapping              = queries.MakeStructMapping(historicoContabilType)
	historicoContabilPrimaryKeyMapping, _ = queries.BindMapping(historicoContabilType, historicoContabilMapping, historicoContabilPrimaryKeyColumns)
	historicoContabilInsertCacheMut       sync.RWMutex
	historicoContabilInsertCache          = make(map[string]insertCache)
	historicoContabilUpdateCacheMut       sync.RWMutex
	historicoContabilUpdateCache          = make(map[string]updateCache)
	historicoContabilUpsertCacheMut       sync.RWMutex
	historicoContabilUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var historicoContabilAfterSelectMu sync.Mutex
var historicoContabilAfterSelectHooks []HistoricoContabilHook

var historicoContabilBeforeInsertMu sync.Mutex
var historicoContabilBeforeInsertHooks []HistoricoContabilHook
var historicoContabilAfterInsertMu sync.Mutex
var historicoContabilAfterInsertHooks []HistoricoContabilHook

var historicoContabilBeforeUpdateMu sync.Mutex
var historicoContabilBeforeUpdateHooks []HistoricoContabilHook
var historicoContabilAfterUpdateMu sync.Mutex
var historicoContabilAfterUpdateHooks []HistoricoContabilHook

var historicoContabilBeforeDeleteMu sync.Mutex
var historicoContabilBeforeDeleteHooks []HistoricoContabilHook
var historicoContabilAfterDeleteMu sync.Mutex
var historicoContabilAfterDeleteHooks []HistoricoContabilHook

var historicoContabilBeforeUpsertMu sync.Mutex
var historicoContabilBeforeUpsertHooks []HistoricoContabilHook
var historicoContabilAfterUpsertMu sync.Mutex
var historicoContabilAfterUpsertHooks []HistoricoContabilHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *HistoricoContabil) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *HistoricoContabil) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *HistoricoContabil) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *HistoricoContabil) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *HistoricoContabil) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *HistoricoContabil) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *HistoricoContabil) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *HistoricoContabil) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *HistoricoContabil) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range historicoContabilAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddHistoricoContabilHook registers your hook function for all future operations.
func AddHistoricoContabilHook(hookPoint boil.HookPoint, historicoContabilHook HistoricoContabilHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		historicoContabilAfterSelectMu.Lock()
		historicoContabilAfterSelectHooks = append(historicoContabilAfterSelectHooks, historicoContabilHook)
		historicoContabilAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		historicoContabilBeforeInsertMu.Lock()
		historicoContabilBeforeInsertHooks = append(historicoContabilBeforeInsertHooks, historicoContabilHook)
		historicoContabilBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		historicoContabilAfterInsertMu.Lock()
		historicoContabilAfterInsertHooks = append(historicoContabilAfterInsertHooks, historicoContabilHook)
		historicoContabilAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		historicoContabilBeforeUpdateMu.Lock()
		historicoContabilBeforeUpdateHooks = append(historicoContabilBeforeUpdateHooks, historicoContabilHook)
		historicoContabilBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		historicoContabilAfterUpdateMu.Lock()
		historicoContabilAfterUpdateHooks = append(historicoContabilAfterUpdateHooks, historicoContabilHook)
		historicoContabilAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		historicoContabilBeforeDeleteMu.Lock()
		historicoContabilBeforeDeleteHooks = append(historicoContabilBeforeDeleteHooks, historicoContabilHook)
		historicoContabilBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		historicoContabilAfterDeleteMu.Lock()
		historicoContabilAfterDeleteHooks = append(historicoContabilAfterDeleteHooks, historicoContabilHook)
		historicoContabilAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		historicoContabilBeforeUpsertMu.Lock()
		historicoContabilBeforeUpsertHooks = append(historicoContabilBeforeUpsertHooks, historicoContabilHook)
		historicoContabilBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		historicoContabilAfterUpsertMu.Lock()
		historicoContabilAfterUpsertHooks = append(historicoContabilAfterUpsertHooks, historicoContabilHook)
		historicoContabilAfterUpsertMu.Unlock()
	}
}

// One returns a single historicoContabil record from the query.
func (q historicoContabilQuery) One(ctx context.Context, exec boil.ContextExecutor) (*HistoricoContabil, error) {
	o := &HistoricoContabil{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for historico_contabil")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all HistoricoContabil records from the query.
func (q historicoContabilQuery) All(ctx context.Context, exec boil.ContextExecutor) (HistoricoContabilSlice, error) {
	var o []*HistoricoContabil

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to HistoricoContabil slice")
	}

	if len(historicoContabilAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all HistoricoContabil records in the query.
func (q historicoContabilQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count historico_contabil rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q historicoContabilQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if historico_contabil exists")
	}

	return count > 0, nil
}

// Emf pointed to by the foreign key.
func (o *HistoricoContabil) Emf(mods ...qm.QueryMod) empresaFilialQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`emf_id` = ?", o.EmfID),
	}

	queryMods = append(queryMods, mods...)

	return EmpresaFilials(queryMods...)
}

// HisctbLancamentoContabils retrieves all the lancamento_contabil's LancamentoContabils with an executor via hisctb_id column.
func (o *HistoricoContabil) HisctbLancamentoContabils(mods ...qm.QueryMod) lancamentoContabilQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`lancamento_contabil`.`hisctb_id`=?", o.HisctbID),
	)

	return LancamentoContabils(queryMods...)
}

// HisctbRotinaContabilItens retrieves all the rotina_contabil_iten's RotinaContabilItens with an executor via hisctb_id column.
func (o *HistoricoContabil) HisctbRotinaContabilItens(mods ...qm.QueryMod) rotinaContabilItenQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`rotina_contabil_itens`.`hisctb_id`=?", o.HisctbID),
	)

	return RotinaContabilItens(queryMods...)
}

// LoadEmf allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (historicoContabilL) LoadEmf(ctx context.Context, e boil.ContextExecutor, singular bool, maybeHistoricoContabil interface{}, mods queries.Applicator) error {
	var slice []*HistoricoContabil
	var object *HistoricoContabil

	if singular {
		var ok bool
		object, ok = maybeHistoricoContabil.(*HistoricoContabil)
		if !ok {
			object = new(HistoricoContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeHistoricoContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeHistoricoContabil))
			}
		}
	} else {
		s, ok := maybeHistoricoContabil.(*[]*HistoricoContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeHistoricoContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeHistoricoContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &historicoContabilR{}
		}
		args[object.EmfID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &historicoContabilR{}
			}

			args[obj.EmfID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`empresa_filial`),
		qm.WhereIn(`empresa_filial.emf_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load EmpresaFilial")
	}

	var resultSlice []*EmpresaFilial
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice EmpresaFilial")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for empresa_filial")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for empresa_filial")
	}

	if len(empresaFilialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Emf = foreign
		if foreign.R == nil {
			foreign.R = &empresaFilialR{}
		}
		foreign.R.EmfHistoricoContabils = append(foreign.R.EmfHistoricoContabils, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EmfID == foreign.EmfID {
				local.R.Emf = foreign
				if foreign.R == nil {
					foreign.R = &empresaFilialR{}
				}
				foreign.R.EmfHistoricoContabils = append(foreign.R.EmfHistoricoContabils, local)
				break
			}
		}
	}

	return nil
}

// LoadHisctbLancamentoContabils allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (historicoContabilL) LoadHisctbLancamentoContabils(ctx context.Context, e boil.ContextExecutor, singular bool, maybeHistoricoContabil interface{}, mods queries.Applicator) error {
	var slice []*HistoricoContabil
	var object *HistoricoContabil

	if singular {
		var ok bool
		object, ok = maybeHistoricoContabil.(*HistoricoContabil)
		if !ok {
			object = new(HistoricoContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeHistoricoContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeHistoricoContabil))
			}
		}
	} else {
		s, ok := maybeHistoricoContabil.(*[]*HistoricoContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeHistoricoContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeHistoricoContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &historicoContabilR{}
		}
		args[object.HisctbID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &historicoContabilR{}
			}
			args[obj.HisctbID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`lancamento_contabil`),
		qm.WhereIn(`lancamento_contabil.hisctb_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load lancamento_contabil")
	}

	var resultSlice []*LancamentoContabil
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice lancamento_contabil")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on lancamento_contabil")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for lancamento_contabil")
	}

	if len(lancamentoContabilAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.HisctbLancamentoContabils = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &lancamentoContabilR{}
			}
			foreign.R.Hisctb = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.HisctbID, foreign.HisctbID) {
				local.R.HisctbLancamentoContabils = append(local.R.HisctbLancamentoContabils, foreign)
				if foreign.R == nil {
					foreign.R = &lancamentoContabilR{}
				}
				foreign.R.Hisctb = local
				break
			}
		}
	}

	return nil
}

// LoadHisctbRotinaContabilItens allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (historicoContabilL) LoadHisctbRotinaContabilItens(ctx context.Context, e boil.ContextExecutor, singular bool, maybeHistoricoContabil interface{}, mods queries.Applicator) error {
	var slice []*HistoricoContabil
	var object *HistoricoContabil

	if singular {
		var ok bool
		object, ok = maybeHistoricoContabil.(*HistoricoContabil)
		if !ok {
			object = new(HistoricoContabil)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeHistoricoContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeHistoricoContabil))
			}
		}
	} else {
		s, ok := maybeHistoricoContabil.(*[]*HistoricoContabil)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeHistoricoContabil)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeHistoricoContabil))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &historicoContabilR{}
		}
		args[object.HisctbID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &historicoContabilR{}
			}
			args[obj.HisctbID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rotina_contabil_itens`),
		qm.WhereIn(`rotina_contabil_itens.hisctb_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load rotina_contabil_itens")
	}

	var resultSlice []*RotinaContabilIten
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice rotina_contabil_itens")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on rotina_contabil_itens")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rotina_contabil_itens")
	}

	if len(rotinaContabilItenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.HisctbRotinaContabilItens = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &rotinaContabilItenR{}
			}
			foreign.R.Hisctb = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.HisctbID == foreign.HisctbID {
				local.R.HisctbRotinaContabilItens = append(local.R.HisctbRotinaContabilItens, foreign)
				if foreign.R == nil {
					foreign.R = &rotinaContabilItenR{}
				}
				foreign.R.Hisctb = local
				break
			}
		}
	}

	return nil
}

// SetEmf of the historicoContabil to the related item.
// Sets o.R.Emf to related.
// Adds o to related.R.EmfHistoricoContabils.
func (o *HistoricoContabil) SetEmf(ctx context.Context, exec boil.ContextExecutor, insert bool, related *EmpresaFilial) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `historico_contabil` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"emf_id"}),
		strmangle.WhereClause("`", "`", 0, historicoContabilPrimaryKeyColumns),
	)
	values := []interface{}{related.EmfID, o.HisctbID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EmfID = related.EmfID
	if o.R == nil {
		o.R = &historicoContabilR{
			Emf: related,
		}
	} else {
		o.R.Emf = related
	}

	if related.R == nil {
		related.R = &empresaFilialR{
			EmfHistoricoContabils: HistoricoContabilSlice{o},
		}
	} else {
		related.R.EmfHistoricoContabils = append(related.R.EmfHistoricoContabils, o)
	}

	return nil
}

// AddHisctbLancamentoContabils adds the given related objects to the existing relationships
// of the historico_contabil, optionally inserting them as new records.
// Appends related to o.R.HisctbLancamentoContabils.
// Sets related.R.Hisctb appropriately.
func (o *HistoricoContabil) AddHisctbLancamentoContabils(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoContabil) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.HisctbID, o.HisctbID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `lancamento_contabil` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"hisctb_id"}),
				strmangle.WhereClause("`", "`", 0, lancamentoContabilPrimaryKeyColumns),
			)
			values := []interface{}{o.HisctbID, rel.LCTBID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.HisctbID, o.HisctbID)
		}
	}

	if o.R == nil {
		o.R = &historicoContabilR{
			HisctbLancamentoContabils: related,
		}
	} else {
		o.R.HisctbLancamentoContabils = append(o.R.HisctbLancamentoContabils, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &lancamentoContabilR{
				Hisctb: o,
			}
		} else {
			rel.R.Hisctb = o
		}
	}
	return nil
}

// SetHisctbLancamentoContabils removes all previously related items of the
// historico_contabil replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Hisctb's HisctbLancamentoContabils accordingly.
// Replaces o.R.HisctbLancamentoContabils with related.
// Sets related.R.Hisctb's HisctbLancamentoContabils accordingly.
func (o *HistoricoContabil) SetHisctbLancamentoContabils(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*LancamentoContabil) error {
	query := "update `lancamento_contabil` set `hisctb_id` = null where `hisctb_id` = ?"
	values := []interface{}{o.HisctbID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.HisctbLancamentoContabils {
			queries.SetScanner(&rel.HisctbID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Hisctb = nil
		}
		o.R.HisctbLancamentoContabils = nil
	}

	return o.AddHisctbLancamentoContabils(ctx, exec, insert, related...)
}

// RemoveHisctbLancamentoContabils relationships from objects passed in.
// Removes related items from R.HisctbLancamentoContabils (uses pointer comparison, removal does not keep order)
// Sets related.R.Hisctb.
func (o *HistoricoContabil) RemoveHisctbLancamentoContabils(ctx context.Context, exec boil.ContextExecutor, related ...*LancamentoContabil) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.HisctbID, nil)
		if rel.R != nil {
			rel.R.Hisctb = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("hisctb_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.HisctbLancamentoContabils {
			if rel != ri {
				continue
			}

			ln := len(o.R.HisctbLancamentoContabils)
			if ln > 1 && i < ln-1 {
				o.R.HisctbLancamentoContabils[i] = o.R.HisctbLancamentoContabils[ln-1]
			}
			o.R.HisctbLancamentoContabils = o.R.HisctbLancamentoContabils[:ln-1]
			break
		}
	}

	return nil
}

// AddHisctbRotinaContabilItens adds the given related objects to the existing relationships
// of the historico_contabil, optionally inserting them as new records.
// Appends related to o.R.HisctbRotinaContabilItens.
// Sets related.R.Hisctb appropriately.
func (o *HistoricoContabil) AddHisctbRotinaContabilItens(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*RotinaContabilIten) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.HisctbID = o.HisctbID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `rotina_contabil_itens` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"hisctb_id"}),
				strmangle.WhereClause("`", "`", 0, rotinaContabilItenPrimaryKeyColumns),
			)
			values := []interface{}{o.HisctbID, rel.RocID, rel.RociItem}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.HisctbID = o.HisctbID
		}
	}

	if o.R == nil {
		o.R = &historicoContabilR{
			HisctbRotinaContabilItens: related,
		}
	} else {
		o.R.HisctbRotinaContabilItens = append(o.R.HisctbRotinaContabilItens, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &rotinaContabilItenR{
				Hisctb: o,
			}
		} else {
			rel.R.Hisctb = o
		}
	}
	return nil
}

// HistoricoContabils retrieves all the records using an executor.
func HistoricoContabils(mods ...qm.QueryMod) historicoContabilQuery {
	mods = append(mods, qm.From("`historico_contabil`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`historico_contabil`.*"})
	}

	return historicoContabilQuery{q}
}

// FindHistoricoContabil retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindHistoricoContabil(ctx context.Context, exec boil.ContextExecutor, hisctbID int, selectCols ...string) (*HistoricoContabil, error) {
	historicoContabilObj := &HistoricoContabil{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `historico_contabil` where `hisctb_id`=?", sel,
	)

	q := queries.Raw(query, hisctbID)

	err := q.Bind(ctx, exec, historicoContabilObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from historico_contabil")
	}

	if err = historicoContabilObj.doAfterSelectHooks(ctx, exec); err != nil {
		return historicoContabilObj, err
	}

	return historicoContabilObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *HistoricoContabil) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no historico_contabil provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(historicoContabilColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	historicoContabilInsertCacheMut.RLock()
	cache, cached := historicoContabilInsertCache[key]
	historicoContabilInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			historicoContabilAllColumns,
			historicoContabilColumnsWithDefault,
			historicoContabilColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(historicoContabilType, historicoContabilMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(historicoContabilType, historicoContabilMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `historico_contabil` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `historico_contabil` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `historico_contabil` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, historicoContabilPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into historico_contabil")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.HisctbID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for historico_contabil")
	}

CacheNoHooks:
	if !cached {
		historicoContabilInsertCacheMut.Lock()
		historicoContabilInsertCache[key] = cache
		historicoContabilInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the HistoricoContabil.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *HistoricoContabil) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	historicoContabilUpdateCacheMut.RLock()
	cache, cached := historicoContabilUpdateCache[key]
	historicoContabilUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			historicoContabilAllColumns,
			historicoContabilPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update historico_contabil, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `historico_contabil` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, historicoContabilPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(historicoContabilType, historicoContabilMapping, append(wl, historicoContabilPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update historico_contabil row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for historico_contabil")
	}

	if !cached {
		historicoContabilUpdateCacheMut.Lock()
		historicoContabilUpdateCache[key] = cache
		historicoContabilUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q historicoContabilQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for historico_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for historico_contabil")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o HistoricoContabilSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), historicoContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `historico_contabil` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, historicoContabilPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in historicoContabil slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all historicoContabil")
	}
	return rowsAff, nil
}

var mySQLHistoricoContabilUniqueColumns = []string{
	"hisctb_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *HistoricoContabil) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no historico_contabil provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(historicoContabilColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLHistoricoContabilUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	historicoContabilUpsertCacheMut.RLock()
	cache, cached := historicoContabilUpsertCache[key]
	historicoContabilUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			historicoContabilAllColumns,
			historicoContabilColumnsWithDefault,
			historicoContabilColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			historicoContabilAllColumns,
			historicoContabilPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert historico_contabil, could not build update column list")
		}

		ret := strmangle.SetComplement(historicoContabilAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`historico_contabil`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `historico_contabil` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(historicoContabilType, historicoContabilMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(historicoContabilType, historicoContabilMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for historico_contabil")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(historicoContabilType, historicoContabilMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for historico_contabil")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for historico_contabil")
	}

CacheNoHooks:
	if !cached {
		historicoContabilUpsertCacheMut.Lock()
		historicoContabilUpsertCache[key] = cache
		historicoContabilUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single HistoricoContabil record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *HistoricoContabil) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no HistoricoContabil provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), historicoContabilPrimaryKeyMapping)
	sql := "DELETE FROM `historico_contabil` WHERE `hisctb_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from historico_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for historico_contabil")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q historicoContabilQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no historicoContabilQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from historico_contabil")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for historico_contabil")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o HistoricoContabilSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(historicoContabilBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), historicoContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `historico_contabil` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, historicoContabilPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from historicoContabil slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for historico_contabil")
	}

	if len(historicoContabilAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *HistoricoContabil) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindHistoricoContabil(ctx, exec, o.HisctbID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HistoricoContabilSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := HistoricoContabilSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), historicoContabilPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `historico_contabil`.* FROM `historico_contabil` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, historicoContabilPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in HistoricoContabilSlice")
	}

	*o = slice

	return nil
}

// HistoricoContabilExists checks if the HistoricoContabil row exists.
func HistoricoContabilExists(ctx context.Context, exec boil.ContextExecutor, hisctbID int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `historico_contabil` where `hisctb_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, hisctbID)
	}
	row := exec.QueryRowContext(ctx, sql, hisctbID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if historico_contabil exists")
	}

	return exists, nil
}

// Exists checks if the HistoricoContabil row exists.
func (o *HistoricoContabil) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return HistoricoContabilExists(ctx, exec, o.HisctbID)
}
